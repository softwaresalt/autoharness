---
artifact_type: task
created_at: 2026-09-26T07:47:01.6178264Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: v1
    size_source: agent
id: 189.001-T
labels:
    - harness-surface:harness-architect
    - ship-lifecycle
    - unit-b
    - unit-b-entry
parent_id: 189-F
priority: high
references:
    - docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md
    - docs/reviews/2026-09-25-ship-lifecycle-release-units-plan-review.md
status: queued
title: 'Ship lifecycle B4a: observation ledger, recheck and inputs_sha256 digest'
updated_at: 2026-09-26T07:47:08.0003834Z
---



<!-- BEGIN:acceptance-criteria -->
- Digest (inputs_sha256): 64 lowercase hex over a domain-separated canonical preimage of the request, both candidates per record including stable absence, raw selected records, manifest, template and installed observations, normalized declarations and the result projection without the digest itself, in deterministic order; digest stability across runs and platforms (LF bytes); every bound input changes the digest.
- Ledger and recheck: every present or absent candidate and every surface read is ledgered; after the provisional result the ledger is re-observed through the same reader (new claims); a completed recheck that disagrees -> class 1; a mutated record between read and recheck -> INPUT_CHANGED_DURING_RESOLUTION, with both the first and the recheck observations bound on mutation.
- One golden fixture pins the exact inputs_sha256 hex; all fixtures are temporary workspaces and no test reads the real repository surface (IM-10).
- RED record before implementation (IM-13, per the A1 wording): task ID, exact P-004 canonical command, native exit, and each roster test's outcome with its own marker AHLC_B4A_LEDGER_DIGEST:<t> per the Marker Convention (pairwise distinct; a shared marker is refused under FI-9). The golden inputs_sha256 test computes the digest through the implementation, so it reaches a stub and belongs to the roster; it is not a structural pinned-hash test (B1's pinned hash is over a static schema file). Any structural test that reaches no stub is recorded outside the roster, like characterization tests.
- Environment prechecks: git worktree list shows one worktree; git status --porcelain is empty for the task's files before the first write; the canonical command runs from the repository root. Over the estimate by more than 30 minutes -> stop and return to Stage for a split.
- IM-14: no added text claims race, TOCTOU or hardlink-alias resistance.
- IM-15: a test or proof PASS confers no claim authority; ordinary P-001/P-002/P-004/P-020 gates at claim and closure.
- Manifest harvest carry IM-14-F52 (E4): B4a's frozen Files cell omits tests/test_harness_noclaim_audit.py; when B4a adds or changes a line of harness_surfaces.py that is a detector hit (a trigger) or a LEDGER-ed line, B4a adds the audit test to its Files and adds or updates the LEDGER entry (a non-claim confirmed by local review); otherwise the Files budget stands.
- CONST-II-F01 (E2) determination: not applied to B4a's own Verification, because the golden digest test is a roster test, not a structural test. If the IM-14-F52 carry brings tests/test_harness_noclaim_audit.py into B4a's Files, record the pre-implementation run of that structural audit test as gap characterization (Principle II evidence), not as RED.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan task B4a (docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md, blob b7a77c76, section 'B4a: Observation ledger, recheck and digest'; release unit S(B-entry); review authority: manifest E5 Verdict under OP-5).

Files (budget): src/autoharness/harness_surfaces.py; tests/test_harness_surfaces_digest.py (plus tests/test_harness_noclaim_audit.py only under the IM-14-F52 carry).

Change: Ledger every present or absent candidate and every surface read. After the provisional result, re-observe the ledger through the same reader (new claims). A completed recheck that disagrees -> class 1. inputs_sha256: 64 lowercase hex over a domain-separated canonical preimage of the request, both candidates per record including stable absence, raw selected records, manifest, template and installed observations, normalized declarations and the result projection without the digest itself; deterministic order.

Marker: AHLC_B4A_LEDGER_DIGEST (prefix). Trace: PE-DATA-01 to 03, IM-10 (fixtures only), IM-13, IM-14, IM-15. Estimate 85 min; size S; complexity medium.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Label harness-surface:harness-architect (Python production module under src/): roster and RED obligation apply. First task of S(B-entry); it continues B3 (188.003-T, S(B-core)) in the same module, and that order is held by the shipment edge (this shipment is blocked by 194-S), not a task edge. Blocks B4b (B1 to B5 all edit harness_surfaces.py, serial). Supersedes retired 181.015-T (plan Trace From the Retired Tasks; PD-03 splits the retired B4 concern into B4a and B4b). Context, not a B4a carry: templates are not eol=lf pinned (IM-02-F01, deferred to the IM-10 unit); the digest is stated over LF bytes and B's tests use LF fixture bytes.
<!-- END:implementation-notes -->