---
artifact_type: task
created_at: 2026-09-12T22:50:00.1936665Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 165.004-T
id: 165.005-T
labels:
    - engine
    - dag-readiness
    - advisory-semantics
parent_id: 165-F
priority: medium
status: queued
title: 'Engine: dag-readiness alignment via shared closure-evidence helper + advisory labelling'
updated_at: 2026-09-12T22:50:00.1936665Z
---

Turn T4's RED tests GREEN. Align `compute_dag_readiness` (L1968) and `_dag_all_predecessors_finished` (L1873) with the authoritative `pre_claim` model.

HARDENING F3 (ARCHITECTURALLY MANDATORY): the closure-evidence check MUST be implemented via a SHARED helper consumed by BOTH gates — reusing `FilesystemTopologyReaders.closure_complete` (L654) through one common derivation path. A PARALLEL reimplementation is a REVIEW-BLOCKING DEFECT, because independent logic is the root architectural cause of the original divergence and would allow the two gates to re-diverge on a future dimension.

DELIVERABLES:
1. Advisory readiness models the same closure-evidence requirement `pre_claim` applies.
2. Advisory output carries an explicit non-authorizing label.
3. `next_eligible` / cursor output disambiguated so it cannot read as authorization.
4. `pre_claim` remains the SOLE claim authority; `dag-readiness` never authorizes.

Plan-review P3 (advisory): state whether the shared helper lives in `topology.py` or a new module.

SIZE: M (~2h). COMPLEXITY: high (shared-abstraction extraction across two gate paths without changing authority boundaries).
Plan §T5. Depends on T4 (165.004-T).