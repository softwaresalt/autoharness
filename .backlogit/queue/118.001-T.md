---
artifact_type: task
created_at: 2026-08-10T07:08:36.0647872Z
custom_fields:
    complexity: medium
    origin_feature: 117-F
    size: M
    size_ruleset_version: 2h-width-isolated-v1
    size_source: agent
id: 118.001-T
labels:
    - shipment-1
    - P0
    - characterization
    - test-only
    - powershell
    - H1
parent_id: 118-F
priority: critical
references:
    - start.ps1
    - docs/plans/2026-08-09-copilot-cli-supervisor-control-plane-plan.md
status: queued
title: T1 Characterization suite for start.ps1 observable launch contract
updated_at: 2026-08-10T08:09:08.5381665Z
---

SIZE: M. COMPLEXITY: medium. (Two independent axes; also emitted as structured backlog fields.) Shipment 1 (P0). TEST-ONLY, zero production change. Pin the CURRENT observable contract of start.ps1 BEFORE any logic moves to Python (hardening H1: characterize-before-migrate is a hard ordering constraint). Assert the observable contract, not script structure, so the future shim can satisfy the identical suite: (a) .env.local parsing - KEY=VALUE lines only, single matching surrounding quote pair stripped, NO-CLOBBER precedence (an already-set process variable wins); (b) COPILOT_HOME defaults to <root>/.copilot and ENGRAM_DATA_DIR to <root>/.engram, each honoring a pre-set value; (c) GITHUB_TOKEN / GITHUB_PERSONAL_ACCESS_TOKEN resolution via gh auth token, with gh absent or failing being non-fatal; (d) Copilot exe resolution order COPILOT_EXE_PATH -> COPILOT_EXE -> PATH, and hard failure with the current actionable message when unresolvable; (e) --remote appended ONLY when COPILOT_USE_REMOTE is 'true' or '1' AND the operator did not already pass --remote; (f) operator argv forwarded VERBATIM to the child; (g) child exit code propagated unchanged. Mock gh / backlogit / engram / copilot; no real Copilot invocation. ACCEPTANCE: suite passes against today's start.ps1 unmodified, and is later re-run BYTE-IDENTICAL by 120.007-T. Any need to edit an assertion during migration is escalated as an operator product decision, not an implementation detail.