---
artifact_type: feature
created_at: 2026-07-29T13:43:55.7744618Z
custom_fields:
    harness_status: pending
id: 098-F
labels:
    - workflow-policy
    - closure-sequencing
    - compact-context
    - dark-mode
    - dogfood-drift
references:
    - .backlogit/queue/009-DL.md
    - docs/exec-plans/2026-05-09-context-window-compaction-spike.md
    - docs/compound/010-S-session-lifecycle-gates.md
status: queued
title: P-020 Post-Merge Context Compaction — mandatory closure gate + dogfood drift fix
updated_at: 2026-07-29T13:43:55.7744618Z
---



<!-- BEGIN:acceptance-criteria -->
AC1: `templates/policies/workflow-policies.md.tmpl` contains a P-020 policy block (Applies To ship/orchestrator/compact-context; Gate Point = Ship post-merge closure Step 6) with Statement/Precondition/Postcondition/Violation-Action, an explicit relationship to P-001 (closure) and P-017 (dark-mode closure), the invocation-vs-candidate-selection decoupling, non-blocking-failure semantics (skip = violation; failed run = warn+continue), and NO reference to a literal `/compact` command. Amendment log gains a 1.15.0 row.
AC2: `templates/agents/_ship.agent.md.tmpl` Step 6 closure step 8 references P-020, states non-blocking failure handling, and the dark-mode DARK_MODE_COMPLETE summary lists compaction status.
AC3: `templates/agents/_orchestrator.agent.md.tmpl` states required post-merge compaction (P-020) is part of the closure set that blocks next-shipment routing, and DARK_MODE_COMPLETE content includes compaction status.
AC4: `templates/skills/compact-context/SKILL.md.tmpl` When-to-Use cites P-020 as the mandatory per-merge trigger and documents the invocation-mandatory / candidate-threshold-gated distinction.
AC5: `templates/instructions/harness-architecture.instructions.md.tmpl` (Primitive 1 & 10) and `templates/instructions/context-efficiency.instructions.md.tmpl` reference P-020.
AC6: `templates/foundation/AGENTS.md.tmpl` and `templates/foundation/constitution.instructions.md.tmpl` enumerate P-020 where P-016/P-017 are listed.
AC7: Installed dogfood drift fixed — `.github/agents/_ship.agent.md` Step 5 closure has the mandatory compact-context step (currently MISSING) + P-020 + dark-mode compaction status; `.github/agents/_orchestrator.agent.md` mirrors the closure-sequencing note.
AC8: `.github/instructions/harness-architecture.instructions.md` and `AGENTS.md` (quality-gate/policy list) reference P-020.
AC9: `src/autoharness/verify_workspace.py` FOUNDATION_ASSERTIONS gains `ship_post_merge_compaction_gate` (path `.github/agents/_ship.agent.md`, must_contain compact-context markers); `tests/test_verify_workspace.py` covers it; `.autoharness/harness-manifest.yaml` checksums/assertion-text updated for changed installed artifacts.
AC10 (GATE): `autoharness verify` passes and the canonical gate `PYTHONPATH=src python -m unittest discover -s tests` (CI ci.yml:89) is green. Markdown/frontmatter/cross-reference validation passes for all touched templates. No unresolved `{{VARIABLE}}` introduced.
<!-- END:acceptance-criteria -->

<!-- BEGIN:context -->
Discovered current state: the Ship TEMPLATE already mandates compact-context at post-merge closure (step 8 in `templates/agents/_ship.agent.md.tmpl`), but it is only a bare 'Mandatory' bullet — not a first-class policy, not verify-asserted, not drift-detectable. The installed dogfood `.github/agents/_ship.agent.md` Step 5 closure has already LOST that step, proving doc-only guidance is insufficient. P-020 + verify assertion is the remedy, consistent with the P-016/P-017/P-018/P-019 first-class-policy convention and the 010-S session-lifecycle-gate precedent.
<!-- END:context -->

<!-- BEGIN:description -->
Formalize mandatory post-merge context compaction as first-class workflow policy **P-020**, wire it consistently through the Ship/Orchestrator closure sequencing and the compact-context skill, fix the installed dogfood Ship copy that has drifted (its Step 5 closure lost the compact-context step), and add a verify_workspace assertion so the gate is machine-verified and drift-detectable.

ENVIRONMENT-AGNOSTIC CONSTRAINT (core rule): the enforcement is framed around invoking the env-agnostic `compact-context` skill, NEVER the Copilot-CLI-specific `/compact` command. autoharness must work across VS Code+Copilot, Copilot CLI, Codex, Cursor, and Claude Code.

KEY DESIGN: decouple mandatory INVOCATION (guaranteed at the Ship Step 6 post-merge closure boundary = per-merge) from threshold-based CANDIDATE SELECTION (skill-internal, unchanged: threshold_days=14, max_files=40, max_size_kb=500, never compacts active checkpoints). Mandating invocation is NOT mandating heavy compaction — the Tier-1 skill is an idempotent no-op when nothing qualifies, neutralizing the cost/latency objection.

Derived from deliberation 009-DL (Option 3). Source stash 9940C563.
<!-- END:description -->