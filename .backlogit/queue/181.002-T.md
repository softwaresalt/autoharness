---
artifact_type: task
created_at: 2026-09-19T02:53:18.1634306Z
custom_fields:
    complexity: high
    size: L
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 181.001-T
id: 181.002-T
labels:
    - harness-surface:harness-architect
    - ship-lifecycle
    - python
    - cli
parent_id: 181-F
priority: medium
status: queued
title: 'P4 T2 (PREPARE, inert): implement the harness-surface requirement resolver'
updated_at: 2026-09-21T03:01:17.2207247Z
---

P4 T2 (PREPARE, inert). Implement the harness-surface resolver AND its CLI routing. THIS TASK OWNS EXACTLY TWO FILES: src/autoharness/harness_surfaces.py and src/autoharness/cli.py. There is NO unnamed follow-on task for the CLI - the surface the plan activates is owned here. Size L, complexity high.

MODULE CONTRACT. Public keyword-only entry point resolve_harness_surfaces(*, workspace_root: Path, autoharness_home: Path, shipment_id: str) -> HarnessResolution. Public enums HarnessState (HARNESS_READY / NO_HARNESS / UNRESOLVED) and SurfaceStatus (PRESENT / MISSING / STALE / INVALID); frozen dataclasses SurfaceResolution and HarnessResolution with the exact fields listed in the governing plan. THE MODULE IS READ-ONLY: no file write, no directory creation, no environment mutation, no process spawn, no backlog mutation, and no persisted readiness state of any kind.

SOURCES AND CLASSIFICATION. Resolve the backlog root with the EXISTING helper resolve_backlog_root from src/autoharness/backlog_root.py; do not re-implement discovery and do not accept a backlog path argument. The shipment's custom_fields.items is authoritative for membership; exactly one live-or-archive record must exist per ID, and zero, duplicate or unparseable records are UNRESOLVED. Map <id> deterministically to .github/skills/<id>/SKILL.md and require exactly one manifest artifacts entry whose path is that exact path AND whose template is exactly skills/<id>/SKILL.md.tmpl. Apply the plan's classification table verbatim as the SINGLE classification rule; do not restate it in different words.

RENDER, BYTES AND TRUST ROOTS. Render uppercase placeholders only, valued solely from the matched manifest entry's variables_used; an unresolved placeholder value is UNRESOLVED. Canonical bytes are UTF-8 with CRLF and lone CR translated to LF and nothing else normalized. PRESENT requires rendered canonical bytes to equal installed canonical bytes AND the manifest checksum to equal the SHA-256 of the installed canonical bytes. Backlog, manifest and installed paths are canonicalized under workspace_root; templates under autoharness_home; the roots are never substituted for one another.

CONTAINMENT, read fail-closed: lexical rejection first, then component-wise canonical containment, then a no-follow prohibition on symlink / junction / reparse components below either root, then a single opened handle with pre/post identity and size validation. Any race, indeterminate, unreadable or non-regular result is UNRESOLVED, and no read outside a canonical root is ever performed.

CLI ROUTING in src/autoharness/cli.py: `autoharness harness resolve --workspace . --autoharness-home <resolved-or-default> --shipment <id> --json`, deterministic and side-effect free, exit 0 HARNESS_READY, exit 1 NO_HARNESS, exit 2 UNRESOLVED/invalid/unsafe/ambiguous/raced. Result JSON on stdout, diagnostics on stderr, every path root-relative and redacted. NO MCP surface and NO operation-framework registration are added by this task. Blocked by 181.001-T.

REVIEW STATE IS POINTER-ONLY. The verdict manifest docs/reviews/2026-09-18-ship-harness-lifecycle-foundation-plan-review.md is the SOLE AUTHORITY for this unit's review state. This record asserts NO verdict, NO attempt number, NO publication eligibility and NO finding closure, and any such value read from this record is stale by construction. Governing plan docs/plans/2026-09-18-ship-harness-lifecycle-foundation-plan.md (plan_id ship-harness-lifecycle-foundation, plan_role active, revision 10).
