---
artifact_type: feature
created_at: 2026-09-19T02:34:58.2824864Z
custom_fields:
    harness_status: pending
id: 177-F
labels:
    - spike
    - ci-isolation
    - supply-chain
    - precursor
priority: critical
status: queued
title: 'SPIKE: network-denied Linux container isolation for external binary conformance'
updated_at: 2026-09-19T05:09:57.540587Z
---

BOUNDED SPIKE (S2). Determine whether the isolation required to execute an untrusted external release binary is achievable on this repository's CI. SEVEN REQUIRED PROPERTIES, each with an ASSIGNED DETERMINING TASK and a named evidence shape - none is assumed, and none is classified by the authoring task: I1 no credentials of any kind in the job environment (177.004-T, by inspecting the actual job environment and recording variable NAMES ONLY); I2 network egress denied AFTER acquisition completes (177.001-T, after the acquisition observed in 177.005-T); I3 repository absent or mounted read-only (177.001-T); I4 disposable mounts (177.002-T); I5 TOCTOU-resistant handle across verify-execute (177.002-T); I6 hardlink/symlink substitution resistance (177.002-T); I7 a redirect rule derived from OBSERVED acquisition behaviour (observed by 177.005-T, transcribed by 177.003-T) - no assumption-derived redirect policy. SAFETY-FIRST ORDERING IS ENFORCED, NOT PREFERRED: 177.004-T determines I1 FIRST and is a blocks predecessor of BOTH untrusted-acquisition work (177.005-T) and containment-probe work (177.002-T), with 177.001-T inheriting the precedence transitively through 177.005-T. No probe or acquisition job may run in a job environment whose credential absence is unverified. Attempt 02 finding F1 (P1) found the plan's blast radius and H7 resting the probe-safety argument on this ordering while the task table, item_deps and three task records denied it; the ordering is now a blocks edge and every affected record states it. ATTEMPT 03 FINDING K1 (P1) THEN FOUND THE BLOCKS EDGE INSUFFICIENT AS A SAFETY PREDICATE, AND THE GATE IS NOW A VERDICT PREDICATE. A blocks edge clears on PREDECESSOR COMPLETION, and 177.004-T completes on THREE outcomes - DETERMINED/ACHIEVABLE, DETERMINED/NOT ACHIEVABLE (R6), and NOT DETERMINED - FLOOR INVOKED at its 90 min bound (R9) - two of which leave credential absence unverified or affirmatively falsified while still clearing the edge. 177.004-T therefore EMITS a single non-secret machine-readable line to docs/spikes/2026-09-18-conformance-isolation-i1-gate.md: I1_GATE: ACHIEVABLE, I1_GATE: NOT_ACHIEVABLE or I1_GATE: FLOOR_INVOKED. 177.005-T AND 177.002-T READ THAT LINE AS THEIR FIRST ACTION AND FAIL CLOSED. The gate is OPEN only when the file exists, is readable, carries EXACTLY ONE I1_GATE: line and its token is ACHIEVABLE; everything else - missing, unreadable, no line, more than one line, unrecognised token, NOT_ACHIEVABLE, FLOOR_INVOKED - is CLOSED. ON A CLOSED GATE NEITHER TASK RUNS THE UNTRUSTED ACQUISITION OR THE CONTAINMENT PROBE AT ALL; they record I7 and I4/I5/I6 respectively as NOT DETERMINED - FLOOR INVOKED NAMING I1 AS THE BLOCKER, with all three required parts, and 177.001-T records I2 the same way via the acquisition that never ran. I3 IS NOT GATED and is determined normally. THE COMPOSED STATE IS THEN ISOLATION_FLOOR_ONLY - or ISOLATION_CLEANUP_FAILED if cleanup also fails - AND NEVER ISOLATION_CHARACTERIZED, structurally, because the pass state requires all seven properties DETERMINED. The blocks edges remain as ORDERING - they guarantee the gate artifact exists before it is read - but they are NEVER offered as the safety argument (H12, H13, R10). COMMITTED PROBE WORKFLOW HAS A DECLARED LIFECYCLE (attempt 03 finding K2, P2): GitHub executes only workflows that exist on a branch, so the hosted-runner probe workflow IS COMMITTED and is stated as such rather than called a throwaway. 177.004-T CREATES exactly one workflow at .github/workflows/spike-177-isolation-probe.yml in its own commit, workflow_dispatch-only, with a minimal permissions: block and no secrets; all four determining tasks REUSE that single workflow by dispatch input; 177.003-T REMOVES it at spike close in the same commit that lands the findings artifact, and removes it anyway if the spike aborts. Rollback is git revert of the single creation commit. Evidence recorded: creation commit SHA and path, four dispatch run IDs and URLs, removal commit SHA, and a git status --porcelain observation showing the workflow ABSENT from the branch tip - written by 177.003-T as a FIXED EIGHT-LINE CLEANUP_ BLOCK so it is machine-readable rather than prose. 181-S still owns every DURABLE committed CI surface; this one is transient and does not survive its unit. CLEANUP IS AN EXECUTABLE LIMB OF THE FINAL PREDICATE (attempt 04 finding K3, P2): 177.006-T evaluates that block as checks C1-C5 and INDEPENDENTLY RE-OBSERVES THE BRANCH TIP as C6, never trusting the recorded observation; cleanup resolves CLEANUP_PROVEN only when all six hold, and otherwise CLEANUP_FAILED carrying one of WORKFLOW_PRESENT_AT_TIP, TIP_UNOBSERVABLE, EVIDENCE_MISSING, EVIDENCE_MALFORMED or EVIDENCE_INCONSISTENT. Through plan revision 4 the branch-cleanliness expectation was stated only in prose while the composed-state vocabulary, defined exhaustively over the seven-entry coverage ledger, had no token able to express its failure, so an all-DETERMINED ledger on a branch still carrying the probe workflow forced the pass state. COVERAGE AND COMPOSED-STATE GATE (D6): 177.006-T checks structurally that every property has a determining task, resolves every ledger row to DETERMINED, NOT DETERMINED - FLOOR INVOKED, or ABSENT, reads the I1 gate token, resolves cleanup, and emits exactly one of FIVE states in a declared precedence - ISOLATION_NOT_OBSERVED, then ISOLATION_UNDETERMINED, then ISOLATION_CLEANUP_FAILED, then ISOLATION_FLOOR_ONLY, then ISOLATION_CHARACTERIZED - so the resolution is a TOTAL FUNCTION. ISOLATION_CHARACTERIZED (all DETERMINED and CLEANUP_PROVEN) is THE ONLY PASS, and it is STRUCTURALLY UNREACHABLE while the exact probe-workflow path is present at the branch tip or its creation, dispatch or removal evidence is missing, malformed or inconsistent, because the cleanup test sits above it in the precedence order. ISOLATION_FLOOR_ONLY (no ABSENT row, at least one FLOOR INVOKED, CLEANUP_PROVEN) is NOT a pass: the named properties are UNPROVEN, and 181-S may harvest ONLY evidence-and-documentation floor tasks. ISOLATION_CLEANUP_FAILED is NOT a pass and authorizes NOTHING - it expresses EVIDENCE COMPLETE, SPIKE NOT CLOSED, and its remedy is inside the unit: remove the workflow, re-emit the CLEANUP_ block, re-run 177.006-T, which REPLACES the verdict line; unlike ISOLATION_UNDETERMINED it needs no further determining run and no waiver. ISOLATION_UNDETERMINED (any ABSENT row) and ISOLATION_NOT_OBSERVED (no artifact, ledger or verdict line, or more than one COMPOSED_STATE: line) authorize NOTHING. Attempt 02 finding F2 (P2) found ISOLATION_UNDETERMINED asserted to block harvest AND leave the floor standing simultaneously; successor eligibility is now stated per state. FAIL CLOSED: silence never produces eligibility, any cleanup result not affirmatively CLEANUP_PROVEN is CLEANUP_FAILED, and NO state presents the isolation floor as PROVEN. SECURITY MODEL IS A CONSTRAINT, NOT A QUESTION: isolated Linux container, no credentials, restricted and disposable mounts, post-acquisition network denial, containment over verification. A property that proves unachievable moves 181-S to the evidence-and-documentation floor; it never relaxes the requirement. NO production code, NO conformance fixture, and NO execution of the untrusted binary ships from this unit. 002-C stays blocked, outside every manifest, with no dependency edge in either direction, under all five composed states. This spike GATES SAFE_CLOSE conformance execution. Source: 7F9CB5E9. Decision: docs/decisions/2026-09-18-shared-execution-architecture-and-portfolio-reslicing-decision.md (revision 1, D5, D6, D7). Governing plan: docs/plans/2026-09-18-conformance-isolation-spike-plan.md (revision 5, disposition REMEDIATED-PENDING-REVIEW, awaiting independent attempt 05).