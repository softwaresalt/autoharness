---
artifact_type: task
created_at: 2026-09-17T19:58:15.2332712Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 167.001-T
    - 167.010-T
id: 167.004-T
labels:
    - closure
    - cli
    - python
    - validation
parent_id: 167-F
priority: medium
references:
    - docs/plans/2026-09-17-closure-evidence-naming-contract-plan.md
    - docs/decisions/2026-09-17-closure-evidence-naming-contract-deliberation.md
status: queued
title: Add `autoharness gate closure-evidence` write-time validation subcommand
updated_at: 2026-09-17T22:06:40.093858Z
---

PLAN UNIT U4 (plan revision 5). Domain: Python CLI. Posture: test-first.

FILES (2): src/autoharness/cli.py, tests/test_cli_gate_closure_evidence.py (NEW).

PURPOSE
Move closure-evidence failure from a successor's gate back to the author's keyboard, with a write-time validator that accepts an artifact IF AND ONLY IF the read-time consumer would accept it (decision D6).

NORMATIVE SOURCE: plan revision 5, unit U4, and Contract Specification C5 (diagnostic contract).

CHANGES
- New subcommand: autoharness gate closure-evidence --path <file> [--shipment <id>] [--workspace <path>] [--json], added as ONE additional branch in the flat dispatcher at cli.py:369-389, plus its usage/help block alongside the existing pipeline-topology / dag-readiness blocks. --workspace is this command's WORKSPACE-ROOT CONTEXT and is the value passed as 167.010-T's required workspace_root argument whenever the command constructs or validates a closure path.
- Validation, in order:
  1. the filename matches the CANONICAL WRITE PATTERN from 167.001-T — a legacy-named artifact is a WRITE-TIME FAILURE, because recognition is read-side only (D3);
  2. the artifact satisfies the COMPLETE CONSUMER ACCEPTANCE PREDICATE;
  3. the artifact is discoverable for its declared shipment via classify_closure_candidates.
- SINGLE VALIDITY DEFINITION. Step 2 MUST call the existing consumer predicate topology._closure_artifact_complete (and, through it, _closure_conditions_satisfied) DIRECTLY, BY IMPORT. Re-implementing a scalar-enum check here is a DEFECT, not a simplification: such a check would accept a READY_WITH_CONDITIONS artifact with an unsatisfied or absent conditions: block that the consumer then rejects, so the write-time gate would certify an artifact the read-time gate blocks — a second, weaker definition of validity for the same contract. Reuse is IMPORT-ONLY; the predicate is NOT MODIFIED and remains byte-identical.
- DIAGNOSTIC GRANULARITY FOLLOWS C5. The authoritative predicate returns a BOOLEAN — it reports whether an artifact is acceptable, not why it was refused — so a field- or reason-specific frontmatter diagnostic could only come from a second implementation of the validity logic. Therefore:
  * Steps 1 and 3, and an absent or unreadable --path, are CLI-OWNED checks and emit SPECIFIC diagnostics naming the offending filename, shipment, or path.
  * Step 2 emits a GENERIC AUTHORITATIVE-PREDICATE REJECTION: it names the artifact path, names topology._closure_artifact_complete as the deciding authority, and quotes CLOSURE_PREDICATE_REQUIREMENT_DOC. It does NOT assert which field or which condition caused the refusal.
- Exit codes follow the established gate convention: 0 pass, 1 validation failure, 2 invalid input.

SCOPE GUARD
Validation only. This command NEVER writes, renames, or repairs an artifact (D8). It is NOT wired into any pre-existing gate path.

TESTS (4)
1. Canonical filename + acceptable frontmatter passes with exit 0.
2. A legacy filename fails with exit 1 and the canonical pattern quoted.
3. An absent or unparseable path exits 2 naming the path.
4. The --json payload carries a failed_check discriminator with domain {filename, frontmatter_predicate, discoverability, input} together with the path.

The semantic parity battery is 167.011-T, not this task.

ACCEPTANCE
- No independent membership test over closure_status or compaction_status values appears in cli.py.
- The frontmatter rejection message carries no field-level cause.

DEPENDS ON: 167.001-T, 167.010-T.