---
artifact_type: task
created_at: 2026-08-21T03:11:44.3059434Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 138.001-T
labels:
    - bug
    - tests
    - p-021
    - baseline-red
    - atomic
parent_id: 138-F
priority: high
status: queued
title: Resolve 019-DL through a lifecycle-stable resolver in the P-021 contract tests + add drift regression guards
updated_at: 2026-08-21T03:12:30.2535536Z
---

ATOMIC gate-green task. Repair both P-021 contract-test loads of the archived 019-DL deliberation artifact via a shared lifecycle-stable resolver, plus two regression guards.

WHY ATOMIC (do not split): splitting leaves an intermediate commit where one module is repaired and its sibling still raises FileNotFoundError in `setUpClass`, so the suite stays red at that task's completion gate. Ship's per-task green gate requires all three files change together.

FILES (exactly three, all under `tests/`; nothing outside `tests/` may be modified):

1. `tests/test_scope_containment_policy_contract.py` (134.011-T, base module)
   Add a module-level helper resolving a backlog artifact by ID:
   * Resolve the storage root via `autoharness.backlog_root.resolve_backlog_root(_REPO_ROOT, env={})`. Importable because `pyproject.toml` sets `pythonpath = ["src"]`.
   * The explicit `env={}` is MANDATORY (plan amendment A2): `resolve_backlog_root` consults `BACKLOGIT_WORKSPACE_DIR` first and raises `BacklogUnavailableError` when the override names a missing directory. Without isolation, any developer or CI runner with that variable set makes these contract tests fail for a reason unrelated to the code under test - the same nondeterministic-red class this task removes. Mirror the isolation pattern already used in `tests/test_backlog_root.py`.
   * Probe `queue/<artifact_id>.md`, then `archive/<artifact_id>.md`; return the first that exists.
   * On a miss, raise an error naming the artifact ID and EVERY probed candidate path.
   Host chosen because both siblings already import from this module, so the resolver rides the existing one-way allocation edge (011 -> 013 -> 012) and introduces no import cycle. Do NOT modify `OWNED_BEHAVIOURS` or the matrix.

2. `tests/test_scope_containment_boundary_contract.py` (134.013-T)
   * Line 127: replace the hardcoded `repo_root / ".backlogit" / "queue" / "019-DL.md"` construction with a resolver call; import the resolver alongside the constants this module already shares.
   * Lines 56, 69, 76, 214, 291, 329: update stale prose citations of `.backlogit/queue/019-DL.md` to cite 019-DL by artifact ID and note the location is resolved, not hardcoded. Docstring/comment lines only.

3. `tests/test_scope_containment_semantics_contract.py` (134.012-T)
   * Line 137: replace the hardcoded `_REPO_ROOT / ".backlogit" / "queue" / "019-DL.md"` construction with the same resolver call, imported from the base module alongside the constants it already imports.

REGRESSION GUARD 1 - behavioural (primary):
Assert that the resolver locates 019-DL; that it resolves to EXACTLY ONE location (simultaneous presence in both `queue/` and `archive/` signals a half-completed archival and must fail loudly - do not weaken this by reflex); and that the loaded text still contains the `C4 AMENDMENT` and `C5 AMENDMENT` markers, proving content equivalence rather than silent coverage reduction. The failure message must name backlog lifecycle drift explicitly.

REGRESSION GUARD 2 - structural (secondary):
Assert no P-021 contract module constructs a lifecycle-volatile `queue/`-anchored artifact path outside the resolver. Scope the scan to CODE lines of the three named modules so prose citations do not false-positive. The guard MUST exempt the resolver's own definition explicitly and BY NAME (plan amendment A3) so the exemption is visible and cannot silently widen. This guard is what prevents recurrence: it fails at authoring time rather than at the next archival. If it proves brittle it may be NARROWED, but MUST NOT be dropped silently without recording why.

ACCEPTANCE CRITERIA:
1. Both `setUpClass` collapses are eliminated and this change introduces no NEW failure. (Amendment A1: Stage ran no tests and cannot bound other pre-existing causes of red. If further pre-existing failures surface on OTHER surfaces, Ship MUST NOT absorb them - classify under P-021 C1, capture a `DEFERRED SCOPE EXPANSION` entry, and escalate as a shipment-level blocker. That is not a licence to widen this task.)
2. Neither sibling module contains a hardcoded `queue/019-DL.md` path.
3. Both modules load the artifact through the shared resolver.
4. Guard 1 fails, with a lifecycle-drift message, if 019-DL is moved or duplicated across `queue/` and `archive/`.
5. Guard 2 fails if a new lifecycle-volatile path literal is introduced into any of the three modules.
6. No B1-B18 assertion is weakened, removed, skipped, or has its subject changed. The three-way allocation guards (RANGE-COMPLETENESS, pairwise-disjointness, expected-allocation) remain green and untouched.
7. No file outside `tests/` is modified.

EVIDENCE (verified read-only by Stage; Ship must confirm the green transition at execution):
* `git cat-file -e origin/main:.backlogit/queue/019-DL.md` -> exit 128 (absent)
* `git cat-file -e origin/main:.backlogit/archive/019-DL.md` -> exit 0 (present)
* Archived copy retains `C5 AMENDMENT` (line 111) and `C4 AMENDMENT` (line 116)

COORDINATION: `137.003-T` in 145-S also edits `tests/test_scope_containment_boundary_contract.py` (correcting false `stash remove`/`archive` CLI-alias comments). This shipment merges FIRST, so `137.003-T` rebases onto the repaired file. Line ranges are disjoint - trivial textual rebase, no semantic conflict, no re-plan of 145-S.

REFERENCES:
* Stash: 7852CE0D
* Deliberation: `.backlogit/queue/021-DL.md` (021-DL)
* Plan: `docs/plans/2026-08-20-p021-contract-test-deliberation-path-resolution-plan.md`
* Review: `docs/reviews/2026-08-20-p021-contract-test-deliberation-path-resolution-review.md` (PASS)

SIZING: size S, complexity low (recorded via the separate sizing update calls; see the two-axis 2-hour gate).