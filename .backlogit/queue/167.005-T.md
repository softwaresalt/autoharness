---
artifact_type: task
created_at: 2026-09-17T19:58:32.6824601Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 167.001-T
id: 167.005-T
labels:
    - closure
    - skill-template
    - documentation
    - producer-contract
parent_id: 167-F
priority: medium
references:
    - docs/plans/2026-09-17-closure-evidence-naming-contract-plan.md
    - docs/decisions/2026-09-17-closure-evidence-naming-contract-deliberation.md
status: queued
title: Reconcile operational-closure producer spec to the canonical pattern (template + installed mirror, atomic)
updated_at: 2026-09-17T22:12:14.6207492Z
---

PLAN UNIT U5 (plan revision 5). Domain: skill documentation. Posture: migration-first.

FILES (2, BOTH IN ONE COMMIT — ATOMIC):
- templates/skills/operational-closure/SKILL.md.tmpl  (the PRODUCT)
- .github/skills/operational-closure/SKILL.md          (the INSTALLED DOGFOOD MIRROR)

WHY ATOMIC (risk R4, plan decision P-D5)
Direct application of the 165.008-T / 165.010-T precedent: those tasks were MERGED specifically so that template source and installed dogfood mirror land in ONE commit, closing a wrong-contract window. Splitting them here would leave a window in which the documented contract and the dogfooded contract disagree — which is EXACTLY the defect shape under repair.

CHANGES (identical in both files, modulo the {{DOCS_CLOSURE}} / docs/closure substitution)
1. The Output bullet at line 23 changes from `{{DOCS_CLOSURE}}/{YYYY-MM-DD}-{slug}-closure.md` to the CANONICAL pattern. The TEMPLATE keeps its `{{DOCS_CLOSURE}}` placeholder (`{{DOCS_CLOSURE}}/{shipment_id}-{feature_id}-post-merge-closure.md`); the INSTALLED MIRROR keeps the rendered literal (`docs/closure/{shipment_id}-{feature_id}-post-merge-closure.md`). That placeholder-vs-literal difference is correct and is preserved — templates are the product. Both quote closure_contract.CANONICAL_CLOSURE_PATTERN_DOC VERBATIM modulo that one substitution, so 167.009-T's guard can compare prose against the code constant.
2. The Output section gains an explicit REQUIRED FRONTMATTER KEYS statement naming `closure_status` and `compaction_status`, which the shipped producer spec leaves implied by prose alone — the METADATA half of the drift under repair (the 005-S failure shape in the source bug report).
3. A new Required Protocol step mandates invoking `autoharness gate closure-evidence` before the closure artifact is committed.
4. A short note records that the DATE is carried in FRONTMATTER rather than the filename, and that legacy date-prefixed names remain READABLE BUT ARE NEVER WRITTEN (D2/D3) — so the read/write asymmetry cannot be misread as permission.

SCOPE GUARD
No other section of either skill file is edited. No other skill file is touched. No behaviour change — this is documentation reconciliation only.

ACCEPTANCE
- The template retains `{{DOCS_CLOSURE}}` and introduces NO other unresolved placeholder; the installed mirror carries the rendered literal and NO `{{...}}`.
- Both files agree with each other and with CANONICAL_CLOSURE_PATTERN_DOC (asserted by 167.009-T).
- Touches NOTHING under docs/closure/ (D8).

DEPENDS ON: 167.001-T (MERGE-ORDER CONSTRAINT — the documentation MUST NOT land before the constant it quotes exists).