---
artifact_type: shipment
created_at: 2026-08-10T08:13:34.1852841Z
custom_fields:
    items:
        - 120-F
        - 120.001-T
        - 120.002-T
        - 120.003-T
        - 120.004-T
        - 120.005-T
        - 120.006-T
        - 120.007-T
        - 120.008-T
        - 117-F
dependencies:
    - 128-S
id: 129-S
links:
    - target_id: 126-S
      link_type: supersedes
priority: high
status: queued
title: 'S3 Copilot supervisor: application services, adapters, start.ps1/start.sh migration (P1)'
updated_at: 2026-08-10T08:18:56.997922Z
---

Shipment 3 (FINAL) of the strict serial chain `127-S` -> `128-S` -> `129-S` for Plan 1. Supersedes the retired `126-S`. BLOCKED until `128-S` ships.

MANIFEST CONTRACT (H10.5 — fully-covered root covering feature, plus program closure). Lists the covering feature `120-F` FIRST, then all eight of its children (`120.001-T`..`120.008-T`) in dependency order, and finally the childless product umbrella `117-F`.

(i) FULL COVERAGE — `120-F` has exactly these eight children, so `returnUnreleasedFeatureItems(120-F)` returns the empty set at close.
(ii) ROOT PLACEMENT — `120-F` has no parent feature.
(iii) UMBRELLA CLOSURE — `117-F` is a SECOND root feature member. This is safe because it is CHILDLESS: `descendantItems(117-F)` is empty, so `returnUnreleasedFeatureItems(117-F)` returns nothing and `collectArchiveCandidateIDs` sweeps only `117-F` itself. As an explicit member it is marked `done` and archived by the engine at final close. This is precisely what retires the program record with NO post-close operator action, NO `adopt_item`, and NO manual archive step. `117-F` reaches this shipment only through non-hierarchical `related_to` links to `118-F`/`119-F`/`120-F`, which `featureScopeRoots` does not traverse — which is why the two earlier closes never touch it.

`120-F` is listed FIRST so the read-only `covering_feature` render projection resolves to the implementation feature rather than the umbrella.

NO SHIP-SIDE MITIGATION IS REQUIRED OR PERMITTED. CLOSE-PATH CONTRACT: this manifest intentionally lists its covering feature, which is the FULLY-COVERED ROOT exception to the task-only rule in docs/compound/097-S-shipment-task-only-safe-close.md - see the "Reconciliation - the FULLY-COVERED ROOT exception" section there. The task-only rule targets PARTIAL-feature shipments; task-only membership does NOT make this topology safe (ARM A orphaned 14/14 tasks). Close with a single `backlogit shipment ship`. Expected `ShipShipment` result: `returned_ids: []`; `archived_ids` = the eight tasks + `120-F` + `117-F` + `129-S`. After this close the entire Plan-1 program is terminal with zero residue in the queue.

SCOPE: application services (`bootstrap.py`, `sidecar.py`, `resolve.py`, `app.py`, `approvals.py`), the `autoharness run` CLI adapter, the `start.ps1` / `start.sh` (+ `templates/` copies) compatibility-shim migration, and rollout/rollback documentation. This is the ONLY behavior-changing shipment in the chain and therefore ships LAST (P1). `120.007-T` is gated by the `118-F` characterization suites, which must be re-run BYTE-IDENTICAL after migration FOR EVERY UNCHANGED SCENARIO (H1 / H9), subject to exactly TWO approved deltas carved out at `120.007-T` (P1-2 ruling, 2026-08-12): non-fatal PAT-without-`gh`, and POSIX gaining the shared bootstrap policy (`ENGRAM_DATA_DIR` default plus PAT resolution) because `120.001-T` is the single source of truth on every platform.

RESOLUTION - ALL GATES ON THIS SHIPMENT ARE CLOSED (2026-08-11, operator rulings accepted). F16 (RULING 3 - no policy escape hatch, DoD #2 preserved): the AUTOHARNESS_SUPERVISOR=0 escape hatch is WITHDRAWN from 120.007-T and from the 120.008-T runbook, which must not document it as available. The self-contradiction is resolved in favour of DoD #2: rollback is a single-file revert per shim to the git-SHA-preserved pre-migration script and REQUIRES A REDEPLOY, a cost accepted deliberately in exchange for the guarantee that no orchestration policy survives in PowerShell or bash. A test now asserts no environment-variable branch into any legacy path. F20 (RULING 5 - narrow the mutation scope): 120.002-T's blanket 'backlogit and graphtor are not mutated' phrasing, which contradicted its own mandate to run those operations, is withdrawn. The prohibition is now scoped to DOMAIN/AUTHORITY mutation (no artifact/shipment/checkpoint writes, no Engram authority records, no decision authority), while `backlogit sync` and Engram pre-warm/bind are EXPLICITLY PERMITTED as derived-index maintenance that creates no domain facts. F21 (RULING 2 - contracts in the shared supervisor core): the approval request/response contract moved UP into 118.003-T contracts.py, and 120.005-T now implements it and depends on it. THE CONCLUSION PREVIOUSLY DRAWN HERE - "so the approval channel is no longer orderable out of the runtime" - IS WITHDRAWN (P1-C ruling, 2026-08-12). Ruling 2 discharged the definition-ordering half (F19) only; it created no caller and left F21, a RUNTIME WIRING defect, open. F21 IS DISCHARGED BY THE LATER F32/F33 RULING: 120.004-T (single orchestrator) DEPENDS ON AND INVOKES 120.005-T for every gated action and FAILS CLOSED if the approval service is unavailable or denies. Edge direction is 120.004-T -> 120.005-T; the inverse does not exist and must not be added. F25 (RULING 7 - stable CLI option contract): 120.006-T now defines the COMPLETE, STABLE `autoharness run` option contract and is declared the only surface exposing it - including --force-unlock, which is the sole reachable remedy for a stranded lock, and --max-restarts, whose default remains 0. Tests must assert for EVERY option both that it parses AND that it is forwarded, since parsing alone was never evidence of reachability. F26 (RULING 8 - amend P-015): resolved by the new 118.007-T in 127-S, which lands before any close in the chain. NO P0 OR P1 REMAINS OPEN AGAINST 129-S.

GATE STATE REVERTED 2026-08-11: the Cycle-16 GATE-CLEAR declaration is WITHDRAWN. The confirmatory current-HEAD Copilot review of 90a011c5 returned three surviving P1s (F30, F31, F32/F33) - see docs/reviews/2026-08-09-copilot-cli-supervisor-control-plane-review.md. F32/F33 gates this shipment directly: ruling 2 discharged F19 but NOT F21, which was always a runtime WIRING defect - 120.005-T still has ZERO reverse dependencies and 120.004-T (the single orchestrator) does not mention approvals at all, so H2 fail-closed approvals can still be omitted from the shipped runtime. F30 also names this shipment specifically, because 129-S deliberately carries the childless root 117-F. Stage HALTED per the operator's one-bounded-pass standing instruction and attempted no remediation. NOT gate-clear.

GATE STATE 2026-08-11 (Cycle 19, F34 propagation): this shipment carried NO F34 obligation of its own - F34 landed on 127-S in 118.005-T/118.006-T - and it remains GATE-CLEAR. One member is nevertheless touched by the accepted F34 ruling and is recorded here for traceability: 120.006-T (T17 CLI adapter) OWNS the operator-facing --force-unlock remedy, which is genuinely REACHABLE only under the new guard/record contract (a stable never-deleted OS-locked guard file as the sole exclusion primitive, with holder metadata in a separate removable record). Under the now-removed O_CREAT|O_EXCL backend the remedy would have refused unconditionally, so the flag would have been exposed but useless. 120.006-T gains one acceptance criterion: a REFUSED force-unlock must propagate the distinct REFUSED outcome and exit code VERBATIM, never converted into success, generic failure, or a retry - and the adapter must not re-implement or weaken the locking contract (zero policy in the adapter). This adds NO new dependency and does NOT change this shipment's membership or ordering.
GATE STATE 2026-08-11 (Cycle 18): the F30/F31/F32-F33 gate is CLEARED by three final operator rulings, validated structurally (verifier V13 quantified close-path predicate with negative controls; V14 approval-reachability from the runtime chain tail). F32/F33 lands here and is fixed by REVERSING the edge between 120.004-T and 120.005-T: the approvals task previously depended on the orchestrator and had ZERO reverse dependencies, so the runtime chain T15 -> T17 -> T18 -> T19 was satisfiable with approvals never started. 120.004-T now depends on 120.005-T, putting the fail-closed approval path ON the critical path, with omission blocked by a no-default required parameter, an exhaustive gated-action catalog, and mandatory negative controls. F30 also names this shipment: it is admissible only because a childless root is fully covered, and 117-F's childlessness is POSITIVELY VERIFIED rather than assumed. All three findings are RESOLVED; 0 unresolved P0 / 0 unresolved P1. GATE-CLEAR IS NOT AN INSTRUCTION TO CLAIM - claiming remains Ship's decision under Ship's own Role Boundary.
