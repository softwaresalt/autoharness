---
artifact_type: shipment
created_at: 2026-08-10T08:13:34.1852841Z
custom_fields:
    items:
        - 120-F
        - 120.001-T
        - 120.002-T
        - 120.003-T
        - 120.004-T
        - 120.005-T
        - 120.006-T
        - 120.007-T
        - 120.008-T
        - 117-F
dependencies:
    - 128-S
id: 129-S
links:
    - target_id: 126-S
      link_type: supersedes
priority: high
status: queued
title: 'S3 Copilot supervisor: application services, adapters, start.ps1/start.sh migration (P1)'
updated_at: 2026-08-10T08:18:56.997922Z
---

Shipment 3 (FINAL) of the strict serial chain `127-S` -> `128-S` -> `129-S` for Plan 1. Supersedes the retired `126-S`. BLOCKED until `128-S` ships.

*** ADDITIONAL GATES — OPEN P1s F16, F20, F21 AND F25 (2026-08-10). *** Open P1 **F25** ALSO gates this shipment: `120.006-T` is the ONLY task touching the CLI and it never enumerates the `autoharness run` option contract, while `--force-unlock` is owned by `118.005-T` (`locking.py`) and `--max-restarts N`/resume by `119.006-T` (`recovery.py`) — neither of which touches `cli.py`. Every task can therefore pass its DoD with those controls UNREACHABLE FROM THE SOLE PUBLIC SURFACE. This is structurally identical to F21 and COMPOUNDS F22: `--force-unlock` is the documented remedy for the very lockout F22 creates, so resolving F22 without F25 leaves the lockout with no reachable exit. Resolution: define the complete option contract here with forwarding tests, preserving the no-policy-in-the-adapter rule. Stage adopted no resolution and changed no dependency edge. Open P1 F20 ALSO gates this shipment: 120.002-T requires a backlogit/Engram sync pre-warm while simultaneously asserting the service does not mutate backlogit or Engram (plan section 3.1 Must NOT forbids a sidecar store), which no implementation can honour literally.  The predecessor is NOT the only gate on this shipment. Open P1 **F16** must also be dispositioned before `129-S` is claimed: `120.007-T` mandates that NO legacy shell policy survive in the shim (also hard DoD #2) while the SAME task mandates an `AUTOHARNESS_SUPERVISOR=0` escape hatch that executes the legacy inline path WITHOUT a redeploy. A runtime branch into the legacy path requires that path to be present in the shipped shim — exactly the duplication both the task and the DoD forbid; a git SHA is documentation and cannot supply a runtime branch. The contradiction originates upstream in the reviewed plan (§9 rollback bullet, §10 T18) and hardening (H8 T18 row, H10 S3), and propagates into `120.008-T`. Requires an operator product decision (Option A drop the escape hatch, vs Option B retain a versioned legacy implementation and relax DoD #2). Open P1 **F21** is a THIRD gate on this shipment: the fail-closed approval channel can be omitted from the shipped runtime without any task failing. Verified against the live graph - `120.005-T` (T16, `approvals.py`) DEPENDS ON `120.004-T` (T15, `run_session()`) and has ZERO reverse dependencies, so the runtime chain `120.004-T` -> `120.006-T` -> `120.007-T` -> `120.008-T` is fully satisfiable with `120.005-T` never started - yet T15 is specified as THE SINGLE ORCHESTRATOR and plan section 3.6 places the approval exchange inside supervisor runtime behaviour. The H2 fail-closed guarantee (non-interactive approvals resolve to a declared safe default or REFUSED, never silent auto-approval) is therefore a SAFETY control this ordering permits to be dropped silently. Requires an operator product decision (A: split the approval contract ahead of T15; B: make T15 depend on T16 and move the integration tests into T15's DoD; C: attach an explicit wiring obligation plus test to a task already on the runtime chain). Option A matches the direction recorded for F19. Stage changed no dependency edge. See `docs/reviews/2026-08-09-copilot-cli-supervisor-control-plane-review.md` Cycle 4.

MANIFEST CONTRACT (H10.5 — fully-covered root covering feature, plus program closure). Lists the covering feature `120-F` FIRST, then all eight of its children (`120.001-T`..`120.008-T`) in dependency order, and finally the childless product umbrella `117-F`.

(i) FULL COVERAGE — `120-F` has exactly these eight children, so `returnUnreleasedFeatureItems(120-F)` returns the empty set at close.
(ii) ROOT PLACEMENT — `120-F` has no parent feature.
(iii) UMBRELLA CLOSURE — `117-F` is a SECOND root feature member. This is safe because it is CHILDLESS: `descendantItems(117-F)` is empty, so `returnUnreleasedFeatureItems(117-F)` returns nothing and `collectArchiveCandidateIDs` sweeps only `117-F` itself. As an explicit member it is marked `done` and archived by the engine at final close. This is precisely what retires the program record with NO post-close operator action, NO `adopt_item`, and NO manual archive step. `117-F` reaches this shipment only through non-hierarchical `related_to` links to `118-F`/`119-F`/`120-F`, which `featureScopeRoots` does not traverse — which is why the two earlier closes never touch it.

`120-F` is listed FIRST so the read-only `covering_feature` render projection resolves to the implementation feature rather than the umbrella.

NO SHIP-SIDE MITIGATION IS REQUIRED OR PERMITTED. CLOSE-PATH CONTRACT: this manifest intentionally lists its covering feature, which is the FULLY-COVERED ROOT exception to the task-only rule in docs/compound/097-S-shipment-task-only-safe-close.md - see the "Reconciliation - the FULLY-COVERED ROOT exception" section there. The task-only rule targets PARTIAL-feature shipments; task-only membership does NOT make this topology safe (ARM A orphaned 14/14 tasks). Close with a single `backlogit shipment ship`. Expected `ShipShipment` result: `returned_ids: []`; `archived_ids` = the eight tasks + `120-F` + `117-F` + `129-S`. After this close the entire Plan-1 program is terminal with zero residue in the queue.

SCOPE: application services (`bootstrap.py`, `sidecar.py`, `resolve.py`, `app.py`, `approvals.py`), the `autoharness run` CLI adapter, the `start.ps1` / `start.sh` (+ `templates/` copies) compatibility-shim migration, and rollout/rollback documentation. This is the ONLY behavior-changing shipment in the chain and therefore ships LAST (P1). `120.007-T` is gated by the `118-F` characterization suites, which must be re-run BYTE-IDENTICAL after migration (H1 / H9).

*** ADDITIONAL GATE - OPEN P1 F26 (2026-08-11), WHICH GATES ALL THREE SHIPMENTS. *** The close-path contract in docs/compound/097-S-shipment-task-only-safe-close.md instructs Ship to close this shipment with a single `backlogit shipment ship`, but .github/agents/_ship.agent.md prohibits that operation UNCONDITIONALLY ("NEVER the cascade backlogit_ship_shipment, P-015" / "Do NOT call backlogit shipment ship"), with no fully-covered-root carve-out; P-015 in templates/policies/workflow-policies.md.tmpl scopes its Applies-when to partial-feature shipments but states its prohibition and postcondition absolutely. Under fail-closed P-010/P-015 the instructed close command is UNEXECUTABLE, and a Stage planning artifact cannot grant Ship an exemption from Ship's own operative prohibition. THE MANIFEST SHAPE IS NOT AFFECTED: under safe-close this fully-covered-root manifest is still correct, because the covering feature is itself a manifest item and no unshipped siblings exist, so the protected set is empty and safe-close archives exactly the release unit. What is contested is the CLOSE COMMAND, and with it the scope of the 64/64 evidence, which proves cascade safety rather than safe-close correctness. OPERATOR RULING REQUIRED: either amend P-015 + the Ship agent + shipment-reconcile coherently for a verified fully-covered-root exception, or keep safe-close and revise the compound contract and its expected evidence (returned_ids: [] would no longer be the artifact to expect). Stage adopted NO resolution. See docs/reviews/2026-08-09-copilot-cli-supervisor-control-plane-review.md.
