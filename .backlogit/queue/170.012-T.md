---
artifact_type: task
created_at: 2026-09-19T00:35:58.2833614Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 170.004-T
id: 170.012-T
labels:
    - test
    - tdd-red
    - branch-resolution
    - ship-consumption
parent_id: 170-F
priority: high
status: queued
title: 'Branch T8a (RED): end-to-end Ship-consumption fixture for selected_branch, observed failing'
updated_at: 2026-09-19T00:35:58.2833614Z
---

TDD RED PHASE. Author the FAILING end-to-end Ship-consumption fixture and OBSERVE IT FAILING. FIXTURE SHAPE: a shipment whose explicit custom_fields.implementation_branch differs from EVERY title-derived alias the old derivation would produce, so a surviving title-derived expression cannot accidentally match. ASSERTIONS: (1) Ship COMPARES against exactly the gate-emitted selected_branch; (2) Ship CREATES exactly the gate-emitted selected_branch - both the comparison site and the creation site consume the same value, because a Ship that compares correctly but creates a title-derived name still lands on the wrong branch; (3) NO title-derived branch expression survives in EITHER copy, template or installed mirror; (4) a BLOCKED gate produces a HALT with NO derived fallback - Ship never invents a branch name when the gate refused to emit one; (5) a leading-hyphen-adjacent selected_branch value survives the `--`-terminated fixed-argv boundary intact, so an option-like branch name is passed as data and never parsed as a flag. IMPORT SAFETY (BINDING): the module imports cleanly under unittest.defaultTestLoader with ZERO loader.errors; not-yet-existing surfaces are read inside the test body, never at module import time, because an import failure yields a missing observation rather than valid red. Blocked by 170.004-T (T5, the emission of selected_branch this fixture consumes). Does NOT depend on the Ship template or mirror tasks: red precedes implementation. Scope: tests/. Stash 86498B64 + 14F4D6F3. Plan docs/plans/2026-09-17-workspace-authoritative-branch-resolution-plan.md (plan_id workspace-authoritative-branch-resolution, plan_role active, revision 7). Verdict manifest docs/reviews/2026-09-17-workspace-authoritative-branch-resolution-plan-review.md (latest_attempt 07, verdict REMEDIATED-PENDING-REVIEW at plan revision 7, awaiting independent attempt 08).