---
artifact_type: task
created_at: 2026-09-19T02:52:52.0671143Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 181.001-T
labels:
    - harness-surface:harness-architect
    - ship-lifecycle
    - red
    - p-004
parent_id: 181-F
priority: medium
status: queued
title: 'P4 T1 (RED): harness-generation lifecycle state contract tests observed failing'
updated_at: 2026-09-21T03:01:03.9490183Z
---

P4 T1 (RED). Author the failing contract tests for the harness-surface resolver and its CLI, in exactly two modules: tests/test_harness_surfaces.py and tests/test_harness_resolve_cli.py. Size M, complexity high.

THE TESTS MUST BE IMPORT-SAFE ASSERTION RED. src/autoharness/harness_surfaces.py and the CLI entry point must be importable, and every failure must be an ASSERTION failure about behaviour - never ImportError, ModuleNotFoundError, AttributeError on a missing symbol, or a collection error. A RED phase that fails at import proves nothing about the contract and does not satisfy this task.

COVERAGE. (a) Resolver: every row of the governing plan's classification table - zero / multiple / partial manifest matches are UNRESOLVED; entry present with missing installed file or missing template is NO_HARNESS; parity failure is NO_HARNESS/STALE; full parity is HARNESS_READY. (b) Declaration grammar: one or more unique harness-surface:<id> labels, or exactly harness-surface:none, never mixed, never duplicated, ID regex enforced, and absence of any harness-surface label is UNRESOLVED. (c) Containment: absolute, drive, UNC, environment-variable, home-expansion and .. declarations rejected lexically before any filesystem access; sibling-prefix escape rejected by component-wise containment; symlink / junction / reparse components below either canonical root rejected; cross-root substitution rejected. (d) Race: pre/post identity and size mismatch on the single opened handle yields UNRESOLVED with no read outside a canonical root. (e) JSON: schema_version, stable enum strings, stable ordering, root-relative redacted paths, closed reason-code set, and inputs_sha256 emitted on every outcome where its inputs were readable. (f) CLI exit codes 0 / 1 / 2.

TARGETED RED COMMAND: `PYTHONPATH=src python -m unittest tests.test_harness_surfaces tests.test_harness_resolve_cli`. Blocked by nothing; first task in the chain.

REVIEW STATE IS POINTER-ONLY. The verdict manifest docs/reviews/2026-09-18-ship-harness-lifecycle-foundation-plan-review.md is the SOLE AUTHORITY for this unit's review state. This record asserts NO verdict, NO attempt number, NO publication eligibility and NO finding closure, and any such value read from this record is stale by construction. Governing plan docs/plans/2026-09-18-ship-harness-lifecycle-foundation-plan.md (plan_id ship-harness-lifecycle-foundation, plan_role active, revision 10).
