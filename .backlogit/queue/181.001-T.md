---
artifact_type: task
created_at: 2026-09-19 02:52:52.067114+00:00
custom_fields:
  complexity: high
  size: M
  size_ruleset_version: 2h-rule-v1
  size_source: agent
dependencies: []
id: 181.001-T
labels:
- harness-surface:harness-architect
- ship-lifecycle
- red
- p-004
parent_id: 181-F
priority: medium
status: queued
title: 'P4 T1 (RED): resolver, secure-read and CLI contract tests observed failing as assertions'
updated_at: 2026-09-21 03:01:03.949018+00:00
---

P4 T1 (RED). Author the failing contract tests in exactly three modules: tests/test_harness_read.py, tests/test_harness_surfaces.py, tests/test_harness_resolve_cli.py. Size M, complexity high. First task in the chain; blocked by nothing.

NO EXTERNAL FIXTURE DEPENDENCY. Every fixture this task needs is created PROGRAMMATICALLY inside the test modules, in tempfile.TemporaryDirectory, by local helper functions. This task reads NOTHING from tests/fixtures/, so it does not depend on 181.003-T, which runs later.

IMPORTS ARE LAZY AND LOCAL TO EACH TEST METHOD, never at module scope. A helper wraps importlib.import_module and converts ImportError or ModuleNotFoundError into `raise AssertionError(...) from None`. An absent module therefore produces an ASSERTION FAILURE INSIDE A RUNNING TEST, not a collection-time import error that aborts discovery. The suite must still collect cleanly with none of the three production modules present.

COVERAGE. (a) Secure-read primitive: the full lexical rejection list - absolute, drive letter, UNC and extended prefixes, environment-variable syntax, home expansion, .. component, empty or . component, NUL and control characters, reserved Windows device names - all rejected BEFORE any filesystem call; component-wise containment including the sibling-prefix case (/ws-evil is not inside /ws); reparse component rejection; non-regular file; pre/post identity and size mismatch; every bound in the bounds table; and PLATFORM_INVARIANT_UNAVAILABLE. (b) BOTH PLATFORM BRANCHES on every platform, through a deterministic injected mock adapter returning scripted results for the POSIX and Windows paths including reparse, identity-mismatch and invariant-unavailable cases - these tests are NOT skippable. A real-filesystem symlink/junction test is additional and MAY skip where the OS forbids link creation without elevation. (c) Resolver: every row of the governing plan's classification table and every row of the A1-A6 aggregation table, including A4 NO_SURFACES_REQUIRED with empty surfaces and exit 0, and the dominance of A2/A3 over A4. (d) Declaration grammar: the total function over all seven rows, with absence yielding INVALID/DECLARATION_ABSENT. (e) Every reason code in the closed set is reachable and is emitted by exactly the condition the plan assigns it. (f) Frozen env snapshot: os.environ is read once; BACKLOGIT_WORKSPACE_DIR outside the workspace yields BACKLOG_ROOT_OUTSIDE_WORKSPACE; the snapshot is bound into inputs_sha256. (g) JSON: every field present on every outcome, null never omitted, tuples emitted as arrays, stable ordering, root-relative redaction with no absolute path or machine name, structured Diagnostic with no raw exception text. (h) inputs_sha256 determinism, domain separation, absence sentinels, and SELF-EXCLUSION. (i) CLI exit codes 0/1/2 equal to the emitted exit_code field.

TARGETED RED COMMAND: `PYTHONPATH=src python -m unittest tests.test_harness_read tests.test_harness_surfaces tests.test_harness_resolve_cli`

REVIEW STATE IS POINTER-ONLY. The verdict manifest docs/reviews/2026-09-18-ship-harness-lifecycle-foundation-plan-review.md is the SOLE AUTHORITY for this unit's review state. This record asserts NO verdict, NO attempt number, NO publication eligibility and NO finding closure. Governing plan docs/plans/2026-09-18-ship-harness-lifecycle-foundation-plan.md (plan_id ship-harness-lifecycle-foundation, plan_role active, revision 11). Governing decision docs/decisions/2026-09-18-shared-execution-architecture-and-portfolio-reslicing-decision.md (revision 8). Source stash 76EBDE6D.
