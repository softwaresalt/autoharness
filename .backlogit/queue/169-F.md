---
artifact_type: feature
created_at: 2026-09-18T04:48:27.3902511Z
custom_fields:
    harness_status: pending
id: 169-F
labels:
    - policy
    - shipment-claim
    - contract-drift
    - p-002.7
priority: high
status: queued
title: Canonical post-claim member-status contract (P-002.7) with cross-surface structural evidence
updated_at: 2026-09-19T19:05:06.9501137Z
---

RELEASE UNIT: make the post-claim member-status contract explicit and testable. backlogit's claim operation admits shipment members by a rule this repository has never written down, so agents infer it. This unit writes the canonical vocabulary, its THREE claim-to-admission transition rows, an observed-version attribution, and a bidirectional cross-reference to the Ship intake-reconciliation note - into FOUR DECLARED SURFACES, and proves each assertion red before it is allowed to be green. FOUR DECLARED SURFACES, produced by an enumeration RULE rather than a hand list (decision F7): templates/policies/workflow-policies.md.tmpl, .github/policies/workflow-policies.md, templates/agents/_ship.agent.md.tmpl, .github/agents/_ship.agent.md. The rule excludes .autoharness/staging/, which is gitignored at .gitignore:6 as generated verify-workspace output and is not a mirror, and docs/, which narrates the contract without declaring it. Current marker count is ZERO; the contract does not yet exist anywhere. PHASE SHAPE - THIS UNIT'S ORDER IS PREPARE, RED, VERIFY, ACTIVATE, CONFIRM, DOCS, AND IT CONFORMS TO DECISION D2's BINDING ROLLOUT INVARIANT PREPARE THEN VERIFY THEN ACTIVATE. Attempt-04 finding M1 (P1) recorded that the revision-4 order PREPARE, RED, ACTIVATE, VERIFY, DOCS contradicted D2: it placed the unit's ONLY gate emitter AFTER the single irreversible ACTIVATE commit, so the evidence D2 requires BEFORE activation was produced after it, and a non-pass verdict was reachable only by reverting a landed commit. THE MAPPING ONTO D2 IS EXPLICIT: this unit's PREPARE and RED together are D2's PREPARE, whose own rule is that tests go RED first then GREEN entirely within the inert surface; this unit's VERIFY is D2's VERIFY, producing the complete evidence set before any activation and changing no behaviour; this unit's ACTIVATE is D2's ACTIVATE, one task and one commit. CONFIRM and DOCS are post-activation steps that D2 neither names nor forbids. D2's third evidence limb, compatibility against the D3 pinned-fixture corpus, is INAPPLICABLE here and is recorded as such rather than skipped: this unit introduces no normalizer and reads no historical record corpus. 169.011-T PREPARE authors the canonical definition, the enumeration rule and the near-miss fixtures as INERT test-owned data at TWO NAMED HELPER PATHS, tests/p002_7_candidate_definition.py and tests/p002_7_near_miss_fixtures.py, which are named rather than left to the executing agent because the revision-6 freshness binding digests a FULLY ENUMERATED input list and an unnamed artifact cannot appear in one; neither matches unittest discover's default test*.py pattern, so neither adds a collected test module, and neither lies inside the enumeration rule's search scope, so neither can ever be counted as a fifth declared surface. Five RED tasks then prove every assertion family failing - 169.009-T transition rows, 169.010-T bidirectional wiring, 169.012-T mirror divergence, 169.013-T version attribution, 169.014-T negative rows and four-surface closure. 169.017-T VERIFY is the PRE-ACTIVATION READINESS GATE: it evaluates the complete evidence set - absence RED, discriminating RED and INERT GREEN against 169.011-T's candidate definition - while every declared surface is still unmutated, and emits the pre-activation readiness verdict. 169.015-T is ONE ACTIVATE TASK PRODUCING ONE COMMIT across all four surfaces, and it READS 169.017-T's verdict AS ITS FIRST ACTION AND FAILS CLOSED. 169.016-T CONFIRM runs after activation to confirm installed/template parity and active-consumer behaviour, adds no assertion, and emits the post-activation status-contract verdict. 169.007-T DOCS documents the contract, both verdicts and their non-conflation - AND IT TOO READS 169.016-T's VERDICT AS ITS FIRST ACTION AND FAILS CLOSED. BOTH VERDICT-CROSSING EDGES ARE AUTHORIZED BY A TOKEN, NOT BY AN EDGE. A blocks edge clears on PREDECESSOR COMPLETION, and BOTH verdict emitters complete on ALL THREE of their tokens, two of which are non-authorizing, so on BOTH edges - 169.015-T from 169.017-T, and 169.007-T from 169.016-T - the edge supplies ORDERING ONLY, guaranteeing the artifact EXISTS before it is read, and the TOKEN PLUS ITS FRESHNESS BINDING supplies the AUTHORIZATION. This is the same gate-predicate shape attempt 03 finding K1 (P1) required of 177-F's I1 gate. Through revision 5 only the ACTIVATE edge was treated this way while the plan asserted in FOUR places that DOCS must not proceed on a non-pass confirmation; attempt-05 finding N1 (P2) recorded that gap, and revision 6 closes it by giving 169.007-T the same first-action fail-closed read. TWO VERDICTS, NEVER CONFLATED: 169.017-T writes PREACTIVATION_STATE: lines carrying PREACTIVATION_READY, PREACTIVATION_BLOCKED or PREACTIVATION_NOT_OBSERVED to .autoharness/gates/p002-7-preactivation-readiness.txt, and PREACTIVATION_READY is the ONLY token that authorizes activation; 169.016-T writes COMPOSED_STATE: lines carrying the STATUS_CONTRACT_* tokens to .autoharness/gates/p002-7-status-contract-verdict.txt, and STATUS_CONTRACT_HELD is the ONLY token that authorizes documentation. Different paths, different line prefixes, DISJOINT vocabularies, and neither emitter reads or writes the other's path; a foreign token can never open either gate. THE POST-ACTIVATION VERDICT IS A CONFIRMATION, NOT THE EVIDENCE GATE THAT AUTHORIZED ACTIVATION, and on STATUS_CONTRACT_DIVERGENT or STATUS_CONTRACT_NOT_OBSERVED the unit HALTS, DOCS DOES NOT PROCEED AND THAT HALT IS MECHANICAL - 169.007-T's own read finds a token that is not the literal STATUS_CONTRACT_HELD, so it touches no documentation file, makes no commit and exits 1 - the single activation commit is REVERTED AS A UNIT, and the unit returns to Stage. NEITHER GATE ARTIFACT IS A DECLARED SURFACE: both sit under .autoharness/gates/, outside the enumeration rule's search scope, so declared_surface_count stays fixed at 4. THE FRESHNESS BINDING (attempt-05 finding N2, P2, which recorded that all three readiness line forms carried a checked= vintage named in R12 as the stale-verdict mitigation while 169.015-T's declared conditions NEVER READ IT). Revision 6 makes the vintage a PREDICATE and binds each verdict to the IMMUTABLE IDENTITY of the evidence it was computed from, using ONE set of shared primitives instantiated twice: CCD/v1, a canonical content digest over a fully enumerated ordered list of repository-relative POSIX paths, normalizing CRLF to LF only, lowercase-hex sha256 per file into a <path> LF <file-digest> LF UTF-8 preimage - SEVEN inputs for the readiness candidate_digest (the four declared surfaces plus the two helper modules and tests/test_p002_7_member_status_contract.py) and FOUR for the confirmation surface_digest (the declared surfaces alone, because DOCS documents shipped text and must not be closed by a test-only edit); a head identity pair, head_commit and head_committed_at, read from the repository; and B/v1, a binding digest over five LF-terminated lines - tag, head_commit, content_digest, resolved_surface_count, checked - under the tags p002-7-preactivation-binding/v1 and p002-7-confirmation-binding/v1. BOTH AUTHORITATIVE CONSUMERS RECOMPUTE F1-F5 AGAINST THE REPOSITORY rather than trusting the line: F1 head identity equals current HEAD, F2 content digest equals recomputation, F3 resolved_surface_count equals the phase value (0 readiness, 4 confirmation), F4 checked is present once, is RFC 3339 UTC YYYY-MM-DDThh:mm:ssZ, and is not earlier than the head commit's committer timestamp, F5 binding equals B/v1 recomputation. ANY failure is CLOSED. THIS CLOSES THE DOCUMENTED POST-REVERT RE-ACTIVATION PATH MECHANICALLY: after a revert the four surfaces are BYTE-IDENTICAL to their pre-activation state, so a digest over surfaces alone would still match, but git revert NECESSARILY CREATES A NEW COMMIT, so F1 fails and F4 fails independently - the gate closes without either consumer being told. The remedy is the one the records already mandate: RE-RUN the emitter, which replaces the whole artifact atomically and necessarily produces a FRESH BINDING. THE SCOPE IS STATED HONESTLY: staleness and mistake detection, NOT a tamper-proof security boundary, and no wall-clock future check, deliberately, because clock skew would make the contract environment-dependent. The digest and binding computations are EMITTER AND GATE LOGIC, NOT ASSERTIONS: they produce no assertion outcome, belong to no assertion family, and never enter the conformance suite. THE ACTIVATION IS NOT SPLITTABLE: attempt-01 finding A1 (P0) found the template and mirror writes as separate tasks joined by a blocks edge. Sequential task edges are NOT atomic - between the two commits the pair is divergent and the mirror assertion fails on a commit that is on the branch. Archived 169.001-T, 169.002-T and 169.003-T carry that provenance; 169.015-T absorbs them, and the stale item_deps edges those archived tasks left behind were removed in the attempt-02 remediation cycle under finding G2 (P2). IT IS ALSO NOT NARROWABLE MID-FLIGHT: if 169.015-T ever threatens the 2-hour rule it HALTS and the unit RETURNS TO STAGE for explicit scope redesign. Ship neither splits the commit nor narrows the contract in place, because re-deriving an assertion is AUTHORING one inside a transcription-only task and would enter a never-red assertion after the production text exists. Attempt 02 finding G3 (P2) recorded that the previous R5 rule required exactly that. Stage, not Ship, decides any reduction of declared_surface_count, and re-plans its consequences - retiring 169.010-T's family outright and re-observing 169.014-T's closure assertion RED - before activation resumes; and because a redesign changes the candidate definition, F2 fails on candidate_digest even with no intervening commit, so a stale readiness verdict cannot authorize a revised activation. EVERY ASSERTION IS PROVEN RED FIRST: attempt-01 finding A2 (P0) found mirror-divergence, version-attribution and negative-row assertions still entering in GREEN tasks with no red observation, the unclosed half of attempt-08 B2. Those assertions now live in RED tasks; archived 169.005-T and 169.008-T carry that provenance and 169.016-T absorbs their observation role. Each RED task records TWO observations per assertion: an ABSENCE red against current surfaces, failing individually with its own marker, and a DISCRIMINATING red against a near-miss fixture - the second proving the assertion tests the CONTRACT rather than a file's existence. Aggregate suite exit codes are explicitly insufficient evidence. COMPOSED-STATE VOCABULARY (decision D6), one vocabulary across plan and records: STATUS_CONTRACT_HELD is the ONLY pass token; STATUS_CONTRACT_DIVERGENT names the offending surface and divergence; STATUS_CONTRACT_NOT_OBSERVED covers import failure, zero executed assertions, a family with no per-assertion record, an unreadable digest input, and an ABSENT verdict line, is evaluated FIRST, and is NEVER a pass. Attempt 02 finding G1 (P1) found the plan declaring these three tokens while 169.016-T emitted an unrelated two-token pair that deleted the not-observed state. THE VERDICT HAS ONE DESTINATION AND ONE LITERAL FORMAT (attempt 03 finding L1, P1, which found the verdict line declared with NO destination artifact and NO line format, making the ABSENT-line rule undecidable - a reader could not tell absence from presence-elsewhere - and leaving the gate unevaluable). DESTINATION: .autoharness/gates/p002-7-status-contract-verdict.txt, which is GITIGNORED, so the verdict is a generated observation ABOUT the tracked surfaces, is never committed, and never leaves the working tree dirty. FORMAT: exactly one line, prefix "COMPOSED_STATE: ", token first, fields separated by " | ", in one of three literal forms carrying families/assertions_passed/declared_surface_count/resolved_surface_count/head_commit/surface_digest/binding for HELD, families/failed/surface/divergence for DIVERGENT, and a closed reason vocabulary (import_error, loader_errors, failed_test_placeholder, zero_assertions, family_unrecorded, digest_input_unreadable) for NOT_OBSERVED, each with checked=<YYYY-MM-DDThh:mm:ssZ> in RFC 3339 UTC. Only the authorizing form carries head_commit, the content digest and binding, because a not-observed verdict is reachable precisely when the inputs cannot be read, so its digest is undefined by rule and could not be emitted truthfully. WRITE BEHAVIOUR: each artifact has a SINGLE WRITER; the write is ATOMIC via same-directory temp file plus rename; each run REPLACES THE WHOLE FILE and the artifact is NEVER APPENDED TO, so a re-run necessarily yields a fresh binding. ABSENCE EVALUATION IS FAIL-CLOSED: missing, unreadable, empty, no prefixed line, MORE THAN ONE such line, or an unrecognised token all resolve to the NOT_OBSERVED token. A FRESHNESS FAILURE IS A GATE FAILURE, NOT AN ARTIFACT REWRITE: it closes the consumer's gate and never authorizes any consumer to rewrite, amend or delete an artifact it does not own. THE UNIT IS SELF-CONTAINED: each emitter's own invocation exits zero ONLY on its authorizing token, and each consumer's own first-action read is the stronger, independent mechanism, so no CI change, no new workflow and no task outside 177-S is needed for either gate to be evaluable. .github/workflows/ci.yml IS NOT A VERDICT CONSUMER - its test job runs the stdlib unittest suite and consumes an EXIT CODE, PRODUCING the observations the verdicts derive FROM; no task in this unit modifies it. Ship's claim sequence at .github/agents/_ship.agent.md item 4 consumes the P-002.7 CONTRACT TEXT, not either verdict line. PLAN HARDENING IS REQUIRED AND PRESENT: the four surfaces span TWO template families, templates/policies/ and templates/agents/, which is an elevated blast-radius signal, so requires_plan_hardening is declared true rather than argued down (attempt 02 finding G5, P3). ATTEMPT-08 RECORD, STATED CORRECTLY: attempt 08 recorded TWO P1 findings and one P2, not a single evidence defect. B1, the phantom stash ID 3EF5AAF9, is CLOSED - decision F10 corrected the source to 3EF5AAF2, which is the ID carried throughout this unit. B2, assertions entering in GREEN, remained OPEN through attempt 01 and is what this re-derivation closes. NOT IN THIS UNIT: the verify-workspace token implementation and its negative-case suite, deferred to typed-policy-representation entry E770139B. Stash 3EF5AAF2. Decision docs/decisions/2026-09-18-shared-execution-architecture-and-portfolio-reslicing-decision.md (D2, D6, D8, F7, F10, R5). Plan docs/plans/2026-09-18-post-claim-member-status-contract-plan.md (plan_id post-claim-member-status-contract-v2, revision 6, disposition REMEDIATED-PENDING-REVIEW, awaiting independent attempt 06).