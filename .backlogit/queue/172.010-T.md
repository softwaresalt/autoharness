---
artifact_type: task
created_at: 2026-09-18T20:25:47.4555401Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 172.006-T
id: 172.010-T
labels:
    - test
    - structural
    - inventory
    - checkpoint
parent_id: 172-F
priority: medium
status: queued
title: 'Resume-hint T6b: inventory-backed structural verifier over every checkpoint producer surface'
updated_at: 2026-09-18T20:25:47.4555401Z
---

INVENTORY-BACKED STRUCTURAL VERIFICATION. Assert that NO checkpoint producer surface in the repository reaches backlogit_create_checkpoint (MCP) or backlogit checkpoint create (CLI) directly, and that the only permitted harness create path is the autoharness checkpoint create adapter.

THE SURFACE SET IS DISCOVERED, NOT LISTED. The test derives its inventory by scanning the repository for any file that references a checkpoint-create operation by name, across ALL of: templates/agents/ and .github/agents/; templates/skills/ and .github/skills/; templates/instructions/ and .github/instructions/; docs/ guidance that instructs an agent to create a checkpoint; and src/autoharness/, where only the adapter implementation itself may reach the raw call. Each discovered reference is classified as ADAPTER INVOCATION (allowed), RAW CREATE INSIDE THE ADAPTER IMPLEMENTATION (allowed, exactly one site), or RAW CREATE ANYWHERE ELSE (fail).

WHY THIS IS NOT 172.006-T's ASSERTION. 172.006-T asserts the two WIRED producer templates behave correctly. A structural gate hardcoded to the two surfaces known today passes silently the moment a third producer is added, which is how this class of gate is normally defeated. This task fails a bypassing producer by DISCOVERING it, not by having listed it. The test is itself verified against a SYNTHETIC THIRD PRODUCER fixture that reaches the raw create call and MUST fail the assertion; a test that cannot be made to fail proves nothing.

CONTRACT AND CHECKSUM ASSERTIONS. Also assert that: (a) the authoritative Checkpoint Payload Contract no longer advertises backlogit_create_checkpoint / backlogit checkpoint create as a harness producer path; (b) .github/instructions/backlogit.instructions.md and templates/instructions/backlogit.instructions.md.tmpl agree in the relevant clause, so the installed copy and the template it is generated from cannot drift; (c) the workspace install-manifest checksum for every tracked file modified by 172.001-T matches that file's current content, so the atomic contract update does not leave the manifest reporting drift against files the update itself authored. Where a modified surface is not manifest-tracked, the test records that explicitly rather than assuming it.

Blocked by 172.006-T (the wired system must be green before its inventory is audited). Terminal task of 180-S together with 172.007-T; the two are mutually independent and may run in either order. Scope: tests/ only - this task implements no source, template or instruction change.

Stash 71200CBB. Plan docs/plans/2026-09-17-checkpoint-resume-hint-contract-plan.md (plan_id checkpoint-resume-hint-contract, plan_role active, revision 6, task T6b). Decision docs/decisions/2026-09-17-seven-entry-contract-defect-staging-portfolio-deliberation.md (revision 3). Verdict manifest docs/reviews/2026-09-17-checkpoint-resume-hint-contract-plan-review.md (latest_attempt 06, verdict REMEDIATED-PENDING-REVIEW, awaiting independent attempt 07).