---
artifact_type: task
created_at: 2026-08-05T00:14:36.875186Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-two-axis-v1
    size_source: agent
dependencies:
    - 109.002-T
id: 109.017-T
labels:
    - topology
    - gate
    - ship-agent
    - integration
    - p-001
    - p-016
    - enforcement
parent_id: 109-F
priority: medium
status: queued
title: 'B6: wire gate pipeline-topology into Ship agent template at claim/worktree/build/PR/closure'
updated_at: 2026-08-05T02:10:15.361769Z
---



<!-- BEGIN:acceptance-criteria -->
* Ship ordering is explicit and matches the existing P-011 flow: (1) run the gate with `--shipment <ID> --phase pre_claim` BEFORE implementation branch/worktree creation; (2) create/select the implementation branch/worktree; (3) re-run `--phase pre_claim` immediately BEFORE shipment claim; (4) claim the shipment; (5) IMMEDIATELY run `--phase post_claim` to re-read ALL shipment records and require exactly one active shipment, the selected target; only then may lifecycle work proceed.
* Build, PR creation, and shipment closure/safe-close each have their own preceding invocation with `--phase lifecycle` (or `post_claim`) for the same claimed shipment. Git hooks cover commit/push.
* Every Ship invocation passes `--shipment <ID>` bound to the shipment Ship is operating on. Missing/unresolvable target or phase in agent mode -> exit 2, never inferred or fail-open.
* The immediate post-claim invocation is the GLOBAL verification contract from 109.001-T, not a target-status-only check: any other active shipment, zero active shipments, or ambiguity halts with CLAIM_VERIFY_FAILED/remediation.
* Each invocation point halts fail-closed on a non-zero verdict (exit 1 blocked / 2 invalid) with the gate's remediation token surfaced; exit 0 proceeds.
* Structural assertions (verify_workspace + template-structure tests) enforce the exact relative order: pre_claim gate < branch/worktree creation < pre_claim gate < claim < immediate post_claim global verification < build/lifecycle gate sequence; they also prove lifecycle gates precede build, PR, and closure. Tests fail if branch/worktree is labeled post_claim, if post_claim verification is absent/non-immediate, or if any guarded operation precedes its required gate.
* Bootstrap exemption documented: the gate cannot be enforced on the feature's own bootstrapping shipments (114-S/115-S build the gate); the rollout note records this so self-referential shipments are not blocked by an as-yet-uninstalled gate.
* Implementation changes are confined to templates/agents/_ship.agent.md.tmpl and installed .github/agents/_ship.agent.md plus the scoped verify_workspace/template-structure assertion files required by this task; no backlogit source mutation and no unrelated template family.

<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
[Shipment B / integration] Wire `gate pipeline-topology` into the Ship agent's actual P-011 order. Because branch/worktree creation occurs before claim, run an explicit shipment-scoped `--phase pre_claim` gate before branch/worktree creation, run pre_claim again immediately before claim, then run an immediate `--phase post_claim` GLOBAL verification after claim. Only after that verification may build/PR/closure lifecycle gates and operations proceed. Git hooks cover commit/push. Add fail-closed handling and structural assertions for the exact sequence.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Wire invocations into the existing Ship intake, branch-before-mutation (P-011), claim, build, PR, and closure steps of _ship.agent.md. Reuse the gate's --json/--mode agent-mode contract delivered by Shipment A. Do not describe branch/worktree as post-claim: its guard is `--phase pre_claim`. After branch/worktree creation, re-run pre_claim immediately before the claim to narrow the TOCTOU window; after the claim returns, invoke `--phase post_claim` immediately and reuse 109.001-T's full global re-read before any lifecycle work. Build/PR/closure may use `lifecycle` or `post_claim`. Coordinate structural assertions with 109.015-T; assertions must parse ordering, not merely count command strings.
<!-- END:implementation-notes -->