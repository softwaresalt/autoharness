---
artifact_type: task
created_at: 2026-09-19T00:27:50.1786981Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 169.011-T
id: 169.009-T
labels:
    - test
    - p-002
    - tdd-red
parent_id: 169-F
priority: high
status: queued
title: 'P-002.7 T0a (RED): composed state-machine contract test for the three transition states, observed failing'
updated_at: 2026-09-19T19:07:00.0000000Z
---

TDD RED PHASE. THIS TASK CREATES THE UNIT'S SINGLE CONFORMANCE TEST MODULE AT THE EXACT PATH tests/test_p002_7_member_status_contract.py AND OWNS THAT PATH (attempt 06 finding O1, P2). THE PATH IS DECLARED RATHER THAN LEFT TO THE EXECUTING AGENT, because the revision-6 freshness binding computes a CCD/v1 canonical content digest over a FULLY ENUMERATED input list in which this module is the SEVENTH readiness input, and CCD/v1 rule 1 makes the digest UNDEFINED if a listed path does not exist - so a module created at any other filename halts the unit at 169.017-T with PREACTIVATION_NOT_OBSERVED | reason=digest_input_unreadable. Revision 6 declared the two helper paths for exactly this reason and left this third input named only at consumers and narrators; revision 7 closes that gap so every one of the seven digest inputs has ONE UNAMBIGUOUS PRODUCER. THE OTHER FOUR RED TASKS - 169.010-T, 169.012-T, 169.013-T and 169.014-T - EXTEND THIS SAME MODULE IN PLACE AND CREATE NO SECOND TEST MODULE; if this exact path is not what exists after this task runs, the plan's Tasks table, this record, the enumerated digest input list and 169.015-T's own F2 recomputation disagree, and that mismatch is detectable before any surface is mutated. THIS DECLARATION ADDS NO ASSERTION AND WIDENS NO RESPONSIBILITY: this task's assertion family is unchanged. Owns the POSITIVE TRANSITION-ROW assertion family. Author the composed state-machine contract test for the THREE claim-to-admission transition states described by the not-yet-authored P-002.7 clause, and OBSERVE IT FAILING. The three rows are read from the canonical definition authored as test-owned data in 169.011-T; this task asserts them, it does not invent them. The NEGATIVE closure assertion - that there is no FOURTH row - belongs to 169.014-T, so that positive presence and negative closure are each proven red by their own owner. TWO OBSERVATIONS PER ASSERTION ARE REQUIRED under the plan's discriminating RED rule. (1) ABSENCE RED: the assertion executed against the current surfaces, failing INDIVIDUALLY, with its own declared marker in its own failure text; an aggregate non-zero suite exit is explicitly insufficient evidence and does not prove that THIS assertion failed. (2) DISCRIMINATING RED: the same assertion executed against the near-miss fixture from 169.011-T - the canonical definition with one transition row's target status altered - and observed failing there too, proving the assertion tests the ROW CONTENT rather than the clause's mere existence. IMPORT SAFETY (BINDING): this module must import cleanly under unittest.defaultTestLoader with ZERO loader.errors and ZERO _FailedTest placeholders, asserted directly. The not-yet-existing clause text is read INSIDE the test body through a helper, never at module import time - a module that raises on import produces a MISSING OBSERVATION, not a red one, and would fail its own P-004 gate. Blocked by 169.011-T; blocks 169.017-T, the pre-activation readiness gate, which is the sole immediate predecessor of the ACTIVATE task 169.015-T. Scope: tests/, and specifically the single conformance module tests/test_p002_7_member_status_contract.py, which this task CREATES at that exact path. Stash 3EF5AAF2. Plan docs/plans/2026-09-18-post-claim-member-status-contract-plan.md (plan_id post-claim-member-status-contract-v2, revision 7, reviewed and PASSED at independent terminal attempt 07). Verdict manifest docs/reviews/2026-09-18-post-claim-member-status-contract-v2-plan-review.md.