---
artifact_type: task
created_at: 2026-08-18T05:13:21.1056929Z
custom_fields:
    complexity: low
    size: XS
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 131.001-T
labels:
    - reliability
    - hotfix
    - gate-correctness
    - pipeline-topology
parent_id: 131-F
priority: high
references:
    - docs/compound/2026-08-18-topology-gate-forward-dependent-suppression-residual-defect.md
    - docs/plans/2026-08-18-topology-gate-forward-dependent-directional-predicate-plan.md
    - docs/plans/2026-08-18-topology-gate-forward-dependent-directional-predicate-hardening.md
    - docs/reviews/2026-08-18-topology-gate-forward-dependent-directional-predicate-review.md
status: queued
title: Apply directional predicate in _prior_shipment_id + regression test
updated_at: 2026-08-18T05:13:45.3490872Z
---

SIZE: XS. COMPLEXITY: low. (Both axes recorded in prose here as a durable fallback because .autoharness/backlog-registry.yaml declares NO `features.sizing` flag; structured fields are additionally set via update_task where the tool accepts them. Size = effort/volume; complexity = difficulty/uncertainty. These are independent axes and neither is derived from the other. XS: two files, one function, one new test, diff already written. Low complexity: the reasoning is already banked and verified in the compound doc; this is mechanical application, not rediscovery.)

Apply the directional predicate to src/autoharness/gates/topology.py::_prior_shipment_id and add one regression test to tests/test_gates_topology.py.

STEP 1 -- src/autoharness/gates/topology.py, _prior_shipment_id (line ~1345). Replace the direction-blind guard `if any(target in shipment.blocking_predecessor_ids for shipment in shipments): return None` with the numerically-lower-only loop recorded verbatim in the compound doc: iterate shipments, `re.match(r"^(\d+)-S$", shipment.shipment_id)`, `continue` on no match, `continue` when `int(other.group(1)) >= target_num`, and `return None` only when `target in shipment.blocking_predecessor_ids`. The `>=` is load-bearing (H2) -- it also skips the target's own record; do NOT simplify to `>`. Everything after the guard (the `prior` selection loop and return) is unchanged.

STEP 2 -- Replace the stale comment block at lines ~1330-1344, which explicitly asserts the superseded over-broad rule and would argue a future maintainer back into the bug (H5). New comment must state WHY direction is load-bearing.

STEP 3 -- tests/test_gates_topology.py: add `test_higher_numbered_forward_dependent_does_not_suppress_targets_own_predecessor_check` to the existing ImplicitNumericPredecessorTests class (line ~1609), using the existing _FakeReaders (line 20) and _shipment (line 61) helpers -- no new fixtures. Scenario: 111-S queued, 112-S queued, 113-S queued with deps=('112-S',); target 112-S; assert primary_token == 'PREDECESSOR_NOT_SHIPPED' and predecessor_id == '111-S'.

ACCEPTANCE CRITERIA. (1) The NEW test passes. (2) The EXISTING test_multi_hop_reverse_dependency_disables_fallback_entirely_not_just_the_violator (line ~1677) STILL passes unmodified -- verifying only the new test is exactly the v1->v2 failure mode repeating (H1). Neither test may be weakened or deleted to make the other pass; if they appear to conflict, HALT and escalate. (3) tests/test_gates_topology.py: 95 tests (94 + 1), all pass. (4) Full tests/ suite: no NEW failures. Known-unrelated pre-existing failure, do NOT fix here: tests/test_deploy_harness_scripts.py::DeployHarnessPs1ChecklistExecutionTests::test_checklist_report_prints_non_interactively (local env version-string mismatch; reproduces with and without the fix). (5) No existing test flips from a blocking token to a passing one (H7) -- this change may only make the gate block MORE.

CONSTRAINTS. Reuse the recorded diff verbatim; do NOT re-derive it from prose -- re-derivation is exactly how the current bug was produced from the previous lesson (H4). Scope-contained to these two files; do NOT bundle the 129-F/138-S cancellation, storage-root work, any other gate, or opportunistic cleanup (H9). On the PR, thread-based Copilot review is INSUFFICIENT: also run `gh pr view <N> --json reviews` and explicitly triage any "Suppressed comments" block, because this exact defect escaped through that gap on PR #357 (H6).