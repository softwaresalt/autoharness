---
artifact_type: task
created_at: 2026-08-21T00:44:46.4637837Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 137.002-T
id: 137.001-T
labels:
    - tests
    - templates
    - contract-test
parent_id: 137-F
priority: medium
references:
    - docs/plans/2026-08-20-template-dogfood-paired-edit-contract-plan.md
status: queued
title: Pin the divergent pair set and cause taxonomy in the parity contract test
updated_at: 2026-08-21T00:45:17.0365993Z
---

Pin the divergent template/dogfood pair set so a new pair cannot silently join it.

FILE: tests/test_scope_containment_policy_contract.py

That file already splits _CLEAN_BYTE_IDENTICAL_PAIRS (asserted byte-identical via _render_template) from _DIVERGENT_MARKER_ONLY_PAIRS (asserted by marker presence in both files plus manifest-checksum-matches-actual-bytes). KEEP BOTH MECHANISMS — the split is correct. Add:

1. A per-pair CAUSE ANNOTATION on each divergent entry, matching the taxonomy authored in 137.002-T.
2. An assertion that the divergent set has EXACTLY the expected membership, so a fifth pair becoming non-renderable is a test failure that NAMES the new pair, rather than an unnoticed shrug.
3. A pointer to the 137.002-T maintenance-contract document in the in-file rationale comment, replacing the current inline-only explanation.

ACCEPTANCE
- Existing byte-identity assertions for the four clean pairs still pass, unchanged.
- The membership assertion fails with a clear message if a pair is added OR removed.
- The membership assertion fails CLOSED: an unknown pair present must fail, not warn.
- No production code changes in this task.

EXECUTION ORDER: run AFTER 137.002-T. The numeric ID ordering is misleading here — 137.002-T authors the taxonomy that this task annotates, so it must land first. A `requires` dependency edge on 137.002-T is recorded in the backlog.

Plan: docs/plans/2026-08-20-template-dogfood-paired-edit-contract-plan.md (Task 2)