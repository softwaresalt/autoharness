---
artifact_type: task
created_at: 2026-09-03T20:17:07.4265Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 160.020-T
labels:
    - ci
    - toolchain
    - dependencies
    - prerequisite
    - fail-closed
parent_id: 160-F
priority: high
status: queued
title: 'Test-toolchain alignment prerequisite: declare and lock pytest, move CI to the canonical runner (T0)'
updated_at: 2026-09-03T20:17:50.1469124Z
---

Make SHIP-10's already-authorized test harness EXECUTABLE (T0) by aligning this repository's test toolchain to the runner its own Constitution mandates: declare a PINNED `pytest` test dependency, regenerate `uv.lock`, and move CI's authoritative test invocation to `uv run python -m pytest`.

WHY THIS EXISTS, AND WHY IT IS SAME-CONTRACT COMPLETION RATHER THAN SCOPE EXPANSION (ADDED IN REVIEW-FIX CYCLE 9, OPERATOR ITEM). MEASURED EVIDENCE, NOT INFERENCE: `.github/instructions/constitution.instructions.md` declares the workspace Test gate as `pytest` (suite under `tests/`) and is NON-NEGOTIABLE; `.github/instructions/github-pr-automation.instructions.md` gives the canonical command `uv run python -m pytest`; `pyproject.toml` ALREADY carries a live `[tool.pytest.ini_options]` block with `pythonpath = ["src"]`. Against that, `uv.lock` locks exactly eight distributions - attrs, autoharness, jsonschema, jsonschema-specifications, pyyaml, referencing, rpds-py, typing-extensions - and `pytest` is NOT among them, `pyproject.toml` declares no `dependency-groups` and no test extra, and `.github/workflows/ci.yml` explicitly runs `PYTHONPATH=src python -m unittest discover -s tests`. So pytest is CONFIGURED and MANDATED but NOT LOCKED, and CI has drifted off the mandated runner.

THAT IS THIS SHIPMENT'S OWN BLOCKER, NOT A NEIGHBOURING DEFECT. Every one of the plan's 47 ledger cases except this task's own case 47 red is observed through `uv run python -m pytest` under the source-provenance protocol; the evidence contract resolves test identity by PYTEST NODE ID; and 160.017-T's terminal ledger asserts membership via `uv run python -m pytest --collect-only`. A harness the plan already mandates CANNOT BE EXECUTED AT ALL until the runner it mandates resolves. Declaring and locking that runner therefore COMPLETES THE AUTHORIZED CONTRACT; it does not open a new one. Cycle 8 deferred it and left 160.015-T (T14) KNOWINGLY BLOCKED on an operator decision; the operator has made that decision and this task removes the block. NO separate future shipment is created for it, because a prerequisite of this shipment's own gate is not future work.

DELIVERABLE D1 - PINNED TEST DEPENDENCY, THROUGH THE PROJECT'S SUPPORTED MECHANISM. Declare `pytest` as a development/test dependency using the dependency-group mechanism this project's toolchain supports (`[dependency-groups]` in `pyproject.toml`, which `uv` resolves natively), at a PINNED version. AN UNPINNED OR FLOATING REQUIREMENT IS PROHIBITED - `pip install pytest`, a bare `uv add pytest` with no pin, and an ad hoc install step in a workflow are all forbidden. Adding an unpinned test dependency to a locked toolchain is precisely the failure mode this shipment is otherwise policing. EXACTLY ONE DIRECT DECLARATION IS ADDED. `pytest`'s TRANSITIVE CLOSURE will enter the lock as a consequence of locking - that is what a lockfile is for and it is stated here rather than discovered in review - but NO OTHER DIRECT DEPENDENCY may be declared, added, removed, or version-changed.

DELIVERABLE D2 - REGENERATE `uv.lock`. Run the project's lock operation so `uv.lock` gains a `pytest` node and its transitive closure. THE DIFF MUST BE REVIEWED AND BOUNDED: it must contain the new test-group nodes and nothing else. A lock regeneration that silently bumps an unrelated pinned distribution is a REJECTION - restore and redo it, do not accept it and explain it afterwards.

DELIVERABLE D3 - MOVE CI TO THE CANONICAL RUNNER. In `.github/workflows/ci.yml`, `test` job ONLY:
(i) ADD the `astral-sh/setup-uv` step pinned at 08807647e7069bb48b6ef5acd8ec9567f424441b (v8.1.0) - the SAME SHA already pinned in `release.yml` - immediately after the Python setup step. Copy the pin; do not choose one and do not float it.
(ii) REPLACE the drifted invocation `PYTHONPATH=src python -m unittest discover -s tests` with the canonical command `uv run python -m pytest`.
(iii) RETAIN A FAIL-CLOSED AVAILABILITY PREFLIGHT immediately before the test step: assert `uv run python -m pytest --version` succeeds, and FAIL THE JOB WITH A NAMED ERROR if it does not. IT IS RETAINED DELIBERATELY, NOT DELETED AS REDUNDANT once pytest is locked: it is what turns a FUTURE lock regression back into a loud, named CI failure instead of a silent runner substitution. A SILENT SKIP, A FALLBACK TO `python -m unittest`, AND `continue-on-error` ARE ALL PROHIBITED. A GATE THAT SILENTLY CHANGES RUNNERS IS A DELETED GATE.
NOTE ON PROVENANCE OF (i)-(iii): cycle 8 assigned these `ci.yml` changes to 160.015-T (T14). CYCLE 9 MOVES THEM HERE, because this task is what makes the canonical command RESOLVABLE; leaving the invocation change fifteen tasks downstream of the declaration that enables it would have left `ci.yml` invoking a command that could not resolve for the whole interval. 160.015-T NOW OWNS `release.yml` ONLY AND MUST NOT TOUCH `ci.yml`.

EXISTING TESTS ARE NOT REWRITTEN. `unittest.TestCase` modules under `tests/**` are collected by pytest unchanged. THIS TASK REWRITES NO TEST AND RENAMES NO TEST.

DELIVERABLE D0 - ONE RED-FIRST STATIC CI/TOOLCHAIN CONTRACT CASE, AUTHORED BEFORE D1/D2/D3 (ADDED IN CYCLE 11, ITEM 8). **THE WITHDRAWN CYCLE-9 CLAIMS "THIS TASK AUTHORS NO TEST" AND "THIS TASK AUTHORS NO CASE AND OWNS NO GREEN TRANSITION" ARE DELETED AND MUST NOT RETURN.** They made this task the ONLY behaviour-changing task in the shipment exempt from the plan's own RED-FIRST discipline, and the exemption was unearned: a CI toolchain change is exactly the kind of change that silently regresses, and the plan's whole thesis is that an unwitnessed transition is not evidence.
CASE: `test_ci_invokes_the_locked_canonical_test_runner` - RED-FIRST, AUTHORED BY THIS TASK, GREEN TRANSITION OWNED BY THIS TASK. Ledger row 47.
WHAT IT ASSERTS - STATIC CONTRACT ONLY, OVER REPOSITORY SOURCE FILES, BUILDING NOTHING:
(a) `pyproject.toml` declares `pytest` in the test dependency group at a PINNED version (an unpinned or floating specifier FAILS the assertion);
(b) `uv.lock` contains a `pytest` node;
(c) `.github/workflows/ci.yml`'s `test` job invokes `uv run python -m pytest` and does NOT invoke `python -m unittest discover`;
(d) that same job carries the `astral-sh/setup-uv` step pinned at the SAME full SHA as `release.yml`, and carries the fail-closed `--version` preflight BEFORE the test step, with no `continue-on-error` and no skipping `if:` on either step.
It reads those three files as text/parsed data. IT BUILDS NO WHEEL, RESOLVES NO PLUGIN PAYLOAD, RUNS NO GENERATOR, AND ASSERTS NOTHING ABOUT ANY PAYLOAD MEMBER - which is why its `artifact_ref` identity source is a SOURCE-TREE identity, defined once in 160.017-T's `artifact_ref` clause and NOT restated here.
DUAL-PHASE EXECUTION CONTRACT - THE MINIMUM THAT IS HONEST UNDER BOTH GOVERNING DOCUMENTS, AND NO POLICY EXPANSION. MEASURED: P-004 (`.github/policies/workflow-policies.md`) states its red-phase precondition as literally `PYTHONPATH=src python -m unittest discover -s tests` exiting non-zero; the Constitution names `pytest`. BOTH ARE SATISFIED WITHOUT AMENDING EITHER:
- THE CASE IS WRITTEN AS A `unittest.TestCase` METHOD, so BOTH runners collect it. Every test this shipment authors is unittest-compatible for the same reason; that is a shipment authoring convention, not a policy change.
- **RED OBSERVATION**: taken on the COMMITTED PRE-CHANGE TREE, under P-004's EXACT COMMAND `PYTHONPATH=src python -m unittest discover -s tests`. It is red because (a)-(d) are all false before D1/D2/D3 land - measured: `uv.lock` locks eight distributions and `pytest` is not among them, and `ci.yml` runs `unittest discover`. `observation_phase: baseline`. THIS IS THE ONLY PRE-LOCK LEDGER OBSERVATION IN THE SHIPMENT, and its raw log records the command VERBATIM per the provenance protocol, so no reader can mistake it for a pytest run. **THIS IS A STATEMENT ABOUT RECORDED LEDGER OBSERVATIONS ONLY AND DOES NOT NARROW P-004 (CYCLE 11, ITEM 12).** P-004's precondition is WHOLE-SUITE AND GATE-SCOPED - `PYTHONPATH=src python -m unittest discover -s tests` must exit non-zero with expected failure markers before the `harness-ready` label - and that gate confirmation is taken with THAT EXACT COMMAND AT EVERY RED-PHASE GATE IN THE SHIPMENT, both before and after this task lands. Honouring it costs nothing: `unittest` is stdlib, so the command needs no lock, no dependency group and no network, and every case this shipment authors is a `unittest.TestCase` method, so `discover` collects all of them. This task DOES NOT amend, narrow, or reinterpret P-004; it only adds the Constitution's `pytest` as the CI gate's authoritative runner.
- **GREEN OBSERVATION**: taken AFTER D1/D2/D3 land, under the canonical `uv run python -m pytest`. `observation_phase: post-change`.
- `test_node_id` ON BOTH RECORDS IS THE CASE'S PYTEST NODE ID. That is not a retroactive claim about how the red run was invoked: a node ID is a property of the case's FILE AND CLASS LOCATION, fixed when the case is authored, and 160.017-T verifies it against a TERMINAL `uv run python -m pytest --collect-only` run at the END of the shipment, by which time pytest is locked. NO NEW FIELD, NO RUNNER FIELD, AND NO SCHEMA CHANGE IS INTRODUCED FOR THIS.
ORDER WITHIN THIS TASK IS BINDING: author the case -> COMMIT it -> observe it RED under the P-004 unittest command -> then apply D1, D2, D3 -> COMMIT -> observe it GREEN under `uv run python -m pytest`. The red commit precedes the red observation and the green implementation commit precedes the green observation, exactly as 160.017-T check 6 requires of every RED-FIRST case.
IT AUTHORS NO PAYLOAD BEHAVIOUR TEST OF ANY KIND. Its single case asserts the toolchain contract and nothing else; it makes no assertion about wheel members, plugin members, the manifest, the schemas, the classifier, or any install behaviour.

LEDGER EFFECT, STATED HONESTLY: the canonical ledger goes from 46 to **47 cases**, RED-FIRST from 32 to **33**, CHARACTERIZATION unchanged at **14**. Owner assignments go from 52 to **53**. RED-FIRST author-to-owner ORDERING PATHS REMAIN **34**, because this case's author and owner are the SAME task and a task cannot need to precede itself. NO DAG EDGE IS ADDED - the graph stays at 19 nodes / 51 edges. THIS TASK PRODUCES ONE OBSERVATION PAIR AND ITS RAW LOGS, so 160.017-T DOES expect and DOES verify evidence from it, under producer path observations/logs/T0/**.

BLAST RADIUS - MEASURED, AND IT INTERSECTS NO PAYLOAD. All three mutated files are classified EXCLUDE IN BOTH IN-SCOPE CHANNELS under plan AC11: `pyproject.toml`, `uv.lock`, and `.github/workflows/**`. This task therefore CANNOT change a single member of the wheel or the plugin payload, and CANNOT invalidate 160.001-T's baseline member inventories or aggregate digests. State that in the task's own evidence rather than leaving it to be re-derived.

TRACKED-WRITE APPROVAL PARITY WITH 160.006-T (T7) AND 160.007-T (T8) - ADDED IN CYCLE 11, ITEM 7, AND BINDING. All three files this task mutates are TRACKED and their bytes CHANGE, so every one of this task's writes is class `OVERWRITE` under plan AC3e's partition. **A TRACKED `OVERWRITE` REQUIRES EXTERNAL FRESH LIVE OPERATOR APPROVAL REGARDLESS OF WHICH TASK PERFORMS IT AND REGARDLESS OF WHETHER A GENERATOR WAS INVOLVED.** THE WITHDRAWN CYCLE-9 CLAUSE - "NO AC3e PARTITION RECORD IS PRODUCED, AND THAT IS CORRECT ... AC3e IS NOT A GENERAL EDIT-APPROVAL REGIME" - CONFLATED TWO SEPARABLE THINGS AND IS DELETED. What is generator-scoped is the AC3e CLASSIFICATION MACHINERY (Layer 1: the generator computes the partition, prints it, and unconditionally refuses tracked OVERWRITE/REMOVE while accepting no approval input). What is NOT generator-scoped is the LAYER-2 APPROVAL OBLIGATION over a tracked overwrite. Exempting authored edits from Layer 2 would have made "author the edit by hand" a silent bypass of the exact control Constitution Principle VII exists to impose, which is a strictly worse outcome than running the generator.
THEREFORE, BEFORE APPLYING ANY OF D1/D2/D3, THIS TASK:
(a) COMPUTES AND RECORDS THE REVIEWED PARTITION over its own three files - `pyproject.toml` (OVERWRITE), `uv.lock` (OVERWRITE), `.github/workflows/ci.yml` (OVERWRITE) - as per-class path lists plus byte digests. No path of this task is `CREATE`, `NO-OP` or `REMOVE`; if a computed class ever disagrees with that, HALT and report rather than reclassifying to fit.
(b) OBTAINS FRESH LIVE OPERATOR APPROVAL OUT-OF-BAND over the harness operator channel, over THAT EXACT PARTITION - never as a program input, because no signal a process can read satisfies R-3. A backlog comment, this task's text, a prior recorded approval, the pre-change byte record, or an agent-generated confirmation is AUDIT EVIDENCE ONLY and can NEVER serve as that authorization. **RECORDED BYTES NEVER AUTHORIZE.**
(c) APPLIES THE EXACT REVIEWED DIFF, then RE-VERIFIES THE RESULTING BYTES against what was reviewed and HALTS ON ANY DIVERGENCE. It does not apply an approximation of the reviewed diff and explain the delta afterwards.
(d) APPENDS the reviewed-partition record (per-class path lists, byte digests, the approval decision, and the R-3 approval reference) to the SAME pre-change byte record this task already writes at docs/audits/2026-09-03-ship10-payload-evidence/. NO NEW FILE, NO NEW SCHEMA, NO NEW DESTINATION. 160.017-T is the READ CONSUMER of that record.
**THIS APPROVAL APPLIES AT IMPLEMENTATION TIME, WHEN THIS TASK IS EXECUTED BY Ship. IT IS NOT A GATE ON STAGE PUBLICATION OF THIS BACKLOG RECORD**, which mutates no tracked source, config or workflow file at all.

DEPENDENCIES: NONE. This task is a ROOT of the shipment DAG, alongside 160.002-T (T1) and 160.001-T (T2a). IT BLOCKS 160.005-T (T3a, the RED-FIRST harness), 160.016-T (T3b, the CHARACTERIZATION harness), and 160.015-T (T14). The first two are the earliest tasks in the graph that must EXECUTE a test - T3a's completion condition is a RECORDED RED, an observation that cannot be taken at all if the runner does not resolve - so blocking them places this task before EVERY SHIP-10 test author and consumer that requires pytest, since 160.008-T, 160.012-T, 160.013-T, 160.009-T, 160.010-T, 160.003-T, 160.004-T, 160.014-T, 160.006-T, 160.007-T, 160.015-T and 160.017-T are all reachable from T3a/T3b. The 160.015-T edge is TRANSITIVELY IMPLIED and is recorded EXPLICITLY ANYWAY: T14 is the task cycle 8 left knowingly blocked on the unlocked runner, and encoding the release of that block as a real edge makes the resolution machine-visible instead of narrative. NO CYCLE IS INTRODUCED - a root has no prerequisites, so it is unreachable from every node and an edge out of a source can never close a cycle.

SAME-FILE SERIALIZATION WITH 160.006-T (T7) IS GUARANTEED BY THE DAG, NOT BY CONVENTION. Both tasks edit `pyproject.toml`, in DISJOINT REGIONS: T7 owns the WHEEL BUILD TABLE, this task owns the `[dependency-groups]` TEST-DEPENDENCY SECTION. This task is a root and reaches T7 through T0 -> T3a -> ... -> T7, so it always completes first and no extra serialization edge is required. THIS TASK MUST NOT TOUCH ANY BUILD TABLE, and T7 must not touch the dependency group.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope.
- `pyproject.toml`: THE TEST-DEPENDENCY SECTION ONLY. `[project]` metadata, RUNTIME `[project] dependencies`, `[build-system]`, version, entry points, `[tool.hatch.build.targets.wheel]`, `[tool.hatch.build.targets.sdist]`, and BOTH `core-metadata-version = "2.4"` pins (invariant I4) MUST be left BYTE-IDENTICAL. The existing `[tool.pytest.ini_options]` block is READ, NOT REWRITTEN - it is already correct and is the configuration the constitution's gate resolves against.
- `uv.lock`: REGENERATED OUTPUT ONLY, bounded to the new test-group nodes.
- `tests/**`: THE SINGLE CASE FILE FOR D0 ONLY. No other test module may be created, edited, renamed or deleted, and no existing test may be rewritten.
- `.github/workflows/ci.yml`: the `test` job only, exactly the three changes in D3. NO other step, job, trigger, `permissions:` block, secret reference, or pinned action SHA may be added, removed, reordered, or altered, and the setup-uv pin must not float.
- `.github/workflows/release.yml`: NOT WRITABLE BY THIS TASK. It belongs to 160.015-T.
- FREEZE-SCOPE: `src/autoharness/` is read-only (invariants I1/I2). No source, no schema, no template, and no `build_support/**` file is touched.
- NO-PUBLISH CHECKPOINT: this task MUST NOT run `twine upload`, `gh release create`, or dispatch `release.yml`.
- ROLLBACK CHECKPOINT (binding, rollback contract R-1/R-3/R-4): before this task lands, record the exact PRE-CHANGE BYTES OF ALL THREE MUTATED FILES - `pyproject.toml`, `uv.lock`, and `.github/workflows/ci.yml` - into docs/audits/2026-09-03-ship10-payload-evidence/, the destination authorized for this task in the plan's H2 writable-surface table. THREE PRE-CHANGE CAPTURES, ONE PER MUTATED FILE. THAT RECORD IS EVIDENCE, NEVER AUTHORIZATION.
  RECOVERY IS A FORWARD REVERT COMMIT (R-1). This is the DEFAULT and the ONLY pre-authorized path.
  A DESTRUCTIVE RESTORE OR OVERWRITE OF ANY OF THE THREE FILES IS NOT PRE-AUTHORIZED BY THIS RECORD OR BY ANY OTHER. It requires FRESH LIVE OPERATOR APPROVAL obtained IN THE SESSION PERFORMING IT, over a channel the agent CANNOT SYNTHESIZE (R-3). A backlog comment, this task's text, a prior recorded approval, or an agent-generated confirmation is AUDIT EVIDENCE ONLY and can NEVER serve as that authorization.
  IF NO SUCH CHANNEL IS AVAILABLE, THE AGENT HALTS AND DOES NOT RESTORE (R-4).
Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

ACCEPTANCE: AC2b-CI. `pyproject.toml` declares EXACTLY ONE new direct dependency, `pytest`, PINNED, in the test dependency group, and every clause named byte-identical above is byte-identical - verified by diff, not by assertion. `uv.lock` contains a `pytest` node and its transitive closure, and NO unrelated distribution's version changed. `uv run python -m pytest --version` SUCCEEDS from the locked environment. `.github/workflows/ci.yml`'s `test` job carries the pinned setup-uv step at the SAME SHA as `release.yml`, invokes the suite as `uv run python -m pytest`, and carries the fail-closed `--version` preflight ahead of it; no `continue-on-error` and no skipping `if:` is present on either step; every other byte of `ci.yml` is unchanged. `uv run python -m pytest --collect-only` RUNS AND COLLECTS the existing suite, which is the capability 160.017-T's node-id membership proof depends on. Pre-change bytes of all three mutated files are recorded per the rollback checkpoint before the task lands. EXACTLY ONE ledger case is authored - `test_ci_invokes_the_locked_canonical_test_runner` - and the ledger is 47 cases / 33 RED-FIRST / 14 CHARACTERIZATION with 53 owner assignments. NO OTHER case is authored, added, renamed, or removed, and NO DAG EDGE IS ADDED. Its RED observation is recorded under the P-004 unittest command on the committed pre-change tree BEFORE D1/D2/D3, and its GREEN observation under `uv run python -m pytest` after; both are written to observations/logs/T0/** with the provenance protocol applied, and 160.017-T verifies the pair. The reviewed OVERWRITE partition over all three files is computed, externally approved fresh and live, applied exactly, re-verified byte-for-byte, and appended to the pre-change byte record. NO production payload artifact, no schema, no manifest, and no channel is created by this task.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size M, complexity medium. **RESIZED S -> M IN CYCLE 11, ITEM 8, ON ADDED WORKLOAD - NOT ON REESTIMATION OF THE OLD WORKLOAD.** The cycle-9 S covered one declaration block in `pyproject.toml`, one regenerated `uv.lock`, three bounded `ci.yml` edits in one job, and three pre-change byte captures. Cycle 11 adds, on top of all of that: authoring a static contract case with four assertions across three files, a commit, a RED observation under the P-004 unittest command, a second commit, a GREEN observation under pytest, two raw logs under the provenance protocol, and a computed-plus-externally-approved-plus-re-verified OVERWRITE partition record. That is materially more than the original surface and M is the honest reading; claiming it still fits S would be exactly the understatement this shipment's sizing discipline exists to catch. NO SPLIT AT M: the red case and the change it witnesses are ONE red-green transition and splitting them would put a knowingly-red case in the shipment with its green owned by a different task. COMPLEXITY MEDIUM, NOT LOW: the pin version must be CHOSEN rather than copied from an existing pin, and the lock regeneration must be held to a bounded reviewable diff rather than accepted as whatever the resolver emits. NO SPLIT: the three deliverables are one indivisible toolchain change - a pinned declaration with no lock is not resolvable, a lock with no CI invocation change leaves CI on the drifted runner, and a CI invocation change with no declaration is exactly the knowingly-blocked state cycle 9 exists to remove.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T0)