---
artifact_type: feature
created_at: 2026-07-10T00:11:01.2702993-07:00
custom_fields:
    harness_status: pending
id: 071-F
labels:
    - ci
    - chore
    - dogfood
    - cleanup
priority: low
status: queued
title: Rename dogfood CI aggregation gate from build to ci gate
updated_at: 2026-07-10T00:11:01.2702993-07:00
---

Dogfood-instance correction: rename the misleadingly-named always-running aggregation-gate job in .github/workflows/ci.yml from job id build (name: build) to job id ci-gate (name: ci gate), and rewrite the two comment blocks that justified the build name. The build name was only chosen to satisfy a now-deleted main-protected ruleset that required a check literally named build; it is now vestigial and misleading (autoharness has no build step — the expensive job is the test unittest suite). The template templates/ci/ci.yml.tmpl already uses ci-gate/ci gate, so no template change is needed. Purely aligns the dogfood instance with the template default. Chore kind; standalone, not part of 070-F/082-S.