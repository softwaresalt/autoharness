---
artifact_type: task
created_at: 2026-08-31T22:18:59.4106444Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: autoharness-stage-2h-v1
    size_source: agent
dependencies:
    - 158.002-T
id: 158.001-T
parent_id: 158-F
priority: medium
status: queued
title: Make the harvest sizing gate fail closed when the registry advertises sizing
updated_at: 2026-09-01T00:01:12.309358Z
---

Convert the two-axis rule from advisory prose into a gate: (1) per task, enum-validate BOTH size and complexity BEFORE any write and reject rather than coerce or default; (2) split triggers become checked conditions - size implying more than two hours forces a split regardless of complexity, and complexity:high forces a split or an explicit de-risking step regardless of size. FAIL CLOSED when the active registry ADVERTISES features.sizing; DEGRADE LOUDLY when it does not - preserve enum-validated values as clearly-labelled prose in the description AND report the degradation explicitly in the harvest report, never silently. AUTHORITY DECISION (P1, settled in review-fix cycle 2 - MANDATORY ACCEPTANCE, resolves DSE-S8-2's open question): the ADVERTISED registry flag features.sizing is the SOLE AUTHORITATIVE SIGNAL for whether the fail-closed branch is armed. The mere PRESENCE of sizing parameters on an operation (for example backlogit_update_item exposing size / size_source / size_ruleset_version / complexity) NEVER enables the gate on its own and MUST NOT be used as an alternative or fallback trigger. Rationale: the registry is the declared contract every generated agent resolves against, whereas operation parameters are an implementation surface that varies by tool build; deriving the gate from parameters would make the same workspace fail closed or degrade depending on which binary is installed. MEASURED CONSEQUENCE FOR THIS WORKSPACE (re-verified in cycle 2 against .autoharness/backlog-registry.yaml): the features block declares 15 keys and NO sizing key at all, while backlogit_update_item does accept the sizing params - so under this decision THIS workspace takes the DEGRADE-AND-REPORT path until SHIP-7 regenerates the registry, and the gate is present but inert locally. That is the correct, honest outcome, not a bug; it is sequenced away by the SHIPMENT-level blocks edge 166-S blocked by 165-S. Ship a test asserting BOTH directions: a registry advertising features.sizing arms the halt, and a registry that omits the key degrades-and-reports EVEN IF the operation surface accepts sizing params. PLAN REVIEW FINDING 1 (P1 - torn state): validate both enums BEFORE the create call so a rejection happens before any write; if a write nonetheless fails mid-sequence the halt message must name the item ID and the exact remaining call so the state is recoverable and idempotently completable. BINDING H2: respect the three-call write sequence exactly - create with no sizing params; one update setting size together with size_source: agent and a non-empty size_ruleset_version; a SEPARATE update setting complexity. These seams are mutually exclusive. BINDING H1: enforce-on-new-only, zero day-one blast radius, no retroactive sweep of the existing corpus. FINDING 5: declare a single size_ruleset_version identifier in the harvest documentation and use it consistently. NON-GOALS: no schema change, no auto-splitting (the gate halts and reports; S2 also forbids auto-split and the two must not disagree), no threshold derivation (that is 6A2D62DD, which stays active and unconsumed). SEQUENCING (SHIP-8 plan H3, corrected in cycle 2): BLOCKED BY 158.002-T, which is now the RED-TEST HALF ONLY and must be observed red first; this task turns tests T1 and T3 green; the shipment-assembly budget check and test T2 are the separate successor task 158.003-T, which is blocked by THIS task. SIZE/COMPLEXITY (two-axis, ruleset autoharness-stage-2h-v1): size M, complexity medium. SAFETY MODE: careful + freeze-scope bounded to templates/skills/harvest/SKILL.md.tmpl and templates/agents/_stage.agent.md.tmpl plus their mirrors (SHIP-8 plan H5), because this task edits the decomposition contract every future Stage run reads. Source: 2E67938C. Plan: docs/plans/2026-08-31-ship8-stage-size-complexity-enforcement-plan.md