---
artifact_type: task
created_at: 2026-05-17T17:49:14.3205204-07:00
id: 039.006-T
labels:
    - release
    - closure
    - backlog
    - ops
parent_id: 039-F
priority: medium
status: active
title: 'Post-release closure: verify distribution, record release commit, finalize backlog state'
updated_at: 2026-05-17T17:56:25.6953163-07:00
---

Perform all post-release closure steps after the GitHub Actions release workflow succeeds and the GitHub Release is published.

## Verification steps (human confirmation required for PyPI)

### 1. PyPI package availability
```
uv tool install autoharness==1.4.3
# or dry-run on a clean env:
uv pip install autoharness==1.4.3 --dry-run
```
Confirm `autoharness==1.4.3` resolves from PyPI (not Git URL).

### 2. Upgrade path confirmation
```
uv tool upgrade autoharness
```
Should upgrade cleanly to `1.4.3` without submodule traversal errors.

### 3. GitHub Release record
Record the release workflow run URL and merge commit SHA on shipment 035-S description:
- Merge commit SHA (from 039.004-T merge)
- Tag: `v1.4.3`
- Release workflow run URL
- GitHub Release URL: `https://github.com/softwaresalt/autoharness/releases/tag/v1.4.3`

## Backlog state finalization

- Move feature `039-F` to `done`
- Move shipment `035-S` to `shipped` (via `backlogit_ship_shipment` with merge commit SHA)
- Update `033-S` / `035-F` backlog descriptions if any cross-references need updating

## Uncommitted local backlog artifacts

The operator noted that `.backlogit/queue/033-S.md` has an uncommitted metadata update and `.backlogit/stash.jsonl` is modified locally. After post-release closure, Ship should commit these local backlog artifacts:
```
git add .backlogit/
git commit -m "chore(backlog): post-merge closure for 035-S (release-1.4.3)"
```
Push as a separate `chore/035-S-post-merge-closure` branch with its own PR (following the post-merge branch protocol from v1.3.2 Ship agent template). This closes the uncommitted state on main.

## Session memory

Write session memory entry to `docs/memory/` capturing: shipment 035-S, tasks 039.001–039.006, release commit SHA, decisions, and gate notes for the next release cycle.

## Width constraint

No code changes. Backlog state updates, git tag/backlog commit, and session memory only.