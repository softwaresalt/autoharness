---
artifact_type: task
created_at: 2026-05-17T15:10:36.9214179-07:00
id: 038.002-T
labels:
    - graphtor-docs
    - instruction-authoring
    - capability-pack
parent_id: 038-F
priority: medium
status: queued
title: 'GD-B: Install graphtor-docs capability pack — resolve template variables and render instruction file'
updated_at: 2026-05-17T15:10:36.9214179-07:00
---

## What

Render `templates/instructions/graphtor-docs.instructions.md.tmpl` into `.github/instructions/graphtor-docs.instructions.md` with both template variables resolved.

### Variable resolution (from GD-A outputs)

- `{{GRAPHTOR_SOURCES_PATH}}` → value discovered in GD-A (e.g. `.graphtor/sources.yaml`) — if not found, use `.graphtor/sources.yaml` as the conventional default path with a note
- `{{GRAPHTOR_BINARY_PATH}}` → value discovered in GD-A (e.g. `D:/Tools/graphtor.exe`) — if not found, use the string `graphtor` (assumes PATH) with a note

### Render step

Substitute both placeholders and write the file to `.github/instructions/graphtor-docs.instructions.md`. Preserve the footer comment (`Generated by autoharness | Template: graphtor-docs.instructions.md.tmpl`).

Then update `.autoharness/harness-manifest.yaml` artifacts list:
```yaml
- path: ".github/instructions/graphtor-docs.instructions.md"
  primitive: 1
  template: "instructions/graphtor-docs.instructions.md.tmpl"
  checksum: "<SHA-256 of installed file>"
  note: "graphtor-docs capability pack overlay instruction"
```

## Acceptance Criteria

- `.github/instructions/graphtor-docs.instructions.md` exists
- **Zero** `{{VARIABLE}}` placeholders remain — both `{{GRAPHTOR_SOURCES_PATH}}` and `{{GRAPHTOR_BINARY_PATH}}` resolved
- File has valid YAML frontmatter (`description`, `applyTo: '**'`)
- Checksum entry added to `harness-manifest.yaml` artifacts list
- Footer comment preserved

## Constraint

Single new file + manifest update. ~20–25 min. Width-isolated.

## Dependency

GD-A must be complete first (template variable values required).