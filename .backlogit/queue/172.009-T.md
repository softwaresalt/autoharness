---
artifact_type: task
created_at: 2026-09-18T14:50:35.1155315Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 172.003-T
    - 172.004-T
    - 172.005-T
id: 172.009-T
labels:
    - validation
    - fail-closed
    - enforcement
    - checkpoint
parent_id: 172-F
priority: medium
status: queued
title: 'Resume-hint T5c (ENABLE): wire the validator into both producer pre-write paths'
updated_at: 2026-09-18T17:25:32.2405187Z
---

ENABLE ENFORCEMENT -- SEPARATE FROM IMPLEMENTATION BY DESIGN. 172.005-T builds validate_checkpoint_payload(payload, origin) as a callable boundary that is NOT yet wired into any write path. THIS task performs the single switch-on step: wire the validator into BOTH producer paths pre-write so a non-conforming payload cannot be written. THE VALIDATOR CANNOT BE ENABLED BEFORE BOTH PRODUCERS ARE UPDATED. That is encoded, not merely asserted: this task is blocked by 172.003-T (Stage producer guarantee), 172.004-T (Ship producer guarantee), 172.005-T (the validator itself) and 172.006-T (the suite observed green). Because enforcement is a distinct node, there is no window in which a live producer is validated against a contract it has not yet adopted, and no contradictory prose of the form "implement and enforce, but only after later tasks land". Blocks 172.007-T: the live-corpus audit is only meaningful once enforcement is actually on. Scope: src/autoharness/ wiring plus the two producer call sites. Stash 71200CBB. Plan docs/plans/2026-09-17-checkpoint-resume-hint-contract-plan.md (revision 5). Verdict manifest docs/reviews/2026-09-17-checkpoint-resume-hint-contract-plan-review.md (attempt 05, REMEDIATED-PENDING-REVIEW at plan revision 5 — independent review cycle 3, the final authorized review-fix cycle, returned BLOCKED at plan revision 4; Stage remediation cycle 3 closed every finding and raised the plan to revision 5. Stage does not review its own remediation, so no PASS is asserted at revision 5 and the next reviewer pass is attempt 06). REVISION 5 CORRECTION (attempt-05 finding): the switch-on step delivers the EXECUTABLE ADAPTER autoharness checkpoint create --state-dump <path-or-json> [--origin harness] and repoints BOTH producer templates at it, replacing their direct backlogit_create_checkpoint and backlogit checkpoint create invocations. The adapter validates STRICTLY PRE-WRITE through validate_checkpoint_payload, exits non-zero and writes NOTHING on a failing outcome, and otherwise performs the OFFICIAL backlogit create call itself; it adds no storage, no schema, no field and no default. DEPENDENCY SET CORRECTED: blocked by 172.003-T, 172.004-T and 172.005-T ONLY. The revision-4 edge onto 172.006-T is REMOVED, because 172.006-T asserts the wiring THIS task creates and now blocks on it instead.