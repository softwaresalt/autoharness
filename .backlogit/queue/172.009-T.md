---
artifact_type: task
created_at: 2026-09-18T14:50:35.1155315Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 172.003-T
    - 172.004-T
    - 172.006-T
    - 172.002-T
id: 172.009-T
labels:
    - validation
    - fail-closed
    - enforcement
    - checkpoint
parent_id: 172-F
priority: medium
status: queued
title: 'Resume-hint T9 (ATOMIC ACTIVATION): contract, both producers, every registry and declaration, markers and manifest in one change set'
updated_at: 2026-09-19T03:15:08.8482446Z
---

ATOMIC ACTIVATION - ONE CHANGE SET, EVERY AFFECTED SURFACE TOGETHER.

THE ACTIVATION INVARIANT, STATED ONCE: at NO COMMIT BOUNDARY may a contract prohibiting the raw create path exist while any producer still lacks a wired adapter. Everything before this task is inert - nothing prohibited, nothing rewired, raw create still working. This task is the ONLY task that changes that, and it changes every affected surface in a single change set.

IN ONE CHANGE SET:
1. UPDATE THE AUTHORITATIVE CHECKPOINT PAYLOAD CONTRACT - the installed instruction AND its template - to name the adapter as the producer path, removing the raw create advertisement. Both copies change together, byte-identically in the affected blocks.
2. REWRITE BOTH PRODUCER CALL SITES - Stage's Step 6 checkpoint write and Ship's closure-step checkpoint write - in BOTH templates AND BOTH installed mirrors, replacing the direct backlogit_create_checkpoint / backlogit checkpoint create invocation with an adapter invocation. After this, no producer path constructs a payload and writes it without passing the gate.
3. WIRE THE STARTUP SURFACES onto autoharness checkpoint scan --json per the contract text already adopted in 172.002-T.
4. RE-DERIVE AND UPDATE EVERY REGISTRY MAPPING: inventory ALL installed AND template registries and update each one's create_checkpoint mapping to the GUARDED operation. The inventory is derived by enumeration, not by naming the registries known today.
5. UPDATE EVERY AGENT TOOL DECLARATION - installed and template - to declare the guarded operation rather than the raw one.
6. ADD the bounded producer markers and their inventory entries for the newly wired surfaces, and SWITCH ON the prohibition assertion in the reconciling verifier.
7. REGENERATE INSTALL-MANIFEST CHECKSUMS for every modified tracked file, so the manifest does not report drift against files this activation itself authored. Where a modified surface is not manifest-tracked, that is recorded explicitly rather than assumed.

RAW CREATE IS REACHABLE ONLY INSIDE THE ADAPTER after this task - exactly one raw-allowed inventory entry, the adapter implementation. CLI and MCP both reach it through the SAME adapter.

ROLLBACK IS THE INVERSE OF THE SAME SINGLE CHANGE SET: restore the raw advertisements, call sites, registry mappings and declarations together, after which the adapter and scanner are inert again. A partial rollback is exactly the intermediate state the invariant forbids.

Blocked by 172.006-T (the replacement is proven green), 172.002-T (the scan contract is adopted) and 172.003-T and 172.004-T (both producers have adopted the guarantee). Blocks 172.010-T. Scope: instructions + agents (templates and installed mirrors) + every registry + agent tool declarations + install manifest. Stash 71200CBB. Plan docs/plans/2026-09-17-checkpoint-resume-hint-contract-plan.md (revision 7). Verdict manifest docs/reviews/2026-09-17-checkpoint-resume-hint-contract-plan-review.md (latest_attempt 07, verdict REMEDIATED-PENDING-REVIEW at plan revision 7, awaiting independent attempt 08).