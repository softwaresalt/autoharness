---
artifact_type: task
created_at: 2026-09-19T02:52:04.187793Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: v1
    size_source: agent
dependencies:
    - 180.009-T
id: 180.010-T
parent_id: 180-F
priority: medium
status: queued
title: 'P3 T10 (ACTIVATE): one commit migrating all four review-verdict consumers onto the manifest gate'
updated_at: 2026-09-21T00:17:00.0000000Z
---

ACTIVATE in ONE commit: migrate templates/skills/harvest/SKILL.md.tmpl, templates/skills/plan-review/SKILL.md.tmpl, .github/skills/harvest/SKILL.md and .github/skills/plan-review/SKILL.md from the inline decision marker to the normalized manifest gate, simultaneously. Harvest is the load-bearing consumer: its Phase 1 currently reads decision: PASS from a Plan Review section inside the plan body and never opens the manifest, so a stale inline PASS admits a plan to decomposition today. SELF-HOSTING: this unit was authored and reviewed under the CURRENT inline-marker system; this commit is what switches the workspace onto the manifest system, and the new gate never admits its own plan. Sequential task edges are not atomic - all four surfaces move here or none do.

HARNESS-MANIFEST PARITY IS PART OF THE SAME ATOMIC UNIT (decision D11). Two of the four migrated surfaces - .github/skills/harvest/SKILL.md and .github/skills/plan-review/SKILL.md - are MANIFEST-TRACKED INSTALLED ARTIFACTS, each carrying an artifacts: entry in .autoharness/harness-manifest.yaml with a sha256 of its pre-migration content. The two templates are NOT tracked - the manifest tracks no template - so this commit REFRESHES EXACTLY TWO MANIFEST ENTRIES, not four. IN THE SAME COMMIT AND THE SAME ROLLBACK UNIT, rewrite each of those two checksums to the sha256 of the installed file AS WRITTEN BY THIS COMMIT, then VERIFY CHECKSUM PARITY under the INDEX-BOUND FAIL-CLOSED VERIFICATION CONTRACT below.

FAIL-CLOSED VERIFICATION BOUND TO THE EXACT INDEX SNAPSHOT - NOT GENERIC verify-workspace. Parity is adjudicated by a verification step this task owns, run AFTER ALL COMMIT MEMBERS ARE STAGED and BEFORE the commit is created, and bound to the EXACT INDEX SNAPSHOT THE COMMIT WILL RECORD. The generic verify-workspace command is NOT that gate: it reports whole-workspace install state read from the WORKING TREE and classifies drift, it does not adjudicate a named staged digest against a named artifacts: entry, and its exit status is therefore not a checksum assertion. The owned step: (1) ASSERTS THE STAGED SET IS EXACTLY THE FIVE INTENDED COMMIT MEMBERS - git diff --cached --name-only equals, as a set, templates/skills/harvest/SKILL.md.tmpl, templates/skills/plan-review/SKILL.md.tmpl, .github/skills/harvest/SKILL.md, .github/skills/plan-review/SKILL.md and .autoharness/harness-manifest.yaml, with a MISSING member, an EXTRA staged path or a DUPLICATE entry each FAILING; (2) REJECTS ANY UNSTAGED DIFFERENCE FOR THOSE MEMBERS - git diff --name-only restricted to the five must be EMPTY and none may be untracked, because if the working tree differs from the index for any member the bytes verified are not the bytes committed; (3) HASHES THE INSTALLED ARTIFACTS FROM THE INDEX - for each of the two touched installed skills, recompute SHA-256 over the STAGED BLOB READ AS :<path> (git cat-file -p :.github/skills/harvest/SKILL.md, git cat-file -p :.github/skills/plan-review/SKILL.md), NEVER a working-tree read; (4) PARSES THE MANIFEST FROM THE INDEX TOO, as :.autoharness/harness-manifest.yaml, so the entries compared against are the entries THIS COMMIT RECORDS rather than any the working tree may hold; (5) ADJUDICATES THE ENTRY SET TOTALLY - for each touched installed skill there is EXACTLY ONE artifacts: entry whose literal path matches, its primitive and template metadata are exactly as required and UNCHANGED by this commit, and its checksum equals the digest from (3) BYTE-FOR-BYTE, with a MISSING entry, an EXTRA entry (any artifacts: entry this commit adds or removes), a DUPLICATE path, a METADATA divergence or a CHECKSUM mismatch each FAILING; this activation installs NO NEW ARTIFACT - the two templates stay untracked - so NO NEW ENTRY IS REQUIRED HERE, while a future activation that does install one asserts the new entry EXISTS with exact path/template/primitive values before comparing its checksum; (6) RECORD THE INDEX IDENTITY AND FORBID RE-STAGING - record the index identity with git write-tree IMMEDIATELY AFTER (5) passes, then create the commit with NO INTERVENING INDEX MUTATION (no git add, git rm, git stash, checkout or restore); git commit -a and PATH ARGUMENTS to git commit are PROHIBITED, because both re-stage content after verification and would commit bytes the gate never adjudicated; (7) FAIL CLOSED BEFORE THE COMMIT EXISTS - steps (1)-(6) all run BEFORE the commit is created, so any failure among them, any unreadable input or any digest mismatch EXITS NON-ZERO AND THE COMMIT IS NOT CREATED, which is the whole of the pre-commit gate; and (8) ADJUDICATE ACCEPTANCE AFTER THE COMMIT IS CREATED - the commit is then created NORMALLY and this step DOES NOT PREVENT ITS CREATION and claims no such power; immediately afterwards compare git rev-parse HEAD^{tree} against the tree recorded in (6), and on a MISMATCH the commit records a tree the gate never adjudicated, so the activation is NOT ACCEPTED AND NOT PUBLISHABLE - it BLOCKS THE PUSH, blocks every downstream state token, verdict, manifest advance and handoff depending on this activation, and the LOCAL COMMIT MUST BE REVERTED OR CORRECTED AND THE WHOLE CONTRACT RE-RUN before any further step proceeds.

THE COMMIT THEREFORE CONTAINS EXACTLY FIVE FILES AND MIGRATES FOUR CONSUMERS - templates/skills/harvest/SKILL.md.tmpl, templates/skills/plan-review/SKILL.md.tmpl, .github/skills/harvest/SKILL.md, .github/skills/plan-review/SKILL.md and .autoharness/harness-manifest.yaml: manifest refreshes are COMMIT MEMBERS, not consumer surfaces, and they change no consumer count, no declared surface count and no gate arity anywhere in 180-F. A commit that migrates either installed skill WITHOUT its manifest refresh leaves the manifest asserting a file this commit has already rewritten - an installed-artifact parity hole in the very file this unit is making the single authority - and is an IMMEDIATE REVERT, not a fixup commit. ROLLBACK COVERS ALL FIVE COMMIT MEMBERS: git revert of this single commit restores all four consumer surfaces AND both manifest checksums together. This binds a FUTURE implementation commit; it authorizes no staging-time edit to the live manifest and none has occurred.

Plan docs/plans/2026-09-18-review-authority-foundation-plan.md (revision 5, disposition REMEDIATED-PENDING-REVIEW; the exact manifest-parity contract is stated at revision 3 bound to the exact index snapshot at revision 4, and split into pre-commit prechecks plus post-commit acceptance at revision 5). Decision docs/decisions/2026-09-18-shared-execution-architecture-and-portfolio-reslicing-decision.md (revision 4, D11).