---
artifact_type: feature
created_at: 2026-07-29T10:12:51.4456095Z
custom_fields:
    harness_status: pending
id: 096-F
priority: medium
references:
    - docs/deferred/agent-intercom-opt-in-nonmcp.md
    - docs/deferred/agent-intercom-opt-in-nonmcp.patch
status: queued
title: agent-intercom opt-in / non-MCP capability-pack reclassification (stash DD75C983)
updated_at: 2026-07-29T10:25:15.0082106Z
---

<!-- BEGIN:description -->
Reclassify the `agent-intercom` capability pack from a `full`-preset default to an **opt-in extra add-on**, and reflect that it **no longer supports an MCP server** (it operates over a non-MCP intercom/ACP tool surface, carrying no `mcp_requirements`). It remains a **woven overlay** when explicitly enabled — it is NOT demoted to a detached single-file add-on.

Resumed from backlogit stash `DD75C983`. This is a partially-complete change made durable via committed artifacts:

- Authoritative build plan: `docs/deferred/agent-intercom-opt-in-nonmcp.md` (self-contained: 13-file change list, "Additional required scope — deploy wrappers", resume steps, checksum-refresh + verify_workspace).
- Completed 13-file patch: `docs/deferred/agent-intercom-opt-in-nonmcp.patch` (registry `default_in_preset: []` + `mcp_requirements: []`, schema/profile/docs/skills/instructions edits, targeted test updates). This portion passed targeted suites locally; it is applied verbatim, not re-derived.

Remaining work NOT in the patch: the deploy wrappers still make agent-intercom non-opt-in (they default omitted pack input to `all` and enumerate every registry pack for non-`starter` presets without consulting `default_in_preset`). This feature's tasks finish the wrapper opt-in fix + wrapper tests, refresh the 3 manifest checksums, and run verify_workspace.

Ship must open this as its OWN dedicated PR. Do NOT bundle with the 079 telemetry ship work.
<!-- END:description -->

<!-- BEGIN:notes -->
## Plan hardening (P-006) — execution-plan hardening, design unchanged  The design already exists in docs/deferred/agent-intercom-opt-in-nonmcp.md; hardening targets EXECUTION risk only.  1. FOUR-FILE wrapper scope (not two). The durable plan and stash text name only scripts/deploy-harness.{sh,ps1}, but the identical wrapper logic is mirrored in the product templates templates/scripts/deploy-harness.{sh,ps1}.tmpl, and tests/test_deploy_harness_scripts.py asserts script<->template parity. Fixing only the two scripts would break rendering/parity tests and leave freshly-installed harnesses non-opt-in. Task 096.002-T explicitly requires all 4 files.  2. Omitted-vs-explicit-all contract made testable. The ambiguity (default sentinel "all" masks omission) is pinned by concrete acceptance criteria and dedicated tests (096.003-T): omitted+full/standard excludes agent-intercom; explicit all includes it; starter empty. Enumerators must parse default_in_preset via deterministic line scan (no new YAML dependency), matching existing Get-RegistryPacks/registry_packs style.  3. Checksum refresh made explicit and verifiable. 096.004-T requires raw-bytes sha256 of on-disk CRLF files for the 3 manifest-tracked mirrors, plus reconciling the workspace-profile.yaml checksum line, and treats patch-carried checksum values as authored (recompute against final bytes). Gated by test_manifest_capability_pack_enum.py + verify_workspace (0 blockers, no unresolved {{VARIABLE}}).  Ordering enforced via dependencies: 001 (apply patch) -> 002 (wrapper logic) -> 003 (wrapper tests) -> 004 (checksum + verify). Each task is width-isolated and within the 2-hour rule.  Ship must open a dedicated PR; do NOT bundle with the 079 telemetry ship work.    ## Plan-review outcome (multi-persona) — READY  Personas run: scope/architecture, correctness/completeness, test-coverage, risk/safety.  - Scope/architecture: tasks map 1:1 to remaining work, width-isolated (patch-apply / wrapper-logic / tests / checksum+verify), each within the 2-hour rule. Overlay principle preserved (opt-in yet woven, non-MCP). PASS. - Correctness/completeness: the critical execution gap (4-file wrapper scope incl. product templates) is captured; checksum scope verified COMPLETE — deploy wrappers are NOT manifest-tracked, so only the 3 mirrors + workspace-profile.yaml need refresh. verify_workspace gate present. PASS. - Test-coverage: 096.003-T adds behavior tests that fail pre-fix / pass post-fix; targeted suite enumerated in 096.004-T. PASS. - Risk/safety: moderate blast radius hardened; dependency ordering enforced (001->002->003->004); dedicated-PR / no-079-bundle constraint recorded. PASS.  Findings: P0=0, P1=0. Advisory only: (P2) task 096.001-T applies the patch which touches harness-manifest.yaml checksums, and 096.004-T re-verifies them — reconciliation is explicit in 096.004-T, not a conflict. Verdict: READY. 

## Plan-review (amended, PR #252 review-fix cycle 1) — READY

Copilot plan-review raised 1 valid P-006 finding on 096.002-T: the precedence contract could be read as forcing `starter` empty even with explicit packs, contradicting the installer contract and re-introducing 082-S FU-1 (starter discarding explicit selections). Verified against `.github/skills/install-harness/SKILL.md:551` ("explicit capability_packs used as-is regardless of preset") and `docs/memory/082-S-closure.md:72` (FU-1 defect).

Resolution:
- 096.002-T rewritten with a single ordered precedence contract uniform across ALL presets: (1) EXPLICIT input always honored regardless of preset (starter must not discard explicit all/subset); (2) OMITTED input resolves to the preset's default_in_preset members (starter -> empty). Removed the unconditional "starter selects no packs regardless of input" language.
- 096.003-T adds starter cases: starter+explicit all (all packs incl agent-intercom), starter+explicit subset (exact subset, guards FU-1), starter+omitted (empty); kept full/standard omitted-excludes + explicit-all-includes cases; references install-harness parity (SKILL.md:551).

Re-review personas (scope, correctness, test-coverage, risk): contract now internally consistent, matches installer semantics, and the explicit-subset starter test would fail on the old defect. Findings: P0=0, P1=0. Verdict: READY.
<!-- END:notes -->