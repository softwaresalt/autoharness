---
artifact_type: task
created_at: 2026-08-21T20:23:34.1594573Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 141.002-T
id: 141.003-T
labels:
    - bug
    - tests
    - isolation
    - backlog-root
parent_id: 141-F
priority: medium
references:
    - docs/plans/2026-08-21-full-suite-test-isolation-plan.md
status: queued
title: De-couple test_backlog_root.py from ambient cwd (16 sites) and shrink the guard allowlist
updated_at: 2026-08-21T20:23:34.1594573Z
---

Remove ambient-cwd coupling from `tests/test_backlog_root.py` (16 of the 58 sites) and shrink the isolation guard's allowlist accordingly.

**Plan:** `docs/plans/2026-08-21-full-suite-test-isolation-plan.md` Task 2 (+ amendments A4, H3 refinement)

## Steps
1. **Per-site containment review — mandatory before editing.** `test_backlog_root.py` exercises backlog-root RESOLUTION, where "the candidate directory is inside the workspace" is frequently part of the scenario under test. Treat containment as REQUIRED unless a site provably does not depend on it, and record a one-line justification per site.
2. Replace `dir=Path.cwd()` with `dir=Path(__file__).resolve().parents[1]` where containment is required; plain `tempfile.TemporaryDirectory()` only where it provably is not. Never relocate a containment test to system temp.
3. Remove `test_backlog_root.py` from the isolation guard's allowlist in the SAME change (amendment A4), leaving exactly `test_gates_topology.py` allowlisted.

## Acceptance criteria
- AC-a. Zero `dir=Path.cwd()` sites remain in `tests/test_backlog_root.py`.
- AC-b. `tests/test_test_suite_isolation_contract.py` is GREEN with an allowlist containing exactly `test_gates_topology.py`.
- AC-c. All 16 sites have a recorded containment determination with justification.
- AC-d. `test_backlog_root.py` passes IN ISOLATION before and after with an IDENTICAL pass count. No assertion is weakened.

## Dependency
Depends on 141.002-T (the guard and its allowlist mechanism must exist first, and the allowlist is a single shared file — serialising avoids a conflict).