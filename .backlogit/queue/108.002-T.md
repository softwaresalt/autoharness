---
artifact_type: task
created_at: 2026-08-03T22:28:31.2023084Z
custom_fields:
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 108.001-T
id: 108.002-T
labels:
    - telemetry
    - schema
    - non-conflation
parent_id: 108-F
priority: medium
status: active
title: Add non-conflated complexity dimension to event schema
updated_at: 2026-08-04T05:39:46.3194518Z
---

Add the structurally-SEPARATE optional task-level complexity field (top-level task_complexity_label enum trivial|low|medium|high|null plus complexity_source), explicitly NOT nested inside work_sizing_snapshot, to the tool-telemetry-event contract so size (effort) and complexity (reasoning/risk) never conflate. Apply the change to BOTH schema mirrors -- schemas/tool-telemetry-event.schema.json (root) AND schemas/tool-telemetry-event/1.0.0.schema.json (versioned) -- keeping them byte-identical except the $id so the parity test test_tool_event_root_and_versioned_mirrors_match_except_id stays green. Record and apply the schema-version decision in src/autoharness/schema_contracts.py (SCHEMA_CONTRACTS[tool-telemetry-event].known_versions): treat as additive-optional; if a new versioned file is required, add it WITHOUT deleting 1.0.0.schema.json and register the new version. Feature/shipment complexity maps to not_applicable (backlogit complexity is task-only). Additive/optional/backward-compatible. Update docs/telemetry-reference.md and the schema-contract tests (tests/test_telemetry_schema_contracts.py). Depends on the evidence field map. Family: schema. Runtime model + serialization/composer handling is split out to 108.004-T.