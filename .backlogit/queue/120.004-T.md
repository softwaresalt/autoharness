---
artifact_type: task
created_at: 2026-08-10T07:10:52.7953172Z
custom_fields:
    complexity: high
    origin_feature: 117-F
    size: M
    size_ruleset_version: 2h-width-isolated-v1
    size_source: agent
dependencies:
    - 120.002-T
    - 120.003-T
    - 119.003-T
    - 120.001-T
    - 119.001-T
    - 119.005-T
    - 120.005-T
id: 120.004-T
labels:
    - shipment-3
    - P1
    - orchestration
    - app-service
    - H3
    - H8
parent_id: 120-F
priority: high
references:
    - docs/plans/2026-08-09-copilot-cli-supervisor-control-plane-plan.md
    - docs/plans/2026-08-09-copilot-cli-supervisor-control-plane-hardening.md
status: active
title: 'T15 supervise/app.py: run_session() orchestration application service'
updated_at: 2026-08-13T06:52:11.8060311Z
---

SIZE: M. COMPLEXITY: high. (Two independent axes; also emitted as structured backlog fields.) Shipment 3 (P1). Create src/autoharness/supervise/app.py exposing run_session(...) -> SupervisorResult as THE SINGLE ORCHESTRATOR composing lock -> bootstrap -> sidecar preflight -> resolve -> spawn -> supervise -> drain, driving the session state machine, emitting events, writing the journal, and honoring cancellation/restart. This is the function every adapter calls; NO orchestration logic may live in cli.py, start.ps1, or start.sh. Returns the typed envelope with the child's exit code propagated VERBATIM (H3). H8 DE-RISKING: this task is PURE COMPOSITION over dependencies that are already implemented and independently tested (process, session, journal, events, recovery, bootstrap, sidecar, resolve). It introduces NO new algorithm - if it grows one, that is a decomposition failure and the task MUST be split rather than expanded. Integration tests run the whole orchestration against the scripted FAKE child: happy path, sidecar degraded, unresolvable Copilot, lock contention -> REFUSED, cancel, and non-zero child exit. F32/F33 RULING - MANDATORY APPROVAL WIRING (supersedes the F21 disposition, which was WRONG): the earlier resolution moved the approval TYPES into 118.003-T contracts.py and treated F21 as discharged. It was not. F21 was never a definition-ordering defect - it was a RUNTIME WIRING defect, and moving a type definition upstream does not create a caller. THIS TASK NOW DEPENDS ON 120.005-T (edge direction REVERSED; the former 120.005-T -> 120.004-T edge is REMOVED), so the approval service exists and is tested before this orchestrator is built. run_session() MUST ROUTE EVERY GATED ACTION THROUGH THE 120.005-T APPROVAL SERVICE. OMISSION MUST BE STRUCTURALLY IMPOSSIBLE, not merely discouraged: (a) the approval service is a REQUIRED parameter of run_session() with NO DEFAULT, NO None-accepting overload, and NO module-level fallback instance - a test asserts via inspect.signature that the parameter exists and its default is inspect.Parameter.empty, so a caller cannot omit it and the function cannot self-supply a permissive one; (b) the set of GATED ACTIONS is declared ONCE as an enumerated catalog in 118.003-T contracts.py - which 118.003-T is now EXPLICITLY OBLIGED to define and export as its own acceptance criterion (P1-3 ruling, 2026-08-12; the producer obligation previously existed only in this consumer's prose, so the catalog had a consumer and no producer) - and this task adds NO SECOND LIST: it IMPORTS the catalog and a test asserts the enumeration it dispatches over is THE SAME REGISTRY OBJECT/IDENTIFIER SET as the upstream contract rather than a local copy, so a divergent duplicate fails the suite; a further test asserts EXACT SET EQUALITY between the catalog's exported identifier set and the set of actions run_session actually routes through the approval service - equality in BOTH directions, so an action present in the catalog but never dispatched FAILS, and an action dispatched but absent from the catalog FAILS (ruling B, 2026-08-12, superseding the earlier "catalog is non-empty ... covers it exhaustively" wording, which was VACUOUS because a one-entry catalog satisfied it while real gated behavior such as 119.006-T's session_restart went unrouted). The frozen initial set that both sides must agree on is exactly {session_restart, force_unlock} as enumerated and justified in 118.003-T, so OMISSION IS NOW A FAILING TEST RATHER THAN A SILENT PASS; and an UNKNOWN/UNREGISTERED gated action FAILS CLOSED - dispatching an identifier absent from the catalog must raise or refuse, never fall through to an unguarded side effect, asserted with a negative-control action that is deliberately not registered; (c) a RECORDING/SPY approval service is injected and the integration suite asserts that for EVERY action in the catalog an ApprovalRequested was raised and a decision consumed BEFORE the side effect is observable - the assertion is on OBSERVED CALLS AND ORDERING, not on source text, so a lexical or import-only check cannot satisfy it; (d) NEGATIVE CONTROLS ARE MANDATORY AND MUST FAIL THE SUITE WHEN ABSENT - a DENY decision must make the gated side effect NOT occur and the session resolve to REFUSED; an approval service that RAISES must FAIL CLOSED (never proceed, never swallow); an approval service that is UNAVAILABLE must likewise FAIL CLOSED - if the service cannot be obtained, is not injected, or is unreachable at the moment a catalogued gated action is dispatched, the orchestrator REFUSES the action and does not perform the side effect, asserted with a negative control that supplies a missing/unreachable service and proves no gated side effect is observable; and a deliberately-unwired fixture orchestrator MUST be rejected by the same assertions, proving they can actually detect the defect F21 described rather than passing vacuously; (e) H2 FAIL-CLOSED end-to-end: a NON-INTERACTIVE integration run must resolve each gated action to its declared safe default or to REFUSED and journal it as auto-resolved, asserted at the run_session level - not only inside 120.005-T's own unit tests - because F21's whole point was that a guarantee proven only in an unreachable module is not a guarantee. ACCEPTANCE IS NOT MET by any test that merely imports approvals.py, references the contract types, or greps for a symbol.