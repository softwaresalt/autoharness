---
artifact_type: task
created_at: 2026-08-21T20:24:35.6076266Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 142.001-T
labels:
    - bug
    - cli
    - verify-workspace
    - measurement
    - regression-guard
    - test-first
parent_id: 142-F
priority: medium
references:
    - docs/plans/2026-08-21-verify-workspace-variable-derivation-plan.md
    - .backlogit/queue/023-DL.md
status: queued
title: Variable inventory, per-variable classification, and the unresolved-placeholder ratchet guard
updated_at: 2026-08-21T20:24:35.6076266Z
---

MEASUREMENT AND GUARD ONLY — this task changes NO derivation logic. It must run first because it defuses the parity-contract landmine before any code moves.

**Plan:** `docs/plans/2026-08-21-verify-workspace-variable-derivation-plan.md` Task 0 (+ amendments B1, B5)

## Test-first requirement
Add `tests/test_template_variable_derivation_contract.py`:
- **T0a (ratchet)** — render the full staged tree through `_derive_template_variables` + `_render_template` and assert the set of unresolved placeholders EQUALS a checked-in EXPECTED SET. Seed it with the current 62. Green on day one; each later task removes entries. A NEW unresolved variable appearing fails immediately because the set is EXACT, not a count bound.
- **T0b (amendment B5)** — expressed as a MONOTONE RATCHET over the same expected set, NOT as a standing red assertion. The final derivation task empties the set, at which point T0b degenerates to the zero assertion. Every intermediate commit stays green.

## Steps
1. Produce a per-variable CLASSIFICATION TABLE covering all 62 variables. For each: variable name, the `.github/skills/install-harness/SKILL.md` resolution-table row that governs it, and one of `RESOLVED-FROM-SOURCE` or `DERIVE-TO-EMPTY-STRING`.
   - Every raw escalation/role slot (`LEGACY_ESCALATION_*`, `STAGE_ESCALATION_*`, `SHIP_ESCALATION_*`, `STAGE_*`, `SHIP_*`) is classified `DERIVE-TO-EMPTY-STRING` per constraint C3 and amendment B6.
2. **BLOCKING MEASUREMENT (023-DL R1 / amendment B1).** For each of the 62, determine whether it appears in ANY of the four CLEAN template/dogfood pairs asserted byte-identical by `tests/test_scope_containment_policy_contract.py::test_clean_pairs_are_byte_identical_via_render_template`. Record the intersection EXPLICITLY, including "empty" if empty.
   - **Amendment B1.** If the intersection is NON-EMPTY, the affected derivation task STOPS and returns to Stage — UNLESS the divergence is a pure placeholder-to-same-literal substitution (the correct rendered value is provably identical to the literal already in the dogfood copy). Any other divergence is a normative edit and re-enters P-021 capture.

## Acceptance criteria
- AC0a. The classification table covers all 62 variables, each with a cited SKILL.md row.
- AC0b. The clean-pair intersection is recorded EXPLICITLY (an unrecorded result is not acceptable, including a null result).
- AC0c. T0a is GREEN with exactly 62 entries in the expected set; T0b is GREEN as a ratchet at 62.

## Baseline for reference
83 unresolved occurrences / 62 distinct variables / 10 staged files. `.autoharness/config.yaml` 45; `_orchestrator.agent.md` 15; `escalation-protocol.instructions.md` 6; `copilot-instructions.md` 4; `constitution.instructions.md` 4; `graphtor-docs.instructions.md` 2; `AGENTS.md` 2; `start.ps1` 2; `start.sh` 2; `github-pr-automation.instructions.md` 1.