---
artifact_type: task
created_at: 2026-08-28T03:53:00.7143116Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 148.002-T
    - 148.007-T
id: 148.008-T
labels:
    - S0
    - tests
    - verification
parent_id: 148-F
priority: medium
references:
    - docs/plans/2026-08-27-policy-registry-and-review-persona-layer-plan.md
    - tests/
status: active
title: U8 — Verification and regression tests for both gaps
updated_at: 2026-08-29T09:07:26.90545Z
---

Add regression tests proving both gaps are closed: registry content/placeholder assertions, persona route-resolution with explicit PRIMARY_LANGUAGE_LOWER expansion-or-exemption, named-reader (Law-2) assertions, and _resolve_policy_registry precedence. Domain: tests. Files: up to 3.

<!-- BEGIN:acceptance-criteria -->
* All **5** scenarios green under `PYTHONPATH=src python -m unittest discover -s tests`.
* **Scenario 1 — registry content AND value-level binding (EXTENDED 2026-08-28, review-fix cycle 3 — D9-A/D9-B).** In addition to P-001..P-021 and the `dark_factory_policy_contract` `must_contain` tokens:
  * **D9-A value assertion (REQUIRED)**: the rendered compile/red-phase clauses carry the **correct compile-check value**. Assert the rendered registry contains the literal `python -m py_compile src/autoharness/cli.py` at both `{{BUILD_CHECK_COMMAND}}` sites — template **L48** (harness-architect postcondition, "the harness compiles") and template **L88** (red-phase precondition, "exits 0 AND `{{TEST_COMMAND}}` exits non-zero") — **and** assert the rendered registry contains **no** occurrence of `pip install -e .`. **A wrong binding cannot be caught by the placeholder scan**: a wrong value resolves exactly as cleanly as a right one, so resolution-shaped assertions are structurally blind to it. This assertion is what makes the binding checkable. (`pip install -e .` is the manifest's separate `BUILD_COMMAND` and is **not** bound anywhere in this render.)
  * **D9-B named exemption**: the registry's zero-`{{...}}` assertion runs under the named, commented **`EXEMPT_POLICY_PROSE_META_TOKEN`** rule — a **closed 1-token allow-list** for the bare ellipsis `{{...}}` carried from template **L359** (P-013.5 fail-closed-verification prose, "...is empty or an unresolved `{{...}}` placeholder"). That token is intended literal registry content and survives the render, so a **bare** zero-placeholder assertion over the registry is **impossible as written** and would fail permanently. Every other `{{...}}` match in the registry MUST fail.
* **Scenario 2 declares its placeholder branch** (EXPAND or EXEMPT) explicitly in code and in a comment. A test that silently omits the `{{PRIMARY_LANGUAGE_LOWER}}` citation, or that asserts it resolves as a raw path, FAILS this unit.
* **Scenario 3 asserts reader existence, never absence.** Any assertion that `correctness-reviewer`, `maintainability-reviewer`, or `technology-reviewer` is absent FAILS this unit.
* **Scenario 5 (ADDED 2026-08-28, review-fix cycle 2) — pinned-binding conformance.** For each of the five D8 variables, assert the **installed** artifact carries the pinned value **verbatim**:
  * `python-reviewer.agent.md` carries the D8-B `{{LANGUAGE_SAFETY_CHECKS}}`, `{{LANGUAGE_IDIOM_CHECKS}}`, `{{LANGUAGE_ERROR_HANDLING_CHECKS}}`, `{{LANGUAGE_PERFORMANCE_CHECKS}}` bullet lists exactly as recorded in the plan;
  * `concurrency-reviewer.agent.md` carries `asyncio, task, queue, thread, process`, **and** that string equals `_language_defaults("python")["concurrency_patterns"]` read at test time (**cross-check** — fails if pin and resolver diverge);
  * `templates/agents/review/technology-reviewer.agent.md.tmpl` and `templates/agents/review/concurrency-reviewer.agent.md.tmpl` **still contain all five placeholders unmodified** (D8-C: proves no template was hard-coded).
  A render that merely "looks reasonable" FAILS this scenario.
* **Placeholder scan — zero `{{...}}` across ALL 14 newly installed artifacts (1 registry + 13 personas), under TWO named rules and no others.** This scan is SEPARATE from scenario 2's route resolution and operates on installed artifact bodies — the scenario-2 EXEMPT branch does NOT relax it.
* **Rule 1 — `EXEMPT_OUTPUT_SCHEMA_EXEMPLARS` (D8-D; CORRECTED 2026-08-28, review-fix cycle 3).** Applies to the **13 persona artifacts** only. A matched token is exempt **iff BOTH**: (a) it is a member of the **CLOSED 3-token allow-list** — `{{file_path}}`, `{{line_number}}`, **`{{principle_number}}`** — **and** (b) it is inside a **fenced output-schema code block**. Neither condition alone suffices.
  * The prior revision of this criterion enumerated only **two** tokens and **omitted `{{principle_number}}`**, which occurs at `templates/agents/review/constitution-reviewer.agent.md.tmpl` **L43** in the same fenced JSON block as the other two. With that omission the scan false-positives on the installed `constitution-reviewer.agent.md` and the DoD's zero-placeholder rule is **unsatisfiable**. The enumeration is now complete.
  * All three are **exemplar tokens intentionally retained** as literal persona content, describing the JSON shape a reviewer subagent emits **at review time**. They are **NOT unresolved install variables**, they have **no install-time value**, and **NONE may be bound to fabricated content** — inventing a value for `{{principle_number}}` corrupts the persona output schema and is a **HARD STOP**. The test asserts the tokens are **present and literal**, not that they were substituted.
  * The allow-list is **CLOSED**: any token not on it is a genuine unbound variable and MUST fail.
* **Rule 2 — `EXEMPT_POLICY_PROSE_META_TOKEN` (D9-B).** Applies to the **policy registry** only; closed **1-token** allow-list (bare ellipsis `{{...}}`, template L359), as detailed under Scenario 1 above.
* **No third exemption.** Rules 1 and 2 are the complete set for the placeholder scan; neither may be widened, and neither is the same as scenario 2's route exemption. Applying any of the three outside its declared input surface FAILS this unit.
* **Checksum round-trip**: each manifest checksum recomputed from the installed file matches.
* **INV-3 exit-code invariance**: `verify-workspace` exit-code behavior compared pre/post; any change is a HARD STOP, not a finding.
* **Post-flight diff**: `verify-workspace` after vs. before; every delta classified as *expected status-change* (RK-B) or *finding*.
* Carried-forward findings recorded, NOT fixed: `workspace-profile.yaml` still declares `test.runner: pytest` while Q5 names the unittest command authoritative; `8AC574F1` residual skill gap; **RK-J** (`LANGUAGE_*_CHECKS` are Stage-reviewed prose, not resolver-derived — follow-up is to add the keys to `_language_defaults`, out of S0 scope).
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan unit **U8** — **CORRECTED 2026-08-28** (scenario 2 gains explicit placeholder handling; scenario 3 is INVERTED) — **EXTENDED 2026-08-28 review-fix cycle 2** (scenario 5 added; fenced-code exemption added) — **EXTENDED AGAIN 2026-08-28 review-fix cycle 3** (scenario 1 gains the D9-A value-level binding assertion and the D9-B registry meta-token exemption; the D8-D exemplar allow-list is completed to three tokens).

**5 scenarios:**

**1. Policy registry content + correct binding.** `.github/policies/workflow-policies.md` exists, contains P-001..P-021 and the `dark_factory_policy_contract` `must_contain` tokens (`P-017`, `Run pipeline in dark mode`, `DARK_MODE_ACTIVE`, `BRAINSTORM_HANDOFF_READY`, `DARK_MODE_COMPLETE`).

* **Zero unresolved `{{...}}` subject to `EXEMPT_POLICY_PROSE_META_TOKEN` (D9-B)** — closed 1-token allow-list for the literal ellipsis token carried from template L359.
* **Value-level binding assertion (D9-A)** — the rendered L48 compile postcondition and L88 red-phase precondition contain `python -m py_compile src/autoharness/cli.py`, and the file contains no `pip install -e .`. **This is the assertion the cycle-3 P1 finding required**: `{{BUILD_CHECK_COMMAND}}` had been bound to `pip install -e .` (the manifest's separate `BUILD_COMMAND`, via profile `build.command`), and a placeholder scan cannot detect a wrong-but-resolved value.

**2. Route-resolution with DECLARED placeholder handling.** Every persona path cited by any installed skill/agent under `.github/agents/subagents/` RESOLVES. This asserts a *property*, not a hardcoded list, so it will not rot as skills change.

**The trap this scenario must handle**: the raw citation scan also yields the literal token `.github/agents/subagents/{{PRIMARY_LANGUAGE_LOWER}}-reviewer.agent.md` from installed `install-harness/SKILL.md` L1203. That is **installer guidance describing the render mapping, not a live route**, and it can NEVER resolve as a raw path. The test MUST take one branch explicitly and by name — **asserting raw resolution of a placeholder path is an impossible assertion and is forbidden**:

* **(PREFERRED) EXPAND** — substitute `{{PRIMARY_LANGUAGE_LOWER}}` from `.autoharness/harness-manifest.yaml` `variables.PRIMARY_LANGUAGE_LOWER` (= `python`) before resolving, so the citation resolves to the real installed `python-reviewer.agent.md`. Strictly stronger: it also proves the L1203 mapping itself is satisfied.
* **(FALLBACK) EXEMPT** — skip citations containing an unexpanded `{{...}}` segment via a named, commented allow-rule identifying them as installer guidance rather than live routes.

Whichever branch is taken, the test records which one and why. A silent regex that merely happens to miss the token is NOT acceptable.

**3. Named-reader / Law-2 test — INVERTED.** The prior revision asserted three personas were ABSENT. That assertion is now false and MUST NOT be written; it would fail against the corrected S0 and would freeze the false premise into a permanent regression test. Assert instead:

* every installed persona under `.github/agents/subagents/` has a **named reader** — an installed-path citation OR a bare-filename citation in the install/tune drift contract;
* `correctness-reviewer.agent.md` and `maintainability-reviewer.agent.md` are **PRESENT**, so the `tune-harness` L462-L469 local-first review drift condition does not trigger;
* `python-reviewer.agent.md` is present and its manifest entry records `technology-reviewer.agent.md.tmpl` as its template source.

**4. Resolution precedence.** `_resolve_policy_registry`: installed-first, template-fallback.

**5. Pinned-binding conformance (the P2-2 guard).** The five D8 values must appear **verbatim** in the installed artifacts, the concurrency pin must equal the live `_language_defaults("python")` value, and both source templates must remain **unmodified**. Without this scenario, nothing mechanically prevents Ship from improvising unreviewed persona content into a normative review surface — which is exactly the defect this scenario exists to catch.

**Gate command (Q5)**: `PYTHONPATH=src python -m unittest discover -s tests`
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
**Depends on U2 and U7.**

**THREE distinct placeholder checks — do not conflate them** (exemption columns updated 2026-08-28, review-fix cycle 3):

| Check | Input | Placeholder policy |
|---|---|---|
| Hardening step 2 — placeholder scan | bodies of the 14 installed artifacts | zero `{{...}}` tolerated, under **exactly two named, commented rules**: **`EXEMPT_OUTPUT_SCHEMA_EXEMPLARS`** (D8-D — personas only; closed 3-token allow-list `{{file_path}}`, `{{line_number}}`, `{{principle_number}}`, and only inside a fenced output-schema code block) and **`EXEMPT_POLICY_PROSE_META_TOKEN`** (D9-B — registry only; closed 1-token allow-list, the bare ellipsis `{{...}}` at template L359). No third rule. |
| Scenario 2 — route resolution | persona path citations found in installed skills/agents | `{{PRIMARY_LANGUAGE_LOWER}}` EXPANDED (preferred) or EXEMPTED by named rule |
| Scenario 5 — pinned-binding conformance | the 5 D8 variables in installed artifacts + the 2 source templates | installed side: **zero** placeholders, verbatim pinned values; template side: placeholders MUST still be present and unmodified |

The scenario-2 exemption is about a *citation inside installer guidance prose*. The D8-D exemption is about *intended literal content inside a fenced JSON example*. The D9-B exemption is about *a meta-token in policy prose that names the concept of a placeholder*. None may be applied to a genuinely unbound variable, none may be applied outside its declared input surface, and applying any of them to the placeholder scan wholesale would defeat the scan.

**Cycle-3 correction to the D8-D allow-list.** It previously listed only `{{file_path}}` and `{{line_number}}`. `{{principle_number}}` (`constitution-reviewer.agent.md.tmpl` **L43**) was omitted, which made the DoD's zero-placeholder rule **unsatisfiable** for `constitution-reviewer.agent.md`. All three are retained exemplars with no install-time value; **none may be bound to fabricated content**. The allow-list is now closed at exactly three.

**Cycle-3 addition: a resolved placeholder is not a correct placeholder.** Scenario 1's D9-A value assertion exists because the realized cycle-3 P1 defect — `{{BUILD_CHECK_COMMAND}}` bound to `pip install -e .` instead of `python -m py_compile src/autoharness/cli.py` — would have passed **every** assertion this unit previously carried. Placeholder-shaped verification is blind to value correctness by construction. Where a binding is contested or semantically load-bearing (D2's `{{TEST_COMMAND}}`, D9's `{{BUILD_CHECK_COMMAND}}`), assert the **value**, not merely the absence of the token.

**Scenario 5 inverts the polarity between the two sides** — installed artifacts must have NO placeholders (modulo the two named rules), source templates must STILL have them. That pairing is what proves the values were bound at render time rather than hard-coded into the template (D8-C, technology-agnostic template discipline).

Learnings-Researcher P2 applied here: `docs/compound/2026-08-21-ship-executable-set-must-wire-into-actual-loop-variable.md` — the 'defined but not wired' class. Scenario 2 therefore tests the ROUTE, not file presence.

Learnings-Researcher P2 applied here: `docs/compound/012-S-portability-scan-allow-list.md` — new rules over an existing corpus carry historical blast radius. Addressed by pre-flight capture (U1) and the post-flight delta classification above.

RK-H (new): the P1 that blocked this plan was a *measurement-shape* defect — a path-shaped grep could not see bare-filename citations. Scenario 3 is written to measure readers across BOTH citation shapes so the same defect cannot recur silently.

RK-J (new, 2026-08-28 cycle 2): the four `LANGUAGE_*_CHECKS` pins are Stage-reviewed prose, not resolver-derived (`_language_defaults` has no performance key at all). Scenario 5 locks the shipped values so they cannot silently drift, but the follow-up — adding `safety/idiom/error/performance_checks` keys to `_language_defaults` — is **out of S0 scope** (it would change the resolver, blast radius beyond the persona layer). Record, do not fix.

**2-hour rule (re-assessed 2026-08-28, review-fix cycle 3)**: this unit is now **5 scenarios**, the widest in the shipment alongside U6's 5 files. It stays inside the rule. Scenario 5 is a verbatim-content comparison against values already pinned in D8-B plus one resolver cross-check and one template-unchanged assertion. The cycle-3 additions (D9-A value assertion; two named exemption rules) are **literal-string assertions over an already-rendered file** and a scan-filter predicate — no new discovery, no new domain, no unpinned decision. All still fit the existing `<=3` test-file budget, and the difficulty remains concentrated in scenarios 2 and 3, which are unchanged. **Size remains M; complexity remains medium. No split required.**

Do NOT fix the profile staleness in this shipment (D7).
<!-- END:implementation-notes -->