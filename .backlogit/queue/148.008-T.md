---
artifact_type: task
created_at: 2026-08-28T03:53:00.7143116Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 148.002-T
    - 148.007-T
id: 148.008-T
labels:
    - S0
    - tests
    - verification
parent_id: 148-F
priority: medium
references:
    - docs/plans/2026-08-27-policy-registry-and-review-persona-layer-plan.md
    - tests/
status: queued
title: U8 — Verification and regression tests for both gaps
updated_at: 2026-08-28T06:42:41.2305496Z
---

Add regression tests proving both gaps are closed: registry content/placeholder assertions, persona route-resolution with explicit PRIMARY_LANGUAGE_LOWER expansion-or-exemption, named-reader (Law-2) assertions, and _resolve_policy_registry precedence. Domain: tests. Files: up to 3.

<!-- BEGIN:acceptance-criteria -->
* All 4 scenarios green under `PYTHONPATH=src python -m unittest discover -s tests`.
* **Scenario 2 declares its placeholder branch** (EXPAND or EXEMPT) explicitly in code and in a comment. A test that silently omits the `{{PRIMARY_LANGUAGE_LOWER}}` citation, or that asserts it resolves as a raw path, FAILS this unit.
* **Scenario 3 asserts reader existence, never absence.** Any assertion that `correctness-reviewer`, `maintainability-reviewer`, or `technology-reviewer` is absent FAILS this unit.
* **Placeholder scan**: zero `{{...}}` across ALL 14 newly installed artifacts (1 registry + 13 personas). This scan is SEPARATE from scenario 2's route resolution and operates on installed artifact bodies — the scenario-2 EXEMPT branch does NOT relax it.
* **Checksum round-trip**: each manifest checksum recomputed from the installed file matches.
* **INV-3 exit-code invariance**: `verify-workspace` exit-code behavior compared pre/post; any change is a HARD STOP, not a finding.
* **Post-flight diff**: `verify-workspace` after vs. before; every delta classified as *expected status-change* (RK-B) or *finding*.
* Carried-forward findings recorded, NOT fixed: `workspace-profile.yaml` still declares `test.runner: pytest` while Q5 names the unittest command authoritative; `8AC574F1` residual skill gap.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan unit **U8** — **CORRECTED 2026-08-28** (scenario 2 gains explicit placeholder handling; scenario 3 is INVERTED).

**4 scenarios:**

**1. Policy registry content.** `.github/policies/workflow-policies.md` exists, contains P-001..P-021 and the `dark_factory_policy_contract` `must_contain` tokens (`P-017`, `Run pipeline in dark mode`, `DARK_MODE_ACTIVE`, `BRAINSTORM_HANDOFF_READY`, `DARK_MODE_COMPLETE`), and has **zero unresolved `{{...}}`**.

**2. Route-resolution with DECLARED placeholder handling.** Every persona path cited by any installed skill/agent under `.github/agents/subagents/` RESOLVES. This asserts a *property*, not a hardcoded list, so it will not rot as skills change.

**The trap this scenario must handle**: the raw citation scan also yields the literal token `.github/agents/subagents/{{PRIMARY_LANGUAGE_LOWER}}-reviewer.agent.md` from installed `install-harness/SKILL.md` L1203. That is **installer guidance describing the render mapping, not a live route**, and it can NEVER resolve as a raw path. The test MUST take one branch explicitly and by name — **asserting raw resolution of a placeholder path is an impossible assertion and is forbidden**:

* **(PREFERRED) EXPAND** — substitute `{{PRIMARY_LANGUAGE_LOWER}}` from `.autoharness/harness-manifest.yaml` `variables.PRIMARY_LANGUAGE_LOWER` (= `python`) before resolving, so the citation resolves to the real installed `python-reviewer.agent.md`. Strictly stronger: it also proves the L1203 mapping itself is satisfied.
* **(FALLBACK) EXEMPT** — skip citations containing an unexpanded `{{...}}` segment via a named, commented allow-rule identifying them as installer guidance rather than live routes.

Whichever branch is taken, the test records which one and why. A silent regex that merely happens to miss the token is NOT acceptable.

**3. Named-reader / Law-2 test — INVERTED.** The prior revision asserted three personas were ABSENT. That assertion is now false and MUST NOT be written; it would fail against the corrected S0 and would freeze the false premise into a permanent regression test. Assert instead:

* every installed persona under `.github/agents/subagents/` has a **named reader** — an installed-path citation OR a bare-filename citation in the install/tune drift contract;
* `correctness-reviewer.agent.md` and `maintainability-reviewer.agent.md` are **PRESENT**, so the `tune-harness` L462-L469 local-first review drift condition does not trigger;
* `python-reviewer.agent.md` is present and its manifest entry records `technology-reviewer.agent.md.tmpl` as its template source.

**4. Resolution precedence.** `_resolve_policy_registry`: installed-first, template-fallback.

**Gate command (Q5)**: `PYTHONPATH=src python -m unittest discover -s tests`
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
**Depends on U2 and U7.**

**Two distinct placeholder checks — do not conflate them:**

| Check | Input | Placeholder policy |
|---|---|---|
| Hardening step 2 — placeholder scan | bodies of the 14 installed artifacts | zero `{{...}}` tolerated, no exemptions |
| Scenario 2 — route resolution | persona path citations found in installed skills/agents | `{{PRIMARY_LANGUAGE_LOWER}}` EXPANDED (preferred) or EXEMPTED by named rule |

The scenario-2 exemption is about a *citation inside installer guidance prose*, not about an unresolved variable in a rendered artifact. Applying scenario 2's tolerance to the placeholder scan would defeat the scan.

Learnings-Researcher P2 applied here: `docs/compound/2026-08-21-ship-executable-set-must-wire-into-actual-loop-variable.md` — the 'defined but not wired' class. Scenario 2 therefore tests the ROUTE, not file presence.

Learnings-Researcher P2 applied here: `docs/compound/012-S-portability-scan-allow-list.md` — new rules over an existing corpus carry historical blast radius. Addressed by pre-flight capture (U1) and the post-flight delta classification above.

RK-H (new): the P1 that blocked this plan was a *measurement-shape* defect — a path-shaped grep could not see bare-filename citations. Scenario 3 is written to measure readers across BOTH citation shapes so the same defect cannot recur silently.

Do NOT fix the profile staleness in this shipment (D7).
<!-- END:implementation-notes -->