---
artifact_type: task
created_at: 2026-08-28T03:53:00.7143116Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 148.002-T
    - 148.007-T
id: 148.008-T
labels:
    - S0
    - tests
    - verification
parent_id: 148-F
priority: medium
references:
    - docs/plans/2026-08-27-policy-registry-and-review-persona-layer-plan.md
    - tests/
status: queued
title: U8 — Verification and regression tests for both gaps
updated_at: 2026-08-28T03:53:00.7143116Z
---

Add regression tests proving both gaps are closed: registry content/placeholder assertions, persona route-resolution, Law-2 exclusion absence, and _resolve_policy_registry precedence. Domain: tests. Files: <=3.

<!-- BEGIN:acceptance-criteria -->
* All 4 scenarios green under `PYTHONPATH=src python -m unittest discover -s tests`.
* **Placeholder scan**: zero `{{...}}` across ALL 12 newly installed artifacts, not just the registry.
* **Checksum round-trip**: each manifest checksum recomputed from the installed file matches.
* **INV-3 exit-code invariance**: `verify-workspace` exit-code behavior compared pre/post; any change is a HARD STOP, not a finding.
* **Post-flight diff**: `verify-workspace` after vs. before; every delta classified as *expected status-change* (RK-B) or *finding*.
* Carried-forward findings recorded, NOT fixed: `workspace-profile.yaml` still declares `test.runner: pytest` while Q5 names the unittest command authoritative; `8AC574F1` residual skill gap.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan unit **U8**. Verification and regression tests, plus the post-flight delta classification.

**4 scenarios**:
1. `.github/policies/workflow-policies.md` exists, contains P-001..P-021 and the `dark_factory_policy_contract` `must_contain` tokens (`P-017`, `Run pipeline in dark mode`, `DARK_MODE_ACTIVE`, `BRAINSTORM_HANDOFF_READY`, `DARK_MODE_COMPLETE`), and has **zero unresolved `{{...}}`**.
2. **Route-resolution test**: every persona path cited by any installed skill/agent under `.github/agents/subagents/` RESOLVES. This asserts a *property*, not a hardcoded list, so it will not rot as skills change. This is the direct anti-regression for GAP 2.
3. The 3 Law-2-excluded personas are ABSENT — asserting the exclusion is intentional and stays intentional.
4. `_resolve_policy_registry` precedence: installed-first, template-fallback.

**Gate command (Q5)**: `PYTHONPATH=src python -m unittest discover -s tests`
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
**Depends on U2 and U7.**

Learnings-Researcher P2 finding applied here: `docs/compound/2026-08-21-ship-executable-set-must-wire-into-actual-loop-variable.md` — the 'defined but not wired' class. Scenario 2 therefore tests the ROUTE, not file presence.

Learnings-Researcher P2 finding applied here: `docs/compound/012-S-portability-scan-allow-list.md` — new rules over an existing corpus carry historical blast radius. Addressed by pre-flight capture (in U1) and the post-flight delta classification above.

Do NOT fix the profile staleness in this shipment (D7).
<!-- END:implementation-notes -->