---
artifact_type: task
created_at: 2026-09-19T07:00:12.9841978Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 169.009-T
    - 169.010-T
    - 169.012-T
    - 169.013-T
    - 169.014-T
id: 169.017-T
labels:
    - test
    - p-002.7
    - verify
    - preactivation-gate
parent_id: 169-F
priority: medium
status: queued
title: 'P-002.7 VERIFY (pre-activation): evaluate the complete inert evidence set and emit the pre-activation readiness verdict'
updated_at: 2026-09-19T19:00:58.8490481Z
---

VERIFY PHASE - PRE-ACTIVATION READINESS GATE. INERT: ADDS NO ASSERTION, CHANGES NO DECLARED SURFACE, AUTHORS NO CLAUSE TEXT. This task is the unit's PRE-ACTIVATION EVIDENCE GATE. It was created in the revision-5 remediation cycle to close attempt-04 finding M1 (P1), which recorded that the revision-4 rollout ordered PREPARE, RED, ACTIVATE, VERIFY, DOCS and therefore placed the unit's ONLY gate emitter AFTER the single irreversible ACTIVATE commit that mutates all four declared surfaces - contradicting the binding rollout invariant of decision D2, PREPARE then VERIFY then ACTIVATE, which requires the COMPLETE EVIDENCE SET to be produced BEFORE ANY ACTIVATION. The unit's rollout is now PREPARE, RED, VERIFY, ACTIVATE, CONFIRM, DOCS, and this task is the VERIFY step. WHAT IT EVALUATES - THE COMPLETE PRE-ACTIVATION EVIDENCE SET, against the INERT CANDIDATE authored by 169.011-T at tests/p002_7_candidate_definition.py, with no declared surface yet changed. For each of the FIVE assertion families - (A) the three claim-to-admission transition rows (169.009-T), (B) bidirectional cross-reference wiring (169.010-T), (C) template/mirror byte-identity for both pairs (169.012-T), (D) the observed-version attribution paragraph (169.013-T), (E) the exactly-three-rows and exactly-four-surfaces closure assertions (169.014-T) - it resolves THREE per-assertion observations: (1) ABSENCE RED, the assertion observed failing individually against the current unmutated surfaces with its own declared marker in its own failure text; (2) DISCRIMINATING RED, the same assertion observed failing against its named near-miss fixture from tests/p002_7_near_miss_fixtures.py, which is what proves it tests the CONTRACT rather than a file's existence; (3) INERT GREEN, the same assertion executed against 169.011-T's canonical candidate definition AS TEST-OWNED DATA and observed PASSING - decision D2's "tests go RED first, then GREEN, entirely within the inert surface", which is observable before any declared surface changes and is exactly the conformant framing attempt 04 recorded as available and unused. THE EVIDENCE MUST BE PER-ASSERTION, NOT AGGREGATE: a green suite exit does not demonstrate that assertion D passed, it demonstrates that nothing failed, which is a weaker claim than this contract requires. COMPATIBILITY EVIDENCE IS INAPPLICABLE AND THAT IS RECORDED RATHER THAN SKIPPED: decision D2's third evidence limb points at the D3 pinned-fixture corpus, and this unit introduces no normalizer and reads no historical record corpus, so its complete evidence set is absence RED, discriminating RED and inert GREEN. INERT PRECONDITION. This task also resolves the surface-enumeration rule over the declared search scope and REQUIRES resolved_surface_count = 0 - the P-002.7 marker present in NONE of the four declared surfaces. A non-zero count means a declared surface has already been mutated, the candidate is no longer inert, and the pre-activation evidence set would be describing a state that no longer exists. SOLE EMITTER OF THE PRE-ACTIVATION READINESS VERDICT. DESTINATION - EXACTLY ONE PATH: .autoharness/gates/p002-7-preactivation-readiness.txt. That directory is GITIGNORED at .gitignore:7, so the readiness verdict is a GENERATED OBSERVATION ABOUT the tracked surfaces rather than a tracked surface itself; it is never committed, never appears in a diff, and CANNOT LEAVE THE WORKING TREE DIRTY. THIS IS A DIFFERENT ARTIFACT FROM THE POST-ACTIVATION CONFIRMATION VERDICT AND THE TWO ARE NEVER CONFLATED: different path, different line prefix, and DISJOINT token vocabularies. This task writes PREACTIVATION_STATE: lines carrying PREACTIVATION_* tokens to p002-7-preactivation-readiness.txt; 169.016-T writes COMPOSED_STATE: lines carrying STATUS_CONTRACT_* tokens to p002-7-status-contract-verdict.txt. Neither task reads or writes the other's path, and neither vocabulary is valid in the other's file. FORMAT - EXACTLY ONE LINE, ONE OF EXACTLY THREE LITERAL FORMS, prefix "PREACTIVATION_STATE: ", token as the FIRST field, fields separated by " | ", FIELD ORDER FIXED EXACTLY AS SHOWN: "PREACTIVATION_STATE: PREACTIVATION_READY | families=5 | absence_red=5 | discriminating_red=5 | inert_green=5 | declared_surface_count=4 | resolved_surface_count=0 | head_commit=<40-hex> | candidate_digest=<64-hex> | checked=<YYYY-MM-DDThh:mm:ssZ> | binding=<64-hex>"; "PREACTIVATION_STATE: PREACTIVATION_BLOCKED | families=5 | failed=<n> | family=<A|B|C|D|E> | gap=<short-name> | checked=<YYYY-MM-DDThh:mm:ssZ>"; "PREACTIVATION_STATE: PREACTIVATION_NOT_OBSERVED | reason=<import_error|loader_errors|failed_test_placeholder|zero_assertions|absence_red_unrecorded|discriminating_red_unrecorded|inert_green_unrecorded|family_unrecorded|surface_not_inert|digest_input_unreadable> | checked=<YYYY-MM-DDThh:mm:ssZ>". <40-hex> and <64-hex> are lowercase hexadecimal of exactly 40 and exactly 64 characters. A BLOCKED line naming more than one family repeats the family= field once per family, still on the ONE line. THE FRESHNESS BINDING, ADDED AT REVISION 6 TO CLOSE ATTEMPT-05 FINDING N2 (P2), which recorded that all three line forms carried a checked= vintage field named in R12 as the stale-verdict mitigation while 169.015-T's five declared conditions NEVER READ IT, leaving a stale PREACTIVATION_READY with no mechanical freshness binding on the documented post-revert re-activation path. COMPUTE, IMMEDIATELY BEFORE THE ATOMIC WRITE AND ONLY FOR THE READY FORM: (i) head_commit, the full 40-character lowercase hex commit ID of HEAD at the instant of evaluation, read from the repository and never from an environment variable or from a file this unit writes; (ii) candidate_digest, the CCD/v1 canonical content digest over EXACTLY these SEVEN repository-relative POSIX paths IN THIS ORDER - templates/policies/workflow-policies.md.tmpl, .github/policies/workflow-policies.md, templates/agents/_ship.agent.md.tmpl, .github/agents/_ship.agent.md, tests/p002_7_candidate_definition.py, tests/p002_7_near_miss_fixtures.py, tests/test_p002_7_member_status_contract.py - with NO GLOB, NO DIRECTORY WALK AND NO DISCOVERY, so emitter and consumer compute over identical input; (iii) checked, an RFC 3339 UTC timestamp YYYY-MM-DDThh:mm:ssZ; and (iv) binding, the B/v1 digest over the five-line tagged preimage under tag p002-7-preactivation-binding/v1. CCD/v1: for each path in list order read the file's bytes - if it does not exist or is unreadable the digest is UNDEFINED and this task emits PREACTIVATION_NOT_OBSERVED with reason=digest_input_unreadable, never a placeholder and never an omitted field; normalize by replacing every CRLF byte pair with a single LF and nothing else; sha256 to lowercase hex; build the preimage as the concatenation in list order of <path> LF <file-digest> LF encoded UTF-8 with / separators; the value is the lowercase-hex sha256 of that preimage. The CRLF normalization and repository-relative POSIX paths are what make the value ENVIRONMENT-AGNOSTIC across a Windows and a POSIX checkout, with no host, user, absolute path or clock input. B/v1: lowercase-hex sha256 of the UTF-8 encoding of exactly five LF-terminated lines in this order - <tag>, head_commit=<value>, content_digest=<value>, resolved_surface_count=<value>, checked=<value> - where the preimage key is ALWAYS the literal content_digest= even though the emitted field is named candidate_digest=, and every <value> is the field's value exactly as it appears on the emitted line, byte for byte. WHY ONLY THE READY FORM CARRIES head_commit, candidate_digest AND binding: a not-observed verdict is reachable precisely when the inputs cannot be read, so a digest over them is UNDEFINED BY RULE and could not be emitted truthfully, and a non-authorizing line is never consumed as an authorization so it has nothing to bind. checked= stays on all three forms because a diagnostic vintage is useful on every one. THE SCOPE IS STATED HONESTLY: this is a STALENESS AND MISTAKE-DETECTION contract, NOT a tamper-proof security boundary - an actor who recomputes the binding can produce a self-consistent forgery and this unit claims no protection against that - and NO wall-clock future check is imposed, deliberately, because clock skew would make the contract environment-dependent. OWNERSHIP AND WRITE BEHAVIOUR - SINGLE WRITER, ATOMIC, WHOLE-FILE REPLACE, FRESH BINDING ON EVERY RUN. This task is the SOLE WRITER of that path; no other task, and not the test module, writes it. The write is ATOMIC: write the line to a temporary file in the SAME DIRECTORY and RENAME it over the destination, so a reader sees either the previous verdict or the new one and NEVER A PARTIAL LINE. Each run REPLACES THE WHOLE FILE - never appended to, and holding exactly one PREACTIVATION_STATE: line at all times; a pre-existing file from an earlier run is overwritten, not merged. Because the whole file is replaced and head_commit, candidate_digest and checked are recomputed from scratch on every run, A RE-RUN NECESSARILY PRODUCES A FRESH BINDING: no path preserves a previous run's binding and no previous run's binding can survive alongside a new verdict. ABSENCE EVALUATION - FAIL CLOSED, AND ABSENCE IS A READING RATHER THAN AN ERROR: a consumer resolves the artifact to PREACTIVATION_NOT_OBSERVED if ANY of the following holds - the file does not exist; it exists but is unreadable; it is empty; it contains no line beginning "PREACTIVATION_STATE: "; it contains MORE THAN ONE such line; or the token in the first field is not one of the three declared. AN F1-F5 FRESHNESS FAILURE IS A GATE FAILURE, NOT AN ARTIFACT REWRITE: it resolves the CONSUMER'S GATE CLOSED, does not change the token in this artifact, and NEVER authorizes any consumer to rewrite, amend or delete it; the only way to obtain a fresh binding is to RE-RUN THIS TASK. EVALUATE THE THREE CONDITIONS IN THIS ORDER. (1) PREACTIVATION_NOT_OBSERVED - emit if ANY of the following holds: the test module raised on import under unittest.defaultTestLoader; loader.errors is non-empty; any _FailedTest placeholder is present; zero assertions executed; any family has no per-assertion ABSENCE RED record; any family has no per-assertion DISCRIMINATING RED record; any family has no per-assertion INERT GREEN record; the inert precondition is violated or unresolvable, meaning resolved_surface_count is not 0; or any of the seven candidate_digest inputs is missing or unreadable, or HEAD is unresolvable, so the freshness binding cannot be computed (reason=digest_input_unreadable). THIS IS CHECKED FIRST, because a suite that did not run cannot be distinguished from a suite that passed by looking at its exit code. (2) PREACTIVATION_BLOCKED - emit if the module imported cleanly, all five families recorded both RED observations, and AT LEAST ONE INERT GREEN ASSERTION FAILED against the candidate definition; name the family and the gap in the line. (3) PREACTIVATION_READY - emit ONLY if the module imported cleanly; all five families recorded an absence RED, a discriminating RED and a PASSING inert GREEN per assertion; the enumeration rule resolves declared_surface_count = 4 with resolved_surface_count = 0; no assertion exists in the suite that was not observed red first; AND head_commit, candidate_digest and checked were all computed successfully and binding was derived from them under B/v1. The freshness fields are computed LAST, immediately before the atomic write, so the binding describes the tree state at the moment the verdict was emitted rather than at the moment evaluation began. FAIL CLOSED: any state not affirmatively established by (2) or (3) is PREACTIVATION_NOT_OBSERVED, and NO-OBSERVATION IS NEVER READY. EXIT CODE: EXIT ZERO ONLY when the token written is PREACTIVATION_READY; EXIT NON-ZERO for PREACTIVATION_BLOCKED and PREACTIVATION_NOT_OBSERVED. The exit code and the artifact carry the same verdict by construction, because one step produces both. THE AUTHORIZATION IS THE VERDICT PLUS ITS FRESHNESS BINDING, NOT THE EDGE. 169.015-T depends on this task, but a dependency edge clears on PREDECESSOR COMPLETION, and this task COMPLETES ON ALL THREE TOKENS - two of which leave the unit UNREADY. THE EDGE IS ORDERING ONLY: it guarantees the readiness artifact EXISTS before it is read. THE AUTHORIZATION PREDICATE IS THE TOKEN PLUS F1-F5: 169.015-T READS THIS ARTIFACT AS ITS FIRST ACTION AND FAILS CLOSED. The gate is OPEN only when the whole file is exactly one PREACTIVATION_STATE: line whose token is the literal PREACTIVATION_READY AND 169.015-T's own recomputation reproduces head_commit against current HEAD, candidate_digest against the seven inputs, resolved_surface_count 0, a well-formed checked not earlier than the head commit's committer timestamp, and binding under this task's tag; everything else - missing, unreadable, empty, more than one line, unrecognised token, PREACTIVATION_BLOCKED, PREACTIVATION_NOT_OBSERVED, or any freshness mismatch - is CLOSED. ON A CLOSED GATE 169.015-T DOES NOT TOUCH ANY DECLARED SURFACE AT ALL: it halts, exits 1, and the unit returns to Stage. This is the same gate-predicate shape attempt 03 finding K1 (P1) required of 177-F's I1 gate, where a blocks edge alone was found insufficient as a safety predicate. ADDING ANY NEW ASSERTION IN THIS TASK IS A TDD VIOLATION and a finding; a gap discovered here returns to a RED task rather than being closed in place. The digest and binding computations are EMITTER LOGIC, NOT ASSERTIONS: they produce no assertion outcome, belong to no assertion family, and must not enter the conformance suite. Scope: observation, evidence recording, and one generated gitignored artifact. No production surface is touched, no clause text is authored, and no CI workflow is added or modified - .github/workflows/ci.yml is a PRODUCER of the unittest observations this gate reads, never a consumer of either verdict line. Blocked by 169.009-T, 169.010-T, 169.012-T, 169.013-T and 169.014-T; blocks 169.015-T. Stash 3EF5AAF2. Plan docs/plans/2026-09-18-post-claim-member-status-contract-plan.md (plan_id post-claim-member-status-contract-v2, revision 6, disposition REMEDIATED-PENDING-REVIEW, awaiting independent attempt 06). Verdict manifest docs/reviews/2026-09-18-post-claim-member-status-contract-v2-plan-review.md.