---
artifact_type: task
created_at: 2026-09-19T07:00:12.9841978Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 169.009-T
    - 169.010-T
    - 169.012-T
    - 169.013-T
    - 169.014-T
id: 169.017-T
labels:
    - test
    - p-002.7
    - verify
    - preactivation-gate
parent_id: 169-F
priority: medium
status: queued
title: 'P-002.7 VERIFY (pre-activation): evaluate the complete inert evidence set and emit the pre-activation readiness verdict'
updated_at: 2026-09-19T07:00:12.9841978Z
---

VERIFY PHASE - PRE-ACTIVATION READINESS GATE. INERT: ADDS NO ASSERTION, CHANGES NO DECLARED SURFACE, AUTHORS NO CLAUSE TEXT. This task is the unit's PRE-ACTIVATION EVIDENCE GATE. It was created in the revision-5 remediation cycle to close attempt-04 finding M1 (P1), which recorded that the revision-4 rollout ordered PREPARE, RED, ACTIVATE, VERIFY, DOCS and therefore placed the unit's ONLY gate emitter AFTER the single irreversible ACTIVATE commit that mutates all four declared surfaces - contradicting the binding rollout invariant of decision D2, PREPARE then VERIFY then ACTIVATE, which requires the COMPLETE EVIDENCE SET to be produced BEFORE ANY ACTIVATION. The unit's rollout is now PREPARE, RED, VERIFY, ACTIVATE, CONFIRM, DOCS, and this task is the VERIFY step. WHAT IT EVALUATES - THE COMPLETE PRE-ACTIVATION EVIDENCE SET, against the INERT CANDIDATE authored by 169.011-T, with no declared surface yet changed. For each of the FIVE assertion families - (A) the three claim-to-admission transition rows (169.009-T), (B) bidirectional cross-reference wiring (169.010-T), (C) template/mirror byte-identity for both pairs (169.012-T), (D) the observed-version attribution paragraph (169.013-T), (E) the exactly-three-rows and exactly-four-surfaces closure assertions (169.014-T) - it resolves THREE per-assertion observations: (1) ABSENCE RED, the assertion observed failing individually against the current unmutated surfaces with its own declared marker in its own failure text; (2) DISCRIMINATING RED, the same assertion observed failing against its named near-miss fixture, which is what proves it tests the CONTRACT rather than a file's existence; (3) INERT GREEN, the same assertion executed against 169.011-T's canonical candidate definition AS TEST-OWNED DATA and observed PASSING - decision D2's "tests go RED first, then GREEN, entirely within the inert surface", which is observable before any declared surface changes and is exactly the conformant framing attempt 04 recorded as available and unused. THE EVIDENCE MUST BE PER-ASSERTION, NOT AGGREGATE: a green suite exit does not demonstrate that assertion D passed, it demonstrates that nothing failed, which is a weaker claim than this contract requires. COMPATIBILITY EVIDENCE IS INAPPLICABLE AND THAT IS RECORDED RATHER THAN SKIPPED: decision D2's third evidence limb points at the D3 pinned-fixture corpus, and this unit introduces no normalizer and reads no historical record corpus, so its complete evidence set is absence RED, discriminating RED and inert GREEN. INERT PRECONDITION. This task also resolves the surface-enumeration rule over the declared search scope and REQUIRES resolved_surface_count = 0 - the P-002.7 marker present in NONE of the four declared surfaces. A non-zero count means a declared surface has already been mutated, the candidate is no longer inert, and the pre-activation evidence set would be describing a state that no longer exists. SOLE EMITTER OF THE PRE-ACTIVATION READINESS VERDICT. DESTINATION - EXACTLY ONE PATH: .autoharness/gates/p002-7-preactivation-readiness.txt. That directory is GITIGNORED at .gitignore:7, so the readiness verdict is a GENERATED OBSERVATION ABOUT the tracked surfaces rather than a tracked surface itself; it is never committed, never appears in a diff, and CANNOT LEAVE THE WORKING TREE DIRTY. THIS IS A DIFFERENT ARTIFACT FROM THE POST-ACTIVATION CONFIRMATION VERDICT AND THE TWO ARE NEVER CONFLATED: different path, different line prefix, and DISJOINT token vocabularies. This task writes PREACTIVATION_STATE: lines carrying PREACTIVATION_* tokens to p002-7-preactivation-readiness.txt; 169.016-T writes COMPOSED_STATE: lines carrying STATUS_CONTRACT_* tokens to p002-7-status-contract-verdict.txt. Neither task reads or writes the other's path, and neither vocabulary is valid in the other's file. FORMAT - EXACTLY ONE LINE, ONE OF EXACTLY THREE LITERAL FORMS, prefix "PREACTIVATION_STATE: ", token as the FIRST field, fields separated by " | ", checked=YYYY-MM-DD last: "PREACTIVATION_STATE: PREACTIVATION_READY | families=5 | absence_red=5 | discriminating_red=5 | inert_green=5 | declared_surface_count=4 | resolved_surface_count=0 | checked=YYYY-MM-DD"; "PREACTIVATION_STATE: PREACTIVATION_BLOCKED | families=5 | failed=<n> | family=<A|B|C|D|E> | gap=<short-name> | checked=YYYY-MM-DD"; "PREACTIVATION_STATE: PREACTIVATION_NOT_OBSERVED | reason=<import_error|loader_errors|failed_test_placeholder|zero_assertions|absence_red_unrecorded|discriminating_red_unrecorded|inert_green_unrecorded|family_unrecorded|surface_not_inert> | checked=YYYY-MM-DD". A BLOCKED line naming more than one family repeats the family= field once per family, still on the ONE line. OWNERSHIP AND WRITE BEHAVIOUR - SINGLE WRITER, ATOMIC, WHOLE-FILE REPLACE. This task is the SOLE WRITER of that path; no other task, and not the test module, writes it. The write is ATOMIC: write the line to a temporary file in the SAME DIRECTORY and RENAME it over the destination, so a reader sees either the previous verdict or the new one and NEVER A PARTIAL LINE. Each run REPLACES THE WHOLE FILE - never appended to, and holding exactly one PREACTIVATION_STATE: line at all times; a pre-existing file from an earlier run is overwritten, not merged. ABSENCE EVALUATION - FAIL CLOSED, AND ABSENCE IS A READING RATHER THAN AN ERROR: a consumer resolves the artifact to PREACTIVATION_NOT_OBSERVED if ANY of the following holds - the file does not exist; it exists but is unreadable; it is empty; it contains no line beginning "PREACTIVATION_STATE: "; it contains MORE THAN ONE such line; or the token in the first field is not one of the three declared. EVALUATE THE THREE CONDITIONS IN THIS ORDER. (1) PREACTIVATION_NOT_OBSERVED - emit if ANY of the following holds: the test module raised on import under unittest.defaultTestLoader; loader.errors is non-empty; any _FailedTest placeholder is present; zero assertions executed; any family has no per-assertion ABSENCE RED record; any family has no per-assertion DISCRIMINATING RED record; any family has no per-assertion INERT GREEN record; or the inert precondition is violated or unresolvable, meaning resolved_surface_count is not 0. THIS IS CHECKED FIRST, because a suite that did not run cannot be distinguished from a suite that passed by looking at its exit code. (2) PREACTIVATION_BLOCKED - emit if the module imported cleanly, all five families recorded both RED observations, and AT LEAST ONE INERT GREEN ASSERTION FAILED against the candidate definition; name the family and the gap in the line. (3) PREACTIVATION_READY - emit ONLY if the module imported cleanly; all five families recorded an absence RED, a discriminating RED and a PASSING inert GREEN per assertion; the enumeration rule resolves declared_surface_count = 4 with resolved_surface_count = 0; and no assertion exists in the suite that was not observed red first. FAIL CLOSED: any state not affirmatively established by (2) or (3) is PREACTIVATION_NOT_OBSERVED, and NO-OBSERVATION IS NEVER READY. EXIT CODE: EXIT ZERO ONLY when the token written is PREACTIVATION_READY; EXIT NON-ZERO for PREACTIVATION_BLOCKED and PREACTIVATION_NOT_OBSERVED. The exit code and the artifact carry the same verdict by construction, because one step produces both. THE AUTHORIZATION IS THE VERDICT, NOT THE EDGE. 169.015-T depends on this task, but a dependency edge clears on PREDECESSOR COMPLETION, and this task COMPLETES ON ALL THREE TOKENS - two of which leave the unit UNREADY. THE EDGE IS ORDERING ONLY: it guarantees the readiness artifact EXISTS before it is read. THE AUTHORIZATION PREDICATE IS THE TOKEN: 169.015-T READS THIS ARTIFACT AS ITS FIRST ACTION AND FAILS CLOSED. The gate is OPEN only when the file exists, is readable, carries EXACTLY ONE PREACTIVATION_STATE: line, and that line's token is PREACTIVATION_READY; everything else - missing, unreadable, empty, no line, more than one line, unrecognised token, PREACTIVATION_BLOCKED, PREACTIVATION_NOT_OBSERVED - is CLOSED. ON A CLOSED GATE 169.015-T DOES NOT TOUCH ANY DECLARED SURFACE AT ALL: it halts and the unit returns to Stage. This is the same gate-predicate shape attempt 03 finding K1 (P1) required of 177-F's I1 gate, where a blocks edge alone was found insufficient as a safety predicate. ADDING ANY NEW ASSERTION IN THIS TASK IS A TDD VIOLATION and a finding; a gap discovered here returns to a RED task rather than being closed in place. Scope: observation, evidence recording, and one generated gitignored artifact. No production surface is touched, no clause text is authored, and no CI workflow is added or modified - .github/workflows/ci.yml is a PRODUCER of the unittest observations this gate reads, never a consumer of either verdict line. Blocked by 169.009-T, 169.010-T, 169.012-T, 169.013-T and 169.014-T; blocks 169.015-T. Stash 3EF5AAF2. Plan docs/plans/2026-09-18-post-claim-member-status-contract-plan.md (plan_id post-claim-member-status-contract-v2, revision 5, disposition REMEDIATED-PENDING-REVIEW, awaiting independent attempt 05). Verdict manifest docs/reviews/2026-09-18-post-claim-member-status-contract-v2-plan-review.md.