---
artifact_type: task
created_at: 2026-09-08T07:43:13.481551Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 163.004-T
    - 033-DL
id: 163.005-T
labels:
    - skill
    - template
    - paired-edit
    - docs-currency
parent_id: 163-F
priority: medium
status: queued
title: 'U3: scenario matrix and quality criteria currency for the two new halt tokens'
updated_at: 2026-09-10T23:53:28.5215977Z
---

Paired edit in BOTH copies. Add to the Deterministic Safe-Close Scenario Matrix (SKILL.md L1002) and Quality Criteria (L1011): (1) NEGATIVE - missing pre-cascade evidence: cascade path selected, no evidence record => RECONCILE_FAIL_PRECASCADE_EVIDENCE_MISSING; (2) NEGATIVE - post-hoc reconstruction: evidence record present but collection_completed_at is after the cascade invocation => RECONCILE_FAIL_PRECASCADE_EVIDENCE_STALE; (3) POSITIVE - empty validated set (the 159-S shape): evidence record present, scan_performed: true, linked_deliberation_ids: [], timestamp strictly before the invocation => PASS. Row (3) is mandatory: it names the concrete LEGITIMATE PASSING STATE required by lesson 6 of the composed-workflow-protocol-state-machine-validation compound learning (docs/compound/2026-09-06-composed-workflow-protocol-state-machine-validation.md - published 2026-09-10, commit 5171ace1 on main; stash 15A02E21 and follow-up 1CD92B69 both reconciled and archived 2026-09-10) and is the evidence that this gate is reachable rather than the 161-F unsatisfiable-gate shape. Depends on U2. Plan: docs/plans/2026-09-08-shipment-reconcile-step-0c-live-pre-mutation-evidence-plan.md U3.

--- REVISION R1 (2026-09-10, gate 033-DL, stash DDBF283E/EB23D1B9; plan section "Revision R1") --- R1 GOVERNS where it conflicts with the text above. The scenario matrix and quality criteria must now carry FOUR halt tokens, not two, and TWO ADDITIONAL ROWS in both copies (.github/skills/shipment-reconcile/SKILL.md and templates/skills/shipment-reconcile/SKILL.md.tmpl): (1) NEGATIVE - BACKDATED RECONSTRUCTION: evidence record present with collection_completed_at strictly BEFORE the cascade invocation and with HEAD SHA and manifest item list both MATCHING the closed state (a perfect forgery under the pre-R1 mechanism), but with the PRECASCADE_EVIDENCE_ANCHOR event absent from the engine log or appended AFTER the mutation events => RECONCILE_FAIL_PRECASCADE_ANCHOR_MISSING or RECONCILE_FAIL_PRECASCADE_ANCHOR_NOT_PRE_MUTATION, and explicitly NOT ..._EVIDENCE_STALE. (2) POSITIVE - THE LEGITIMATE 159-S SHAPE, restated so the gate is PROVABLY SATISFIABLE (lesson 6 and the 15A02E21 unreachable-gate lesson): evidence record present, scan_performed: true, linked_deliberation_ids: [], anchor event present, anchor sha256 matching the record on disk, anchor preceding ALL engine-written mutation events in append order => PASS. The pre-existing negative rows for EVIDENCE_MISSING and EVIDENCE_STALE remain, with EVIDENCE_STALE's row NARROWED to content mismatch (HEAD SHA / manifest) or absent collection_completed_at - it must no longer be described as the post-hoc-reconstruction detector, since under R1 that detection belongs to the two anchor tokens.

--- AMENDMENT R1-11 (2026-09-10, review-fix cycle 1; plan section "R1-11", decision section "D-7") --- [L2 PUBLICATION WORDING SUPERSEDED BY AMENDMENT R1-12(3) AND R1-13 BELOW, WHICH GOVERN; the "force-added past the workspace .gitignore rule" phrasing below is RETAINED AS HISTORY, NOT AS AN INSTRUCTION - the CONCLUSION that no third matrix row is added is UNAFFECTED.] NO THIRD ROW IS ADDED. The R1-11 durability split introduces an L2 REPOSITORY AUDIT EVIDENCE obligation (commit the evidence record AND that shipment's own engine log .backlogit/logs/{shipment_id}.jsonl, force-added past the workspace .gitignore rule, verbatim and byte-unmodified, at the closure commit). It is DELIBERATELY NOT a scenario-matrix row: the matrix enumerates PRE-CASCADE GATE OUTCOMES, and L2 is a POST-CLOSE PUBLICATION obligation with no gate outcome - it is written after a mutation the gate already permitted, so it cannot be gated on. TOKEN COUNT REMAINS FOUR; adding a fifth PRECASCADE halt token for L2 was considered and REJECTED as an unsatisfiable gate on an artifact that cannot yet exist. If the Quality Criteria mention L2 at all, it MUST be mentioned as a CLOSURE OBLIGATION EXPLICITLY MARKED NON-GATING, never as a pass/fail criterion of the pre-cascade check, and the matrix rows must continue to read the engine log (L1) and only the engine log.

--- AMENDMENT R1-12 (2026-09-10, review-fix cycle 2; plan sections "R1-2"/"R1-7"/"R1-12", redesign decision D-7/D-8) --- R1-12 GOVERNS where it conflicts with any text above. STILL FOUR TOKENS, STILL FIVE ROWS, STILL NO THIRD L2 ROW.
(1) TIMESTAMP-ORDERING SUPERSESSION APPLIED TO THE MATRIX ROWS. The ORIGINAL row (2) above - "NEGATIVE - post-hoc reconstruction: evidence record present but collection_completed_at is after the cascade invocation => RECONCILE_FAIL_PRECASCADE_EVIDENCE_STALE" - is SUPERSEDED AND MUST NOT BE WRITTEN INTO THE MATRIX IN THAT FORM. Under R1, collection_completed_at is CORROBORATING METADATA ONLY. The EVIDENCE_STALE row NARROWS to: HEAD SHA mismatch, manifest item list mismatch, or collection_completed_at ABSENT (a content/completeness failure, since the contract requires the field). A collection_completed_at that is PRESENT but LATE MUST NOT by itself produce a rejecting row, because a valid digest-bound anchor preceding the mutation events in APPEND ORDER is the ordering proof. Post-hoc-reconstruction detection belongs to ANCHOR_MISSING / ANCHOR_NOT_PRE_MUTATION, never to EVIDENCE_STALE.
(2) THE POSITIVE ROW'S TIMESTAMP CLAUSE IS LIKEWISE NARROWED. The ORIGINAL row (3) above required "timestamp strictly before the invocation" as a PASS condition. That clause is REMOVED from the pass criteria. The R1 positive row governs: evidence record present, scan_performed: true, linked_deliberation_ids: [], anchor event present, anchor sha256 MATCHING the record on disk, anchor preceding ALL engine-written mutation events IN APPEND ORDER => PASS. Agreement between collection_completed_at and the anchor is explicitly NOT a PASS condition.
(3) L2 REMAINS OFF THE MATRIX, AND THE R1-11 WORDING IS CORRECTED. Still NO third row: the matrix enumerates PRE-CASCADE GATE OUTCOMES and L2 is a POST-CLOSE PUBLICATION obligation with no gate outcome. Where the Quality Criteria mention L2, they MUST describe it as a CLOSURE OBLIGATION EXPLICITLY MARKED NON-GATING, and they MUST NOT describe it as "force-added past the workspace .gitignore rule" - that R1-11 phrasing rests on a premise direct inspection refutes (origin/main carries no .backlogit/logs/ ignore rule; 993 of 1016 logs including 171-S's own are ALREADY TRACKED; the rule exists only as an uncommitted working-tree edit; ignore rules never hide tracked paths). Use the R1-12 conditional wording instead: the shipment's own engine log is published into the closure commit VERBATIM AND BYTE-UNMODIFIED by the staging action its tracking state requires (Case A already tracked - ordinary commit; Case B untracked and not ignored - ordinary add; Case C untracked and ignored - bounded force-add), with the identical durable postcondition in all three cases.
(4) OPTIONAL, NON-GATING MENTION OF THE U6 STEP. If the Quality Criteria enumerate closure obligations, they MAY name the POST-CASCADE PUBLICATION step (plan U6 / task 163.008-T) and its reportable condition RECONCILE_CLOSURE_INCOMPLETE_L2_EVIDENCE, marked explicitly as a P-001 INCOMPLETE-CLOSURE REPORT and NOT a halt token. It MUST NOT appear as a pass/fail criterion of the pre-cascade check, and it MUST NOT be added to the scenario matrix. Matrix rows continue to read the engine log (L1) and only the engine log.

--- AMENDMENT R1-13 (2026-09-10, review-fix cycle 3; plan section "R1-13", redesign decision D-7/D-8) --- L2 WORDING CORRECTED AGAIN; MATRIX UNCHANGED. STILL FOUR TOKENS, STILL FIVE ROWS, STILL NO THIRD L2 ROW; SIZE UNCHANGED (S / low).
(1) WHERE THE QUALITY CRITERIA DESCRIBE L2, THEY MUST NOT SAY "BYTE-IDENTICAL". R1-12(3) above replaced the force-add phrasing with "VERBATIM AND BYTE-UNMODIFIED"; that phrase must be written as the APPEND-ONLY PREFIX RULE, because the engine appends the cascade's own shipment_status_changed / commit_tracked / archived events to the same log AFTER the gate's read and BEFORE the closure commit, so a byte-identical claim would describe a state no legitimate close reaches. CORRECT WORDING: the anchor line and all pre-cascade bytes the gate evaluated are present BYTE-UNMODIFIED and in UNCHANGED ORDER as the PREFIX of the committed log; the engine's post-gate mutation lines are EXPECTED APPEND-ONLY EXTENSIONS; any modification, deletion, truncation or reordering of pre-existing bytes is a failure.
(2) IF THE CLOSURE-OBLIGATION MENTION IN (4) ABOVE IS WRITTEN, it MAY also name the R1-13 REMEDIATION PATH in one clause - closure REMAINS INCOMPLETE and is remediated by a FOLLOW-UP CLOSURE COMMIT before P-001 release-unit completion, never by amending published history - marked, like the rest of the L2 mention, as EXPLICITLY NON-GATING. It MUST NOT become a matrix row or a pass/fail criterion.
(3) UNCHANGED: the five matrix rows and their tokens; the narrowed EVIDENCE_STALE row (HEAD SHA / manifest mismatch or ABSENT collection_completed_at only); the positive row with no timestamp clause; L2 off the matrix; and the rule that matrix rows read the engine log (L1) and only the engine log.