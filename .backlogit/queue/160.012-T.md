---
artifact_type: task
created_at: 2026-09-03T09:18:38.7161169Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.001-T
    - 160.005-T
    - 160.016-T
id: 160.012-T
labels:
    - tests
    - e2e
    - upgrade
    - parity
parent_id: 160-F
priority: medium
status: queued
title: Upgrade-path and verify-workspace parity e2e tests
updated_at: 2026-09-03T17:34:09.3658274Z
---

UPGRADE-PATH AND VERIFY-WORKSPACE PARITY CASES (T10). Authored and observed against the BASELINE build, then re-observed against the trimmed build by the transition ledger (160.017-T).

SEQUENCING (machine-encoded, not prose). This task is BLOCKED BY 160.001-T (baseline capture, T2a), 160.005-T (RED-FIRST harness) and 160.016-T (CHARACTERIZATION harness), and it BLOCKS 160.006-T and 160.007-T. A verification family that first exists after the behaviour it verifies cannot distinguish "this still works" from "this never worked".

TWO-CLASS CONTRACT (plan section Test strategy). Every case MUST DECLARE ITS CLASS IN THE TEST FILE ITSELF, PER CASE. THIS FAMILY IS MIXED - two CHARACTERIZATION cases and one RED-FIRST case - so there is NO family-wide class label and none may be introduced. A family label that contradicts the per-case truth is exactly the drift that made the plan and this record disagree.
- RED-FIRST: asserts behaviour that DOES NOT YET EXIST. Observed FAILING on the baseline, passing after implementation.
- CHARACTERIZATION: asserts an EXISTING, PRESERVED behaviour that CORRECTLY PASSES ON THE BASELINE. Observed GREEN on the untrimmed baseline and GREEN AGAIN after trimming. NOT required to be red, and MUST NOT be weakened or inverted to manufacture one.
- If a CHARACTERIZATION case goes RED after the wiring tasks land, that is a REGRESSION CAUSED BY THE WIRING TASK, and the wiring task - not this one - is at fault.

BASELINE OBSERVATION IS A DELIVERABLE, not a side effect. Record, per case, the class and the observed initial result against the artifacts captured by 160.001-T (T2a) - untrimmed wheel, untrimmed plugin payload (the sdist channel was de-scoped in cycle 5, finding 1), recorded verify-workspace finding set, recorded `autoharness home`/`version` behaviour - loading the structured (non-prose) half from tests/fixtures/payload-baseline/**. That baseline is a hard prerequisite, not a convenience.

Tests run against the BUILT ARTIFACT, never the source tree (verification V1, risk R1).

THE THREE CASES, EACH WITH ITS OWN CLASS AND GREEN/PRESERVATION OWNER:

1. test_verify_workspace_parity_trimmed_vs_baseline (AC6) - CLASS: CHARACTERIZATION. OBSERVED INITIAL STATE: GREEN on the baseline - the case is by construction a comparison against a recorded prior state, and comparing the baseline finding set with itself yields an empty difference. ASSERTS: install a scratch workspace from the build under test, run `autoharness verify-workspace`, and assert ZERO NEW findings versus the captured untrimmed baseline. Parity is asserted as a SET DIFFERENCE against the recorded baseline finding set, never as an absolute "no findings" claim - the untrimmed baseline may itself carry findings, and those are not this shipment's regressions. PRESERVATION OWNER: 160.006-T / 160.007-T.

2. test_upgrade_from_1_5_0_leaves_no_orphans (AC6, risk R3, verification V3) - CLASS: RED-FIRST. OBSERVED INITIAL STATE: RED by MISSING ARTIFACT - the trimmed build to upgrade TO does not exist yet. THIS CASE CANNOT BE CHARACTERIZATION: orphans are created BY trimming, so there is no green-on-baseline observation to make; upgrading v1.5.0 to v1.5.0 leaves no orphans trivially and asserts nothing. ASSERTS: enumerate residual engine files after an upgrade from the untrimmed baseline to the trimmed build, and assert the residual set is EMPTY or matches an explicitly enumerated expected set. An orphan is an engine file present after upgrade that the trimmed manifest does not classify as payload. GREEN OWNER: 160.006-T / 160.007-T.

THIS CASE MUST NOT DOWNLOAD ANYTHING, AND IT MUST NOT SUBSTITUTE A SET-DIFFERENCE PROXY OVER TWO *DECLARED MANIFESTS* (REWRITTEN IN CYCLE 7, NARROWED IN CYCLE 8 ITEM 8; supersedes the cycle-6 finding-18 formulation). THREE FORMULATIONS ARE WITHDRAWN AND NONE MAY BE REINTRODUCED. (1) The original "install v1.5.0 (untrimmed) into a scratch workspace" read as a PyPI FETCH AT TEST TIME. A network download inside the suite is NON-HERMETIC AND NON-DETERMINISTIC: it fails whenever the network or the index is unavailable, producing an environmental red that trains reviewers to ignore reds; its result depends on a MUTABLE REMOTE; and it makes an ordinary unit-test run reach outside the machine. (2) The cycle-6 replacement over-corrected: it computed a SET DIFFERENCE between 160.001-T's recorded baseline inventory and the trimmed build's DECLARED MANIFEST, and then CONCEDED that the case does not prove a real installer leaves no orphans - comparing two manifests compares INTENT, not what an installer produces. (3) CYCLE 7'S "HERMETIC ACTUAL UPGRADE" IS ITSELF WITHDRAWN IN CYCLE 8 AS NOT MECHANICALLY PROVABLE IN THIS REPOSITORY AS IT STANDS. It required rebuilding the baseline wheel from an exported pinned tree inside an isolated environment with no index and no network. MEASURED REASON: `pyproject.toml` declares `[build-system] requires = ["hatchling"]`, and `hatchling` IS NOT PRESENT IN `uv.lock` - the lock file pins exactly eight distributions (attrs, autoharness, jsonschema, jsonschema-specifications, pyyaml, referencing, rpds-py, typing-extensions) and the build backend is not among them. A build in a clean, index-free environment therefore has NO LOCAL SOURCE for its own build backend and must resolve it from the network, so the "nothing is fetched" clause was FALSE AS WRITTEN. Cycle 8 does not repair this by inventing a wheelhouse, a new pin, or a new dependency - that is out of scope for this shipment. IT NARROWS THE CASE TO WHAT IS MECHANICALLY PROVABLE TODAY and captures the real end-to-end offline upgrade test as deferred stash entry 60C207F1 with its residual risk stated in the plan.

THE BINDING FORM (CYCLE 8) - A LOCAL-ARTIFACT ORPHAN-AND-BEHAVIOUR PROOF USING ONLY ARTIFACTS THIS SHIPMENT ALREADY PRODUCES. NO NETWORK, NO BASELINE REBUILD, NO EXPORTED TREE, NO VIRTUAL ENVIRONMENT, NO GIT WORKTREE, NO OS TEMPORARY DIRECTORY, NO NEW TOOL, NO NEW DEPENDENCY, NO NEW FIXTURE STORE, NO NEW CASE:
  (U1) READ THE BASELINE INSTALL FACTS FROM 160.001-T. Use the RECORDED INSTALL OUTPUT INVENTORY and the recorded runtime-behaviour capture (`autoharness home`, `autoharness version`, `_DATA_DIR` resolution) that 160.001-T (T2a) committed as evidence. These are the OBSERVED facts of a real baseline install; they are read, never recomputed here.
  (U2) READ THE TRIMMED INSTALL FACTS FROM THE ACTUAL BUILT WHEEL. Build the trimmed wheel as this shipment already does, and read its `RECORD` - the wheel's own installer-authored file list, which is WHAT `pip`/`uv` WILL ACTUALLY PLACE ON DISK. THE `RECORD` IS THE INSTALLER'S INPUT, NOT A HAND-DECLARED MANIFEST: this is precisely the distinction that makes the cycle-6 proxy unacceptable and this form acceptable. Any scratch materialization needed to unpack the wheel is performed under the plan's SINGLE DECLARED WORKSPACE-CONTAINED RUN ROOT `dist/.autoharness-scratch/<run-id>/`, whose canonical resolved path MUST be a strict descendant of the workspace root (fail closed otherwise), which is gitignored by the existing `dist/.gitignore`, which is a declared `generated_output_roots` entry removed from classifier input before matching, and which is NEVER derived from `TMPDIR`, `TEMP` or `TMP`. It is NOT auto-deleted; bounded cleanup is a separately approved destructive action.
  (U3) VERIFY THE BASELINE REFERENCE BEFORE RELYING ON IT. Recompute the AGGREGATE DIGEST over the baseline inventory using the plan's SINGLE CANONICAL DEFINITION - SHA-256 over the newline-joined, path-lexicographically-sorted `(path, size, sha256)` triples, ASCII unit separator between fields, forward-slash-normalized UTF-8 paths - and compare it to 160.001-T's recorded E1 value. A MISMATCH IS A FAILURE OF THIS CASE. There is exactly one digest contract in this shipment and this is it.
  (U4) COMPUTE THE RESIDUAL ORPHAN SET. An ORPHAN is a path the BASELINE install placed on disk that the TRIMMED wheel's `RECORD` does not list. Assert the residual orphan set is EMPTY or matches an EXPLICITLY ENUMERATED expected set. Because the trimmed side is read from `RECORD` rather than from a declared manifest, this asserts what the installer will lay down, not what the manifest intends.
  (U5) ASSERT RUNTIME-BEHAVIOUR PARITY AGAINST THE RECORDED BASELINE. Assert that `autoharness home`, `autoharness version` and `_DATA_DIR` resolution as determined by the trimmed wheel's contents agree with 160.001-T's recorded baseline behaviour. Invariants I1 and I2 are OBSERVED here, never modified.

WHAT THIS PROVES - AND WHAT IT DOES NOT - STATED PLAINLY AND WITHOUT OVERCLAIM (CYCLE 8, ITEM 8). IT PROVES: the trimmed wheel's own installer-authored `RECORD` leaves NO orphaned former engine file relative to the REAL, RECORDED baseline install, and preserves the RECORDED runtime behaviour. That is AC6's upgrade clause and verification V3 narrowed to a MECHANICALLY PROVABLE GUARANTEE over local artifacts. IT DOES NOT PROVE: that an installer EXECUTING a baseline-to-trimmed upgrade in a live environment removes those files. THIS CASE VERIFIES THE INSTALLER'S INPUT, NOT THE INSTALLER'S EXECUTION. That gap is NOT concealed and NOT re-labelled as proved - it is captured as deferred stash entry 60C207F1 (actual offline end-to-end upgrade testing) and recorded in the plan's residual-risk section at MEDIUM. A HONEST NARROW GUARANTEE PLUS AN EXPLICIT DEFERRAL IS CORRECT; A BROAD GUARANTEE THAT THE TOOLCHAIN CANNOT DELIVER IS NOT.

3. test_home_and_version_behave_identically (AC6, invariants I1, I2) - CLASS: CHARACTERIZATION. OBSERVED INITIAL STATE: GREEN on the baseline - `autoharness home` and `autoharness version` behave as 160.001-T recorded. THE CASE WAS NAMED IN REVIEW-FIX CYCLE 3: it previously existed only as an unnamed bullet, which made it invisible to the plan's case ledger and unverifiable by the transition ledger - an unnamed assertion has no owner and no recorded transition. ASSERTS: both commands behave identically pre- and post-change, against the recorded baseline behaviour. I1 and I2 are OBSERVED here, never modified. PRESERVATION OWNER: 160.006-T / 160.007-T.

SCOPE BOUNDARY: this task does NOT cover install-time payload boundary assertions (160.008-T) and does NOT cover channel resolver contracts (160.013-T). Those are separate failure domains with separate diagnosis paths.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. Verification-only - it OBSERVES `_DATA_DIR`, `autoharness home`, and `autoharness version` and MUST NOT modify them (freeze-scope: src/autoharness/ runtime behaviour is read-only for this shipment). THE UPGRADE CASE FETCHES NOTHING (CYCLE 7, NARROWED IN CYCLE 8 ITEM 8). Its baseline side is READ FROM 160.001-T's RECORDED INSTALL INVENTORY AND RECORDED RUNTIME BEHAVIOUR, and its trimmed side is READ FROM THE NEWLY BUILT WHEEL'S `RECORD`, per U1-U5 above. Two withdrawn wordings are DELETED and neither may return: "the v1.5.0 install used by the upgrade case is fetched from the already-published release" (contradicted this record's own hermeticity rule and would have reintroduced a network dependency into the suite), and cycle 7's "its baseline side is REBUILT LOCALLY from the pinned commit and recorded rebuild recipe" (NOT ACHIEVABLE INDEX-FREE - `hatchling` is absent from `uv.lock`, so a clean-environment build must resolve its own backend from the network). INVARIANT I5 STILL BINDS AND IS UNCHANGED IN SUBSTANCE: the already-published v1.5.0 artifacts MUST NOT be mutated, retracted, re-uploaded, or overwritten by this task (published-artifact immutability checkpoint). I5 IS A PROHIBITION ON DISTURBING THE PUBLISHED RELEASE - never a licence to download it and never an instruction to use it as a test input. THERE IS NO EXPORT DIRECTORY, NO REBUILT BASELINE WHEEL, NO VIRTUAL ENVIRONMENT AND NO GIT WORKTREE. All scratch materialization this task performs lives under the plan's SINGLE DECLARED WORKSPACE-CONTAINED RUN ROOT `dist/.autoharness-scratch/<run-id>/` - INSIDE the repository working tree, gitignored by the existing `dist/.gitignore`, canonical-path-contained fail-closed, never derived from `TMPDIR`/`TEMP`/`TMP`, declared as a `generated_output_roots` entry and therefore removed from classifier input before matching, and NOT auto-deleted (bounded cleanup is a separately approved destructive action). NO OS TEMPORARY DIRECTORY IS USED. No tracked file may be deleted (destructive-operation checkpoint). BASELINE-FIDELITY CHECKPOINT: CHARACTERIZATION cases are observed green against the BASELINE build, never against a partially-changed tree. ITS ONLY AUTHORIZED WRITE OUTSIDE ITS OWN TEST FILES IS ITS FIRST-OBSERVATION RECORD at docs/audits/2026-09-03-ship10-payload-evidence/observations/T10.json (see FIRST-OBSERVATION DESTINATION below); this is stated here, in the operative write-surface clause, because a destination named only further down the record would still read as unauthorized under freeze-scope (extended review-fix cycle 4, finding 10). NO-PUBLISH: no twine upload, no gh release create, no triggering of the release.yml publish path. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

BOUNDED RAW LOG - SECOND AUTHORIZED EVIDENCE WRITE (added in review-fix cycle 5, findings 10 and 15; local field reference removed in cycle 8, item 7). Every observation this task records is backed by a RAW LOG this task also writes, into ITS OWN SUBDIRECTORY AND NOWHERE ELSE: docs/audits/2026-09-03-ship10-payload-evidence/observations/logs/T10/**. This path is authorized in the plan's H2 writable-surface table, so writing it does NOT violate freeze-scope, and 160.017-T can verify the evidence without following a reference outside the single audit directory. WHY THE LOG IS REQUIRED: cycle 4 had 160.017-T accept each producer's SELF-DECLARED outcome, which verifies nothing - a task that never ran would be indistinguishable from a clean pass. The log is the artifact that makes the claim checkable. The log also carries the source-provenance capture the plan's canonical contract requires before the test command runs, and the observation is taken on a CLEAN, FULLY COMMITTED TREE.
BOUNDS (binding, enforced by 160.017-T): PER LOG <= 256 KiB AND <= 2,000 lines - a run exceeding either limit is truncated head-and-tail with an explicit truncation marker (how the record signals truncation and its pre-truncation counts is defined in the plan's canonical contract, not here); PER CASE at most 3 logs (first observation, second observation, one retry); PER SHIPMENT at most 200 files under the logs root, none of them binary; RETENTION until 160.017-T completes and the shipment closes.
OBSERVATION-RECORD CONTENT IS DEFINED ONCE, IN THE PLAN, AND IS NOT RESTATED HERE (cycle 6, finding 23; TIGHTENED IN CYCLE 8, ITEM 7). The SINGLE CANONICAL definition is the field table in docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section "Evidence-verification contract for 160.017-T". Seven task records each carried their own restatement and the copies had ALREADY DIVERGED, so a producer reading only its own record would have written a record 160.017-T rejects. THIS RECORD STATES ONLY WHAT IS TASK-SPECIFIC: which cases it produces records for, and which log subdirectory is its own. THIS RECORD ENUMERATES NO FIELD, NAMES NO FIELD, AND DEFINES NO FIELD TYPE OR VALIDATION RULE - NOT EVEN PARTIALLY. Cycle 7 deleted the "ADDITIONAL RECORD FIELDS" restatement but left a second local enumeration standing under a different heading, which was the same defect wearing a different label; cycle 8 deletes that one too. A PARTIAL COPY DRIFTS FROM THE CANONICAL TABLE EXACTLY AS A FULL COPY DOES. FOR EVERY FIELD, ITS TYPE, AND ITS ACCEPTANCE RULE, READ THE PLAN'S CANONICAL TABLE - there is no second source. 160.017-T verifies each record INDEPENDENTLY against its evidence and REJECTS any record that is missing a field, malformed, or inconsistent; it does not warn and does not skip.
This task WRITES its own JSON and its own logs and NOTHING ELSE outside its test files; it MUST NOT write into another producer's subdirectory, and MUST NOT write the ledger.

ACCEPTANCE: AC6 (parity + upgrade clauses); invariants I1, I2, I5 observed unbroken. All three cases authored with a per-case declared class; the two CHARACTERIZATION cases observed and recorded GREEN on the baseline; the one RED-FIRST case observed and recorded RED by missing artifact.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size M, complexity medium. RESIZED S -> M IN CYCLE 8, ITEM 9. Cycle 8 NARROWED the upgrade case (no baseline rebuild, no exported tree, no virtual environment, no dual install), but the RETAINED MECHANISM IS STILL MULTI-STAGE and that is what size measures: build the trimmed wheel, materialize and unpack it under the declared workspace-contained run root WITH CANONICAL-PATH CONTAINMENT ENFORCED FAIL-CLOSED, parse the wheel's `RECORD`, recompute the canonical `(path, size, sha256)` aggregate digest and reconcile it against 160.001-T's recorded E1 value, compute the residual orphan set against the recorded baseline install inventory, and assert runtime-behaviour parity - across THREE cases, one of which spans all of it. SIZE AND COMPLEXITY ARE INDEPENDENT AXES AND ARE NOT DERIVED FROM EACH OTHER: the narrowing REDUCED UNCERTAINTY (the withdrawn hermetic form depended on a build backend that is not in `uv.lock`, which is exactly why it could not be proved), but it did NOT reduce the number of distinct stages that must be wired and verified. VOLUME, NOT DIFFICULTY, IS WHAT CROSSES THE BAND HERE. COMPLEXITY STAYS MEDIUM: the judgement is concentrated in defining what counts as an orphan - which AC6 and the trimmed wheel's `RECORD` now settle mechanically rather than by declaration - and the digest is RECONCILED against a recorded value rather than trusted, so drift surfaces as a named failure instead of a silent wrong answer. The task remains inside the 2-hour bound at M.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T10)

FIRST-OBSERVATION DESTINATION (authorized, binding; added in extended review-fix cycle 4, finding 7). This task AUTHORS cases and records their FIRST observation, and that observation is a NON-DEFERRABLE acceptance condition consumed by 160.017-T's transition ledger. Review-fix cycle 3 authorized 160.002-T's spike record, 160.001-T's baseline, the pre-change byte captures (SIX in the current contract: three owned by 160.020-T, one each by 160.006-T, 160.007-T and 160.015-T) and 160.017-T's ledger, but NOT the first observations of the authoring tasks - so under freeze-scope this task had NO LEGAL DESTINATION FOR ITS OWN MANDATORY DELIVERABLE, the same class of self-contradiction cycle 2 fixed for schema_contracts.py and cycle 3 fixed for the baseline. A red recorded only in a transcript is uncitable, and 160.017-T cannot verify a transition pair whose first half does not durably exist.
DESTINATION: docs/audits/2026-09-03-ship10-payload-evidence/observations/T10.json - a subdirectory of the SINGLE bounded evidence surface already authorized for this shipment. NO NEW STORE, no new tool, no new convention.
RECORD CONTENT IS NOT SPECIFIED HERE (CYCLE 8, ITEM 7). The observation-record field set, every field's type, and every acceptance rule are defined ONCE, in the plan's canonical Evidence-verification contract (docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md, section "Evidence-verification contract for 160.017-T"). READ THAT CONTRACT; there is no second source and no local list. This record contributes ONLY its exact case set (the three cases enumerated above), its destination path, and its log bounds.
CONSUMPTION: 160.017-T joins these records to the canonical case table, verifies each independently against its evidence, and rejects any record that is missing, malformed or inconsistent - all exactly as the canonical contract states, and not restated here. A ledger row with no record, or a record with no ledger row, FAILS THE SHIPMENT; this is what makes verification V6 a machine check rather than a promise.
BOUND: records are APPEND-ONLY within the shipment and written ONLY by this task. No task writes another task's record, and 160.017-T READS them without modifying them. This is the ONLY write this task performs outside tests/**.
