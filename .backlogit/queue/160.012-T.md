---
artifact_type: task
created_at: 2026-09-03T09:18:38.7161169Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.001-T
    - 160.005-T
    - 160.016-T
id: 160.012-T
labels:
    - tests
    - e2e
    - upgrade
    - parity
parent_id: 160-F
priority: medium
status: queued
title: Upgrade-path and verify-workspace parity e2e tests
updated_at: 2026-09-03T10:29:41.1759459Z
---

=== REVIEW-FIX CYCLE 2 RE-SCOPE (finding 1) - READ FIRST, IT CHANGES WHEN THIS TASK RUNS ===

WHAT CHANGED: cycle 1 sequenced this task AFTER the wheel wiring (160.006-T) and the plugin wiring (160.007-T), so its cases were authored against an ALREADY-CHANGED payload and had NO RED OWNER ANYWHERE. That is two defects in one edge. (a) A verification family that first exists after the behaviour it verifies cannot distinguish "this still works" from "this never worked" - there is no prior observation to compare against. (b) It let install, upgrade, resolver, registration and integrity behaviour change with no test having been observed in a known state beforehand, which the shipment's test-first contract forbids.

THE EDGE IS NOW REVERSED. This task is BLOCKED BY 160.001-T (baseline capture), 160.005-T (RED-FIRST harness) and 160.016-T (CHARACTERIZATION harness), and it BLOCKS 160.006-T and 160.007-T. The wiring tasks cannot begin until this family has authored its cases and recorded its baseline observations. This is enforced by machine dependency edges, not by prose.

TWO-CLASS CONTRACT (plan section Test strategy). Every case this task authors MUST DECLARE ITS CLASS in the test file itself. The classes are mutually exclusive and exhaustive; a case declaring neither or both is a failure detected by the transition ledger (160.017-T).
- RED-FIRST: the case asserts behaviour that DOES NOT YET EXIST. It must be observed FAILING on the baseline before the implementing task begins, and observed passing after. Required observation pair: (red on baseline, green after implementation).
- CHARACTERIZATION: the case asserts an EXISTING, PRESERVED behaviour or invariant that CORRECTLY PASSES ON THE BASELINE. It is observed GREEN on the untrimmed baseline and must be observed GREEN AGAIN after trimming. Required observation pair: (green on baseline, green after change). A CHARACTERIZATION case is NOT required to be red, and MUST NOT be weakened, inverted, or otherwise broken to manufacture a red observation. Manufactured red is worse than no red: it destroys the signal for cases where red is real.
- If a CHARACTERIZATION case goes RED after the wiring tasks land, that is a REGRESSION CAUSED BY THE WIRING TASK, and the wiring task - not this one - is at fault.

BASELINE OBSERVATION IS A DELIVERABLE OF THIS TASK, not a side effect. Record, per case, the class and the observed baseline result, against the baseline artifacts captured by 160.001-T (untrimmed wheel, untrimmed sdist, untrimmed plugin payload, recorded verify-workspace finding set, recorded v1.5.0 registration failure modes). An observation with no recorded baseline reference cannot be replayed and does not satisfy this task.

=== ORIGINAL TASK BODY (unchanged except where the re-scope above overrides sequencing) ===
CLASS ASSIGNMENT FOR THIS FAMILY: parity and orphan cases are CHARACTERIZATION. test_verify_workspace_parity_trimmed_vs_baseline is by construction a comparison against a recorded prior state, so the baseline observation IS the test's other half - it cannot exist without one. Observe green on the baseline (parity with itself, and the recorded finding SET from 160.001-T), then green again for trimmed-vs-baseline.

Upgrade-path and verify-workspace PARITY verification against the TRIMMED built artifact. Split out of 160.008-T in Orchestrator review-fix cycle 1 (finding 7): the original task combined install-time boundary, upgrade/orphan, and resolver failure domains in one unit, exceeding the 2-hour rule and mixing three independent diagnosis surfaces.

Tests run against the BUILT ARTIFACT, never the source tree (verification V1, risk R1). Both cases consume the untrimmed baseline captured by 160.001-T (T2); that baseline is a hard prerequisite, not a convenience.

TESTS:
- test_verify_workspace_parity_trimmed_vs_baseline (AC6) - install a scratch workspace from the trimmed build, run `autoharness verify-workspace`, and assert ZERO NEW findings versus the captured untrimmed baseline. Parity is asserted as a set difference against the recorded baseline finding set, not as an absolute "no findings" claim - the untrimmed baseline may itself carry findings and those are not this shipment's regressions.
- test_upgrade_from_1_5_0_leaves_no_orphans (AC6, risk R3, verification V3) - install v1.5.0 (untrimmed) into a scratch workspace, upgrade to the trimmed build, enumerate residual engine files, and assert the residual set is EMPTY or matches an explicitly enumerated expected set. An orphan is an engine file present after upgrade that the trimmed manifest does not classify as payload.
- Assert `autoharness home` and `autoharness version` behave identically pre/post change (invariants I1, I2 observed, not modified).

SCOPE BOUNDARY: this task does NOT cover install-time payload boundary assertions (160.008-T) and does NOT cover channel resolver contracts (160.013-T). Those are separate failure domains with separate diagnosis paths.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. Verification-only task - it observes `_DATA_DIR`, `autoharness home`, and `autoharness version` and MUST NOT modify them (freeze-scope: src/autoharness/ runtime behaviour is read-only for this shipment). The v1.5.0 install used by the upgrade case is fetched from the already-published release and MUST NOT be mutated or retracted (invariant I5). Scratch workspaces are created under a gitignored temporary path; no tracked file may be deleted (destructive-operation checkpoint). NO-PUBLISH: this task must not run twine upload, gh release create, or trigger the release.yml publish path. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

ACCEPTANCE: AC6 (parity + upgrade clauses); invariants I1, I2, I5 observed unbroken. GREEN RESPONSIBILITY: this task owns green for its own two cases only.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T10)