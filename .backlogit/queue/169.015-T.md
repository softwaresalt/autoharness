---
artifact_type: task
created_at: 2026-09-19T04:23:28.4930707Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 169.017-T
id: 169.015-T
labels:
    - policy
    - p-002.7
    - activate
    - atomic
    - dogfood
parent_id: 169-F
priority: medium
status: queued
title: 'P-002.7 ACTIVATE: transcribe the clause into all four declared surfaces in one commit'
updated_at: 2026-09-21T00:17:00.0000000Z
---

ACTIVATE PHASE - ONE TASK, ONE COMMIT, ALL FOUR DECLARED SURFACES PLUS THE HARNESS-MANIFEST REFRESH DECISION D11 BINDS TO THEM - FIVE FILES IN ONE COMMIT, FOUR DECLARED SURFACES (decisions D2 and D11). ACTIVATION IS AUTHORIZED BY A VERDICT, NOT BY AN EDGE. FIRST ACTION, BEFORE ANY DECLARED SURFACE IS OPENED FOR WRITING: read the pre-activation readiness artifact .autoharness/gates/p002-7-preactivation-readiness.txt, written by 169.017-T, and FAIL CLOSED. THE GATE IS OPEN ONLY WHEN ALL OF THE FOLLOWING HOLD. WHOLE-FILE CONDITION: the file EXISTS, is READABLE, is NOT EMPTY, and its ENTIRE CONTENT IS EXACTLY ONE LINE - after stripping at most one trailing newline there is exactly one line and no other content, blank or otherwise. LINE CONDITIONS: that line begins with the literal prefix "PREACTIVATION_STATE: " and the token in its FIRST FIELD is the literal PREACTIVATION_READY, compared as an EXACT, CASE-SENSITIVE, BYTE-FOR-BYTE MATCH. FRESHNESS CONDITIONS F1-F5, as declared in the plan's Shared gate primitives section and RECOMPUTED BY THIS TASK against the repository: F1 head_commit is present exactly once, is 40 lowercase hex, and EQUALS this task's own reading of HEAD at the instant it evaluates; F2 candidate_digest is present exactly once, is 64 lowercase hex, and EQUALS this task's own CCD/v1 recomputation over EXACTLY the seven enumerated inputs in this order - templates/policies/workflow-policies.md.tmpl, .github/policies/workflow-policies.md, templates/agents/_ship.agent.md.tmpl, .github/agents/_ship.agent.md, tests/p002_7_candidate_definition.py, tests/p002_7_near_miss_fixtures.py, tests/test_p002_7_member_status_contract.py; F3 resolved_surface_count is present exactly once and equals 0; F4 checked is present exactly once, matches the literal RFC 3339 UTC form YYYY-MM-DDThh:mm:ssZ, and is NOT EARLIER THAN the committer timestamp of the commit named by head_commit; F5 binding is present exactly once, is 64 lowercase hex, and EQUALS this task's own B/v1 recomputation from the line's own head_commit, candidate_digest, resolved_surface_count and checked values under the tag p002-7-preactivation-binding/v1. EVERYTHING ELSE IS CLOSED: missing, unreadable, empty, more than one line of content, no PREACTIVATION_STATE: line, MORE THAN ONE such line, an unrecognised token, PREACTIVATION_BLOCKED, PREACTIVATION_NOT_OBSERVED, a foreign STATUS_CONTRACT_* token or COMPOSED_STATE: prefix appearing in this file, or ANY F1-F5 FAILURE - a head_commit that is not the current HEAD, a candidate_digest that does not match recomputation, a resolved_surface_count other than 0, a checked that is absent, malformed or earlier than the head commit's committer timestamp, a binding that does not match recomputation, or any of those fields absent or duplicated. ON A CLOSED GATE THIS TASK DOES NOT TOUCH ANY DECLARED SURFACE AT ALL - it makes no commit, writes no clause text, writes to NEITHER GATE ARTIFACT, EXITS NON-ZERO (EXIT CODE 1), and HALTS, returning the unit to STAGE. It exits zero ONLY when the gate was OPEN and the single activation commit landed. THE EDGE IS ORDERING, THE TOKEN IS THE AUTHORIZATION: the dependency edge on 169.017-T guarantees only that the readiness artifact EXISTS before it is read, and it clears on PREDECESSOR COMPLETION, which 169.017-T reaches on ALL THREE of its tokens - two of which leave the unit UNREADY. The edge is never offered as the safety argument. This gate exists because attempt-04 finding M1 (P1) recorded that revision 4 placed the unit's ONLY gate emitter AFTER this irreversible commit, contradicting decision D2's rollout invariant PREPARE then VERIFY then ACTIVATE, under which the complete evidence set is produced and adjudicated BEFORE any activation. THE FRESHNESS BINDING F1-F5 EXISTS BECAUSE ATTEMPT-05 FINDING N2 (P2) recorded that all three readiness line forms carried a checked= vintage field that R12 named as the stale-verdict mitigation while this task's five declared conditions never read it, so a stale PREACTIVATION_READY carried NO MECHANICAL FRESHNESS BINDING on the documented post-revert re-activation path. THAT PATH IS NOW CLOSED MECHANICALLY: after a reverted activation the four declared surfaces are BYTE-IDENTICAL to their pre-activation state, so a content digest over surfaces alone would still match - but git revert NECESSARILY CREATES A NEW COMMIT, so F1 FAILS ON head_commit and the gate is CLOSED even though the token still reads PREACTIVATION_READY. F1 IS THE SOLE MECHANISM ON THIS PATH. F4 DOES NOT FAIL HERE AND SUPPLIES NO INDEPENDENT COVER: it compares checked against the committer timestamp of the COMMIT NAMED BY head_commit - the stale line's own commit - which a stale line satisfies. Revision 6 asserted that F4 closed this path a second, independent way; attempt 06 recorded that false claim as O3 (P2). F4's real function is to reject a line whose vintage PRECEDES ITS OWN HEAD COMMIT, and with F5 to prevent checked being altered after emission. BECAUSE F1 IS THE SOLE MECHANISM, ONLY MERGE-FRIENDLY RECOVERY IS SUPPORTED: git revert of the activation commit, or any new forward commit. GIT RESET --HARD TO THE PRE-ACTIVATION COMMIT, AND ANY REBASE OR AMEND RESTORING THE PRE-ACTIVATION COMMIT IDENTITY, ARE PROHIBITED AND OUTSIDE THIS CONTRACT - they would restore the exact HEAD, declared-surface content and test material the stale line describes, so F1-F5 would all hold and the gate would open on evidence never re-adjudicated. Never treat a history-rewriting undo as an allowed rollback route. The remedy after any supported undo is the one six records already mandate: RE-RUN 169.017-T, which replaces the whole artifact and emits a FRESH BINDING. THE STRICTNESS IS DELIBERATE AND FAIL-CLOSED: any commit landing between VERIFY and ACTIVATE, even an unrelated one, closes this gate, because the evidence was adjudicated against a tree that no longer exists; that is the correct direction of error for a gate guarding an irreversible four-surface mutation. THIS TASK NEITHER PRODUCES NOR REINTERPRETS EVIDENCE. It reads ONE TOKEN, recomputes identities, and acts. It does not re-run the suite, does not re-derive or re-evaluate the evidence set, does not reinterpret a CLOSED gate as open on its own judgement, and WRITES TO NEITHER GATE ARTIFACT - .autoharness/gates/p002-7-preactivation-readiness.txt is owned solely by 169.017-T and .autoharness/gates/p002-7-status-contract-verdict.txt solely by 169.016-T. AN F1-F5 FAILURE IS A GATE FAILURE, NOT AN ARTIFACT REWRITE: it resolves this task's gate CLOSED and NEVER authorizes this task to rewrite, amend or delete the readiness artifact. RECOMPUTING AN IDENTITY IS NOT RE-DERIVING EVIDENCE: F1-F5 compute content and commit identities and produce NO ASSERTION OUTCOME, execute no test and read no assertion result, so TRANSCRIPTION ONLY - NO AUTHORING is untouched and no assertion is introduced that would need to be observed red first. THIS TASK ABSORBS the responsibilities of archived 169.001-T (policy template), 169.002-T (installed dogfood mirror) and 169.003-T (Ship agent pair). Attempt-01 A1 found those as SEPARATE TASKS joined by a blocks edge, which the plan's own ACTIVATE invariant and D2 forbid: sequential task edges are NOT atomic, so a mirror-divergence assertion that is live after the first commit and before the second fails at a commit that is on the branch. One commit is the ONLY shape that keeps every mirror pair consistent at every commit boundary. WRITE, IN A SINGLE COMMIT, the P-002.7 clause into all four enumerated surfaces: (1) templates/policies/workflow-policies.md.tmpl, (2) .github/policies/workflow-policies.md, (3) templates/agents/_ship.agent.md.tmpl, (4) .github/agents/_ship.agent.md. Each template and its installed mirror receive BYTE-IDENTICAL P-002.7 blocks. IN THAT SAME COMMIT AND THE SAME ROLLBACK UNIT, REFRESH THE HARNESS-MANIFEST CHECKSUMS OF THE TWO DECLARED SURFACES THAT ARE MANIFEST-TRACKED INSTALLED ARTIFACTS (decision D11). Surfaces (2) .github/policies/workflow-policies.md and (4) .github/agents/_ship.agent.md each carry an artifacts: entry in .autoharness/harness-manifest.yaml recording a sha256 of their pre-activation content; surfaces (1) and (3) are TEMPLATES and the manifest tracks NO template, so this commit refreshes EXACTLY TWO ENTRIES, not four. Rewrite each of those two checksum values to the sha256 of the file AS WRITTEN BY THIS COMMIT, then VERIFY CHECKSUM PARITY UNDER AN OWNED FAIL-CLOSED VERIFICATION CONTRACT BOUND TO THE EXACT INDEX SNAPSHOT THIS COMMIT RECORDS - NOT the generic verify-workspace command, which reports whole-workspace install state read from the WORKING TREE and classifies drift, does not adjudicate a named staged digest against a named artifacts: entry, and whose exit status is therefore NOT a checksum assertion. RUN THE VERIFICATION AFTER ALL COMMIT MEMBERS ARE STAGED AND BEFORE THE COMMIT IS CREATED: (1) ASSERT THE STAGED SET IS EXACTLY THE FIVE INTENDED COMMIT MEMBERS - git diff --cached --name-only equals, as a set, templates/policies/workflow-policies.md.tmpl, .github/policies/workflow-policies.md, templates/agents/_ship.agent.md.tmpl, .github/agents/_ship.agent.md and .autoharness/harness-manifest.yaml, with a MISSING member, an EXTRA staged path or a DUPLICATE entry each FAILING; (2) REJECT ANY UNSTAGED DIFFERENCE FOR THOSE MEMBERS - git diff --name-only restricted to the five must be EMPTY and none may be untracked, because if the working tree differs from the index for any member the bytes verified are not the bytes committed; (3) HASH THE INSTALLED ARTIFACTS FROM THE INDEX - for each of the two touched installed surfaces, recompute SHA-256 over the STAGED BLOB READ AS :<path> (git cat-file -p :.github/policies/workflow-policies.md, git cat-file -p :.github/agents/_ship.agent.md), NEVER a working-tree read; (4) PARSE THE MANIFEST FROM THE INDEX TOO, as :.autoharness/harness-manifest.yaml, so the entries compared against are the entries THIS COMMIT RECORDS rather than any the working tree may hold; (5) ADJUDICATE THE ENTRY SET TOTALLY - for each touched installed surface there is EXACTLY ONE artifacts: entry whose literal path matches, its primitive and template metadata are exactly as required and UNCHANGED by this commit, and its checksum equals the digest from (3) BYTE-FOR-BYTE, with a MISSING entry, an EXTRA entry (any artifacts: entry this commit adds or removes), a DUPLICATE path, a METADATA divergence or a CHECKSUM mismatch each FAILING; this activation installs NO NEW ARTIFACT, because surfaces (1) and (3) are templates and the manifest tracks no template, so NO NEW ENTRY IS REQUIRED HERE, while a future activation that does install a new artifact asserts the new entry EXISTS with exact path/template/primitive values before comparing its checksum; (6) RECORD THE INDEX IDENTITY AND FORBID RE-STAGING - record the index identity with git write-tree IMMEDIATELY AFTER (5) passes, then create the commit with NO INTERVENING INDEX MUTATION (no git add, git rm, git stash, checkout or restore); git commit -a and PATH ARGUMENTS to git commit are PROHIBITED, because both re-stage content after verification and would commit bytes the gate never adjudicated; (7) FAIL CLOSED BEFORE THE COMMIT EXISTS - steps (1)-(6) all run BEFORE the commit is created, so any failure among them, any unreadable input or any digest mismatch EXITS NON-ZERO AND THE COMMIT IS NOT CREATED, which is the whole of the pre-commit gate; and (8) ADJUDICATE ACCEPTANCE AFTER THE COMMIT IS CREATED - the commit is then created NORMALLY and this step DOES NOT PREVENT ITS CREATION and claims no such power; immediately afterwards compare git rev-parse HEAD^{tree} against the tree recorded in (6), and on a MISMATCH the commit records a tree the gate never adjudicated, so the activation is NOT ACCEPTED AND NOT PUBLISHABLE - it BLOCKS THE PUSH, blocks every downstream state token, verdict, manifest advance and handoff depending on this activation, and the LOCAL COMMIT MUST BE REVERTED OR CORRECTED AND THE WHOLE CONTRACT RE-RUN before any further step proceeds, AND NO DECLARED SURFACE IS LEFT ACTIVATED OR PUBLISHED AGAINST A STALE MANIFEST. THE VERIFICATION IS GATE LOGIC, NOT AN ASSERTION: it produces no assertion outcome, belongs to no assertion family, never enters the conformance suite, and therefore needs no prior red observation - exactly as the F1-F5 recomputation above does not. A commit that mutates either installed surface WITHOUT its manifest refresh leaves the manifest asserting a file this commit has already changed - an installed-artifact parity hole - and is an IMMEDIATE REVERT, not a fixup commit. THE MANIFEST IS A COMMIT MEMBER, NOT A DECLARED SURFACE: declared_surface_count REMAINS 4, the F2 CCD/v1 recomputation continues to run over EXACTLY the seven enumerated inputs with .autoharness/harness-manifest.yaml among NONE of them, F3's resolved_surface_count REMAINS 0, and 169.016-T's post-activation surface enumeration continues to exclude .autoharness/ exactly as 169-F declares. This is a FUTURE implementation contract: it authorizes no staging-time edit to the live manifest and none has occurred. ROLLBACK IS UNCHANGED IN SHAPE: git revert of this single commit restores all four declared surfaces AND both manifest checksums together, because they are one commit and one rollback unit. The Ship surfaces carry the intake-reconciliation note and the reverse cross-reference sentence naming the policy clause; the policy surfaces carry the vocabulary block, the three transition rows, the observed-version attribution paragraph, and the forward cross-reference sentence naming the Ship note. TRANSCRIPTION ONLY - NO AUTHORING. Every word written here already exists as test-owned data from 169.011-T at tests/p002_7_candidate_definition.py. That is the mechanism that keeps a four-surface commit inside the 2-hour rule: the cost is mechanical application, not composition, and the gate read is a bounded, fully-specified sequence of seven file reads and two git queries with no judgement in it. NO ASSERTION MAY BE ADDED HERE. Every assertion this commit turns green was observed failing, individually and discriminatingly, in 169.009-T, 169.010-T, 169.012-T, 169.013-T and 169.014-T, and was observed passing against the INERT CANDIDATE by 169.017-T before this task ran. An assertion appearing first in this commit is a TDD violation and a finding. IF THE 2-HOUR BOUND IS THREATENED, THIS TASK HALTS AND THE UNIT RETURNS TO STAGE. SHIP NEITHER SPLITS NOR NARROWS. Splitting the activation into two commits is prohibited under every circumstance. NARROWING IN PLACE IS ALSO PROHIBITED: reducing declared_surface_count changes which assertions the contract needs, and RE-DERIVING AN ASSERTION IS AUTHORING ONE, which this task's own TRANSCRIPTION ONLY - NO AUTHORING invariant forbids and which would put a never-red assertion into the suite after the production text exists - the attempt-08 B2 shape this unit was re-authored to eliminate. Attempt 02 finding G3 (P2) recorded that the previous R5 rule required exactly that. INSTEAD: stop, and return the shipment to STAGE for explicit scope redesign. Stage decides whether to reduce declared_surface_count, and if it does, it re-plans the consequences as ordinary work - retiring 169.010-T's bidirectional cross-reference family and its fixture OUTRIGHT (dropping the Ship-agent pair removes the reverse half of the cross-reference from every declared surface, so that family does not narrow, it disappears), and re-observing 169.014-T's surface-closure assertion RED against the new expectation - and then RE-RUNNING 169.017-T to produce a fresh PREACTIVATION_READY verdict, before any activation resumes. A stale readiness verdict from a superseded contract NEVER authorizes a revised activation, and since revision 6 that is ENFORCED by F1-F5 rather than only mandated in prose: a redesign changes the candidate definition, so F2 fails on candidate_digest even if no commit intervened. That is decision R5 applied to its purpose: a contract touching too many surfaces to activate in one task is too large a CONTRACT, and resizing a contract is a PLANNING act, not something Ship performs mid-task. THE TWO HALT TRIGGERS ARE INDEPENDENT AND BOTH END THE SAME WAY - a CLOSED pre-activation gate (wrong token, absent or malformed artifact, or any F1-F5 freshness failure) and a threatened 2-hour bound each stop this task with ZERO declared surfaces touched and return the unit to Stage. Blocked by 169.017-T, the pre-activation readiness gate, which is itself blocked by all five RED tasks; blocks 169.016-T, the post-activation confirmation. Stash 3EF5AAF2. Plan docs/plans/2026-09-18-post-claim-member-status-contract-plan.md (plan_id post-claim-member-status-contract-v2, revision 8, disposition PASS, terminal attempt 08 - gate_result PASS with p0_open 0, p1_open 0 and p2_open 0 at reviewed_content_head a192e50c, artifact docs/reviews/review-history/2026-09-18-post-claim-member-status-contract-v2-plan-review-attempt-08.md, review_terminal true and awaiting_attempt null). Verdict manifest docs/reviews/2026-09-18-post-claim-member-status-contract-v2-plan-review.md. Decision docs/decisions/2026-09-18-shared-execution-architecture-and-portfolio-reslicing-decision.md (revision 4, D11).