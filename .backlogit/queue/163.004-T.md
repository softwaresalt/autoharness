---
artifact_type: task
created_at: 2026-09-08T07:43:07.9370757Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 163.001-T
    - 163.002-T
    - 163.003-T
id: 163.004-T
labels:
    - skill
    - template
    - paired-edit
    - atomic
    - gate
    - fail-closed
parent_id: 163-F
priority: high
status: queued
title: 'U2 ATOMIC CORE: pre-mutation evidence record + fail-closed live-execution check (INDIVISIBLE)'
updated_at: 2026-09-08T07:43:07.9370757Z
---

DO NOT SPLIT. Paired edit, SAME COMMIT: .github/skills/shipment-reconcile/SKILL.md AND templates/skills/shipment-reconcile/SKILL.md.tmpl, hand-edited in parallel; the resolved dogfood copy is NOT regenerated (096-S). Indivisibility rationale: the record without the check is an artifact nobody consults (the 159-S state restated); the check without the record is an UNSATISFIABLE GATE - precisely the defect class the composed-workflow-protocol-state-machine-validation compound learning (cited by title only -- not yet committed to any branch; pending publication, tracked by stash 15A02E21 / follow-up 1CD92B69) was written about and that 161-F exists to repair. Shipping half reproduces it. AC: (a) EMISSION (RQ-1) - on the CASCADE path, before the Cascade Close Sub-Procedure step 1 invocation, Step 0(c) writes a durable record at .backlogit/reconcile/{shipment_id}-precascade-snapshot-{timestamp}.md containing (1) shipment_id, manifest item list, git HEAD SHA at collection time; (2) the qualifying feature member set BY REFERENCE to Step 0(c)'s own classification, never a separate re-derivation (preserves the existing reference-only rule at L733/L737); (3) per qualifying feature, the three engine-defined sources read VERBATIM with each candidate's resolved record location (queue/ or archive/) from Step 0(b)'s resolution rule; (4) the dedup + existence + artifact_type: deliberation validation outcome per candidate ID; (5) the resulting validated ID set recorded EXPLICITLY EVEN WHEN EMPTY (scan_performed: true, linked_deliberation_ids: []), never an omitted field; (6) collection_completed_at UTC, written and flushed BEFORE the cascade invocation. (b) FAIL-CLOSED CHECK (RQ-2) - before the cascade step 1 invocation, re-read the record and halt on either of two NEW, SEPARATELY LABELLED, INDEPENDENTLY FAILING tokens (never merged into one test): RECONCILE_FAIL_PRECASCADE_EVIDENCE_MISSING (no record for this shipment_id) and RECONCILE_FAIL_PRECASCADE_EVIDENCE_STALE (record exists but collection_completed_at is absent, or not strictly before the cascade invocation timestamp, or its recorded HEAD SHA / manifest item list does not match the state being closed). Both emit a P-005 violation and halt; NEITHER authorizes a fallback to safe-close. (c) ANTI-RECONSTRUCTION CLAUSE stated in-skill: a post-hoc reconstruction - EVEN ONE PROVABLY BYTE-IDENTICAL to what a live scan would have read - does NOT satisfy this check; the check is on the record's PRE-EXISTENCE, never on its REPRODUCIBILITY. (d) SINGLE AUTHORITATIVE STATEMENT (D-5) - state it ONCE in the Step 0(c) extension; the Cascade Close Sub-Procedure REFERENCES it. Do NOT add a fourth prose restatement of 'do this before'; the ordering is already stated three times (L473-475, L487-492, L747-749) and the defect is missing EVIDENCE, not a missing requirement (H-1). (e) PROTECTED INVARIANTS (H-3) - the three engine-defined sources and the exact matcher are unchanged; RECONCILE_FAIL_SNAPSHOT_AMBIGUOUS/_MISSING semantics unchanged (new tokens are additive); the reference-only allowed_ids/required_ids rule preserved; declared frontmatter status never inferred from storage location; the two-set gate's two conditions stay unmerged; mode: detect-mixed-role stays strictly read-only. (f) ORDERING/LOCK (H-4) - emission happens INSIDE the existing single-writer lock on .backlogit/queue/{shipment_id}.md, between Step 0(c) and the cascade invocation; introduces no new lock, no second writer, no new failure window; MUST NOT be hoisted before lock acquisition. (g) No Python source changes. (h) 163.001-T, 163.002-T and 163.003-T go GREEN. Depends on U1a/U1b/U1c. Plan: docs/plans/2026-09-08-shipment-reconcile-step-0c-live-pre-mutation-evidence-plan.md U2.