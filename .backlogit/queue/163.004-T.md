---
artifact_type: task
created_at: 2026-09-08T07:43:07.9370757Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 163.001-T
    - 163.002-T
    - 163.003-T
    - 033-DL
id: 163.004-T
labels:
    - skill
    - template
    - paired-edit
    - atomic
    - gate
    - fail-closed
parent_id: 163-F
priority: high
status: queued
title: 'U2 ATOMIC CORE: pre-mutation evidence record + fail-closed live-execution check (INDIVISIBLE)'
updated_at: 2026-09-10T22:44:10.3248995Z
---

DO NOT SPLIT. Paired edit, SAME COMMIT: .github/skills/shipment-reconcile/SKILL.md AND templates/skills/shipment-reconcile/SKILL.md.tmpl, hand-edited in parallel; the resolved dogfood copy is NOT regenerated (096-S). Indivisibility rationale: the record without the check is an artifact nobody consults (the 159-S state restated); the check without the record is an UNSATISFIABLE GATE - precisely the defect class the composed-workflow-protocol-state-machine-validation compound learning (docs/compound/2026-09-06-composed-workflow-protocol-state-machine-validation.md - published 2026-09-10, commit 5171ace1 on main; stash 15A02E21 and follow-up 1CD92B69 both reconciled and archived 2026-09-10) was written about and that 161-F exists to repair. Shipping half reproduces it. AC: (a) EMISSION (RQ-1) - on the CASCADE path, before the Cascade Close Sub-Procedure step 1 invocation, Step 0(c) writes a durable record at .backlogit/reconcile/{shipment_id}-precascade-snapshot-{timestamp}.md containing (1) shipment_id, manifest item list, git HEAD SHA at collection time; (2) the qualifying feature member set BY REFERENCE to Step 0(c)'s own classification, never a separate re-derivation (preserves the existing reference-only rule at L733/L737); (3) per qualifying feature, the three engine-defined sources read VERBATIM with each candidate's resolved record location (queue/ or archive/) from Step 0(b)'s resolution rule; (4) the dedup + existence + artifact_type: deliberation validation outcome per candidate ID; (5) the resulting validated ID set recorded EXPLICITLY EVEN WHEN EMPTY (scan_performed: true, linked_deliberation_ids: []), never an omitted field; (6) collection_completed_at UTC, written and flushed BEFORE the cascade invocation. (b) FAIL-CLOSED CHECK (RQ-2) - before the cascade step 1 invocation, re-read the record and halt on either of two NEW, SEPARATELY LABELLED, INDEPENDENTLY FAILING tokens (never merged into one test): RECONCILE_FAIL_PRECASCADE_EVIDENCE_MISSING (no record for this shipment_id) and RECONCILE_FAIL_PRECASCADE_EVIDENCE_STALE (record exists but collection_completed_at is absent, or not strictly before the cascade invocation timestamp, or its recorded HEAD SHA / manifest item list does not match the state being closed). Both emit a P-005 violation and halt; NEITHER authorizes a fallback to safe-close. (c) ANTI-RECONSTRUCTION CLAUSE stated in-skill: a post-hoc reconstruction - EVEN ONE PROVABLY BYTE-IDENTICAL to what a live scan would have read - does NOT satisfy this check; the check is on the record's PRE-EXISTENCE, never on its REPRODUCIBILITY. (d) SINGLE AUTHORITATIVE STATEMENT (D-5) - state it ONCE in the Step 0(c) extension; the Cascade Close Sub-Procedure REFERENCES it. Do NOT add a fourth prose restatement of 'do this before'; the ordering is already stated three times (L473-475, L487-492, L747-749) and the defect is missing EVIDENCE, not a missing requirement (H-1). (e) PROTECTED INVARIANTS (H-3) - the three engine-defined sources and the exact matcher are unchanged; RECONCILE_FAIL_SNAPSHOT_AMBIGUOUS/_MISSING semantics unchanged (new tokens are additive); the reference-only allowed_ids/required_ids rule preserved; declared frontmatter status never inferred from storage location; the two-set gate's two conditions stay unmerged; mode: detect-mixed-role stays strictly read-only. (f) ORDERING/LOCK (H-4) - emission happens INSIDE the existing single-writer lock on .backlogit/queue/{shipment_id}.md, between Step 0(c) and the cascade invocation; introduces no new lock, no second writer, no new failure window; MUST NOT be hoisted before lock acquisition. (g) No Python source changes. (h) 163.001-T, 163.002-T and 163.003-T go GREEN. Depends on U1a/U1b/U1c. Plan: docs/plans/2026-09-08-shipment-reconcile-step-0c-live-pre-mutation-evidence-plan.md U2.

--- REVISION R1 (2026-09-10, gate 033-DL, stash DDBF283E/EB23D1B9; plan section "Revision R1") --- R1 GOVERNS where it conflicts with the AC text above. STILL DO NOT SPLIT - the indivisibility rationale is STRENGTHENED by R1, because the anchor IS the record's proof, binding emission and check more tightly than before. WHY: AC (a)/(b) above proved pre-existence with collection_completed_at, the HEAD SHA and the manifest list - ALL THREE WRITTEN BY THE AGENT WHOSE COMPLIANCE IS BEING CHECKED, through the ordinary protocol surface. A byte-identical post-hoc reconstruction that BACKDATES the timestamp satisfied every clause, so the gate could not fail the 159-S history it exists to reject (a gate no illegitimate state can fail - the dual of 15A02E21's gate no legitimate state could satisfy). AMENDED AC: (a+) EMISSION - after writing the evidence record and INSIDE the existing single-writer lock, compute sha256 over the record's bytes and APPEND AN ANCHOR EVENT to .backlogit/logs/{shipment_id}.jsonl via backlogit's append operation, carrying the literal token PRECASCADE_EVIDENCE_ANCHOR, the shipment_id, the record's repo-relative path, and that sha256. The ENGINE - not the agent - writes the event's timestamp, actor and APPEND POSITION; the cascade mutation later appends its own shipment_status_changed / commit_tracked / archived events to the SAME file, so ordering is proved by RELATIVE APPEND POSITION IN AN ENGINE-OWNED FILE. Verified 2026-09-10 against live data: engine authorship (150-S/151-S/152-S logs), comment events carry engine timestamps (005-F/005-S/011-DL), logs SURVIVE ARCHIVAL ON THE LOCAL FILESYSTEM so the proof outlives the mutation it proves for a reviewer in the closing workspace (see AMENDMENT R1-11 for the repository-durability split), and 159-S.jsonl contains NO anchor before archived. (a++) DEMOTE collection_completed_at to CORROBORATING METADATA and state that demotion EXPLICITLY in one sentence in-skill; agreement between it and the anchor is NOT a PASS condition. (b+) FOUR separately labelled, independently failing tokens (never merged - the B57F9E24 rule; collapsing them destroys the diagnostic distinguishing "you never ran Step 0(c)" from "you reconstructed it afterwards"): EVIDENCE_MISSING (no record); EVIDENCE_STALE (record exists but HEAD SHA or manifest mismatch, or collection_completed_at absent); ANCHOR_MISSING (no PRECASCADE_EVIDENCE_ANCHOR event for this shipment_id, OR its sha256 does not match the record on disk); ANCHOR_NOT_PRE_MUTATION (anchor exists but does not precede the earliest engine-written mutation event in append order). All four emit P-005 and halt; NONE authorizes a safe-close fallback. The two pre-existing token NAMES are unchanged and their semantics NARROWED, not redefined. (c+) KEEP the anti-reconstruction clause verbatim and APPEND one sentence: the pre-existence evidence is the engine-written anchor's position relative to the engine's own mutation events, and NO FIELD THE COLLECTING AGENT AUTHORS IS LOAD-BEARING for that determination. (d) SINGLE AUTHORITATIVE STATEMENT extends to the anchor obligation. TORN-STATE (both directions fail closed, no compensating logic needed): record written but anchor append fails -> ANCHOR_MISSING halts the close, orphaned record is inert; anchor written but record write fails -> caught by the same token's digest-mismatch clause. UNCHANGED: no Python source change; no new files; every H-3 protected invariant; the blocks dependency on 169-S; U2 still authored against post-169-S text; NO retroactive-compliance claim for 159-S. THREAT MODEL - the target is TAMPER-EVIDENT AND OUT-OF-PROTOCOL, NOT TAMPER-PROOF: forging the anchor requires hand-splicing a line into an engine-owned append-only JSONL, unreachable through the agent's normal tool surface and itself a P-005 violation. Do not overstate the claim in the skill text.

--- AMENDMENT R1-11 (2026-09-10, review-fix cycle 1; plan section "R1-11", decision section "D-7") --- DURABILITY SPLIT. ADDITIVE TO R1; STILL DO NOT SPLIT; token count REMAINS FOUR. WHY: R1 described the anchor as durable and auditable without naming the audience. That was true of the LOCAL WORKSPACE and FALSE OF THE REPOSITORY - .backlogit/logs/ is covered by a workspace .gitignore rule, so a fresh-clone reviewer can see neither the anchor nor its append position, and the only evidence left visible to them would have been the agent-authored collection_completed_at that R1 exists to demote. TWO LAYERS, SEPARATELY NAMED, NEITHER SUBSTITUTABLE: L1 LOCAL RUNTIME ORDERING PROOF (GATE-BEARING) - the PRECASCADE_EVIDENCE_ANCHOR event in the engine-owned .backlogit/logs/{shipment_id}.jsonl, read inside the existing lock before the cascade invocation; ALL FOUR tokens of (b+) evaluate L1 AND ONLY L1; unchanged by this amendment. L2 REPOSITORY AUDIT EVIDENCE (NEVER GATE-BEARING) - at the closure commit for a shipment closed through the cascade path, commit (i) the evidence record under .backlogit/reconcile/ and (ii) THAT SHIPMENT'S OWN engine log .backlogit/logs/{shipment_id}.jsonl, force-added past the ignore rule, VERBATIM AND BYTE-UNMODIFIED. AMENDED AC: (e+) state the L2 obligation ONCE in the same Step 0(c) extension (the D-5 single-statement rule extends to it, referenced from the Cascade Close Sub-Procedure), and state IN THE SAME PLACE that it is NOT a halt token, NOT a gate input, and NOT a fallback. BOUND - EXACT, DO NOT WIDEN: one shipment's log, at its own closure commit, unmodified. This is NOT a general un-ignoring of .backlogit/logs/, NOT a bulk backfill of historical logs, and NOT a .gitignore change; logs for shipments not closed through this path stay ignored and untracked. WHY THE ENGINE'S OWN BYTES AND NOT AN AGENT-WRITTEN EXPORT: a transcription, excerpt or summary would reintroduce exactly the R1-0 defect - evidence authored by the party whose compliance is being checked. A force-add is a staging action over bytes the agent did not write; it cannot alter append order, and any alteration is a content change visible in the diff. WHAT A FRESH-CLONE REVIEWER CAN THEN VERIFY WITHOUT TRUSTING ANY AGENT-AUTHORED TIMESTAMP: (1) recompute sha256 over the committed evidence record and compare it to the sha256 in the committed anchor event; (2) confirm the anchor line precedes the engine-written shipment_status_changed / commit_tracked / archived lines IN APPEND ORDER; (3) confirm the record's HEAD SHA and manifest describe the closed state. collection_completed_at is consulted in NONE of the three; its (a++) demotion is preserved exactly. FAIL-CLOSED PRESERVED AND PLACED WHERE IT CAN ACT: L2 is written AFTER a mutation the gate already permitted, so it CANNOT be a pre-cascade token; a fifth token for it was CONSIDERED AND REJECTED as an unsatisfiable gate on an artifact that cannot yet exist (the 15A02E21 mistake). Instead, OPERATIONAL CLOSURE IS INCOMPLETE UNTIL L2 IS COMMITTED - a reportable P-001 condition. A missing L2 NEVER authorizes a close, NEVER downgrades a halt, and is NEVER a fallback for a failed L1 check; the mechanical close stands and only the PUBLICATION is unfinished. UNCHANGED BY R1-11: no Python source change; no new file (L2 force-tracks an engine log the close already writes); no .gitignore change; no new implementation unit and no new task; every H-3 protected invariant; the blocks dependency on 169-S; NO retroactive-compliance claim for 159-S.