---
artifact_type: task
created_at: 2026-09-18T06:36:12.997637Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 168.005-T
id: 168.007-T
labels:
    - runner
    - p-004
    - python
parent_id: 168-F
priority: high
status: queued
title: 'P-004 T7 (IMPLEMENTATION): P004Result per-test observation boundary and ID-addressed runner'
updated_at: 2026-09-18T15:38:08.3000029Z
---

Implement the P004Result observation boundary and its runner entry point in src/autoharness/ so the gate compares declared outcomes against PER-TEST observations rather than scanning merged runner output. STDLIB API FACTS (binding, corrected in revision 4): (a) TestResult.addFailure(test, err) and addError(test, err) receive err as an EXC_INFO TUPLE (exc_type, exc_value, traceback), NOT a preformatted string; the per-test formatted traceback text is produced by the INHERITED TestResult._exc_info_to_string(err, test), and P004Result MUST reuse that inherited formatting by calling super().addFailure/addError and then reading the appended entry from self.failures/self.errors, rather than re-implementing traceback formatting. (b) DO NOT assume unittest.loader._FailedTest.id() equals the requested name -- it is not a guaranteed API. Placeholder detection MUST instead rely on the documented positional correspondence of unittest.defaultTestLoader.loadTestsFromNames(names): the returned suite has one top-level element per requested name in order, so a placeholder is correlated to its requested name by POSITION, corroborated by isinstance against unittest.loader._FailedTest and by loader.errors. Detection is load-time, before the run, and classifies the name P004_MISSING_OBSERVATION, never a genuine red. Also override addSuccess, addSkip, addExpectedFailure and addUnexpectedSuccess so those outcomes are recorded in their own right and never folded into pass or fail; a skipped declared test is P004_MISSING_OBSERVATION. Emit the observed outcome map test_id -> (outcome, detail_text) and the exact-set-equality comparison against the declared map. Scope: src/autoharness/. Blocked by 168.005-T: the red contract tests are authored and observed failing FIRST, and this task turns them green. Stash 76EBDE6D. Plan docs/plans/2026-09-17-p004-red-phase-precondition-scoping-plan.md (revision 4). Verdict manifest docs/reviews/2026-09-17-p004-red-phase-precondition-scoping-plan-review.md (attempt 04, REMEDIATED-PENDING-REVIEW at plan revision 4 — local review cycle 2 returned BLOCKED at revision 3 on non-propagation of the design into these records; Stage remediation cycle 2 propagated it, and Stage does not review its own remediation, so the next reviewer pass is attempt 05).