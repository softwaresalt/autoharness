---
artifact_type: task
created_at: 2026-08-04T23:38:51.4036624Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-two-axis-v1
    size_source: agent
id: 109.011-T
parent_id: 109-F
priority: medium
status: queued
title: 'C1: CI topology-check entrypoint reading backlogit shared state'
updated_at: 2026-08-05T00:36:01.6728344Z
---



<!-- BEGIN:acceptance-criteria -->
* Entrypoint runs `gate pipeline-topology` in ci mode and returns fail-closed exit codes.
* NON-SHIPMENT-SCOPED CI INVOCATION: the entrypoint runs WITHOUT a human-supplied `--shipment`; it resolves the target deterministically from backlogit shared state (currently-claimed active shipment) and/or the branch under validation. When a target resolves it also enforces shipment-scoped readiness/ownership; the ambient global invariants (at-most-one-active, branch-to-claimed-shipment ownership) are always enforced fail-closed. The deterministic resolution rule is documented.
* Enforces global invariants readable from backlogit shared state.
* Does not depend on machine-local worktree state (documented: worktree uniqueness is local-only).
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
[Shipment C / CI] Implement a CI topology-check entrypoint (script/CLI mode) that reads backlogit shared state in a CI context and enforces the cross-machine invariants (at-most-one-active-shipment, branch-to-claimed-shipment ownership) as the NON-BYPASSABLE backstop, since local hooks are bypassable and Git has no pre-worktree-add hook. Runs non-shipment-scoped: the target is resolved deterministically from the currently-claimed shipment / branch under validation (no human-supplied `--shipment`), so shipment-scoped readiness is enforced when a target resolves and ambient invariants always run fail-closed.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Reuse the gate core (Shipment A). ci mode = fail-closed, no advisory-degrade.
<!-- END:implementation-notes -->