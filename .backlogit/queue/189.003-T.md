---
artifact_type: task
created_at: 2026-09-26T07:48:44.6007139Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: v1
    size_source: agent
dependencies:
    - 189.002-T
id: 189.003-T
labels:
    - harness-surface:harness-architect
    - ship-lifecycle
    - unit-b
    - unit-b-entry
parent_id: 189-F
priority: high
references:
    - docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md
    - docs/reviews/2026-09-25-ship-lifecycle-release-units-plan-review.md
status: queued
title: 'Ship lifecycle B5: harness resolve CLI adapter (one JSON document, empty stderr, exit equals exit_code)'
updated_at: 2026-09-26T07:48:51.2708684Z
---



<!-- BEGIN:acceptance-criteria -->
- Command 'harness resolve --workspace <path> --shipment <id> --json' in src/autoharness/cli.py. Parse errors behave as ordinary argparse (usage on stderr, exit 2, no document); --help prints help to stdout, exits 0 and emits no document.
- After a successful parse, --json writes exactly one UTF-8 document followed by one LF, as sys.stdout.buffer.write(doc + b"\n"), writes nothing to stderr (CR-B5), and exits with the document's exit_code.
- IM-03 (B5 half): all 47 codes through _resolve (small limits for read-limit codes); every code reachable with the defaults also through resolve_shipment and the CLI; every output validates against the B1 schema; stdout is exactly one document plus one LF; stderr is empty on every post-parse path; exit equals exit_code; parse errors emit no JSON. All fixtures are temporary workspaces (IM-10).
- Audit inventory: B4a's to B5's paths and the captured 'harness resolve --help' text are added to the audit inventory (see the IM-14-F51 carry below); tests/test_harness_noclaim_audit.py stays green, and any LEDGER entry B5 adds is a non-claim confirmed by local review.
- Closure evidence (plan Runtime Verification row B-entry): a recorded CLI transcript (bytes and SHA-256) for one fixture per state.
- RED record before implementation (IM-13, per the A1 wording): task ID, exact P-004 canonical command, native exit, and each roster test's outcome with its own marker AHLC_B5_CLI_ENVELOPE:<t> per the Marker Convention (pairwise distinct; a shared marker is refused under FI-9). The audit and captured-help structural tests reach no stub and are recorded outside the roster, like characterization tests.
- Environment prechecks: git worktree list shows one worktree; git status --porcelain is empty for the task's files before the first write; the canonical command runs from the repository root. Over the estimate by more than 30 minutes -> stop and return to Stage for a split.
- IM-14: no added text claims race, TOCTOU or hardlink-alias resistance.
- IM-15: a test or proof PASS confers no claim authority; ordinary P-001/P-002/P-004/P-020 gates at claim and closure.
- Manifest harvest carry IM-14-F51 (E4): the frozen 'B4a's to B5's paths and the captured harness resolve --help text are added to the audit inventory' cell is read as: FLOOR grows only by B1 (harness_surfaces.py) and D1 (harness_verdict.py); harness_surfaces.py is already in FLOOR (B1); the other B4a to B5 paths (src/autoharness/cli.py, tests/test_harness_surfaces_digest.py, tests/test_harness_surfaces_reducer.py, tests/test_harness_resolve_cli.py and the audit test itself) and the captured --help text are residue units under the Residue rule, not FLOOR entries.
- Manifest harvest carry CONST-II-F01 (E2): structural tests 'may pass'; record the pre-implementation failing run of the audit and captured-help structural tests as gap characterization (Principle II evidence), not as RED.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan task B5 (docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md, blob b7a77c76, section 'B5: CLI adapter'; release unit S(B-entry); review authority: manifest E5 Verdict under OP-5).

Files (budget): src/autoharness/cli.py; tests/test_harness_resolve_cli.py; tests/test_harness_noclaim_audit.py.

Change: 'harness resolve --workspace <path> --shipment <id> --json'. Parse errors behave as ordinary argparse (usage on stderr, exit 2, no document); --help prints help to stdout, exits 0 and emits no document. After a successful parse, --json writes exactly one UTF-8 document followed by one LF, as sys.stdout.buffer.write(doc + b"\n"), writes nothing to stderr (CR-B5), and exits with the document's exit_code. B4a's to B5's paths and the captured 'harness resolve --help' text are added to the audit inventory.

Marker: AHLC_B5_CLI_ENVELOPE (prefix). Trace: IM-03 (B5 half), IM-07 (CR-B5 stderr rule), PE-DATA-01 to 03, IM-10, IM-13, IM-14 / PE-SAFETY-06, IM-15. Estimate 80 min; size S; complexity medium.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Label harness-surface:harness-architect (Python production module under src/): roster and RED obligation apply. Blocked by B4b; last task of S(B-entry). Risky action (plan Plan Hardening): adding the public 'harness resolve' CLI command (Medium, ordinary pull request review); envelope test red -> B does not ship; the command stays inert until D3 in any case. Supersedes retired 181.007-T (the Ship-side consumer, IM-07, is D1, whose handoff test later runs this CLI in a subprocess).
<!-- END:implementation-notes -->