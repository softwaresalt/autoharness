---
artifact_type: task
created_at: 2026-09-03T08:25:02.000273Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.014-T
    - 160.008-T
    - 160.009-T
    - 160.010-T
    - 160.012-T
    - 160.013-T
id: 160.006-T
labels:
    - packaging
    - wheel
    - pyproject
    - release-risk
parent_id: 160-F
priority: medium
status: queued
title: Wire wheel packaging to the manifest, preserving core-metadata-version pins
updated_at: 2026-09-03T20:19:57.4822577Z
---

Wire the Python WHEEL channel to the payload manifest (T7), replacing the ad-hoc `[tool.hatch.build.targets.wheel.force-include]` table with the manifest-derived table.

SCOPE: this task wires THE WHEEL ONLY, and it is the SOLE EDITOR OF THE `pyproject.toml` BUILD TABLES in this shipment. CYCLE 9 NARROWING: it is NOT the sole editor of the FILE - 160.020-T (T0) owns the disjoint `[dependency-groups]` test-dependency section. See the SAFETY MODE region list below.

SDIST DE-SCOPED (cycle 5, finding 1). The withdrawn wording said the sdist "is wired by 160.019-T". 160.019-T is RETIRED (archived at .backlogit/archive/160.019-T.md) because the sdist is a third distribution channel discovered during plan review, outside the deliberated source request E9E5E6CC; it shares neither the plugin's allowlist mechanism nor its failure mode, so absorbing it was a P-021 C1 scope expansion. Its measured disclosure evidence is preserved in deferred entry 99818C6D. CONSEQUENCE FOR THIS TASK: the `[tool.hatch.build.targets.sdist]` table is now an OUT-OF-SCOPE NEIGHBOUR living in the SAME FILE this task edits. Leave it BYTE-IDENTICAL - including its `core-metadata-version` pin. No task in this shipment edits it, and no task will follow to repair it.

CLASS OWNERSHIP - TWO DIFFERENT DUTIES. A task cannot honestly "turn green" a case that already passes on the baseline, so the two duties are named separately and never merged.

RED-FIRST cases this task turns RED to GREEN:
- test_wheel_generated_table_matches_manifest (AC2c) - authored by 160.005-T. The COMMITTED wheel table equals the manifest-derived table. This is a property of the WIRING, which is why it is owned here and not by the generator task: 160.014-T is forbidden to commit the tables it produces.
- test_docs_root_guides_only (AC4) - authored by 160.005-T. Genuinely red today: the wheel currently ships all 642 docs files.
- test_wheel_contains_required_runtime_set (AC4) - authored by 160.005-T. RECLASSIFIED FROM CHARACTERIZATION TO RED-FIRST IN CYCLE 5 (FINDING 12). Measured at HEAD, the wheel's force-include table lists templates, schemas, four .github/ subtrees, both copilot instruction files, all of docs, and AGENTS.md - and OMITS .github/policies/** (carrying workflow-policies.md, referenced by installed agents) and scripts/** (whose deploy-harness/lock/ci-topology-check members are referenced by installed skills). The required-runtime assertion therefore FAILS on the baseline wheel: it is red, not baseline-true. This task turns it green by ADDING those paths when it derives the table from the manifest. Do NOT treat this as a preserved invariant - it is a defect this task fixes.
- test_upgrade_from_1_5_0_leaves_no_orphans (AC6, R3, V3) - authored by 160.012-T. Red by missing artifact; this task is one of its TWO green owners alongside 160.007-T.
- test_gate4_crossrefs_intact_in_built_payload (AC9, AC2d) - authored by 160.010-T. Red by missing artifact and by unimportable classifier; this task is one of its TWO green owners alongside 160.007-T.

CHARACTERIZATION cases this task must PRESERVE GREEN:
- Authored by 160.016-T: test_wheel_excludes_backlogit_explicitly, test_wheel_excludes_dev_directories, test_core_metadata_version_pins_preserved (BOTH pins - see I4 below). test_wheel_contains_required_runtime_set is NO LONGER in this list; cycle 5 finding 12 moved it to the RED-FIRST set above.
- Authored by 160.008-T: test_install_emits_only_generated_output, test_no_engine_records_in_target_workspace.
- Authored by 160.012-T: test_verify_workspace_parity_trimmed_vs_baseline, test_home_and_version_behave_identically.
- Authored by 160.013-T: test_data_dir_resolution_pip_install, test_data_dir_resolution_clone_editable, test_python_channel_needs_no_plugin_root_artifact.
- Authored by 160.009-T: test_register_phase_all_environments_python_channel.
These are green on the baseline. The wheel's existing force-include table already lists only templates, schemas, four .github/ subtrees, two instruction files, docs and AGENTS.md - so .backlogit/, tests/ and experiments/ are ALREADY absent from the wheel. They are INVARIANTS THIS TASK MUST NOT BREAK while rewriting the table, not behaviours it introduces. Recording a red observation for them would require deliberately breaking the wheel first, which is fabricated evidence. If any of them goes red during this task, THAT IS A REGRESSION IN THIS TASK, not progress.

This task does NOT own the release-path cases (160.015-T), the generator cases (160.014-T), or the plugin-wiring cases (160.007-T). There are no sdist cases.

APPROACH - hardening decision H1 (generated-and-asserted, NOT a dynamic build hook): hatchling's force-include is static TOML and cannot read the YAML manifest without a custom `hatch_build.py` hook. A build hook would add a new failure mode into the release pipeline - the hardest place to debug. Instead: the manifest is the source of truth, the pyproject wheel include table is GENERATED from it by the single command `python -m build_support.payload generate --channel wheel` (160.014-T), and a test asserts the two are equal. The table is a DERIVED artifact; hand edits are prohibited and detected by `generate --check`.

RELEASE-PATH GATE IS A SEPARATE TASK: AC2b - the unbypassable in-job payload-composition gate added to `.github/workflows/release.yml` - is delivered by 160.015-T. Editing a CI workflow is a different surface with a different blast radius than editing packaging config, and folding it in here would mix CI work with packaging work and push this task past the 2-hour rule. This task MUST NOT edit release.yml.

REMOVES: the `"docs" = "src/autoharness/data/docs"` force-include (all 642 files / 6.9 MB). Proven dead weight - verify_workspace.py resolves every docs/* path against workspace_path (the target workspace), never the packaged data dir. That "referenced != required payload" judgment is centralized in `build_support.payload.classify_target_workspace_path` (160.004-T) and must not be re-derived here.

INVARIANT I4 - NON-NEGOTIABLE: preserve `core-metadata-version = "2.4"` on BOTH the wheel AND sdist targets. These pins exist because hatchling 1.32.0 defaults to Metadata-Version 2.5, which the SHA-pinned pypa/gh-action-pypi-publish action's bundled twine < 7.0.0 REJECTS. See docs/compound/2026-08-30-unpinned-hatchling-metadata-version-vs-pinned-publish-action.md. Dropping either pin breaks publishing.

WHY BOTH PINS ARE STILL ASSERTED THOUGH ONLY ONE CHANNEL IS IN SCOPE: this task rewrites the WHEEL build table in a file that ALSO carries the out-of-scope sdist table. The CHARACTERIZATION case test_core_metadata_version_pins_preserved asserts BOTH pins, and its owner is now THIS TASK ALONE (cycle 5 - the mirrored obligation formerly held by 160.019-T has no holder because that task is retired). The sdist pin assertion is therefore an UNCHANGED-GUARD on a neighbouring table, protecting out-of-scope content from this task's same-file edit. It is not sdist work and does not re-import the sdist channel.

WARNING (verification V2): a local `uvx twine check` CANNOT reproduce the metadata failure - it always resolves the latest twine, and it checks whatever is in dist/, which after `uv build` is wheel AND sdist regardless of which channel this shipment governs. Local build success is NOT evidence that publishing will succeed. Metadata compatibility remains an explicit CP2 pre-publication check covering both artifacts.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. ELEVATED - this task edits a release-critical file.
- pyproject.toml: the WHEEL build table ONLY. Project metadata, dependencies, version, entry points, the sdist target, the `[tool.pytest.ini_options]` block, the `[dependency-groups]` test-dependency section owned by 160.020-T (cycle 9), and BOTH `core-metadata-version` pins are behaviour-preserving and must be asserted unchanged.
- .github/workflows/release.yml is OUT OF SCOPE for this task and must not be touched (see 160.015-T).
- SOLE EDITOR OF THE BUILD TABLES (cycle 5, NARROWED IN CYCLE 9): with 160.019-T retired, NO OTHER TASK IN THIS SHIPMENT EDITS THE `pyproject.toml` BUILD TABLES. The same-file serialization constraint that previously ordered this task before 160.019-T is DISSOLVED, and the corresponding dependency edges were removed. CYCLE 9 CORRECTION - THIS TASK IS NO LONGER THE SOLE EDITOR OF THE FILE ITSELF: 160.020-T (T0) adds the pinned pytest `[dependency-groups]` TEST-DEPENDENCY SECTION. THE TWO REGIONS ARE DISJOINT and this task MUST NOT touch the dependency group, just as T0 MUST NOT touch a build table. NO EXTRA SERIALIZATION EDGE IS NEEDED: T0 is a DAG root and reaches this task through T0 -> 160.005-T -> ... -> 160.006-T, so it always completes first. Do NOT write, reformat, or "tidy" the sdist table here - it is out of scope and unguarded except by the pin assertion above.
- ROLLBACK CHECKPOINT (binding, rollback contract R-1/R-3/R-4): before this task lands, record the exact PRE-CHANGE BYTES of pyproject.toml into docs/audits/2026-09-03-ship10-payload-evidence/ - the destination authorized for this task in the plan's H2 writable-surface table. THOSE RECORDED BYTES ARE EVIDENCE, NOT AUTHORIZATION. The DEFAULT and ONLY pre-authorized recovery path is a FORWARD REVERT COMMIT (R-1), which is non-destructive and auditable. A destructive single-file restore or overwrite of the working tree requires FRESH LIVE OPERATOR APPROVAL obtained in the session performing it, over a channel the agent cannot synthesize (R-3); no recorded byte capture, backlog comment, task text, or prior approval can substitute for it. If no such channel is available, HALT and leave the tree untouched (R-4) - a stalled rollback is recoverable, an unapproved destructive overwrite of uncommitted work is not.
- REVIEWED-PARTITION RECORD AND TWO-LAYER APPROVAL (cycle 6, findings 7 and 12). This task performs a MANDATORY TRACKED EDIT to pyproject.toml, so it is a Layer-2 applier. LAYER 1 is the generator (160.014-T), which computes the AC3e partition, prints it, and unconditionally refuses tracked OVERWRITE/REMOVE while taking NO approval input of any kind. LAYER 2 is this task: it obtains fresh live operator approval OUT-OF-BAND over the harness operator channel - never as a program input, because no signal a process can read satisfies R-3 - then applies THE EXACT REVIEWED DIFF and RE-VERIFIES THE RESULTING BYTES, halting on any difference from what was reviewed. It then APPENDS a bounded record of the reviewed partition (per-class path lists plus byte digests) to the SAME pre-change byte record above, at docs/audits/2026-09-03-ship10-payload-evidence/ - this task's already-authorized destination in the plan's H2 writable-surface table. NO NEW FILE, NO NEW SCHEMA, NO NEW DESTINATION, and the generator writes nothing there. 160.017-T is the READ CONSUMER of that record.
- NO-PUBLISH CHECKPOINT: this task MUST NOT run `twine upload`, `gh release create`, or trigger release.yml's publish path. Publication is Ship/Orchestrator scope (CP2).
- FREEZE-SCOPE: `src/autoharness/` is read-only (invariants I1/I2).
Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2 (writable-surface table).

ACCEPTANCE: AC1, AC3, AC4, AC2c, AC6, AC9, I4. The FIVE RED-FIRST cases above go red to green with the transitions recorded for 160.017-T's ledger; every CHARACTERIZATION case listed above is re-observed GREEN after the table rewrite and recorded as preserved; the `[tool.hatch.build.targets.sdist]` table is verified BYTE-IDENTICAL to its pre-change bytes.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size M, complexity medium. RE-CONFIRMED under the cycle-5 changes: one generated table replacing one hand-authored table in a release-critical file, with two pins and one neighbouring table that must survive byte-identical. The reclassified required-runtime case adds paths to the same generated table rather than a new surface.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T7)