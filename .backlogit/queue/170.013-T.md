---
artifact_type: task
created_at: 2026-09-19T00:36:23.2470921Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 170.012-T
id: 170.013-T
labels:
    - implementation
    - branch-resolution
    - ship-consumption
    - template
parent_id: 170-F
priority: high
status: queued
title: 'Branch T8b: Ship template consumes selected_branch for comparison AND creation'
updated_at: 2026-09-19T00:36:23.2470921Z
---

IMPLEMENTATION. Rewrite the Ship TEMPLATE branch COMPARISON site and branch CREATION site to consume the topology gate's emitted selected_branch VERBATIM, deleting every title-derived branch expression. THE DEFECT THIS FIXES: the Ship template currently derives the branch name from the work-item TITLE at both sites (around lines 188 and 197 of templates/agents/_ship.agent.md.tmpl), and never reads selected_branch at all. The consequence is that an explicit custom_fields.implementation_branch satisfies the FIRST gate - the only surface that reads it - after which Ship creates a DIFFERENT, title-derived branch, so the successful topology gate governs nothing. BOTH SITES MUST CHANGE: comparison alone is insufficient, because a Ship that compares correctly but creates a title-derived name still lands on the wrong branch. CONTRACT: (1) read selected_branch from the gate JSON; (2) compare against it verbatim; (3) create exactly it; (4) on a BLOCKED gate, HALT - never fall back to a derived name; (5) pass the value across the `--` terminator as fixed argv so a leading-hyphen-adjacent name is data, not a flag. PRESERVED UNCHANGED: the two-rung resolver precedence (explicit_contract then title_alias) and the leading-hyphen safety property both remain exactly as specified; this task changes the CONSUMER, not the resolver. Scope: templates/agents/_ship.agent.md.tmpl. Blocked by 170.012-T (the RED fixture). Atomic with the installed-mirror task. Stash 86498B64 + 14F4D6F3. Plan docs/plans/2026-09-17-workspace-authoritative-branch-resolution-plan.md (plan_id workspace-authoritative-branch-resolution, plan_role active, revision 7). Verdict manifest docs/reviews/2026-09-17-workspace-authoritative-branch-resolution-plan-review.md (latest_attempt 07, verdict REMEDIATED-PENDING-REVIEW at plan revision 7, awaiting independent attempt 08).