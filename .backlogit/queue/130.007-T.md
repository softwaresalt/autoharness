---
artifact_type: task
created_at: 2026-08-18T00:37:30.7681417Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 130.006-T
id: 130.007-T
labels:
    - tests
    - contract
    - anti-regression
parent_id: 130-F
priority: medium
status: active
title: Add checkpoint payload contract tests across templates and mirrors
updated_at: 2026-08-18T01:23:29.95778Z
---

Surface: new tests/test_checkpoint_payload_contract.py. Follow the structure of tests/test_crash_resumption_protocol.py (module-level path constants, _read helper, per-surface test classes, the existing _assert_checksum_matches idiom); reuse that pattern rather than duplicating its logic. Assertions: (1) CONTRACT PRESENT - the canonical Checkpoint Payload Contract section exists in templates/instructions/backlogit.instructions.md.tmpl AND .github/instructions/backlogit.instructions.md. (2) SCHEMA_VERSION MANDATED - "schema_version": 1 is required at all four agent-template write sites (_stage.agent.md.tmpl x2, _ship.agent.md.tmpl x2) and in both agent mirrors. (3) OFFICIAL CREATE PATH - each write site references the official create operation (backlogit_create_checkpoint / backlogit checkpoint create) and prohibits writing checkpoint files directly. (4) CONTEXT NESTING - each surface requires domain fields nested under context. (5) NEGATIVE / ANTI-REGRESSION - the malformed top-level shape cannot be produced by following the text: assert no surface instructs top-level placement of feature_id, shipment_id, stash_source, mode, route or artifacts. Per hardening H8 this assertion MUST be anchored to the contract block and the fenced example and match instructional patterns - NOT bare substring scans of the whole document, since those words appear legitimately in unrelated prose and a spuriously failing test will simply be deleted. (6) FENCED EXAMPLE CONFORMANT - extract the JSON example from the overlay template and mirror, json.loads it, assert schema_version == 1, assert context is a dict, and assert none of the domain keys appear at top level. This is the assertion that would have caught the original defect. (7) MANIFEST COHERENCE - checksums match on-disk bytes for every artifact this shipment touched, and the single-checksum shape is preserved. Also verify tests/test_crash_resumption_protocol.py still passes. Per hardening H11 run pytest via the installed interpreter: uv run currently fails here with a TLS handshake failure fetching hatchling, and that environment failure MUST NOT be reported as a shipment test failure. Depends on all prior tasks. Reference: docs/plans/2026-08-17-checkpoint-payload-contract-plan.md (T7).