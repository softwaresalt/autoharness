---
artifact_type: task
created_at: 2026-08-28T03:48:32.0066975Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 148.001-T
id: 148.002-T
labels:
    - S0
    - code
    - verify-workspace
parent_id: 148-F
priority: medium
references:
    - docs/plans/2026-08-27-policy-registry-and-review-persona-layer-plan.md
    - src/autoharness/verify_workspace.py
status: active
title: U2 — Reconcile the policy-registry resolution contradiction in verify_workspace
updated_at: 2026-08-29T09:06:59.5411266Z
---

Correct the _resolve_policy_registry docstring/contract in verify_workspace.py, which asserts the dogfood self-install never installs a policies mirror — false after U1. Preserve the template-fallback branch. Domain: code. Files: 1.

<!-- BEGIN:acceptance-criteria -->
* Docstring accurately describes installed-first precedence with retained template fallback.
* **INV-2**: the template-fallback branch is preserved; its characterization test is written and passing BEFORE the edit.
* 2 precedence scenarios green: (a) installed present -> installed wins; (b) installed absent + `autoharness_home` present -> template wins.
* **INV-3**: no `required` flag, severity, or exit code edited in this shipment. `verify-workspace` exit-code behavior unchanged pre/post — a change is a HARD STOP, not a finding.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan unit **U2**. `src/autoharness/verify_workspace.py` contains two contradictory contracts about the same path:

* `_resolve_policy_registry` (L4027-L4045) documents *"the dogfood self-install **never** installs a policies mirror"* and falls back to the template.
* `DARK_FACTORY_ASSERTIONS[dark_factory_policy_contract]` (L866) sets `required: True` on that exact path.

After U1 the first statement becomes **factually false**, so its docstring and the assumption it encodes must be corrected — otherwise it becomes a stale normative surface (family F03a, the very class this programme exists to catch).

**Change**: correct the docstring to state installed-first precedence with template fallback retained for *target* workspaces that legitimately have no mirror. **Keep the fallback branch intact** — removing it would break non-mirrored installs. No change to the precedence order itself.

**Execution posture**: characterization-first — capture current resolution behavior in a test BEFORE touching the docstring/branch, so the fallback is demonstrably preserved.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on U1 (the registry must exist before the engine's contract is reconciled).

`verify_workspace.py` is 226 KB and high-traffic. Confine the change to one function's docstring/comments. Do not refactor.

RK-B: installing the registry flips `dark_factory_policy_contract` from missing to *evaluated*, which may surface NEW assertion failures previously masked by the missing file. That is a **status-change, not a regression** — report newly-surfaced failures as findings; do not silently patch them.
<!-- END:implementation-notes -->