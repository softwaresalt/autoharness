---
artifact_type: task
created_at: 2026-09-26T08:17:11.1112296Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: v1
    size_source: agent
dependencies:
    - 191.002-T
id: 191.003-T
labels:
    - harness-surface:none
    - ship-lifecycle
    - unit-d
parent_id: 191-F
priority: high
references:
    - docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md
    - docs/reviews/2026-09-25-ship-lifecycle-release-units-plan-review.md
status: queued
title: 'Ship lifecycle D3: Ship activation (one task, one commit: template, mirror, manifest checksum)'
updated_at: 2026-09-26T17:47:12.3725803Z
---



<!-- BEGIN:acceptance-criteria -->
- Preflight, all required; any failure halts before the first write: (1) template and mirror blobs at the parent equal FI-12; the manifest's Ship mirror entry equals its 08787a4b value and the SHA-256 of HEAD:.github/agents/_ship.agent.md, and every other manifest change since 08787a4b is listed with its commit; (2) IM-01 CI evidence and the Windows run exist for C (IM-02); (3) B's suite, including the IM-16 tests, is green; (4) the IM-10 unit has shipped: S(IM-10) 196-S is shipped (enforced by the 196-S -> S(D) blocks edge; the render-parity resolver is not run against harness-architect until IM-10 ships), and 'harness resolve' over a real shipment whose tasks declare harness-architect returns HARNESS_READY / 0 / ALL_SURFACES_PRESENT, accepted by D1; (5) D2's tests fail as recorded; (6) the non-claim audit passes with no skip.
- Change: exactly templates/agents/_ship.agent.md.tmpl, .github/agents/_ship.agent.md and .autoharness/harness-manifest.yaml (only the Ship mirror entry's checksum and note): replace the passages named in charter 6.7 and insert the per-task pre-claim step in the mirror, as D2 asserts; refresh the Ship mirror checksum from the raw staged blob (IM-12).
- Mode: careful and freeze-scope (constitution VIII) for D3 and its rollback; the freeze boundary is the three paths. Any unplanned change to the three paths since 08787a4b (for example, overlap with 177-S, 178-S or 180-S) halts and returns to Stage.
- Verification: D2 green; the targeted and the whole canonical suite green; 'git show --name-only' of the commit lists exactly the three paths; the checksum replayed from HEAD:<path>. Landed by merge commit only (P-009); after merge, the merge commit has two parents and the three paths equal the activation commit's blobs.
- Residue (IM-14): Ship audits D3's prepared template and mirror lines before D3 is presented ready (readiness record).
- Manifest harvest carry IM-14-F55 (E4): at S(D)'s closure the shipment merge diff repeats D3's checked-off lines; skip them unless changed (the incremental Residue rule) and re-audit only the affected lines.
- Rollback: one rollback change, with fresh operator approval bound to the listed SHAs: revert D4's commit (if merged), then <activation-sha>, then D2's test commit, in that order; then replay the three paths' checksums from HEAD:<path>, confirm the template and mirror blobs equal their pre-activation values, and run the whole canonical suite green. The rollback lands through a chore/ branch pull request merged by merge commit (P-009), and the approved SHAs are rechecked against that branch before merge. D1 stays.
- Manifest harvest carry CONST-II-F01 (E2) determination: not applied to D3, because D3 adds no test; its structural evidence is D2's recorded gap characterization, checked by preflight (5), and D2 green after activation.
- Environment prechecks: git worktree list shows one worktree; git status --porcelain is empty for the task's files before the first write; the canonical command runs from the repository root. Over the estimate by more than 30 minutes -> stop and return to Stage for a split.
- IM-14: no added text claims race, TOCTOU or hardlink-alias resistance.
- IM-15: a test or proof PASS confers no claim authority; ordinary P-001/P-002/P-004/P-020 gates at claim and closure.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan task D3 (docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md, blob b7a77c76, section 'D3: Ship activation (one task, one commit)'; release unit S(D); review authority: manifest E5 Verdict under OP-5).

Files (budget): exactly templates/agents/_ship.agent.md.tmpl, .github/agents/_ship.agent.md and .autoharness/harness-manifest.yaml (only the Ship mirror entry's checksum and note).

Change: after the six-part preflight, replace the passages named in charter 6.7 and insert the per-task pre-claim step in the mirror, as D2 asserts; refresh the Ship mirror checksum from the raw staged blob (IM-12). One task, one commit.

Trace: IM-08 / PE-ACTIVATE-02, IM-01 / IM-02 (preflight 2), IM-10 (preflight 4), IM-12, IM-14 (preflight 6, readiness record), IM-15. Estimate 110 min; size M; complexity medium.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Label harness-surface:none (agent text and manifest only, no src/ Python). Blocked by D2; blocks D4. IM-10 dependency: S(IM-10) 196-S (feature 190-F, tasks 190.001-T and 190.002-T) must be shipped before D3's preflight (4) can pass; the 196-S -> S(D) shipment edge enforces it, and preflight (4) re-checks it. Supersedes retired 181.004-T (pre-activation evidence, now D3 preflight; its 'marker-bearing assertion failure' rule is superseded by FI-9) and 181.005-T (three-file activation). After activation, the post-activation monitoring (first three Ship sessions record each T1 outcome; owner: the operator) is recorded in Ship's docs/memory/ session notes.

PR #457 review (thread PRRT_kwDORzpWpM6lc0L1, deferred stash 4AAE9329): the activated Ship mirror's and template's same-route-guard literals (_ship.agent.md ~820-821) must state the current config routes - Ship == tier3 == claude-opus-5.5/anthropic/high, with the explicit escalation route gpt-6-sol/openai/xhigh keeping escalation genuine - not the stale claude-sonnet-5 / claude-opus-5 text.
<!-- END:implementation-notes -->