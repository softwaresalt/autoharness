---
artifact_type: task
created_at: 2026-09-03T08:26:01.210017Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 160.010-T
labels:
    - tests
    - integrity
    - gate4
    - version-resolution
parent_id: 160-F
priority: medium
status: queued
title: Gate 4 cross-reference and AUTOHARNESS_VERSION resolution tests
updated_at: 2026-09-03T08:26:01.210017Z
---

Close the two integrity gaps found in plan review (P1-3, P1-5).

TEST 1 — test_gate4_crossrefs_intact_in_built_payload (AC9, finding P1-3):
Constitution Quality Gate 4 requires that all referenced files, skills, and agents exist. Trimming can break this INSIDE the delivered artifact while the source tree stays perfectly valid — so Gate 4 must run against the BUILT PAYLOAD, not the source tree.
- No shipped file may contain a relative reference to a manifest-excluded path.
- Where a shipped engine file legitimately MENTIONS an excluded path as instructional text, record it in an explicit (file, reference) allow-list rather than silently tolerating it. Follow the established (rule, file_glob) tuple precedent in docs/compound/012-S-portability-scan-allow-list.md.

TEST 2 — test_version_resolves_on_plugin_install_without_cli (AC10, finding P1-5):
install-harness FAILS the install if {{AUTOHARNESS_VERSION}} is unresolved (its Step 4.1). Resolution chain: (1) `autoharness version` CLI; (2) autoharness_home/pyproject.toml or src/autoharness/__init__.py; (3) for plugin installs with no Python CLI, the plugin/package manifest version.
The plugin channel excludes src/ and pyproject.toml, so source (3) is the ONLY remaining resolver for plugin-only consumers.
- Install from the trimmed plugin payload with NO Python CLI on PATH.
- Assert the manifest records a CONCRETE version and never a literal {{AUTOHARNESS_VERSION}}.
- Assert plugin.json and .github/plugin/marketplace.json are present by name.

ACCEPTANCE: AC9, AC10.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T10)