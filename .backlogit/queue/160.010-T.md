---
artifact_type: task
created_at: 2026-09-03T08:26:01.210017Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.001-T
    - 160.005-T
    - 160.016-T
id: 160.010-T
labels:
    - tests
    - integrity
    - gate4
    - version-resolution
parent_id: 160-F
priority: medium
status: queued
title: Gate 4 cross-reference and AUTOHARNESS_VERSION resolution tests
updated_at: 2026-09-03T17:34:48.6966314Z
---

GATE 4 CROSS-REFERENCE AND VERSION-RESOLUTION CASES (T13). Authored and observed against the BASELINE build, then re-observed against the trimmed build by the transition ledger (160.017-T).

SEQUENCING (machine-encoded, not prose). This task is BLOCKED BY 160.001-T (baseline capture), 160.005-T (RED-FIRST harness) and 160.016-T (CHARACTERIZATION harness), and it BLOCKS 160.006-T and 160.007-T. A verification family that first exists after the behaviour it verifies cannot distinguish "this still works" from "this never worked".

TWO-CLASS CONTRACT (plan section Test strategy). Every case MUST DECLARE ITS CLASS IN THE TEST FILE ITSELF, PER CASE. BOTH CASES IN THIS FAMILY ARE RED-FIRST; there is no family-wide label, and if a future case is added it declares its own class rather than inheriting one.
- RED-FIRST: asserts behaviour that DOES NOT YET EXIST. Observed FAILING on the baseline, passing after implementation.
- CHARACTERIZATION: asserts an EXISTING, PRESERVED behaviour that CORRECTLY PASSES ON THE BASELINE. NOT required to be red, and MUST NOT be weakened or inverted to manufacture one.

BASELINE OBSERVATION IS A DELIVERABLE, not a side effect. Record, per case, the class and the observed initial result against 160.001-T's artifacts, loading the structured (non-prose) half from tests/fixtures/payload-baseline/**.

CASE 1 - test_gate4_crossrefs_intact_in_built_payload (AC9, AC2d) - CLASS: RED-FIRST.
OBSERVED INITIAL STATE: RED. Two independent causes, either sufficient: (i) MISSING ARTIFACT - the case is scoped to the TRIMMED built payload, which does not exist yet; (ii) IMPORT ERROR - `build_support.payload.classify_target_workspace_path` is not importable until 160.004-T lands.
THE CLASS AND SCOPE WERE CORRECTED IN REVIEW-FIX CYCLE 3. This record previously declared the case CHARACTERIZATION on the reasoning that Gate 4 holds on the untrimmed baseline. It does hold there - VACUOUSLY, because the untrimmed payload contains everything, so no reference can dangle. A case that is green only because nothing was removed asserts nothing about removal. The case is therefore scoped to the TRIMMED artifact and is RED-FIRST.
ASSERTS: Constitution Quality Gate 4 - all referenced files, skills and agents exist - INSIDE THE BUILT PAYLOAD, not the source tree. Trimming can break Gate 4 inside the delivered artifact while the source tree stays perfectly valid.
- No shipped file may contain a relative reference to a manifest-excluded path.
- Where a shipped engine file legitimately MENTIONS an excluded path as instructional text, record it in an explicit (file, reference) allow-list rather than silently tolerating it. Follow the established (rule, file_glob) tuple precedent in docs/compound/012-S-portability-scan-allow-list.md.
- The "this reference resolves against the target workspace, not the payload" decision MUST be obtained by CALLING `classify_target_workspace_path`. This case must NOT re-list the docs/* prefixes locally. **THE PARENTHETICAL AUTHORITY "AC2d single-occurrence rule, scoped to executable and config surfaces" IS DELETED (CYCLE 11, ITEM 9). THERE IS NO SINGLE-OCCURRENCE RULE** - it was withdrawn as unsatisfiable in cycle 6, finding 19 (57 occurrences across 23 files on the surface it claimed to govern, 8 of them in freeze-scoped `src/**`), and citing it here kept a withdrawn rule alive as live authority. The current AC2d binding is the four canonical predicates P1-P4. The no-local-re-listing requirement SURVIVES ON ITS OWN FOOTING as P2, the centralized-derivation predicate: a case that re-lists the prefixes is not exercising the centralized classifier at all, so it would pass even if the classifier were wrong.
GREEN OWNER: 160.006-T / 160.007-T.

CASE 2 - test_version_resolves_on_plugin_install_without_cli (AC10) - CLASS: RED-FIRST.
OBSERVED INITIAL STATE: RED by MISSING ARTIFACT - there is no trimmed plugin payload to install from.
ASSERTS: install-harness FAILS the install if the AUTOHARNESS_VERSION template variable is unresolved (its Step 4.1). The variable is named SYMBOLICALLY here; its exact delimited literal form appears in this record ONLY inside the fenced fixture block below (corrected in extended review-fix cycle 4, finding 12 - this paragraph previously carried the bare delimited token in ordinary prose while the record simultaneously claimed to carry none, so the record contradicted itself and any repository-wide unresolved-placeholder scan would read the prose occurrence as a real unresolved variable). The resolution chain is: (1) `autoharness version` CLI; (2) autoharness_home/pyproject.toml or src/autoharness/__init__.py; (3) for plugin installs with no Python CLI, the plugin/package manifest version. The plugin channel excludes src/ and pyproject.toml, so source (3) is the ONLY remaining resolver for plugin-only consumers.
- Install from the trimmed plugin payload with NO Python CLI on PATH.
- Assert the manifest records a CONCRETE version and never an unresolved literal placeholder. The exact placeholder token is asserted via a FIXTURE CONSTANT, never re-typed at an assertion site.

THE EXACT LITERAL, RESERVED TO THIS FENCED FIXTURE EXAMPLE (the single permitted occurrence of the delimited form in this record; everywhere else the variable is named symbolically as AUTOHARNESS_VERSION):

```
# tests/fixtures/version-placeholder.py
UNRESOLVED_VERSION_TOKEN = "{{AUTOHARNESS_VERSION}}"
```

The assertion is: the plugin/package manifest version field equals a CONCRETE version string and is NOT EQUAL to UNRESOLVED_VERSION_TOKEN. Importing the constant rather than re-typing the delimited form is what keeps the prose of this record, and of any other record citing it, free of tokens that a placeholder scan would flag.
- Assert plugin.json and .github/plugin/marketplace.json are present by name.
- The plugin-root VERSION RESOLUTION contract is specified by 160.013-T (AC6b), whose case test_plugin_version_resolves_from_plugin_manifest asserts the RESOLVER behaviour. THIS case consumes that contract at the INSTALL-HARNESS layer and does not redefine it; the two are complementary, not duplicates.
GREEN OWNER: 160.007-T.

SCOPE BOUNDARY: payload composition is 160.005-T / 160.006-T / 160.007-T; install boundary is 160.008-T; upgrade/parity is 160.012-T; resolver contracts are 160.013-T; registration is 160.009-T. This task covers CROSS-REFERENCE INTEGRITY and VERSION RESOLUTION only.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. Verification-only. The no-Python-CLI environment is simulated under THE SINGLE DECLARED SCRATCH ROOT `dist/.autoharness-scratch/<run-id>/` (plan AC3e; CORRECTED IN CYCLE 11, ITEM 3 - the withdrawn generic "gitignored temporary path" wording named no root and admitted an OS temporary directory, and is DELETED). `<run-id>` is a per-run UUID4 hex string; CONTAINMENT IS VALIDATED CANONICALLY - the resolved scratch path MUST be a strict descendant of the resolved workspace root, and a validation failure FAILS CLOSED. The root is NEVER taken from `TMPDIR`, `TEMP`, `TMP`, or any OS temporary-directory API, and `tempfile.TemporaryDirectory()` and the pytest `tmp_path` / `tmp_path_factory` fixtures ARE PROHIBITED for it. It is gitignored by the existing `dist/.gitignore`, and it is EXCLUDED FROM CLASSIFIER AND PAYLOAD INPUT BEFORE MATCHING. IT IS NEVER AUTOMATICALLY DELETED - cleanup is a separate operation requiring destructive approval, with a scrubbed PATH; the test MUST NOT uninstall, downgrade, or otherwise mutate the developer's real interpreter or installed distributions, and MUST NOT delete any tracked file. `src/autoharness/` is read-only. ITS ONLY AUTHORIZED WRITE OUTSIDE ITS OWN TEST FILES IS ITS FIRST-OBSERVATION RECORD at docs/audits/2026-09-03-ship10-payload-evidence/observations/T13.json (see FIRST-OBSERVATION DESTINATION below); this is stated here, in the operative write-surface clause, because a destination named only further down the record would still read as unauthorized under freeze-scope (extended review-fix cycle 4, finding 10). **EVIDENCE WRITE SURFACE - TWO AUTHORIZED DESTINATIONS, BOTH OPERATIVE (CYCLE 12, ITEM 8).** This task is an EVIDENCE PRODUCER and is authorized to write BOTH: (1) its first-observation record at `docs/audits/2026-09-03-ship10-payload-evidence/observations/T13.json`, AND (2) its bounded raw logs under its OWN subdirectory `docs/audits/2026-09-03-ship10-payload-evidence/observations/logs/T13/**` and nowhere else. **ANY "ONLY AUTHORIZED WRITE ... IS ITS OBSERVATION RECORD" WORDING IS WITHDRAWN AS A CONTRADICTION:** the evidence contract REQUIRES a raw log for every observation (`evidence_ref`/`evidence_sha256`/`evidence_bytes` are mandatory fields and 160.017-T validates the referenced log), so a safety clause authorizing only the JSON would have made this task's own mandatory evidence an unauthorized write under freeze-scope. BOTH destinations sit inside the SAME single bounded evidence surface; NEITHER is a second store. Log bounds are FAIL-CLOSED per 160.017-T (<=256 KiB AND <=2,000 lines per log; halt and report rather than truncate). THIS AUTHORIZES DESTINATIONS ONLY: this task DOES NOT redefine, restate, or extend the canonical evidence field list, which lives once in the plan's "Evidence-verification contract for 160.017-T" table. NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

BOUNDED RAW LOG - SECOND AUTHORIZED EVIDENCE WRITE (added in review-fix cycle 5, findings 10 and 15; local field reference removed in cycle 8, item 7). Every observation this task records is backed by a RAW LOG this task also writes, into ITS OWN SUBDIRECTORY AND NOWHERE ELSE: docs/audits/2026-09-03-ship10-payload-evidence/observations/logs/T13/**. This path is authorized in the plan's H2 writable-surface table, so writing it does NOT violate freeze-scope, and 160.017-T can verify the evidence without following a reference outside the single audit directory. WHY THE LOG IS REQUIRED: cycle 4 had 160.017-T accept each producer's SELF-DECLARED outcome, which verifies nothing - a task that never ran would be indistinguishable from a clean pass. The log is the artifact that makes the claim checkable. The log also carries the source-provenance capture the plan's canonical contract requires before the test command runs, and the observation is taken on a CLEAN, FULLY COMMITTED TREE.
BOUNDS (binding, enforced by 160.017-T): PER LOG <= 256 KiB AND <= 2,000 lines - **A LOG EXCEEDING EITHER LIMIT IS REJECTED. ONE RULE, FAIL CLOSED (CYCLE 11, ITEM 6).** THE WITHDRAWN TRUNCATION PATH - truncate head-and-tail, write a truncation marker, and record `truncated: true` with pre-truncation byte and line counts - IS DELETED AND MUST NOT RETURN. It added three extension fields nothing else consumed, self-reported by the producer whose output the bound exists to constrain, and it CONTRADICTED 160.017-T, which now rejects an over-bound log outright - so a producer following the old rule would have truncated compliantly and still failed the shipment. WHAT THIS TASK DOES INSTEAD: keep the run bounded at source - scope the command, or split the observation across the per-case log allowance. IF AN OBSERVATION GENUINELY CANNOT FIT INSIDE THE BOUND, HALT AND REPORT; do not truncate and do not proceed. PER CASE at most 3 logs (first observation, second observation, one retry); PER SHIPMENT at most 200 files under the logs root, none of them binary; RETENTION until 160.017-T completes and the shipment closes.
OBSERVATION-RECORD CONTENT IS DEFINED ONCE, IN THE PLAN, AND IS NOT RESTATED HERE (cycle 6, finding 23; TIGHTENED IN CYCLE 8, ITEM 7). The SINGLE CANONICAL definition is the field table in docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section "Evidence-verification contract for 160.017-T". Seven task records each carried their own restatement and the copies had ALREADY DIVERGED, so a producer reading only its own record would have written a record 160.017-T rejects. THIS RECORD STATES ONLY WHAT IS TASK-SPECIFIC: which cases it produces records for, and which log subdirectory is its own. THIS RECORD ENUMERATES NO FIELD, NAMES NO FIELD, AND DEFINES NO FIELD TYPE OR VALIDATION RULE - NOT EVEN PARTIALLY. Cycle 7 deleted the "ADDITIONAL RECORD FIELDS" restatement but left a second local enumeration standing under a different heading, which was the same defect wearing a different label; cycle 8 deletes that one too. A PARTIAL COPY DRIFTS FROM THE CANONICAL TABLE EXACTLY AS A FULL COPY DOES. FOR EVERY FIELD, ITS TYPE, AND ITS ACCEPTANCE RULE, READ THE PLAN'S CANONICAL TABLE - there is no second source. 160.017-T verifies each record INDEPENDENTLY against its evidence and REJECTS any record that is missing a field, malformed, or inconsistent; it does not warn and does not skip.
This task WRITES its own JSON and its own logs and NOTHING ELSE outside its test files; it MUST NOT write into another producer's subdirectory, and MUST NOT write the ledger.

ACCEPTANCE: AC9, AC10, AC2d. Both cases authored with a per-case declared class and both observed and recorded RED with the failure mode stated above; every observation referenced to the 160.001-T baseline artifacts.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size S, complexity medium. Two cases; the scrubbed-PATH simulated environment is the only fixture requiring care.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T13)

FIRST-OBSERVATION DESTINATION (authorized, binding; added in extended review-fix cycle 4, finding 7). This task AUTHORS cases and records their FIRST observation, and that observation is a NON-DEFERRABLE acceptance condition consumed by 160.017-T's transition ledger. Review-fix cycle 3 authorized 160.002-T's spike record, 160.001-T's baseline, the pre-change byte captures (SIX in the current contract: three owned by 160.020-T, one each by 160.006-T, 160.007-T and 160.015-T) and 160.017-T's ledger, but NOT the first observations of the authoring tasks - so under freeze-scope this task had NO LEGAL DESTINATION FOR ITS OWN MANDATORY DELIVERABLE, the same class of self-contradiction cycle 2 fixed for schema_contracts.py and cycle 3 fixed for the baseline. A red recorded only in a transcript is uncitable, and 160.017-T cannot verify a transition pair whose first half does not durably exist.
DESTINATION: docs/audits/2026-09-03-ship10-payload-evidence/observations/T13.json - a subdirectory of the SINGLE bounded evidence surface already authorized for this shipment. NO NEW STORE, no new tool, no new convention.
RECORD CONTENT IS NOT SPECIFIED HERE (CYCLE 8, ITEM 7). The observation-record field set, every field's type, and every acceptance rule are defined ONCE, in the plan's canonical Evidence-verification contract (docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md, section "Evidence-verification contract for 160.017-T"). READ THAT CONTRACT; there is no second source and no local list. This record contributes ONLY its exact case set (the two cases enumerated above, BOTH RED-FIRST - 2 RED-FIRST / 0 CHARACTERIZATION), its destination path, and its log bounds. THE CLASS IS DECLARED PER CASE AND READ FROM THE TEST FILE, never emitted from a family-level constant, so that a case added later declares its own class instead of inheriting one (cycle 6, finding 16: the withdrawn boilerplate called this family MIXED, which contradicts the two-case RED-FIRST inventory above and had been copied unchanged from a genuinely mixed family). That is a task-specific fact about THIS family, not a redefinition of any field.
CONSUMPTION: 160.017-T joins these records to the canonical case table, verifies each independently against its evidence, and rejects any record that is missing, malformed or inconsistent - all exactly as the canonical contract states, and not restated here. A ledger row with no record, or a record with no ledger row, FAILS THE SHIPMENT; this is what makes verification V6 a machine check rather than a promise.
BOUND: records are APPEND-ONLY within the shipment and written ONLY by this task. No task writes another task's record, and 160.017-T READS them without modifying them. This is the ONLY write this task performs outside tests/**.
