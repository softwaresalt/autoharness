---
artifact_type: task
created_at: 2026-08-31T22:13:02.4059472Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 151.004-T
id: 151.001-T
parent_id: 151-F
priority: medium
status: queued
title: Define the Source artifact cleanup section in the operational-closure skill template
updated_at: 2026-09-03T09:10:07.4862397Z
---

CORRECTED in review-fix cycle 2 (P1): this task previously specified a "backlogit-gated Source artifact cleanup subsection", which CONTRADICTED the reviewed SHIP-1 plan. The plan's cycle-1 Placeholder specification is binding and the executable task now matches it exactly. DELIVERABLE: add an UNCONDITIONAL "Source artifact cleanup" subsection to the closure-artifact MUST-include list at Step 2 of templates/skills/operational-closure/SKILL.md.tmpl (currently lines 65-84 of 124). The subsection names custom_fields.source_stash_id and custom_fields.source_deliberation_id and requires the archived/skipped ID record, matching the producer contract at templates/agents/_ship.agent.md.tmpl:856-859. Mirror into .github/skills/operational-closure/SKILL.md. PLACEHOLDER SPECIFICATION (BINDING, and the substance of this correction): (a) the subsection is written as UNCONDITIONAL PROSE - there is NO backlogit gate, NO tool-scoped branch, and NO conditional wrapper of any kind around it; (b) the ONLY placeholder this subsection may introduce is the exact, valid customization point {{BACKLOG_DIRECTORY}} - written in full, in that exact spelling, with no shorthand, abbreviation, or variant form - which is the real variable documented at .github/skills/install-harness/SKILL.md:205 (source backlog_tool.directory; resolves to .backlogit for a backlogit composition and backlog for a backlog-md composition); (c) {{FEATURE_...}} IS FORBIDDEN AND MUST NOT APPEAR - it is not a real placeholder family, no such variable exists in any autoharness template or in the install-harness variable tables, so it would render as an unresolved {{...}} in every install; (d) no gate, conditional, or branch may be introduced on {{BACKLOG_DIRECTORY}} either - it ALWAYS resolves, because every composition including the manual one declares a backlog directory, which is precisely why it carries no unresolved-variable risk; (e) source_stash_id and source_deliberation_id are custom_fields NAMES in the closure artifact, not variables, and are written LITERALLY, never as placeholders; (f) the template already uses {{DOCS_CLOSURE}} - no new placeholder beyond {{BACKLOG_DIRECTORY}} may be introduced by this task. ACCEPTANCE: all three tokens (Source artifact cleanup, source_stash_id, source_deliberation_id) present in both files; the subsection is unconditional with no tool gate; {{BACKLOG_DIRECTORY}} is the only newly introduced placeholder and appears in exactly that spelling; zero occurrences of {{FEATURE_ anywhere in the changed files; closure_source_artifact_cleanup passes against the RENDERED template not only the installed copy; and zero unresolved {{ remains in the rendered output for BOTH the backlogit and the non-backlogit variable set (plan review finding 1). Template-first: never hand-patch only the installed copy. SEQUENCING: BLOCKED BY 151.004-T, the assertion sweep, which must be observed RED on this assertion first (SHIP-1 plan H6). SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size M, complexity medium. SAFETY MODE: careful (SHIP-1 plan H7). Source: 053E2BD2 defect 1. Plan: docs/plans/2026-08-31-ship1-v1_5_0-guardrail-contract-restoration-plan.md