---
artifact_type: task
created_at: 2026-08-10T07:10:16.3721578Z
custom_fields:
    complexity: medium
    origin_feature: 117-F
    size: M
    size_ruleset_version: 2h-width-isolated-v1
    size_source: agent
dependencies:
    - 118.003-T
    - 118.001-T
id: 120.001-T
labels:
    - shipment-3
    - P1
    - bootstrap
    - app-service
    - secrets
    - H5
parent_id: 120-F
priority: high
references:
    - start.ps1
    - start.sh
    - docs/plans/2026-08-09-copilot-cli-supervisor-control-plane-plan.md
status: queued
title: 'T12 supervise/bootstrap.py: workspace bootstrap application service'
updated_at: 2026-08-10T08:11:23.1295833Z
---

SIZE: M. COMPLEXITY: medium. (Two independent axes; also emitted as structured backlog fields.) Shipment 3 (P1). Create src/autoharness/supervise/bootstrap.py as the SOLE AUTHORITY for workspace bootstrap policy currently duplicated in start.ps1 and start.sh: .env.local loading (KEY=VALUE, single matching surrounding quote pair stripped, NO-CLOBBER precedence), workspace-local COPILOT_HOME (default <root>/.copilot) and ENGRAM_DATA_DIR (default <root>/.engram) each honoring a pre-set value, and GITHUB_TOKEN / GITHUB_PERSONAL_ACCESS_TOKEN resolution via gh auth token with gh-absent and gh-failing both NON-FATAL. THIS IS THE INTENTIONAL, APPROVED TARGET BEHAVIOR AND A DELIBERATE DELTA FROM THE LEGACY start.ps1 BASELINE (P1-B ruling, 2026-08-12): the legacy script assigns the PAT unguarded and ERRORS when gh is absent (pinned as baseline evidence by 118.001-T). Here, PAT setup MUST NOT be fatal when gh is absent or fails - it emits an EXPLICIT WARNING plus a structured event naming the missing tool and the variable left unset, leaves the variable unset rather than set-empty, and EXPOSES NO SECRET VALUE OR FRAGMENT in that warning, event, or any diagnostic. Returns a typed BootstrapResult. Resolved secret VALUES are registered with the redactor here (H5), so a token matching no regex is still redacted downstream. Secrets exist ONLY in the child process environment and never enter the journal, events, SupervisorResult.data, or telemetry. ACCEPTANCE: satisfies the start.ps1 and start.sh characterization assertions byte-for-byte in behavior FOR EVERY UNCHANGED SCENARIO, plus the approved-delta assertion for PAT-without-gh proving the non-fatal warning path (asserted at 120.007-T). This task ADDS the service only; it does not yet modify the scripts.