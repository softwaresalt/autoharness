---
artifact_type: task
created_at: 2026-08-21T21:19:26.2648718Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 143.001-T
labels:
    - bug
    - tests
    - diagnostics
    - git-subprocess
    - unconditional
parent_id: 143-F
priority: medium
references:
    - docs/plans/2026-08-21-full-suite-test-isolation-plan.md
    - docs/plans/2026-08-21-full-suite-test-isolation-hardening.md
status: queued
title: Make the two check=True git subprocess sites self-diagnosing (unconditional)
updated_at: 2026-08-21T21:19:26.2648718Z
---

UNCONDITIONAL — this task executes regardless of what 141.001-T found. It has exactly one terminal outcome (`done`) and never depends on the bisect verdict.

**Plan:** `docs/plans/2026-08-21-full-suite-test-isolation-plan.md` Task 3a
**Split from:** 141.005-T (archived; this task carries its unconditional half — old AC11)

## Why this is separated from the conditional half

The two `check=True` git subprocess sites are the reason two of the five victims surface as bare `CalledProcessError` ERRORS rather than readable failures. Making them self-diagnosing is correct whatever the bisect concluded, so it must not be trapped behind a conditional gate. Bundling it with the conditional remediation is what made the old 141.005-T un-closable.

## Steps
1. Capture stdout/stderr at both `check=True` git subprocess sites and include the captured stderr verbatim in the failure message, so the next exit-128 names its own cause instead of surfacing as a bare `CalledProcessError`. Sites:
   - `tests/test_gate_pipeline_topology_cli.py` — the `git init` path in `test_backlog_only_workspace_succeeds`
   - `tests/test_telemetry_gitignore_template.py` — `MetricsEmissionHardGateTests._git`
2. Do NOT modify any assertion in either module. The change is confined to subprocess invocation and failure-message construction.
3. Verify by deliberately forcing each site to fail once (locally, not committed) and confirming the captured stderr appears in the message.

## Acceptance criteria
- AC-a. Both `check=True` git sites capture stdout/stderr and surface the captured stderr in the failure message. (Carries forward the old 141.005-T AC11 verbatim in intent.)
- AC-b. `git diff` shows ZERO assertion-line edits in either module. Victim #1 (`PipelineTopologyStorageRootResolutionTests::test_backlog_only_workspace_succeeds`) and victim #5 (`MetricsEmissionHardGateTests::test_emitted_metrics_artifacts_are_never_tracked`) both live in these files — their assertions must be UNCHANGED (verify by diff).
- AC-c. Both modules pass IN ISOLATION before and after with IDENTICAL pass counts.
- AC-d. Hosted CI remains green.

## Anti-goal
Do not "fix" either victim by relaxing its assertion while touching these files. AC-b exists specifically to make that detectable.

## Scope guard (024-DL R5 / P-021 C1)
`tests/` surface only. No change under `src/autoharness/`.