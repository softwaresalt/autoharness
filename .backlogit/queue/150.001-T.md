---
artifact_type: task
created_at: 2026-08-30T05:11:46.6727627Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: autoharness-2h-rule-v1
    size_source: agent
id: 150.001-T
labels:
    - release
    - blocker
    - templates
    - markdownlint
parent_id: 150-F
priority: medium
references:
    - docs/plans/2026-08-29-v1_5_0-release-preparation-plan.md
status: queued
title: Harden circuit-breaker checkpoint format (add H1 + quote frontmatter values) in template and installed mirror
updated_at: 2026-08-30T05:11:46.6727627Z
---

RELEASE BLOCKER FIX. Consumes stash 8CB31EFB (high) + 1BDBD08B (medium) — folded per 1BDBD08B's own explicit grouping recommendation (same prescribed checkpoint format block, same file pair, one shared checksum refresh).

WHY BLOCKING: the template SHIPS in the wheel (pyproject force-includes templates -> src/autoharness/data/templates). The space-hash truncation mode is DEMONSTRATED in the wild; the colon-space mode can abort docline lint REPO-WIDE, directly threatening this release's own markdown quality gate.

SCOPE (paired edit — BOTH files, template-first):
1. templates/instructions/circuit-breaker.instructions.md.tmpl
2. .github/instructions/circuit-breaker.instructions.md

CHANGES to the prescribed checkpoint format block (installed copy ~L326-338):
a) Add an H1 line between the YAML frontmatter and "## Failure Chain", e.g. "# Circuit Breaker - {operation}". Fixes MD041 (first line must be a top-level heading) so a firing breaker cannot block the push at the exact moment an agent is already in a failure state.
b) Double-quote the four free-form frontmatter values (agent, skill, operation, identity). Unquoted prose containing colon-space raises "mapping values are not allowed here"; a space-hash sequence is silently TRUNCATED by yaml.safe_load.

3. Refresh the manifest checksum for the installed instruction in .autoharness/harness-manifest.yaml. Compute LF-normalized from the committed git blob (git cat-file -p HEAD:<path>) per the established .gitattributes eol=lf convention, and add an explanatory note in the existing style.

ACCEPTANCE:
- Both files changed identically (no template/dogfood divergence — PA-6).
- tests/test_docs_frontmatter_decodes.py still passes.
- markdownlint reports zero violations.
- Manifest checksum matches the new committed content.

CONSTRAINT: this task and the workspace-discovery checksum task BOTH edit .autoharness/harness-manifest.yaml — this one runs FIRST.