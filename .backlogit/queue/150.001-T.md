---
artifact_type: task
created_at: 2026-08-30T05:11:46.6727627Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: autoharness-2h-rule-v1
    size_source: agent
id: 150.001-T
labels:
    - release
    - blocker
    - templates
    - markdownlint
parent_id: 150-F
priority: medium
references:
    - docs/plans/2026-08-29-v1_5_0-release-preparation-plan.md
status: queued
title: Harden circuit-breaker checkpoint format (H1 + YAML-safe escaped scalars + regression cases)
updated_at: 2026-08-30T07:11:16.5726441Z
---

RELEASE BLOCKER FIX. Consumes stash 8CB31EFB (high) + 1BDBD08B (medium) — folded per 1BDBD08B's own explicit grouping recommendation (same prescribed checkpoint format block, same file pair, one shared checksum refresh).

WHY BLOCKING: the template SHIPS in the wheel (pyproject force-includes templates AND .github/instructions). The space-hash truncation mode is DEMONSTRATED in the wild; the colon-space mode can abort docline lint REPO-WIDE, directly threatening this release's own markdown quality gate.

SCOPE (paired edit — BOTH files, template-first):
1. templates/instructions/circuit-breaker.instructions.md.tmpl
2. .github/instructions/circuit-breaker.instructions.md

CHANGES to the prescribed checkpoint format block (installed copy ~L326-338):

a) Add an H1 line between the YAML frontmatter and "## Failure Chain", e.g. "# Circuit Breaker - {operation}". Fixes MD041 so a firing breaker cannot block the push at the exact moment an agent is already in a failure state.

b) *** EMIT THE FOUR FREE-FORM VALUES AS PROPERLY ESCAPED YAML SCALARS. NAIVE DOUBLE-QUOTING IS NOT SUFFICIENT. ***
   CORRECTED after PR #422 review cycle 3. An earlier version of this task said only "double-quote the four values". That fixes colon-space and space-hash but STILL BREAKS on an embedded double quote or backslash, leaving the SAME failure class (repo-wide docline lint abort) reachable.
   MEASURED (5 representative values, yaml.safe_load):
     embedded double quote  -> naive quoting: PARSE-FAIL | JSON-escaped: OK
     embedded backslash     -> naive quoting: PARSE-FAIL | JSON-escaped: OK
     trailing backslash     -> naive quoting: PARSE-FAIL | JSON-escaped: OK
     colon-space            -> naive quoting: OK         | JSON-escaped: OK
     space-hash             -> naive quoting: OK         | JSON-escaped: OK
   NOT HYPOTHETICAL IN THIS FILE: the template's own placeholder at L331 is
     skill: {skill name or "direct"}
   so naive wrapping produces  skill: ""direct""  which does not parse.
   REQUIRED: prescribe a JSON STRING LITERAL for each of agent, skill, operation, identity. YAML 1.2 double-quoted scalars share JSON's escape rules, so json.dumps-style output is always valid YAML. State this explicitly in the instruction text — do NOT merely show bare quotes in the example, because an agent copying the example will reproduce the unsafe form.

c) ADD REGRESSION CASES covering all four hazard classes — embedded double quote, embedded/trailing backslash, colon-space, space-hash. Each case must assert the emitted frontmatter BOTH (i) PARSES and (ii) ROUND-TRIPS to the original value. Round-trip matters independently: the space-hash mode PARSES successfully but SILENTLY TRUNCATES, which is the failure already observed in the wild (docs/archive/memory/circuit-break-copilot-review-cycle.md). A parse-only assertion would not catch it.

3. Refresh the manifest checksum for the installed instruction in .autoharness/harness-manifest.yaml. Compute LF-normalized from the committed git blob (git cat-file -p HEAD:<path>) per the established .gitattributes eol=lf convention, and add an explanatory note in the existing style.

ACCEPTANCE:
- Both files changed identically (no template/dogfood divergence — PA-6).
- All four hazard classes parse AND round-trip.
- tests/test_docs_frontmatter_decodes.py still passes.
- markdownlint reports zero violations.
- Manifest checksum matches the new committed content.

CONSTRAINT: this task and 150.002-T BOTH edit .autoharness/harness-manifest.yaml — this one runs FIRST.