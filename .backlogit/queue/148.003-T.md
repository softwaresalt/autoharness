---
artifact_type: task
created_at: 2026-08-28T03:50:06.4604665Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 148.003-T
labels:
    - S0
    - render-mapping
    - review-personas
    - corrected-2026-08-28
parent_id: 148-F
priority: medium
references:
    - docs/plans/2026-08-27-policy-registry-and-review-persona-layer-plan.md
    - templates/agents/review/
    - .github/skills/plan-review/SKILL.md
status: queued
title: U3 — Resolve and pin the technology-reviewer -> python-reviewer render mapping
updated_at: 2026-08-28T07:57:06.1903363Z
---

Pin the render mapping that produces .github/agents/subagents/python-reviewer.agent.md from the EXISTING templates/agents/review/technology-reviewer.agent.md.tmpl. No new template is authored. Domain: render mapping + recorded decision. Files: 0 new templates.

<!-- BEGIN:acceptance-criteria -->
* `PRIMARY_LANGUAGE_LOWER` = `python` confirmed from `.autoharness/harness-manifest.yaml` L394 and cross-checked against `workspace-profile.yaml`.
* Every placeholder in `templates/agents/review/technology-reviewer.agent.md.tmpl` is enumerated with its pinned binding value recorded.
* The mapping `technology-reviewer.agent.md.tmpl` -> `python-reviewer.agent.md` is recorded, citing installed `install-harness/SKILL.md` L1203 as the authority.
* **NO new template file is created.** `templates/agents/review/python-reviewer.agent.md.tmpl` MUST NOT exist at the end of this unit.
* Corrected decision D4 recorded for the closure record.

**ADDED 2026-08-28 (review-fix cycle 2, P2-2) — the five synthesized bindings:**

* **All five** unbound synthesized persona variables are pinned to an exact value with a named in-repo reviewed derivation, recorded as decision **D8** in the plan, **before any render occurs**:
  1. `{{CONCURRENCY_PATTERNS}}` (`concurrency-reviewer.agent.md.tmpl` L31)
  2. `{{LANGUAGE_SAFETY_CHECKS}}` (`technology-reviewer.agent.md.tmpl` L21)
  3. `{{LANGUAGE_IDIOM_CHECKS}}` (L25)
  4. `{{LANGUAGE_ERROR_HANDLING_CHECKS}}` (L29)
  5. `{{LANGUAGE_PERFORMANCE_CHECKS}}` (L33)
* `{{CONCURRENCY_PATTERNS}}` is pinned to the literal `asyncio, task, queue, thread, process`, **copied from** `src/autoharness/verify_workspace.py` `_language_defaults("python")["concurrency_patterns"]` (L2200, wired at L2885). If the value read at execution time differs from this pin, that is a **HARD STOP**, not a licence to improvise.
* Bindings 2-5 are pinned to the verbatim D8-B bullet lists in the plan. **No synthesis, paraphrase, extension, or reordering by Ship.**
* **Template files are NOT edited.** `technology-reviewer.agent.md.tmpl` and `concurrency-reviewer.agent.md.tmpl` retain their placeholders unmodified; values bind at render time only. Hard-coding Python content into a `.tmpl` FAILS this unit (D8-C, technology-agnostic template discipline).
* **Scope of the pin is declared complete and closed (CORRECTED 2026-08-28, review-fix cycle 3 — D8-D):**
  * **Already manifest/config-bound, no pin needed**: `{{PRIMARY_LANGUAGE}}`, `{{PRIMARY_LANGUAGE_LOWER}}`, `{{TIER_1_FAMILY}}`, `{{TIER_1_PROVIDER}}`, `{{TIER_1_REASONING_EFFORT}}`.
  * **Output-schema exemplars — EXACTLY THREE tokens, handled by ONE named rule.** The prior revision of this bullet enumerated only two and **omitted `{{principle_number}}`**, which occurs at `templates/agents/review/constitution-reviewer.agent.md.tmpl` **L43** inside the same fenced JSON output-schema block as the other two. The complete, closed set is:

    | Exemplar token | Occurs in | Role in the JSON example |
    |---|---|---|
    | `{{file_path}}` | 11 of the 13 persona templates | the file a *finding* refers to |
    | `{{line_number}}` | 10 of the 13 persona templates | the line a *finding* refers to |
    | `{{principle_number}}` | `constitution-reviewer.agent.md.tmpl` **L43** | the constitutional principle a *finding* cites |

  * All three are **exemplar tokens intentionally retained** as literal installed-persona content describing the JSON shape a reviewer subagent emits **at review time**. They are **NOT unresolved install variables**, they have **no install-time value**, and **NONE may be bound to fabricated content** — binding `{{principle_number}}` (or either other token) to any invented value corrupts the persona output schema and is a **HARD STOP**.
  * They are handled by **one explicit named rule**, **`EXEMPT_OUTPUT_SCHEMA_EXEMPLARS`**: during the zero-unresolved-`{{...}}` scan over installed persona artifacts, a matched token is exempt **iff** it is (a) a member of the closed allow-list above **and** (b) inside a fenced output-schema code block. Both conditions required; neither alone sufficient. The allow-list is **CLOSED** — any token not on it is a genuine unbound variable and MUST fail.
  * This rule is **distinct** from U8 scenario 2's route exemption and from U1/D9-B's `EXEMPT_POLICY_PROSE_META_TOKEN` registry rule. Do not conflate or widen any of the three.
* This unit **blocks U6** (`148.006-T`) as well as U5 — the `{{CONCURRENCY_PATTERNS}}` pin must exist before `concurrency-reviewer` is rendered.
* Authoritative test command unchanged: `PYTHONPATH=src python -m unittest discover -s tests` (Q5).
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan unit **U3** — **CORRECTED 2026-08-28**, **SCOPE EXTENDED 2026-08-28 (review-fix cycle 2)**.

The prior revision of this task claimed `python-reviewer` had no template anywhere in `templates/` and scheduled authoring a new one. **That premise was false.**

`templates/agents/review/technology-reviewer.agent.md.tmpl` **already exists**, and installed `.github/skills/install-harness/SKILL.md` L1203 declares the canonical mapping:

```text
technology-reviewer.agent.md -> .github/agents/subagents/{{PRIMARY_LANGUAGE_LOWER}}-reviewer.agent.md
```

`.autoharness/harness-manifest.yaml` L394 binds `PRIMARY_LANGUAGE_LOWER: "python"` (cross-checked against `workspace-profile.yaml` `languages.primary: "python"`), so in this workspace that mapping renders exactly `python-reviewer.agent.md`.

**Work in this unit** (no files authored):

1. Confirm `PRIMARY_LANGUAGE_LOWER` resolves to `python` from the manifest.
2. Enumerate every placeholder in `technology-reviewer.agent.md.tmpl` and pin each binding, so U5's render is mechanical and reviewable.
3. **EXTENDED**: also pin `{{CONCURRENCY_PATTERNS}}` for `concurrency-reviewer.agent.md.tmpl`, which U6 renders. **U3 is now the single binding-pin unit for every synthesized persona variable in S0**, regardless of which unit performs the render.
4. Record the corrected decision **D4** and the new decision **D8** in the shipment closure record.

**Why the extension was necessary (P2-2).** Installed `install-harness/SKILL.md` L329-L333 classes these five placeholders as *"Synthesized from language ... model"* — i.e. it expects the installer to **invent** their content. A "mechanical render" instruction over a synthesized placeholder is self-contradictory: Ship would either emit unresolved `{{...}}` (failing the U8 placeholder scan and the DoD zero-placeholder rule) or improvise **unreviewed persona content** into a normative review surface. Neither is acceptable, so all five are pinned here with reviewed derivations first.

This unit exists to make U5's and U6's renders deterministic and to leave the corrected mapping and binding decisions on the record; it is deliberately **not** a template-authoring unit and **not** a template-editing unit.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
**Decision D4 — CORRECTED.** `python-reviewer` is a RENDER TARGET of the existing generic `technology-reviewer` template, not a missing fixed template.

The superseded D4 read: *"authored as a fixed `python-reviewer`, NOT a `{{PRIMARY_LANGUAGE}}`-parameterized template."* That was reasoning from the false premise. Authoring a fixed Python-specific duplicate of an existing generic template would **fork the canonical surface** and create precisely the drift condition stash `336F3AB7` reports and this shipment exists to close.

**Why the earlier audit missed it**: a filename search for `python-reviewer*` across `templates/` cannot find a template whose render target is renamed by a mapping. The template basename (`technology-reviewer`) and the installed filename (`python-reviewer`) intentionally differ. Recorded as RK-H in the plan.

**Downstream consequence for U7**: the manifest entry for `python-reviewer.agent.md` must record `template: "agents/review/technology-reviewer.agent.md.tmpl"`. A provenance check assuming basename equality would otherwise misread this as drift (RK-I).

---

**Decision D8 — pinned bindings for the 5 synthesized persona variables (ADDED 2026-08-28, review-fix cycle 2).**

Full table and the verbatim D8-B bullet-list content live in the plan under U3. Summary of derivations:

| Variable | Pinned value | Reviewed derivation |
|---|---|---|
| `{{CONCURRENCY_PATTERNS}}` | `asyncio, task, queue, thread, process` | **Code-backed.** `verify_workspace.py` `_language_defaults("python")["concurrency_patterns"]` L2200, wired L2885. Copied verbatim from the shipped resolver — not synthesized. |
| `{{LANGUAGE_SAFETY_CHECKS}}` | D8-B bullet list | `_language_defaults("python")` `unsafe_policy` + `lint_policy`, plus `constitution.instructions.md` I *Safety-First Python*. |
| `{{LANGUAGE_IDIOM_CHECKS}}` | D8-B bullet list | `_language_defaults("python")` `naming_conventions` + `documentation_conventions`, plus constitution I (prefer stdlib / existing deps). |
| `{{LANGUAGE_ERROR_HANDLING_CHECKS}}` | D8-B bullet list | `_language_defaults("python")` `error_handling_policy` + `error_handling_conventions` + `error_pattern` (`raise/except`), plus constitution I (*silent failures are forbidden*). |
| `{{LANGUAGE_PERFORMANCE_CHECKS}}` | D8-B bullet list | **Weakest — RK-J.** `_language_defaults` has **no** performance key, so no language-model source exists. Derived from `constitution.instructions.md` X *Agent Context Efficiency* + I. |

**D8-C — technology-agnostic template discipline preserved.** Values bind at render time into *installed artifacts*; both `.tmpl` files keep their placeholders. Hard-coding Python content into a template is forbidden and is the exact defect already documented at `verify_workspace.py` L2172-L2180.

**RK-J residual**: bindings 2-5 are Stage-reviewed prose, not code-derived, and are authoritative **for this shipment only**. Follow-up (out of S0 scope, would change the resolver): add `safety/idiom/error/performance_checks` keys to `_language_defaults` so future renders derive them deterministically.

**Verification**: U8 scenario 5 (added) asserts verbatim conformance in the installed artifacts, cross-checks the concurrency pin against the live resolver, and asserts both templates remain unmodified.

**Out of scope**: generalizing persona routing so every workspace resolves `<lang>-reviewer`. Installed skills cite the literal `python-reviewer.agent.md`; changing that has blast radius across all stack packs and is a separate shipment.

**2-hour rule (re-assessed 2026-08-28).** Scope grew from *enumerate + pin one template's bindings* to *pin five synthesized values across two templates, with derivation evidence*. This remains analysis-and-record work: **no file is authored or edited by this unit**. All five derivations were located during staging and are recorded in the plan, so execution is confirmation, not discovery. Size raised **XS -> S**; complexity held at **low** (the sources are named and exact; no design judgement remains for Ship). Estimated well inside 2 hours. **No split required.**

**Blocks U5** (`148.005-T`) — mapping + D8-B bindings pinned before the render.
**Blocks U6** (`148.006-T`) — ADDED 2026-08-28: the `{{CONCURRENCY_PATTERNS}}` pin must exist before `concurrency-reviewer` is rendered.
<!-- END:implementation-notes -->