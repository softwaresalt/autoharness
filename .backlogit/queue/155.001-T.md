---
artifact_type: task
created_at: 2026-08-31T22:16:34.6324833Z
custom_fields:
    complexity: medium
    queue_position: 36
    size: M
    size_ruleset_version: autoharness-stage-2h-v1
    size_source: agent
id: 155.001-T
parent_id: 155-F
priority: medium
status: queued
title: Add a fail-closed markdownlint CI job and make the pre-push hook fail when the linter is missing
updated_at: 2026-08-31T22:33:30.7351477Z
---

.githooks/pre-push.sh L29 guards the lint behind command -v markdownlint and L36-37 warn-and-skip, so the gate is absent on every machine without markdownlint-cli; there is no markdownlint CI job at all, so the hook is the only carrier and it fails open. PLAN REVIEW FINDING 1 (P0 - HARD PRECONDITION, ordered): (a) run the linter over the current tree and RECORD the violation count - measure, do not assume; (b) if non-zero, the CI job lands scoped to CHANGED FILES on PRs, and a repository-wide run is added only once the count reaches zero. Turning it fail-closed repository-wide before the tree lints clean would make main permanently red and block this runs own portfolio. BINDING H1: the hooks fail-on-missing message must name the exact install command (npm install -g markdownlint-cli) and the documented escape hatch (git push --no-verify) - a gate, not a wall. BINDING H2: the CI job lands before or with the hook change. BINDING H3: CI must lint the SAME glob with the SAME config as the hook. Pin the markdownlint-cli version; no unpinned global installs (finding 7). NON-GOAL: no new markdownlint rules and no change to .markdownlint.json. Source: D1A46B8C.