---
artifact_type: task
created_at: 2026-08-05T23:51:12.2325548Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 1.0.0
    size_source: agent
dependencies:
    - 111.001-T
    - 111.004-T
    - 111.002-T
    - 111.005-T
    - 111.006-T
    - 111.007-T
id: 111.003-T
parent_id: 111-F
priority: medium
status: queued
title: 'C4: verify/assertion-anchor + docs for crash-resumption (candidates a/c noted deferred)'
updated_at: 2026-08-06T08:18:11.3159269Z
---



<!-- BEGIN:acceptance-criteria -->
1. verify_workspace / assertion-anchors cover: (a) presence of the crash-resumption protocol section in the orchestrator agent template (C1), INCLUDING the MANDATORY operator-confirmation-before-restore gate (no auto-resume, no dead-session auto-recovery) AND the ROLE-OWNERSHIP RECOVERY CONTRACT (third re-review 2026-08-05, 111.001-R P1-6; zero-candidate correction publication-review 2026-08-05, 111.001-R P1-7): the ZERO-CANDIDATE NO-OP (no active recovery candidate => normal orchestration CONTINUES, NOT a failure and NOT an operator handoff), explicit UNIQUE checkpoint selection (no auto-pick) engaged ONLY when one or more candidates exist, CheckpointV1 `agent`-ownership validation (`stage`/`ship`), Orchestrator routes restore/resume/prune EXCLUSIVELY to the owning agent and NEVER restores/resumes/prunes Stage/Ship-owned state directly, and fail-closed handling (AMONG EXISTING CANDIDATES only) on missing/invalid/ambiguous ownership or a non-unique selection; AND (b) presence of the prune-on-restore / checkpoint-recovery section HOSTED in the installed backlogit-pack overlay instruction (`.github/instructions/backlogit.instructions.md`, authored by C2 / installed by 111.005-T). 2. OVERLAY VERIFICATION uses the STANDARD FIRST-PARTY artifact contract (re-review 2026-08-05, 111.001-R P1-3 CORRECTED): a TARGETED SECTION-PRESENCE assertion that the checkpoint-recovery section text exists in the installed overlay, PLUS the EXISTING first-party manifest artifact `checksum` coherence check (installed file hash == the SINGLE `artifacts[].checksum` recorded for `.github/instructions/backlogit.instructions.md`). Do NOT reference community-template `installed_checksum`/`source_checksum`, and do NOT assert installed-vs-source-template drift comparison — that dual-checksum surface is NOT part of the first-party overlay contract and is not supported. 3. Docs describe the protocol, the ZERO-CANDIDATE NO-OP (no active recovery candidate => normal orchestration continues, not a failure/handoff), the explicit-selection + `agent`-ownership validation + owner-exclusive routing of restore/resume/prune that engages ONLY when one or more candidates exist (Orchestrator never restores/resumes/prunes Stage/Ship-owned state directly; fail closed AMONG EXISTING CANDIDATES on missing/invalid/ambiguous ownership or non-unique selection), the operator-confirmation-before-restore gate, single-active preservation, and degraded fallbacks (substrate-unreachable => fail-closed operator handoff, no prune/resume). 4. Docs explicitly note candidates (a) unified action-observation execution abstraction and (c) background Verification & Compaction layer remain DEFERRED in living tracker 34D50F2D. 5. No external binary mutation referenced. 6. CHECKPOINT-RESOLUTION ORDER (hardening H3) — verify/assertion-anchors ALSO assert that the orchestrator agent-template crash-resumption section states resolve_checkpoint (backlogit) occurs ONLY AFTER a CONFIRMED SUCCESSFUL owner resume performed by the OWNING agent (stage-owned => Stage subagent; ship-owned => Ship subagent): never before resume, never on ambiguous/torn state, and never executed directly by the Orchestrator for Stage/Ship-owned checkpoints. The named host is the orchestrator agent-template crash-resumption protocol section (C1/111.001-T). 7. DEGRADED-MODE FALLBACK (hardening H5, task C3/111.002-T) with NAMED host sections/artifacts — verify/assertion-anchors ALSO assert a SINGLE fail-closed behavior: (a) in the orchestrator agent-template CRASH-RESUMPTION PROTOCOL section (C1/111.001-T) that backlogit unreachable at restore => NO auto-resume => operator handoff; AND (b) in the backlogit-pack overlay instruction `.github/instructions/backlogit.instructions.md` CHECKPOINT-RECOVERY / PRUNE-ON-RESTORE section (C2/111.004-T, installed by 111.005-T) that engram unreachable => FAIL CLOSED to OPERATOR HANDOFF (NO prune, NO resume) — the bounded file-based prune degradation path is REMOVED and MUST NOT appear (publication-review 2026-08-06 engram-unavailable contradiction; aligned with 111-F H5). Both mirror the Tool-Availability-Gate + ENGRAM_DEGRADED patterns and fail-safe to operator handoff. No new standalone template/file is asserted (width isolation: the two existing named hosts only). 8. OWNER-AGENT PROTOCOL COVERAGE (publication-review P1 2026-08-06, owner-agent recovery conflict; task C5/111.006-T) — verify/assertion-anchors ALSO assert, in BOTH owner agent templates (`templates/agents/_stage.agent.md.tmpl` AND `templates/agents/_ship.agent.md.tmpl`), that each owner agent's own startup/recovery protocol states: the ZERO-CANDIDATE NORMAL STARTUP (no own active recovery candidate => continue normal work, not a failure/handoff); EXPLICIT OPERATOR FILENAME SELECTION when candidates exist (no auto-pick); CheckpointV1 `agent`-ownership validation against the agent's own role (Stage restores/resumes/prunes/RESOLVES only `stage`-owned; Ship only `ship`-owned — CROSS-ROLE RESOLUTION is prohibited in addition to cross-role restore/resume/prune); OWNER-SCOPED RESOLUTION AFTER EXPLICIT OPERATOR CONFIRMATION/FILENAME SELECTION — each owner agent RESOLVES ONLY the SINGLE explicitly operator-selected, ownership-matched checkpoint (matching owner), and ONLY AFTER a CONFIRMED SUCCESSFUL owner resume (NOT before), with NO BULK/broad resolution sweep of all/other active checkpoints and NO cross-role resolution; and FAIL CLOSED with NO FRESH-START FALLBACK on an invalid/ambiguous/torn/malformed checkpoint read (the agent MUST NOT silently discard and start fresh). Named hosts = the crash-resumption/startup sections of the two owner agent templates. 9. INSTALLED DOGFOOD MIRROR COVERAGE (Copilot PR #304 findings 4 + 5; task C6/111.007-T) — verify/assertion-anchors ALSO assert: (a) each of the THREE installed dogfood agent mirrors — `.github/agents/_orchestrator.agent.md`, `.github/agents/_stage.agent.md`, and `.github/agents/_ship.agent.md` — CONTAINS the new crash-resumption / owner-agent fail-closed recovery prose (installed-mirror SECTION-PRESENCE, so the ACTIVE dogfood Orchestrator/Stage/Ship agents provably receive the behavior, not just the templates); AND (b) the `.autoharness/harness-manifest.yaml` `artifacts[].checksum` for each of those three mirrors is COHERENT with the installed file (single-`checksum` first-party contract, LF-normalized committed-blob hash — the same coherence check used for other first-party manifest artifacts; NO installed_checksum/source_checksum, NO source-drift comparison). Named hosts = the installed `.github/agents/_orchestrator.agent.md`, `_stage.agent.md`, `_ship.agent.md` mirrors + their manifest entries; the install/refresh itself is owned by C6/111.007-T.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Add verify/assertion-anchor coverage and docs so the harness validates the new crash-resumption prose.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on C1, C2, D (111.005-T install/tune), C5 (111.006-T owner-agent templates), C6 (111.007-T installed dogfood agent mirrors + manifest checksums). Source: 002-SP + review 111.001-R (incl. re-review 2026-08-05) + Copilot PR #304 findings 4/5 (installed mirror + manifest-checksum + verification). Verification uses the standard first-party single `artifacts[].checksum` + section-presence — no installed_checksum/source_checksum, no source-drift comparison. PUBLICATION-REVIEW 2026-08-05 (111.001-R P1-7): verify + docs also cover the ZERO-CANDIDATE NO-OP (no active recovery candidate => normal orchestration continues, not a failure/handoff); the selection/ownership/routing/fail-closed gate is asserted to engage ONLY when one or more valid recovery candidates exist. PUBLICATION-REVIEW 2026-08-06 (engram-unavailable contradiction): AC#7(b) asserts a SINGLE fail-closed operator-handoff behavior for engram-unreachable (no file-based prune). PR #304 findings 4/5: AC#9 added for installed-mirror section-presence + three-mirror manifest-checksum coherence (dep 111.003->111.007).
<!-- END:implementation-notes -->