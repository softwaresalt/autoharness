---
artifact_type: task
created_at: 2026-07-13T22:29:03.8354201-07:00
id: 079.001-T
parent_id: 079-F
priority: medium
status: queued
title: Extending ExecutionEpoch token economics and correlation fields
updated_at: 2026-07-13T22:29:03.8354201-07:00
---

Implement the ratified `ExecutionEpoch` v1.1 additive token-economics and epoch-correlation fields from docs/decisions/2026-07-13-telemetry-metrics-reporting-ownership.md. Keep this scoped to the epoch model and model tests: token consumption vs generation, cumulative context-area totals, per-metric provenance maps, and persisted correlation fields needed for task/session/phase reporting.

<!-- BEGIN:acceptance-criteria -->
- Tests are written first for v1.1 serialization, generation-vs-consumption separation, per-metric unavailable provenance defaults, and final cumulative-token preservation.
- The module-level `SCHEMA_VERSION` constant in `src/autoharness/telemetry/epoch.py` (the dataclass default for `schema_version`) is bumped to `1.1.0`; `ExecutionEpoch` has no class-level `SCHEMA_VERSION` attribute. Legacy v1.0 payload reads normalize to v1.1 with additive fields marked `unavailable`, never a hybrid `schema_version: 1.0.0` record containing v1.1 fields.
- Add root correlation fields exactly as assigned by the decision matrix: `workspace_id`, `session_id`, `agent_role`, `phase`, `backlog_item_id`, `shipment_id`, `branch`, and `commit_sha` so every promised reporting slice is backed by a persisted field.
- `EconomicPayload` adds token/cost fields exactly as assigned by the decision matrix: `cached_input_tokens`, `cumulative_input_tokens`, `cumulative_output_tokens`, `context_tokens_before`, `context_tokens_after`, `context_area_tokens`, `avoided_read_estimated_tokens`, `tool_output_estimated_tokens`, `metric_sources`, and `metric_quality`; v1.0 records default additive metric provenance to `unavailable`.
- Cumulative fields are final epoch-close totals or max/final observed cumulative values; tests assert the host-supplied final cumulative total is preserved on round-trip and is never re-derived or summed. Cross-event double-count assertions belong to 084-F event→epoch composition.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Primary files should be `src/autoharness/telemetry/epoch.py` and `tests/test_telemetry_epoch.py`. Do not add a `ToolTelemetryEvent` runtime model or event sink in this task. Keep CozoDB/agent-engram ingestion out of telemetry; agent-engram remains a downstream consumer only.
<!-- END:implementation-notes -->