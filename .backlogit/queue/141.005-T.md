---
artifact_type: task
created_at: 2026-08-21T20:24:13.2648536Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 141.001-T
    - 141.004-T
id: 141.005-T
labels:
    - bug
    - tests
    - isolation
    - remediation
    - blocked-on-diagnosis
parent_id: 141-F
priority: medium
references:
    - docs/plans/2026-08-21-full-suite-test-isolation-plan.md
    - docs/plans/2026-08-21-full-suite-test-isolation-hardening.md
status: queued
title: Fix the confirmed polluter and make the git subprocess sites self-diagnosing
updated_at: 2026-08-21T20:24:13.2648536Z
---

Remediate the CONFIRMED polluter identified by 141.001-T, and make the two `check=True` git subprocess sites self-diagnosing.

**Plan:** `docs/plans/2026-08-21-full-suite-test-isolation-plan.md` Task 3 (+ amendments A1, A2)

## BLOCKING PRECONDITION (amendment A2)
This task is BLOCKED until 141.001-T delivers AC1 (a minimal reproducing pair). If 141.001-T hard-stopped, this task is returned BLOCKED via the official return-blocked operation carrying the narrowed candidate set, and the feature closes with 141.001-T–141.004-T only. **There is no speculative-fix path.** 141.001-T–141.004-T are independently valuable and independently mergeable; this task is not.

## Steps
1. Remediate the confirmed polluter AT ITS OWN SOURCE — fixture/teardown scoping in the POLLUTING test. Do NOT modify any victim's assertions.
2. Make the two `check=True` git subprocess sites self-diagnosing: capture stdout/stderr and include the captured stderr in the failure message, so the next exit-128 names its own cause instead of surfacing as a bare `CalledProcessError`. Sites:
   - `tests/test_gate_pipeline_topology_cli.py` (the `git init` path in `test_backlog_only_workspace_succeeds`)
   - `tests/test_telemetry_gitignore_template.py` `MetricsEmissionHardGateTests._git`

## Acceptance criteria
- AC8. The canonical full-suite gate is GREEN on Windows: ZERO failures, ZERO errors (skips unchanged). `$env:PYTHONPATH='src'; python -m unittest discover -s tests`.
- AC9. The minimal reproducing command from 141.001-T AC1 now PASSES.
- AC10. All five victim tests retain their original assertions VERBATIM — demonstrated by `git diff`: either no change at all, or changes confined to `setUp`/`tearDown`/imports with ZERO assertion-line edits.
- AC10b (amendment A1). The five victims are additionally re-run IN ISOLATION with identical pass/fail semantics, and the verbatim diff of the five victim files is attached to this task record.
- AC11. Both `check=True` git sites surface captured stderr on failure.
- AC12. Hosted CI remains green (no Linux regression from the anchor changes in 141.002-T–141.004-T).

## Anti-goal (the single most dangerous outcome available here)
The cheapest path to a green suite is to relax `test_root_tracked_json_matches_allowlist` (itself a 133-F/142-S regression guard), broaden the telemetry gitignore assertions, or delete a victim. Every one of those turns the gate green while destroying the protection the gate exists to provide. AC10/AC10b exist specifically to make that path detectable.