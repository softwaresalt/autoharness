---
artifact_type: task
created_at: 2026-09-12T22:48:41.4051957Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 165.001-T
id: 165.002-T
labels:
    - engine
    - topology-gate
    - contract-change
parent_id: 165-F
priority: medium
status: queued
title: 'Engine: four-state DAG-authoritative derivation, provenance, and atomic legacy-test re-expression'
updated_at: 2026-09-13T00:18:52.9580852Z
---

Turn 165.001-T's expected-failure tests into ordinary passing assertions by implementing the four-state derivation contract in src/autoharness/gates/topology.py.

IMPLEMENTATION:
1. `_shipment_readiness_check` (L1574-1578) derives `predecessor_ids` EXCLUSIVELY from `ShipmentState.blocking_predecessor_ids`. The `_prior_shipment_id` union is REMOVED from the claim path.
2. Parse the root declaration: add a root-declaration field to `ShipmentState` (L91-98) populated from the shipment record's `labels` frontmatter in the reader (L560-600). The label is `dag-root`. VALIDATED: `backlogit update <id> --labels ...` persists `labels:` on a shipment record, so no backlogit change is needed.
   FAIL CLOSED ON MALFORMED LABELS (plan-review cycle 2, P2-2): parse `labels` with the same `_tuple_of_str` fail-closed discipline the reader already applies to `custom_fields.items` and `dependencies` (L580-590). A present-but-wrong-shaped `labels` block (e.g. a bare string) MUST raise BacklogUnavailableError, NOT silently degrade to "no declaration". Silent degradation would turn a malformed record into an unexplained block, and — worse, if the polarity were ever inverted — into an unearned pass. The reader's existing comment at L565-572 states exactly this rule for `custom_fields`; follow it.
3. Implement the four-state resolution (decision D1). Non-empty edges -> evaluate as today, provenance `explicit`. Empty edges -> DECLARED ROOT (label present) passes as `declared_root`; GENESIS (no declaration AND no shipped-terminal shipment exists anywhere in the workspace) passes as `genesis`; otherwise UNSEQUENCED blocks with token UNSEQUENCED_SHIPMENT and provenance `unsequenced`. States must be TOTAL and MUTUALLY EXCLUSIVE.
   THE GENESIS PROBE MUST INCLUDE ARCHIVED SHIPMENT RECORDS (plan-review cycle 2, P2-1): use `archived_status` / `archived_record_present` alongside live status. A workspace whose shipped history has been archived is NOT genesis. Reading only live records would let archiving silently re-enter genesis and convert a block into a pass.
4. Attach `predecessor_source` to EVERY `shipment_readiness` details payload — blocked AND passed (hardening F1). Recommended type is an enumerable Literal over the four values rather than a bare `str`.
5. The UNSEQUENCED_SHIPMENT message MUST name BOTH remedies (record the real `blocks` edge, or declare the shipment a root). A block with no stated exit is a deadlock, which is precisely the review-fix-cycle-1 defect this design corrects.
6. NO numeric comparison may remain on the claim path in ANY state (compound learning: three prior defects all came from getting the DIRECTION of a numeric predicate wrong).
7. Do NOT delete `_prior_shipment_id` — 165.003-T re-homes it as the audit report's input. Deleting it here would create a dangling reference.

ATOMIC LEGACY-TEST RE-EXPRESSION (SAME TASK, SAME COMMIT — hardening F5). The five `ImplicitNumericPredecessorTests` cases (tests/test_gates_topology.py, approx L1648-1750) PIN the claim-path numeric behaviour this task removes. They CANNOT be handled in a later task: doing so would leave the suite RED between tasks. Handle them here, with a recorded PER-CASE disposition for all five:
- test_lower_numbered_shipment_declaring_target_as_its_own_dependency_is_not_treated_as_predecessor
- test_implicit_numeric_predecessor_still_blocks_when_no_declared_reverse_dependency
- test_implicit_numeric_predecessor_still_blocks_when_lower_shipment_has_unrelated_deps
- test_multi_hop_reverse_dependency_disables_fallback_entirely_not_just_the_violator
- test_higher_numbered_forward_dependent_does_not_suppress_targets_own_predecessor_check
DISPOSITION RULE (hardening F4, bulk deletion FORBIDDEN): a case whose safety intent survives becomes an explicit-DAG or four-state assertion; a case that only ever described the heuristic's INTERNAL reverse-dependency suppression becomes a direct unit test of `_prior_shipment_id` AS A PURE FUNCTION, preserving the encoded knowledge for the audit path (165.003-T) without asserting any claim behaviour. Any case with genuinely no equivalent requires WRITTEN RATIONALE in the task record. Update the class docstring to record the retired heuristic as historical context.

HARDENING F6 (MANDATORY): the forward-dependent suppression defect recorded in docs/compound/2026-08-18-topology-gate-forward-dependent-suppression-residual-defect.md is STILL UNFIXED ON MAIN. PROVE BY TEST — assertion is insufficient — that it can no longer affect a CLAIM once the claim path no longer consults the heuristic. If any claim-path residue remains, record a follow-up stash entry rather than silently fixing it.

`pre_claim` remains the SOLE claim authority. `--force` semantics unchanged. No config key, no schema change (decision D2).

SIZE: M (~2h). COMPLEXITY: high (sole-claim-authority contract change plus atomic test re-expression).
Plan §T2. Depends on 165.001-T. Blocks 165.003-T, 165.004-T, 165.006-T.