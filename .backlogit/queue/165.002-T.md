---
artifact_type: task
created_at: 2026-09-12T22:48:41.4051957Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 165.001-T
id: 165.002-T
labels:
    - engine
    - topology-gate
    - contract-change
parent_id: 165-F
priority: medium
status: queued
title: 'Engine: DAG-authoritative predecessor derivation, provenance, and migration switch'
updated_at: 2026-09-12T22:48:41.4051957Z
---

Turn T1's RED tests GREEN. In `src/autoharness/gates/topology.py`:

1. `_shipment_readiness_check` (L1574-1578) derives `predecessor_ids` EXCLUSIVELY from `ShipmentState.blocking_predecessor_ids`. Remove the `_prior_shipment_id` union from the claim-blocking path.
2. Add `predecessor_source` to EVERY `shipment_readiness` details payload — blocked AND passed (hardening F1). Recommended type `Literal["explicit", "none"]` rather than bare `str` (plan-review P2).
3. Implement the `UNSEQUENCED_SHIPMENT` advisory signal for shipments with no explicit predecessor.
4. Implement `gates.pipeline_topology.unsequenced_shipment` config switch (`warn` default | `block` opt-in). HARDENING F7: validate against an enum and FAIL CLOSED on an invalid value — never coerce or default silently.
5. Retire `_prior_shipment_id` as a claim-blocking input. Do NOT delete the function outright if other call sites exist; retire the claim-path usage to avoid dangling references (plan-review, Python Reviewer).

HARDENING F6 (MANDATORY): the forward-dependent suppression defect recorded in docs/compound/2026-08-18-topology-gate-forward-dependent-suppression-residual-defect.md is STILL UNFIXED ON MAIN. This task must PROVE by test that retirement renders it moot — assertion alone is insufficient. If it is NOT moot, record a follow-up stash entry rather than silently fixing it.

`pre_claim` remains the SOLE claim authority. `--force` semantics unchanged.

SIZE: M (~2h). COMPLEXITY: high (sole-claim-authority contract change, config validation, migration semantics).
Plan §T2. Depends on T1 (165.001-T).