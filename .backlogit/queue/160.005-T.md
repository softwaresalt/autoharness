---
artifact_type: task
created_at: 2026-09-03T08:24:46.501434Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.001-T
id: 160.005-T
labels:
    - packaging
    - tests
    - fail-closed
parent_id: 160-F
priority: medium
status: queued
title: Fail-closed payload composition RED test harness (test-first, completes at red)
updated_at: 2026-09-03T09:35:09.0588405Z
---

Author the fail-closed payload composition guard as a RED TEST HARNESS. TEST-FIRST (Constitution Principle II, NON-NEGOTIABLE; plan-review finding P1-2; Orchestrator review-fix cycle 1 finding 2).

COMPLETION CRITERION - RED, NOT GREEN (this is the whole point of the task): this task is COMPLETE when every case listed below is AUTHORED and OBSERVED FAILING, and the observed red output is RECORDED in the task's completion evidence. This task MUST NOT be completed by making any case pass, and MUST NOT be blocked on any case passing. At the moment this task completes, the manifest, the loader, and both channel wirings DO NOT YET EXIST - a red result is the expected and required outcome.

RED EVIDENCE REQUIRED: record, per case, the observed failure mode (collection/import error, assertion failure, or missing-artifact error). A case that is green on arrival is a DEFECT of this task: it means the case does not actually exercise the not-yet-built behaviour and must be strengthened until it fails.

GREEN IS OWNED DOWNSTREAM. This task assigns green responsibility explicitly; no case is left unowned:
- MANIFEST-SCOPED cases -> green owned by 160.003-T (manifest allowlist):
  test_manifest_validates_against_schema (AC1)
  test_every_tracked_path_is_classified (AC11)
  test_start_scripts_and_workspace_config_excluded (AC11)
- RESOLVER-SCOPED cases -> green owned by 160.004-T (loader/resolver):
  test_manifest_is_sole_source_of_payload_paths (AC1)
  test_unclassified_tracked_path_fails_build (AC2, AC8) - the V4 negative case
  test_payload_size_reported (AC8)
- WHEEL-COMPOSITION cases -> green owned by 160.006-T (wheel wiring):
  test_wheel_excludes_backlogit_explicitly (AC3)
  test_wheel_excludes_dev_directories (AC3)
  test_wheel_contains_required_runtime_set (AC4)
  test_docs_root_guides_only (AC4)
- PLUGIN-COMPOSITION cases -> green owned by 160.007-T (plugin wiring):
  test_plugin_payload_excludes_dev_directories (AC3)
- GENERATION/DRIFT cases -> green owned by 160.014-T (single deterministic generation command):
  test_generated_tables_match_manifest (AC1) - `generate --check` exits zero on a freshly generated tree
  test_generate_is_byte_deterministic_and_cross_platform (AC1)
- RELEASE-PATH case -> green owned by 160.015-T (unbypassable release-path gate):
  test_release_workflow_runs_payload_gate_before_publish (AC2b)

TESTS RUN AGAINST THE BUILT ARTIFACT, NOT THE SOURCE TREE (verification V1). A source-tree assertion cannot detect a packaging defect - the defect exists only in the built payload (risk R1).

CASE DETAIL:
- test_wheel_excludes_backlogit_explicitly - asserted BY NAME in its own case; .backlogit is the largest disclosure surface (2,110 files) and must not depend on a glob.
- test_payload_size_reported - report measured file count and byte size PER CHANNEL so payload growth is observable in CI.
- test_unclassified_tracked_path_fails_build (verification V4) - inject a synthetic unclassified tracked path and assert the build FAILS. A fail-closed guard never observed failing is not known to be fail-closed. This case must be observed red here (guard absent) AND observed passing-for-the-right-reason downstream.
- test_release_workflow_runs_payload_gate_before_publish (AC2b) - parse .github/workflows/release.yml STRUCTURALLY (not by regex over raw text) and assert (i) a payload-composition gate step exists in the single `release` job, (ii) its step index is strictly LESS than both the build step index and the publish step index, (iii) it carries no `continue-on-error: true`, (iv) it carries no `if:` expression at all. This is what makes AC2's fail-closed promise enforceable in the ACTUAL release path rather than only in a test suite nobody is required to run. Green owned by 160.015-T.
- test_generated_tables_match_manifest and test_generate_is_byte_deterministic_and_cross_platform (AC1) - the derived channel tables are reproducible from the manifest by one command, identically on Windows and POSIX. Green owned by 160.014-T.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. This task authors test files under tests/ ONLY. It MUST NOT author or modify the manifest, the schema, build_support/, pyproject.toml, marketplace.json, or release.yml - doing so would make its own cases green and destroy the red observation. NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

ACCEPTANCE: every case above authored; every case observed RED; red evidence recorded; green ownership table above reproduced in the task's completion note so no case is orphaned.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T3)