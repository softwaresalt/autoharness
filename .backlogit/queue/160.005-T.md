---
artifact_type: task
created_at: 2026-09-03T08:24:46.501434Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.001-T
id: 160.005-T
labels:
    - packaging
    - tests
    - fail-closed
parent_id: 160-F
priority: medium
status: queued
title: Fail-closed payload composition RED-FIRST test harness (completes at red)
updated_at: 2026-09-03T10:17:55.8947079Z
---

RED-FIRST test harness (T3a). RE-SCOPED in review-fix cycle 2 (finding 1): this task now authors ONLY the RED-FIRST class. The CHARACTERIZATION class moved to 160.016-T (T3b). TEST-FIRST (Constitution Principle II, NON-NEGOTIABLE).

WHY THE SPLIT: cycle 1 required EVERY case to be observed RED. That was wrong in two directions and both errors corrupt the evidence. (a) Several cases LEGITIMATELY START GREEN - the wheel's force-include table already lists only templates, schemas, four .github/ subtrees, two instruction files, docs and AGENTS.md, so .backlogit/, tests/ and experiments/ are already absent from the wheel, and both core-metadata-version pins already exist. Demanding red on those forces an author to fabricate a failure or weaken a correct assertion until it breaks. MANUFACTURED RED IS WORSE THAN NO RED because it destroys the signal for the cases where red is real. (b) The install/upgrade/resolver/registration/integrity families had NO RED OWNER AT ALL and were authored after the wiring they govern. Cases that legitimately start green are now explicitly classed CHARACTERIZATION and owned by 160.016-T; they are NOT falsely required to be red.

COMPLETION CRITERION - RED, NOT GREEN: this task is COMPLETE when every case below is AUTHORED and OBSERVED FAILING, and the observed red output is RECORDED per case in the completion evidence. It MUST NOT be completed by making any case pass and MUST NOT be blocked on any case passing. A case that is GREEN ON ARRIVAL is a DEFECT OF THIS TASK: it does not exercise the not-yet-built behaviour and must be strengthened until it fails, OR - if it is green because the behaviour already correctly exists - it is MISCLASSIFIED and belongs to 160.016-T.

RED EVIDENCE REQUIRED: record, per case, the observed failure mode (collection/import error, assertion failure, or missing-artifact error).

CLASSIFICATION IS AUTHORED DATA: each case declares its class RED-FIRST in the test file itself so 160.017-T's transition ledger can assert the required observation pair. A case with no declared class, or one declaring both classes, is a failure.

GREEN IS OWNED DOWNSTREAM; no case is left unowned:
- SCHEMA-SCOPED -> green owned by 160.018-T: test_schema_live_and_versioned_mirror_agree (AC1); test_payload_manifest_contract_registered (AC1). NEW IN CYCLE 2 - cycle 1 let the schema, its mirror and the registration land with no red owner at all.
- MANIFEST-SCOPED -> green owned by 160.003-T: test_manifest_validates_against_schema (AC1); test_every_tracked_path_is_classified (AC11); test_start_scripts_and_workspace_config_excluded (AC11).
- RESOLVER-SCOPED -> green owned by 160.004-T: test_manifest_is_sole_source_of_payload_paths (AC1); test_unclassified_tracked_path_fails_build (AC2, AC8 - the V4 negative case); test_payload_size_reported (AC8).
- GENERATOR-SCOPED -> green owned by 160.014-T: test_generate_emits_expected_tables_to_a_scratch_target (AC2c); test_generate_is_byte_deterministic_and_cross_platform (AC2c); test_only_one_generation_path_exists (AC2c).
- WHEEL-WIRING -> green owned by 160.006-T: test_wheel_generated_table_matches_manifest (AC2c); test_docs_root_guides_only (AC4).
- SDIST-WIRING -> green owned by 160.019-T (NEW IN CYCLE 2, finding 3): test_sdist_generated_table_matches_manifest (AC2c, AC3b); test_sdist_excludes_backlogit_explicitly (AC3b); test_sdist_excludes_dev_directories (AC3b); test_sdist_byte_size_and_disclosure_reported (AC3b, AC8).
- PLUGIN-WIRING -> green owned by 160.007-T: test_plugin_generated_declaration_matches_manifest (AC2c); test_plugin_payload_excludes_dev_directories (AC3); test_plugin_source_path_is_tracked_and_fetchable (AC3c).
- RELEASE-PATH -> green owned by 160.015-T: test_release_workflow_runs_payload_gate_before_publish (AC2b); test_release_gate_covers_all_three_channels (AC2b, AC3b).

TESTS RUN AGAINST THE BUILT ARTIFACT, NOT THE SOURCE TREE (verification V1) - built wheel, built sdist, and resolved plugin payload. A source-tree assertion cannot detect a packaging defect (risk R1); a wheel-only assertion cannot detect an sdist defect.

CASE DETAIL:
- test_sdist_excludes_backlogit_explicitly / test_sdist_excludes_dev_directories - the sdist is the REAL disclosure surface. Its target declares only core-metadata-version, so the default sweep ships everything and `uv build` publishes it with the wheel. These cases are genuinely RED today, unlike their wheel counterparts.
- test_wheel_excludes_backlogit_explicitly is NOT authored here - it is already green on the baseline and is 160.016-T's CHARACTERIZATION case.
- test_payload_size_reported - report measured file count and byte size PER CHANNEL (wheel, sdist, plugin) so payload growth is observable in CI.
- test_unclassified_tracked_path_fails_build (V4) - inject a synthetic unclassified tracked path and assert the build FAILS. A fail-closed guard never observed failing is not known to be fail-closed.
- test_plugin_source_path_is_tracked_and_fetchable (AC3c, NEW IN CYCLE 2, finding 5) - assert marketplace.json `source` resolves to a path PRESENT IN A CONSUMER-FETCHABLE CHECKOUT. dist/.gitignore contains `*`, so a `source` under dist/ is unfetchable and the install simply fails. Assert the path is tracked, not merely that it exists locally.
- test_release_workflow_runs_payload_gate_before_publish (AC2b) - parse release.yml STRUCTURALLY (not by regex over raw text) and assert (i) a payload-composition gate step exists in the single `release` job, (ii) its step index is strictly LESS than both the build step index and the publish step index, (iii) no `continue-on-error: true`, (iv) no `if:` expression at all.
- test_release_gate_covers_all_three_channels (AC2b, AC3b, NEW IN CYCLE 2) - assert the gate's channel argument resolves to wheel AND sdist AND plugin. A gate covering two of three channels is not a gate for the third.
- test_generate_emits_expected_tables_to_a_scratch_target (AC2c) - REPLACES cycle 1's test_generated_tables_match_manifest for the generator. That case asserted the COMMITTED tables match, which 160.014-T was forbidden to produce; the committed-parity property is now per-channel and owned by the wiring tasks.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. RED-PRESERVATION CHECKPOINT: this task authors test files under tests/ ONLY. It MUST NOT author or modify the manifest, either schema file, schema_contracts.py, build_support/, pyproject.toml, marketplace.json, plugin-payload/, or release.yml - doing so would make its own cases green and destroy the red observation. NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

ACCEPTANCE: every RED-FIRST case above authored and declared; every one observed RED; red evidence recorded per case; the green-ownership mapping reproduced in the completion note so no case is orphaned; no CHARACTERIZATION case authored here.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T3a)