---
artifact_type: task
created_at: 2026-09-03T08:24:46.501434Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.001-T
id: 160.005-T
labels:
    - packaging
    - tests
    - fail-closed
parent_id: 160-F
priority: medium
status: queued
title: Fail-closed payload composition RED-FIRST test harness (completes at red)
updated_at: 2026-09-03T16:12:05.2157404Z
---

RED-FIRST test harness (T3a). This task authors ONLY the RED-FIRST class. The CHARACTERIZATION class belongs to 160.016-T (T3b). TEST-FIRST (Constitution Principle II, NON-NEGOTIABLE).

WHY THE TWO HARNESSES ARE SEPARATE: requiring EVERY case to be observed RED is wrong in two directions and both errors corrupt the evidence. (a) Several cases LEGITIMATELY START GREEN - the wheel's force-include table already lists only templates, schemas, four .github/ subtrees, two instruction files, docs and AGENTS.md, so .backlogit/, tests/ and experiments/ are already absent from the wheel, and both core-metadata-version pins already exist. Demanding red on those forces an author to fabricate a failure or weaken a correct assertion until it breaks. MANUFACTURED RED IS WORSE THAN NO RED because it destroys the signal for the cases where red is real. (b) The install/upgrade/resolver/registration/integrity families must not be authored after the wiring they govern. Cases that legitimately start green are classed CHARACTERIZATION and owned by 160.016-T; they are NOT falsely required to be red.

COMPLETION CRITERION - RED, NOT GREEN: this task is COMPLETE when every case below is AUTHORED and OBSERVED FAILING, and the observed red output is RECORDED per case in the completion evidence. It MUST NOT be completed by making any case pass and MUST NOT be blocked on any case passing. A case that is GREEN ON ARRIVAL is a DEFECT OF THIS TASK: it does not exercise the not-yet-built behaviour and must be strengthened until it fails, OR - if it is green because the behaviour already correctly exists - it is MISCLASSIFIED and belongs to 160.016-T.

RED EVIDENCE REQUIRED: record, per case, the observed failure mode (collection/import error, assertion failure, or missing-artifact error). The plan's canonical case table states the expected initial observation for every case; the recorded evidence must match it.

CLASSIFICATION IS AUTHORED DATA: each case declares its class RED-FIRST in the test file itself so 160.017-T's transition ledger can assert the required observation pair. A case with no declared class, or one declaring both classes, is a failure. CLASS IS DECLARED PER CASE, NEVER PER FAMILY - a family-wide label cannot be true of a mixed family and has caused real plan/queue divergence.

TWENTY-FIVE RED-FIRST CASES. GREEN IS OWNED DOWNSTREAM; no case is left unowned:
- SCHEMA-SCOPED -> green owned by 160.018-T: test_schema_live_and_versioned_mirror_agree (AC1); test_payload_manifest_contract_registered (AC1).
- MANIFEST-SCOPED -> green owned by 160.003-T: test_manifest_validates_against_schema (AC1); test_every_tracked_path_is_classified (AC11, AC3d); test_start_scripts_and_workspace_config_excluded (AC11); test_target_workspace_prefixes_have_exactly_one_authored_occurrence (AC2d - ADDED IN EXTENDED REVIEW-FIX CYCLE 4, FINDING 9).
- RESOLVER-SCOPED -> green owned by 160.004-T: test_manifest_is_sole_source_of_payload_paths (AC1); test_unclassified_tracked_path_fails_build (AC2, AC8 - the V4 negative case); test_payload_size_reported (AC8); test_generated_output_root_excluded_from_classifier_inputs (AC3d, AC2).
- GENERATOR-SCOPED -> green owned by 160.014-T: test_generate_emits_expected_tables_to_a_scratch_target (AC2c); test_generate_is_byte_deterministic_and_cross_platform (AC2c); test_only_one_generation_path_exists (AC2c); test_plugin_generation_is_idempotent (AC2c, AC3d).
- WHEEL-WIRING -> green owned by 160.006-T: test_wheel_generated_table_matches_manifest (AC2c); test_docs_root_guides_only (AC4).
- SDIST-WIRING -> green owned by 160.019-T: test_sdist_generated_table_matches_manifest (AC2c, AC3b); test_sdist_excludes_backlogit_explicitly (AC3b); test_sdist_excludes_dev_directories (AC3b); test_sdist_byte_size_and_disclosure_reported (AC3b, AC8).
- PLUGIN-WIRING -> green owned by 160.007-T: test_plugin_generated_declaration_matches_manifest (AC2c); test_plugin_payload_excludes_dev_directories (AC3); test_plugin_payload_conforms_to_the_selected_strategy (AC3d, AC3c - RENAMED AND MADE BRANCH-AWARE in cycle 4, finding 6).
- RELEASE-PATH -> green owned by 160.015-T: test_release_workflow_runs_payload_gate_before_publish (AC2b); test_release_gate_covers_all_three_channels (AC2b, AC3b).

TESTS RUN AGAINST THE BUILT ARTIFACT, NOT THE SOURCE TREE (verification V1) - built wheel, built sdist, and resolved plugin payload. A source-tree assertion cannot detect a packaging defect (risk R1); a wheel-only assertion cannot detect an sdist defect.

CASE DETAIL:
- test_sdist_excludes_backlogit_explicitly / test_sdist_excludes_dev_directories - the sdist is the REAL disclosure surface. Its target declares only core-metadata-version, so the default sweep ships everything and `uv build` publishes it with the wheel. These cases are genuinely RED today, unlike their wheel counterparts.
- test_wheel_excludes_backlogit_explicitly is NOT authored here - it is already green on the baseline and is 160.016-T's CHARACTERIZATION case.
- test_payload_size_reported - report measured file count and byte size PER CHANNEL (wheel, sdist, plugin) so payload growth is observable in CI.
- test_unclassified_tracked_path_fails_build (V4) - inject a synthetic unclassified tracked path and assert the build FAILS. A fail-closed guard never observed failing is not known to be fail-closed.
- test_generated_output_root_excluded_from_classifier_inputs (AC3d) - assert NO path under ANY declared `generated_output_roots` entry appears in ANY channel's resolved input set, AND that the AC2 unclassified-path check does not flag generated content. Red today: neither the resolver nor the manifest key exists. This case is the guard against the recursion defect described below.
- test_plugin_generation_is_idempotent (AC2c, AC3d) - run `generate --channel plugin` TWICE against a scratch target and assert a BYTE-IDENTICAL tree and an IDENTICAL resolved input set on both runs. Distinct from test_generate_is_byte_deterministic_and_cross_platform, which pins OUTPUT stability; this pins INPUT stability after the generator has written output.
- test_plugin_payload_conforms_to_the_selected_strategy (AC3d, AC3c) - BRANCH-AWARE, REPLACING test_plugin_payload_tree_is_self_excluded_and_flat (corrected in extended review-fix cycle 4, finding 6). THE WITHDRAWN FORMULATION used TREE ABSENCE as its initial RED state; that is invalid, because under branch (a) the tree LEGITIMATELY NEVER EXISTS, so absence is a valid FINAL state there and the case would have been VACUOUSLY GREEN ON ARRIVAL - a green-on-arrival RED-FIRST case is a defect of this task. The case is now PARAMETERIZED BY THE STRATEGY 160.002-T SELECTS, read from the manifest's declared plugin-strategy key. THE SELECTED STRATEGY IS REQUIRED DATA: a run in which no strategy is declared FAILS, so the case can never be satisfied by silence. Under BRANCH (a) assert the CONJUNCTION: (i) NO `plugin-payload/` tree is materialized anywhere, AND (ii) the payload the native mechanism ACTUALLY RESOLVES equals the MANIFEST-DERIVED PLUGIN SET - file-set EQUALITY against the resolver output, not a subset check and not merely "smaller than the repository". Conjunct (ii) is what makes branch (a) genuinely red at baseline: tree-absence is already true, but the baseline payload is the ENTIRE REPOSITORY. Under BRANCH (b) assert the tracked tree EXISTS, is TRACKED, is FLAT (contains NO nested directory named `plugin-payload`), is SELF-EXCLUDED from every channel's classifier input, and that `generate --channel plugin --check` reports NO drift immediately after a regeneration - this is what proves the drift is not permanent, since if `plugin-payload/**` were classified as a plugin INCLUDE input each run would materialize the tree inside itself (`plugin-payload/plugin-payload/...`) and `--check` would report drift that regenerating only deepens. AT AUTHORING TIME the manifest does not exist, so the case is RED on strategy-undeclared under BOTH branches, and remains red on the payload assertion once the strategy is declared.
- test_plugin_source_path_is_tracked_and_fetchable IS NOT AUTHORED HERE (cycle 4, finding 5). It was RECLASSIFIED from RED-FIRST to CHARACTERIZATION and moved to 160.016-T, with 160.007-T as its PRESERVATION owner. It cannot be red: the baseline `source: "."` resolves to the repository ROOT, which IS tracked and IS present in every consumer checkout, so the tracked-and-fetchable property is BASELINE-TRUE; and branch (a) PRESERVES `source: "."`, so the case would be green at authoring, green throughout, and would never transition. Authoring it here and recording it red would require weakening the assertion until the baseline broke it - MANUFACTURING RED, which destroys the signal for the cases where red is real.
- test_target_workspace_prefixes_have_exactly_one_authored_occurrence (AC2d) - ADDED IN CYCLE 4, FINDING 9. AC2d specified this test in prose ("the test asserts exactly one authored occurrence") but NO LEDGER ROW EVER NAMED IT, so it was invisible to 160.017-T and effectively unowned. TWO ASSERTIONS IN ONE CASE. (i) OCCURRENCE SCAN: exactly ONE authored occurrence of the target-workspace prefixes across build_support/**, src/**, tests/** EXCLUDING tests/fixtures/**, pyproject.toml, .github/plugin/marketplace.json, .github/workflows/**, and the manifest - the manifest's `target_workspace_paths` key being that one occurrence. tests/fixtures/** is carved out because 160.001-T's baseline fixtures legitimately RECORD prefixes as inert data; scanning them wholesale made an accurate baseline fail the drift test. (ii) DERIVATION ASSERTION, which is what preserves the guarantee the carve-out would otherwise weaken: NO module under build_support/** or src/** may obtain target-workspace prefixes from a fixture, a test constant, or any source other than classify_target_workspace_path reading the manifest key. A fixture literal is inert; a fixture literal that something EXECUTABLE reads is a second source of truth, and that - not its existence - is the drift this rule stops. RED TODAY: the manifest and the key do not exist, so the scan finds ZERO occurrences and the exactly-one assertion fails. Green owner 160.003-T.
- test_release_workflow_runs_payload_gate_before_publish (AC2b) - parse release.yml STRUCTURALLY (not by regex over raw text) and assert (i) a payload-composition gate step exists in the single `release` job, (ii) its step index is strictly LESS than both the build step index and the publish step index, (iii) no `continue-on-error: true`, (iv) no `if:` expression at all.
- test_release_gate_covers_all_three_channels (AC2b, AC3b) - assert the gate's channel argument resolves to wheel AND sdist AND plugin. A gate covering two of three channels is not a gate for the third. This case, together with test_manifest_validates_against_schema (whose schema declares a CLOSED channel enum), is what machine-enforces the closed three-channel set.
- test_generate_emits_expected_tables_to_a_scratch_target (AC2c) - asserts generation against a SCRATCH target. The COMMITTED-parity property is per-channel and owned by the wiring tasks (160.006-T, 160.019-T, 160.007-T), not by the generator, because the generator is forbidden to commit the tables it produces.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. RED-PRESERVATION CHECKPOINT: this task authors test files under tests/ ONLY. It MUST NOT author or modify the manifest, either schema file, schema_contracts.py, build_support/, pyproject.toml, marketplace.json, plugin-payload/, or release.yml - doing so would make its own cases green and destroy the red observation. It also does NOT write baseline fixtures under tests/fixtures/payload-baseline/**, which are 160.001-T's exclusive surface. Its ONLY authorized write outside tests/ is its own first-observation record under docs/audits/2026-09-03-ship10-payload-evidence/observations/ (see FIRST-OBSERVATION DESTINATION below). NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

FIRST-OBSERVATION DESTINATION (authorized, binding; added in extended review-fix cycle 4, finding 7). The red observations this task produces are a NON-DEFERRABLE acceptance condition consumed by 160.017-T, so they need a durable, authorized home; cycle 3 authorized 160.001-T's baseline and 160.017-T's ledger but left THIS task's observations with no legal destination under freeze-scope. They are written to docs/audits/2026-09-03-ship10-payload-evidence/observations/T3a.json - a subdirectory of the SINGLE bounded evidence surface already authorized for this shipment, NOT a new store. FORMAT (machine-readable handoff, one record per case): `test_name` matching the plan ledger row VERBATIM, `declared_class` = "RED-FIRST", `author_task` = "160.005-T", `first_observation` = "red", `observed_at` ISO-8601, `command` (exact invocation), `exit_status`, `failing_assertion` (non-null), `evidence_ref` (path to the raw log in the same directory). 160.017-T JOINS this file to the canonical case table ON `test_name`; a ledger row with no record, a record with no ledger row, or any field mismatch FAILS THE SHIPMENT. Records are APPEND-ONLY within the shipment and written ONLY by this task.

ACCEPTANCE: all twenty-five RED-FIRST cases above authored and declared; every one observed RED with the failure mode the plan's case table predicts; red evidence recorded per case IN THE MACHINE-READABLE FORM ABOVE; the green-ownership mapping reproduced in the completion note so no case is orphaned; no CHARACTERIZATION case authored here. CASE-COUNT RECONCILIATION (cycle 4): the count REMAINS TWENTY-FIVE by two offsetting changes, not by oversight - finding 5 REMOVED test_plugin_source_path_is_tracked_and_fetchable to 160.016-T (-1) and finding 9 ADDED test_target_workspace_prefixes_have_exactly_one_authored_occurrence (+1).

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size M, complexity medium. The case count is high but the MARGINAL cost per case is low: all twenty-five share three fixtures (a built wheel, a built sdist, a resolved plugin payload), and the great majority fail uniformly at import or missing-artifact rather than needing bespoke setup. The judgement concentrates in four cases - the V4 unclassified-path injection, the structural release.yml parse, the idempotence run-twice harness, and the self-exclusion assertion - which is what holds complexity at medium rather than low. The three recursion-guard cases added in review-fix cycle 3 reuse the existing plugin-payload fixture and do not change the size band.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T3a)