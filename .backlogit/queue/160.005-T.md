---
artifact_type: task
created_at: 2026-09-03T08:24:46.501434Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.001-T
    - 160.020-T
id: 160.005-T
labels:
    - packaging
    - tests
    - fail-closed
parent_id: 160-F
priority: medium
status: queued
title: Fail-closed payload composition RED-FIRST test harness (completes at red)
updated_at: 2026-09-03T20:25:46.2619615Z
---

RED-FIRST test harness (T3a). This task authors ONLY the RED-FIRST class. The CHARACTERIZATION class belongs to 160.016-T (T3b). TEST-FIRST (Constitution Principle II, NON-NEGOTIABLE).

WHY THE TWO HARNESSES ARE SEPARATE: requiring EVERY case to be observed RED is wrong in two directions and both errors corrupt the evidence. (a) Several cases LEGITIMATELY START GREEN - the wheel's force-include table already lists only templates, schemas, four .github/ subtrees, two instruction files, docs and AGENTS.md, so .backlogit/, tests/ and experiments/ are already absent from the wheel, and both core-metadata-version pins already exist. Demanding red on those forces an author to fabricate a failure or weaken a correct assertion until it breaks. MANUFACTURED RED IS WORSE THAN NO RED because it destroys the signal for the cases where red is real. (b) The install/upgrade/resolver/registration/integrity families must not be authored after the wiring they govern. Cases that legitimately start green are classed CHARACTERIZATION and owned by 160.016-T; they are NOT falsely required to be red.

COMPLETION CRITERION - RED, NOT GREEN: this task is COMPLETE when every case below is AUTHORED and OBSERVED FAILING, and the observed red output is RECORDED per case in the completion evidence. It MUST NOT be completed by making any case pass and MUST NOT be blocked on any case passing. A case that is GREEN ON ARRIVAL is a DEFECT OF THIS TASK: it does not exercise the not-yet-built behaviour and must be strengthened until it fails, OR - if it is green because the behaviour already correctly exists - it is MISCLASSIFIED and belongs to 160.016-T.

RED EVIDENCE REQUIRED: record, per case, the observed failure mode (collection/import error, assertion failure, or missing-artifact error). The plan's canonical case table states the expected initial observation for every case; the recorded evidence must match it.

CLASSIFICATION IS AUTHORED DATA: each case declares its class RED-FIRST in the test file itself so 160.017-T's transition ledger can assert the required observation pair. A case with no declared class, or one declaring both classes, is a failure. CLASS IS DECLARED PER CASE, NEVER PER FAMILY - a family-wide label cannot be true of a mixed family and has caused real plan/queue divergence.

TWENTY-TWO RED-FIRST CASES. GREEN IS OWNED DOWNSTREAM; no case is left unowned:
- SCHEMA-SCOPED -> green owned by 160.018-T: test_schema_live_and_versioned_mirror_agree (AC1); test_payload_manifest_contract_registered (AC1).
- MANIFEST-SCOPED -> green owned by 160.003-T: test_manifest_validates_against_schema (AC1); test_every_tracked_path_is_classified (AC11, AC3d); test_start_scripts_and_workspace_config_excluded (AC11); test_target_workspace_prefix_derivation_is_centralized (AC2d - ADDED IN EXTENDED REVIEW-FIX CYCLE 4, FINDING 9).
- RESOLVER-SCOPED -> green owned by 160.004-T: test_manifest_is_sole_source_of_payload_paths (AC1); test_unclassified_tracked_path_fails_build (AC2, AC8 - the V4 negative case); test_payload_size_reported (AC8); test_generated_output_root_excluded_from_classifier_inputs (AC3d, AC2).
- GENERATOR-SCOPED -> green owned by 160.014-T: test_generate_emits_expected_tables_to_a_scratch_target (AC2c); test_generate_is_byte_deterministic_and_cross_platform (AC2c); test_only_one_generation_path_exists (AC2c); test_plugin_generation_is_idempotent (AC2c, AC3d).
- WHEEL-WIRING -> green owned by 160.006-T: test_wheel_generated_table_matches_manifest (AC2c); test_docs_root_guides_only (AC4); test_wheel_contains_required_runtime_set (AC4 - RECLASSIFIED FROM CHARACTERIZATION TO RED-FIRST IN REVIEW-FIX CYCLE 5, FINDING 12; see CASE DETAIL).

  THE SDIST-WIRING FAMILY IS DELETED (cycle 5, finding 1). Its four cases - test_sdist_generated_table_matches_manifest, test_sdist_excludes_backlogit_explicitly, test_sdist_excludes_dev_directories, test_sdist_byte_size_and_disclosure_reported - are NOT AUTHORED, because the sdist channel is out of scope and their green owner 160.019-T is RETIRED (archived at .backlogit/archive/160.019-T.md). Their names and intent are PRESERVED AS EVIDENCE in deferred entry 99818C6D so the discovery is not lost. Do NOT author them here.
- PLUGIN-WIRING -> green owned by 160.007-T: test_plugin_generated_declaration_matches_manifest (AC2c); test_plugin_payload_excludes_dev_directories (AC3); test_plugin_payload_conforms_to_the_selected_strategy (AC3d, AC3c - RENAMED AND MADE BRANCH-AWARE in cycle 4, finding 6).
- RELEASE-PATH -> green owned by 160.015-T: test_release_workflow_runs_payload_gate_before_publish (AC2b); test_release_gate_covers_all_declared_channels (AC2b - RENAMED from test_release_gate_covers_all_three_channels in cycle 5).

TESTS RUN AGAINST THE BUILT ARTIFACT, NOT THE SOURCE TREE (verification V1) - the built wheel and the resolved plugin payload. A source-tree assertion cannot detect a packaging defect (risk R1); a plugin-only assertion cannot detect a wheel defect, and vice versa, so BOTH in-scope channels are asserted independently and never inferred from each other.

CASE DETAIL:
- test_wheel_excludes_backlogit_explicitly is NOT authored here - it is already green on the baseline and is 160.016-T's CHARACTERIZATION case. Measured: the wheel's force-include table already omits .backlogit, tests and experiments.
- test_wheel_contains_required_runtime_set (AC4) - RECLASSIFIED FROM CHARACTERIZATION TO RED-FIRST IN CYCLE 5 (FINDING 12). A characterization case asserts a property the BASELINE ALREADY HAS. This one does not: measured at HEAD, the wheel's force-include table lists templates, schemas, four .github/ subtrees, both copilot instruction files, all of docs, and AGENTS.md - and OMITS .github/policies/** (which carries workflow-policies.md, referenced by installed agents) and scripts/** (whose deploy-harness/lock/ci-topology-check members are referenced by installed skills). Asserting the required runtime set therefore FAILS on the baseline wheel, which is the definition of RED-FIRST. Classing it CHARACTERIZATION would have required either weakening it to whatever the baseline happens to contain - proving nothing - or recording a false green-at-baseline observation that 160.017-T's ledger would then enshrine. Green owner 160.006-T, which adds the missing paths when it wires the wheel to the manifest.
- test_payload_size_reported - report measured file count and byte size PER CHANNEL (wheel, plugin) so payload growth is observable in CI.
- test_unclassified_tracked_path_fails_build (V4) - inject a synthetic unclassified tracked path and assert the build FAILS. A fail-closed guard never observed failing is not known to be fail-closed.
- test_generated_output_root_excluded_from_classifier_inputs (AC3d) - assert NO path under ANY declared `generated_output_roots` entry appears in ANY channel's resolved input set, AND that the AC2 unclassified-path check does not flag generated content. Red today: neither the resolver nor the manifest key exists. This case is the guard against the recursion defect described below.
- test_plugin_generation_is_idempotent (AC2c, AC3d) - run `generate --channel plugin` TWICE against a scratch target and assert a BYTE-IDENTICAL tree and an IDENTICAL resolved input set on both runs. Distinct from test_generate_is_byte_deterministic_and_cross_platform, which pins OUTPUT stability; this pins INPUT stability after the generator has written output.
- test_plugin_payload_conforms_to_the_selected_strategy (AC3d, AC3c) - BRANCH-AWARE, REPLACING test_plugin_payload_tree_is_self_excluded_and_flat (corrected in extended review-fix cycle 4, finding 6). THE WITHDRAWN FORMULATION used TREE ABSENCE as its initial RED state; that is invalid, because under branch (a) the tree LEGITIMATELY NEVER EXISTS, so absence is a valid FINAL state there and the case would have been VACUOUSLY GREEN ON ARRIVAL - a green-on-arrival RED-FIRST case is a defect of this task. The case is now PARAMETERIZED BY THE STRATEGY 160.002-T SELECTS, read from the manifest's declared plugin-strategy key. THE SELECTED STRATEGY IS REQUIRED DATA: a run in which no strategy is declared FAILS, so the case can never be satisfied by silence. Under BRANCH (a) assert the CONJUNCTION: (i) NO `plugin-payload/` tree is materialized anywhere, AND (ii) the payload the native mechanism ACTUALLY RESOLVES equals the MANIFEST-DERIVED PLUGIN SET - file-set EQUALITY against the resolver output, not a subset check and not merely "smaller than the repository". Conjunct (ii) is what makes branch (a) genuinely red at baseline: tree-absence is already true, but the baseline payload is the ENTIRE REPOSITORY. Under BRANCH (b) assert the tracked tree EXISTS, is TRACKED, is FLAT (contains NO nested directory named `plugin-payload`), is SELF-EXCLUDED from every channel's classifier input, and that `generate --channel plugin --check` reports NO drift immediately after a regeneration - this is what proves the drift is not permanent, since if `plugin-payload/**` were classified as a plugin INCLUDE input each run would materialize the tree inside itself (`plugin-payload/plugin-payload/...`) and `--check` would report drift that regenerating only deepens. AT AUTHORING TIME the manifest does not exist, so the case is RED on strategy-undeclared under BOTH branches, and remains red on the payload assertion once the strategy is declared.
- test_plugin_source_path_is_tracked_and_fetchable IS NOT AUTHORED HERE (cycle 4, finding 5). It was RECLASSIFIED from RED-FIRST to CHARACTERIZATION and moved to 160.016-T, with 160.007-T as its PRESERVATION owner. It cannot be red: the baseline `source: "."` resolves to the repository ROOT, which IS tracked and IS present in every consumer checkout, so the tracked-and-fetchable property is BASELINE-TRUE; and branch (a) PRESERVES `source: "."`, so the case would be green at authoring, green throughout, and would never transition. Authoring it here and recording it red would require weakening the assertion until the baseline broke it - MANUFACTURING RED, which destroys the signal for the cases where red is real.
- test_target_workspace_prefix_derivation_is_centralized (AC2d) - ADDED IN CYCLE 4, FINDING 9; ITS ASSERTIONS REWRITTEN IN CYCLE 6, FINDING 19 AND THE STALE DETAIL DELETED HERE IN CYCLE 7. AC2d had specified this test in prose but NO LEDGER ROW EVER NAMED IT, so it was invisible to 160.017-T and effectively unowned. THE CASE NAME WAS CHANGED AGAIN IN CYCLE 9, ITEM 10: `test_target_workspace_prefix_derivation_predicate_holds` IS WITHDRAWN AND MUST NOT APPEAR IN THE SHIPPED SUITE. "Predicate holds" is a TAUTOLOGY that names no property - every passing test's predicate holds - so the name told a reader nothing about what is asserted. The new name states the CENTRALIZATION property that AC2d's P1-P4 derivation predicate actually establishes. THE NAME IS A STABLE LOGICAL IDENTIFIER, NOT A BEHAVIOUR SPECIFICATION; the binding remains P1-P4 exactly as written below and NO ASSERTION CHANGED. The rename is ATOMIC across the plan case table, the ledger (46 rows AT THE TIME OF THAT RENAME; the ledger is 47 rows now after cycle 11 item 8 - the rename touched no count), and every task record that names it (this task, 160.003-T, 160.004-T, 160.017-T, 160.018-T), performed IN THE SAME CYCLE so 160.017-T's verbatim bijection is re-established immediately; the case is RED-FIRST AND NOT YET AUTHORED, so there is no shipped suite to desynchronize. THE CASE COUNT IS UNCHANGED AT 46 AND THIS TASK STILL AUTHORS TWENTY-TWO. THE NAME WAS PREVIOUSLY CHANGED IN CYCLE 8, ITEM 11; ITS CLASS (RED-FIRST), AUTHOR (this task) AND GREEN OWNER (160.003-T) ARE ALL UNCHANGED, so the 46-row ledger, the twenty-two-case count for this task, and 160.017-T's verbatim ledger-to-suite bijection are UNTOUCHED - A RENAME IS NOT AN ADDITION. Cycle 7 retained the old name `test_target_workspace_prefixes_have_exactly_one_authored_occurrence` on the ground that renaming would break the bijection for no gain; THAT REASONING IS WITHDRAWN. The old name asserted the "exactly one authored occurrence" rule that cycle 6, finding 19 had ALREADY PROVED UNSATISFIABLE AND WITHDRAWN (57 occurrences across 23 files at the measured tip), so the name was carrying a LIVE STALE SEMANTIC, not a harmless historical label - a reader who trusts the name authors the withdrawn assertion. The rename is mechanically safe because the case is RED-FIRST and NOT YET AUTHORED: there is no shipped suite to desynchronize, and the bijection is re-established by renaming the ledger row and every task reference IN THE SAME CYCLE, which cycle 8 does atomically across the plan case table, this record, 160.003-T, 160.004-T, 160.017-T and 160.018-T. WHAT THE CASE ASSERTS IS THE CANONICAL FOUR-PREDICATE SET P1-P4, NOT AN OCCURRENCE COUNT. The withdrawn "(i) OCCURRENCE SCAN: exactly ONE authored occurrence of the target-workspace prefixes across build_support/**, src/**, tests/** EXCLUDING tests/fixtures/**, pyproject.toml, .github/plugin/marketplace.json, .github/workflows/** and the manifest" assertion IS UNSATISFIABLE AS MEASURED - 57 occurrences across 23 files at the tip, 8 in freeze-scoped src/** - so it is RED ON ARRIVAL AND CAN NEVER GO GREEN, and NO source edit is authorized to make it true. DO NOT AUTHOR IT. AUTHOR EXACTLY THESE FOUR MECHANICALLY DECIDABLE PREDICATES, whose authoritative text is plan AC2d: P1 DECLARATION UNIQUENESS (AST) - parse every module under build_support/** and assert ZERO string constants equal to or prefixed by a target-workspace prefix, the manifest's `target_workspace_paths` key being the single authored declaration; decidable by walking ast.Constant nodes. P2 CALL-SITE ROUTING (AST) - every AC5/AC9/R2 target-workspace classification decision in build_support/** reaches that decision by CALLING classify_target_workspace_path; decidable by collecting ast.Call nodes and asserting the set of deciding modules equals {the classifier module}. P3 NO FIXTURE INGESTION (AST) - no module under build_support/** imports from, opens, or path-references tests/fixtures/**; this is what keeps the fixture carve-out honest, since 160.001-T's baseline fixtures legitimately RECORD prefixes as INERT DATA and only a fixture literal something EXECUTABLE READS is a second source of truth. P4 FROZEN PRE-EXISTING ALLOW-LIST, NON-INCREASE (symbol-level) - the two src/autoharness/verify_workspace.py executable literals (the DOCS_COMPOUND and DOCS_CLOSURE workspace-relative defaults) are recorded in a named allow-list keyed BY ENCLOSING SYMBOL, not by line number; assert the allow-list contains EXACTLY those two AND that NO ADDITIONAL executable target-workspace literal appears anywhere in src/**. P4 is a NON-INCREASE predicate - true today and enforceable under freeze-scope - and it fails the moment a THIRD literal appears. RED TODAY, AND RED FOR A REASON THAT SURVIVES THE REWRITE: the manifest and its `target_workspace_paths` key do not exist, so P1 has no single authored declaration to find and P2 has no classifier module to route through; both fail at authoring time. Green owner 160.003-T.
- test_release_workflow_runs_payload_gate_before_publish (AC2b) - **WIDENED IN CYCLE 11, ITEM 2, TO MATCH THE OWNER-SIDE PREDICATE 160.015-T HAS CARRIED SINCE CYCLE 6, FINDING 1. ONE CASE, WIDER PREDICATE: NOT RENAMED, NOT SPLIT, NO LEDGER ROW ADDED.** The withdrawn single-gate form asserted only that "a payload-composition gate step exists" and precedes build and publish; that predates the cycle-6 split of the gate into TWO steps and would have been GREEN against a release workflow carrying only Gate A - it could not have witnessed the very defect the split exists to prevent, and it disagreed with the record of the task that owns its green transition. Parse release.yml STRUCTURALLY (not by regex over raw text) and assert: (i) BOTH gate steps exist in the single `release` job; (ii) indexOf(Gate A) < indexOf(build); (iii) indexOf(build) < indexOf(Gate B) < indexOf(publish); (iv) NEITHER carries `continue-on-error: true`; (v) NEITHER carries any `if:` expression at all; (vi) NO STEP POSITIONED BETWEEN GATE B AND PUBLISH WRITES INTO `dist/` - this is the assertion that makes Gate B's digest-identity claim true rather than assumed, and the only step in that window today is `uvx twine check dist/*`, which is read-only. RED ON THE BASELINE for the plainest possible reason: release.yml today contains NEITHER gate step.
- test_release_gate_covers_all_declared_channels (AC2b) - RENAMED IN CYCLE 5 from test_release_gate_covers_all_three_channels, which hardcoded a channel COUNT into a test name and would have had to be renamed again by any future channel change. Assert the gate's channel argument resolves to EVERY channel the manifest's closed enum declares - currently wheel AND plugin - derived from the enum rather than from a literal list, so a gate covering a proper subset fails and a future channel addition is covered without editing the test. This case, together with test_manifest_validates_against_schema (whose schema declares a CLOSED channel enum), is what machine-enforces the closed channel set.
- test_generate_emits_expected_tables_to_a_scratch_target (AC2c) - asserts generation against a SCRATCH target. The COMMITTED-parity property is per-channel and owned by the wiring tasks (160.006-T, 160.007-T), not by the generator, because the generator is forbidden to commit the tables it produces.

DEPENDENCIES (CYCLE 9): blocked by 160.001-T (baseline capture) and - ADDED IN CYCLE 9 - 160.020-T (test-toolchain alignment). THE 160.020-T EDGE IS LOAD-BEARING, NOT DECORATIVE: this task's COMPLETION CONDITION IS A RECORDED RED OBSERVATION for every case it authors, and an observation cannot be taken at all until the runner the plan mandates actually resolves. Measured before cycle 9, `pytest` was configured in `pyproject.toml` but absent from `uv.lock`, and ci.yml invoked `unittest discover`; 160.020-T declares the pinned dependency, regenerates the lock, and moves CI to `uv run python -m pytest`. Because this task and 160.016-T are the EARLIEST NODES IN THE DAG THAT MUST EXECUTE A TEST, blocking them on 160.020-T places the toolchain fix BEFORE EVERY SHIP-10 test author and consumer that requires pytest - 160.008-T, 160.012-T, 160.013-T, 160.009-T, 160.010-T, 160.003-T, 160.004-T, 160.014-T, 160.006-T, 160.007-T, 160.015-T and 160.017-T are all reachable from here. THIS TASK INVENTS NO PYTEST SETUP OF ITS OWN and MUST NOT edit `pyproject.toml`, `uv.lock`, or any workflow file; it RELIES on its 160.020-T prerequisite.
SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. RED-PRESERVATION CHECKPOINT: this task authors test files under tests/ ONLY. It MUST NOT author or modify the manifest, either schema file, schema_contracts.py, build_support/, pyproject.toml, marketplace.json, plugin-payload/, or release.yml - doing so would make its own cases green and destroy the red observation. It also does NOT write baseline fixtures under tests/fixtures/payload-baseline/**, which are 160.001-T's exclusive surface. Its authorized writes outside tests/ are its first-observation record AND its bounded raw logs, both enumerated in the EVIDENCE WRITE SURFACE clause below (the withdrawn "ONLY ... is its own first-observation record" wording is superseded in cycle 12, item 8). **EVIDENCE WRITE SURFACE - TWO AUTHORIZED DESTINATIONS, BOTH OPERATIVE (CYCLE 12, ITEM 8).** This task is an EVIDENCE PRODUCER and is authorized to write BOTH: (1) its first-observation record at `docs/audits/2026-09-03-ship10-payload-evidence/observations/T3a.json`, AND (2) its bounded raw logs under its OWN subdirectory `docs/audits/2026-09-03-ship10-payload-evidence/observations/logs/T3a/**` and nowhere else. **ANY "ONLY AUTHORIZED WRITE ... IS ITS OBSERVATION RECORD" WORDING IS WITHDRAWN AS A CONTRADICTION:** the evidence contract REQUIRES a raw log for every observation (`evidence_ref`/`evidence_sha256`/`evidence_bytes` are mandatory fields and 160.017-T validates the referenced log), so a safety clause authorizing only the JSON would have made this task's own mandatory evidence an unauthorized write under freeze-scope. BOTH destinations sit inside the SAME single bounded evidence surface; NEITHER is a second store. Log bounds are FAIL-CLOSED per 160.017-T (<=256 KiB AND <=2,000 lines per log; halt and report rather than truncate). THIS AUTHORIZES DESTINATIONS ONLY: this task DOES NOT redefine, restate, or extend the canonical evidence field list, which lives once in the plan's "Evidence-verification contract for 160.017-T" table. NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

FIRST-OBSERVATION DESTINATION (authorized, binding; added in extended review-fix cycle 4, finding 7). The red observations this task produces are a NON-DEFERRABLE acceptance condition consumed by 160.017-T, so they need a durable, authorized home; cycle 3 authorized 160.001-T's baseline and 160.017-T's ledger but left THIS task's observations with no legal destination under freeze-scope. They are written to docs/audits/2026-09-03-ship10-payload-evidence/observations/T3a.json - a subdirectory of the SINGLE bounded evidence surface already authorized for this shipment, NOT a new store. RECORD CONTENT IS NOT SPECIFIED HERE (CYCLE 8, ITEM 7). The observation-record field set, every field's type, and every acceptance rule are defined ONCE, in the plan's canonical Evidence-verification contract (docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md, section "Evidence-verification contract for 160.017-T"). READ THAT CONTRACT; there is no second source and no local list. This record contributes ONLY its exact case set (the twenty-two RED-FIRST cases enumerated above), its destination path, and its log bounds. Records are APPEND-ONLY within the shipment and written ONLY by this task. 160.017-T joins, verifies and rejects per that canonical contract.

BOUNDED RAW LOG - SECOND AUTHORIZED EVIDENCE WRITE (added in review-fix cycle 5, findings 10 and 15; local field reference removed in cycle 8, item 7). Every observation this task records is backed by a RAW LOG this task also writes, into ITS OWN SUBDIRECTORY AND NOWHERE ELSE: docs/audits/2026-09-03-ship10-payload-evidence/observations/logs/T3a/**. This path is authorized in the plan's H2 writable-surface table, so writing it does NOT violate freeze-scope, and 160.017-T can verify the evidence without following a reference outside the single audit directory. WHY THE LOG IS REQUIRED: cycle 4 had 160.017-T accept each producer's SELF-DECLARED outcome, which verifies nothing - a task that never ran would be indistinguishable from a clean pass. The log is the artifact that makes the claim checkable. The log also carries the source-provenance capture the plan's canonical contract requires before the test command runs, and the observation is taken on a CLEAN, FULLY COMMITTED TREE - for this task, the RED HARNESS COMMIT must exist before the RED observation is taken.
BOUNDS (binding, enforced by 160.017-T): PER LOG <= 256 KiB AND <= 2,000 lines - **A LOG EXCEEDING EITHER LIMIT IS REJECTED. ONE RULE, FAIL CLOSED (CYCLE 11, ITEM 6).** THE WITHDRAWN TRUNCATION PATH - truncate head-and-tail, write a truncation marker, and record `truncated: true` with pre-truncation byte and line counts - IS DELETED AND MUST NOT RETURN. It added three extension fields nothing else consumed, self-reported by the producer whose output the bound exists to constrain, and it CONTRADICTED 160.017-T, which now rejects an over-bound log outright - so a producer following the old rule would have truncated compliantly and still failed the shipment. WHAT THIS TASK DOES INSTEAD: keep the run bounded at source - scope the command, or split the observation across the per-case log allowance. IF AN OBSERVATION GENUINELY CANNOT FIT INSIDE THE BOUND, HALT AND REPORT; do not truncate and do not proceed. PER CASE at most 3 logs (first observation, second observation, one retry); PER SHIPMENT at most 200 files under the logs root, none of them binary; RETENTION until 160.017-T completes and the shipment closes.
OBSERVATION-RECORD CONTENT IS DEFINED ONCE, IN THE PLAN, AND IS NOT RESTATED HERE (cycle 6, finding 23; TIGHTENED IN CYCLE 8, ITEM 7). The SINGLE CANONICAL definition is the field table in docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section "Evidence-verification contract for 160.017-T". Seven task records each carried their own restatement and the copies had ALREADY DIVERGED, so a producer reading only its own record would have written a record 160.017-T rejects. THIS RECORD STATES ONLY WHAT IS TASK-SPECIFIC: which cases it produces records for, and which log subdirectory is its own. THIS RECORD ENUMERATES NO FIELD, NAMES NO FIELD, AND DEFINES NO FIELD TYPE OR VALIDATION RULE - NOT EVEN PARTIALLY. Cycle 7 deleted the "ADDITIONAL RECORD FIELDS" restatement but left a second local enumeration standing under a different heading, which was the same defect wearing a different label; cycle 8 deletes that one too. A PARTIAL COPY DRIFTS FROM THE CANONICAL TABLE EXACTLY AS A FULL COPY DOES. FOR EVERY FIELD, ITS TYPE, AND ITS ACCEPTANCE RULE, READ THE PLAN'S CANONICAL TABLE - there is no second source. 160.017-T verifies each record INDEPENDENTLY against its evidence and REJECTS any record that is missing a field, malformed, or inconsistent; it does not warn and does not skip.
This task WRITES its own JSON and its own logs and NOTHING ELSE outside its test files; it MUST NOT write into another producer's subdirectory, and MUST NOT write the ledger.

ACCEPTANCE: all twenty-two RED-FIRST cases above authored and declared; every one observed RED with the failure mode the plan's case table predicts; red evidence recorded per case EXACTLY AS THE PLAN'S CANONICAL EVIDENCE-VERIFICATION CONTRACT REQUIRES (cycle 8, item 7 - the earlier wording here pointed at a local field list that no longer exists and must not be reintroduced); the green-ownership mapping reproduced in the completion note so no case is orphaned; no CHARACTERIZATION case authored here; NO sdist case authored here. CASE-COUNT RECONCILIATION: cycle 4 held the count at TWENTY-FIVE by two offsetting changes - finding 5 REMOVED test_plugin_source_path_is_tracked_and_fetchable to 160.016-T (-1) and finding 9 ADDED test_target_workspace_prefix_derivation_is_centralized (+1). CYCLE 5 moves it to TWENTY-TWO: finding 1 DELETED the four SDIST-WIRING cases (-4) and finding 12 RECLASSIFIED test_wheel_contains_required_runtime_set from CHARACTERIZATION into this harness (+1). 25 - 4 + 1 = 22.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size M, complexity medium - **HELD AT M IN CYCLE 11, ITEM 10, WITH A CONCRETE RECORDED FOLLOW-UP RATHER THAN A SILENT RE-CONFIRMATION.** STATED PLAINLY FIRST, BECAUSE THE EARLIER WORDING UNDERSTATED IT: THIS TASK AUTHORS **TWENTY-TWO** RED-FIRST CASES AND RECORDS **TWENTY-TWO** RED OBSERVATIONS UNDER THE FULL PROVENANCE PROTOCOL (commit, capture `git rev-parse HEAD` and `git status --porcelain=v1`, run, record). That is a LOT OF WORK, IT SITS AT THE TOP OF THE M BAND, AND IT MAY EXCEED THE 2-HOUR HUMAN-EQUIVALENT BOUND THAT WOULD FORCE A SPLIT. It is NOT "a few scenarios" and must not be described as one. WHY IT IS NOT SPLIT IN THIS CYCLE, AND WHY THAT IS NOT A DEFERRAL BY CONVENIENCE: splitting requires CHOOSING A SPLIT BOUNDARY (by acceptance criterion, by shared fixture, or by green owner), RE-DERIVING THE AUTHOR COLUMN FOR TWENTY-TWO LEDGER ROWS, ADDING A DAG NODE AND ITS EDGES, and RECOMPUTING THE RED-FIRST ORDERING PATHS. That is SEPARATE EXECUTION-PLANNING WORK, not a field edit, and cycle 11 was explicitly authorized to add NO new task. **IT IS THEREFORE CAPTURED AS DEFERRED STASH ENTRY `0B83AC8F` (P-021), WITH A NAMED DISPOSITION AND A NAMED RESIDUAL RISK**: either measure the real authoring cost on the first few cases and split this task if it exceeds the bound, or record a MEASURED confirmation that it fits. RESIDUAL RISK IF NEITHER HAPPENS: an over-bound task degrades agent reliability, and this is the RED-FIRST harness on which 34 ordering paths and every downstream green transition depend, so an incomplete red harness silently weakens the whole evidence contract. THE SHIPMENT'S PUBLICATION READINESS IS THEREFORE **READY_WITH_FOLLOWUPS**, CARRYING THAT ID - not READY. WHAT STILL ARGUES FOR M RATHER THAN A LARGER BAND (recorded, not asserted): the case count FELL from twenty-five to twenty-two in the cycle-5 recount, and the MARGINAL cost per case is genuinely low. All twenty-two share TWO fixtures (a built wheel and a resolved plugin payload - the built-sdist fixture is no longer needed), and the great majority fail uniformly at import or missing-artifact rather than needing bespoke setup. The judgement concentrates in four cases - the V4 unclassified-path injection, the structural release.yml parse, the idempotence run-twice harness, and the self-exclusion assertion - which is what holds complexity at medium rather than low. The three recursion-guard cases added in review-fix cycle 3 reuse the existing plugin-payload fixture and do not change the size band.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T3a)
