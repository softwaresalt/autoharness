---
artifact_type: task
created_at: 2026-08-31T23:16:12.9558576Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 154.003-T
id: 154.004-T
parent_id: 154-F
priority: medium
status: queued
title: Close the technology-reviewer to language-instruction dangling reference via the Decision F co-installation invariant
updated_at: 2026-09-03T17:03:25.2178326Z
---

SPLIT from 154.003-T in review-fix cycle 1 for REVERTABILITY: the architecture-rule amendment (154.003-T) constrains every skill in the harness, while this task corrects one install contract. Bundling made the governance amendment unrevertable without also reverting the reference fix. Sequenced AFTER 154.003-T so the manifest refresh lands last.

PREMISE CORRECTION (binding): the earlier claim that the template hardcodes python.instructions.md is FALSE. templates/agents/review/technology-reviewer.agent.md.tmpl:56 reads "Reference the workspace's `{{PRIMARY_LANGUAGE_LOWER}}.instructions.md` as the authoritative style guide" - a GENERIC PLACEHOLDER. The template-to-installed-name mapping is DOCUMENTED, not assumed: .github/skills/install-harness/SKILL.md:1057 states technology instructions install as {language}.instructions.md. The ACTUAL defect is a CO-INSTALLATION GAP - this workspace installed the technology reviewer for PRIMARY_LANGUAGE=Python without the matching instruction, so .github/agents/subagents/python-reviewer.agent.md:68 dangles.

=== VERIFICATION SEAM (BINDING; ADDED IN REVIEW-FIX CYCLE 5, FINDING 4) ===

WITHDRAWN OBSERVABLES: the cycle-3/4 matrix observables phrased as live composition runs - "Composition succeeds", "{L}.instructions.md is present in the composed workspace", "Composition EXITS NON-ZERO", "NO workspace is composed" - are WITHDRAWN AS EXECUTION CONTRACTS and MUST NOT be reintroduced in that form. THEY PRESUME A CALLABLE COMPOSER THAT DOES NOT EXIST. Measured at HEAD: src/autoharness/ contains cli, verify_workspace, backlog_root, schema_contracts, startup_script_contract, detectors/, eval/, gates/ and telemetry/ - there is NO installer, NO composer, and NO entry point that composes a workspace. /install-harness is an AGENT SKILL (.github/skills/install-harness/SKILL.md) that an agent reads and obeys; it is not a program that can be invoked, given a synthetic template set, and observed to exit non-zero. Tests written to the withdrawn wording would have had to build a composer first and then assert against it - proving something about the newly-written fake and nothing about the shipped skill.

ACTUAL SURFACE AND VERIFICATION: this task's product is (1) the COMPOSITION CONTRACT TEXT in .github/skills/install-harness/SKILL.md, and (2) the RENDERED TECHNOLOGY-REVIEWER TEMPLATE and its installed mirror. Both are mechanically verifiable with the EXISTING harness - autoharness.verify_workspace._derive_template_variables + _render_template for rendering, plus direct reads of the template set - following tests/test_ci_template_rendering.py and tests/test_circuit_breaker_policy_contract.py. Every row below is therefore asserted as EITHER a CONTRACT-TEXT assertion (the skill declares the required rule, unambiguously and locatably) OR a TEMPLATE-SET/RENDER assertion (the artifacts the rule resolves over actually have the properties the rule depends on). No row invents an engine.

ATOMIC / FAIL-CLOSED SEMANTICS ARE CLAIMED ONLY AS CONTRACT TEXT: M3's halt is a rule the skill must STATE, because no operation in this task's scope executes and there is no process whose exit status could be observed. If a callable composer is ever built, M3 becomes an executable case at that time - that is a separate, separately-authorized change, not an obligation of this task.

DECISION F (binding, adopted; strategies (a) install-the-file and (b) repoint-to-template-name are both REJECTED with reasons recorded in the plan), RESTATED IN ORCHESTRATOR REVIEW-FIX CYCLE 1 (FINDING 11) TO REMOVE A CONTRADICTION. The earlier wording required the reviewer to be WITHHELD when no language instruction resolved, while simultaneously claiming this repository's reviewer DEGRADES; both could not hold, and with templates/instructions/technology-python.instructions.md.tmpl present the withhold branch would additionally have FORCED creation of python.instructions.md here, contradicting the explicit not-in-scope clause. The contract is THREE NON-OVERLAPPING CONDITIONS, each with exactly ONE disposition.

CONDITION A (PRIMARY_LANGUAGE=L declared AND technology-reviewer selected) -> F1 CO-INSTALLATION: amend .github/skills/install-harness/SKILL.md step 1057 to a MUST - the matching {L}.instructions.md MUST be installed in the same composition, from technology-{L}.instructions.md.tmpl when a variant exists, otherwise from the generic technology.instructions.md.tmpl skeleton; if NEITHER template exists in the template set the composition HALTS WITH A NAMED ERROR identifying both missing templates - it does NOT silently drop the reviewer and does NOT silently install a dangling one. MEASURED: this repository's template set contains technology-{go,python,rust,typescript}.instructions.md.tmpl AND the generic technology.instructions.md.tmpl, so the halt branch is UNREACHABLE here and exists only to fail closed on a broken template set.

CONDITION B (technology-reviewer NOT selected) -> the F1 CO-INSTALLATION RULE DOES NOT FIRE. Condition B is a statement about DECISION F's rule only. It is NOT a statement that no language instruction is installed: install-harness Step 2.2 item 1 installs {language}.instructions.md UNCONDITIONALLY as part of the Instruction Layer, independently of reviewer selection, and Decision F neither adds to nor suppresses that.

CONDITION C (an ALREADY-COMPOSED workspace carries a technology-reviewer with no matching instruction) -> F2 GRACEFUL REFERENCE; THIS REPOSITORY IS CONDITION C.

F1-SCOPE (binding): F1 is FORWARD, AT COMPOSITION TIME, AND NOT RETROACTIVE - it governs what /install-harness does when it composes a workspace, it does not re-compose existing workspaces, and this task runs no composition against this repository. That is what makes Condition C a real ongoing case rather than a transitional one.

F2: keep the {{PRIMARY_LANGUAGE_LOWER}} placeholder (it is correct) and add an explicit fallback clause so a rendered reviewer whose composition ships no language instruction degrades to generic coding-discipline guidance instead of citing an absent file. F2 ALONE CLOSES F0ADCC03 FOR THIS REPOSITORY: re-rendering the mirror at .github/agents/subagents/python-reviewer.agent.md removes the dangling reference WITHOUT creating python.instructions.md.

EXPLICITLY NOT IN SCOPE: no python.instructions.md is created in this repository; recomposing this dogfood workspace under F1 is captured as deferred entry 9B5FD7D5. NO COMPOSER, INSTALLER, OR OTHER NEW PYTHON RUNTIME is authorized by this task. BINDING H7: ship a contract test demonstrated RED BEFORE GREEN asserting the co-installation invariant. INSTALLED PATH NOTE: the installed subagent mirror is .github/agents/subagents/ (docs/compound/096-S-canonical-subagent-install-path.md); templates/agents/review/ is the template source path. SAFETY MODE: careful. Source: F0ADCC03. Plan: docs/plans/2026-08-31-ship4-review-persona-policy-contract-integrity-plan.md

=== BINDING OUTCOME MATRIX (the single authoritative statement of the five distinguishable outcomes) ===

The three conditions above are stated as PROSE. Prose conditions get implemented inconsistently because three of the five distinguishable outcomes (M1, M2 and M3) are buried inside Condition A as sub-branches, and one of those sub-branches (M3) is a fail-closed HALT. The following matrix is BINDING and each row is a REQUIRED TEST CASE. An implementation is not complete until every row has an asserted, MECHANICALLY VERIFIABLE observable. Kind: CONTRACT = assertion over SKILL.md text; SET = assertion over the template set on disk; RENDER = assertion over template output produced by _render_template.

| # | Input condition | Template set state | Required outcome | Kind | Required observable |
|---|---|---|---|---|---|
| M1 | PRIMARY_LANGUAGE=L declared AND technology-reviewer SELECTED | technology-{L}.instructions.md.tmpl EXISTS (LANGUAGE VARIANT) | F1 CO-INSTALLATION from the LANGUAGE-VARIANT template | CONTRACT + SET | SKILL.md states as a MUST that the variant template technology-{L}.instructions.md.tmpl is the FIRST-CHOICE source for {L}.instructions.md when it exists, naming the installed filename; AND the template set is asserted to actually contain the variant for at least one measured language, carrying a MARKER UNIQUE TO THE VARIANT (not merely file existence), so the rule has a real artifact to resolve to. |
| M2 | PRIMARY_LANGUAGE=L declared AND technology-reviewer SELECTED | technology-{L}.instructions.md.tmpl ABSENT, generic technology.instructions.md.tmpl PRESENT | F1 CO-INSTALLATION from the GENERIC SKELETON | CONTRACT + SET | SKILL.md states the ORDERED fallback explicitly - variant first, generic skeleton ONLY when no variant exists - in a form where the ordering is locatable, so a reader cannot implement generic-always or variant-always; AND the generic skeleton is asserted present and carrying a MARKER UNIQUE TO THE GENERIC template, distinct from every variant marker. The assertion MUST reference a language with NO variant in the measured set; naming python would make M2 indistinguishable from M1. |
| M3 | PRIMARY_LANGUAGE=L declared AND technology-reviewer SELECTED | NEITHER technology-{L}.instructions.md.tmpl NOR technology.instructions.md.tmpl present | HALT WITH A NAMED ERROR - fail closed | CONTRACT | SKILL.md states that when NEITHER template resolves the composition HALTS with a NAMED error identifying BOTH missing template paths, and states BOTH NEGATIVES explicitly - the reviewer is NOT silently dropped AND NOT installed dangling. All three clauses (halt, both paths named, both negatives) are separately asserted, because text asserting only "halt" cannot distinguish a fail-closed rule from a rule that halts after already writing a partial workspace. NO synthetic template set and NO exit-code observation is required or permitted here: nothing executes. |
| M4 | technology-reviewer NOT SELECTED | any | THE F1 CO-INSTALLATION RULE DOES NOT FIRE - no-op for Decision F | CONTRACT | SKILL.md scopes the F1 rule EXPLICITLY to compositions that select the technology reviewer, and states EXPLICITLY that F1 neither requires nor suppresses the Instruction Layer's own unconditional {language}.instructions.md install (Step 2.2 item 1). The scoping clause MUST be attributed to the F1 rule by name, NEVER expressed as a claim about the presence or absence of {L}.instructions.md itself. |
| M5 | ALREADY-COMPOSED workspace carrying a technology reviewer with NO matching instruction (CONDITION C - THIS REPOSITORY) | n/a (no composition runs) | F2 GRACEFUL REFERENCE | RENDER | Render templates/agents/review/technology-reviewer.agent.md.tmpl with PRIMARY_LANGUAGE_LOWER=python via _derive_template_variables + _render_template and assert the output degrades to generic coding-discipline guidance and contains NO unconditional reference to .github/instructions/python.instructions.md; assert the installed mirror .github/agents/subagents/python-reviewer.agent.md agrees with that render; AND assert .github/instructions/python.instructions.md IS STILL ABSENT. Together these prove F2 rather than an accidental slide into rejected strategy (a). THIS IS THE ONE ROW WITH A REAL EXECUTABLE OBSERVABLE, and it is the row that closes F0ADCC03. |

M4 SCOPE (binding, corrected in review-fix cycle 3, finding 2 - THIS IS THE ONLY M4 OBSERVABLE; the earlier "assert the file's ABSENCE" wording is WITHDRAWN and MUST NOT be reintroduced). The withdrawn wording required M4 to assert that {L}.instructions.md is GLOBALLY ABSENT when the technology reviewer is not selected. That is factually wrong and would break the harness: .github/skills/install-harness/SKILL.md Step 2.2 item 1 - "Technology instructions ({language}.instructions.md)" - installs the primary-language instruction UNCONDITIONALLY as part of the INSTRUCTION LAYER, for every composition that declares a PRIMARY_LANGUAGE, with no dependence on reviewer selection. Under the withdrawn wording a CORRECT composition would FAIL M4, and the only way to pass would be to SUPPRESS a file the Instruction Layer independently requires - turning a scoping test into a regression generator. M4 therefore isolates ONLY the rule attributed to F1, and it MUST NOT constrain, gate, or suppress independently-required primary-language instruction generation.

M4 DISCRIMINATING POWER IS PRESERVED WITHOUT THE ABSENCE ASSERTION, AND SURVIVES THE CYCLE-5 SEAM CORRECTION. The failure M4 exists to catch is a rule that fires F1 UNCONDITIONALLY. Cycle 3 detected that by RUNTIME ATTRIBUTION (no F1 install unit recorded); with no composer to record install units, the equivalent and equally discriminating observable is CONTRACT ATTRIBUTION: the F1 rule must be written with its selection precondition attached to it, and must carry the explicit non-interference clause about Step 2.2 item 1. A SKILL.md that states co-installation as an unconditional rule FAILS this row exactly as an unconditional implementation would have. Note the substitution honestly: this is a weaker guarantee than an executed attribution check, and it is the strongest one available on a text-only surface.

M1 and M2 are the two branches the cycle-1 prose collapsed into one clause ("from technology-{L}.instructions.md.tmpl when a variant exists, otherwise from the generic skeleton"); they need SEPARATE cases with DISTINGUISHING markers, because the failure mode is a resolver that always picks one branch, which a shared assertion cannot detect. M3 is the fail-closed branch. M4 is the reviewer-not-selected row. M5 is this repository's live condition and the only executable one.

F1-SCOPE REMAINS BINDING ACROSS THE MATRIX: M1-M4 describe COMPOSITION-TIME behaviour and are NOT retroactive; no row re-composes an existing workspace, and no row runs a composition against THIS repository. M5 is reached by re-rendering the mirror only.

=== APPROVAL-LANGUAGE CONFORMANCE ===

This task issues no destructive restore and consults no approval signal, so Decision G does not gate it. Recorded here only so the shipment-wide rule is unambiguous: anywhere in SHIP-4 that an executable instruction gates a destructive action, the gate is Decision G1's FRESH, LIVE, NON-SYNTHESIZABLE OPERATOR APPROVAL obtained in the session performing the action. An APPROVAL: P-007-ARCHIVE-RESTORE backlog comment is AUDIT EVIDENCE ONLY - written after a G1 result is obtained, never consulted as authorization, and never sufficient on its own, because the gated party can author it and then read its own writing.

ACCEPTANCE: F1, F1-SCOPE and F2 all landed; every matrix row M1-M5 covered by a test case with its stated observable and its declared kind (CONTRACT / SET / RENDER); contract test observed red then green; dogfood mirror + manifest checksums refreshed; PYTHONPATH=src python -m unittest discover -s tests green; verify-harness passes. NO acceptance criterion asserts a composition exit code or a composed-workspace filesystem state, because no composer exists to produce one. NEGATIVE ASSERTION (mandatory, M5): after re-render, assert that .github/agents/subagents/python-reviewer.agent.md contains NO unconditional reference to a non-existent .github/instructions/python.instructions.md, AND assert that .github/instructions/python.instructions.md is STILL ABSENT. Both assertions together prove the fix was F2's graceful reference rather than an accidental slide into rejected strategy (a). This M5-scoped absence assertion is about THIS ALREADY-COMPOSED REPOSITORY, where no composition runs and nothing installs the file; it is NOT a general rule about composition-time behaviour and must not be read across to M4.

COUNT CORRECTION (extended review-fix cycle 4, finding 14 - tightly-coupled prose; RE-VERIFIED IN CYCLE 5): the matrix header and its lead-in previously read "four distinguishable outcomes" and "two of the four ... buried inside Condition A" while the matrix itself enumerates FIVE rows (M1-M5) of which THREE (M1, M2, M3) sit under Condition A. The counts are FIVE and THREE respectively and are correct as written above. The cycle-5 seam correction changed only the KIND and OBSERVABLE columns: no row, input condition, or required outcome is added, removed or altered, and the required-test-case set remains exactly M1, M2, M3, M4, M5.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size S, complexity low. RE-CONFIRMED under the cycle-5 seam correction: four of the five rows become locatable-text assertions over one SKILL.md section plus template-set reads, and the fifth uses the existing render helpers with an in-repo worked example. This is strictly less work and less uncertainty than the withdrawn framing, which would have required building a composer before a single row could be asserted.