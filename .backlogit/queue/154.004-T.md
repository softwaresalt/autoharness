---
artifact_type: task
created_at: 2026-08-31T23:16:12.9558576Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 154.003-T
id: 154.004-T
parent_id: 154-F
priority: medium
status: queued
title: Close the technology-reviewer to language-instruction dangling reference via the Decision F co-installation invariant
updated_at: 2026-09-03T10:36:52.1082715Z
---

SPLIT from 154.003-T in review-fix cycle 1 for REVERTABILITY: the architecture-rule amendment (154.003-T) constrains every skill in the harness, while this task corrects one install contract. Bundling made the governance amendment unrevertable without also reverting the reference fix. Sequenced AFTER 154.003-T so the manifest refresh lands last. PREMISE CORRECTION (binding): the earlier claim that the template hardcodes python.instructions.md is FALSE. templates/agents/review/technology-reviewer.agent.md.tmpl:56 reads "Reference the workspace's `{{PRIMARY_LANGUAGE_LOWER}}.instructions.md` as the authoritative style guide" - a GENERIC PLACEHOLDER. The template-to-installed-name mapping is DOCUMENTED, not assumed: .github/skills/install-harness/SKILL.md:1057 states technology instructions install as {language}.instructions.md. The ACTUAL defect is a CO-INSTALLATION GAP - this workspace installed the technology reviewer for PRIMARY_LANGUAGE=Python without the matching instruction, so .github/agents/subagents/python-reviewer.agent.md:68 dangles. DECISION F (binding, adopted; strategies (a) install-the-file and (b) repoint-to-template-name are both REJECTED with reasons recorded in the plan). DECISION F RESTATED IN ORCHESTRATOR REVIEW-FIX CYCLE 1 (FINDING 11) TO REMOVE A CONTRADICTION. The earlier wording required the reviewer to be WITHHELD when no language instruction resolved, while simultaneously claiming this repository's reviewer DEGRADES; both could not hold, and with templates/instructions/technology-python.instructions.md.tmpl present the withhold branch would additionally have FORCED creation of python.instructions.md here, contradicting the explicit not-in-scope clause. The contract is now THREE NON-OVERLAPPING CONDITIONS, each with exactly ONE disposition. CONDITION A (PRIMARY_LANGUAGE=L declared AND technology-reviewer selected) -> F1 CO-INSTALLATION: amend .github/skills/install-harness/SKILL.md step 1057 to a MUST - the matching {L}.instructions.md MUST be installed in the same composition, from technology-{L}.instructions.md.tmpl when a variant exists, otherwise from the generic technology.instructions.md.tmpl skeleton; if NEITHER template exists in the template set the composition HALTS WITH A NAMED ERROR identifying both missing templates - it does NOT silently drop the reviewer and does NOT silently install a dangling one. MEASURED: this repository's template set contains technology-{go,python,rust,typescript}.instructions.md.tmpl AND the generic technology.instructions.md.tmpl, so the halt branch is UNREACHABLE here and exists only to fail closed on a broken template set. CONDITION B (technology-reviewer NOT selected) -> nothing to install, nothing to reference; outside Decision F. CONDITION C (an ALREADY-COMPOSED workspace carries a technology-reviewer with no matching instruction) -> F2 GRACEFUL REFERENCE; THIS REPOSITORY IS CONDITION C. F1-SCOPE (binding): F1 is FORWARD, AT COMPOSITION TIME, AND NOT RETROACTIVE - it governs what /install-harness does when it composes a workspace, it does not re-compose existing workspaces, and this task runs no composition against this repository. That is what makes Condition C a real ongoing case rather than a transitional one. F2: keep the {{PRIMARY_LANGUAGE_LOWER}} placeholder (it is correct) and add an explicit fallback clause so a rendered reviewer whose composition ships no language instruction degrades to generic coding-discipline guidance instead of citing an absent file. F2 ALONE CLOSES F0ADCC03 FOR THIS REPOSITORY: re-rendering the mirror at .github/agents/subagents/python-reviewer.agent.md removes the dangling reference WITHOUT creating python.instructions.md. EXPLICITLY NOT IN SCOPE: no python.instructions.md is created in this repository; recomposing this dogfood workspace under F1 is captured as deferred entry 9B5FD7D5. BINDING H7: ship a contract test demonstrated RED BEFORE GREEN asserting the co-installation invariant. ACCEPTANCE: F1, F1-SCOPE and F2 all landed; contract test observed red then green; dogfood mirror + manifest checksums refreshed; verify-harness passes. NEGATIVE ASSERTION (mandatory, added in review-fix cycle 1): after re-render, assert that .github/agents/subagents/python-reviewer.agent.md contains NO unconditional reference to a non-existent .github/instructions/python.instructions.md, AND assert that .github/instructions/python.instructions.md is STILL ABSENT. Both assertions together prove the fix was F2's graceful reference rather than an accidental slide into rejected strategy (a). INSTALLED PATH NOTE: the installed subagent mirror is .github/agents/subagents/ (docs/compound/096-S-canonical-subagent-install-path.md); templates/agents/review/ is the template source path. SAFETY MODE: careful. Source: F0ADCC03. Plan: docs/plans/2026-08-31-ship4-review-persona-policy-contract-integrity-plan.md

=== EXPLICIT OUTCOME MATRIX (ADDED IN REVIEW-FIX CYCLE 2, FINDING 10) ===

The three conditions above are stated as PROSE. Prose conditions get implemented inconsistently because two of the four distinguishable outcomes are buried inside Condition A as sub-branches, and one of those sub-branches is a HALT. The following matrix is BINDING and each row is a REQUIRED TEST CASE. An implementation is not complete until every row has an asserted observable.

| # | Input condition | Template set state | Required outcome | Required observable |
|---|---|---|---|---|
| M1 | PRIMARY_LANGUAGE=L declared AND technology-reviewer SELECTED | technology-{L}.instructions.md.tmpl EXISTS (LANGUAGE VARIANT) | F1 CO-INSTALLATION from the LANGUAGE-VARIANT template | Composition succeeds; {L}.instructions.md is present in the composed workspace; its content derives from technology-{L}.instructions.md.tmpl (assert a marker unique to the variant, not merely file existence); the rendered technology reviewer's {{PRIMARY_LANGUAGE_LOWER}} reference RESOLVES to that file. |
| M2 | PRIMARY_LANGUAGE=L declared AND technology-reviewer SELECTED | technology-{L}.instructions.md.tmpl ABSENT, generic technology.instructions.md.tmpl PRESENT | F1 CO-INSTALLATION from the GENERIC SKELETON | Composition succeeds; {L}.instructions.md is present and derives from the GENERIC skeleton (assert a marker unique to the generic template, so a silent fall-through to M1 cannot pass this row); reviewer reference resolves. This row must be exercised with a language having NO variant - asserting it with python would silently test M1. |
| M3 | PRIMARY_LANGUAGE=L declared AND technology-reviewer SELECTED | NEITHER technology-{L}.instructions.md.tmpl NOR technology.instructions.md.tmpl present | HALT WITH A NAMED ERROR - fail closed | Composition EXITS NON-ZERO; the error names BOTH missing template paths; NO workspace is composed; the reviewer is NOT silently dropped AND NOT installed dangling. Assert BOTH negatives explicitly - a test asserting only the non-zero exit cannot tell a fail-closed halt from a crash that left a half-composed workspace. Requires a synthetic template set with both templates removed; MEASURED: this repository's real template set contains technology-{go,python,rust,typescript}.instructions.md.tmpl AND the generic skeleton, so this row is UNREACHABLE against the real set and MUST be driven from a synthetic fixture. |
| M4 | technology-reviewer NOT SELECTED | any | NO-OP - outside Decision F | NO {L}.instructions.md is installed by this rule; NO technology reviewer is rendered; composition succeeds. Assert the file's ABSENCE, not merely that no error occurred - an implementation that installs the instruction unconditionally would pass a success-only assertion while shipping a file nothing references. |
| M5 | ALREADY-COMPOSED workspace carrying a technology reviewer with NO matching instruction (CONDITION C - THIS REPOSITORY) | n/a (no composition runs) | F2 GRACEFUL REFERENCE | Re-rendered reviewer degrades to generic coding-discipline guidance; contains NO unconditional reference to the absent .github/instructions/python.instructions.md; AND .github/instructions/python.instructions.md IS STILL ABSENT. Both assertions together prove F2 rather than an accidental slide into rejected strategy (a). |

M1 and M2 are the two branches the cycle-1 prose collapsed into one clause ("from technology-{L}.instructions.md.tmpl when a variant exists, otherwise from the generic skeleton"); they need SEPARATE cases with DISTINGUISHING markers, because the failure mode is a resolver that always picks one branch, which a shared assertion cannot detect. M3 is the fail-closed branch and is the only row that must be driven from a synthetic template set. M4 is the reviewer-not-selected row, which cycle 1 mentioned only as "outside Decision F" with no asserted observable - an unasserted no-op is indistinguishable from an unimplemented one. M5 is this repository's live condition.

F1-SCOPE REMAINS BINDING ACROSS THE MATRIX: M1-M4 are COMPOSITION-TIME behaviours and are NOT retroactive; no row re-composes an existing workspace, and no row runs a composition against THIS repository. M5 is reached by re-rendering the mirror only.

=== APPROVAL-LANGUAGE CONFORMANCE (CYCLE 2, FINDING 10) ===

This task issues no destructive restore and consults no approval signal, so Decision G does not gate it. Recorded here only so the shipment-wide rule is unambiguous: anywhere in SHIP-4 that an executable instruction gates a destructive action, the gate is Decision G1's FRESH, LIVE, NON-SYNTHESIZABLE OPERATOR APPROVAL obtained in the session performing the action. An APPROVAL: P-007-ARCHIVE-RESTORE backlog comment is AUDIT EVIDENCE ONLY - written after a G1 result is obtained, never consulted as authorization, and never sufficient on its own, because the gated party can author it and then read its own writing.