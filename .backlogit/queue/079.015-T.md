---
artifact_type: task
created_at: 2026-07-14T17:49:40.8121115-07:00
dependencies:
    - 079.013-T
    - 079.016-T
id: 079.015-T
parent_id: 079-F
priority: medium
status: queued
title: Wiring Ship lifecycle telemetry begin/record handoff
updated_at: 2026-07-14T18:31:00.000-07:00
---

Wire the real Ship execution lifecycle to use the stable begin context from
079.014-T, the record/idempotency close path from 079.016-T, and the sizing
snapshot producer from 079.013-T. This task prevents the telemetry contract from
passing via adapter-only unit tests by proving the actual host surface captures
before implementation and records with the same context at close.

<!-- BEGIN:acceptance-criteria -->
- The generated Ship lifecycle template `templates/agents/.ship.agent.md.tmpl` and the dogfood installed mirror `.github/agents/.ship.agent.md` invoke `autoharness telemetry begin` immediately after a task is claimed and before pre-build knowledge retrieval, build-feature delegation, implementation tool work, or review feedback can occur.
- The Ship task loop carries the returned `context_ref`/stable `epoch_id` through build execution and passes the same context to `autoharness telemetry record --context-ref` at task close before or as the task is marked done; the close path never re-reads backlogit size, hierarchy, or shipment membership.
- Ship tolerates the structured disabled/no-op result from `telemetry begin` when telemetry is disabled: it skips context carry/record close for that task without failing the lifecycle or creating telemetry artifacts.
- A deterministic workflow/contract test simulates the Ship lifecycle and proves the same pre-execution context appears in the persisted SQLite and JSONL `ExecutionEpoch` records after task close.
- The workflow test mutates backlogit size or shipment membership after `telemetry begin` and before `telemetry record`; the persisted epoch still contains the original pre-execution `WorkSizingSnapshot` and stable `epoch_id`.
- If `build-feature` must receive the context reference explicitly, update the skill template and installed mirror in the same parity-preserving change; if that exceeds the 2-hour/file budget, split before implementation rather than weakening the handoff.
- Dogfood/template parity and any harness-manifest checksum or installed-artifact tracking implications are updated and tested so generated Ship behavior and the installed autoharness dogfood mirror cannot drift.
- Tests assert 092-S cannot be treated as execution-ready while this task or 079.013-T remains incomplete, and the shipment manifest still contains child task IDs only. Generated Ship intake resolves the covering feature through each task's `parent_id` and accepts task-only manifests instead of requiring parent feature membership. This task is dependency-ordered through `dependencies`, not `blocked-on-external`; the external release is represented by 079.013-T.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Concrete integration target from source inspection: `templates/agents/.ship.agent.md.tmpl`
Step 4.1/4.5 (claim, execute, complete task) and
`.github/agents/.ship.agent.md` Step 2 (claim/execute/complete). Keep
implementation scoped to host lifecycle wiring and deterministic contract tests;
do not alter backlogit source or implement broader dependency-aware claim guards
in this task.
<!-- END:implementation-notes -->
