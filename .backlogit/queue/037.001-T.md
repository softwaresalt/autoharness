---
artifact_type: task
created_at: 2026-05-17T15:10:35.1683882-07:00
id: 037.001-T
labels:
    - agent-intercom
    - metadata
    - dogfood
parent_id: 037-F
priority: medium
status: queued
title: 'IC-A: Remediate agent-intercom metadata drift — workspace-profile config_paths + harness-manifest verification check'
updated_at: 2026-05-17T15:10:35.1683882-07:00
---

## What

Correct two stale references that point to `.vscode/mcp.json` when the actual MCP configuration is at `.mcp.json` (workspace root).

### `.autoharness/workspace-profile.yaml`
- Change `agent_intercom.config_paths` from `[".vscode/mcp.json"]` to `[".mcp.json"]`
- Add `.cursor/mcp.json` to `agent_intercom.config_paths` for environment-agnostic completeness (AGENTS.md Rule 3)
- Add `<!-- intercom:start -->` and `<!-- intercom:end -->` to `agent_intercom.instruction_markers` if the copilot-instructions.md block is present

### `.autoharness/harness-manifest.yaml`
- Update the `pack: "agent-intercom"` overlay entry `verification_checks` to reference `.mcp.json` instead of `.vscode/mcp.json`
- Add a check: `"agent-intercom.instructions.md installed at .github/instructions/"` (currently absent — the instruction file is not yet installed)

## File

`.autoharness/workspace-profile.yaml`, `.autoharness/harness-manifest.yaml`

## Acceptance Criteria

- `agent_intercom.config_paths` in workspace-profile.yaml includes `.mcp.json` (not only `.vscode/mcp.json`)
- `harness-manifest.yaml` intercom overlay verification_checks reference `.mcp.json`
- `harness-manifest.yaml` intercom overlay verification_checks include a check for the instruction file
- Existing `agent_intercom.detected: true` and `recommended: true` values are preserved

## Constraint

Two related metadata files, same session. ~20–30 min. No code changes.

## Dependency

None. This is the first IC task and unblocks IC-B.