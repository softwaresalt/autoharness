---
artifact_type: task
created_at: 2026-08-31T22:18:28.8445039Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 157.001-T
parent_id: 157-F
priority: medium
status: queued
title: Regenerate the installed backlogit registry from template and verify the delta is purely additive
updated_at: 2026-09-03T17:07:42.3846405Z
---

.autoharness/backlog-registry.yaml is 269 lines against its 460-line template (both re-measured at HEAD in review-fix cycle 5).

=== ADDITIVE INVENTORY, RECOMPUTED IN REVIEW-FIX CYCLE 5 (FINDING 6) ===

WITHDRAWN INVENTORY: the "22 declared operations" list is WITHDRAWN. It was INCOMPLETE - it OMITTED stash_remove - and an inventory that is silently short by one is worse than none, because it presents itself as exhaustive and an implementer regenerating against it would treat the extra operation as an unexplained surprise (or, if they hand-filtered to the list, would reproduce the drift this task exists to remove). The corrected delta below was recomputed mechanically by loading BOTH YAML files and taking the key-set difference, not by reading prose.

MEASURED DELTA AT HEAD (source template templates/backlog/registries/backlogit.registry.yaml vs installed .autoharness/backlog-registry.yaml):

OPERATIONS: source declares 54; installed declares 31; ADDITIVE = 23; INSTALLED-ONLY = 0 (nothing in the installed registry is absent from the template, so nothing is at risk of being dropped by regeneration).

THE 23 ADDITIVE OPERATIONS, IN FULL:
  add_link, adopt_item, archive_item, cleanup_checkpoints, deliberate, doctor, export_command_map, fetch_stash, get_links, get_metadata_catalog, get_version, get_wit_metadata, harvest_stash, list_templates, list_types, merge_sync, remove_link, stash, stash_archive, stash_edit, stash_get, stash_remove, telemetry_harvest.

THE PREVIOUSLY-OMITTED OPERATION: stash_remove (mcp_tool: backlogit_stash_remove, params: {stash_id}). It is DECLARED IN THE TEMPLATE and ABSENT FROM THE INSTALLED REGISTRY, so it IS part of the additive delta and regeneration WILL introduce it.

DEPRECATION DISPOSITION (explicit, binding - do NOT leave this implicit):
  stash_remove IS DEPRECATED IN FAVOUR OF stash_archive. The live MCP tool surface states it directly - backlogit_stash_remove is described as "[Deprecated: use backlogit_stash_archive] Remove an active stash entry" - and the canonical non-destructive operation is stash_archive (cli_command: backlogit stash archive {{stash_id}}), whose CLI `remove` alias resolves to the same archive handler rather than a separate destructive delete.
  THE SOURCE REGISTRY TEMPLATE CARRIES NO DEPRECATION MARKER FOR IT. Measured: the template's stash_remove entry has only mcp_tool and params - no `deprecated` key, no superseded-by pointer, no note. The deprecation is therefore visible ONLY on the tool surface and NOT in the artifact this task regenerates. RECORD THAT GAP AS A FINDING.
  DECISION - PARITY INCLUDES IT: regeneration INSTALLS stash_remove along with the other 22. Rationale: (i) H1 forbids hand-merging, and hand-filtering one operation out of a regenerated file IS a hand-merge, reintroducing exactly the drift class this shipment removes; (ii) parity means the installed registry MATCHES its template - a deliberate one-key divergence is new drift, differing from the old drift only in being intentional; (iii) a deprecated-but-declared operation is HARMLESS when present and merely unused, whereas suppressing it makes the installed file unreproducible from its own template. Deciding to EXCLUDE it would require first adding a deprecation marker to the TEMPLATE and teaching regeneration to honour it - a template/tooling change on a different surface, out of scope here.
  CONSUMER GUIDANCE (record alongside the regeneration, do not enforce here): agents SHOULD prefer stash_archive; stash_remove is present for registry parity only. Adding a machine-readable deprecation marker to the template and a regeneration rule that honours it is a P-021 CAPTURE, not work for this task.

FIELD MAP: the entire sizing field map (size, complexity, size_source, size_ruleset_version) is absent from BOTH update_task.params and field_mapping in the installed registry.

FEATURES: source declares 22; installed declares 15; ADDITIVE = 7; INSTALLED-ONLY = 0. THE 7: deliberation, discovery, lifecycle_hygiene, semantic_links, sizing, stash, telemetry_harvest.

NO UNKNOWN ADDITIVE CHANGE IS PERMITTED (binding): the diff required by PLAN REVIEW FINDING 1 below must reconcile EXACTLY against these counts - 23 operations, 7 features, 0 installed-only, plus the sizing field map. Any operation, feature, or field present in the re-derived diff and NOT named above is an UNKNOWN ADDITIVE CHANGE: HALT, record it, and do not regenerate until it is accounted for. A count mismatch in either direction is a halt, not a rounding difference.

=== REST OF THE CONTRACT (unchanged) ===

BINDING H1: REGENERATE from the template - never hand-merge, which reproduces the drift class.

CYCLE-2 ADDITION - MANIFEST CHECKSUM COUPLING (P0 BLOCKER, MANDATORY ACCEPTANCE): .autoharness/backlog-registry.yaml is a MANIFEST-TRACKED artifact, so regenerating it CHANGES ITS CONTENT AND THEREFORE ITS CHECKSUM. This task MUST, in the SAME unit of work as the regeneration: (a) REFRESH the recorded checksum for .autoharness/backlog-registry.yaml in .autoharness/harness-manifest.yaml through the harness's own manifest-refresh path, never by hand-computing and pasting a digest; (b) VERIFY THE COUPLING AFTERWARDS by re-reading the manifest entry and confirming it matches the regenerated file on disk; and (c) run verify-harness and confirm it passes, because a regenerated registry with a stale manifest checksum is exactly a manifest-integrity failure and would leave the workspace in a state where every subsequent verify run fails. Ordering is strict: regenerate -> refresh checksum -> verify coupling -> verify-harness. A regeneration whose checksum refresh is deferred to a later task is NOT acceptable and MUST NOT be split out; the two are one atomic change. Record the before/after checksum pair in the task's evidence so the refresh is auditable rather than asserted. NOTE the same coupling is the reason SHIP-3's task 3 and SHIP-5's 155.004-T also refresh checksums in-shipment; this is the identical failure mode, and this task is the one flagged as a CURRENT P0 blocker because the registry regeneration is unconditional.

PLAN REVIEW FINDING 1 (P0 - HARD PRECONDITION): first produce and RECORD a full key-level diff, classify every delta as additive / value-changed / removed, and HALT on any value-changed or removed key rather than proceeding - blind regeneration could silently destroy legitimate workspace-local customization. Only a purely-additive delta may be applied automatically. The measured delta for this workspace is additive-only (0 installed-only operations, 0 installed-only features), but this task must RE-DERIVE that rather than trust the plan, and must reconcile it against the exact inventory above.

PLAN REVIEW FINDING 2 (P1): the seven flags describe TOOL CAPABILITIES, not grants of authority; P-010 role boundaries and Constitution VII destructive-command approval are unchanged - record explicitly that no policy, role, or approval surface is modified.

PLAN REVIEW FINDING 4 (P1): features.sizing becoming visible changes generated Stage agents' documented behaviour on next tune, so update the compatibility matrix and the operating-model doc to state that sizing is advertised and what the enforcement consequence is.

BINDING H4: this lands BEFORE SHIP-8 sizing enforcement, encoded as a SHIPMENT-level blocks edge (166-S blocked by 165-S); SHIP-8's 158.001-T treats the ADVERTISED features.sizing flag as the sole authoritative gate signal, so until this task runs, SHIP-8's fail-closed branch is correctly inert in this workspace.

ACCEPTANCE: purely-additive key-level diff recorded and re-derived, reconciling EXACTLY to 23 additive operations (stash_remove included and named), 7 additive features, 0 installed-only keys, and the sizing field map, with any unaccounted key halting the task; the deprecation disposition for stash_remove recorded, including the finding that the template carries no deprecation marker; registry regenerated from template with no hand-filtering of any key; manifest checksum for the registry refreshed through the official path and the coupling re-verified by read-back; verify-harness passes; compatibility matrix and operating-model doc updated; no policy/role/approval surface modified.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size M, complexity medium. RE-CONFIRMED under the cycle-5 recomputation: one more operation in an already-enumerated additive set plus a recorded deprecation disposition; no new surface, no new tool, no new failure domain. SAFETY MODE: careful + freeze-scope bounded to .autoharness/backlog-registry.yaml and its .autoharness/harness-manifest.yaml checksum entry (SHIP-7 plan H9), because the registry is the resolution substrate for every generated agent and H2 requires any non-additive delta to halt rather than be absorbed. Source: 2E67938C enabling condition. Plan: docs/plans/2026-08-31-ship7-backlog-registry-parity-restoration-plan.md