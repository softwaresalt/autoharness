---
artifact_type: task
created_at: 2026-08-31T22:16:24.3171986Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 154.003-T
parent_id: 154-F
priority: medium
status: queued
title: Resolve the leaf-executor contradiction with a bounded one-hop review-family exception and ship the bounded depth-constraint verifier
updated_at: 2026-09-03T09:47:58.4798377Z
---

RE-SCOPED in review-fix cycle 1: the python-reviewer dangling-reference half is SPLIT OUT to 154.004-T for REVERTABILITY. This task now covers the architecture-rule amendment ONLY, plus the bounded verifier that makes it real. Cycle 0's finding 4 (which accepted the bundling on manifest-churn grounds) is REVERSED: the two halves have different blast radii - one amends a governance rule constraining every skill in the harness, the other corrects one install contract - and bundling made the governance amendment unrevertable without also reverting the reference fix. PROBLEM (7628C291): harness-architecture.instructions.md L163 and role-enforcement.instructions.md L81 forbid skills from spawning subagents, but templates/skills/review/SKILL.md.tmpl L33-35 declares its own Subagent Depth Constraint and L159 spawns five always-on personas. D4/H3: amend the two INSTRUCTION artifacts with a BOUNDED one-hop review-family exception. AUTHORING-SURFACE CORRECTION (Orchestrator review-fix cycle 1, tightly-coupled to finding 10): the two artifacts DO NOT have the same authoring surface and must not be edited as if they did. (a) role-enforcement.instructions.md IS template-backed - its manifest entry names template "instructions/role-enforcement.instructions.md.tmpl" - so amend templates/instructions/role-enforcement.instructions.md.tmpl AND re-render its installed mirror at .github/instructions/role-enforcement.instructions.md. (b) harness-architecture.instructions.md is NOT template-backed - it is the SOLE manifest entry whose template field reads "global instruction definition", and templates/instructions/harness-architecture.instructions.md.tmpl DOES NOT EXIST - so amend .github/instructions/harness-architecture.instructions.md DIRECTLY and refresh its manifest checksum. Do NOT create a .tmpl for it; inventing a template source would fork the artifact from its declared install contract. Both checksums are refreshed either way. do NOT change skill behaviour (making the skills conform would delete the multi-persona adversarial review capability the whole pipeline depends on). DECISION H (binding, cycle 1) - the exception is a BOUNDED VERIFIER, not a claim. Cycle 0 called the exception "machine-checkable" while it was self-declared prose that nothing read; that OVERCLAIM is withdrawn. H-a: state the exception as a PROPERTY with fixed checkable form - a skill qualifies iff its SKILL.md contains a "## Subagent Depth Constraint" section whose body states (i) max depth 1 hop and (ii) that spawned subagents are leaf executors that MUST NOT spawn further. Current members (plan-review, review) are EXAMPLES, never the definition (this is what defeats the maintenance trap in cycle-0 finding 3). H-b MANDATORY ACCEPTANCE - ship ONE test in the EXISTING tests/ suite over .github/skills/*/SKILL.md and templates/skills/*/SKILL.md.tmpl asserting exactly THREE properties: (1) UNDECLARED SPAWNING - any skill whose text spawns a subagent without a conforming constraint section is a FAILURE (this is the detector the original wording promised and did not deliver); (2) INVALID CONSTRAINT FORM - a constraint section missing either the depth bound or the leaf-executor clause is a FAILURE; (3) INVALID DEPTH - a declared depth other than 1 is a FAILURE (depth >1 is exactly the unbounded nesting H3 forbids). H-c BOUNDED BY CONSTRUCTION - this is NOT a new runtime framework: one test in the existing unittest suite performing STATIC TEXT INSPECTION of SKILL.md files only. NO runtime interception, NO spawn-time enforcement, NO new CLI surface, NO new package, NO agent-runtime hook. H-d RESIDUAL LIMITATION MUST BE WRITTEN INTO THE AMENDED INSTRUCTION TEXT: the verifier checks DECLARATIONS IN SKILL TEXT; an agent that spawns at runtime without it appearing in SKILL.md is NOT detected; enforcement is at the DOCUMENT LAYER ONLY. Runtime enforcement is out of scope (A4DAC571). BINDING H7: the verifier is demonstrated RED BEFORE GREEN. P-013.5 model inheritance is UNCHANGED - persona subagents declare no model_family/model_provider/reasoning_effort and still inherit the invoking agent's route. ACCEPTANCE: both instruction templates + dogfood mirrors amended; exception stated as a property with examples; three-property verifier shipped and observed red then green; H-d limitation present verbatim in the amended instruction text; manifest checksums refreshed; verify-harness passes. SAFETY MODE: careful. Source: 7628C291. Plan: docs/plans/2026-08-31-ship4-review-persona-policy-contract-integrity-plan.md