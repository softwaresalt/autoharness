---
artifact_type: task
created_at: 2026-08-21T00:44:53.812203Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 137.002-T
labels:
    - docs
    - templates
    - maintenance-contract
parent_id: 137-F
priority: medium
references:
    - docs/decisions/2026-08-20-template-dogfood-render-parity-spike.md
    - docs/plans/2026-08-20-template-dogfood-paired-edit-contract-plan.md
status: queued
title: Author the template/dogfood paired-edit maintenance contract document
updated_at: 2026-08-21T04:57:39.6822857Z
---

Author a durable document that states the template/dogfood maintenance contract which currently exists only as undocumented practice.

MUST CONTAIN
1. The two categories: MECHANICALLY RENDERED (byte-identical through verify_workspace._render_template) and PAIRED-EDIT MAINTAINED.
2. The current inventory, listing all eight pairs explicitly:
   - Mechanically rendered (4): role-enforcement, circuit-breaker, copilot-code-review, feature-flow-dark.
   - Paired-edit maintained (4): _ship, _stage, _orchestrator agents and github-pr-automation.instructions.
3. The per-pair CAUSE TAXONOMY from spike finding F4, citing the measured evidence rather than restating it from memory:
   - install-time conditional content (backlog-md / "no backlog tool" branches present in templates, correctly absent from backlogit-installed dogfood copies);
   - semantic prose drift (dominant cause for github-pr-automation, which has ZERO conditional markers on either side yet still diverges by 725 bytes);
   - variable-derivation coverage gap (unresolved placeholders remain after rendering).
4. The AUTHOR OBLIGATION: editing either side of a paired-edit pair obliges the author to consider the other side in the same change and to refresh the harness-manifest.yaml checksum.
5. An explicit statement that paired-edit status is a RECORDED EXCEPTION, NOT A GOAL — the drift it tolerates is technical debt with an owner, not a design choice to be extended casually.

ACCEPTANCE (self-contained — this task completes on the document alone)
- The document exists under docs/.
- Its YAML frontmatter decodes.
- All five MUST CONTAIN items above are present, with the inventory listing all eight pairs by name and the cause taxonomy attributed per pair.
- Every claim is traceable to the spike. No new measurements invented.
- All of Ship's mandatory gates are green at this task's completion gate.

EXPLICITLY NOT THIS TASK'S ACCEPTANCE (corrected 2026-08-20, Stage)
Being REFERENCED FROM the parity contract test is NOT a criterion here. That reference is authored exclusively by SUCCESSOR task 137.001-T (its requirement 3: "A pointer to the 137.002-T maintenance-contract document in the in-file rationale comment"), and is verified by 137.001-T's own acceptance and by 137-F covering-feature acceptance.

WHY: the previous wording made this task's completion depend on work owned by 137.001-T, while 137.001-T depends on THIS task — a successor-owned acceptance criterion that this task could never satisfy on its own without duplicating the successor's edit. This task is now independently complete on document creation, frontmatter validity, and content.

DO NOT, in this task: edit tests/test_scope_containment_policy_contract.py or any other test; add a reverse dependency on 137.001-T; or duplicate the in-file pointer edit. Width isolation: docs-only.

EXECUTION ORDER: this is the FIRST task of the shipment. It must land before 137.001-T (which annotates this taxonomy and adds the pointer to this document) and before the stash-archive migration task (which performs a paired edit under the rule this document states). The `137.001-T -> 137.002-T (blocks)` edge already records this; no reverse edge exists or should be added.

Spike: docs/decisions/2026-08-20-template-dogfood-render-parity-spike.md
Plan: docs/plans/2026-08-20-template-dogfood-paired-edit-contract-plan.md (Task 1; see Amendment F3 on acceptance ownership)