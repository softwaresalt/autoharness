---
artifact_type: task
created_at: 2026-09-18T05:01:16.4660787Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 172.001-T
id: 172.002-T
labels:
    - agent-template
    - recovery-contract
parent_id: 172-F
priority: medium
status: queued
title: 'Resume-hint T2 (inert): startup recovery scan contract invokes ''autoharness checkpoint scan --json'''
updated_at: 2026-09-19T01:07:29.5027711Z
---

Describe the STARTUP RECOVERY SCAN CONTRACT in the agent templates and their installed mirrors as an INVOCATION OF THE EXECUTABLE COMMAND autoharness checkpoint scan --json, whose JSON output the agent consumes.

A MARKDOWN SURFACE CANNOT CALL A PYTHON PREDICATE BY PROSE. Describing a classification rule in agent text and asserting that the agent "applies the predicate" is unverifiable and unexecutable; the agent has no import. What an agent CAN do is invoke a command and branch on its structured output, and whether it did so is a decidable property of the template text. The contract therefore names the command, its --json output shape, and the branches the agent takes - it RESTATES NO PREDICATE INLINE. A per-agent restatement produces two drifting definitions of one rule, which is the failure mode the single-decision-procedure design exists to prevent.

THE BRANCHES, STATED EXPLICITLY: a record in a terminal status with no usable hint appears in the reported legacy set as CHECKPOINT_LEGACY_HINTLESS_RESOLVED and is EXCLUDED from the candidate set; a record in a NON-TERMINAL status with a missing, empty or whitespace-only hint appears in the blocking set as CHECKPOINT_ACTIVE_HINTLESS and FAILS CLOSED to operator handoff; a structurally unclassifiable record appears in the blocking set as CHECKPOINT_RECORD_MALFORMED and likewise FAILS CLOSED. A non-empty blocking set halts startup; the legacy set is reported by filename and count so the exemption is visible.

NO RECORD IS LEFT UNCLASSIFIED: the contract states the classification is total and deterministic over the corpus, so a reader cannot infer a silent-pass path.

THIS TASK IS INERT. It describes what the startup surface will invoke; it does not yet switch any producer call site onto the adapter and prohibits nothing. The producer rewiring and the prohibition land together in the single atomic activation 172.009-T.

Blocked by 172.001-T: the scan contract is written against the classification policy, not ahead of it. Blocks 172.009-T. Scope: templates/agents/ plus the installed .github/agents/ mirrors; all copies change and revert together. Stash 2A7C48A8. Plan docs/plans/2026-09-17-checkpoint-resume-hint-contract-plan.md (revision 7). Verdict manifest docs/reviews/2026-09-17-checkpoint-resume-hint-contract-plan-review.md (latest_attempt 07, verdict REMEDIATED-PENDING-REVIEW at plan revision 7, awaiting independent attempt 08).