---
artifact_type: task
created_at: 2026-09-15T22:16:40.1355997Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 166.003-T
    - 166.001-T
    - 166.004-T
id: 166.005-T
labels:
    - skill
    - shipment-reconcile
    - close-mechanism
    - template-parity
parent_id: 166-F
priority: medium
status: queued
title: 'Safe-close Steps 2/3/5/8: baseline-invariance gate + fail-closed record-transition halt (+ template mirror)'
updated_at: 2026-09-16T06:31:57.1769059Z
---

REFRAMED IN PLACE 2026-09-15. Supersedes the Terminal-Close Sub-Procedure. NO sub-procedure and NO third verdict are added. Authoritative plan: docs/plans/2026-09-15-flat-manifest-shipment-closure-plan.md (unit U5). Depends on 166.003-T + 166.004-T.

SURFACES (identical edits to BOTH, byte-parity required):
- .github/skills/shipment-reconcile/SKILL.md
- templates/skills/shipment-reconcile/SKILL.md.tmpl

SCOPE: Safe-Close Mode Steps 2, 3, 5 and 8.

CHANGES
1. STEP 2 — the protected set becomes an OBSERVATION SET. Out-of-manifest artifacts sharing the covering feature's hierarchy are enumerated in order to be WATCHED for change, NOT in order to be REQUIRED PRESENT. DELETE the sequence-aware-exclusion provenance requirement (predecessor shipment must carry archived_status: shipped|done) entirely — INV-3 makes it moot, because an out-of-manifest artifact may be in ANY state and still must not gate closure.
2. STEP 3 — REPLACE the baseline-integrity PRESENCE gate ("confirm every protected-set member currently exists in queue/; if any is already in archive/ or missing, halt with HALT — cascade detected") with the INV-7 BASELINE-INVARIANCE gate: record each observed artifact's baseline LOCATION and CONTENT HASH alongside the existing `git status --short -- ".backlogit/"` capture. An observed artifact that is already archived, descoped, or missing AT BASELINE is RECORDED AS BASELINE STATE and is explicitly NOT A HALT — it carries no signal about THIS run. This is the change that unblocks 173-S on the safe-close path and closes the same class for 168-S (stash 3CA122AC).
3. STEP 5 — the verify-after-each invariant compares against the BASELINE FINGERPRINT (did anything outside closure scope CHANGE relative to baseline?) rather than against "still present in queue/". A change to any observed artifact — relocation, deletion, content edit, or a cleared parent_id — is a cascade and halts. Retain the git-status cross-check.
4. STEP 8 — add the fail-closed halt for the residual R1 gap (plan-review P1-3). When `backlogit move <shipment_id> --status shipped` is refused (backlogit 1.10.1 returns exit 9: "shipment must be shipped via ShipShipment, not a direct status update") AND the manifest is NOT cascade-eligible under Step 0(c), HALT with a NEW code RECONCILE_FAIL_NO_SAFE_RECORD_TRANSITION. The text MUST state: (a) there is no known safe path to archived_status: shipped for this shape in 1.10.1; (b) the cascade op MUST NOT be substituted, because the measured engine RETURNS live out-of-manifest siblings and SILENTLY CLEARS their parent_id, orphaning them; (c) archiving an active shipment record stamps archived_status: active and fails the existing provenance gate; (d) this is an unresolved UPSTREAM TOOLING GAP requiring operator escalation. NO silent workaround, NO retry, NO fallback.
5. ADD the seven negative scenarios to the Deterministic Safe-Close Scenario Matrix: (i) out-of-manifest descendant status: done -> SAFE_CLOSE; (ii) out-of-manifest descendant live queued -> SAFE_CLOSE; (iii) out-of-manifest descendant in archive/ with no status -> SAFE_CLOSE; (iv) out-of-manifest live GRANDCHILD -> SAFE_CLOSE; (v) observed artifact archived AT BASELINE -> recorded, NOT a halt; (vi) observed artifact changed DURING the run -> halt; (vii) partial shipment with refused record transition -> RECONCILE_FAIL_NO_SAFE_RECORD_TRANSITION with no cascade substitution.
6. PRESERVE UNCHANGED: Step 4's per-item archive loop and its manifest-items-only pre-archived classification; Step 6's git-revert-on-cascade action; Steps 7/9/10; and the explicit non-goal that clearing parent_id is REJECTED and must never be introduced as an alternative or migration step.

ACCEPTANCE: 166.002-T Part B doc-contract tests pass against BOTH mirrors; both mirrors byte-identical on every changed block; no unresolved {{...}} placeholders introduced.

SIZE: M. COMPLEXITY: high.