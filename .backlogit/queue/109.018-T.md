---
artifact_type: task
created_at: 2026-08-05T00:14:39.6102167Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 2h-two-axis-v1
    size_source: agent
dependencies:
    - 109.002-T
id: 109.018-T
labels:
    - topology
    - gate
    - orchestrator
    - integration
    - p-001
    - p-016
    - enforcement
parent_id: 109-F
priority: medium
status: queued
title: 'B7: wire gate pipeline-topology into Orchestrator agent template routing + cursor-advance'
updated_at: 2026-08-05T00:36:06.1332782Z
---



<!-- BEGIN:acceptance-criteria -->
* The Orchestrator agent template invokes `autoharness gate pipeline-topology` (fail-closed in agent mode) before routing a queued shipment to Ship (pre-claim eligibility gate) and before advancing the multi-shipment cursor.
* SHIPMENT-SCOPED TARGET: the Orchestrator passes `--shipment <ID>` bound to the queued shipment it is evaluating — at route-to-Ship it passes the candidate shipment being routed; at cursor-advance it passes the successor shipment being selected — so the pre-claim eligibility gate is scoped to that specific target (agent-mode fail-closed; a missing target -> exit 2).
* EXPLICIT PHASE: both Orchestrator invocations are PRE-CLAIM (the candidate/successor shipment is not yet claimed), so each passes `--phase pre_claim` — route-to-Ship eligibility and cursor-advance both demand ZERO active shipments (agent-mode fail-closed; a missing/invalid phase -> exit 2, never inferred).
* The gate invocation is additive to — not a replacement for — the existing hard dependency (blocks) eligibility gate and P-001/P-016 closure-gated routing checks.
* Structural assertions confirm the routing-time and cursor-advance invocation points exist, pass `--shipment` AND `--phase pre_claim`, and run before the route/claim handoff.
* Change confined to templates/agents/_orchestrator.agent.md.tmpl and installed .github/agents/_orchestrator.agent.md; no backlogit mutation.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
[Shipment B / integration] Wire `gate pipeline-topology` invocation into the Orchestrator agent template at its Step 2 route-to-Ship decision (pre-claim eligibility) and at cursor-advance, so the promised at-most-one-active-shipment and branch/worktree topology invariants are mechanically checked at the orchestration boundary, not just in Ship. This complements 109.017-T (Ship-side invocation) to cover all promised lifecycle gate points.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Insert the gate call in the orchestrator Step 2 selection/eligibility recheck and in the dark-run cursor-advance block, passing `--shipment <candidate/successor ID>` and `--phase pre_claim` at both points. Keep it read-only/fail-closed; the orchestrator still never mutates backlogit transition rules. Separate from 109.019-T which corrects the sequencing status semantics.
<!-- END:implementation-notes -->