---
artifact_type: task
created_at: 2026-07-13T23:40:49.756-07:00
dependencies:
    - 079.005-T
    - 079.004-T
id: 079.011-T
labels:
    - blocked-on-external
parent_id: 079-F
priority: medium
status: blocked
title: Computing telemetry aggregation + derived efficiency metrics
updated_at: 2026-07-14T14:44:00.000-07:00
---

Build the telemetry aggregation layer that consumes normalized epoch records and computes deterministic time-series buckets, trend inputs, expected-tool gap rates, size-label distributions, and derived efficiency/cost metrics. Keep this task scoped to aggregation and formulas; reader normalization is 079.005-T and report slicing/rendering is 079.012-T.

<!-- BEGIN:acceptance-criteria -->
- Aggregation tests cover date/range buckets over normalized epoch records from 079.005-T without reading sinks directly.
- Per-tool gap tests assert `missing_expected_tool_counts[tool] = max(expected_tool_counts[tool] - observed_tool_counts[tool], 0)` and scalar totals remain equal to their per-tool map sums.
- Derived metrics are deterministic: `net_offload_tokens = avoided_read_estimated_tokens - tool_output_estimated_tokens`, `consumption_generation_ratio = input_tokens / output_tokens`, `gap_rate`, and `cost_per_successful_epoch`.
- Range and bucket derived ratios are computed from aggregate totals by summing numerators and denominators across epochs, then dividing; reports must not average per-epoch ratios.
- A successful epoch is defined from `AbsoluteOutcome` as having at least one recorded gate exit code, all recorded gate exit codes equal to `0`, and not `blocked`; empty gate-outcome collections are not successful. `cost_per_successful_epoch = sum(cogs_usd over successful epochs) / count(successful epochs)`.
- Any derived metric reports `unavailable` — never `0` and never an exception — when any operand is `unavailable`/null or its denominator is zero, including zero successful epochs or `output_tokens == 0`.
- Aggregation groups by `task_size_label`, `feature_planned_size_label`, and `shipment_planned_size_label` as ordinal buckets only, reports within-label dispersion and monotonicity observations without numeric label distances, and keeps planned-vs-composition plus cost-per-size-point `unavailable` unless a named/versioned label-to-point mapping exists.
- Tests prohibit parent+child cost double counting: aggregations either attribute task/epoch actuals upward to feature/shipment slices or report task-level actuals, but do not add parent planned labels as independent cost-bearing work.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Primary files should be a telemetry reporting aggregation/metrics module plus focused aggregation tests. Do not add report output formatting, CLI exposure, live event emission/composition, backlogit hierarchical-sizing adapter reads, or other source adapters in this task.
<!-- END:implementation-notes -->
