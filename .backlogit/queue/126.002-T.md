---
artifact_type: task
created_at: 2026-08-15T05:19:44.9176883Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: stage-2h-v1
    size_source: agent
dependencies:
    - 126.001-T
id: 126.002-T
labels:
    - cli
    - resolver
    - fail-closed
owner: stage
parent_id: 126-F
priority: high
status: queued
title: Add a shared storage-root resolver mirroring upstream precedence
updated_at: 2026-08-15T05:19:44.9176883Z
---

Add one shared helper in src/autoharness that resolves the backlog storage root, mirroring backlogit 1.9.0 precedence EXACTLY: BACKLOGIT_WORKSPACE_DIR (validated), then .backlog, then .backlogit. When both roots exist it MUST report and halt, mirroring upstream AmbiguousWorkspaceRootError. It MUST NOT merge, copy, delete, or rename either directory, and MUST NOT fall through to the candidate scan when an explicit override names a missing directory. Autoharness MUST NOT invent a competing directory-name config key or env var (H8). EXCLUSION (H5, verbatim): this task MUST NOT run 'backlogit migrate --workspace-dir', MUST NOT rename this repository's .backlogit directory, and MUST NOT change .autoharness/backlog-registry.yaml's directory value. That migration is operator-gated and must never run under dark-mode automation.