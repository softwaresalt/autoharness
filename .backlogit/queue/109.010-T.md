---
artifact_type: task
created_at: 2026-08-04T23:38:48.3332171Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-two-axis-v1
    size_source: agent
dependencies:
    - 109.013-T
id: 109.010-T
parent_id: 109-F
priority: medium
status: queued
title: 'B4: documentation — usage, install/activation, bypass/audit, cross-machine limits'
updated_at: 2026-08-05T03:11:00Z
---



<!-- BEGIN:acceptance-criteria -->
* Docs cover usage, install/activation, bypass/audit, manual-dev mode, cross-machine limitation.
* Documents the `--shipment <SHIPMENT_ID>` target contract: REQUIRED in agent shipment-scoped modes (fail-closed, exit 2 if missing) vs deterministic implicit resolution (currently-claimed shipment / current-branch slug) for non-shipment hook/ci contexts, and the ambient-only behavior when no target resolves.
* Documents the `--phase <pre_claim|post_claim|lifecycle|ambient>` contract: REQUIRED and fail-closed (exit 2) in agent shipment-scoped mode where it MUST be one of the shipment-scoped values pre_claim|post_claim|lifecycle (the non-scoped `ambient` is rejected as invalid there), NEVER inferred; deterministic phase resolution (default `ambient`, NOT `lifecycle`) for non-shipment hook/ci contexts; semantics pre_claim = ZERO active shipments, post_claim/lifecycle = exactly-one-active-and-target, ambient = at-most-one-active validating the resolved target when one exists (zero-active ordinary hook/CI PASSES and is non-blocking, exactly-one-not-target/unresolved or two-or-more BLOCKS). Explicitly notes ambient is DISTINCT from lifecycle at the zero-active row (ambient passes, lifecycle blocks). Includes which lifecycle point passes which phase (Ship branch/worktree creation = pre_claim AND the immediately-before-claim check = pre_claim — branch/worktree precedes the claim under P-011 and is NEVER post_claim; the immediate post-claim GLOBAL verification = post_claim; build/PR/closure = post_claim/lifecycle; Orchestrator route + cursor-advance = pre_claim; hook/pre-commit/pre-push/CI ambient runs = ambient).
* Cross-references resolve; frontmatter valid; markdownlint heading rules pass (P-008).
* No unresolved template variables in examples.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
[Shipment B / hooks+install] Author documentation for `gate pipeline-topology`: usage/flags/exit codes (including `--shipment` and `--phase`, the `ambient` phase and its deterministic hook/ci resolution), opt-in install + activation, the audited --force bypass + audit-log location, manual-developer compatibility (advisory-degrade mode), and the cross-machine scope split (active-shipment invariant is detection-only, not serialized at any scope; worktree=machine-local).
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Place under docs/ alongside existing gate documentation.
<!-- END:implementation-notes -->