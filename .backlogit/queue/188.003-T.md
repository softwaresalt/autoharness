---
artifact_type: task
created_at: 2026-09-26T07:36:05.2613671Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: v1
    size_source: agent
dependencies:
    - 188.002-T
id: 188.003-T
labels:
    - harness-surface:harness-architect
    - ship-lifecycle
    - unit-b
    - unit-b-core
parent_id: 188-F
priority: high
references:
    - docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md
    - docs/reviews/2026-09-25-ship-lifecycle-release-units-plan-review.md
status: queued
title: 'Ship lifecycle B3: manifest snapshot and surface classification'
updated_at: 2026-09-26T07:53:54.0752906Z
---



<!-- BEGIN:acceptance-criteria -->
- One fixture per class 5, 6 and 7 code, all in temporary workspaces; no test reads the real repository surface (IM-10).
- Global manifest checks (only when the surface union is non-empty): one manifest read and one YAML parse through a yaml.SafeLoader subclass; a duplicate mapping key -> MANIFEST_DUPLICATE_KEY, including a duplicate key nested below the top level; decode, YAML and shape failures -> MANIFEST_DECODE_INVALID, MANIFEST_YAML_INVALID, MANIFEST_SHAPE_INVALID; a global failure emits no per-surface rows.
- Per-surface classification in plan order: zero path matches -> MISSING / MANIFEST_ENTRY_NOT_FOUND; several -> INVALID / MANIFEST_ENTRY_AMBIGUOUS; template mismatch -> INVALID / MANIFEST_TEMPLATE_MISMATCH; unresolved placeholders -> INVALID / TEMPLATE_VARIABLE_UNRESOLVED; installed missing -> MISSING; render or checksum disagreement -> STALE (RENDER_MISMATCH or CHECKSUM_MISMATCH); both equal -> PRESENT.
- Alias test: verify_workspace.render_template is verify_workspace._render_template; _render_template is unchanged (one grammar; UTF-8, LF).
- B2's and B3's paths are added to the audit inventory (see the IM-14-F51 carry below); tests/test_harness_noclaim_audit.py stays green, and any LEDGER entry B3 adds is a non-claim confirmed by local review.
- RED record before implementation (IM-13, per the A1 wording): task ID, exact P-004 canonical command, native exit, and each roster test's outcome with its own marker AHLC_B3_MANIFEST_CLASSIFY:<t> per the Marker Convention (pairwise distinct; a shared marker is refused under FI-9). The alias and audit tests reach no stub and are recorded outside the roster, like characterization tests.
- Environment prechecks: git worktree list shows one worktree; git status --porcelain is empty for the task's files before the first write; the canonical command runs from the repository root. Over the estimate by more than 30 minutes -> stop and return to Stage for a split.
- IM-14: no added text claims race, TOCTOU or hardlink-alias resistance.
- IM-15: a test or proof PASS confers no claim authority; ordinary P-001/P-002/P-004/P-020 gates at claim and closure.
- Manifest harvest carry IM-14-F51 (E4): the frozen 'B2's and B3's paths are added to the audit inventory' cell is read as: FLOOR grows only by B1 and D1; harness_surfaces.py is already in FLOOR (B1); B2's and B3's other paths (the test modules and the verify_workspace.py alias line) are residue units under the Residue rule, not FLOOR entries.
- Manifest harvest carry CONST-II-F01 (E2): structural tests 'may pass'; record the pre-implementation failing run of the alias and audit structural tests as gap characterization (Principle II evidence), not as RED.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan task B3 (docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md, blob b7a77c76, section 'B3: Manifest snapshot and surface classification'; release unit S(B-core); review authority: manifest E5 Verdict under OP-5).

Files (budget): src/autoharness/harness_surfaces.py; src/autoharness/verify_workspace.py (alias only); tests/test_harness_surfaces_manifest.py; tests/test_harness_noclaim_audit.py.

Change: only when the surface union is non-empty. One manifest read and one YAML parse through a yaml.SafeLoader subclass that rejects duplicate mapping keys (MANIFEST_DUPLICATE_KEY), decode (MANIFEST_DECODE_INVALID), YAML (MANIFEST_YAML_INVALID) and shape (MANIFEST_SHAPE_INVALID) checks; a global failure emits no per-surface rows. Per surface, in order: zero path matches -> MISSING / MANIFEST_ENTRY_NOT_FOUND; several -> INVALID / MANIFEST_ENTRY_AMBIGUOUS; template mismatch -> INVALID / MANIFEST_TEMPLATE_MISMATCH; then template read and render with the manifest's one top-level variables_used mapping through the existing pure renderer, called via a new module-level alias verify_workspace.render_template bound to _render_template (one grammar; UTF-8, LF), unresolved placeholders -> INVALID / TEMPLATE_VARIABLE_UNRESOLVED; installed missing -> MISSING; render or checksum disagreement -> STALE (RENDER_MISMATCH or CHECKSUM_MISMATCH); both equal -> PRESENT. Templates are read from <workspace>/templates/ through the WORKSPACE root; a workspace without it yields TEMPLATE_NOT_FOUND. Reader errors map per the plan's Reader-error mapping table (manifest, template and installed stages).

Marker: AHLC_B3_MANIFEST_CLASSIFY (prefix). Trace: IM-10 (fixtures only), PE-DATA-01 to 03, IM-13, IM-14, IM-15. Estimate 100 min; size S; complexity medium.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Label harness-surface:harness-architect (Python production module under src/): roster and RED obligation apply. Blocked by B2; last task of S(B-core); S(B-entry) B4a continues in the same module. Risky action (plan Plan Hardening): the public alias verify_workspace.render_template; alias test red -> B-core does not ship; _render_template is unchanged. Context, not a B3 carry: templates are not eol=lf pinned (IM-02-F01, deferred to the IM-10 unit); B's tests use LF fixture bytes and D3 preflight fails closed on a CRLF checkout.
<!-- END:implementation-notes -->