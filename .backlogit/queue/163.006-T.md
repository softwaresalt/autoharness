---
artifact_type: task
created_at: 2026-09-08T07:43:18.645763Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 163.004-T
id: 163.006-T
labels:
    - diagram
    - docs-currency
parent_id: 163-F
priority: medium
status: queued
title: 'U4: diagram currency - update diagram 05 with the pre-mutation evidence anchor and all four halt tokens'
updated_at: 2026-09-10T23:53:42.2108733Z
---

UPDATE the EXISTING docs/diagrams/05-shipment-reconcile-cascade-premode.mmd. CORRECTION (2026-09-10, review-fix cycle 1): this task previously said the file and docs/diagrams/ "do not yet exist" and instructed CREATE. That was WRONG. The docs/diagrams/ set (00-conventions-legend, 01..06, README, and the eraser/ subset) was authored and OPERATOR-APPROVED on 2026-09-07 under gate G-DIAG-REVIEW, is recorded in the 15A02E21 deliberation's linked_artifacts, and is present in the workspace as a working-tree artifact not yet committed to main. Diagram 05 therefore EXISTS: this task UPDATES it and MUST NOT create a second diagram beside it. If the diagram set is published by an unrelated change before this task runs, still UPDATE the published file.

The update must draw, on the PRE-MUTATION side of the cascade invocation: (1) the Step 0(c) EVIDENCE-RECORD EMISSION node (.backlogit/reconcile/{shipment_id}-precascade-snapshot-{timestamp}.md); (2) the R1 ANCHOR node - the PRECASCADE_EVIDENCE_ANCHOR append carrying the record's sha256 into the engine-owned .backlogit/logs/{shipment_id}.jsonl - drawn INSIDE the existing single-writer lock and strictly BEFORE the cascade invocation, so the append-order relationship the gate depends on is VISIBLE rather than implied; (3) ALL FOUR halt tokens as SEPARATELY LABELLED exits - RECONCILE_FAIL_PRECASCADE_EVIDENCE_MISSING, RECONCILE_FAIL_PRECASCADE_EVIDENCE_STALE, RECONCILE_FAIL_PRECASCADE_ANCHOR_MISSING, RECONCILE_FAIL_PRECASCADE_ANCHOR_NOT_PRE_MUTATION - NEVER collapsed into a shared "fail" exit, which would erase in the diagram the exact diagnostic R1-2 exists to create (the B57F9E24 never-merge rule applies to the drawing, not only to the prose); (4) the R1-11 L1/L2 DURABILITY SPLIT, with L2 (commit the evidence record AND that shipment's own engine log, force-added past the workspace .gitignore rule, verbatim and byte-unmodified, at the closure commit) [THIS PARENTHETICAL IS SUPERSEDED - SEE AMENDMENT R1-12(4) AND R1-13 BELOW, WHICH GOVERN WHAT IS ACTUALLY DRAWN; retained as history, not as an instruction] drawn on the POST-CLOSE side and VISIBLY NOT WIRED INTO ANY GATE DECISION, so no reader infers a fifth halt token.

Per lesson 2 of the composed-workflow-protocol-state-machine-validation compound learning (docs/compound/2026-09-06-composed-workflow-protocol-state-machine-validation.md - published 2026-09-10, commit 5171ace1 on main; stash 15A02E21 and follow-up 1CD92B69 both reconciled and archived 2026-09-10), the diagram is where the seam between Step 0(c) and the cascade invocation becomes reviewable; prose hides it. Verify with scripts/check_eraser_diagrams.py if that check is wired at execution time. Depends on U2. Plan: docs/plans/2026-09-08-shipment-reconcile-step-0c-live-pre-mutation-evidence-plan.md U4.

--- AMENDMENT R1-12 (2026-09-10, review-fix cycle 2; plan section "U4"/"R1-12", redesign decision D-7/D-8) --- CLEAN-CLONE SATISFIABILITY. R1-12 GOVERNS where it conflicts with the CORRECTION paragraph above.
(1) SINGLE CANONICAL PATH, STABLE: docs/diagrams/05-shipment-reconcile-cascade-premode.mmd. This is the one and only diagram this task touches, and the path does NOT vary with repository state.
(2) RESOLVE THE PATH AT EXECUTION TIME - DO NOT ASSUME A REPOSITORY STATE. Verified 2026-09-10: docs/diagrams/ is UNTRACKED and ABSENT from origin/main (it was authored and operator-approved 2026-09-07 under gate G-DIAG-REVIEW and exists only as a working-tree artifact). The earlier CREATE-only instruction was wrong, and the cycle-1 UPDATE-only correction was ALSO wrong, for the same reason: each ASSUMED a repository state instead of RESOLVING one, leaving the task unsatisfiable from a clean clone. The governing rule is: IF THE CANONICAL FILE IS PRESENT, UPDATE IT IN PLACE; IF IT IS ABSENT, CREATE THAT SAME CANONICAL FILE AT THAT SAME PATH with the required content. Either way the outcome is ONE FILE AT ONE PATH. NEVER create a second or parallel diagram beside it, and never branch the canonical path on whether the file was found.
(3) scripts/check_eraser_diagrams.py IS OPTIONAL, NEVER A DEPENDENCY. Verified 2026-09-10: it is UNTRACKED and ABSENT from origin/main. Run it ONLY if it is published and wired at execution time. This task's completion, acceptance and verification MUST NOT depend on an untracked file. The earlier "verify with scripts/check_eraser_diagrams.py if that check is wired at execution time" wording is retained in this weaker, explicitly-optional form.
(4) L2/U6 DEPICTION CORRECTED. Requirement (4) above says to draw the L2 split with L2 "force-added past the workspace .gitignore rule". That phrasing rests on a premise direct inspection refutes: origin/main carries NO .backlogit/logs/ ignore rule, git ls-files shows 993 of 1016 logs ALREADY TRACKED including 171-S's own, the ignore rule exists only as an uncommitted working-tree edit, and ignore rules never hide tracked paths. DRAW INSTEAD: L2 as the shipment's own engine log PUBLISHED INTO THE CLOSURE COMMIT verbatim and byte-unmodified, on the POST-CLOSE side, together with the U6 POST-CASCADE PUBLICATION step (task 163.008-T) and its reportable condition RECONCILE_CLOSURE_INCOMPLETE_L2_EVIDENCE - both drawn VISIBLY NOT WIRED INTO ANY GATE DECISION, so no reader infers a fifth halt token. Do not draw a .gitignore decision node; the publication is conditional on the path's TRACKING STATE (Cases A/B/C), which is an implementation detail of the step and not a gate.
(5) UNCHANGED: the four separately labelled halt-token exits, never collapsed; the anchor node inside the single-writer lock and strictly before the cascade invocation; the evidence-record emission node. SIZING: S / low, unchanged by this amendment and now stated explicitly in the redesign decision D-5 sizing table (which previously and incorrectly listed this task as "unchanged" at XS / trivial after cycle 3 had already re-sized it).

--- AMENDMENT R1-13 (2026-09-10, review-fix cycle 3; plan section "R1-13", redesign decision D-7/D-8) --- L2/U6 LABEL WORDING CORRECTED; SIZE UNCHANGED (S / low); STILL ONE FILE AT ONE PATH.
(1) DO NOT LABEL L2 "BYTE-IDENTICAL". Where the L2/U6 node or its label describes fidelity, use the APPEND-ONLY PREFIX RULE wording - the anchor line and the pre-cascade bytes the gate evaluated are published UNMODIFIED as the PREFIX of the committed log, with the engine's post-gate mutation lines shown as EXPECTED APPEND-ONLY EXTENSIONS. A byte-identical label would depict a state no legitimate close reaches, because the cascade's own engine-written mutation events are appended to the same log after the gate reads it.
(2) OPTIONAL, STILL POST-CLOSE AND STILL UNWIRED: the U6 REMEDIATION EDGE may be drawn - missing L2 => closure INCOMPLETE => FOLLOW-UP CLOSURE COMMIT => re-verify - provided it remains VISIBLY OUTSIDE every gate decision and is not drawn as a fifth halt-token exit. Do not draw an amend/force-push path; remediation is always an ADDITIONAL commit.
(3) UNCHANGED: the upsert rule of R1-12(2) (update the canonical file if present, create that same canonical path if absent, never a parallel diagram); the optional-script rule of R1-12(3); the four separately labelled halt-token exits; and the anchor node inside the lock and strictly before the cascade invocation.