---
artifact_type: task
created_at: 2026-09-03T08:25:16.3185355Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.002-T
    - 160.014-T
id: 160.007-T
labels:
    - packaging
    - plugin
    - marketplace
parent_id: 160-F
priority: medium
status: queued
title: Wire plugin channel payload per the spike outcome
updated_at: 2026-09-03T09:37:01.1206783Z
---

Constrain the Copilot plugin channel payload to the manifest.

GREEN RESPONSIBILITY (assigned by the RED harness 160.005-T): this task turns the PLUGIN-COMPOSITION case green - test_plugin_payload_excludes_dev_directories (AC3). It does NOT own green for the wheel-composition or release-path cases.

BLOCKED BY the plugin-channel mechanism spike (160.002-T). Do NOT begin until that spike resolves branch (a) or (b) - the shape of this task depends on its outcome. Also blocked by the loader/resolver (160.004-T), whose single generation command produces the payload declaration this task installs.

BRANCH (a) - marketplace source supports an exclusion/allowlist mechanism:
- Wire the manifest-derived exclusions directly into `.github/plugin/marketplace.json`, GENERATED by `python -m build_support.payload generate --channel plugin`. The declaration is a derived artifact; hand edits are prohibited and detected by `generate --check`.

BRANCH (b) - no such mechanism exists:
- Add a build step that materializes a payload directory (dist/plugin/) from the resolved manifest, and repoint marketplace.json `source` from "." to that directory.
- The materialization step MUST invoke the single generation command (160.004-T); it must not reimplement resolution.
- dist/plugin/** is classified EXCLUDE in the manifest (160.003-T) and must be gitignored - it is generated staging, never tracked payload.
- This adds a build step and changes the release flow - coordinate with the wheel wiring task (160.006-T), whose AC2b gate step must also cover the plugin channel via `generate --channel all --check`.

CURRENT STATE: `source: "."` makes the ENTIRE repository the delivered payload - 3,238 tracked files / ~18 MB, including .backlogit/ (2,110 files of this workspace's own backlog records, 65% of all tracked files, zero runtime role).

MANDATORY PAYLOAD MEMBERS (AC10): plugin.json and .github/plugin/marketplace.json must both remain in the payload carrying a concrete `version`. Because the plugin channel excludes `src/` and `pyproject.toml`, the plugin manifest is the ONLY remaining resolver for the `{{AUTOHARNESS_VERSION}}` template variable, and install-harness FAILS the install if that variable is unresolved. The plugin-root resolver contract is specified and tested by 160.013-T (AC6b); this task must satisfy it, not restate it.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. ELEVATED - this task edits a consumer-facing distribution declaration.
- .github/plugin/marketplace.json: payload/source declaration ONLY. The plugin `name`, `version`, publisher identity, and marketplace coordinates are behaviour-preserving and must be asserted unchanged.
- ROLLBACK CHECKPOINT: before this task lands, record the exact pre-change bytes of marketplace.json. Rollback is `source: "."` restored - the payload returns to the untrimmed superset, degraded but functional.
- DESTRUCTIVE-OPERATION CHECKPOINT (branch (b) only): the dist/plugin/ materialization step writes ONLY under the gitignored build-output path and MUST NEVER delete, move, or overwrite a tracked file. Assert with a test that runs the materialization against a dirty working tree and verifies the tracked file set is unchanged.
- NO-PUBLISH CHECKPOINT: this task MUST NOT run `copilot plugin publish`, `gh release create`, or any marketplace push. Publication is Ship/Orchestrator scope (CP2).
- FREEZE-SCOPE: `src/autoharness/` is read-only (invariants I1/I2).
Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

ACCEPTANCE: AC3 (plugin payload excludes all dev directories), AC10. The plugin-composition harness case goes from red to green and the transition is recorded.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T8)