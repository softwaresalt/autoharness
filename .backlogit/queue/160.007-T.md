---
artifact_type: task
created_at: 2026-09-03T08:25:16.3185355Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.002-T
id: 160.007-T
labels:
    - packaging
    - plugin
    - marketplace
parent_id: 160-F
priority: medium
status: queued
title: Wire plugin channel payload per the spike outcome
updated_at: 2026-09-03T08:25:16.3185355Z
---

Constrain the Copilot plugin channel payload to the manifest.

BLOCKED BY the plugin-channel mechanism spike (160.002-T). Do NOT begin until that spike resolves branch (a) or (b) — the shape of this task depends on its outcome.

BRANCH (a) — marketplace source supports an exclusion/allowlist mechanism:
- Wire the manifest-derived exclusions directly into `.github/plugin/marketplace.json`.

BRANCH (b) — no such mechanism exists:
- Add a build step that materializes a payload directory (e.g. dist/plugin/) from the resolved manifest, and repoint marketplace.json `source` from "." to that directory.
- This adds a build step and changes the release flow — coordinate with the wheel wiring task.

CURRENT STATE: `source: "."` makes the ENTIRE repository the delivered payload — 3,238 tracked files / ~18 MB, including .backlogit/ (2,110 files of this workspace's own backlog records, 65% of all tracked files, zero runtime role).

MANDATORY PAYLOAD MEMBERS (AC10): plugin.json and .github/plugin/marketplace.json must both remain in the payload carrying a concrete `version`. Because the plugin channel excludes src/ and pyproject.toml, the plugin manifest is the ONLY remaining resolver for {{AUTOHARNESS_VERSION}}, and install-harness FAILS the install if that variable is unresolved.

ACCEPTANCE: AC3 (plugin payload excludes all dev directories), AC10.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T7)