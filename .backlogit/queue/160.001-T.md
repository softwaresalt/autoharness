---
artifact_type: task
created_at: 2026-09-03T08:23:42.9606871Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 160.001-T
labels:
    - packaging
    - schema
    - baseline
parent_id: 160-F
priority: medium
status: queued
title: Baseline characterization capture across wheel, sdist and plugin channels
updated_at: 2026-09-03T10:16:53.5906476Z
---

BASELINE CHARACTERIZATION CAPTURE (T2a). RE-SCOPED in review-fix cycle 2 (findings 1 and 16): the schema publication, the versioned mirror, the SCHEMA_CONTRACTS registration, and the pair-divergence assertion have all MOVED OUT of this task to 160.018-T (T2b). Two reasons, both binding: (a) those are PRODUCTION ARTIFACTS that cycle 1 let land with NO RED OWNER ANYWHERE, which this shipment's test-first contract forbids - 160.018-T is now blocked by the RED-FIRST harness 160.005-T and is the named green owner of two RED-FIRST cases; (b) one S/low task combined baseline capture across three channels, two schema documents, runtime contract registration, and an immutability assertion - four deliverables with different reversibility, which understated both axes. THIS TASK IS NOW BASELINE CAPTURE ONLY.

WHAT THIS TASK PRODUCES: the durable, referenceable BASELINE against which every CHARACTERIZATION case is observed green and every parity/upgrade/registration assertion is measured. A baseline that exists only in a transcript CANNOT BE ASSERTED AGAINST; it is recorded as a checked-in fixture or a recorded evidence file, referenced BY NAME from every task that consumes it.

HARD PREREQUISITE FOR: 160.016-T (the CHARACTERIZATION harness observes green against this baseline build), 160.008-T, 160.012-T, 160.013-T, 160.009-T and 160.010-T (all five verification families author and observe against it).

CAPTURE, ALL THREE CHANNELS (the third is NEW in cycle 2, finding 3):
- Build the current WHEEL and record its full file inventory + byte size.
- Build the current SDIST and record its full file inventory + byte size. THIS IS THE CHANNEL THE SHIPMENT WAS MISSING: [tool.hatch.build.targets.sdist] declares only core-metadata-version, so hatchling's default sweep packages the whole project and `uv build` publishes it alongside the wheel. The baseline must MEASURE that, not assume it - record exactly which excluded-set paths the current sdist contains.
- Record the current resolved PLUGIN payload inventory (3,238 tracked files / ~18 MB).
- Capture `autoharness verify-workspace` output on a scratch workspace installed from the untrimmed build. Record the FINDING SET, not just pass/fail - 160.012-T asserts parity as a SET DIFFERENCE against this recorded set.
- Capture `/install-harness` output inventory for the AC5/I3 byte-identity comparison.
- Capture the v1.5.0 plugin-only registration failure modes for vscode, claude, codex, and verify-workspace, so 160.009-T can assert those limitations are UNCHANGED rather than merely present.
- Record both `core-metadata-version = "2.4"` pin values and their exact table placement, so 160.016-T's CHARACTERIZATION case has a concrete baseline and 160.019-T can prove the sdist pin survived a table rewrite.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. THIS TASK IS RECORD-ONLY: it writes baseline evidence and NOTHING ELSE. It MUST NOT create or modify any schema, MUST NOT touch src/autoharness/ (the schema_contracts.py registration is 160.018-T's narrowly authorized exception, not this task's), and MUST NOT modify pyproject.toml, marketplace.json, plugin-payload/, or release.yml. Baseline capture installs into scratch workspaces under a gitignored temporary path and must not mutate the developer's real workspace or ~/.autoharness/. PUBLISHED-ARTIFACT IMMUTABILITY CHECKPOINT: the v1.5.0 artifacts used for baseline capture are already published and MUST NOT be mutated or retracted (invariant I5). NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2 (writable-surface table).

ACCEPTANCE: every baseline artifact above recorded durably in a referenceable form, covering all THREE channels, and referenced by name from the characterization, parity, upgrade, and registration tasks that consume it. No production artifact is created by this task.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T2a)