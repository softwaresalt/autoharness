---
artifact_type: task
created_at: 2026-09-03T08:23:42.9606871Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 160.001-T
labels:
    - packaging
    - schema
    - baseline
parent_id: 160-F
priority: medium
status: queued
title: Baseline characterization capture across wheel, sdist and plugin channels
updated_at: 2026-09-03T15:24:26.008922Z
---

BASELINE CHARACTERIZATION CAPTURE (T2a). Scope is baseline capture ONLY. The schema publication, the versioned mirror, the SCHEMA_CONTRACTS registration, and the pair-divergence assertion belong to 160.018-T (T2b), not here. Two reasons, both binding: (a) those are PRODUCTION ARTIFACTS that must have a RED OWNER, so 160.018-T is blocked by the RED-FIRST harness 160.005-T and is the named green owner of two RED-FIRST cases; (b) one S/low task combining baseline capture across three channels, two schema documents, runtime contract registration, and an immutability assertion understated both sizing axes.

WHAT THIS TASK PRODUCES: the durable, referenceable BASELINE against which every CHARACTERIZATION case is observed green and every parity/upgrade/registration assertion is measured. A baseline that exists only in a transcript CANNOT BE ASSERTED AGAINST.

DURABLE DESTINATION (authorized, binding). This task writes to EXACTLY TWO authorized paths, both named in the SHIP-10 plan H2 writable-surface table:
  1. docs/audits/2026-09-03-ship10-payload-evidence/ - the NARRATIVE baseline record: what was built, how, on which interpreter and OS, and the human-readable summary of each inventory and finding set. This is the single bounded evidence surface shared by 160.006-T, 160.019-T, 160.007-T, 160.015-T and 160.017-T; it is one directory for the whole shipment, not a new store per artifact.
  2. tests/fixtures/payload-baseline/** - the MACHINE-READABLE half: the wheel, sdist and plugin file inventories, the verify-workspace finding set, the install output inventory, and the registration failure-mode records, in a stable serialized form.
THE SPLIT IS DELIBERATE, NOT DUPLICATION. The two halves have different consumers: a human reviewer reads the narrative record, while 160.016-T, 160.008-T, 160.012-T, 160.013-T, 160.009-T, 160.010-T and 160.017-T LOAD the fixtures and DIFF against them. A prose document is not diffable, and a serialized inventory is not reviewable; neither half can do the other's job.
THE tests/fixtures/payload-baseline/** AUTHORIZATION IS FIXTURE-DATA ONLY. This task is NOT authorized under the general tests/** row (which covers 160.005-T, 160.016-T, 160.008-T, 160.012-T, 160.013-T, 160.009-T, 160.010-T and 160.017-T). It may write baseline fixture data and NOTHING ELSE under tests/ - no test module, no conftest, no helper. Authoring a test here would silently make this task a test-authoring task and break the two-harness contract.

HARD PREREQUISITE FOR: 160.016-T (the CHARACTERIZATION harness observes green against this baseline build), and 160.008-T, 160.012-T, 160.013-T, 160.009-T, 160.010-T (all five verification families author and observe against it).

CAPTURE, ALL THREE CHANNELS:
- Build the current WHEEL and record its full file inventory + byte size.
- Build the current SDIST and record its full file inventory + byte size. THIS IS THE CHANNEL THE SHIPMENT WAS MISSING: [tool.hatch.build.targets.sdist] declares only core-metadata-version, so hatchling's default sweep packages the whole project and `uv build` publishes it alongside the wheel. The baseline must MEASURE that, not assume it - record exactly which excluded-set paths the current sdist contains.
- Record the current resolved PLUGIN payload inventory (3,238 tracked files / ~18 MB).
- Capture `autoharness verify-workspace` output on a scratch workspace installed from the untrimmed build. Record the FINDING SET, not just pass/fail - 160.012-T asserts parity as a SET DIFFERENCE against this recorded set.
- Capture `/install-harness` output inventory for the AC5/I3 byte-identity comparison.
- Capture `autoharness home` and `autoharness version` behaviour, so 160.012-T's named case test_home_and_version_behave_identically has a concrete baseline to compare against.
- Capture the v1.5.0 plugin-only registration failure modes for vscode, claude, codex, and verify-workspace, so 160.009-T can assert those limitations are UNCHANGED rather than merely present.
- Record both `core-metadata-version = "2.4"` pin values and their exact table placement, so 160.016-T's CHARACTERIZATION case has a concrete baseline and 160.019-T can prove the sdist pin survived a table rewrite.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. THIS TASK IS RECORD-ONLY: it writes the two evidence destinations named above and NOTHING ELSE. It MUST NOT create or modify any schema, MUST NOT touch src/autoharness/ (the schema_contracts.py registration is 160.018-T's narrowly authorized exception, not this task's), and MUST NOT modify pyproject.toml, marketplace.json, plugin-payload/, or release.yml. Baseline capture installs into scratch workspaces under a gitignored temporary path and must not mutate the developer's real workspace or ~/.autoharness/. PUBLISHED-ARTIFACT IMMUTABILITY CHECKPOINT: the v1.5.0 artifacts used for baseline capture are already published and MUST NOT be mutated or retracted (invariant I5). NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2 (writable-surface table).

ACCEPTANCE: every baseline artifact above recorded durably in the two authorized destinations, covering all THREE channels, and referenced by name from the characterization, parity, upgrade, and registration tasks that consume it. No production artifact is created by this task.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size S, complexity low. Three builds, three inventories, and a set of recorded observations, all mechanical; the naming of the destinations removes the only judgement call.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T2a)