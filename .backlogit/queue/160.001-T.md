---
artifact_type: task
created_at: 2026-09-03T08:23:42.9606871Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 160.001-T
labels:
    - packaging
    - schema
    - baseline
parent_id: 160-F
priority: medium
status: queued
title: Baseline payload capture and payload-manifest schema (new, 1.0.0)
updated_at: 2026-09-03T09:50:19.7499403Z
---

Capture the pre-change baseline and author the new payload manifest schema.

BASELINE (HARD PREREQUISITE for all later parity, upgrade, and byte-identity tests - 160.008-T, 160.012-T, 160.013-T all consume it):
- Build the current wheel and record its full file inventory + byte size.
- Record current resolved plugin payload inventory (3,238 tracked files / ~18 MB).
- Capture `autoharness verify-workspace` output on a scratch workspace installed from the untrimmed build. Record the FINDING SET, not just pass/fail - 160.012-T asserts parity as a set difference against this recorded set.
- Capture `/install-harness` output inventory for the AC5/I3 byte-identity comparison.
- Capture the v1.5.0 plugin-only registration failure modes for vscode, claude, codex, and verify-workspace, so 160.009-T can assert those limitations are UNCHANGED rather than merely present.
- Baseline artifacts must be recorded in a durable, referenceable form (checked-in fixture or recorded evidence file). A baseline that exists only in a transcript cannot be asserted against.

SCHEMA:
- New `schemas/payload-manifest.schema.json` at version 1.0.0.
- Declares `include`, `exclude`, per-channel (`wheel`, `plugin`) overlays, and the single `target_workspace_paths` key (the SOLE authored list of docs/* prefixes that resolve against the target workspace rather than the payload - see 160.004-T's centralized classifier).

CRITICAL: this is a NEW schema. Do NOT mutate any existing schema in place - this repository has three recorded occurrences of in-place schema mutation without a version bump (docs/compound/2026-08-30-157-s-149-f-schema-mutation-in-place-third-occurrence.md).

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. This task ADDS one new schema file and records baseline evidence. It MUST NOT mutate any existing schema, and MUST NOT modify pyproject.toml, marketplace.json, release.yml, or src/autoharness/. Baseline capture installs into scratch workspaces under a gitignored temporary path and must not mutate the developer's real workspace or `~/.autoharness/`. The v1.5.0 artifacts used for baseline capture are already published and MUST NOT be mutated or retracted (invariant I5). NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

SCHEMA PUBLICATION LAYOUT (BINDING, added in Orchestrator review-fix cycle 1 by the plan-review Learnings Researcher persona, P1): creating the schema at 1.0.0 is NECESSARY BUT NOT SUFFICIENT to avoid this repository's recorded three-occurrence schema-mutation-in-place bug class. That learning (docs/compound/2026-08-30-157-s-149-f-schema-mutation-in-place-third-occurrence.md) records that the shorter and more dangerous fix path applies exactly to CONTRACTS UNREGISTERED IN SCHEMA_CONTRACTS, because an unregistered schema has NOTHING that detects a later in-place mutation and a fourth occurrence would land silently. Three MANDATORY deliverables, all acceptance criteria, none deferrable: (1) PUBLISH BOTH FILES - schemas/payload-manifest.schema.json (the live contract) AND the immutable versioned mirror schemas/payload-manifest/1.0.0.schema.json. This is the two-file convention observed on EVERY other versioned schema in this repository (harness-config, harness-manifest, workspace-profile, tool-telemetry-event, validation-gates); a top-level schema with NO versioned mirror is a conformance FAILURE. (2) REGISTER THE CONTRACT in src/autoharness/schema_contracts.py so the existing mutation detector covers payload-manifest from its FIRST release rather than after the first incident. (3) ASSERT THE PAIR - ship a test that FAILS if the live file and the 1.0.0 mirror diverge while the declared $id/version still reads 1.0.0. That assertion is precisely what was absent in all three prior occurrences. NOTE the freeze-scope interaction: deliverable (2) is the ONE permitted touch of src/autoharness/ by this task (schema_contracts.py registration only); every other src/autoharness/ file remains frozen.

ACCEPTANCE: AC1 (schema exists, is well-formed, and is a new 1.0.0 schema); every baseline artifact above recorded durably and referenced by name from the parity, upgrade, and registration tests that consume it.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T2)