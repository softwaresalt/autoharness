---
artifact_type: task
created_at: 2026-08-21T20:22:10.1722176Z
custom_fields:
    complexity: low
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 140.001-T
labels:
    - bug
    - docs
    - docline
    - migration
    - test-first
parent_id: 140-F
priority: medium
references:
    - docs/plans/2026-08-21-docs-compound-docline-conformance-plan.md
    - .backlogit/queue/025-DL.md
status: active
title: Verify and apply the docs/compound frontmatter migration (source + doc_type, 73 files)
updated_at: 2026-08-21T22:56:51.615925Z
---

Backfill `source` and `doc_type` across the 73 markdown files under `docs/compound/` using the official idempotent, body-preserving `backlogit docs migrate`.

**Plan:** `docs/plans/2026-08-21-docs-compound-docline-conformance-plan.md` Task 1 (+ amendments C1, C2)

## Test-first requirement (mandatory, do this before any write)
Add `tests/test_docs_compound_frontmatter_contract.py` asserting every `*.md` under `docs/compound/` has a YAML frontmatter block with non-empty `source` AND `doc_type`. Scope is `*.md` only — `.gitkeep` and any future non-markdown asset are excluded (amendment C2). The failure message must NAME every offending file so a skipped file cannot hide. This test MUST be RED at task start (73 failures) and GREEN at task end.

## Steps
1. Dry-run: `backlogit docs migrate --path docs/compound --format json`.
2. **BLOCKING GATE.** Inspect the planned `source` value. If the migration would FABRICATE provenance it cannot know, STOP and return to Stage — do not `--apply` an unexamined value. Record the observed value verbatim in this task.
3. Confirm the four known no-frontmatter files and record whether the dry-run plans a synthesised block for each:
   - `2026-08-07-copilot-review-fix-introduces-new-filter-bug.md`
   - `2026-08-15-checksum-drift-fix-correctly-surfaces-preexisting-self-hosted-customization.md`
   - `2026-08-15-never-serialize-raw-operator-content-into-json-reports.md`
   - `2026-08-15-torn-archive-log-entry-without-file-mutation-must-not-be-committed.md`
   If the corpus changed since planning, re-derive the list and record the delta.
4. Apply, scoped exactly: `backlogit docs migrate --apply --yes --path docs/compound`. The `--path` MUST be exactly `docs/compound` — widening it is a P-021 scope breach.
5. Hand-author frontmatter for any of the four files the migration skipped (`doc_type: learning`, `source` matching the value observed in step 2).
6. Re-run the dry-run to prove idempotence.

## Acceptance criteria
- AC1. `tests/test_docs_compound_frontmatter_contract.py` passes; 73/73 files carry non-empty `source` and `doc_type`.
- AC2. `git diff docs/compound` shows changes confined to frontmatter: for every changed file the diff contains NO hunk below the closing `---` of the frontmatter block. Evidence is the diff itself, not an assertion about it.
- AC3. Every pre-existing frontmatter key survives verbatim (none removed, none rewritten). Spot-verify at minimum `2026-08-21-ast-based-structural-regression-guards-beat-line-regex.md` (carries `problem_type`/`category`/`root_cause`/`tags`/`shipment`/`task`/`date`).
- AC4. Second dry-run plans ZERO changes (idempotence proven).
- AC5. The four no-frontmatter files are individually named with their disposition (migrated vs hand-authored).
- AC6. No file outside `docs/compound/` is modified, with ONE explicit exemption: `tests/test_docs_compound_frontmatter_contract.py`, which the test-first requirement above REQUIRES this task to create. Creating that test is an in-scope deliverable, not a scope breach; no other path outside `docs/compound/` may be touched. (Corrected in review-fix cycle 1 - the original AC6 was unsatisfiable, since it forbade the very file the task must author.)
- AC7 (amendment C1). `backlogit docs lint --path docs/compound` reports ZERO required-field errors of ANY kind. Any residual error class is enumerated by field name and either fixed here (if mechanically derivable) or captured as a NEW deferred stash entry under P-021 C1 — never silently accepted.

## Rollback
`git checkout -- docs/compound` restores the corpus exactly; the migration writes nothing outside that path. The contract test `tests/test_docs_compound_frontmatter_contract.py` is a newly added file and is removed separately if the task is abandoned.

## Notes
Per active external entry 90F2A9F8, `backlogit docs lint` hard-aborts on the first decode error anywhere in scope — always use `--path` scoping when verifying.