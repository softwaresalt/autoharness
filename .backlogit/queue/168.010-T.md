---
artifact_type: task
created_at: 2026-09-19T00:11:52.0607048Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 168.001-T
id: 168.010-T
labels:
    - test
    - p-004
    - tdd-red
parent_id: 168-F
priority: high
status: queued
title: 'P-004 T1a (RED): shipment-scoped declared-harness-set storage lifecycle tests observed failing'
updated_at: 2026-09-19T00:13:17.1825015Z
---

TDD RED PHASE. Author the failing storage-lifecycle tests for load_declared_harness_set() BEFORE the loader exists, and OBSERVE THEM RED. The declared harness set is stored SHIPMENT-SCOPED at .autoharness/harness-sets/{shipment_id}.yaml and validated by schemas/declared-harness-set.schema.json. It is NOT stored in .autoharness/harness-manifest.yaml, which is a ~150 KB SINGLETON INSTALL INVENTORY rewritten wholesale by autoharness install/tune, carries no shipment dimension, and would be destroyed or would destroy the declaration on the next install. REQUIRED FAILING CASES: (1) ABSENT - no document for the requested shipment_id emits P004_HARNESS_SET_MISSING and the gate fails closed, never defaulting to an empty set; (2) IDENTITY MISMATCH - a document whose shipment_id field does not equal the requested shipment_id emits P004_HARNESS_SET_STALE and is rejected, never silently accepted; (3) DIGEST DRIFT - a document whose manifest_digest does not equal the current compute_manifest_digest() value emits P004_HARNESS_SET_STALE, so a declaration authored against a different installed harness cannot govern this run; (4) SOLE READER - the loader is the only reader of the storage path, asserted by a structural test that no other module opens .autoharness/harness-sets/; (5) RETENTION - closing a shipment moves its document to the archive location and a subsequent load of the archived shipment_id emits P004_HARNESS_SET_MISSING rather than reading the archived copy. IMPORT SAFETY (BINDING): this module must import cleanly under unittest.defaultTestLoader with ZERO loader.errors, asserted directly. A module that raises at import time yields a _FailedTest placeholder, which the gate classifies P004_MISSING_OBSERVATION - a MISSING OBSERVATION, NOT valid red - so the RED task would fail its own gate. Every not-yet-existing boundary is therefore imported INSIDE the test body through a small _load_boundary() helper, never at module top level. Each declared RED test must INDIVIDUALLY execute and fail or error on its own, producing its OWN detail_text containing its OWN declared marker. Scope: tests/. Blocked by 168.001-T only. Does NOT depend on the implementation task: red precedes implementation. Stash 76EBDE6D. Plan docs/plans/2026-09-17-p004-red-phase-precondition-scoping-plan.md (plan_id p004-red-phase-precondition-scoping, plan_role active, revision 7). Verdict manifest docs/reviews/2026-09-17-p004-red-phase-precondition-scoping-plan-review.md (latest_attempt 07, verdict REMEDIATED-PENDING-REVIEW at plan revision 7, awaiting independent attempt 08).