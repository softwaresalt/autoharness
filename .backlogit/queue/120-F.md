---
artifact_type: feature
created_at: 2026-08-10T08:08:18.2949049Z
custom_fields:
    harness_status: pending
id: 120-F
labels:
    - supervisor
    - control-plane
    - copilot-cli
    - candidate-a
    - 34D50F2D
    - plan-1
    - fast-track
    - P-006
    - shipment-s3
    - per-shipment-covering-feature
references:
    - docs/plans/2026-08-09-copilot-cli-supervisor-control-plane-plan.md
    - docs/plans/2026-08-09-copilot-cli-supervisor-control-plane-hardening.md
    - docs/reviews/2026-08-09-copilot-cli-supervisor-control-plane-review.md
    - docs/spikes/2026-08-09-plan1-shipment-topology-proof/sim-shipment-closure.ps1
    - start.ps1
    - start.sh
status: queued
title: 'S3 Copilot supervisor: application services, adapters, start.ps1/start.sh migration (Plan 1)'
updated_at: 2026-08-10T08:17:59.3278156Z
---

Shipment-3 covering feature for Plan 1 (Local Copilot CLI supervisor / control-plane runtime). Scope: application services, the `autoharness run` CLI adapter, the local console-only approval channel, the start.ps1 / start.sh (+ templates copies) compatibility-shim migration, and rollout/rollback documentation. This is the ONLY behavior-changing shipment in the chain and therefore ships last (P1). Owns exactly the eight S3 tasks and nothing else. STRUCTURAL ISOLATION (H10.5): ROOT feature with NO parent feature; every descendant is an explicit member of its own shipment, so backlogit 1.8.0 returnUnreleasedFeatureItems finds zero unreleased descendants at close and clears no parent_id. Product grouping with 118-F / 119-F is preserved by non-hierarchical related_to links from the umbrella 117-F, which featureScopeRoots does not traverse. The final shipment additionally carries the childless umbrella 117-F as an explicit member so the product record closes with the program and needs no post-close operator action. Supersedes the S3 slice of 117-F.

<!-- BEGIN:Scope -->
T12 bootstrap.py workspace bootstrap service (120.001-T), T13 sidecar.py lifecycle/preflight service (120.002-T), T14 resolve.py Copilot CLI resolution + argv composition (120.003-T), T15 app.py run_session() orchestration (120.004-T), T16 approvals.py local console-only structured command + approval channel (120.005-T), T17 `autoharness run` CLI adapter (120.006-T), T18 start.ps1 / start.sh (+ templates copies) converted to compatibility shims (120.007-T), T19 observability + rollout/rollback documentation (120.008-T). This is the ONLY behavior-changing shipment and therefore ships LAST. 120.007-T is gated by the 118-F characterization suites (blocks-on 118.001-T / 118.002-T / 118.005-T), which must be re-run BYTE-IDENTICAL after migration (H1 / H9). Blocked until 128-S ships.
<!-- END:Scope -->

<!-- BEGIN:Shipment-Safety -->
H10.5 CONTRACT — this feature is the covering feature of the FINAL shipment 129-S and MUST satisfy both invariants at all times:
(i) FULL COVERAGE — 120-F has exactly eight children (120.001-T..120.008-T) and every one is an explicit member of 129-S.
(ii) ROOT PLACEMENT — 120-F has NO parent feature.
129-S carries a SECOND root feature member: the childless product umbrella 117-F. That is safe and deliberate — descendantItems(117-F) is empty, so returnUnreleasedFeatureItems(117-F) returns nothing, and as an explicit member it is marked done and archived by the engine at final close. This is what retires the program record without any operator action, adopt_item call, or manual repair. 120-F is listed FIRST in the manifest so the read-only covering_feature render projection resolves to the implementation feature rather than the umbrella. Verified against the real engine by docs/spikes/2026-08-09-plan1-shipment-topology-proof/sim-shipment-closure.ps1, ARM B (final close archives 120-F, its eight tasks, 117-F, and 129-S itself, with returned_ids empty).
<!-- END:Shipment-Safety -->