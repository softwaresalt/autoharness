---
artifact_type: task
created_at: 2026-08-11T02:07:25.3780717Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-width-isolated-v1
    size_source: agent
id: 118.007-T
labels:
    - shipment-1
    - P0
    - policy
    - templates
    - ship-agent
    - P-015
    - F26-remediation
parent_id: 118-F
priority: critical
references:
    - templates/policies/workflow-policies.md.tmpl
    - templates/agents/_ship.agent.md.tmpl
    - docs/compound/097-S-shipment-task-only-safe-close.md
status: queued
title: T5c Amend P-015 for the verified fully-covered-root close path (policy + Ship agent + reconcile skill)
updated_at: 2026-08-11T02:07:59.0840534Z
---

SIZE: M. COMPLEXITY: medium. (Two independent axes; also emitted as structured backlog fields.) Shipment 1 (P0). POLICY/TEMPLATE WIDTH - deliberately width-isolated from all Python supervisor work in this shipment. Created by the F26 ruling: amend P-015 so that the PERMITTED shipment-close operation and the EXECUTABLE EVIDENCE agree, without ever requiring Ship to perform a P-010-forbidden operation. PROBLEM BEING FIXED: docs/compound/097-S-shipment-task-only-safe-close.md instructs Ship to close a fully-covered-root shipment with the cascade ship operation, while P-015 as generated into the Ship agent prohibits that operation UNCONDITIONALLY; a planning artifact cannot grant Ship an exemption from Ship's own operative rule, so the instructed close path is currently unexecutable. SCOPE (all four surfaces must be amended COHERENTLY in one change, because partial amendment recreates the contradiction): (1) templates/policies/workflow-policies.md.tmpl P-015 - keep the unconditional prohibition as the DEFAULT and add an explicit, narrowly-scoped VERIFIED FULLY-COVERED-ROOT EXCEPTION whose preconditions are machine-checkable: the predicate is quantified over EVERY FEATURE MEMBER of the manifest, not over a single covering feature - each feature member MUST be a ROOT (no parent_id) and MUST be FULLY COVERED (every one of its children is also a member), and the manifest MUST contain NOTHING beyond those root feature members and their children; when any precondition fails, the prohibition applies unchanged. F30 RULING - VERIFIED-CHILDLESS TERMINAL UMBRELLA: the earlier single-covering-feature wording ("the manifest contains EVERY one of its children AND NOTHING ELSE") was WRONG and is withdrawn - it would have REJECTED 129-S, which deliberately also carries the childless terminal umbrella 117-F, so the drafted check would have fallen back to safe-close and the claim that the 64/64 cascade simulation covers the permitted operation for ALL THREE manifests did not hold. The quantified form above admits 117-F because a childless root is trivially fully covered. ANTI-VACUITY REQUIREMENT (non-negotiable, this is the whole risk of the generalization): "fully covered" is VACUOUSLY TRUE for a childless feature, which is exactly the failure mode where a check passes because it found nothing to test. CHILDLESSNESS MUST THEREFORE BE POSITIVELY VERIFIED AGAINST THE LIVE WORKSPACE - query the feature's children and ASSERT THE COUNT IS EXACTLY ZERO - and MUST NOT be inferred from "no missing children were found". A feature whose children cannot be enumerated (query error, unindexed, ambiguous id) is NOT verified childless and the prohibition applies unchanged. The umbrella must additionally be TERMINAL: it parents nothing and no member task declares it as parent. PARTIAL-FEATURE SAFETY IS NOT WEAKENED AND MUST BE PROVEN SO: the exception admits ONLY root features that are fully covered (possibly vacuously, but then positively verified childless); a feature member with ANY child absent from the manifest, or any NON-ROOT feature member, or any member that is neither a qualifying root feature nor a child of one, still forces safe-close; when any feature member fails, the WHOLE MANIFEST falls back to safe-close - qualification is never per-member; (2) templates/agents/_ship.agent.md.tmpl - carry the same exception with the same preconditions so the generated Ship agent and the policy cannot drift; (3) the shipment-reconcile skill - implement the precondition check and select the close path from its result rather than from prose; (4) docs/compound/097-S-shipment-task-only-safe-close.md - remove the CONTESTED banner and state the resolved contract. EVIDENCE ALIGNMENT: the existing 64/64 disposable closure simulation exercises the cascade path under exactly the fully-covered-root preconditions, so after this amendment the evidence proves the operation that is actually permitted. ACCEPTANCE: regenerate the Ship agent and assert the generated file and the policy template state identical preconditions; a partial-feature shipment still takes safe-close; a fully-covered-root shipment that fails ANY precondition still takes safe-close; the check is exercised against ALL THREE REAL MANIFESTS and MUST select the cascade path for 127-S, 128-S AND 129-S - 129-S is the required regression case for F30, because the withdrawn wording rejected it; POSITIVE AND NEGATIVE CONTROLS ARE BOTH MANDATORY so the generalization cannot pass vacuously: a fixture manifest carrying a root feature that is NOT childless but whose children are ABSENT must be REJECTED (this is the case the vacuous reading would wrongly admit, and it is the exact defect the anti-vacuity requirement exists to catch); a fixture whose extra member is a NON-ROOT feature must be REJECTED; a fixture whose extra member is a task belonging to no member feature must be REJECTED; and a fixture where the childlessness query FAILS or returns unknown must be REJECTED rather than treated as childless; the childlessness assertion must be shown to FAIL when a child is added to the umbrella, proving it observes the live workspace instead of a hardcoded allowance for 117-F - NO id-specific special case for 117-F may appear anywhere in policy, agent, or skill; no Ship-side re-parenting, re-adoption, or post-close repair is introduced anywhere.