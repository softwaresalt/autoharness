---
artifact_type: task
created_at: 2026-08-19T20:45:21.2672606Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 1.0.0
    size_source: agent
dependencies:
    - 134.011-T
id: 134.013-T
labels:
    - tests
    - contract
    - verification
    - regression
parent_id: 134-F
priority: medium
references:
    - tests/test_scope_containment_boundary_contract.py
    - tests/test_scope_containment_semantics_contract.py
    - tests/test_scope_containment_policy_contract.py
status: active
title: 'Contract test: P-021 clause-boundary semantics (C1 gate, C4 non-bypass, C5 boundary and reference-role, Stage-only reprioritization, reconciliation consumer)'
updated_at: 2026-08-20T05:33:02.7119306Z
---

Sibling of 134.012-T. Asserts the P-021 CLAUSE-BOUNDARY semantics -- the C1 classification gate, the C4 active-cycle non-bypass, the C5 capture-only boundary and its reference-only carrier, Stage-only reprioritization, and the Stage-side reconciliation consumer -- while 134.012-T retains the behaviour-map allocation guards and the C2/C3 capture-and-discharge semantics.

<!-- BEGIN:acceptance-criteria -->
- [ ] New file tests/test_scope_containment_boundary_contract.py exists and runs under the canonical unittest gate, reusing `from autoharness.verify_workspace import _derive_template_variables, _render_template` and the LF-normalization approach already used by tests/test_circuit_breaker_policy_contract.py. This file MUST NOT import from tests/test_scope_containment_semantics_contract.py: 134.012-T's allocation guard imports THIS file's `OWNED_BEHAVIOURS`, so an import in the other direction would be circular. The dependency edge 012 -> 013 exists for exactly this reason -- this file is authored FIRST so the allocation guard, authored last, can read every constant it checks.
- [ ] CARRIER-SET RESOLUTION rule (identical to 134.012-T's, and inherited rather than re-invented): every criterion below names the BEHAVIOUR ID it asserts and resolves its carrier subset from the `CLAUSE -> BEHAVIOR -> CARRIER-SUBSET MAPPING` block in 134.011-T, which remains the SINGLE source of truth for both presence and subsets across all three contract-test files. This task MUST NOT restate the matrix or hardcode a carrier list. The split of behaviours across the three files is an ALLOCATION of assertion work, NOT a second copy of the map: a boundary-semantics file that grew its own carrier table would recreate exactly the three-copy drift the matrix consolidation repaired.
- [ ] ALLOCATION CONSTANT: declares an explicit `OWNED_BEHAVIOURS` constant equal to exactly {B1, B10, B11, B12, B14, B18} -- the CLAUSE-BOUNDARY group -- and asserts every behaviour it exercises is a member and every member is exercised. 134.012-T's RANGE-COMPLETENESS guard imports this constant and checks the three-way union and pairwise disjointness, so a behaviour silently added or dropped here surfaces as a failure THERE rather than as an untested behaviour nobody reports.
- [ ] C1-GATE test (behaviour B1): asserts the B1 subset resolved from 134.011-T carries the classification gate -- the requirement to run the C1 in-scope test BEFORE fixing a finding -- across all four workflow loops plus both policy surfaces. This clause carries no prior-defect history; the test exists to prevent the omission the carrier-completeness guard caught during the full C1-C7 inversion, not to regress a known failure.
- [ ] C4-NON-BYPASS test (behaviour B10, regresses the PR #372 review finding): asserts the B10 subset -- the authoritative registry plus the Stop-Conditions annotation on `_ship.agent.md.tmpl`, github-pr-automation, pr-lifecycle, `_orchestrator.agent.md.tmpl` and feature-flow-dark -- states that its own pressure does not authorize expansion. On the AUTHORITATIVE surface the test additionally asserts the ACTIVE-CYCLE BOUNDARY in full: that NO authorization of any kind, explicit operator authorization INCLUDED, expands the fix cycle that discovered the expansion, and that operator authorization is FORWARD-acting -- it may create or approve a SEPARATE work unit through C2 capture plus mandatory C6 deliberation, and never retroactively makes an already-discovered expansion in-scope for the cycle in flight. A NEGATIVE guard, UNIVERSAL scope across every C4 carrier plus the plan clause table and the deliberation, asserts no surface names an authorization SUFFICIENT to expand the active cycle; it must fail against the historical defective wording `Only an explicit operator authorization recorded as new/expanded approved scope does`, which closed an otherwise-correct non-bypass list by naming the one authorization that supposedly did authorize expansion, and so read as a licence for the in-cycle `go ahead` that skips capture (C2) and deliberation (C6) entirely. The guard is universal because the phrasing is wrong wherever it appears: it is a listing SHAPE -- insufficient authorizations followed by a sufficient one -- and any carrier can drift back into it.
- [ ] C5-BOUNDARY test (behaviour B11): asserts the B11 subset -- the authoritative registry and `_ship.agent.md.tmpl`'s Role Boundary, NOT role-enforcement -- carries the capture-only creation grant, the forbidden verbs BY NAME (the verb list resolved from its owner 134.002-T, which names discretionary REMOVAL and discretionary ARCHIVAL as separate dispositions, both governed by the DISCRETIONARY qualifier), and the manifest-derived cleanup exception. Kept distinct from B12 so that a regression in the carve-out boundary and a regression in the reprioritization rule fail different tests.
- [ ] C5-EXCEPTION test (behaviour B11, policy-to-role-boundary contract): asserts the authoritative C5 text in workflow-policies.md.tmpl prohibits DISCRETIONARY removal and DISCRETIONARY archival specifically, and that it does NOT blanket-prohibit either verb in a way that would forbid Ship's pre-existing post-merge Step 7 manifest-derived source-stash retirement. A companion assertion checks the two surfaces AGREE: the C5 policy text and the `_ship.agent.md.tmpl` Role Boundary must both carry the provenance distinction (manifest-derived closure = Allowed; discretionary disposition = Forbidden) per hardening H2. A NEGATIVE guard asserts C5 does not contain an unqualified "or remove" prohibition. This is the test that makes the exception non-regressable: without it, a future tightening of C5 would silently re-forbid a correct, already-shipped Ship behaviour.
- [ ] C5-REFERENCE-ROLE test (behaviour B18, carrier role `[R]` REFERENCE-ONLY): asserts role-enforcement.instructions.md.tmpl carries ONLY what 134.003-T is contracted to author -- (a) a capture-only stash write under C2/C5 matches the Allowed column and does NOT trigger the fail-closed unclassified-mutation halt; (b) any OTHER stash operation by Ship, including discretionary remove/archive, remains a P-010 violation that MUST halt; (c) the passage CITES P-021 C5 by policy ID and clause label instead of restating the policy text; (d) the pre-existing fail-closed rationale paragraph is unchanged, so the carve-out reads as an Allowed-column enumeration and never as a weakening of fail-closed semantics. Because this carrier's role is `[R]`, every marker this test resolves MUST be a citation or a recognition/enforcement behaviour; a marker that could only be matched by restated C5 prose is itself a defect against the role and MUST NOT be used. A NEGATIVE guard asserts this test does NOT demand the creation grant or the manifest-derived cleanup exception on this surface: hardening H5 contracts 003 to reference rather than restate C5, so requiring the complete boundary semantics here would fail against a correctly authored template -- which is precisely the false requirement B11 previously encoded by listing 003 in its subset, and which the `[N]` role marker would have reintroduced by the back door even after that split.
- [ ] STAGE-ONLY-REPRIORITIZATION test (behaviour B12): asserts every surface in the B12 subset (`_ship.agent.md.tmpl`, github-pr-automation.instructions.md.tmpl, pr-lifecycle/SKILL.md.tmpl, fix-ci/SKILL.md.tmpl, plus the authoritative registry) states the captured priority is PROVISIONAL and that re-prioritization and triage remain Stage-only (C5 carve-out). Scoped to B12 rather than to the whole C2 or C5 row: circuit-breaker carries no capture payload and therefore no provisional priority, while the Role Boundary (002) and role-enforcement (003) carry the carve-out BOUNDARY (B11/B18) rather than the reprioritization rule.
- [ ] RECONCILIATION-CONSUMER test (behaviour B14, regresses the dangling-obligation defect): asserts that `_stage.agent.md.tmpl` defines the late-identifier reconciliation workflow that `_ship.agent.md.tmpl`'s late-surfacing-thread text delegates to Stage. A duty delegated by one carrier with no defining carrier on the receiving side must fail this test. Because B14's subset is 008 ALONE, this test is the ONLY place the workflow is exercised, so it asserts EVERY obligation its owner 134.008-T defines rather than merely the workflow's existence -- a partial assertion here leaves the remainder untested anywhere. The asserted obligations, resolved from 134.008-T by decomposing EACH owner criterion into its atomic sub-obligations rather than mapping one obligation per criterion line -- the line-level mapping used in owner-reconciliation review cycle 1 is what let a compound clause hide INSIDE a criterion: (1) TRIGGER -- a TWO-PART obligation that MUST be asserted as two independently triggered steps, since asserting only the second is what let a duplicate-producing path go unwatched: (1a) DUPLICATE DETECTION is UNCONDITIONAL, run over EVERY deferred-expansion entry Stage triages regardless of field population and regardless of capture path, with a NEGATIVE guard that must FAIL against any wording gating detection on an `N/A` field -- a duplicate arises from a DISCOVERY failure, not a missing identifier, so a fully-populated duplicate (the common case on a PR-review-comment surface) must still be detected; and (1b) LATE-IDENTIFIER RECONCILIATION is MANDATORY during deliberation/triage AND fires whenever ANY source-ref field is recorded `N/A`, not only the review-thread ID. The test also asserts the INDEPENDENCE of (1a) and (1b) -- neither is stated as a precondition of the other -- and that a `DISCOVERY-STATUS: AMBIGUOUS` or `DISCOVERY-STATUS: LOOKUP-UNAVAILABLE` token (134.004-T) seeds and prioritizes the scan as an ACCELERATOR without becoming its TRIGGER, since a duplicate produced by a lookup that returned a false absence carries no token at all; (2) RETRIEVAL SOURCE and JOIN KEY -- Stage recovers late identifiers from the Ship-owned residual-risk records that CITE THE DEFERRED ENTRY ID, which is the join key, covering the PR/closure record on the late-surfacing-thread path, the task/run/closure records on the threadless path, and the fix-ci run/closure records; and the NEGATIVE half, that Stage MUST NOT ask Ship to supply the identifiers by editing the entry, which is the sentence that keeps this obligation from silently reauthorizing a second Ship write; (3) STAGE AUTHORITY -- reconciliation runs under Stage's own pre-existing stash authority (triage, re-classification, re-prioritization, edit), requiring NO Ship write and NO change to the C5 carve-out, with the single-write invariant and the carve-out both preserved unweakened; (4) ANTI-DUPLICATION, a FIVE-part compound clause that MUST be asserted in full: (4a) the existing entry is updated IN PLACE and no second entry is created for the same expansion; (4b) the Ship-generated deferred entry ID is the stable identity for the expansion across its whole lifetime; (4c) Stage DETECTS the case where more than one entry describes the same expansion, under the UNCONDITIONAL trigger (1a) rather than the `N/A` trigger (1b); (4d) it reconciles into the EARLIEST-CAPTURED entry and ARCHIVES the duplicates under its own authority via backlogit's stash ARCHIVE operation, NEVER by destructive removal -- a NEGATIVE guard asserts the workflow does not instruct Stage to delete or remove a duplicate, and must fail against the historical wording `removes the duplicates under its own authority`; (4e) it RECORDS THE MERGE in the deliberation, naming the surviving entry ID and the ARCHIVED duplicate IDs. Parts (4c)-(4e) are the remediation path and were omitted when this obligation was first written as prevention only: a rule that merely forbids creating duplicates has nothing to say once duplicates exist, and duplicates DO arise -- a prior-run capture whose entry is not found on re-query is exactly how a second entry gets created -- so without (4c)-(4e) the anti-duplication obligation is unenforceable precisely in the situation that produces the defect; (5) NON-BLOCKING -- an `N/A` that never resolves STANDS as a truthful terminal record, reconciliation completes as a no-op, deliberation proceeds, and it is never a gate on deliberation, planning or harvest, nor a C3/C6 shortfall; (6) IDEMPOTENCE -- re-running over an already-reconciled entry is a no-op that never overwrites a concrete identifier with `N/A` and never rewrites an identifier already recorded; (7) PROVENANCE / AUDIT OUTPUT -- reconciled identifiers are carried INTO THE DELIBERATION ARTIFACT alongside the originally captured refs, AND the outcome is RECORDED for auditability across ALL FOUR cases: a successful reconciliation naming the identifiers recovered and the residual-risk record they came from, a `no late identifier found` no-op, a duplicate MERGE naming the surviving and archived IDs per (4e), and a CLEAN DUPLICATE SCAN under the unconditional trigger (1a). Obligation (7) MUST be asserted in every one of those four forms: a test satisfied by recording only SUCCESSFUL reconciliations would leave the no-result case unrecorded, which is precisely the outcome the NON-BLOCKING rule (5) makes common -- the genuine pre-PR finding that never reaches a PR and the CI finding that never gains a thread both terminate as no-result reconciliations, so the half of (7) most likely to be dropped is the half that governs the majority of real terminations. Without it a truthful terminal `N/A` becomes indistinguishable from a reconciliation that was never performed, and the audit trail the whole workflow exists to preserve has a silent hole exactly where the non-blocking path runs; the merge form is equally load-bearing, since an unrecorded merge destroys the evidence that two entries were ever the same expansion; and the CLEAN-SCAN form is load-bearing for the same reason the no-result form is -- because detection (1a) is UNCONDITIONAL, an unrecorded clean scan is indistinguishable from a scan that never ran, so the case that terminates the MAJORITY of entries would be the case with no audit trail; (8) REFERENCE STYLE -- the reconciliation text CITES P-021 C5 AND C6 by policy ID and clause label rather than paraphrasing the registry (hardening H5). Obligation (3) is ALSO what discharges 008's C5 carriage under the cross-clause discharge declared in 134.011-T, so the C5 row is only fully covered if (3) is asserted here explicitly; a version of this test omitting (3) silently reopens a hole in the C5 row that the SUBSET-FIDELITY guard is entitled to assume is closed. The enumeration (1)-(8), including every lettered sub-part, is the COMPLETE set of reconciliation obligations 134.008-T authors as of this cycle; if a future edit adds or extends an obligation on the owner, this criterion MUST grow with it, since B14's single-carrier subset means nothing else will catch the omission. The completeness check that governs this criterion is ATOMIC, not line-level: every distinct MUST/MUST NOT in owner criteria L39-L46 maps to a numbered or lettered obligation here, and a criterion sentence carrying two obligations counts as two.
- [ ] Every test method carries a docstring naming the specific prior-cycle defect, review finding or dangling obligation it regresses, so a future failure is diagnosable without re-deriving the history.
- [ ] Marker strings are stable distinctive substrings, and each is resolved from the task that AUTHORS the text on that carrier, never from a surface that restates it. The owner set is DERIVED from this file's `OWNED_BEHAVIOURS` by taking the union of the authoring tasks in each owned behaviour's carrier subset -- it is NOT a pre-split list, and a criterion added or moved changes it. For {B1, B10, B11, B12, B14, B18} that union is ALL NINE content tasks: 134.001-T (authoritative C1/C4/C5 and Stage-only reprioritization -- B1, B10, B11, B12), 134.002-T (Ship Role Boundary carve-out and forbidden-verb list -- B11), 134.003-T (role-enforcement reference-only recognition -- B18), 134.004-T (Ship C1 gate, C4 Stop-Conditions annotation, provisional priority -- B1, B10, B12), 134.005-T (circuit-breaker C1 operational restatement -- B1; the circuit-breaker is a B1 carrier because the C1 gate is exactly what it performs when classifying a finding, and omitting it from this inventory while the C1-GATE test asserts `both policy surfaces` was the PR #372 cycle-2 provenance defect), 134.006-T (github-pr-automation -- B1, B10, B12), 134.007-T (pr-lifecycle and fix-ci -- B1 and B12 on both skills, B10 on pr-lifecycle ONLY, since fix-ci's C4 carriage is not in the B10 subset), 134.008-T (late-identifier reconciliation -- B14) and 134.009-T (orchestrator and feature-flow-dark C4 -- B10). Prefer short distinctive tokens over fragile long sentences.
- [ ] TEST-DIRECTION NOTE: the negative guards in this file (the C4 bypass-phrasing guard, the C5 unqualified-removal guard, the B18 no-restatement guard and the B14 duplicate-removal guard) are easy to write so they pass vacuously. Each MUST be validated by temporarily reintroducing the historical defective wording named in its criterion and confirming the test FAILS; a guard that cannot be shown to fail against its named historical defect does not satisfy its criterion.
- [ ] Full canonical gate green: $env:PYTHONPATH='src'; python -m unittest discover -s tests. Root `python -m pytest` is NOT the canonical gate here (docs/compound/097-S-canonical-unittest-gate.md).
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Third contract-test file of the feature, split out of 134.012-T in PR #372 review-fix cycle 1 (thread PRRT_kwDORzpWpM6amwKd). 134.012-T had been re-estimated to L with a split line DECLARED IN ADVANCE but deliberately not taken; the review found that deferring a known-necessary split to implementation time is itself the defect, because the split then lands mid-execution when the 2-hour bound is already breached and the person taking it has the least context.

The cut follows the pre-declared line exactly, extended only by lineage for the three behaviours registered after that line was written:

- CAPTURE-AND-DISCHARGE semantics stay in 134.012-T -- B3-B9 plus the derived B16 (`B7 INTERSECT B8`) and B17 (`B6 UNION {007-fix-ci}`). Derived behaviours MUST sit in the same file as their parents, or the DERIVED-SUBSET guard would have to recompute an intersection whose operands live in another module.
- CLAUSE-BOUNDARY semantics move here -- B1, B10, B11, B12, B14 as pre-declared, plus B18, which was split out of B11 in the owner-contract reconciliation and belongs with its sibling.

The two groups share only the carrier-resolution pattern, which is a convention rather than a table, so neither file duplicates the other's map. The behaviour map itself stays where it has always been: 134.011-T.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on 134.011-T for the authoritative map. 134.012-T depends on THIS task, not the reverse: the RANGE-COMPLETENESS guard lives in 134.012-T and imports `OWNED_BEHAVIOURS` from all three contract-test modules, so the file it reads must exist first. Authoring order is therefore 011 -> 013 -> 012.

Separate test FILE from both siblings (tests/test_scope_containment_boundary_contract.py), so a boundary failure, a semantic failure and a structural failure stay distinguishable in the gate output.

Commit with an explicit enumerated path list only (hardening H13).

SIZING: M / medium. Six behaviours across roughly nine assertions and four negative guards, all render-then-assert-substring against the shared helper pattern. B14 is the single heavy criterion (eight obligations including a five-part compound), which is why this group -- rather than the nine-behaviour capture-and-discharge group -- is the one that fits alongside no structural guards.
<!-- END:implementation-notes -->