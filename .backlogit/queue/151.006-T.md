---
artifact_type: task
created_at: 2026-08-31T23:21:38.9937242Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 151.006-T
parent_id: 151-F
priority: medium
status: queued
title: 'De-risking prerequisite: enumerate the verifier assertion table and resolve every assertion path to its source-of-truth'
updated_at: 2026-08-31T23:29:23.7914512Z
---

DE-RISKING PREREQUISITE (SHIP-1 plan, two-axis gate). BLOCKS 151.003-T; it may not begin until this is recorded. NO PRODUCTION EDITS - recorded findings only, to docs/. WHY: 151.003-T is M/high. Its complexity sits entirely in one unanswered question - how does the verifier's assertion table actually address its targets, and can every entry be mechanically resolved to a source-of-truth file? Answer it first and the harness becomes mechanical. DELIVERABLES (all four required): (1) ENUMERATE the three table-driven assertion sets in src/autoharness/verify_workspace.py - PACK_ASSERTIONS, FOUNDATION_ASSERTIONS, DARK_FACTORY_ASSERTIONS - with a total count (cycle 0 cites 71; VERIFY that number rather than inheriting it); (2) for EACH assertion, resolve its `path` to its SOURCE-OF-TRUTH per binding H2 - the .tmpl when one exists, the installed file ONLY when it does not - and record any entry that resolves AMBIGUOUSLY or NOT AT ALL, since those are the entries that would silently degrade the sweep back into reading dogfood copies (the exact blindness being fixed); (3) IDENTIFY the verifier's own variable tables that the render harness must reuse (H2 forbids inventing a parallel substitution path); (4) RECORD the expected wall-clock cost of a full-corpus render so cycle-0 finding 5's mitigation (render once per test class and cache; single test over a prebuilt render if the suite budget is exceeded) can be decided on a measurement rather than a guess. ACCEPTANCE: all four recorded in docs/; the count in (1) is measured and stated; every ambiguous/unresolvable assertion from (2) is listed explicitly; 151.003-T consumes all four. SCOPE BOUND: record only - this task fixes NO assertion and writes NO harness code. SAFETY MODE: careful + investigate-first. Source: 053E2BD2. Plan: docs/plans/2026-08-31-ship1-v1_5_0-guardrail-contract-restoration-plan.md
