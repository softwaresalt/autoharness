---
artifact_type: task
created_at: 2026-08-22T08:16:21.3213016Z
custom_fields:
    complexity: low
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 144.002-T
id: 144.003-T
labels:
    - migration
    - environment
    - assertion-integrity
    - mechanism-a
parent_id: 144-F
priority: medium
references:
    - docs/plans/2026-08-22-git-config-env-containment-plan.md
    - docs/plans/2026-08-22-git-config-env-containment-hardening.md
    - tests/test_gates_topology.py
    - tests/test_gate_dag_readiness_cli.py
    - tests/test_gate_pipeline_topology_cli.py
status: queued
title: Migrate all 13 bulk os.environ mutation sites under tests/ to patched_environ (zero assertion edits)
updated_at: 2026-08-22T08:16:21.3213016Z
---

**LAYER 2 — mechanism-A migration. This is NOT a mechanism-B remediation (R8).**

Plan: `docs/plans/2026-08-22-git-config-env-containment-plan.md` Task 3. Amendments A6, R2, R8 BINDING.

## Sites (re-scan and reconcile BEFORE editing; record any site this list omits)

| Module | Lines |
| --- | --- |
| `tests/test_gates_topology.py` | 1004, 1023, 1045, 1069, 1090, 1112, 1141, 1173, 1207, 1236, 1266 |
| `tests/test_gate_dag_readiness_cli.py` | 217 |
| `tests/test_gate_pipeline_topology_cli.py` | 273 |

Note `clear=False` sites (1045, 1069, 1090, 1173, 1266) ARE in scope: `patch.dict`'s exit path is clear-then-update regardless of the `clear=` argument, so they destroy the environment exactly like the others.

## Rules

- Replace `patch.dict('os.environ', {...})` with `patched_environ(...)` (`from _env_patch import patched_environ`).
- Replace the `import os as _os; _os.environ.pop('X', None)` idiom inside a block with an explicit `X=None` override on the helper call.
- **ZERO assertion-line edits.** No test renamed, reordered, added, or removed.

## A6 (BINDING) — assertion-integrity gate, mechanical not promissory

For EVERY module touched:

- **AIG-1** For every `self.assert*` / `self.fail` callsite: method name, `ast.unparse` of each positional argument, and `ast.unparse` of each keyword argument, in source order. Pre and post lists MUST be EQUAL.
- **AIG-3** Authorized change classes are exhaustively: (N1) import lines, (N2) `with` statement headers, (N3) removal of the `import os as _os` / `_os.environ.pop(...)` idiom inside a migrated block. Any edit outside N1-N3 is disallowed.
- **AIG-4** Every diff line must be cited to N1, N2, or N3. Uncited edits fail the task.

## R8 (BINDING) — mechanism separation

This task migrates `tests/test_gates_topology.py` lines 994/1017/1038/1062/1085 — the five tests `141.004-T` named as the mechanism-B polluters. Migrating a site is a **MECHANISM-A** change: it removes the destructive clear-then-update restore. It is **NOT** a mechanism-B remediation. `145.001-T`'s re-measurement remains **MANDATORY AND UNCONDITIONAL** regardless of this task's outcome. No `144-F` task may record a mechanism-B disposition.

## R2 (BINDING) — acceptance evaluable at THIS task's boundary

Acceptance is:

(a) AIG-1, AIG-3, AIG-4 all pass; **AND**
(b) the five canonical victims are green, **OR** every residual is captured verbatim and explicitly ROUTED to `144.005-T` / `144.006-T` / `144.007-T`.

The absolute `failures=0, errors=0` requirement lives at `144.007-T` only. A residual may be routed but NEVER masked, absorbed, silenced, skipped, or `expectedFailure`d. Do NOT widen this task to chase a residual — that would breach the 2-hour rule and the mechanism separation.

## Canonical invocation (R3)

```powershell
$env:PYTHONPATH = 'src'; python -m unittest discover -s tests
```

`pytest` MUST NOT be substituted.

## Acceptance

- AIG inventory equality recorded for all three touched modules.
- Linux/CI result unchanged.
- Every diff line cited to N1/N2/N3.