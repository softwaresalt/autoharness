---
artifact_type: task
created_at: 2026-08-04T23:38:22.6910078Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-two-axis-v1
    size_source: agent
id: 109.002-T
parent_id: 109-F
priority: medium
status: queued
title: 'A1: gate pipeline-topology subcommand skeleton, dispatch, usage, exit codes'
updated_at: 2026-08-05T00:34:58.6373156Z
---



<!-- BEGIN:acceptance-criteria -->
* `autoharness gate pipeline-topology --help` prints usage including the `--shipment <SHIPMENT_ID>` target flag.
* --json/--mode/--force/--shipment parse; unknown args -> exit 2.
* SHIPMENT-SCOPED CONTRACT: in a shipment-scoped operation (agent-mode pre-claim / route-eligibility / cursor-advance, and any explicit scoped invocation), `--shipment <SHIPMENT_ID>` is REQUIRED; a missing/empty target -> exit 2 (invalid), fail-closed, never fail-open.
* NON-SHIPMENT CONTEXT: hook/ci ambient invocations parse WITHOUT `--shipment` and run target-independent invariants; when a target is resolvable (see A4 resolution rule) it is consumed, otherwise ambient-only checks run fail-closed.
* The parsed target shipment ID is carried on the gate domain-input object consumed by the readiness/ownership/re-verify checks (A4/A3a/A5).
* The gate domain-input also carries an EXPLICIT `phase` (`pre_claim` | `post_claim`) driving the A2 phase-aware active-shipment invariant (pre-claim ZERO-active vs post-claim exactly-one-same-target); the phase is set explicitly by the caller/mode (pre-claim eligibility/route vs post-claim build/PR/closure), never silently inferred, and defaults fail-closed for shipment-scoped agent mode.
* Skeleton returns exit 0 with a structured (JSON-capable) report object that echoes the resolved target shipment (or null for ambient-only).
* Follows existing gates.gate dispatch conventions; no import of install/tune modules.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
[Shipment A / core] Add the `pipeline-topology` subcommand to `autoharness gate` dispatch in src/autoharness/cli.py: extend GATE_USAGE, wire dispatch in _gate_command, and add --json, --mode {agent|manual|ci}, --force, and a `--shipment <SHIPMENT_ID>` target flag plumbing. The parsed target shipment ID becomes part of the gate domain-input consumed by the shipment-scoped checks (readiness/ownership/re-verify). Define the fail-closed exit-code contract: 0 pass, 1 blocked, 2 invalid args/config. In agent shipment-scoped operation a missing/empty `--shipment` -> exit 2 (never proceed); ambient hook/ci invocations may omit it and run target-independent invariants.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Mirror _gate_check_command / GATE_USAGE structure. Module lives under src/autoharness/gates/ (e.g., topology.py) depending only on gates.* per the gate.py layering rule. Model the domain-input as an object carrying {mode, phase, target_shipment_id, json, force} so the scoped and phase-aware checks read from one place; centralize required-vs-optional target resolution so agent mode stays fail-closed while hook/ci stay ambient-deterministic. `phase` (pre_claim | post_claim) is an explicit field consumed by the A2 check (109.005-T) and the post-claim GLOBAL re-verify (109.001-T).
<!-- END:implementation-notes -->