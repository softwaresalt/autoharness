---
artifact_type: task
created_at: 2026-08-30T05:21:16.0392086Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: autoharness-2h-rule-v1
    size_source: agent
dependencies:
    - 150.004-T
id: 150.007-T
labels:
    - release
    - dry-run
    - quality-gates
parent_id: 150-F
priority: medium
references:
    - docs/plans/2026-08-29-v1_5_0-release-preparation-plan.md
    - docs/decisions/2026-08-29-markdownlint-enforcement-contract-for-v1_5_0-deliberation.md
status: queued
title: 'Release dry run B: full test suite, fail-closed markdown gate, verify-workspace/template/schema checks'
updated_at: 2026-08-30T05:21:16.0392086Z
---

RELEASE DRY RUN B — quality gates. Runs BEFORE the PR, alongside dry run A.

1. FULL EXISTING TEST SUITE (canonical gate — do NOT substitute a narrower run):
   PYTHONPATH=src python -m unittest discover -s tests
   Reference: the last recorded closure (157-S / 149-F) measured 2018 passing tests plus CLI runtime PASS. A materially lower count means tests were skipped or lost — investigate before proceeding.

2. MARKDOWN QUALITY GATE — per the RESOLVED POLICY in
   docs/decisions/2026-08-29-markdownlint-enforcement-contract-for-v1_5_0-deliberation.md (Option B):
   run markdownlint over the repository and require ZERO violations.
   *** IF THE markdownlint BINARY IS ABSENT, HALT. DO NOT SKIP. ***
   This is deliberately FAIL-CLOSED and is the exact inverse of the .githooks/pre-push.sh L29/L36 fail-open defect. The deliberation scoped this as a one-time procedural gate: do NOT add a CI job, do NOT change hook control flow, do NOT add scripts/pre-commit-markdownlint.* — all three are explicitly deferred, separate contract surfaces (P-021 C1).

3. VERIFY-WORKSPACE / TEMPLATE / SCHEMA CHECKS:
   - Run the workspace verification and confirm NO new checksum drift.
   - INV-5: the ONLY permitted checksum changes are the two deliberately re-recorded (circuit-breaker instruction, workspace-discovery SKILL.md). ANY other drift is a genuine finding and a STOP CONDITION — do not re-record it for convenience.
   - Template validity: YAML frontmatter valid, no unresolved {{...}} variables, cross-references resolve.
   - INV-4: no versioned schema mirror under schemas/ mutated in place.

STOP CONDITION: any failure halts the release. Do not open the PR.