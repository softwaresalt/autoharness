---
artifact_type: task
created_at: 2026-08-21T21:20:02.1359497Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 143.001-T
id: 143.002-T
labels:
    - bug
    - tests
    - isolation
    - remediation
    - conditional
    - always-terminating
parent_id: 143-F
priority: medium
references:
    - docs/plans/2026-08-21-full-suite-test-isolation-plan.md
    - docs/plans/2026-08-21-full-suite-test-isolation-hardening.md
    - .backlogit/queue/024-DL.md
status: queued
title: Remediate the confirmed polluter, or record an evidenced no-remediation disposition
updated_at: 2026-08-21T21:20:02.1359497Z
---

CONDITIONAL WORK, UNCONDITIONAL COMPLETION. The remediation is conditional on 141.001-T's recorded verdict, but this task has THREE terminal dispositions and EVERY ONE of them ends in `done` with recorded evidence. This task must never be returned `blocked`, and its shipment must never be abandoned.

**Plan:** `docs/plans/2026-08-21-full-suite-test-isolation-plan.md` Task 3b (+ amendments A1, A2R)
**Split from:** 141.005-T (archived; this task carries its conditional half — old AC8/AC9/AC10/AC10b/AC12)

## Why the old shape was un-executable (review-fix cycle 2, PR #386)

141.005-T said: if 141.001-T hard-stops, return this task BLOCKED and let the feature close partially. Per `.github/agents/_ship.agent.md:325-340`, Ship derives its executable task set by filtering the manifest to `-T` artifacts and then reading status: `queued`/`active` are KEPT, pre-archived is SKIPPED-AND-REPORTED, `done` is REPORTED, and **any other status is a FAIL-CLOSED HALT, never a skip**. The manifest is "the closure membership record — it is never the executable task set and is never mutated to make execution proceed". So a `blocked` member halts the entire shipment; there is no partial-close path. Backlogit 1.8.0 also has no shipment `blocked` status (`docs/compound/2026-05-07-backlogit-shipment-status-constraints.md`).

## Step 0 — PRECONDITION GATE (read-only; run first, record the result)

1. Read 141.001-T's recorded `VERDICT` (it is always one of `PAIR-ISOLATED` or `INCONCLUSIVE`; 141.001-T can no longer terminate any other way).
2. If `PAIR-ISOLATED`: re-run the exact minimal reproducing command recorded under 141.001-T AC1 at THIS shipment's head, and re-run the same command minus the polluter.
3. Select the disposition from the observed result — never from prose, never from memory:
   - Reproducer still FAILS -> **R1**
   - Reproducer now PASSES -> **R2**
   - Verdict was `INCONCLUSIVE` -> **R3**
4. Record the selected disposition and the verbatim command output on this task record BEFORE making any edit.

## R1 — remediate the confirmed polluter
1. Remediate AT ITS OWN SOURCE — fixture/teardown scoping in the POLLUTING test. Do NOT modify any victim's assertions.
2. Cite 141.001-T's minimal reproducing pair in the change description. **No source edit is permitted in any branch without this citation** (feature AC-G5) — this is the structural replacement for the old "hard stop" and a speculative fix cannot satisfy it.
3. Verify AC-1, AC-2, AC-3, AC-4 below.

## R2 — isolated, but no longer reproduces at this head
1. Make NO source edit under `tests/`.
2. Record: the 141.001-T pair, the now-passing command output, and the intervening change most likely responsible (141.002-T–141.004-T anchor work, or 150-S).
3. Re-measure the canonical gate and record the result. Verify AC-2, AC-4.

## R3 — no polluter was isolated
1. Make NO source edit under `tests/`. **There is no speculative-fix path.**
2. Re-measure the canonical gate: `$env:PYTHONPATH='src'; python -m unittest discover -s tests`.
3. If GREEN: record that the 141.002-T–141.004-T anchor work resolved the defect, with the run output.
4. If STILL RED: record the residual failure set AND 141.001-T's narrowed candidate set, then capture a NEW deferred stash entry under P-021 C2 carrying both, plus this task ID, 141.001-T, 143-F and this shipment ID as source refs. Verify AC-5.

## Acceptance criteria
- AC-0. The selected disposition (R1 / R2 / R3) is recorded with the verbatim Step 0 command output. An unrecorded disposition is not acceptable, including a null result.
- AC-1. (R1 only) The canonical full-suite gate is GREEN on Windows: ZERO failures, ZERO errors (skips unchanged). `$env:PYTHONPATH='src'; python -m unittest discover -s tests`. (Old 141.005-T AC8.)
- AC-2. (R1 only) The minimal reproducing command from 141.001-T AC1 now PASSES. (Old AC9.)
- AC-3. All five victim tests retain their original assertions VERBATIM — demonstrated by `git diff`: either no change at all, or changes confined to `setUp`/`tearDown`/imports with ZERO assertion-line edits. Binding in EVERY disposition. (Old AC10.)
- AC-4. (amendment A1) The five victims are additionally re-run IN ISOLATION with identical pass/fail semantics, and the verbatim diff of the five victim files is attached to this task record. Binding in EVERY disposition. (Old AC10b.)
- AC-5. (R3-still-red only) A new deferred stash entry exists carrying the residual failure set and the narrowed candidate set, with source refs recorded.
- AC-6. Hosted CI remains green (no Linux regression). (Old AC12.)

## Anti-goal (the single most dangerous outcome available here)
The cheapest path to a green suite is to relax `test_root_tracked_json_matches_allowlist` (itself a 133-F/142-S regression guard), broaden the telemetry gitignore assertions, or delete a victim. Every one of those turns the gate green while destroying the protection the gate exists to provide. AC-3/AC-4 exist specifically to make that path detectable, and they bind in all three dispositions.

## Scope guard (024-DL R5 / P-021 C1)
`tests/` surface only. If remediation implicates production code under `src/autoharness/`, that is a DIFFERENT contract surface — STOP and re-enter P-021 capture.