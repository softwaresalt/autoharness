---
artifact_type: task
created_at: 2026-08-05T00:13:48.4780047Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-two-axis-v1
    size_source: agent
id: 109.016-T
labels:
    - topology
    - ship-agent
    - reconcile
    - safe-close
    - p-015
    - p-001
parent_id: 109-F
priority: medium
status: queued
title: 'A7: make shipment-reconcile safe-close protected-set sequence-aware for multi-shipment-per-feature'
updated_at: 2026-08-05T02:08:56.1883191Z
---



<!-- BEGIN:acceptance-criteria -->
* CLOSE-CONTRACT (SHIP-THEN-RECONCILE, NOT ATOMIC): `backlogit_ship_shipment` may mutate shipment status and/or archive state before returning either success OR error. Ship MUST NOT describe or rely on the call as atomic from the caller's view.
* After EITHER a success return OR an error return, Ship immediately re-reads BOTH live and archived shipment state/provenance for the current shipment. It proceeds only after a successful call plus one coherent archived result with `archived_status: shipped` (or normalized legacy `done`) and no contradictory live residue.
* On any error return, missing/ambiguous state, live+archived duplication, live `shipped` without the expected archive, absent/wrong archived provenance (`queued|active|blocked|abandoned`), or other partial mutation, Ship fails closed with a remediation/reconciliation token. It does not assume rollback, hand-archive, or continue closure merely because one mutation appears complete.
* Given a covering feature spanning multiple serial shipments, a sibling task is excluded from the protected set only when its owning predecessor shipment has the verified coherent successful archived provenance above. Mere archive presence is never sufficient.
* Siblings belonging to queued/active/blocked shipments, unassigned siblings, siblings of archived non-shipped shipments (including `abandoned`), and siblings with missing/ambiguous provenance remain protected in `.backlogit/queue/`; genuine cross-cascades still halt with P-005.
* Deterministic 114->115->116 success tests prove serial close after each predecessor's successful post-call reconciliation. Negative tests cover: error after live status mutation, error after archive mutation, simultaneous live+archived residue, missing archive, archived `active`, archived `abandoned`, and missing provenance; all fail closed and keep affected siblings protected.
* Change is confined to the Ship safe-close/`shipment-reconcile` protected-set + closure-sequence contract in templates/agents/_ship.agent.md.tmpl and installed .github/agents/_ship.agent.md; no backlogit source is mutated.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
[Shipment A / core — sequencing prerequisite] Correct two coupled Ship safe-close contracts for multi-shipment-per-feature sequences. First, treat `backlogit_ship_shipment` as a potentially partially mutating operation: after success OR error, re-read live and archived state/provenance; continue only after a successful call and a coherent archived `archived_status: shipped|done` result with no contradictory live residue. Any error or partial/ambiguous state fails closed for remediation. Second, make the protected-set computation sequence- and terminal-status-aware: exclude predecessor siblings only when that verified successful archived result exists; all non-shipped, missing, or ambiguous cases remain protected. Ship in 114-S so this contract is installed before 115-S/116-S close; P-015 current-shipment-only behavior remains intact.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Do not call ShipShipment atomic and do not assume an error implies rollback. The operation may transition status and/or archive records before a later error is returned. In a finally-style reconciliation step after every return path, query both live and archive locations for the current shipment and classify the observed state. Success requires: call returned success, no live record remains, exactly one archived record exists, and its provenance is `archived_status: shipped` (or normalized legacy `done`). Every other state fails closed with an explicit remediation token and preserved evidence; do not hand-archive or retry blindly. Owning-shipment resolution scans manifests across live and archive records, but predecessor siblings are excluded from the protected set only after this coherent successful terminal reconciliation. Preserve the existing genuine-cascade halt and P-015 no-cascade rule. Add deterministic success fixtures and partial-mutation/error fixtures for both status-first and archive-first failure paths.
<!-- END:implementation-notes -->