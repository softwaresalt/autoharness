---
artifact_type: task
created_at: 2026-08-22T08:18:49.7540592Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 144.004-T
    - 144.006-T
id: 144.007-T
labels:
    - proof
    - full-suite
    - verification
    - mechanism-a
parent_id: 144-F
priority: medium
references:
    - docs/plans/2026-08-22-git-config-env-containment-plan.md
    - docs/plans/2026-08-22-git-config-env-containment-hardening.md
    - .github/workflows/ci.yml
status: queued
title: 'Full-suite proof: canonical Windows + Linux/CI green, environment restoration and skip-set verified'
updated_at: 2026-08-22T08:18:49.7540592Z
---

**PROOF ONLY — no production edit. This is where `failures=0, errors=0` is enforced (R2).**

Plan: `docs/plans/2026-08-22-git-config-env-containment-plan.md` Task 7. Amendments A8, A9, R1, R3 BINDING.

## Six required proofs

1. **Canonical Windows run.** `$env:PYTHONPATH = 'src'; python -m unittest discover -s tests`. Record the verbatim tail. REQUIRED: `failures=0, errors=0`.

2. **Repetition/order stability.** Run the canonical gate a SECOND time in the SAME shell session and confirm identical results — proving the fix is order- and repetition-stable, not a one-shot artifact of a fresh environment block.

3. **A8 (BINDING) — environment-restoration proof, exact method:**

```
probe := python -c "import os,json,sys; sys.stdout.write(json.dumps(dict(os.environ), sort_keys=True))"
a. before := run probe as a child with env=None -> capture stdout
b. run the canonical gate to completion
c. after  := run probe as a child with env=None -> capture stdout
d. assert before == after (BYTE equality)
e. additionally assert, BY EXPLICIT KEY, that GIT_CONFIG_COUNT and every
   GIT_CONFIG_KEY_n / GIT_CONFIG_VALUE_n present in `before` is present in
   `after` with an identical value
```

Step (e) is SEPARATE from (d) because a whole-block comparison can be satisfied by two blocks that are BOTH missing the variable. Use a child probe, NOT `os.environ` — the whole defect is that `os.environ` and the real Win32 block diverge.

4. **Linux/CI parity.** CI green on the PR with no new skips.

5. **No global/system mutation.** `git config --global --list` and `git config --system --list` byte-identical before and after.

6. **R1 (BINDING) — victim #1 both-module check.** Assert BOTH `tests/test_gate_dag_readiness_cli.py::test_backlog_only_workspace_succeeds` AND `tests/test_gate_pipeline_topology_cli.py::test_backlog_only_workspace_succeeds` are green, regardless of which `144.001-T` pinned as the original victim.

## A9 (BINDING) — named skip-SET, not a count bound

Record the NAMED SET of skipped tests before and after. Baseline is 20 skips. The post set MUST be a SUBSET of the pre set, plus at most the git-unavailable skip in `tests/test_environ_restore_contract.py`, which must be enumerated explicitly and MUST NOT trigger on a machine where `shutil.which("git")` is non-None. A bare count bound is insufficient: it permits a SUBSTITUTION (a newly skipped real test offset by a no-longer-skipped one). ANY newly skipped canonical test is a HALT.

## R3 (BINDING)

`pytest` MUST NOT be substituted for any measurement here.

## Mechanism separation (BINDING)

Hand any residual to `145.001-T`. Do NOT absorb a mechanism-B residual into `144-F`, and do NOT record a mechanism-B disposition here.

## Acceptance

All six proofs recorded verbatim on the task. The feature closes ONLY on `failures=0, errors=0`.