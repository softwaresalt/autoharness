---
artifact_type: task
created_at: 2026-08-10T07:10:40.4922818Z
custom_fields:
    complexity: low
    origin_feature: 117-F
    size: S
    size_ruleset_version: 2h-width-isolated-v1
    size_source: agent
dependencies:
    - 118.003-T
id: 120.003-T
labels:
    - shipment-3
    - P1
    - resolve
    - argv
    - app-service
    - H2
    - H4
parent_id: 120-F
priority: high
references:
    - start.ps1
    - start.sh
    - docs/plans/2026-08-09-copilot-cli-supervisor-control-plane-plan.md
status: queued
title: 'T14 supervise/resolve.py: Copilot CLI resolution + argv composition'
updated_at: 2026-08-10T08:11:48.7102388Z
---

SIZE: S. COMPLEXITY: low. (Two independent axes; also emitted as structured backlog fields.) Shipment 3 (P1). Create src/autoharness/supervise/resolve.py owning Copilot CLI resolution and argv composition currently duplicated in start.ps1 and start.sh. Resolution order is exactly COPILOT_EXE_PATH -> COPILOT_EXE (backward compatibility) -> `copilot` on PATH; when none resolves, FAIL FATALLY with the current actionable message and NEVER fall back to a guessed path (H2). Argv composition appends --remote ONLY when COPILOT_USE_REMOTE is 'true' or '1' (case-insensitive) AND the operator did not already pass --remote. F17 NOTE - ON POSIX THIS IS A DELIBERATE BEHAVIOR CHANGE: --remote/COPILOT_USE_REMOTE exists ONLY in start.ps1 today and is entirely absent from start.sh, so unifying it here GIVES POSIX A CAPABILITY IT DID NOT PREVIOUSLY HAVE. That is legitimate in Shipment 3, the only behavior-changing shipment, but it must be called out in the 120.008-T migration notes and must NEVER be back-ported into the Shipment 1 characterization suites, whose job is to pin today's asymmetry. Composition then appends the operator's arguments VERBATIM - never re-parsed, re-quoted, joined, filtered, or reordered (H4). Emits a CopilotResolved event. Pure resolution + composition: this module does not spawn. Returns a typed ResolveResult carrying the absolute executable path and the composed argv list. Tests cover the full resolution precedence matrix, the --remote truthiness table including the double-add guard, verbatim passthrough of arguments containing spaces/quotes/leading dashes, and the unresolvable-executable fatal path.