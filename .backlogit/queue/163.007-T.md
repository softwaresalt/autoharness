---
artifact_type: task
created_at: 2026-09-08T07:43:23.8828409Z
custom_fields:
    complexity: low
    size: XS
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 163.005-T
    - 163.006-T
    - 163.008-T
id: 163.007-T
labels:
    - checksum
    - harness-manifest
    - green
parent_id: 163-F
priority: medium
status: queued
title: 'U5: recompute the single shipment-reconcile harness manifest checksum and GREEN the full suite'
updated_at: 2026-09-11T00:30:59.634383Z
---

U5: recompute THE SINGLE harness manifest checksum for the shipment-reconcile skill and GREEN the full suite.

THE CHECKSUM CONTRACT (CORRECTED BY AMENDMENT R1-14, 2026-09-10 - THIS PARAGRAPH GOVERNS OVER EVERY EARLIER STATEMENT OF THE CHECKSUM SET IN THIS TASK, INCLUDING THE R1-11 NOTE AND THE R1-12 AND R1-13 RESTATEMENTS BELOW): recompute the `checksum:` field of THE SINGLE .autoharness/harness-manifest.yaml artifact entry whose `path:` key is `.github/skills/shipment-reconcile/SKILL.md`. RESOLVE THAT ENTRY BY ITS PATH KEY, NEVER BY LINE NUMBER. That one field is the ONLY applicable manifest checksum target for this task. templates/skills/shipment-reconcile/SKILL.md.tmpl is edited by U2/U6 and its bytes matter for dogfood parity, but it appears in the manifest ONLY as that same entry's `template:` VALUE - it has NO manifest entry and NO checksum of its own, so editing it produces NO manifest mutation. EXPLICITLY FORBIDDEN: do NOT add, remove, rename, reorder or re-checksum ANY OTHER manifest entry. The closure diff MUST show EXACTLY ONE changed `checksum:` value. Then run the full suite green.

SCOPE: this feature's edits only - do NOT absorb 169-S/161-F's own checksum residual. Ordering is deterministic because this shipment carries a blocks dependency on 169-S. Depends on U3 and U4 because it consumes every prior edit's bytes. Plan: docs/plans/2026-09-08-shipment-reconcile-step-0c-live-pre-mutation-evidence-plan.md U5 and section R1-14.

--- AMENDMENT R1-14 (2026-09-10, review-fix cycle 4, operator-authorized extension; plan section "R1-14") --- THE PRIOR CHECKSUM CONTRACT WAS FACTUALLY FALSE AND IS REMOVED FROM THE LOAD-BEARING PATH.
(1) THE DEFECT. Every earlier statement in this task - the original description, the R1-11 note, and the restatements in R1-12(2) and R1-13 - asserted a checksum set of "the five shipment-reconcile entries at L126, L201, L272, L274, L396". Direct inspection of .autoharness/harness-manifest.yaml on 2026-09-10, parsed as YAML across all 72 artifact entries rather than grepped, refutes that claim: EXACTLY ONE entry has a `path:` key containing "shipment-reconcile", namely .github/skills/shipment-reconcile/SKILL.md. The manifest contains ZERO entries whose `path:` begins with "templates/", so the .tmpl has no entry at all. L272 and L274 are TWO FIELDS OF THAT SAME SINGLE ENTRY (its `path:` key and its `template:` value), not two entries. L126, L201 and L396 are `note:` PROSE LINES BELONGING TO THREE UNRELATED ARTIFACTS - .github/agents/_ship.agent.md, .github/skills/file-lock/SKILL.md and .github/policies/workflow-policies.md respectively - which merely MENTION the string "shipment-reconcile" in their notes.
(2) WHY THIS MATTERED. The "five entries" were five GREP HITS for the string "shipment-reconcile", mistaken for five manifest entries. Followed literally, the instruction would have directed the implementer to recompute checksums on THREE UNRELATED ARTIFACTS - the Ship agent definition, the file-lock skill and the workflow-policies registry - corrupting their integrity records while leaving the real target ambiguous. This is a correctness defect, not a wording nit.
(3) NO LINE NUMBERS, EVER. Line numbers in this manifest are unstable: every amendment to a neighbouring `note:` shifts them, which is exactly how the false contract survived three review cycles undetected. The entry is addressed by its stable `path:` key only.
(4) UNCHANGED BY R1-14: U5 still runs LAST and still consumes every prior edit's bytes; DEPENDENCIES UNCHANGED (163.005-T, 163.006-T, 163.008-T); SIZE UNCHANGED (XS / low); no new file and no new harness-managed artifact is added by any unit of this feature; and .backlogit/logs/ and .backlogit/reconcile/ remain WORKSPACE STATE that MUST NOT be added to .autoharness/harness-manifest.yaml in any tracking case.
(5) SUPERSESSION MARKERS. The three superseded statements below are retained ONLY as history behind unmistakable markers. NONE of them is an instruction. Where any of them describes the checksum SET, the contract paragraph at the top of this task governs.

--- NOTE R1-11 (2026-09-10, review-fix cycle 1; plan section "R1-11", decision section "D-7") --- [TWO SUPERSESSIONS APPLY TO THIS NOTE. (a) THE "force-tracks" PHRASING IS SUPERSEDED BY AMENDMENT R1-12(3), WHICH GOVERNS. (b) THE CHECKSUM-SET SENTENCE NAMING "the five shipment-reconcile skill/template entries" IS FALSE AND IS SUPERSEDED BY AMENDMENT R1-14, WHICH GOVERNS - THERE IS ONE PATH-KEYED ENTRY, NOT FIVE. RETAINED AS HISTORY, NOT AS AN INSTRUCTION. THE CONCLUSION - no new checksum entry, no new file - IS UNAFFECTED BY EITHER SUPERSESSION.] The R1-11 L2 durability obligation adds NO new checksum entry and NO new file: it force-tracks an engine log that the close already writes (.backlogit/logs/{shipment_id}.jsonl), which is workspace state, not a harness-managed artifact, and is therefore OUT OF SCOPE for the harness-manifest recompute. [SUPERSEDED SENTENCE, HISTORY ONLY: "The checksum set for this task is unchanged - the five shipment-reconcile skill/template entries above, reflecting the U2/U3 text edits." Read instead: the checksum set is unchanged and is THE SINGLE path-keyed entry .github/skills/shipment-reconcile/SKILL.md, reflecting the U2/U3 text edits.] Do not add .backlogit/logs/ or .backlogit/reconcile/ paths to the manifest.

--- AMENDMENT R1-12 (2026-09-10, review-fix cycle 2; plan section "R1-12", redesign decision D-7/D-8) --- NEW DEPENDENCY AND CORRECTED L2 WORDING. THE CHECKSUM SET IS STILL UNCHANGED.
(1) NEW BLOCKING DEPENDENCY: this task now also depends on 163.008-T (plan unit U6, the POST-CASCADE PUBLICATION step). U6 is a paired prose edit to the SAME two files this task's unit consumes, so U5 must still run LAST and consume its bytes. Dependencies are now 163.005-T, 163.006-T and 163.008-T.
(2) CHECKSUM SET UNCHANGED - EXPLICITLY RE-CONFIRMED. U6 adds NO new file and NO new harness-managed artifact: it adds contract text to .github/skills/shipment-reconcile/SKILL.md and templates/skills/shipment-reconcile/SKILL.md.tmpl. [SUPERSEDED PARENTHETICAL, HISTORY ONLY, NOT AN INSTRUCTION: this clause previously described those files as "ALREADY in this task's recompute set (the five shipment-reconcile entries at L126, L201, L272, L274, L396)". THAT ENUMERATION IS FALSE - SEE R1-14, WHICH GOVERNS. Read instead: the SKILL.md is the single path-keyed manifest entry this task recomputes, and the .tmpl has no manifest entry or checksum at all.] No manifest entry is added, renamed or removed by R1-12.
(3) CORRECTED WORDING IN THE R1-11 NOTE ABOVE. That note says the L2 obligation "force-tracks an engine log that the close already writes". That phrasing rests on a premise direct inspection refutes: origin/main's .gitignore contains NO .backlogit/logs/ rule; git ls-files lists 993 of 1016 local logs as ALREADY TRACKED, including 150-S, 159-S, 169-S and 171-S itself; the ignore rule exists only as an UNCOMMITTED working-tree edit; and a .gitignore rule NEVER hides an already-tracked path. Read it instead as: L2 PUBLISHES the shipment's own engine log into the closure commit by whichever staging action its tracking state requires (Case A already tracked - ordinary commit; Case B untracked and not ignored - ordinary add; Case C untracked and ignored - bounded force-add). THE CONCLUSION THIS NOTE DRAWS IS UNAFFECTED: .backlogit/logs/ and .backlogit/reconcile/ are WORKSPACE STATE, not harness-managed artifacts, and MUST NOT be added to .autoharness/harness-manifest.yaml in any tracking case.
(4) PROPAGATION-LIST CORRECTION. This task was omitted from the R1-11 propagation lists in the plan, in redesign decision D-7 and in 033-DL, despite already carrying an R1-11 note. It is now listed in all three, together with 163.008-T.
(5) SIZING UNCHANGED: XS / low. The added dependency edge changes ordering, not scope.

--- AMENDMENT R1-13 (2026-09-10, review-fix cycle 3; plan section "R1-13", redesign decision D-7/D-8) --- NO CHANGE TO THIS TASK'S CHECKSUM SET, DEPENDENCIES OR SIZE; RECORDED FOR TRACEABILITY. R1-13 corrects the L2 fidelity criterion to the APPEND-ONLY PREFIX RULE (unchanged pre-existing bytes as the prefix of the committed log; the engine's post-gate mutation lines are EXPECTED append-only extensions; any modification, deletion, truncation or reordering of pre-existing bytes is a failure), adds the U1a LATE_BUT_ANCHORED positive fixture variant and its U1b assertion, separates U6's implementer and runtime-actor roles, and defines the missing-evidence remediation path (follow-up closure commit before P-001 release-unit completion; never an amend of published history). NONE of that adds a file or a harness-managed artifact: U6 and U2 still edit only .github/skills/shipment-reconcile/SKILL.md and templates/skills/shipment-reconcile/SKILL.md.tmpl. [SUPERSEDED PARENTHETICAL, HISTORY ONLY, NOT AN INSTRUCTION: this clause previously described those two files as "both already in this task's recompute set (the five shipment-reconcile entries at L126, L201, L272, L274, L396)". THAT ENUMERATION IS FALSE - SEE R1-14, WHICH GOVERNS. Read instead: only the SKILL.md carries a manifest entry, addressed by its path key; the .tmpl carries none.] The U1a/U1b work stays inside tests/. .backlogit/logs/ and .backlogit/reconcile/ remain WORKSPACE STATE and MUST NOT be added to .autoharness/harness-manifest.yaml in any tracking case. DEPENDENCIES UNCHANGED: 163.005-T, 163.006-T, 163.008-T. SIZE UNCHANGED: XS / low.