---
artifact_type: task
created_at: 2026-08-05T23:51:01.1547284Z
custom_fields:
    complexity: low
    size: XS
    size_ruleset_version: 1.0.0
    size_source: agent
dependencies:
    - 112.001-T
id: 112.003-T
parent_id: 112-F
priority: medium
status: queued
title: 'B4: docs for operator-only repair mode (preconditions, audit, permanent exclusions)'
updated_at: 2026-08-06T04:50:00Z
---



<!-- BEGIN:acceptance-criteria -->
1. Documents the mode, all preconditions — INCLUDING that the torn-state refusal uses a PER-TASK ALLOWED-STATE ROLE classification over a REACHABLE mixed-role target: each manifest task must be a UNIQUE, NON-CONFLICTING record for its role — a `queued`/`active` task is a UNIQUE LIVE QUEUE record (`status: queued`/`active`, no archive copy), and a COMPLETED task is a UNIQUE ARCHIVE record in EITHER of backlogit's two valid archive representations — (a) `status: done` in `archive/` (no explicit provenance required) OR (b) `status: archived` + `archived_status: done` with valid `archived_from` provenance — both with NO conflicting live queue record (NEVER a live `status: done` in the QUEUE); FAIL CLOSED on any duplicate/conflicting/missing/malformed-provenance/any-other-archived-status/orphan/out-of-role anomaly — NOT a single-`expected_status` all-`matched` gate (which is UNREACHABLE for the mixed-role target it repairs — publication-review P2 2026-08-06 / 013-DL Addendum D + publication-review P1 2026-08-06 / 013-DL Addendum E + publication-review P1 2026-08-06 / 013-DL Addendum F (two valid archive representations)) — the --confirm operator gate, and the audit trail. 2. Documents the RESIDUAL TOCTOU (best-effort DETECTION, not prevention; no locks/CAS/serialization; the inspection->re-claim window is the same accepted 109-F TOCTOU) and the POST-CONDITION fail-closed remediation: on post-claim two-active, the mode audits + halts + surfaces OPERATOR remediation via backlogit's sanctioned lifecycle and NEVER attempts an active->queued rollback (forbidden by backlogit 1.8.0). Explicitly states the mode is NOT "provably safe". Documents that the re-claim is STRICTLY SINGLE-SHOT (NOT idempotent) — a repeat/racing claim on an already-`active` shipment is rejected with ErrShipmentConflict (the double-claim guard), never a silent re-activation — and that backlogit 1.8.0 has NO `blocked` shipment status: a legacy/malformed `blocked` record is classified as MALFORMED LEGACY DATA and HALTED for operator manual remediation, never a fabricated blocked->queued transition. 3. Explicitly records the PERMANENT exclusion of full automatic/silent self-repair and of backward-repair (return-tasks-to-queued stays manual/operator). 4. Cross-refs 013-DL (incl. Addendum A + Addendum B + Addendum C + Addendum D + Addendum E + Addendum F) and docs/compound/2026-08-01-shipment-record-status-integrity.md. 5. Notes the external backlogit active->queued guard is resolved-upstream (1.8.0).
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Document the operator-only repair-record-status mode.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on B1. Docs-only; no code.
<!-- END:implementation-notes -->