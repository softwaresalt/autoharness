---
artifact_type: task
created_at: 2026-09-19T01:02:05.0699223Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 172.001-T
id: 172.014-T
labels:
    - test
    - tdd
    - red-phase
    - inventory
    - checkpoint
parent_id: 172-F
priority: high
status: queued
title: 'Resume-hint T8a (RED): typed producer-inventory and marker-bijection tests observed failing'
updated_at: 2026-09-19T01:02:05.0699223Z
---

TDD RED PHASE - TYPED PRODUCER INVENTORY AND BOUNDED MARKERS. Author the contract tests that REPLACE the broad free-text scan for raw operation names, and OBSERVE THEM FAILING before the inventory exists.

WHY THE TEXT SCAN IS WRONG. A repository-wide grep for backlogit_create_checkpoint / backlogit checkpoint create cannot distinguish a PROHIBITION DOCUMENT that must name the raw operation in order to forbid it from an ACTUAL PRODUCER DECLARATION that reaches it. The contract text, the policy instruction, and these very tests all name the operation legitimately. A scan that flags them is unusable; a scan tuned to ignore them silently stops detecting real producers.

THE REPLACEMENT: a TYPED INVENTORY plus BOUNDED MACHINE MARKERS. Assert the shape of schemas/checkpoint-producer-inventory.json: each entry declares a surface path, its producer kind, and whether it is raw-allowed. Assert the EXACTLY-ONE-RAW-ALLOWED rule - precisely one inventory entry (the adapter implementation) may reach the raw create call, and a second raw-allowed entry is a hard failure.

MARKER-TO-INVENTORY BIJECTION. Assert that the three bounded markers placed at real producer sites correspond one-to-one with inventory entries: a marker with no inventory entry fails, and an inventory entry with no marker fails. Bijection is what makes the inventory self-maintaining instead of a list that silently rots.

THE DISCRIMINATING CASE (REQUIRED): a fixture in which a PROHIBITION REFERENCE occurrence - a document naming the raw operation solely in order to forbid it - is PROVEN NOT TO BE A VIOLATION, alongside a SYNTHETIC THIRD PRODUCER fixture carrying a real marker with no inventory entry that MUST fail. A verifier that cannot be made to fail proves nothing, and a verifier that cannot tell these two apart is the defect being removed.

IMPORT SAFETY (BINDING): this module imports cleanly under unittest.defaultTestLoader with zero loader.errors and zero _FailedTest placeholders; not-yet-existing boundaries are loaded INSIDE the test body through a _load_boundary() helper. Every declared test individually executes and fails with its own declared marker in its own detail_text.

Blocked by 172.001-T. It DOES NOT depend on 172.015-T. Blocks 172.015-T. Scope: tests/. Stash 2A7C48A8. Plan docs/plans/2026-09-17-checkpoint-resume-hint-contract-plan.md (revision 7). Verdict manifest docs/reviews/2026-09-17-checkpoint-resume-hint-contract-plan-review.md (latest_attempt 07, verdict REMEDIATED-PENDING-REVIEW at plan revision 7, awaiting independent attempt 08).