---
artifact_type: task
created_at: 2026-09-17T19:59:22.4244136Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 167.002-T
    - 167.007-T
id: 167.008-T
labels:
    - closure
    - tests
    - adversarial
    - fail-closed-design
    - pipeline-topology
parent_id: 167-F
priority: medium
references:
    - docs/plans/2026-09-17-closure-evidence-naming-contract-plan.md
    - docs/bugs/2026-09-06-closure-evidence-producer-consumer-contract-mismatch.md
status: queued
title: Add adversarial regression tests proving the widened read set never weakens the closure validity predicate
updated_at: 2026-09-17T22:08:32.3921851Z
---

PLAN UNIT U8 (plan revision 5). Domain: tests. Posture: test-first.

FILES (1): tests/test_gates_topology.py (EXTENDED).

FIXTURE POLICY (Contract Specification C6)
ALL fixtures are TEMPORARY. R1 names come from build_closure_path(); R2 names come from tests/_closure_legacy_names.py::legacy_closure_filename, the single test-only legacy filename helper created by 167.007-T. The canonical builder cannot emit an R2 name, so builder creation of R2 fixtures is explicitly NOT required. docs/closure/ is NEVER read or written.

TESTS (4) — these four shapes are REQUIRED and may NOT be substituted
1. DATE-PREFIXED NAME, ACCEPTABLE CONTENT — the shape that silently failed for 001-S, 008-S, 162-S, 174-S — now resolves True and is reported as a LEGACY match, not a canonical one.
2. RECOGNIZED NAME, MISSING FRONTMATTER — still raises BacklogUnavailableError -> BACKLOG_UNAVAILABLE (the 005-S shape). Fail-closed behaviour is unchanged.
3. RECOGNIZED NAME, compaction_status WITHOUT closure_status — still fails closed (the second 005-S axis; the predicate is unchanged).
4. ADVERSARIAL ID COLLISION, BOTH PATTERNS — an artifact for 16-S must NOT satisfy closure_complete("162-S"), and an artifact for 162-S must NOT satisfy closure_complete("16-S"), under BOTH R1 and R2. This MUST include the FOREIGN FREE-FORM SUFFIX case: a record whose designated-position shipment ID is 16-S but whose free-form suffix contains the token 162-S (for example, an R2 name of the shape 2026-09-11-16-S-supersedes-162-S-closure.md) is NOT attributed to 162-S, because attribution parses the identifier from the designated filename position under the anchored grammar and compares only that parsed group — it never searches the suffix.

ACCEPTANCE
- All four shapes present; none substituted.
- Scenario 4 asserts both directions AND the foreign-suffix case.
- No scenario reads, globs, or asserts against docs/closure/ (D8).
- legacy_closure_filename is imported, not redefined.

DEPENDS ON: 167.002-T, 167.007-T (for the legacy filename helper).