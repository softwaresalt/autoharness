---
artifact_type: task
created_at: 2026-09-03T08:23:47.7285141Z
custom_fields:
    complexity: high
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 160.002-T
labels:
    - spike
    - plugin
    - packaging
    - blocking
parent_id: 160-F
priority: medium
status: queued
title: 'Spike: establish the Copilot plugin-channel payload trimming mechanism'
updated_at: 2026-09-03T17:15:00.8388499Z
---

BLOCKING SPIKE (T1). Establish how the Copilot plugin channel payload can actually be constrained before any plugin wiring begins.

The plan assumes the plugin payload can be trimmed, but the only observed mechanism is `.github/plugin/marketplace.json`'s `source: "."`, and no evidence establishes that Copilot CLI supports an exclusion or allowlist for plugin sources.

Determine which branch holds:
(a) marketplace source supports an exclusion/allowlist mechanism -> wire directly in the plugin-channel task 160.007-T; or
(b) it does not -> 160.007-T must instead materialize a TRACKED payload directory `plugin-payload/` from the manifest and repoint `source` at it; or
(c) neither (a) nor (b) is demonstrably supported -> HALT TO THE OPERATOR with the evidence. Do NOT invent or assume marketplace behaviour that the evidence does not show.

DURABLE FINDING DESTINATION (authorized, binding). The finding is written to docs/spikes/2026-09-03-ship10-plugin-channel-mechanism.md - the path named for this task in the SHIP-10 plan H2 writable-surface table. A blocking spike whose determination gates 160.007-T cannot leave that determination in a transcript: 160.007-T, 160.005-T's test_plugin_source_path_is_tracked_and_fetchable, and the plan's AC3c all cite it, and a citation needs something to cite.

CONSUMER-FETCHABILITY CONSTRAINT (AC3c, BINDING ON EVERY BRANCH): whatever `source` the spike selects MUST resolve to a path that a CONSUMER can actually fetch from a repository checkout. A `dist/plugin/`-style build-output path is NOT valid: `dist/.gitignore` contains `*`, so such a directory is untracked and absent from every consumer checkout - a consumer installing the plugin would resolve `source` to nothing and the install would simply fail. The spike MUST NOT select a build-output path, and MUST NOT propose an unpublishable strategy on the assumption that some later step will publish it.

BRANCH (b) CONSEQUENCES THE SPIKE MUST RECORD IF IT SELECTS (b):
- The materialized payload directory is `plugin-payload/` and is TRACKED, not gitignored. Tracked-ness is the whole point: it is what makes `source` fetchable from a plain `git clone`.
- `plugin-payload/` IS THE PLUGIN CHANNEL'S GENERATED OUTPUT ROOT, NOT A CLASSIFIER INPUT (plan AC3d). It is declared in the manifest's `generated_output_roots` key and is NOT an `include` rule in ANY channel. THE DISTINCTION IS LOAD-BEARING, NOT COSMETIC: the plugin generator materializes INTO that tree, so an `include` rule naming it would make the generator's own output part of its next run's input - the second run would resolve `plugin-payload/**` as source and write it INSIDE `plugin-payload/`, yielding `plugin-payload/plugin-payload/...`, and every further run would nest again. `generate --check` would then report drift that regenerating can never clear, because each regeneration deepens the tree. That is PERMANENT DRIFT and it fails the release gate on every subsequent run.
- Paths under a declared output root are REMOVED FROM THE TRACKED-PATH ENUMERATION BEFORE classification, so generated content is neither classified nor unclassified and never trips AC2's unclassified-path build failure. The removal applies to both in-scope channels, which is also what keeps the tree out of the wheel (it is a derived duplicate of payload content, not source).
- EXCLUSION FROM CLASSIFIER INPUT DOES NOT MEAN EXCLUSION FROM PUBLICATION. The tree stays tracked and committed and remains the directory `marketplace.json`'s `source` points at. Input role and output role are different properties of the same path.
- The materialization step must invoke the single generation command owned by 160.014-T rather than reimplementing resolution. (160.004-T owns the resolver; 160.014-T owns the generation command that drives it.)
- Because the directory is tracked and derived, it inherits H1's generate-and-assert contract exactly as pyproject.toml and marketplace.json do: `generate --channel plugin --check` must detect drift between the committed `plugin-payload/` tree and the manifest, and CI must run it.
- The AC2b release-path gate owned by 160.015-T must cover the plugin channel via `generate --channel all --check`, where `all` means every channel the manifest's closed enum declares - currently wheel and plugin.
- OVERWRITING OR REMOVING TRACKED CONTENT IN `plugin-payload/` IS DESTRUCTIVE AND REQUIRES FRESH LIVE OPERATOR APPROVAL (Constitution Principle VII; plan H2 "Principle VII generation rule"; CORRECTED IN EXTENDED REVIEW-FIX CYCLE 4, FINDING 3). THE WITHDRAWN CYCLE-3 WORDING - "regeneration through the single generation command is the normal, NON-DESTRUCTIVE path; any restore or overwrite OUTSIDE that command requires fresh live operator approval" - IS WITHDRAWN AND MUST NOT BE REINTRODUCED. It exempted the single most dangerous run in the shipment: the first materialization of the tree is pure creation and is harmless, but EVERY SUBSEQUENT regeneration after a manifest change OVERWRITES and possibly REMOVES tracked, committed payload content - and that is exactly the run in which a manifest mistake silently deletes files a consumer depends on. A trusted generator writing over a tracked file destroys committed content just as surely as an untrusted one; "it is a declared output root" describes the path's ROLE, not the REVERSIBILITY of the write, and neither the generator's trustworthiness nor the directory's designation can convert destruction into non-destruction. If the spike selects (b) it MUST record this rule in the finding, in this form:
  * THE PARTITION CONTRACT IS DEFINED IN EXACTLY ONE PLACE - PLAN AC3e - AND IS REFERENCED HERE, NOT RESTATED (CYCLE 6, FINDING 13). This record previously carried its own FOUR-CLASS list (CREATE / NO-OP / OVERWRITE / REMOVE). That list is WITHDRAWN: it had already drifted from the plan's TWO-AXIS contract, which classifies on DESTINATION first (ephemeral scratch root vs. repository scope) and OUTCOME second, and which is authoritative. A restatement in a task record is a copy that cannot be kept in sync - this shipment has already shipped three mutually divergent copies of it - so the spike finding must CITE plan AC3e rather than reproduce it.
  * WHAT THE SPIKE FINDING MUST RECORD, in terms of that contract: the run computes its AC3e partition BEFORE writing anything; the destructive condition is a NON-EMPTY tracked OVERWRITE or REMOVE set; and a run whose tracked OVERWRITE and REMOVE sets are BOTH EMPTY requires NO approval and MUST NOT demand one.
  * APPROVAL IS A TWO-LAYER ARCHITECTURE (plan H2 "Principle VII generation rule", rewritten in cycle 6, finding 7). LAYER 1: the generator computes the partition, returns/prints it, and UNCONDITIONALLY REFUSES any tracked OVERWRITE or REMOVE - it takes NO approval input of any kind, which is exactly what makes the refusal unforgeable. LAYER 2: the fresh, live, non-synthesizable approval required by R-3 is an EXTERNAL AGENT-PROTOCOL GATE obtained by the agent over the operator channel; it is never read by, passed to, or asserted by the program. After approval the OWNING TASK applies the exact reviewed diff. Do NOT record a rule in which the generator "observes" an unsynthesizable approval - no signal a process can read can satisfy R-3, so such a rule either defeats R-3 or deadlocks the mandatory tracked edits this shipment requires.
  * `generate --check` is READ-ONLY by construction and NEVER requires approval.

BRANCH (a) CONSEQUENCE: if a genuine exclusion/allowlist mechanism exists, the spike must record the EXACT mechanism and its documented semantics (option name, file format, precedence), not merely that "exclusions appear supported". 160.007-T wires it directly and `plugin-payload/` is never created.

THERE IS NO UNCONDITIONAL BRANCH-(a) APPROVAL EXEMPTION (BINDING; WITHDRAWN IN REVIEW-FIX CYCLE 5, FINDING 14). The cycle-4 wording - "BRANCH (a) CARRIES NO DESTRUCTIVE-APPROVAL REQUIREMENT AT ALL ... the OVERWRITE-and-REMOVE partition is EMPTY BY CONSTRUCTION ... 160.007-T and 160.014-T MUST NOT request one on this path" - is WITHDRAWN and MUST NOT be reintroduced. ITS PREMISE IS FALSE. Branch (a) does not materialize a `plugin-payload/` tree, but it DOES emit a payload/source DECLARATION into `.github/plugin/marketplace.json`, which is a TRACKED, COMMITTED FILE. If the generated bytes DIFFER from the committed bytes, that write is a tracked OVERWRITE and is destructive in exactly the sense Constitution Principle VII governs - the absence of an output TREE says nothing about whether a tracked FILE is being overwritten. A blanket exemption keyed on the branch would have let the one tracked write branch (a) actually performs proceed unapproved, under a rule that asserted no such write exists.

THE CORRECT RULE - APPROVAL DERIVES SOLELY FROM THE COMPUTED PARTITION, NEVER FROM THE BRANCH. Every materialization run computes its write partition first (plan AC3e), and approval is required IF AND ONLY IF the tracked OVERWRITE set or the tracked REMOVE set is NON-EMPTY. This is evaluated PER RUN, on ACTUAL BYTES:
  * Branch (a), generated marketplace bytes DIFFER from committed bytes -> tracked OVERWRITE -> APPROVAL REQUIRED.
  * Branch (a), generated marketplace bytes BYTE-IDENTICAL to committed bytes -> tracked NO-OP -> NO approval, and none may be demanded.
  * Branch (b), first materialization into an absent untracked path -> CREATE -> no approval.
  * Branch (b), regeneration over tracked payload content that differs -> OVERWRITE and/or REMOVE -> APPROVAL REQUIRED.
The original concern that motivated the exemption remains valid and is preserved by the empty-partition rule below: demanding approval where nothing is destroyed IS a false gate. The fix is to derive the answer from the partition, not to hardcode it per branch. The spike MUST record the partition rule in this form, and MUST NOT record any branch-keyed exemption.

BRANCH (c) CONSEQUENCE: record the evidence gathered and the specific gap, then HALT. Opening a separate approved mechanism (for example a release artifact with supported installer semantics) is a NEW decision requiring operator approval - it is out of scope for this task to self-authorize.

WHICHEVER BRANCH IS SELECTED, the spike's finding is the input to TWO plugin-channel cases, whose classes DIFFER and must not be conflated (corrected in cycle 4, findings 5 and 6):
- test_plugin_source_path_is_tracked_and_fetchable - CLASS CHARACTERIZATION, authored by 160.016-T, PRESERVATION owner 160.007-T. It asserts the resolved `source` path is tracked and present in a consumer-fetchable checkout. It is NOT red-first: the baseline `source: "."` resolves to the repository root, which IS tracked and IS in every consumer checkout, so the property is BASELINE-TRUE and branch (a) preserves it unchanged. Its job is PRESERVATION - whatever branch the spike selects must not break it, which is exactly the `dist/plugin/` failure mode the CONSUMER-FETCHABILITY CONSTRAINT above exists to prevent.
- test_plugin_payload_conforms_to_the_selected_strategy - CLASS RED-FIRST, authored by 160.005-T, green owner 160.007-T. Branch-parametric: under (a) it asserts NO tree is materialized AND the natively-filtered payload EQUALS the manifest-derived plugin set; under (b) it asserts the tracked tree exists, is flat, is self-excluded from classifier input, and `generate --channel plugin --check` reports no drift after regeneration. THE SELECTED STRATEGY IS REQUIRED DATA - a run in which no strategy is declared FAILS, so the case can never be satisfied by silence. The spike MUST therefore record the selected strategy in a form 160.003-T can encode as a declared manifest key, not merely as prose.

ACCEPTANCE: a finding recorded at docs/spikes/2026-09-03-ship10-plugin-channel-mechanism.md naming the mechanism, with evidence (Copilot CLI plugin docs/behaviour), and a decision recorded for branch (a), (b) or (c), including an explicit statement that the selected `source` path is consumer-fetchable and the evidence for it. If branch (b) is selected, the finding must state the output-root semantics above explicitly, because 160.003-T authors the manifest from it. Under EVERY branch the finding must state the partition-derived approval rule and must NOT record a branch-keyed approval exemption. NO SOURCE CHANGES.

BLOCKS: 160.007-T must not begin until this resolves. Proceeding on assumption (a) without evidence risks discovering mid-implementation that the plugin half of the feature is infeasible as written.

This task IS the de-risking step for its own high complexity.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. INVESTIGATION-ONLY - this task produces a written finding at its authorized spike path and changes NO other repository file. It must not edit marketplace.json, the manifest, build_support/, plugin-payload/, or any workflow. Any experimental plugin install performed to gather evidence runs against a scratch profile under THE SINGLE DECLARED SCRATCH ROOT `dist/.autoharness-scratch/<run-id>/` (plan AC3e; CORRECTED IN CYCLE 11, ITEM 3 - the withdrawn generic "gitignored temporary path" wording named no root and admitted an OS temporary directory, and is DELETED). `<run-id>` is a per-run UUID4 hex string; CONTAINMENT IS VALIDATED CANONICALLY - the resolved scratch path MUST be a strict descendant of the resolved workspace root, and a validation failure FAILS CLOSED. The root is NEVER taken from `TMPDIR`, `TEMP`, `TMP`, or any OS temporary-directory API, and `tempfile.TemporaryDirectory()` and the pytest `tmp_path` / `tmp_path_factory` fixtures ARE PROHIBITED for it. It is gitignored by the existing `dist/.gitignore`, and it is EXCLUDED FROM CLASSIFIER AND PAYLOAD INPUT BEFORE MATCHING. IT IS NEVER AUTOMATICALLY DELETED - cleanup is a separate operation requiring destructive approval, and must not mutate the developer's real Copilot CLI plugin registry. NO-PUBLISH: no marketplace add/publish operation against the real `softwaresalt/autoharness` marketplace entry. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size S, complexity high. Small output (one finding document), genuinely high uncertainty (the mechanism may not exist), which is why it is a spike and why it blocks rather than being folded into 160.007-T.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T1)