---
artifact_type: shipment
created_at: 2026-09-12T23:02:00.5291706Z
custom_fields:
    items:
        - 165-F
        - 165.001-T
        - 165.002-T
        - 165.003-T
        - 165.004-T
        - 165.005-T
        - 165.006-T
        - 165.008-T
        - 165.011-T
        - 165.012-T
        - 165.009-T
id: 173-S
labels:
    - dag-root
    - topology-gate
priority: critical
status: queued
title: SHIP-15 — DAG-authoritative predecessor derivation for pipeline-topology pre_claim (165-F)
updated_at: 2026-09-13T07:33:08.6200102Z
---

SHIP-15 — DAG-authoritative predecessor derivation for the `pipeline-topology --phase pre_claim` gate (covering feature 165-F).

MANIFEST (11 items, as stored, parent first; every item appears after its prerequisites): 165-F, 165.001-T (derivation test harness), 165.002-T (engine four-state derivation), 165.003-T (audit_sequencing phase — READ-ONLY REPORT), 165.004-T (advisory parity tests), 165.005-T (advisory engine parity), 165.006-T (CLI provenance + audit rendering), 165.008-T (agent template sources AND installed dogfood mirrors, atomic), 165.011-T (pre-claim bootstrap grant surface + full-provenance force audit), 165.012-T (Orchestrator + Ship bootstrap-grant consumption, sources AND mirrors atomic), 165.009-T (docs + migration guide). The manifest GREW BY TWO in PR review-fix cycle 1 (thread PRRT_kwDORzpWpM6h3Tgb); 165.009-T moved to last position so it still follows every prerequisite.

DEPENDENCY EDGES (acyclic): 165.001-T -> 165.002-T; 165.002-T -> {165.003-T, 165.004-T, 165.006-T}; 165.003-T -> 165.006-T; 165.004-T -> 165.005-T; {165.005-T, 165.006-T} -> {165.008-T}; 165.006-T -> 165.011-T; {165.008-T, 165.011-T} -> 165.012-T; {165.005-T, 165.006-T, 165.012-T} -> 165.009-T. Execution follows the dependency edges, not manifest position; the stored order is dependency-coherent. The two new edges added in PR review-fix cycle 1 are 165.006-T -> 165.011-T (serializes CLI audit-rendering edits) and 165.008-T -> 165.012-T (serializes edits to the same four agent-contract files).

CYCLE-2 MANIFEST CHANGES (Stage review-fix cycle 2, 2026-09-12):
- 165.010-T (installed dogfood mirrors) was MERGED INTO 165.008-T and ARCHIVED. Reason: a dependency-only split between template sources and installed mirrors merely SCHEDULES drift; it leaves a live window in which the dogfooded `.github/agents/` mirrors state the retired contract while the template sources state the new one. Sources and mirrors now land in ONE task and ONE commit. 165.008-T absorbed the size (S -> M). No work was descoped.
- 165.006-T now additionally depends on 165.003-T: deliverable 4 of 165.006-T renders `--phase audit_sequencing` output, which 165.003-T produces. Without that edge 165.006-T could start before the surface it renders exists. This edge SURVIVES cycle 3's removal of the migration ledger: 165.003-T still defines the audit REPORT SHAPE that 165.006-T renders.

CYCLE-3 CORRECTIONS AFFECTING THIS RECORD (Stage review-fix cycle 3, 2026-09-13 — content only; no manifest, edge, or size change):
- 165.003-T's audit is now a PURE READ-ONLY REPORT. The "migration ledger" it was previously specified to emit is WITHDRAWN in full — no durable ledger, no persisted artifact, no future-remediation fields, no persistence subsystem in the gate. The durable migration record is the VERSION-CONTROLLED OPERATOR COMMIT and its diff.
- Closure evidence is removed from 165.004-T and 165.005-T entirely; stash FD0CCB42 owns closure-evidence producer/consumer naming EXCLUSIVELY.
- The bootstrap grant is corrected UPWARD from two invocations to THREE (see below). SUPERSEDED by PR review-fix cycle 1: the three-invocation agent-run grant is RETRACTED as not executable; see BOOTSTRAP DISPOSITION below.

PR REVIEW-FIX CYCLE 1 CHANGES (staging PR #448, Copilot review threads, 2026-09-13 — this is a PR review-fix cycle, separate from and subsequent to the four completed plan-review cycles):
- Thread PRRT_kwDORzpWpM6h3Tgb (this record) ACCEPTED. The U0/U1/U2 agent-run force grant is retracted as unexecutable and replaced by BOOTSTRAP-A (one-time operator-run entry, executable today) plus BOOTSTRAP-B (product behaviour, 165.011-T + 165.012-T). Manifest 9 -> 11 items; two dependency edges added; validity conditions corrected for branch vantage.
- Thread PRRT_kwDORzpWpM6h3Tgi (165.003-T) ACCEPTED. The audit's "writes nothing" claim is narrowed to no backlog mutations and no migration-state/ledger writes; ordinary `pipeline-topology` telemetry remains allowed, observational, and fail-open.
- COUPLED-ARTIFACT SWEEP (completing this same cycle). Every artifact that still asserted a retracted claim was corrected: 165-F's BOOTSTRAP section (U0/U1/U2 grant) and its absolute "gate owns NO write path" claim; the body dependency trailers of 165.006-T (-> 165.011-T) and 165.008-T (-> 165.012-T); 165.009-T's body edge, its missing bootstrap-grant documentation deliverable, and two further absolute no-write claims; and the deliberation record's D6 and D3 sections, which now carry supersession banners (raised to rev 5) with their historical analysis preserved verbatim. The ONLY sizing change: 165.009-T raised S -> M (complexity unchanged at `low`) because its new deliverable 8 pushed a 1.5h documentation task toward the 2-hour bound; it remains within the rule, so no split is forced. No other item's size, complexity, or edge set changed in the sweep.
- OPERATIONAL HANDOFF PRECISION added to BOOTSTRAP-A: B6 now states the entry state to Ship explicitly (already-claimed, no claim-path gate runs, the OBSERVED uniform manifest status for the step-6 `expected_status`, the evidence path), and the installed "Bootstrap exemption" clauses are explicitly ruled out as inapplicable — the gate IS installed, and BOOTSTRAP-A runs it rather than skipping it.

KNOWN TOOL ARTEFACT: this record's `size_composition.members` rollup still lists the archived, off-manifest 165.007-T and 165.010-T because the rollup derives members from `parent_id` children rather than from `custom_fields.items`. The MANIFEST (`custom_fields.items`) is authoritative and correct at 11 items. Tracked as stash 9AA34143 (requires deliberation: yes, cites P-021 C1). Do NOT "reconcile" the rollup by re-adding either archived task.

CLOSURE STRATEGY FOR 173-S — SAFE_CLOSE, VERIFIED AND FINAL (review-fix cycle 3, finding 3, operator ruling (a)). Running the workspace's own close-path classifier over this manifest returns, per item, `SAFE_CLOSE`, naming 165.007-T and 165.010-T as ARCHIVED OFF-MANIFEST DESCENDANTS of 165-F. `SAFE_CLOSE` is the designed fail-closed fallback and IS A SUPPORTED CLOSE PATH: it is VALID, NON-BLOCKING, and is the FINAL CLOSURE STRATEGY for this shipment. `CASCADE` is UNAVAILABLE BY DESIGN — it would require re-adding the two archived tasks to this manifest, which is forbidden. Every earlier claim that BOTH close paths are impossible, or that 173-S cannot be closed, is FALSE and is RETRACTED. The provenance and off-manifest status of 165.007-T and 165.010-T are PRESERVED EXACTLY: no reparenting, no invented holding feature, no re-addition to this manifest, no mutation of their parentage or manifest membership. Ship closes 173-S via SAFE_CLOSE; no further closure work, authorization, or escalation is required.

BOOTSTRAP DISPOSITION — REWRITTEN IN PR REVIEW-FIX CYCLE 1 (staging PR #448, thread PRRT_kwDORzpWpM6h3Tgb, 2026-09-13). The cycle-3 formulation below is RETRACTED as NOT EXECUTABLE, and is replaced by two clearly separated mechanisms: BOOTSTRAP-A (a one-time OPERATOR-RUN entry path for 173-S, executable TODAY against the currently installed agent text) and BOOTSTRAP-B (the PRODUCT BEHAVIOUR 173-S implements, for FUTURE self-hosted migrations). These two are NOT the same mechanism and must never be conflated.

RETRACTION — WHY THE CYCLE-3 U0/U1/U2 GRANT WAS NOT EXECUTABLE (finding ACCEPTED, same contract surface). Cycles 2 and 3 recorded an authorization for "exactly three audited forced `pre_claim` invocations" performed BY THE ORCHESTRATOR AND SHIP. No installed agent contract can perform them. Verified against the installed text at this HEAD:
- `.github/agents/_orchestrator.agent.md` step 2a invokes `autoharness gate pipeline-topology --mode agent --shipment {candidate_shipment_id} --phase pre_claim --json` — NO `--force` — and states "Exit 1 (`blocked`...) or exit 2 (`invalid`): halt routing to Ship ... never inferred, never fail-open."
- `.github/agents/_ship.agent.md` step 3 invokes the same command twice — NO `--force` — and states "Exit 1 ... or exit 2 ...: halt immediately with the reported token/message — never inferred, never fail-open."
- No agent template or installed mirror mentions `--force` for this gate at all. The only agent-visible force provision anywhere in Ship's text is for a DIFFERENT gate (`copilot-review`, `_ship.agent.md` L526-527), and no equivalent provision exists for `pipeline-topology`.
- The shipment record itself states "NO agent may self-authorize a force", so an agent could not supply the flag on its own initiative even if the text were silent.
Therefore the three named invocations would each have HALTED, not forced. Additionally, `--force` is STATELESS: an operator-run forced invocation records an audit line and exits 0 for THAT process only; it does not change the verdict any agent's own subsequent UNFORCED run computes. An operator force followed by ordinary agent routing therefore does not unblock anything. Every cycle-2/cycle-3 statement that Ship (or the Orchestrator) would consume a three-use force grant to enter 173-S is FALSE and is RETRACTED.

SECOND HALF OF THE FINDING, ALSO ACCEPTED: the existing force audit record is insufficient for the evidence the grant claimed. `_audit_pipeline_topology_force` (src/autoharness/cli.py) writes only `timestamp`, `actor`, `reason`, `mode`, `phase`, `target_shipment_id`, `token`, `message`. It records NO invocation label, NO full observed gate payload, NO HEAD SHA, NO manifest identity, NO explicit inferred-predecessor field, and NO authorizing-decision reference. Closing that gap is 165.011-T Deliverable 4.

NO RETROACTIVE AUTHORIZATION. 165.011-T and 165.012-T are shipped BY 173-S. Their agent text and CLI surface are not installed until 173-S merges, and the claim of 173-S necessarily precedes that merge. A future code change CANNOT retroactively authorize the initial claim, and no artifact in this feature may say otherwise. BOOTSTRAP-A is the only thing that gets 173-S claimed; BOOTSTRAP-B is what 173-S leaves behind for the next self-hosted migration.

BOOTSTRAP-A — ONE-TIME OPERATOR-RUN ENTRY PATH FOR 173-S (executable TODAY; decision D6 is its authorization). Performed BY THE HUMAN OPERATOR, not by any agent. The `--force` flag is documented in the CLI as "Operator-only bypass of a failing gate. Audited." — this path uses it exactly as documented and requires no agent to force, bypass, or reinterpret anything.

PRECONDITION: PR #448 is merged to `main`; the operator is on `main` with a clean worktree; `git rev-parse HEAD` is recorded.

VANTAGE CORRECTION (empirically verified this cycle — the cycle-3 conditions got this WRONG). `pre_claim` evaluates `branch_ownership` BEFORE `shipment_readiness` and SHORT-CIRCUITS on the first blocking check. Observed at this HEAD:
- from `chore/stage-173-S` (the Stage branch): exit 1, SOLE blocking check `branch_ownership`, token `BRANCH_MISMATCH`. `PREDECESSOR_NOT_SHIPPED` is NEVER REACHED.
- from `main`: exit 1, SOLE blocking check `shipment_readiness`, token `PREDECESSOR_NOT_SHIPPED`, `details.predecessor_id: "172-S"`, `details.live_status: "queued"`.
- from the canonical 173-S shipment branch: identical to `main`.
Cycle-3 validity condition 3 named "the Stage review-fix cycle 3 final-correction commit on `chore/stage-173-S`" as the required HEAD. Forcing from the Stage branch would have bypassed a `BRANCH_MISMATCH`, which decision D6 never authorized. That condition is CORRECTED: B0/B1 run from `main`, B2 runs from the 173-S shipment branch, and NO forced invocation is ever performed from a Stage branch.

STEPS (exact, in order):
- B0 — on `main`, mirroring the Orchestrator route-to-Ship vantage. Run `autoharness gate pipeline-topology --mode agent --shipment 173-S --phase pre_claim --json` UNFORCED FIRST and verify the payload against the validity conditions below. Only then re-run with `--force` appended. Exits 0, `forced: true`, appends to `.autoharness/gates/pipeline-topology-force-audit.log`.
- B1 — on `main`, mirroring Ship's pre-branch vantage. Same verify-then-force sequence. (B0 and B1 share a vantage and yield identical payloads; both are run so the durable evidence maps 1:1 onto the three gate sites 165.012-T will later instrument.)
- B2 — operator creates the shipment branch themselves: `git checkout -b chore/173-s-ship-15-dag-authoritative-predecessor-derivation-for-pipeline-topology-pre-claim` (a canonical alias accepted by `branch_ownership`). Then, immediately before claiming, run the same verify-then-force sequence from that branch. This is the TOCTOU-narrowing invocation.
- B3 — operator claims: `backlogit shipment claim 173-S`.
- B4 — operator verifies convergence NATIVELY and WITHOUT FORCE: `autoharness gate pipeline-topology --mode agent --shipment 173-S --phase post_claim --json` MUST exit 0 (sole active shipment is 173-S). NO FORCE IS AUTHORIZED AT `post_claim`.

  B4 FAILURE CONTRACT — HALT WITH 173-S LEFT ACTIVE; THERE IS NO AUTOMATIC REVERSAL (PR review-fix cycle 2, staging PR #448, thread PRRT_kwDORzpWpM6h3fq1 — finding ACCEPTED). The cycle-1 wording "the operator HALTS and reverses the claim" promised an operation that DOES NOT EXIST. Verified against the installed tool at this HEAD: `backlogit shipment` exposes exactly `add`, `claim`, `create`, `get`, `list`, `return-blocked`, and `ship` — there is NO `unclaim`, NO `release`, NO `abort`, and NO command or flag that performs an `active` -> `queued` transition. The shipment status enum is {`queued`, `blocked`, `active`, `shipped`, `abandoned`}; `queued` is reachable only as the CREATE-TIME default, never as a transition OUT of `active`. A bootstrap contract that instructs an operator to "reverse the claim" therefore terminates in an unexecutable step at the one moment the workspace is already in a half-entered state, which is the worst possible place for an undefined instruction.
  THE EXECUTABLE CONTRACT IS: on any non-zero B4 verdict the operator HALTS IMMEDIATELY. 173-S REMAINS `active`. The operator performs NO further bootstrap step — B5 and B6 are NOT performed, Ship is NOT invoked, and no forced re-run is attempted (the BOOTSTRAP-A force authority has ALREADY EXPIRED at this point, having expired on the successful B3 claim). The operator records the failing `post_claim` payload verbatim, then performs EXPLICIT REMEDIATION as a deliberate, separately-reasoned act:
    (a) DIAGNOSE AND CONVERGE — the expected remediation. A non-zero `post_claim` after a successful claim means the workspace holds a condition the gate rejects (most plausibly a second `active` shipment, i.e. a P-001 single-active violation that predates or races this entry). The operator resolves THAT condition directly and re-runs B4 UNFORCED until it exits 0, then resumes at B5. 173-S stays `active` throughout; this is a forward fix, not a rollback.
    (b) ABANDON — only if the operator decides the entry must not proceed at all. `active` -> `abandoned` IS a supported transition (`abandoned` is a member of the shipment status enum and is reachable via `backlogit update 173-S --status abandoned`). The operator MUST VERIFY it, by re-reading the record (`backlogit shipment get 173-S`) and confirming `status: abandoned`, and MUST record the observed before/after status. ABANDONMENT IS TERMINAL AND IS NOT A REQUEUE: it does NOT return 173-S to `queued`, does NOT make it re-claimable, and — per this feature's own narrow genesis rule — an abandoned record is a DISQUALIFYING shipment record for genesis in this workspace forever after. Choosing (b) means the 173-S scope must be re-shipped under a NEW shipment record; that re-shipment is its own work unit with its own authorization, not a continuation of this bootstrap.
  NO ARTIFACT IN THIS FEATURE MAY PROMISE AUTOMATIC REVERSAL, AUTOMATIC REQUEUE, OR ANY `active` -> `queued` TRANSITION. No agent performs any part of this remediation: BOOTSTRAP-A is an operator path end to end, and a halt inside it hands control to the operator, never to Ship or the Orchestrator.
- B5 — durable evidence. `.autoharness/gates/` is GITIGNORED in this repository, so the force audit log is LOCAL-ONLY and is NOT by itself durable, reviewable authorization evidence. The operator transcribes, verbatim, the three UNFORCED pre-force JSON payloads, the `git rev-parse HEAD` SHA at each invocation, the 11 ordered manifest item IDs, the blocking token, the inferred predecessor `172-S`, the B4 post_claim result, the OBSERVED manifest task statuses immediately after B3 (the value Ship needs at B6 item (ii)), and decision D6, into a version-controlled record committed on the shipment branch at `docs/bootstrap/2026-09-13-173-S-bootstrap-evidence.md`. THAT COMMIT is the durable authorization record for this bootstrap.
- B6 — operator invokes Ship DIRECTLY (not via Orchestrator routing), with 173-S already `active`, the shipment branch already created and checked out, and the B5 evidence path supplied. HANDOFF PRECISION — the operator states all four of the following to Ship explicitly, because Ship cannot derive them: (i) 173-S is ALREADY CLAIMED, so Ship performs NO claim and runs neither step-3 `pre_claim` invocation nor the step-5 `post_claim` verification (the operator already performed the equivalent at B4, unforced); (ii) the `expected_status` value for Ship's step-6 `shipment-reconcile` `mode: pre` check is the UNIFORM status the operator OBSERVES across the manifest immediately after B3 (`queued` if the claim left manifest tasks unchanged, `active` if it advanced them together) — the operator reads it rather than assuming it, and if the observed statuses are MIXED the operator SKIPS the step-6 check per its own recorded Scope note (`_ship.agent.md` L310-317) rather than passing a value that would be classified `status-mismatch`; (iii) the B5 evidence commit path, as the authorization of record; (iv) that Ship begins at step 1 with branch and claim preconditions already satisfied by the operator, and that NO agent-side force, grant, or gate bypass is authorized anywhere in the run.

NOT AN EXEMPTION PATH — THE INSTALLED "BOOTSTRAP EXEMPTION" CLAUSES DO NOT APPLY. `_orchestrator.agent.md` (L245-247) and `_ship.agent.md` (L232-234, L297-299) each carry a "Bootstrap exemption" note that skips the topology gate WHILE `autoharness gate pipeline-topology` IS NOT YET INSTALLED. That gate IS INSTALLED in this workspace (`src/autoharness/gates/topology.py`, reachable via `autoharness gate pipeline-topology`), so the exemption is INAPPLICABLE by its own stated condition. It MUST NOT be invoked, cited, or stretched to skip a `pre_claim` evaluation for 173-S. BOOTSTRAP-A does not skip the gate — it RUNS the gate, unforced, at every step, and the operator forces only a verified, condition-matched block.

WHY B6 HONOURS THE CURRENTLY INSTALLED SHIP AND ORCHESTRATOR CONTRACTS (each point proven against installed text, not asserted):
- The Orchestrator's step-2a gate is a precondition of THE ORCHESTRATOR ROUTING to Ship. Direct operator invocation of Ship performs no Orchestrator routing, so step 2a never executes. It is not skipped, waived, or exempted — it is not reached.
- Ship's step 3 pre_claim runs and step 4 claim are explicitly the CLAIM PATH: step 3 opens "Before claiming (the first workspace mutation)" and step 4 reads "Claim the shipment ... only after both pre_claim gate runs above pass." Ship performs no claim in this entry, so these claim-path preconditions do not fire. Nothing is bypassed.
- Ship's step 5 post_claim verification states verbatim: "This verification applies only when a shipment was claimed in step 4". Ship did not claim, so it does not run — and the operator performed the equivalent verification at B4 instead, unforced.
- Ship's step 6 intake reconciliation explicitly contemplates this entry: it directs `mode: pre` with `expected_status: queued` "(or `active` if already claimed)". This is direct installed-text support for Ship entering against an ALREADY-CLAIMED shipment.
- Ship's step 1a `SHIPMENT_STATE_INCONSISTENT` early-warning fires only when the shipment record is `queued` while a manifest task is already `active` or `done`. After B3 the record is `active`, so the halt condition CANNOT be met regardless of what the claim did to manifest task statuses — the operator still RECORDS the observed manifest statuses at B5, because Ship needs that uniform value for its step-6 check (see B6 handoff item (ii)).
- Ship's step 1 P-001 gate is satisfied: 173-S is the sole active release unit, confirmed by B4.
- NO AGENT forces, bypasses, or reinterprets any gate anywhere in this path, and no agent self-authorizes. Every forced invocation is a human operator act on the operator-only flag.

PER-INVOCATION VALIDITY CONDITIONS FOR B0/B1/B2 (ALL must hold at the moment of that invocation, checked on the UNFORCED run; any one failing VOIDS the authorization and the operator HALTS):
1. The gate payload contains EXACTLY ONE blocking check and its token is `PREDECESSOR_NOT_SHIPPED`. Any second blocking check, or any other token — including `BRANCH_MISMATCH`, `PRECLAIM_ACTIVE_SHIPMENT_PRESENT`, `SHIPMENT_STATE_INCONSISTENT`, `TARGET_NOT_CLAIMABLE`, `PREDECESSOR_STATE_AMBIGUOUS`, `UNSEQUENCED_SHIPMENT`, any closure-evidence block, or any secrets finding — voids it.
2. `details.predecessor_id` is EXACTLY `172-S`.
3. The vantage is correct for that step: `main` for B0 and B1; the canonical 173-S shipment branch for B2. A Stage branch is NEVER a valid vantage.
4. `HEAD` contains the merged PR #448 correction set, and the live 173-S manifest matches the 11 items listed above exactly, in the stored order.
5. The active-shipment count is zero and 173-S is still `queued`.
6. The forced run is recorded per B5: the invocation label, the full observed pre-force payload, the blocking token, the inferred predecessor, the HEAD SHA, the manifest identity, and decision D6 as the authorization source.

BOOTSTRAP-A BOUNDS: exactly three forced `pre_claim` invocations (B0, B1, B2), for 173-S ONLY, at `pre_claim` ONLY, bound to token `PREDECESSOR_NOT_SHIPPED` and predecessor `172-S` ONLY. The authority EXPIRES IMMEDIATELY on the earlier of (a) the successful B3 claim, or (b) ANY mismatch against conditions 1-6. A FOURTH forced invocation is NOT authorized, including the post-claim `CLAIM_NOT_OBSERVED` reclaim path's `pre_claim` re-run — that run is evaluated WITHOUT force and, if it blocks, halts to the operator for a fresh decision. `--force` semantics are UNCHANGED; this confers no precedent and no broader force authority over any other shipment. The operator records the override in the PR/closure record for 173-S.

BOOTSTRAP-B — PRODUCT BEHAVIOUR SHIPPED BY 173-S (for FUTURE self-hosted migrations; does NOT apply to 173-S's own claim). Two new manifest tasks:
- 165.011-T adds a version-controlled PRE-CLAIM BOOTSTRAP GRANT at `.autoharness/bootstrap-grants/{shipment_id}.yaml` (deliberately outside the gitignored `.autoharness/gates|staging|metrics` trees so a grant is committable and reviewable), a fail-closed exact-match rule binding a grant to one shipment id, one expected token, one expected predecessor, one manifest digest and a closed set of invocation labels, an agent-consumable `--bootstrap-grant-invocation {label}` CLI flag, a DURABLE EXCLUSIVE-CREATE CONSUMPTION RECORD that enforces at-most-once per grant-and-label mechanically (PR review-fix cycle 2, thread PRRT_kwDORzpWpM6h3fqf — the cycle-1 wording said only "is not already consumed", which named no mechanism, no durability, and no atomicity and was therefore not implementable as written), and the FULL-PROVENANCE audit record (invocation label, full observed payload, HEAD SHA, manifest identity, blocking token, inferred predecessor, authorization source/decision) EMITTED FROM THE CLAIMED CONSUMPTION RECORD, plus bounded telemetry parity.
- 165.012-T teaches the Orchestrator (route-to-Ship) and Ship (pre-branch, pre-claim) contracts — template sources AND installed mirrors, one commit — to consult the grant BEFORE invoking the gate, pass the label for that one site when a grant exists, and otherwise behave EXACTLY as today. A missing grant and a non-matching grant are indistinguishable: both halt. No agent may author a grant.
Grant authority is shipment-specific, exact-token/predecessor-bound, at-most-once per named site (enforced by the durable exclusive-create consumption record, not by an unqualified claim), `pre_claim`-only, and no broader than necessary — the same bounds as BOOTSTRAP-A, made mechanical instead of manual.

PR REVIEW-FIX CYCLE 2 CHANGES (staging PR #448, Copilot review threads, 2026-09-13 — content only; NO manifest, edge, size, or complexity change. The manifest remains 11 items / 10 executable tasks):
- Thread PRRT_kwDORzpWpM6h3fqf (165.011-T) ACCEPTED. At-most-once consumption is given a CONCRETE DURABLE ATOMIC MECHANISM — an O_EXCL exclusive-create consumption record under the gitignored `.autoharness/gates/bootstrap-grant-consumption/` runtime tree, claimed BEFORE forced evaluation, with the audit emitted FROM the claimed record. Contention, replay, crash states, malformed/stale records, containment, and operator recovery are all specified and tested. The undefined "not already consumed" phrasing is REMOVED.
- Thread PRRT_kwDORzpWpM6h3fq1 (this record, B4) ACCEPTED. "Reverse the claim" is REMOVED as unexecutable — backlogit has no `active` -> `queued` transition. Replaced by the B4 FAILURE CONTRACT above: halt with 173-S left `active`, explicit operator remediation, optional VERIFIED `active` -> `abandoned`, and no promise of automatic reversal or requeue.
- Thread PRRT_kwDORzpWpM6h3frC (165.001-T / 165.002-T / 165.004-T) ACCEPTED. Canonical CI is `python -m unittest discover -s tests`; a pytest `xfail` marker is INERT there and `unittest.expectedFailure` cannot constrain WHY a test failed. Replaced by a stdlib-only `expect_red(raises=..., message_contains=...)` helper that treats ONLY the named exception type and normalized message as RED, FAILS on any other exception, and FAILS on XPASS.
- Thread PRRT_kwDORzpWpM6h3frF (165.001-T / 165.002-T / 165.004-T / 165.005-T / 165.008-T / 165.009-T / 165-F) ACCEPTED. The three-probe genesis rule (G2/G3/G4) omitted the `blocked` status entirely and is REPLACED by the SOLE-RECORD rule: genesis only when the candidate is the ONLY shipment record across all live and archived records, regardless of status or provenance; unknown, missing, or malformed status FAILS CLOSED as a disqualifying record. Regression cases added.
- Thread PRRT_kwDORzpWpM6h3frY (165.006-T) ACCEPTED. The reintroduced zero-write assertion ("NO WRITE — no file created, no record mutated") is REMOVED from the CLI task and narrowed to no backlog mutation and no migration-state/ledger write, with the pre-existing observational fail-open telemetry append explicitly ALLOWED and positively TESTED — matching the narrowing already applied to 165.003-T in PR review-fix cycle 1.
- SCOPE OF RECORD: 10 EXECUTABLE TASKS and BOOTSTRAP-A steps B0/B1/B2 (not U0/U1/U2, which are the retracted, historical-only agent-run labels). The PR #448 body is ORCHESTRATOR-OWNED and is updated separately; this record and 165-F are authoritative for the counts.

SELF-LIQUIDATION (unchanged and still true): this record carries `labels: [dag-root]`, so once 165.002-T lands, 173-S derives `predecessor_source: declared_root` and passes `pre_claim` NATIVELY with no force and no grant on every subsequent evaluation.

DESCOPED FROM THIS SHIPMENT: 165.007-T (closure-evidence producer/consumer naming defect) was removed from the manifest and archived; it is now DEFERRED SCOPE EXPANSION stash FD0CCB42 (kind bug, priority high, requires deliberation: yes, cites P-021 C1), requiring its own deliberation and its own work unit. It EXCLUSIVELY owns closure-evidence producer/consumer naming. Do not re-add it here.

SOURCES: docs/decisions/2026-09-12-dag-authoritative-predecessor-derivation-deliberation.md (rev 5, PR review-fix cycle 1 supersession notes on D3/D6), docs/plans/2026-09-12-dag-authoritative-predecessor-derivation-plan.md (rev 5, PR review-fix cycle 1), docs/reviews/2026-09-12-dag-authoritative-predecessor-derivation-plan-review.md (plan-review cycle 4 + PR review-fix cycle 1 addendum), docs/bugs/2026-09-11-autoharness-pipeline-topology-numeric-predecessor-bug.md (original intake, committed as the durable source artifact and made self-contained in cycle 3, with external provenance softwaresalt/backlogit#438 recorded in its frontmatter).