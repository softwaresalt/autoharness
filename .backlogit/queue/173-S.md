---
artifact_type: shipment
created_at: 2026-09-12T23:02:00.5291706Z
custom_fields:
    items:
        - 165-F
        - 165.001-T
        - 165.002-T
        - 165.003-T
        - 165.004-T
        - 165.005-T
        - 165.006-T
        - 165.008-T
        - 165.009-T
id: 173-S
labels:
    - dag-root
    - topology-gate
priority: critical
status: queued
title: SHIP-15 — DAG-authoritative predecessor derivation for pipeline-topology pre_claim (165-F)
updated_at: 2026-09-13T00:40:23.4664146Z
---

SHIP-15 — DAG-authoritative predecessor derivation for the `pipeline-topology --phase pre_claim` gate (covering feature 165-F).

MANIFEST (9 items, as stored, parent first; every item appears after its prerequisites): 165-F, 165.001-T (derivation test harness), 165.002-T (engine four-state derivation), 165.003-T (audit_sequencing phase + migration ledger), 165.004-T (advisory parity tests), 165.005-T (advisory engine parity), 165.006-T (CLI provenance + audit rendering), 165.008-T (agent template sources AND installed dogfood mirrors, atomic), 165.009-T (docs + migration guide).

DEPENDENCY EDGES (acyclic): 165.001-T -> 165.002-T; 165.002-T -> {165.003-T, 165.004-T, 165.006-T}; 165.003-T -> 165.006-T; 165.004-T -> 165.005-T; {165.005-T, 165.006-T} -> {165.008-T, 165.009-T}. Execution follows the dependency edges, not manifest position; the stored order is dependency-coherent.

CYCLE-2 MANIFEST CHANGES (Stage review-fix cycle 2, 2026-09-12):
- 165.010-T (installed dogfood mirrors) was MERGED INTO 165.008-T and ARCHIVED. Reason: a dependency-only split between template sources and installed mirrors merely SCHEDULES drift; it leaves a live window in which the dogfooded `.github/agents/` mirrors state the retired contract while the template sources state the new one. Sources and mirrors now land in ONE task and ONE commit. 165.008-T absorbed the size (S -> M). No work was descoped.
- 165.006-T now additionally depends on 165.003-T: deliverable 4 of 165.006-T renders `--phase audit_sequencing` output, which 165.003-T produces. Without that edge 165.006-T could start before the surface it renders exists.
- KNOWN TOOL ARTEFACT: this record's `size_composition.members` rollup still lists the archived, off-manifest 165.007-T and 165.010-T because the rollup derives members from `parent_id` children rather than from `custom_fields.items`. The MANIFEST (`custom_fields.items`) is authoritative and correct at 9 items. Tracked as stash 9AA34143. Do NOT "reconcile" the rollup by re-adding either archived task.

BOOTSTRAP DISPOSITION — AUDITED, SHIPMENT-SPECIFIC `--force`, EXACTLY TWO INVOCATIONS (decision D6, operator-authorized 2026-09-12, re-scoped in review-fix cycle 2). 173-S is a genuine DAG root: it declares no `blocks` edge and its work is independent of the queued 163-S -> 164-S -> 165-S -> 166-S -> 168-S -> 167-S chain and of 169-S..172-S. Under the CURRENTLY SHIPPED code, its absent edges cause `_prior_shipment_id` to synthesise 172-S (queued, unshipped) as a predecessor, so `pre_claim` blocks 173-S with PREDECESSOR_NOT_SHIPPED. The shipment that fixes the defect is blocked BY the defect.

WHY TWO, NOT ONE: the Ship agent's claim protocol runs `--phase pre_claim` TWICE before the claim — once BEFORE branch/worktree creation, and again IMMEDIATELY BEFORE the claim to narrow the TOCTOU window. Cycle 1 authorized a "single-use" force, which is unsatisfiable: Ship would clear the first gate and halt on the second, or would silently stretch one authorization over two invocations. The authorization is therefore stated as exactly TWO audited forced invocations, no more.

AUTHORIZED INVOCATIONS (exhaustive):
- U1 — `pre_claim` run BEFORE branch/worktree creation.
- U2 — `pre_claim` run IMMEDIATELY BEFORE `backlogit_claim_shipment`.

PER-INVOCATION VALIDITY CONDITIONS (ALL must hold at the moment of that invocation; any one failing voids the authorization):
1. The ONLY blocking token in the gate payload is PREDECESSOR_NOT_SHIPPED.
2. The inferred predecessor is EXACTLY 172-S.
3. The current reviewed/staged HEAD is the Stage review-fix cycle 2 commit under which this authorization was recorded, and the 173-S manifest matches the 9 items listed above exactly.
4. NO other topology, check, closure, or secrets violation is present — including PRECLAIM_ACTIVE_SHIPMENT_PRESENT, a non-zero active-shipment count, SHIPMENT_STATE_INCONSISTENT, an invalid/unresolvable shipment id, any closure-evidence block, or any secrets finding.
5. The force is recorded as an AUDIT EVENT naming: the invocation (U1 or U2), the full gate payload observed, the blocking token, the inferred predecessor, the HEAD SHA, and decision D6 as its authorization source.

EXPIRY: the authorization expires IMMEDIATELY upon the earlier of (a) the successful claim of 173-S, or (b) ANY mismatch against conditions 1-4 at either invocation. On expiry-by-mismatch Ship MUST HALT and return to the operator — it must not retry, must not force again, and must not reinterpret the mismatch as an equivalent case.

THIRD INVOCATION IS NOT AUTHORIZED: if the post-claim CLAIM_NOT_OBSERVED retry path re-runs `--phase pre_claim` before reclaiming, the force authority is already EXHAUSTED. That third run must be evaluated WITHOUT `--force`; if it blocks, Ship halts to the operator for a fresh authorization decision.

UNCHANGED BOUNDS: applies to 173-S ONLY, no precedent and no broader force authority over any other shipment; `--force` semantics themselves are UNCHANGED and remain human-authorized, shipment-specific, and audited; NO agent may self-authorize a force; Ship MUST also record the override in the PR/closure record. The authorization is self-liquidating: this record carries `labels: [dag-root]`, so once 165.002-T lands, 173-S derives `predecessor_source: declared_root` and passes NATIVELY with no force on every subsequent evaluation.

DESCOPED FROM THIS SHIPMENT: 165.007-T (closure-evidence producer/consumer naming defect) was removed from the manifest and archived; it is now DEFERRED SCOPE EXPANSION stash FD0CCB42 (kind bug, priority high, requires deliberation: yes), requiring its own deliberation and its own work unit. Do not re-add it here.

SOURCES: docs/decisions/2026-09-12-dag-authoritative-predecessor-derivation-deliberation.md (rev 3), docs/plans/2026-09-12-dag-authoritative-predecessor-derivation-plan.md (rev 3), docs/reviews/2026-09-12-dag-authoritative-predecessor-derivation-plan-review.md (cycle 3, PASS), docs/bugs/2026-09-11-autoharness-pipeline-topology-numeric-predecessor-bug.md (original intake, now committed verbatim as the durable source artifact).