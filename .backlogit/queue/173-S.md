---
artifact_type: shipment
created_at: 2026-09-12T23:02:00.5291706Z
custom_fields:
    items:
        - 165-F
        - 165.001-T
        - 165.002-T
        - 165.003-T
        - 165.004-T
        - 165.005-T
        - 165.006-T
        - 165.008-T
        - 165.009-T
id: 173-S
labels:
    - dag-root
    - topology-gate
priority: critical
status: queued
title: SHIP-15 — DAG-authoritative predecessor derivation for pipeline-topology pre_claim (165-F)
updated_at: 2026-09-13T07:00:10.3289792Z
---

SHIP-15 — DAG-authoritative predecessor derivation for the `pipeline-topology --phase pre_claim` gate (covering feature 165-F).

MANIFEST (9 items, as stored, parent first; every item appears after its prerequisites): 165-F, 165.001-T (derivation test harness), 165.002-T (engine four-state derivation), 165.003-T (audit_sequencing phase — READ-ONLY REPORT), 165.004-T (advisory parity tests), 165.005-T (advisory engine parity), 165.006-T (CLI provenance + audit rendering), 165.008-T (agent template sources AND installed dogfood mirrors, atomic), 165.009-T (docs + migration guide). The manifest is UNCHANGED by review-fix cycle 3.

DEPENDENCY EDGES (acyclic, UNCHANGED by review-fix cycle 3): 165.001-T -> 165.002-T; 165.002-T -> {165.003-T, 165.004-T, 165.006-T}; 165.003-T -> 165.006-T; 165.004-T -> 165.005-T; {165.005-T, 165.006-T} -> {165.008-T, 165.009-T}. Execution follows the dependency edges, not manifest position; the stored order is dependency-coherent.

CYCLE-2 MANIFEST CHANGES (Stage review-fix cycle 2, 2026-09-12):
- 165.010-T (installed dogfood mirrors) was MERGED INTO 165.008-T and ARCHIVED. Reason: a dependency-only split between template sources and installed mirrors merely SCHEDULES drift; it leaves a live window in which the dogfooded `.github/agents/` mirrors state the retired contract while the template sources state the new one. Sources and mirrors now land in ONE task and ONE commit. 165.008-T absorbed the size (S -> M). No work was descoped.
- 165.006-T now additionally depends on 165.003-T: deliverable 4 of 165.006-T renders `--phase audit_sequencing` output, which 165.003-T produces. Without that edge 165.006-T could start before the surface it renders exists. This edge SURVIVES cycle 3's removal of the migration ledger: 165.003-T still defines the audit REPORT SHAPE that 165.006-T renders.

CYCLE-3 CORRECTIONS AFFECTING THIS RECORD (Stage review-fix cycle 3, 2026-09-13 — content only; no manifest, edge, or size change):
- 165.003-T's audit is now a PURE READ-ONLY REPORT. The "migration ledger" it was previously specified to emit is WITHDRAWN in full — no durable ledger, no persisted artifact, no future-remediation fields, no persistence subsystem in the gate. The durable migration record is the VERSION-CONTROLLED OPERATOR COMMIT and its diff.
- Closure evidence is removed from 165.004-T and 165.005-T entirely; stash FD0CCB42 owns closure-evidence producer/consumer naming EXCLUSIVELY.
- The bootstrap grant is corrected UPWARD from two invocations to THREE (see below).

KNOWN TOOL ARTEFACT: this record's `size_composition.members` rollup still lists the archived, off-manifest 165.007-T and 165.010-T because the rollup derives members from `parent_id` children rather than from `custom_fields.items`. The MANIFEST (`custom_fields.items`) is authoritative and correct at 9 items. Tracked as stash 9AA34143 (requires deliberation: yes, cites P-021 C1). Do NOT "reconcile" the rollup by re-adding either archived task.

CLOSURE STRATEGY FOR 173-S — SAFE_CLOSE, VERIFIED AND FINAL (review-fix cycle 3, finding 3, operator ruling (a)). Running the workspace's own close-path classifier over this manifest returns, per item, `SAFE_CLOSE`, naming 165.007-T and 165.010-T as ARCHIVED OFF-MANIFEST DESCENDANTS of 165-F. `SAFE_CLOSE` is the designed fail-closed fallback and IS A SUPPORTED CLOSE PATH: it is VALID, NON-BLOCKING, and is the FINAL CLOSURE STRATEGY for this shipment. `CASCADE` is UNAVAILABLE BY DESIGN — it would require re-adding the two archived tasks to this manifest, which is forbidden. Every earlier claim that BOTH close paths are impossible, or that 173-S cannot be closed, is FALSE and is RETRACTED. The provenance and off-manifest status of 165.007-T and 165.010-T are PRESERVED EXACTLY: no reparenting, no invented holding feature, no re-addition to this manifest, no mutation of their parentage or manifest membership. Ship closes 173-S via SAFE_CLOSE; no further closure work, authorization, or escalation is required.

BOOTSTRAP DISPOSITION — AUDITED, SHIPMENT-SPECIFIC `--force`, EXACTLY THREE INVOCATIONS (decision D6, operator-authorized 2026-09-12, re-scoped in review-fix cycle 2 and CORRECTED UPWARD in review-fix cycle 3, finding 4). 173-S is a genuine DAG root: it declares no `blocks` edge and its work is independent of the queued 163-S -> 164-S -> 165-S -> 166-S -> 168-S -> 167-S chain and of 169-S..172-S. Under the CURRENTLY SHIPPED code, its absent edges cause `_prior_shipment_id` to synthesise 172-S (queued, unshipped) as a predecessor, so `pre_claim` blocks 173-S with PREDECESSOR_NOT_SHIPPED. The shipment that fixes the defect is blocked BY the defect.

WHY THREE, NOT TWO: the cycle-2 grant counted only Ship's two runs and MISSED the ORCHESTRATOR's route-to-Ship eligibility check, which runs `--phase pre_claim` BEFORE SHIP IS INVOKED AT ALL (.github/agents/_orchestrator.agent.md L239-241). Under a two-use grant 173-S would have deadlocked at the pipeline's FIRST gate, before Ship ever ran — the authorization would have been exhausted-by-absence rather than by use. Ship then runs the gate TWICE more before claiming: once BEFORE branch/worktree creation, and again IMMEDIATELY BEFORE the claim to narrow the TOCTOU window, with the claim gated on both (.github/agents/_ship.agent.md L258). The authorization is therefore stated as exactly THREE audited forced invocations, no more.

AUTHORIZED INVOCATIONS (exhaustive):
- U0 — Orchestrator `pre_claim` run as the route-to-Ship eligibility check, BEFORE Ship is invoked (_orchestrator.agent.md L239-241).
- U1 — Ship `pre_claim` run BEFORE branch/worktree creation (_ship.agent.md L227-229).
- U2 — Ship `pre_claim` run IMMEDIATELY BEFORE `backlogit_claim_shipment` (_ship.agent.md L254-255).

EXPLICITLY NOT COVERED: the Orchestrator's CURSOR-ADVANCE `pre_claim` check (_orchestrator.agent.md L261-263) evaluates `{next_shipment_id}` — a DIFFERENT shipment — and is outside this grant entirely. This grant covers 173-S and nothing else.

PER-INVOCATION VALIDITY CONDITIONS (ALL must hold at the moment of that invocation; any one failing voids the authorization):
1. The ONLY blocking token in the gate payload is PREDECESSOR_NOT_SHIPPED.
2. The inferred predecessor is EXACTLY 172-S.
3. The current reviewed/staged HEAD is the Stage review-fix cycle 3 final-correction commit on `chore/stage-173-S` under which this authorization was recorded, and the 173-S manifest matches the 9 items listed above exactly.
4. NO other topology, check, closure, or secrets violation is present — including PRECLAIM_ACTIVE_SHIPMENT_PRESENT, a non-zero active-shipment count, SHIPMENT_STATE_INCONSISTENT, an invalid/unresolvable shipment id, any closure-evidence block, or any secrets finding.
5. The force is recorded as an AUDIT EVENT naming: the invocation (U0, U1, or U2), the full gate payload observed, the blocking token, the inferred predecessor, the HEAD SHA, and decision D6 as its authorization source.

EXPIRY: the authorization expires IMMEDIATELY upon the earlier of (a) the successful claim of 173-S, or (b) ANY mismatch against conditions 1-4 at ANY of the three invocations. On expiry-by-mismatch the invoking agent MUST HALT and return to the operator — it must not retry, must not force again, and must not reinterpret the mismatch as an equivalent case.

FOURTH INVOCATION IS NOT AUTHORIZED: if the post-claim CLAIM_NOT_OBSERVED retry path re-runs `--phase pre_claim` before reclaiming (_ship.agent.md L282-283), the force authority is already EXHAUSTED. That run must be evaluated WITHOUT `--force`; if it blocks, Ship halts to the operator for a fresh authorization decision. POST-CLAIM RETRY WITH FORCE IS UNAUTHORIZED.

UNCHANGED BOUNDS: applies to 173-S ONLY, no precedent and no broader force authority over any other shipment; `--force` semantics themselves are UNCHANGED and remain human-authorized, shipment-specific, and audited; NO agent may self-authorize a force; Ship MUST also record the override in the PR/closure record. The authorization is self-liquidating: this record carries `labels: [dag-root]`, so once 165.002-T lands, 173-S derives `predecessor_source: declared_root` and passes NATIVELY with no force on every subsequent evaluation.

DESCOPED FROM THIS SHIPMENT: 165.007-T (closure-evidence producer/consumer naming defect) was removed from the manifest and archived; it is now DEFERRED SCOPE EXPANSION stash FD0CCB42 (kind bug, priority high, requires deliberation: yes, cites P-021 C1), requiring its own deliberation and its own work unit. It EXCLUSIVELY owns closure-evidence producer/consumer naming. Do not re-add it here.

SOURCES: docs/decisions/2026-09-12-dag-authoritative-predecessor-derivation-deliberation.md (rev 4), docs/plans/2026-09-12-dag-authoritative-predecessor-derivation-plan.md (rev 4), docs/reviews/2026-09-12-dag-authoritative-predecessor-derivation-plan-review.md (cycle 4, authorized final review), docs/bugs/2026-09-11-autoharness-pipeline-topology-numeric-predecessor-bug.md (original intake, committed as the durable source artifact and made self-contained in cycle 3, with external provenance softwaresalt/backlogit#438 recorded in its frontmatter).