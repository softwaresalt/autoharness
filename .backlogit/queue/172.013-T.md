---
artifact_type: task
created_at: 2026-09-19T01:01:34.8984602Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 172.005-T
    - 172.012-T
id: 172.013-T
labels:
    - implementation
    - checkpoint
    - cli
    - mcp
parent_id: 172-F
priority: high
status: queued
title: 'Resume-hint T7b (IMPLEMENTATION, inert): checkpoint scan over CLI and MCP through one adapter'
updated_at: 2026-09-19T01:01:34.8984602Z
---

IMPLEMENTATION (INERT) - THE EXECUTABLE HISTORICAL SCAN ACROSS BOTH FRONT ENDS. Implement autoharness checkpoint scan and the guarded MCP scan operation in src/autoharness/checkpoint/, both delegating to the SAME adapter so their structured tokens and JSON are identical by construction rather than by convention.

The scanner enumerates every record under the checkpoint store and classifies each one through the single total decision procedure with origin bound internally to "historical". It reports three buckets - valid-with-hint, the reported CHECKPOINT_LEGACY_HINTLESS_RESOLVED legacy set, and the blocking set containing CHECKPOINT_ACTIVE_HINTLESS and CHECKPOINT_RECORD_MALFORMED - enumerating the legacy set by filename and count so the exemption is visible rather than silent. NO RECORD IS LEFT UNCLASSIFIED.

ORIGIN IS INTERNAL. The scanner binds origin="historical" itself; no CLI option and no MCP parameter exposes it. The create path binds "harness" by the same constant-only discipline.

THIS SHIPS INERT. No agent template, instruction, or registry references the scan yet; nothing is prohibited and the raw create path still works. Wiring the startup surfaces onto it is part of the single atomic activation 172.009-T, and doing it here would switch enforcement on before the replacement is fully proven.

Blocked by 172.012-T (an observed-failing parity suite exists first) and 172.005-T (the adapter and decision procedure it delegates to). Blocks 172.006-T. Scope: src/autoharness/checkpoint/ only. Stash 2A7C48A8. Plan docs/plans/2026-09-17-checkpoint-resume-hint-contract-plan.md (revision 7). Verdict manifest docs/reviews/2026-09-17-checkpoint-resume-hint-contract-plan-review.md (latest_attempt 07, verdict REMEDIATED-PENDING-REVIEW at plan revision 7, awaiting independent attempt 08).