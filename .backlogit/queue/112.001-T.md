---
artifact_type: task
created_at: 2026-08-05T23:50:52.9223042Z
custom_fields:
    complexity: high
    size: S
    size_ruleset_version: 1.0.0
    size_source: agent
id: 112.001-T
parent_id: 112-F
priority: medium
status: queued
title: 'B1: repair-record-status mode in shipment-reconcile SKILL (preconditions, forward-only, isolation, --confirm)'
updated_at: 2026-08-06T04:50:00Z
---



<!-- BEGIN:acceptance-criteria -->
1. New mode is a SEPARATE mode string, UNREACHABLE from pre/post/safe-close (H1 isolation). 2. Preconditions ALL required, else REFUSE + report-and-halt: (i) TARGET-AWARE READ-ONLY REPAIR-INSPECTION (013-DL Addendum A — the plain `autoharness gate pipeline-topology` invocation is UNREACHABLE here because its detect-before-consistency scan returns SHIPMENT_STATE_INCONSISTENT on the queued-with-active-work TARGET before the active-shipment invariant runs; topology.py `_evaluate_core` orders `_detect_before_consistency` ahead of `_active_invariant_check`). Instead compose EXISTING read-only backlogit reads (`backlogit shipment list` + per-shipment/per-task status reads) to require ZERO active shipments AND confirm the TARGET is the SOLE queued-with-active-work inconsistency (every OTHER queued shipment has NO active/done manifest task); ANY other active shipment, ANY non-target inconsistency, or ANY ambiguity => REFUSE (fail closed on all non-target ambiguity). This inspection is BEST-EFFORT DETECTION, NOT a lock/lease/serializer; (ii) record re-reads `queued` — the ONLY re-claimable status; backlogit 1.8.0 has NO `blocked` shipment status and NO blocked->queued transition, so a non-lifecycle/legacy `blocked` status => MALFORMED LEGACY DATA => REFUSE + HALT for OPERATOR manual remediation, NEVER a fabricated blocked->queued transition (H3 malformed-status hard-exclusion); (iii) CONSERVATIVE torn-state refusal via PER-TASK ALLOWED-STATE classification (review P1-1; CORRECTED by publication-review P2 2026-08-06 / 013-DL Addendum D; FURTHER CORRECTED by publication-review P1 2026-08-06 / 013-DL Addendum E — archived-completed representation): the repair-inspection MUST NOT reuse pre-mode's single-`expected_status` `matched` gate (a valid queued-with-active-work TARGET has, BY DEFINITION, a MIX of per-task ROLES — some live-queued, some live-active, some already-completed — so no single `expected_status` can make every task `matched`; that gate is UNREACHABLE and would REFUSE the exact reparable state). Instead classify each manifest TASK by its ROLE and require a UNIQUE, NON-CONFLICTING representation, per how backlogit actually persists task state: (a) a `queued` task MUST be a UNIQUE LIVE QUEUE record — present in `queue/` with frontmatter `status: queued` and NO archive record for the same id; (b) an `active` task MUST be a UNIQUE LIVE QUEUE record — present in `queue/` with frontmatter `status: active` and NO archive record for the same id; (c) a COMPLETED task MUST be a UNIQUE ARCHIVE record (present in `archive/` ONLY, with NO conflicting live queue record for the same id) in EITHER of backlogit's TWO valid archive representations (publication-review P1 2026-08-06, 013-DL Addendum F — verified read-only against real archived records: both shapes occur, e.g. `status: done` in `.backlogit/archive/112.001-R…md`/`002.001-T.md` AND `status: archived` + `archived_status: done` in `.backlogit/archive/012-DL.md`): (c-a) TERMINAL RELOCATION — an archive record with frontmatter `status: done` (explicit archive provenance `archived_status`/`archived_from` is NOT required); OR (c-b) EXPLICIT ARCHIVAL — an archive record with `status: archived` AND `archived_status: done` AND VALID PROVENANCE (`archived_from` present and well-formed). Both representations require the record be UNIQUE in `archive/` with NO conflicting live queue record. The allowed per-task roles are EXACTLY {live-queued, live-active, archived-completed(done — either archive representation)} — the normal in-progress distribution of a shipment whose claim was silently dropped while its tasks kept progressing and completing normally. FAIL CLOSED (REFUSE + report-and-halt) on ANY: a DUPLICATE record (same id present in BOTH queue and archive); a CONFLICTING record (queue status disagreeing with role, an archive record alongside a live queue record, or a live `status: done` in queue — completed tasks are archived, never live-done); a MISSING record (manifest id with no queue or archive file); MALFORMED/ABSENT PROVENANCE on a `status: archived` archive record (a `status: archived` record missing/ill-formed `archived_status: done`/`archived_from` — a `status: done` archive record legitimately carries no provenance and is NOT malformed) or ANY OTHER archived status/provenance shape (an archive record whose status is NEITHER `done` NOR `archived`-with-`archived_status: done`); an `orphan` (a queue file declaring this `shipment_id` but absent from the manifest); or any status OUTSIDE the allowed roles / torn-partial anomaly. Proceed ONLY when EVERY manifest task satisfies its role predicate UNIQUELY and NON-CONFLICTINGLY; ANY per-item anomaly (duplicate/conflict/missing/malformed-provenance/orphan/out-of-role/torn-partial) => REFUSE + report-and-halt. The distinguishing signal for a torn/inconsistent state is a per-item ANOMALY, NOT the mixedness of roles across the manifest; ambiguity refusal is UNWEAKENED. Pre/post/safe-close keep their existing single-`expected_status` classification UNCHANGED (this correction adds NO template behavior change to those paths); (iv) backlogit reachable; the forward re-claim executes ONLY via the EXISTING atomic all-or-nothing ClaimShipment (061-F) — NO new lock/lease/CAS. ClaimShipment atomicity is PER-SHIPMENT all-or-nothing (no mutual exclusion between concurrent claims). 3. Forward-only: queued->active only; backward-repair permanently excluded. 4. TOCTOU RESIDUAL RISK — EXPLICIT & RETAINED (H8, second re-review 2026-08-05, 013-DL Addendum B): the inspection and the ClaimShipment are SEPARATE UNLOCKED operations; a concurrent claim can race in the window between them and the post-condition may only DETECT a second active AFTER the target is activated — the SAME already-accepted 109-F TOCTOU. NEVER claim prevention, serialization, atomic exclusion, or "provably safe." 5. Post-condition (H2 — DETECTION, not prevention): after the re-claim, re-verify the GLOBAL exactly-one-active-and-it-is-the-target invariant (extend 109-F post-claim invariant; no self-refusal double-count). If a racing claim produced a SECOND active shipment, DETECT the two-active violation and FAIL CLOSED: emit audit+telemetry, HALT, surface for OPERATOR remediation; perform NO remediation mutation and NEVER attempt an active->queued rollback of the target (forbidden by backlogit 1.8.0 isValidShipmentTransition). Fail-closed manual remediation = operator resolves via backlogit's sanctioned lifecycle transitions, never an autoharness active->queued rollback. 6. Gated behind explicit `--confirm`; operator-only (Ship keeps report-and-halt). 7. DEGRADED (backlogit unreachable) => REFUSE + report-and-halt (H6).
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Add a distinct, opt-in, operator-invoked `mode: repair-record-status` to templates/skills/shipment-reconcile/SKILL.md.tmpl that performs ONLY a forward re-claim (queued->active) of a queued-with-active-work shipment under strict preconditions, else falls closed to report-and-halt. This is the core safety-critical prose.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Source of truth: 013-DL Option C (incl. Addendum A + Addendum B + Addendum C + Addendum D + Addendum E) + hardening H1-H8 + review 112.001-R (incl. second/third re-review 2026-08-05 + publication-review P2 2026-08-06: per-task allowed-state torn refusal replaces the unreachable single-`expected_status` all-`matched` gate; + publication-review P1 2026-08-06: archived-completed tasks are accepted in EITHER of backlogit's two valid archive representations — (a) a `status: done` archive record (no provenance required) OR (b) `status: archived` + `archived_status: done` + valid `archived_from` — both unique in archive with no conflicting live queue record; per-task role predicate fails closed on duplicate/conflicting/missing/malformed-provenance/any-other-archived-status; superseded by 013-DL Addendum F). Single template family (shipment-reconcile SKILL). Complexity high but de-risked by the completed deliberation + hardening + review.
<!-- END:implementation-notes -->