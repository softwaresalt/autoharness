---
artifact_type: deliberation
created_at: 2026-08-19T02:38:34.6633161Z
custom_fields:
    linked_stash_id: B48A482A
    linked_stash_kind: feature
    linked_stash_priority: medium
    linked_stash_text: 'Operator direction (2026-08-18T16:45:22.513-07:00): "New harness policy: whenever fixing a bug or resolving a Copilot review comment would expand work beyond the currently approved feature/task scope, the expansion MUST NOT be implemented in that fix cycle. It must be captured as a separate stash entry for later Stage triage and mandatory deliberation/research before any planning or implementation. The active feature/task remains bounded. This applies in all cases; review pressure, severity, dark mode, or apparent convenience do not authorize silent scope expansion. The original in-scope defect/comment should still be resolved as far as possible without the expansion, with the deferred expansion ID referenced in the review reply/residual-risk record." [INTAKE ONLY: excluded future work. NOT part of the completed EDE3CC2D/1EFDA8EE dark run and NOT in DARK_MODE_SCOPE. Untriaged; requires future Stage triage plus mandatory deliberation/research before any planning or implementation.]'
id: 019-DL
priority: medium
status: queued
title: P-021 Bounded Fix-Cycle Scope Containment &amp; Deferred Expansion Capture
updated_at: 2026-08-19T02:38:34.6633161Z
---

<!-- markdownlint-disable-next-line MD025 -->
# P-021 Bounded Fix-Cycle Scope Containment &amp; Deferred Expansion Capture

## Problem Frame

<!-- BEGIN:problem-frame -->
Operator direction B48A482A: when fixing a bug or resolving a Copilot review comment would expand work beyond the currently approved feature/task scope, the expansion MUST NOT be implemented in that fix cycle. It must be captured as a separate stash entry for later Stage triage plus MANDATORY deliberation/research before planning or implementation. The active feature/task stays bounded. Review pressure, severity, dark mode, and convenience do not authorize silent scope expansion. The original in-scope defect/comment is still resolved as far as possible without the expansion, and the deferred expansion ID is referenced in the review reply / residual-risk record.

Current harness state (verified 2026-08-18):

- No named policy encodes this. The closest artifact is a compound learning (docs/compound/2026-08-16-bounded-review-fix-cycle-scope-and-mechanical-consequence-judgment.md) captured from PR #348, which is advisory prose, not a policy with a violation action.
- The circuit-breaker instruction's "Review-Fix Cycle Definition" bounds the NUMBER of cycles (3) but says nothing about the SCOPE of a fix within a cycle. An agent can stay under 3 cycles while silently expanding scope.
- github-pr-automation's autonomous comment-handling loop tells the agent to "fix valid comments" with no scope test.
- CRITICAL LATENT CONTRADICTION: templates/agents/_ship.agent.md.tmpl line 38 Role Boundary lists "stash operations" in Ship's FORBIDDEN column, while Ship Step 9 (pre-merge, line 527-530) and post-merge Step 6 (line 763-766) already REQUIRE Ship to create stash entries for follow-ups. role-enforcement.instructions.md mandates fail-closed halt + P-010 violation on any forbidden-column operation. Mandating a mid-cycle stash capture without resolving this makes the required behavior a policy violation by construction.
<!-- END:problem-frame -->

## Options

<!-- BEGIN:options -->
Option A - Prose-only note in the Ship agent template.
  Rejected: no policy ID, no violation action, no cross-surface coherence, nothing for a Copilot "weakened enforcement" review to cite, and it leaves the Ship role-boundary contradiction unresolved.

Option B - Extend the existing P-018 (Copilot Review Completion & Thread Resolution Merge Gate).
  Rejected: the direction covers bug fixes and CI/build fixes, not just Copilot review comments; and P-018 is a pre-merge gate verdict, whereas this is an in-cycle authoring constraint. Overloading P-018 would blur a deterministic gate with a judgment rule.

Option C - New workflow policy P-021 carried coherently across policy registry, agent templates, shared instructions, skills, prompts, dogfood surfaces, manifest checksums, and a contract test. CHOSEN.

Option D - Option C plus a deterministic CLI gate (autoharness gate scope-containment).
  Rejected for this shipment: "did this fix expand scope" has no machine-detectable signal; a diff-size or touched-file heuristic would produce false BLOCKs and would be trivially defeated. Recorded as residual risk / future work rather than implemented.
<!-- END:options -->

## Chosen Direction

<!-- BEGIN:chosen-direction -->
Option C. Author a new named workflow policy **P-021: Bounded Fix-Cycle Scope Containment & Deferred Expansion Capture** (Amendment Log 1.20.0) and carry it across every coherent autoharness surface, keeping source templates and dogfood-generated surfaces byte-consistent with refreshed manifest checksums.

Normative content of P-021:

1. **Scope test (narrow and literal).** Promote the PR #348 compound learning from advisory prose to the normative in-scope test: a finding is in scope only if fixing it requires ONLY completing the exact change already authorized (the same contract surface). "Same file", "same function", "same PR", "same subsystem", or "related" is NOT sufficient. Anything requiring original design/decision work or an unrelated code path is OUT of scope. When genuinely ambiguous, the fail-safe default is OUT of scope + defer.
2. **Mandatory capture, not silent drop.** An out-of-scope expansion MUST be captured as a stash entry carrying a DEFERRED SCOPE EXPANSION marker, the source refs (PR number when applicable, review-thread ID when applicable — separate refs whose availability is judged independently, not a single fused PR/thread ID — plus task/feature ID and shipment ID), and a `requires deliberation` flag. Capture is a precondition for closing the finding, not an optional courtesy, and is never conditional on a PR or review thread existing.
3. **Bounded resolution of the original.** The in-scope defect/comment is still resolved as far as possible WITHOUT the expansion. The reference obligation is conditional on actual thread availability: where a review thread exists, the deferred entry ID is referenced in the review-thread reply and in the PR/closure residual-risk record, which satisfies P-018 zero-unresolved-threads honestly without fabricating scope; where no thread exists (pre-PR local review, build/CI), the obligation is discharged in full by the task/run/closure residual-risk citation, and P-018 raises no obligation because there is no thread to leave unresolved.
4. **Non-bypass.** Review pressure, finding severity, dark factory mode (P-017), circuit-breaker exhaustion, and apparent convenience never authorize expansion. Only an explicit operator authorization recorded as a new/expanded approved scope does.
5. **Ship capture-only carve-out (resolves the P-010 contradiction).** Ship MAY create stash entries for capture only. Ship MUST NOT triage, prioritize, re-classify, edit, harvest, deliberate on, or remove them. The Ship Role Boundary table and role-enforcement's fail-closed pre-mutation protocol are amended together so the required capture is Allowed rather than a P-010 violation.
6. **Stage intake obligation.** A stash entry marked DEFERRED SCOPE EXPANSION is a distinct triage classification that MUST route to the deliberate skill (Stage Step 2) before any planning, regardless of shape or apparent triviality.
7. **Violation action.** Implementing an out-of-scope expansion inside a fix cycle, or closing an out-of-scope finding without a captured deferred entry, records a P-021 violation via P-005 telemetry and halts.

Coherent surface set (source template + dogfood + manifest checksum where dogfooded):
S1 templates/policies/workflow-policies.md.tmpl (template-only; verified there is NO installed .github/policies/workflow-policies.md mirror)
S2 templates/agents/_ship.agent.md.tmpl + .github/agents/_ship.agent.md - Role Boundary carve-out
S3 templates/instructions/role-enforcement.instructions.md.tmpl + dogfood - fail-closed protocol recognizes the capture-only carve-out
S4 templates/agents/_ship.agent.md.tmpl + dogfood - review-fix / build-fix defer-capture procedure + Stop Conditions rows
S5 templates/instructions/circuit-breaker.instructions.md.tmpl + dogfood - Review-Fix Cycle Definition gains the scope test
S6 templates/instructions/github-pr-automation.instructions.md.tmpl + dogfood - autonomous comment loop reply/resolve/capture/reference
S7 templates/skills/pr-lifecycle/SKILL.md.tmpl and templates/skills/fix-ci/SKILL.md.tmpl (template-only; not dogfooded - .github/skills holds only the 4 global skills)
S8 templates/agents/_stage.agent.md.tmpl + dogfood - Step 1 classification + mandatory deliberate routing
S9 templates/agents/_orchestrator.agent.md.tmpl + dogfood and templates/prompts/feature-flow-dark.prompt.md.tmpl + dogfood - dark-mode non-bypass
S10 .autoharness/harness-manifest.yaml HARNESS_ENFORCED_SUMMARY policy range (currently stale at "P-001 through P-019") + .github/instructions/copilot-code-review.instructions.md re-render
S11 tests/ - new contract test modeled on tests/test_circuit_breaker_policy_contract.py
<!-- END:chosen-direction -->

## Open Questions

<!-- BEGIN:open-questions -->
1. RESOLVED IN SCOPE - Should the deferred stash entry marker be a structured backlogit field (e.g. custom_fields.deferred_scope_expansion) rather than a text marker? Decided: text marker + documented required fields for this shipment. A backlogit schema change is a separate product (backlogit) and would violate width isolation. Recorded as follow-up candidate.
2. DEFERRED - Deterministic enforcement (Option D). No reliable machine signal for scope expansion today. Follow-up candidate: telemetry-based detection (compare touched-file set against the plan's declared file map) rather than a diff heuristic.
3. DEFERRED - Should P-021 also bind the Orchestrator when it routes a fix cycle, beyond the dark-mode non-bypass statement? Kept minimal: the dark-mode non-bypass clause is carried; broader Orchestrator routing semantics are not.
4. OPEN RISK - Ship's Role Boundary currently forbids "stash operations" while Ship Steps 9 and post-merge 6 already require them. This shipment repairs the contradiction. If task 002/003 were dropped, the rest of the feature would mandate a P-010 violation. This dependency is encoded explicitly in the backlog.
5. OPEN RISK - Any concurrent branch touching the same dogfood artifacts will conflict on manifest checksums. Mitigation: single serial shipment, one worktree, checksums recomputed from the merged blob per the 115-S procedure.
<!-- END:open-questions -->

## Notes

<!-- BEGIN:notes -->
Research performed:

- backlogit stash inspection of B48A482A (kind: feature, priority: medium, intake-only, explicitly requires mandatory deliberation/research).
- Learnings retrieval over docs/compound/ (41 entries). Direct hit: 2026-08-16-bounded-review-fix-cycle-scope-and-mechanical-consequence-judgment.md (confidence HIGH) - supplies the exact "same contract surface vs same file" test and the reply/resolve/disclose closure pattern for out-of-scope findings under a P-018 zero-unresolved-threads gate. Secondary: 2026-05-06-p010-agent-role-boundaries.md (role boundary construction).
- Policy registry survey: templates/policies/workflow-policies.md.tmpl, 723 lines, P-001..P-020 plus P-013.1-.6, Amendment Log at 1.19.0. Next free ID is P-021; next amendment version is 1.20.0.
- Dogfood coupling: .autoharness/harness-manifest.yaml records SHA256 checksums for 39 managed artifacts. tests/test_circuit_breaker_policy_contract.py asserts the LF-normalized rendered template is BYTE-IDENTICAL to the dogfood output and that the manifest checksum matches. Therefore every template edit in this feature requires a paired dogfood re-render plus a manifest checksum refresh computed from the LF-normalized committed blob (`git cat-file -p :<path> | sha256`), per the procedure established in 115-S.
- Verified templates/policies has no installed mirror: .github/policies/ does not exist (consistent with docs/archive/plans/2026-07-29-p020-post-merge-compaction-plan.md, "there is no installed workflow-policies.md").
- Verified .github/skills contains only install-harness, tune-harness, verify-harness, workspace-discovery, so pr-lifecycle/fix-ci skill templates have no dogfood pair.
- Verified the existing staleness in HARNESS_ENFORCED_SUMMARY: .github/instructions/copilot-code-review.instructions.md line 52 still reads "Workflow policies P-001 through P-019" although P-020 shipped. The range bump to P-021 is folded in as a coherence correction, explicitly scoped and disclosed rather than treated as an unrelated drive-by.

Self-application note: this deliberation is itself an instance of the policy it defines. The HARNESS_ENFORCED_SUMMARY staleness (S10) was discovered while surveying, and is being carried deliberately and disclosed as an in-scope coherence consequence of adding P-021 (the range literally must include the new ID), not as silent expansion. The deterministic gate idea (Option D) and the backlogit deferred-scope-expansion marker schema are explicitly NOT carried and are recorded as follow-up candidates.

C3 AMENDMENT (Stage fix cycle 2, 2026-08-19, current-HEAD review of 5882fb4d, staging finding P1): clause 3 above was amended in place. As originally deliberated it required the deferred entry ID to be referenced in the review-thread reply UNCONDITIONALLY. That is unsatisfiable on two of the surfaces the policy governs -- Ship's local review runs BEFORE PR creation, and build/CI findings have no review thread even on an open PR -- so an unconditional C3 would have forced Ship either to violate the clause or to fabricate a thread. The carrier tasks 134.004-T and 134.007-T had already been corrected to the conditional disposition, leaving the authoritative clause (134.001-T) and this deliberation as the stale outliers. Clause 3 now makes the reference obligation conditional on actual thread availability, with the threadless case discharged in full by the task/run/closure residual-risk citation. The deliberated DECISION is unchanged -- Option C, a new named policy P-021 carried across all coherent surfaces -- and clauses 1, 2 and 4-7 are untouched. This annotation records the amendment rather than silently rewriting the decision record.

Sizing/decomposition rationale: 11 tasks, each bounded well under the 2-hour rule, width-isolated so no task combines a policy-registry edit with a CLI/schema edit. The policy authoring task (001) is the single upstream dependency for every carrier surface, so the normative text exists before any surface quotes it. The contract test is last because it asserts clauses that must already exist.

C2 AMENDMENT (Stage contract replanning, 2026-08-19, operator-directed): clause 2 above was amended in place. As originally deliberated it recorded the source refs as a single fused `PR/thread ID`. That fusion is the ROOT of the defect corrected downstream in fix cycle 1: it encodes the assumption that a PR and a review thread are jointly present or jointly absent, when the two surfaces the threadless path exists to serve break exactly that pairing -- a build/CI finding has an OPEN PR but no thread, and a pre-PR local-review finding has neither. Carrying the fused token forward produced a plan clause row reading `PR + review-thread ID` with no qualifier, and an authoritative C2 in 134.001-T that qualified only the thread ID with "when applicable" while stating the PR number unconditionally. The carriers 134.004-T and 134.007-T had already been corrected to independent per-field availability, leaving this deliberation, the plan clause table, and the authoritative clause as the stale outliers -- the same inverse-drift shape as the C3 amendment above. All three are now corrected: the PR number and the review-thread ID are SEPARATE refs, each qualified "when applicable", with availability judged independently per field, and capture is explicitly never conditional on a PR or thread existing. The deliberated DECISION is unchanged -- Option C -- and clauses 1 and 3-7 are untouched by this amendment.

DECOMPOSITION UPDATE (2026-08-19): the task count is now 12. Task 008 absorbed the Stage-owned late-identifier reconciliation workflow (and gained a 004 dependency), and task 012 was added to carry the C2/C3 semantic regression suite that task 011's presence-only assertions structurally cannot cover. The split keeps both test tasks under the 2-hour rule and keeps a structural failure distinguishable from a semantic one.
<!-- END:notes -->
