---
artifact_type: task
created_at: 2026-09-03T10:14:55.0589127Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.005-T
id: 160.018-T
labels:
    - packaging
    - schema
    - contract-registration
parent_id: 160-F
priority: medium
status: queued
title: Publish payload-manifest schema (1.0.0 live + versioned mirror) and register the contract
updated_at: 2026-09-03T16:11:08.4551306Z
---

SCHEMA PUBLICATION (T2b).

WHY THIS TASK EXISTS - TWO DEFECTS IT CLOSES:
(1) PRODUCTION BEFORE A RED OWNER. Publishing the schema, its versioned mirror, and the SCHEMA_CONTRACTS registration with NO RED OWNER ANYWHERE means production artifacts land before any test observes their absence. This task is BLOCKED BY 160.005-T (the RED-FIRST harness) and is the NAMED GREEN OWNER of two RED-FIRST cases, so the contract is observed failing before it is created.
(2) SIZE/SCOPE INCOHERENCE. Recording what exists today (160.001-T) and publishing a NEW VERSIONED CONTRACT (this task) are different kinds of work with different reversibility, and are separately sized.

GREEN RESPONSIBILITY - this task turns exactly TWO RED-FIRST cases green, both authored by 160.005-T:
- test_schema_live_and_versioned_mirror_agree (AC1) - red because neither schema file exists (missing artifact).
- test_payload_manifest_contract_registered (AC1) - red because `payload-manifest` is absent from SCHEMA_CONTRACTS.
It does NOT own test_manifest_validates_against_schema - that case needs the MANIFEST as well, and its green owner is 160.003-T. It authors no case and preserves no CHARACTERIZATION case.

THREE MANDATORY DELIVERABLES, all acceptance criteria, none deferrable:

(1) PUBLISH BOTH FILES. schemas/payload-manifest.schema.json (the live contract) AND the immutable versioned mirror schemas/payload-manifest/1.0.0.schema.json. This is the two-file convention observed on EVERY other versioned schema in this repository (harness-config, harness-manifest, workspace-profile, tool-telemetry-event, validation-gates). A top-level schema with NO versioned mirror is a conformance FAILURE.

    The schema declares, at 1.0.0:
    - `include` and `exclude` - the base path rule sets.
    - PER-CHANNEL OVERLAYS FOR ALL THREE CHANNELS - `wheel`, `sdist`, AND `plugin`. The channel key set is a CLOSED THREE-MEMBER ENUM: exactly wheel, sdist and plugin, no fourth member and no open-ended additionalProperties escape. A schema that permits an undeclared channel silently readmits an unclassified publication surface, which is the whole defect this shipment closes.
    - `generated_output_roots` (AC3d) - the list of paths that are OUTPUTS of the generation command rather than INPUTS to classification. Declared here so the resolver (160.004-T) and the classifier have a schema-backed key to read; its binding semantics are specified by 160.003-T and implemented by 160.004-T, not restated here.
    - the single `target_workspace_paths` key - the SOLE authored occurrence of those prefixes across EXECUTABLE AND CONFIGURATION SURFACES (see AC2d and 160.004-T's centralized classifier). THE SCAN SURFACE AND THE FIXTURE CARVE-OUT ARE NOW CONSISTENT (corrected in extended review-fix cycle 4, finding 9). The contradiction being closed: this record permitted test fixtures to name a prefix while AC2d's exactly-one-occurrence scan covered tests/** WHOLESALE, so recording an accurate baseline fixture would have FAILED the drift test - the two rules could not both be satisfied. THE SCAN SURFACE IS build_support/**, src/**, tests/** EXCLUDING tests/fixtures/**, pyproject.toml, .github/plugin/marketplace.json, .github/workflows/**, and the manifest itself; the manifest key is the ONE permitted occurrence within it. Prose, documentation, backlog records and tests/fixtures/** may name a prefix as INERT DATA. THE GUARANTEE IS PRESERVED BY DERIVATION, NOT BY TEXTUAL ABSENCE: a second, explicit assertion in the same case forbids ANY module under build_support/** or src/** from obtaining target-workspace prefixes from a fixture, a test constant, or any source other than classify_target_workspace_path reading this manifest key. A fixture literal is inert; a fixture literal that something EXECUTABLE reads is a second source of truth, and that - not its existence - is the drift the rule exists to stop. Both halves are asserted by test_target_workspace_prefixes_have_exactly_one_authored_occurrence (RED-FIRST, authored 160.005-T, green owner 160.003-T), so the carve-out is TESTED rather than assumed.

(2) REGISTER THE CONTRACT in src/autoharness/schema_contracts.py so the existing mutation detector covers payload-manifest from its FIRST release rather than after the first incident. This repository has THREE RECORDED OCCURRENCES of in-place schema mutation without a version bump (docs/compound/2026-08-30-157-s-149-f-schema-mutation-in-place-third-occurrence.md), and that learning records that the shorter, more dangerous fix path applies EXACTLY to CONTRACTS UNREGISTERED IN SCHEMA_CONTRACTS: an unregistered schema has NOTHING that detects a later in-place mutation, so a fourth occurrence would land silently.

(3) SATISFY THE PAIR-DIVERGENCE ASSERTION - DO NOT AUTHOR IT (corrected in extended review-fix cycle 4, finding 8; the earlier wording "ASSERT THE PAIR. The test that FAILS if..." is WITHDRAWN because it directed this task to WRITE a test while this task authors no case and holds no tests/** authorization). The assertion is test_schema_live_and_versioned_mirror_agree - it FAILS if the live file and the 1.0.0 mirror diverge while the declared $id/version still reads 1.0.0, and that assertion is precisely what was absent in all three prior occurrences. IT IS AUTHORED BY 160.005-T AND OBSERVED RED THERE, before this task begins. THIS TASK'S OBLIGATION IS TO MAKE IT GREEN by publishing a live file and a 1.0.0 mirror that agree - NOT to write the test. A green owner that can edit the test it must satisfy can make it pass by weakening it, which destroys the red-first evidence.

CRITICAL: this is a NEW schema at 1.0.0. Do NOT mutate any existing schema in place.

FREEZE-SCOPE INTERACTION - THE NARROW src/ EXCEPTION: forbidding ALL src/autoharness/ edits while requiring deliverable (2) as a non-deferrable acceptance criterion would make the plan's own acceptance unexecutable under its own safety mode. THIS TASK IS THE ONLY TASK IN SHIP-10 AUTHORIZED TO TOUCH src/autoharness/, and its authorization is bounded to ADDING THE payload-manifest CONTRACT REGISTRATION ENTRY IN schema_contracts.py AND NOTHING ELSE. No other statement, function, or file in src/autoharness/ may be modified. The two rules are NOT in conflict: the freeze-scope prohibition governs RUNTIME BEHAVIOUR (_DATA_DIR resolution order, `autoharness home`, `autoharness version` - invariants I1/I2), while this authorization governs a CONTRACT-REGISTRY DECLARATION that changes no runtime behaviour. A single-line registration is not a licence to edit src/.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. Writable surface for this task is EXACTLY THREE FILES: schemas/payload-manifest.schema.json, schemas/payload-manifest/1.0.0.schema.json, and src/autoharness/schema_contracts.py (registration entry only). THIS TASK MUST NOT WRITE UNDER tests/** (corrected in extended review-fix cycle 4, finding 8 - the earlier inclusion of "and tests/" in this list is WITHDRAWN and MUST NOT be reintroduced). It contradicted this record's own statement that the task "authors no case and preserves no CHARACTERIZATION case", and it contradicted the plan H2 writable-surface table, whose tests/** row authorizes ONLY 160.005-T, 160.016-T, 160.008-T, 160.012-T, 160.013-T, 160.009-T, 160.010-T and 160.017-T. This task is a GREEN OWNER ONLY: its two RED-FIRST cases are authored and observed red by 160.005-T, and it must turn them green THROUGH SCHEMA PUBLICATION AND THE REGISTRATION ENTRY ALONE. Running the test suite is permitted and required; MODIFYING it is not. It MUST NOT modify pyproject.toml, marketplace.json, plugin-payload/, release.yml, the manifest, or any other src/autoharness/ file. NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2 (writable-surface table).

ACCEPTANCE: AC1, AC3d. Both schema files published at 1.0.0 with the closed three-channel overlay enum and the `generated_output_roots` key declared; contract registered in SCHEMA_CONTRACTS; pair-divergence assertion shipped; the two RED-FIRST cases named above go from red to green and the transitions are recorded for 160.017-T's ledger.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size S, complexity medium. Two small JSON documents and one registry line; complexity is medium only because the narrow src/ carve-out must not be widened.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T2b)