---
artifact_type: task
created_at: 2026-09-03T10:14:55.0589127Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.005-T
id: 160.018-T
labels:
    - packaging
    - schema
    - contract-registration
parent_id: 160-F
priority: medium
status: queued
title: Publish payload-manifest schema (1.0.0 live + versioned mirror) and register the contract
updated_at: 2026-09-03T10:14:55.0589127Z
---

SCHEMA PUBLICATION (T2b). NEW in review-fix cycle 2, SPLIT OUT OF 160.001-T (findings 1 and 16).

WHY THIS TASK EXISTS - TWO DEFECTS IT CLOSES:
(1) PRODUCTION BEFORE A RED OWNER (finding 1). Cycle 1 let 160.001-T publish the schema, its versioned mirror, and the SCHEMA_CONTRACTS registration with NO RED OWNER ANYWHERE - production artifacts landing before any test observed their absence. This task is BLOCKED BY 160.005-T (the RED-FIRST harness) and is the NAMED GREEN OWNER of two RED-FIRST cases, so the contract is now observed failing before it is created.
(2) SIZE/SCOPE INCOHERENCE (finding 16). Cycle 1's single S/low task combined baseline capture across three channels, two schema documents, runtime contract registration, and an immutability assertion - four deliverables with different reversibility. Recording what exists today (160.001-T) and publishing a NEW VERSIONED CONTRACT (this task) are different kinds of work and are now separately sized.

GREEN RESPONSIBILITY (assigned by the RED-FIRST harness 160.005-T): this task turns these RED-FIRST cases green:
- test_schema_live_and_versioned_mirror_agree (AC1)
- test_payload_manifest_contract_registered (AC1)
It does NOT own test_manifest_validates_against_schema - that case needs the MANIFEST as well and its green owner is 160.003-T.

THREE MANDATORY DELIVERABLES, all acceptance criteria, none deferrable:

(1) PUBLISH BOTH FILES. schemas/payload-manifest.schema.json (the live contract) AND the immutable versioned mirror schemas/payload-manifest/1.0.0.schema.json. This is the two-file convention observed on EVERY other versioned schema in this repository (harness-config, harness-manifest, workspace-profile, tool-telemetry-event, validation-gates). A top-level schema with NO versioned mirror is a conformance FAILURE. The schema declares `include`, `exclude`, PER-CHANNEL OVERLAYS FOR ALL THREE CHANNELS - `wheel`, `sdist`, AND `plugin` (the sdist overlay is NEW in cycle 2, finding 3) - and the single `target_workspace_paths` key (the SOLE authored occurrence of those prefixes across executable and configuration surfaces; see AC2d and 160.004-T's centralized classifier).

(2) REGISTER THE CONTRACT in src/autoharness/schema_contracts.py so the existing mutation detector covers payload-manifest from its FIRST release rather than after the first incident. This repository has THREE RECORDED OCCURRENCES of in-place schema mutation without a version bump (docs/compound/2026-08-30-157-s-149-f-schema-mutation-in-place-third-occurrence.md), and that learning records that the shorter, more dangerous fix path applies EXACTLY to CONTRACTS UNREGISTERED IN SCHEMA_CONTRACTS: an unregistered schema has NOTHING that detects a later in-place mutation, so a fourth occurrence would land silently.

(3) ASSERT THE PAIR. The test that FAILS if the live file and the 1.0.0 mirror diverge while the declared $id/version still reads 1.0.0. That assertion is precisely what was absent in all three prior occurrences.

CRITICAL: this is a NEW schema at 1.0.0. Do NOT mutate any existing schema in place.

FREEZE-SCOPE INTERACTION - THE NARROW src/ EXCEPTION (corrected in cycle 2, finding 4): cycle 1 simultaneously forbade ALL src/autoharness/ edits and required deliverable (2) as a non-deferrable acceptance criterion - a direct contradiction that made the plan's own acceptance unexecutable under its own safety mode. THIS TASK IS THE ONLY TASK IN SHIP-10 AUTHORIZED TO TOUCH src/autoharness/, and its authorization is bounded to ADDING THE payload-manifest CONTRACT REGISTRATION ENTRY IN schema_contracts.py AND NOTHING ELSE. No other statement, function, or file in src/autoharness/ may be modified. The two rules are NOT in conflict: the freeze-scope prohibition governs RUNTIME BEHAVIOUR (_DATA_DIR resolution order, `autoharness home`, `autoharness version` - invariants I1/I2), while this authorization governs a CONTRACT-REGISTRY DECLARATION that changes no runtime behaviour. A single-line registration is not a licence to edit src/.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. Writable surface for this task is EXACTLY: schemas/payload-manifest.schema.json, schemas/payload-manifest/1.0.0.schema.json, src/autoharness/schema_contracts.py (registration entry only), and tests/. It MUST NOT modify pyproject.toml, marketplace.json, plugin-payload/, release.yml, the manifest, or any other src/autoharness/ file. NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2 (writable-surface table).

ACCEPTANCE: AC1. Both schema files published at 1.0.0 with all three channel overlays declared; contract registered in SCHEMA_CONTRACTS; pair-divergence assertion shipped; the two RED-FIRST cases named above go from red to green and the transition is recorded for 160.017-T's ledger.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T2b)