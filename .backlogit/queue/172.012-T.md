---
artifact_type: task
created_at: 2026-09-19T01:01:28.5384884Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 172.001-T
id: 172.012-T
labels:
    - test
    - tdd
    - red-phase
    - checkpoint
    - cli
parent_id: 172-F
priority: high
status: queued
title: 'Resume-hint T7a (RED): executable historical scan and CLI/MCP parity tests observed failing'
updated_at: 2026-09-19T01:01:28.5384884Z
---

TDD RED PHASE - EXECUTABLE HISTORICAL SCAN, CLI/MCP PARITY. Author the contract tests for the startup historical scan and OBSERVE THEM FAILING before the scan exists.

THE SCAN IS EXECUTABLE, NOT PROSE. Assert that autoharness checkpoint scan --json is an INVOCABLE COMMAND producing machine-readable JSON. An agent Markdown surface cannot "call a Python predicate" by description; it can only invoke a command and read its output. Asserting the command is the only form of assertion that distinguishes "a startup surface ran the scan" from "a predicate exists somewhere in the package".

CLI/MCP PARITY OVER THE SAME CORPUS. Assert the CLI command and the guarded MCP scan operation produce IDENTICAL STRUCTURED TOKENS and IDENTICAL JSON over the same fixture corpus, because they call the SAME adapter. Divergence between two front ends over one corpus is the drift this parity assertion exists to prevent.

ORIGIN IS UNREACHABLE FROM EITHER FRONT END. Assert no --origin option exists on the CLI and no origin parameter is accepted by the MCP operation. The historical origin is bound internally by the scanner alone.

OUTPUT CONTRACT: the JSON enumerates every record with its classification bucket, reports the legacy set by filename and count, and reports the blocking set explicitly. Assert a non-empty blocking set is surfaced as a distinct exit condition the startup surface can branch on, covering BOTH the non-terminal CHECKPOINT_ACTIVE_HINTLESS branch and the CHECKPOINT_RECORD_MALFORMED branch.

IMPORT SAFETY (BINDING): this module imports cleanly under unittest.defaultTestLoader with zero loader.errors and zero _FailedTest placeholders; not-yet-existing boundaries are loaded INSIDE the test body through a _load_boundary() helper. Every declared test individually executes and fails with its own declared marker in its own detail_text.

Blocked by 172.001-T (the outcome set must be defined before tests can assert it). It DOES NOT depend on 172.013-T. Blocks 172.013-T. Scope: tests/. Stash 2A7C48A8. Plan docs/plans/2026-09-17-checkpoint-resume-hint-contract-plan.md (revision 7). Verdict manifest docs/reviews/2026-09-17-checkpoint-resume-hint-contract-plan-review.md (latest_attempt 07, verdict REMEDIATED-PENDING-REVIEW at plan revision 7, awaiting independent attempt 08).