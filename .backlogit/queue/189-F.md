---
artifact_type: feature
created_at: 2026-09-26T07:45:51.8108915Z
custom_fields:
    harness_status: pending
id: 189-F
labels:
    - ship-lifecycle
    - release-unit
    - unit-b
    - unit-b-entry
    - harness-resolver
    - lifecycle-e5
priority: high
references:
    - docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md
    - docs/reviews/2026-09-25-ship-lifecycle-release-units-plan-review.md
status: queued
title: 'Ship lifecycle Unit B-entry: observation ledger and digest, reducer with early return, resolve_shipment entry and harness resolve CLI (harness_surfaces.py, cli.py)'
updated_at: 2026-09-26T07:45:58.5953855Z
---



<!-- BEGIN:description -->
Release unit S(B-entry) of the ship lifecycle release units plan (docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md, revision 9, commit 42fec73f, blob b7a77c7644fa97dff01c724480abfcbcea8f346d, section 'Unit B: One-Entry Resolver, Schema and CLI', tasks B4a to B5; the plan's Release Units and Budgets table and PD-15 split Unit B into S(B-core) and S(B-entry)). Review authority: manifest docs/reviews/2026-09-25-ship-lifecycle-release-units-plan-review.md, epoch LIFECYCLE-E5-b7a77c76, E5 Verdict (dispatch_mode: explicit-model subagent; decision: PASS; publication met), read under operator ruling OP-5 (2026-09-25T23:56:45-07:00). No '## Plan Review' section exists in the plan; the manifest governs.

Goal: the second half of src/autoharness/harness_surfaces.py plus the CLI adapter, giving a working, callable, schema-valid resolver. B4a ledgers every present or absent candidate and every surface read, re-observes the ledger through the same reader after the provisional result (a completed recheck that disagrees -> class 1), and computes inputs_sha256 over a domain-separated canonical preimage. B4b adds the keyword-only entry resolve_shipment(*, workspace_root, shipment_id) over the private _resolve(*, workspace_root, shipment_id, limits) with the FI-2 defaults (limits is the only B patch point, IM-06), the reducer order 1, 1b, 2 to 8 with first-applicable selection (FI-6) and the FI-5 early return. B5 adds 'harness resolve --workspace <path> --shipment <id> --json' in src/autoharness/cli.py: after a successful parse, exactly one UTF-8 document plus one LF on stdout, nothing on stderr (CR-B5), exit equal to the document's exit_code.

Budget: 3 tasks, 275 minutes (within the 6-task / 480-minute shipment limit, IM-11). Tasks, serial (one module, shared file): B4a observation ledger, recheck and digest (85) -> B4b reducer, early return and resolver entry (110) -> B5 CLI adapter (80). B1 to B3 are S(B-core) (188-F), harvested separately.

Requirements trace: IM-03 (B5 half: all 47 codes through _resolve, every code reachable with the defaults also through resolve_shipment and the CLI, every output validates against the B1 schema), IM-04 (B4b: injection at each ReadStage value, multi-error first code, byte-code applicability at absent candidates, precedence without short-circuit), IM-06 (B4b: private limits hook only), IM-07 (B5: stderr rule, CR-B5), IM-10 (B's tests use fixtures only, never the real surface at HEAD), IM-13 (RED practice for every harness-surface:harness-architect task), IM-14 / PE-SAFETY-06 (B4a's to B5's paths and the captured 'harness resolve --help' text join the audit inventory as residue units; FLOOR is unchanged by B-entry; P2-critical), IM-15 (a test or proof PASS confers no claim authority), IM-16 (B4b: early return tested in both orders), PE-DATA-01 to 03 (B4b, B5), IM-11 / PE-TASK-02 (budgets, two-axis sizing).

Dependency position: S(A) -> S(C) -> S(B-core) -> S(B-entry) -> S(D). S(B-entry) is blocked by S(B-core) (194-S) and blocks S(D); S(D) is harvested only after the IM-10 shipment exists, and its blocks edge from S(B-entry) is created with it. No Linux evidence (IM-01, C5) -> C, B and D do not ship.

Harvest note (IM-14-F67, E5 carry): lines created by resolving a conflict in a harvest merge commit fall outside every non-merge harvest commit's diff; harvest merges must be conflict-free, or the combined diff of a conflicted merge is audited.
<!-- END:description -->

<!-- BEGIN:dod -->
B4a to B5 done with the canonical suite green; plan Runtime Verification row B-entry (runtime surface: CLI 'harness resolve'; proves one document per class, stderr empty, exit equals exit_code, schema-valid) with its closure artifact: a recorded CLI transcript (bytes and SHA-256) for one fixture per state; tests/test_harness_surfaces_digest.py, tests/test_harness_surfaces_reducer.py and tests/test_harness_resolve_cli.py green; tests/test_harness_noclaim_audit.py green with FLOOR unchanged by B-entry (harness_surfaces.py already listed by B1) and any LEDGER entries B-entry adds confirmed by local review; the harness-resolution 1.0.0 schema files unchanged (a schema defect found here reverts B-core, never a 1.1.0 bump); no B-entry test reads the real repository surface (IM-10); ordinary P-001/P-002/P-004/P-020 claim and closure gates observed (IM-15); IM-14 incremental residue text audit recorded at closure (the shipment merge diff as a closure note with the LEDGER count, and the closure pull request diff with the body as the record).
<!-- END:dod -->

<!-- BEGIN:goals -->
A working, callable, schema-valid resolver: resolve_shipment and 'harness resolve --json' return one schema-valid document per class with exit equal to exit_code, a deterministic inputs_sha256 bound to every input, and a fail-closed early return on the first read-limit error, all over temporary-workspace fixtures. No Ship caller exists until D3, so the command stays inert (rollback: revert B-entry's commits, then B-core's, Ship with operator approval; if a release tag included B, removing schema 1.0.0 or the CLI command is a contract removal and needs an operator decision instead of a plain revert; no release tag includes S(B-core) before S(B-entry) closes).
<!-- END:goals -->