---
artifact_type: task
created_at: 2026-08-02T04:51:47.7573152Z
custom_fields:
    size: S
id: 105.002-T
labels:
    - ship-integrity
    - shipment-reconcile
    - skill-template
parent_id: 105-F
priority: medium
status: queued
title: T1 Add shipment-record-status classification to shipment-reconcile pre-mode template
updated_at: 2026-08-02T04:51:47.7573152Z
---

Edit templates/skills/shipment-reconcile/SKILL.md.tmpl ONLY. Add a shipment-record-status classification to pre-mode: (1) Output section — add the record-scope classification with a mutually exclusive compatibility matrix, partitioned first by the record's own status (record-consistent; record-queued-with-active-work = record status is queued AND a manifest task is active/done; record-blocked-with-active-work = record status is blocked AND a manifest task is active — takes precedence over record-blocked-with-done-work when a blocked record has both an active task and a done task; record-blocked-with-done-work = record status is blocked AND no task is active AND a manifest task is done). (2) Required Protocol > Pre-Mode — add a step that reads the shipment record's own status (already loaded via {{OP_GET_SHIPMENT_MCP}} in step 2) and compares it to the manifest tasks' statuses already read in step 3 (reuse in-hand data; NO new scan — P2-2). (3) Gate — on any inconsistency set recommendation to 'HALT — operator reconcile required' naming shipment id + record status + conflicting task ids; detect-and-report, NO auto-repair. (4) Quality Criteria — add a row requiring the shipment-record-status classification. Verify: classification present in Output table; the four cases are mutually exclusive with the blocked+active-over-blocked+done precedence rule stated explicitly; Pre-Mode step performs record-vs-tasks compare; recommendation HALTs on inconsistency; Quality Criteria updated; rendered template has no unresolved {{...}}.