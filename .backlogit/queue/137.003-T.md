---
artifact_type: task
created_at: 2026-08-21T00:46:11.9083567Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 137.002-T
id: 137.003-T
labels:
    - templates
    - dogfood
    - agent-contract
    - deprecation
parent_id: 137-F
priority: medium
references:
    - docs/plans/2026-08-20-ship-stash-archive-operation-migration-plan.md
    - docs/plans/2026-08-20-ship-stash-archive-operation-migration-hardening.md
status: queued
title: Migrate Ship agent template and dogfood mirror to backlogit_stash_archive
updated_at: 2026-08-21T00:53:18.9708204Z
---

Migrate the Ship agent contract off the deprecated backlogit_stash_remove. This is a PAIRED EDIT on a divergent template/dogfood pair — perform it under the maintenance contract landed in 137.002-T.

FILES AND SITES (verified by Stage)
- templates/agents/_ship.agent.md.tmpl — line 38 (Role Boundary table), line 819 (post-merge Step 7)
- .github/agents/_ship.agent.md — line 47 (Role Boundary table)

*** TOOL SURFACE GROUND TRUTH (verified read-only against installed backlogit v1.10.0) ***
BOTH surfaces expose the archive operation as primary. There is NO inversion and NO missing surface.
- MCP: backlogit_stash_archive ("Archive an active stash entry") is EXPOSED and is the PRIMARY operation. backlogit_stash_remove is also exposed but self-described "[Deprecated: use backlogit_stash_archive]".
- CLI: `backlogit stash archive <id>` is the canonical subcommand; `stash remove` exists only as an ALIAS resolving to the same archive handler.
- .mcp.json runs the same `backlogit mcp` executable with tools ["*"], so backlogit_stash_archive is reachable in this workspace.
A direct rename to the archive operation is therefore correct, executable, and complete on both surfaces.

REQUIRED WORDING (P-012 MCP-first / CLI-fallback)
1. MCP primary: call backlogit_stash_archive.
2. CLI fallback: `backlogit stash archive <id>` — exposed, non-destructive.
3. Do not describe the operation in removal semantics.
4. backlogit_stash_remove MUST NOT be specified as an execution fallback or any other prescriptive path. It may appear ONLY as non-prescriptive legacy/deprecation context describing the behaviour being removed.

PRESERVE the surrounding clause wording exactly, including the parenthetical distinguishing this manifest-derived closure operation from discretionary removal — that distinction is load-bearing for P-021 C5 and must survive verbatim.

At the Step 7 site, update the follow-on sentence so the already-retired case reads in archive terms: "If the stash entry is already archived, skip and log it."

HARDENING CONSTRAINTS
- H1 (P1): this task and 137.005-T (verifier marker) are MUTUALLY BREAKING if split. verify_workspace asserts .github/agents/_ship.agent.md CONTAINS backlogit_stash_remove. This task alone turns the verifier red; 137.005-T alone turns it red. They MUST land in the same commit, or in adjacent commits within a single PR that is never evaluated between them. Do not split across PRs.
- H4 (P2): the mirror carries ONLY the Role Boundary site (1) while the template has 2. This asymmetry is EXPECTED and MUST NOT be "fixed" by back-porting the missing Step 7 block into the mirror. That back-port is the 6D62077C prose-drift problem and is explicitly out of scope.

ACCEPTANCE
- Both files name backlogit_stash_archive as the MCP primary and `backlogit stash archive` as the CLI fallback, and no longer present removal as the primary semantics.
- No prescriptive execution path in either file names backlogit_stash_remove.
- The documented path is executable on backlogit v1.10.0 as it actually ships.
- Role Boundary clause semantics unchanged apart from the operation.
- Both sides changed in one commit.

Plan: docs/plans/2026-08-20-ship-stash-archive-operation-migration-plan.md (Task A + ADDENDUM)
Hardening: docs/plans/2026-08-20-ship-stash-archive-operation-migration-hardening.md (H1, H4)