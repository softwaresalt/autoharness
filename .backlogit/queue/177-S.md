---
artifact_type: shipment
created_at: 2026-09-18T05:09:03.5965398Z
custom_fields:
    items:
        - 169-F
        - 169.011-T
        - 169.009-T
        - 169.010-T
        - 169.012-T
        - 169.013-T
        - 169.014-T
        - 169.017-T
        - 169.015-T
        - 169.016-T
        - 169.007-T
id: 177-S
labels:
    - contract
    - shipment-claim
    - p-002-7
    - stage-2026-09-17-portfolio
priority: high
status: queued
title: SHIP-19 - Canonical post-claim member-status contract (P-002.7) and cross-surface structural evidence (3EF5AAF2)
updated_at: 2026-09-19T19:06:17.062952Z
---

SHIP-19 reduced. THE DAG ROOT of this portfolio's defect units: it changes declarations and asserts their agreement, needs no operation substrate, no execution boundary and no schema migration, and therefore has NO precursor dependency. Serializing it behind 182-S/184-S/185-S would be a false serial dependency. Delivers the canonical post-claim member-status vocabulary plus a rule-derived cross-surface conformance test, with per-assertion RED observation recorded before GREEN and the complete evidence set adjudicated BEFORE activation.

MANIFEST ORDER IS PHASE ORDER, AND THE PHASE ORDER CONFORMS TO DECISION D2. Items are listed PREPARE, RED, VERIFY, ACTIVATE, CONFIRM, DOCS, so reading the manifest top to bottom shows every assertion proven red, then the complete evidence set adjudicated while every surface is still inert, and only then the commit that turns the contract green. The order is: 169-F covering feature; 169.011-T PREPARE, authoring the canonical definition, the surface-enumeration rule and the near-miss fixtures as inert test-owned data at the two named helper paths tests/p002_7_candidate_definition.py and tests/p002_7_near_miss_fixtures.py; then the five RED tasks 169.009-T, 169.010-T, 169.012-T, 169.013-T and 169.014-T; then 169.017-T VERIFY, the PRE-ACTIVATION READINESS GATE; then 169.015-T, the SINGLE ACTIVATE task whose ONE COMMIT touches all four declared surfaces; then 169.016-T CONFIRM, the post-activation confirmation, which observes and adds no assertion; then 169.007-T DOCS, which is itself verdict-gated.

ATTEMPT-04 M1 (P1) AND WHAT CHANGED. Attempt 04 recorded that the revision-4 order - PREPARE, RED, ACTIVATE, VERIFY, DOCS - contradicted binding decision D2, whose rollout invariant is PREPARE then VERIFY then ACTIVATE with the complete evidence set produced before any activation. Under that order the unit's ONLY gate emitter sat AFTER the single irreversible commit that mutates all four declared surfaces, so a non-pass verdict was reachable only by reverting a landed commit. The revision-5 remediation splits the two roles that were fused in one record. 169.017-T is NEW and is the pre-activation evidence gate. 169.016-T was RETAINED AND RETITLED as the distinct post-activation confirmation step rather than archived, because its substantive work - installed/template parity, active-consumer behaviour, and the five RED-proven families observed against the SHIPPED text - is genuinely post-activation and has no other owner. THE MAPPING ONTO D2 IS EXPLICIT: this unit's PREPARE and RED together are D2's PREPARE, whose own rule is that tests go RED first then GREEN entirely within the inert surface; 169.017-T is D2's VERIFY; 169.015-T is D2's ACTIVATE. CONFIRM and DOCS are post-activation steps D2 neither names nor forbids. D2's compatibility limb points at the D3 pinned-fixture corpus and is INAPPLICABLE to this unit, which introduces no normalizer and reads no historical record corpus; that is recorded rather than skipped.

BOTH VERDICT-CROSSING EDGES ARE AUTHORIZED BY A TOKEN, NOT BY AN EDGE. 169.017-T evaluates, against 169.011-T's INERT candidate with no declared surface yet changed, three per-assertion observations for each of the five families: ABSENCE RED against the current unmutated surfaces, DISCRIMINATING RED against the named near-miss fixture, and INERT GREEN against the candidate definition as test-owned data. It also requires the INERT PRECONDITION, resolved_surface_count = 0. It emits exactly one of PREACTIVATION_READY, PREACTIVATION_BLOCKED or PREACTIVATION_NOT_OBSERVED, evaluating NOT_OBSERVED first and fail-closed. 169.015-T READS THAT VERDICT AS ITS FIRST ACTION AND FAILS CLOSED: the gate is OPEN only when the file exists, is readable, its WHOLE CONTENT is EXACTLY ONE PREACTIVATION_STATE: line, its token is the literal PREACTIVATION_READY, and its F1-F5 freshness binding recomputes; everything else is CLOSED, and on a CLOSED gate 169.015-T touches NO declared surface at all, exits 1, and returns the unit to Stage. 169.007-T DOCS APPLIES THE SAME PREDICATE TO THE CONFIRMATION ARTIFACT: whole-file, literal STATUS_CONTRACT_HELD, plus F1-F5; on a CLOSED gate it touches NO documentation file, makes NO COMMIT, exits 1, and returns the unit to Stage. On BOTH edges the blocks edge is ORDERING ONLY - it guarantees the artifact exists before it is read, and it clears on PREDECESSOR COMPLETION, which each emitter reaches on all three of its tokens, two of which are non-authorizing. This is the gate-predicate shape attempt 03 finding K1 (P1) required of 177-F's I1 gate. Through revision 5 only the ACTIVATE edge carried it; attempt-05 finding N1 (P2) recorded the CONFIRM-to-DOCS edge as a bare completion edge despite four plan assertions that DOCS must not proceed on a non-pass confirmation, and revision 6 closes it.

TWO VERDICTS, TWO ARTIFACTS, NEVER CONFLATED. PRE-ACTIVATION: .autoharness/gates/p002-7-preactivation-readiness.txt, sole writer 169.017-T, prefix "PREACTIVATION_STATE: ", tokens PREACTIVATION_READY (the only token that authorizes activation), PREACTIVATION_BLOCKED, PREACTIVATION_NOT_OBSERVED. POST-ACTIVATION: .autoharness/gates/p002-7-status-contract-verdict.txt, sole writer 169.016-T, prefix "COMPOSED_STATE: ", tokens STATUS_CONTRACT_HELD (the only pass, and the only token that authorizes documentation), STATUS_CONTRACT_DIVERGENT, STATUS_CONTRACT_NOT_OBSERVED. Different paths, different prefixes, DISJOINT vocabularies; neither emitter reads or writes the other's path, and neither vocabulary is valid in the other's file, so a foreign token can never open either gate. THE POST-ACTIVATION VERDICT IS A CONFIRMATION AND IS NEVER THE EVIDENCE GATE THAT AUTHORIZED ACTIVATION. Attempt 02 finding G1 (P1) recorded an earlier two-token emission that deleted the not-observed state; attempt 03 finding L1 (P1) recorded a verdict line with no declared destination and no line format.

THE FRESHNESS BINDING (attempt-05 finding N2, P2). Attempt 05 recorded that all three readiness line forms carried a checked= vintage that R12 named as the stale-verdict mitigation while 169.015-T's declared activation conditions NEVER READ IT, so the documented post-revert re-activation path had no mechanical freshness binding. Revision 6 makes the vintage a PREDICATE and binds each verdict to the IMMUTABLE IDENTITY of the evidence it was computed from, using ONE set of shared primitives instantiated twice. CCD/v1 is a canonical content digest over a fully enumerated ORDERED list of repository-relative POSIX paths - CRLF normalized to LF only, lowercase-hex sha256 per file, concatenated as <path> LF <file-digest> LF and hashed - with SEVEN inputs for the readiness candidate_digest (the four declared surfaces plus the two helper modules and tests/test_p002_7_member_status_contract.py) and FOUR for the confirmation surface_digest (the declared surfaces alone, because DOCS documents shipped text and must not be closed by a test-only edit). EVERY ONE OF THE SEVEN READINESS INPUTS NOW HAS ONE UNAMBIGUOUS PRODUCER (attempt 06 finding O1, P2). The four declared surfaces are written by 169.015-T; tests/p002_7_candidate_definition.py and tests/p002_7_near_miss_fixtures.py are authored in PREPARE by 169.011-T; and tests/test_p002_7_member_status_contract.py is CREATED BY RED TASK 169.009-T at that exact path and EXTENDED IN PLACE by 169.010-T, 169.012-T, 169.013-T and 169.014-T, none of which creates a second test module. Through revision 6 that seventh input was named only at consumers and narrators while all five RED tasks including its creator declared only Scope tests/, so the path could drift and make the digest undefined by CCD/v1 rule 1, halting the unit at 169.017-T with PREACTIVATION_NOT_OBSERVED | reason=digest_input_unreadable. Revision 7 declares it at 169.009-T and in the plan's Tasks, Producer, Blast-radius and Assertion-to-task surfaces, so the exact path cannot drift without a detectable plan/task/consumer mismatch. This is a DECLARATION CHANGE, NOT A SCOPE CHANGE: no assertion moved, no RED task's responsibility widened, and the task topology and all thirteen item_deps edges are unchanged. The head identity pair head_commit and head_committed_at is read from the repository. B/v1 is a binding digest over five LF-terminated lines - tag, head_commit, content_digest, resolved_surface_count, checked - under the tags p002-7-preactivation-binding/v1 and p002-7-confirmation-binding/v1, so checked is COVERED by the binding and cannot be altered after emission. BOTH AUTHORITATIVE CONSUMERS RECOMPUTE F1-F5 against the repository rather than trusting the line: F1 head identity equals current HEAD; F2 content digest equals recomputation; F3 resolved_surface_count equals the phase value, 0 for readiness and 4 for confirmation; F4 checked is present once, matches RFC 3339 UTC YYYY-MM-DDThh:mm:ssZ, and is not earlier than the head commit's committer timestamp; F5 binding equals B/v1 recomputation. ANY failure is CLOSED, with no partial satisfaction. THIS CLOSES THE POST-REVERT PATH MECHANICALLY: after a revert the four surfaces are BYTE-IDENTICAL to their pre-activation state, so a digest over surfaces alone would still match, but git revert NECESSARILY CREATES A NEW COMMIT, so F1 FAILS. F1 ALONE rejects the stale readiness line; F1 AND F2 reject the stale confirmation line, because the reverted surfaces no longer reproduce its surface_digest. F4 REJECTS NEITHER AND IS NOT AN INDEPENDENT POST-REVERT FRESHNESS GUARD - it compares checked against the committer timestamp of the COMMIT THE LINE ITSELF NAMES, which a stale line satisfies; revision 6's claim that F4 closed the path a second, independent way was false and was recorded as attempt 06 finding O3 (P2), and revision 7 removes it from the plan and from every record. F4's genuine function is to reject a line whose vintage PRECEDES ITS OWN HEAD COMMIT, and with F5 to prevent checked being altered after emission. BECAUSE F1 CARRIES THE READINESS PATH ALONE, THE SUPPORTED RECOVERY PATH IS MERGE-FRIENDLY ONLY: git revert and new forward commits. GIT RESET --HARD TO THE PRE-ACTIVATION COMMIT, AND ANY REBASE OR AMEND RESTORING THE PRE-ACTIVATION COMMIT IDENTITY, ARE PROHIBITED AND OUTSIDE THIS CONTRACT and are offered as an allowed rollback route nowhere in this unit, because they would restore exactly the HEAD and content a stale readiness line describes. The contract is ENVIRONMENT-AGNOSTIC - relative POSIX paths, CRLF normalization, no host, user, absolute path or clock input - stays entirely within the existing ignored .autoharness/gates/ boundary, adds no new artifact, directory or ignored path, and is honestly scoped as STALENESS AND MISTAKE DETECTION rather than a tamper-proof security boundary. Recomputing a digest is EMITTER AND GATE LOGIC, NOT AN ASSERTION: it produces no assertion outcome and never enters the conformance suite.

SHARED ARTIFACT CONTRACT, APPLIED TO BOTH. Both destinations sit under .autoharness/gates/, gitignored at .gitignore:7, so neither verdict is ever committed and neither leaves the working tree dirty. NEITHER IS A DECLARED SURFACE: the enumeration rule's search scope is templates/policies/, .github/policies/, templates/agents/ and .github/agents/, so declared_surface_count stays fixed at 4 and a generated gate artifact can never be counted as a fifth; the same holds for the two named helper modules, which lie outside that scope and do not match unittest discover's default test*.py pattern, so they add neither a declared surface nor a collected test module. Each file holds EXACTLY ONE verdict line in one literal form per token, with fields separated by " | " and the token as the first field, and only the AUTHORIZING form carries head_commit, the content digest and binding - a not-observed verdict is reachable precisely when inputs are unreadable, so its digest is undefined by rule and could not be emitted truthfully. Each sole writer writes ATOMICALLY via same-directory temp file plus rename; each run REPLACES THE WHOLE FILE and never appends, so RE-RUNNING AN EMITTER NECESSARILY PRODUCES A FRESH BINDING and no previous run's binding can survive alongside a new verdict. Absence evaluation is FAIL-CLOSED and identical in shape for both: missing, unreadable, empty, no prefixed line, MORE THAN ONE such line, or an unrecognised token all resolve to that artifact's NOT_OBSERVED token. A FRESHNESS FAILURE IS A GATE FAILURE, NOT AN ARTIFACT REWRITE: it closes the consumer's gate and never authorizes the consumer to rewrite, amend or delete an artifact it does not own; the only remedy is to re-run the emitter. Each emitter EXITS ZERO ONLY on its single authorizing or passing token and non-zero otherwise, so exit code and artifact carry the same verdict by construction - but the exit code is the WEAKER of the two mechanisms on each edge, because it depends on the invoking process honouring it, while the consumer's own first-action read does not.

POST-ACTIVATION FAILURE HAS AN EXPLICIT ROLLBACK AND HALT PATH. On STATUS_CONTRACT_DIVERGENT or STATUS_CONTRACT_NOT_OBSERVED, 169.016-T exits non-zero, the unit HALTS, and 169.007-T DOCS DOES NOT PROCEED - A HALT THAT IS NOW MECHANICAL, because 169.007-T's own first-action read finds a token that is not the literal STATUS_CONTRACT_HELD and therefore touches no documentation file, makes no commit and exits 1, without depending on any process having observed 169.016-T's exit code. The single 169.015-T activation commit is REVERTED AS A UNIT with git revert, returning all four declared surfaces to their pre-activation divergent state SIMULTANEOUSLY - a guarantee that holds only because activation is one commit. THE REVERT RE-CLOSES BOTH GATES WITHOUT EITHER CONSUMER BEING TOLD: HEAD advances past every head_commit written before it, so a surviving PREACTIVATION_READY line fails F1 - F1 ALONE, which is sufficient - and a surviving STATUS_CONTRACT_HELD line fails F1 AND F2. F4 FAILS ON NEITHER and is no part of this guarantee, being anchored to the commit the line itself names (attempt 06 finding O3, P2). ONLY MERGE-FRIENDLY RECOVERY IS SUPPORTED - git revert and new forward commits; git reset --hard, rebase and amend restoring the pre-activation identity are PROHIBITED and outside this contract. The unit then returns to Stage. 169.016-T never repairs a surface in place, never amends the activation commit, and never weakens an assertion to reach HELD; a divergence returns to a RED task and the unit re-runs 169.017-T for a fresh readiness verdict before any re-activation.

ABSORBED MEMBERS. Five tasks were archived with absorption provenance during the attempt-01 remediation cycle and are no longer manifest members: 169.001-T, 169.002-T and 169.003-T, the split template/mirror activation writes absorbed into 169.015-T under finding A1; and 169.005-T and 169.008-T, the GREEN tasks that introduced un-red assertions, absorbed into RED tasks 169.012-T, 169.013-T, 169.014-T and into the observation-only 169.016-T under finding A2. They were archived, not deleted; each archived record states what absorbed it and why. No record was archived in the revision-5 or revision-6 cycles, and no record was added in the revision-6 cycle.

ITEM_DEPS EXPRESS EXACTLY THE MANIFEST ORDER. The five RED tasks depend on 169.011-T; 169.017-T depends on all five RED tasks; 169.015-T depends on 169.017-T ALONE; 169.016-T depends on 169.015-T; and 169.007-T depends on 169.016-T. Thirteen edges, one topology, no cycle, and 177-S has no successor shipment. Exactly TWO of those edges cross a three-token verdict - 169.015-T from 169.017-T, and 169.007-T from 169.016-T - and on both the edge supplies ordering only while the token plus its freshness binding supplies the authorization; the remaining eleven cross no verdict. The five direct RED-to-ACTIVATE edges that existed through revision 4 were removed in the revision-5 cycle and replaced by the single edge through the gate: the transitive ordering is identical, and routing it through 169.017-T makes the gate the SOLE immediate predecessor of activation. Attempt 02 finding G2 (P2) recorded that the five archived tasks once carried live edges describing the retired T1-T6 chain; those six edges were removed in the attempt-02 cycle and the archived records keep their absorption provenance in prose only.

THIS UNIT IS SELF-CONTAINED AND ADDS NO CI GATE. 169.017-T's and 169.016-T's own invocations produce their respective verdicts, and 169.015-T's and 169.007-T's own first-action reads are the authoritative, independent evaluators of them. .github/workflows/ci.yml is NOT a verdict consumer - its test job runs the stdlib unittest suite and consumes an exit code, PRODUCING the observations the verdicts are derived FROM, which is the reverse direction. No task in this shipment modifies ci.yml. Ship's claim sequence at .github/agents/_ship.agent.md item 4 consumes the P-002.7 contract text that 169.015-T writes into that declared surface, not either verdict line.

Covering feature 169-F. Ten tasks plus the covering feature. Governing plan: docs/plans/2026-09-18-post-claim-member-status-contract-plan.md (revision 7, disposition REMEDIATED-PENDING-REVIEW, awaiting independent attempt 07). Verdict manifest: docs/reviews/2026-09-18-post-claim-member-status-contract-v2-plan-review.md. Stash: 3EF5AAF2.