---
artifact_type: task
created_at: 2026-08-17T23:35:07.3702277Z
custom_fields:
    complexity: high
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 129.005-T
id: 129.006-T
labels:
    - commit-b
    - migration
    - irreversible
    - BED0DDED
parent_id: 129-F
priority: medium
references:
    - docs/plans/2026-08-17-backlogit-self-migration-plan.md
    - docs/plans/2026-08-17-backlogit-self-migration-hardening.md
status: queued
title: Execute migrate --workspace-dir, assert single root, verify artifact parity
updated_at: 2026-08-17T23:35:07.3702277Z
---

COMMIT B -- THE IRREVERSIBLE STEP. Re-run the exclusive-open lock probe (it must SUCCEED) immediately before proceeding, then execute: backlogit migrate --workspace-dir. IMMEDIATELY assert the fail-closed single-root condition: Test-Path .backlog is True AND Test-Path .backlogit is False. IF BOTH EXIST: STOP. Do not commit, do not continue, do not guess/merge/delete a root to "resolve" it (H10 -- silent disambiguation is exactly the backlog-state-split hazard BED0DDED has forbidden since 2026-08-07). Set BACKLOGIT_WORKSPACE_DIR to restore tooling immediately, then execute the runbook recovery from the out-of-tree backup. On success, verify parity against the 129.004-T inventory: every relative path and size present under .backlog, file count equal, and specifically that queue/, archive/ (820 files), checkpoints/, logs/, templates/, stash.jsonl, telemetry.jsonl, memories.json, hooks.yaml, migration.yaml, registry.yaml, header-def.yaml and config.yaml all survive. PARITY SCOPE (H13): assert on the Markdown/JSONL inventory only -- backlogit.db/-wal/-shm byte differences are EXPECTED and are NOT a failure signal, because backlogit sync rehydrates the index from Markdown source files; but any missing Markdown/JSONL artifact is unrecoverable-by-rebuild and MUST halt the shipment. ACCEPTANCE: exactly one root exists; parity diff empty; no Markdown/JSONL file lost. Plan T006; hardening H10, H13.