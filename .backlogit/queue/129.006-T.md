---
artifact_type: task
created_at: 2026-08-17T23:35:07.3702277Z
custom_fields:
    complexity: high
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 129.005-T
id: 129.006-T
labels:
    - commit-b
    - migration
    - irreversible
    - BED0DDED
parent_id: 129-F
priority: medium
references:
    - docs/plans/2026-08-17-backlogit-self-migration-plan.md
    - docs/plans/2026-08-17-backlogit-self-migration-hardening.md
status: queued
title: Execute migrate --workspace-dir, assert single root, verify artifact parity
updated_at: 2026-08-17T23:56:53.3967155Z
---

COMMIT B -- THE IRREVERSIBLE STEP. PRE-FLIGHT RE-ASSERTION (both must pass immediately before proceeding): (1) re-run the exclusive-open lock probe -- it must SUCCEED (H2); (2) CORRECTED 2026-08-17 -- re-assert that the 129.004-T contained in-repo backup still EXISTS at its recorded path and that its bounded file-count/relative-path inventory check still passes (H4). A missing or mismatched backup at this moment HALTS the shipment; the backup is gitignored and therefore invisible to git, so it must be positively re-verified rather than assumed. Then execute: backlogit migrate --workspace-dir. IMMEDIATELY assert the fail-closed single-root condition: Test-Path .backlog is True AND Test-Path .backlogit is False. IF BOTH EXIST, OR ON ANY PARTIAL MIGRATION: STOP. Do not commit, do not continue, do not guess/merge/delete a root to "resolve" it (H10 -- silent disambiguation is exactly the backlog-state-split hazard BED0DDED has forbidden since 2026-08-07). Follow the H16 NON-DESTRUCTIVE, EVIDENCE-FIRST, OPERATOR-GATED recovery contract: (1) set a VERIFIED BACKLOGIT_WORKSPACE_DIR override -- exactly .backlog or .backlogit (case-sensitive; the resolver rejects paths, separators, . and .., absolute and drive/UNC forms), the named directory exists, is a real directory, and is not a symlink or reparse point -- restoring tooling WITHOUT touching the filesystem (the validated override returns at backlog_root.py:126, before the ambiguity check at :136); (2) PRESERVE both roots and the backup exactly as-is -- delete nothing, restore nothing, move nothing; (3) RECORD EVIDENCE: per-root inventories, which root holds the authoritative config.yaml, git status --porcelain, resolver and backlogit --cwd . output, backup path and verification result, timestamps; (4) HALT and request EXPLICIT OPERATOR APPROVAL for any deletion or restoration -- do not proceed on inference, precedent, or the general migration authorization. ABSOLUTE PROHIBITIONS: never auto-delete a root; never run a broad git checkout -- . or any whole-worktree reset; never guess which root is authoritative. Note WHY the former automatic rollback was withdrawn: .backlogit is git-tracked (E1, 1613 files), so git checkout -- . after the rename would restore .backlogit from HEAD while .backlog still exists on disk, MANUFACTURING the exact dual-root state H10 exists to prevent. A future approved rollback targets an ENUMERATED PATHSPEC LIST plus a named source/destination copy-back only. ON SUCCESS, verify parity against the 129.004-T inventory: every relative path and size present under .backlog, file count equal, and specifically that queue/, archive/ (820 files), checkpoints/, logs/, templates/, stash.jsonl, telemetry.jsonl, memories.json, hooks.yaml, migration.yaml, registry.yaml, header-def.yaml and config.yaml all survive. PARITY SCOPE (H13): assert on the Markdown/JSONL inventory only -- backlogit.db/-wal/-shm byte differences are EXPECTED and are NOT a failure signal, because backlogit sync rehydrates the index from Markdown source files; but any missing Markdown/JSONL artifact is unrecoverable-by-rebuild and MUST halt the shipment. ACCEPTANCE: lock probe and backup re-assertion both passed pre-flight; exactly one root exists; parity diff empty; no Markdown/JSONL file lost. Plan T006; hardening H2, H4, H10, H13, H16.