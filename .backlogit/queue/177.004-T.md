---
artifact_type: task
created_at: 2026-09-19T04:17:29.4461765Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: v1
    size_source: agent
id: 177.004-T
labels:
    - spike
    - ci-isolation
    - credentials
    - security
parent_id: 177-F
priority: medium
status: queued
title: 'S2 T4: determine credential absence (I1) by inspecting the actual job environment'
updated_at: 2026-09-19T05:06:51.0798903Z
---

DETERMINING TASK FOR PROPERTY I1: "No credentials of any kind in the job environment." Attempt 01 found I1 assigned to NO determining task, which made the spike's declared pass state ISOLATION_CHARACTERIZED unreachable by any legitimate execution. This task closes that gap by INSPECTING THE ACTUAL JOB ENVIRONMENT rather than by reasoning about it. METHOD: run a probe job on a GitHub-hosted Linux runner configured exactly as the conformance job would be - permissions block minimized, no secrets passed, checkout credential persistence disabled or checkout omitted - and, FROM INSIDE THAT JOB, record: (a) the NAMES of every environment variable present; (b) the presence or absence of credential-bearing filesystem paths (git credential store and helper configuration, container/registry auth files, cloud and CI credential directories, the runner's own token files); (c) the checkout action's credential-persistence setting in effect; (d) the MECHANISM that produces each absence, so the verdict cites a mechanism and not a default. ACCEPTANCE EVIDENCE: that recorded inventory, taken inside the job, plus the mechanism. A documentation citation alone resolves to ABSENT and yields ISOLATION_UNDETERMINED (H2, R3). EVIDENCE HANDLING IS BINDING: record environment-variable NAMES ONLY and path presence/absence only. NO value, token, key fragment or credential content is written to the findings artifact or to any job log. An inventory that would require recording a secret value to be meaningful is ITSELF the finding that I1 is NOT ACHIEVABLE (R6). This task executes NO untrusted binary and acquires nothing. THIS TASK RUNS FIRST, AND THE ORDERING IS ENFORCED, NOT PREFERRED. It BLOCKS 177.005-T, which pulls a real external release asset onto a runner, and BLOCKS 177.002-T, the adversarial containment probe; 177.001-T inherits the same precedence transitively through 177.005-T. Neither acquisition nor probe work may execute in a job environment whose credential absence is still unverified, because a probe or acquisition job carrying credentials would be a privileged process handling an untrusted artifact even without executing it (H7, H9, R8). Attempt 02 finding F1 (P1) found the previous revision claiming this ordering in the plan's blast radius and H7 while the task table, item_deps and three task records - including this one - asserted independence; the ordering is now a blocks edge in item_deps and no record calls these tasks independent. It also blocks 177.003-T. ELAPSED BOUND 90 min; on reaching it, record I1 as NOT DETERMINED - FLOOR INVOKED, naming what was attempted, what blocked it, and invoking the evidence-and-documentation floor in writing for I1's scope; a bare "not determined" without all three is ABSENT. SECURITY MODEL IS A CONSTRAINT, NOT A QUESTION: if I1 is NOT ACHIEVABLE, 181-S degrades to evidence-and-documentation only; the requirement is never relaxed. Source 7F9CB5E9. Plan docs/plans/2026-09-18-conformance-isolation-spike-plan.md (plan_id conformance-isolation-spike, revision 3, awaiting independent attempt 03). Verdict manifest docs/reviews/2026-09-18-conformance-isolation-spike-plan-review.md.