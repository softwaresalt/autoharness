---
artifact_type: task
created_at: 2026-09-19T04:17:29.4461765Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: v1
    size_source: agent
id: 177.004-T
labels:
    - spike
    - ci-isolation
    - credentials
    - security
parent_id: 177-F
priority: medium
status: queued
title: 'S2 T4: determine credential absence (I1) by inspecting the actual job environment'
updated_at: 2026-09-19T05:06:51.0798903Z
---

DETERMINING TASK FOR PROPERTY I1: "No credentials of any kind in the job environment." Attempt 01 found I1 assigned to NO determining task, which made the spike's declared pass state ISOLATION_CHARACTERIZED unreachable by any legitimate execution. This task closes that gap by INSPECTING THE ACTUAL JOB ENVIRONMENT rather than by reasoning about it. METHOD: run a probe job on a GitHub-hosted Linux runner configured exactly as the conformance job would be - permissions block minimized, no secrets passed, checkout credential persistence disabled or checkout omitted - and, FROM INSIDE THAT JOB, record: (a) the NAMES of every environment variable present; (b) the presence or absence of credential-bearing filesystem paths (git credential store and helper configuration, container/registry auth files, cloud and CI credential directories, the runner's own token files); (c) the checkout action's credential-persistence setting in effect; (d) the MECHANISM that produces each absence, so the verdict cites a mechanism and not a default. ACCEPTANCE EVIDENCE: that recorded inventory, taken inside the job, plus the mechanism. A documentation citation alone resolves to ABSENT and yields ISOLATION_UNDETERMINED (H2, R3). EVIDENCE HANDLING IS BINDING: record environment-variable NAMES ONLY and path presence/absence only. NO value, token, key fragment or credential content is written to the findings artifact or to any job log. An inventory that would require recording a secret value to be meaningful is ITSELF the finding that I1 is NOT ACHIEVABLE (R6). This task executes NO untrusted binary and acquires nothing. THIS TASK RUNS FIRST, AND THE PRECEDENCE IS ENFORCED BY A VERDICT, NOT BY COMPLETION ALONE. It BLOCKS 177.005-T, which pulls a real external release asset onto a runner, and BLOCKS 177.002-T, the adversarial containment probe; 177.001-T inherits the same precedence transitively through 177.005-T. Neither acquisition nor probe work may execute in a job environment whose credential absence is still unverified, because a probe or acquisition job carrying credentials would be a privileged process handling an untrusted artifact even without executing it (H7, H9, H12, R8, R10). Attempt 02 finding F1 (P1) found the previous revision claiming this ordering in the plan's blast radius and H7 while the task table, item_deps and three task records - including this one - asserted independence; the ordering is now a blocks edge in item_deps and no record calls these tasks independent. ATTEMPT 03 FINDING K1 (P1) THEN FOUND THE BLOCKS EDGE INSUFFICIENT AS A SAFETY PREDICATE: a blocks edge clears on PREDECESSOR COMPLETION, and this task completes on THREE outcomes - DETERMINED/ACHIEVABLE, DETERMINED/NOT ACHIEVABLE (R6), and NOT DETERMINED - FLOOR INVOKED at the 90 min bound (R9) - two of which leave credential absence unverified or affirmatively falsified while still clearing the edge. THE GATE IS THEREFORE A VERDICT PREDICATE, AND THIS TASK EMITS IT. AS ITS LAST ACTION, THIS TASK WRITES EXACTLY ONE LINE to docs/spikes/2026-09-18-conformance-isolation-i1-gate.md, creating the file; it is the SOLE WRITER of that artifact. The line is exactly one of: "I1_GATE: ACHIEVABLE | verdict=DETERMINED | mechanism=<mechanism-name> | checked=<date>"; "I1_GATE: NOT_ACHIEVABLE | verdict=DETERMINED | reason=<short-reason> | checked=<date>"; "I1_GATE: FLOOR_INVOKED | verdict=NOT DETERMINED - FLOOR INVOKED | blocked_by=<short-reason> | checked=<date>". THE GATE LINE IS NON-SECRET BY CONSTRUCTION: it carries a token, a verdict, a SHORT NON-SECRET mechanism or reason identifier, and a date. No environment-variable value, token, key fragment or credential content appears in it, in the findings artifact, or in any job log (R6). A reader learns WHETHER credential absence was established, never WHAT any credential is. 177.005-T and 177.002-T read this line as their FIRST action and fail closed on anything other than ACHIEVABLE. THIS TASK ALSO OWNS PROBE WORKFLOW CREATION. GitHub executes only workflows that exist in the repository on a branch, so the hosted-runner probe workflow MUST BE COMMITTED; calling it uncommitted would be false (attempt 03 finding K2, P2). CREATE EXACTLY ONE workflow at the exact path .github/workflows/spike-177-isolation-probe.yml, committed to chore/stage-176-s-workflow-defects IN ITS OWN COMMIT. Add no other workflow file and modify no existing workflow. It MUST declare an explicit minimal permissions: block, be passed NO secrets, and trigger ONLY on workflow_dispatch - never push, pull_request or schedule - so it cannot fire on unrelated branch activity. All four determining tasks reuse THIS SINGLE workflow, selecting their probe by dispatch input (i1-credentials, i7-acquisition, i2-i3-egress, i4-i6-containment). Those declared constraints are WHAT THIS TASK INSPECTS to determine I1; they are not an assumption that I1 holds. REMOVAL IS OWNED BY 177.003-T at spike close, in the same commit that lands the findings artifact; rollback is git revert of this task's single creation commit, or deletion of the single added path. RECORD the creation commit SHA and the added path as evidence. It also blocks 177.003-T. ELAPSED BOUND 90 min; on reaching it, record I1 as NOT DETERMINED - FLOOR INVOKED, naming what was attempted, what blocked it, and invoking the evidence-and-documentation floor in writing for I1's scope; a bare "not determined" without all three is ABSENT. ON THAT PATH THE GATE LINE IS I1_GATE: FLOOR_INVOKED, WHICH IS A CLOSED GATE: 177.005-T and 177.002-T must then NOT run their acquisition or probe at all. SECURITY MODEL IS A CONSTRAINT, NOT A QUESTION: if I1 is NOT ACHIEVABLE, the gate line is I1_GATE: NOT_ACHIEVABLE, the gate is likewise CLOSED, and 181-S degrades to evidence-and-documentation only; the requirement is never relaxed and the spike never proceeds into the untrusted work to see whether it might be safe anyway. Source 7F9CB5E9. Plan docs/plans/2026-09-18-conformance-isolation-spike-plan.md (plan_id conformance-isolation-spike, revision 4, awaiting independent attempt 04). Verdict manifest docs/reviews/2026-09-18-conformance-isolation-spike-plan-review.md.