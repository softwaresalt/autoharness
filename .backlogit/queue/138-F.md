---
artifact_type: feature
created_at: 2026-08-21T03:11:08.5275077Z
custom_fields:
    harness_status: pending
    source_deliberation_id: 021-DL
    source_stash_id: 7852CE0D
id: 138-F
labels:
    - bug
    - tests
    - p-021
    - baseline-red
    - prerequisite
priority: high
status: queued
title: 'Restore green pytest baseline: P-021 contract tests load the archived 019-DL deliberation artifact'
updated_at: 2026-08-21T03:12:13.333584Z
---

Repair the two P-021 scope-containment contract-test modules that hard-load the 019-DL deliberation artifact from `.backlogit/queue/019-DL.md`, a path that no longer exists because the artifact was archived (merge f72109e2, PR #374) to `.backlogit/archive/019-DL.md`. Because the read happens in `setUpClass`, the FileNotFoundError collapses both modules' entire test classes, leaving the configured pytest suite RED at baseline before shipment 144-S starts.

Ship evaluates the full configured suite at each task completion gate, so no 144-S or 145-S task can go green while this stands. This is an execution-blocking prerequisite to both.

P-021 C1: FAILS same-contract-surface against both existing shipments. 144-S/136-F owns the `backlogit docs lint` + plan-frontmatter surface; 145-S/137-F owns the agent-template paired-edit + stash-archive naming surface. This defect lives on the P-021 contract-test harness surface (deliberation-artifact path resolution), owned by neither. Captured as stash 7852CE0D, deliberated as 021-DL, and harvested here into its own feature and its own prerequisite shipment rather than absorbed.

Chosen direction (021-DL Option B): host a lifecycle-stable resolver in `tests/test_scope_containment_policy_contract.py` (the module both siblings already import from, preserving the one-way 011->013->012 allocation edge), delegating root resolution to the already-shipped `autoharness.backlog_root.resolve_backlog_root` with `env={}` isolation, probing `queue/<id>.md` then `archive/<id>.md`. Add a behavioural regression assertion (resolves, resolves uniquely, retains C4/C5 AMENDMENT markers) and a structural guard (no lifecycle-volatile queue-anchored path outside the resolver).

Explicitly NOT in scope: any generic backlog-path abstraction in `src/`; any change to `resolve_backlog_root`; any change to a B1-B18 assertion's subject or strength; any file outside `tests/`.

Artifacts:
* Stash: 7852CE0D
* Deliberation: `.backlogit/queue/021-DL.md` (021-DL)
* Plan: `docs/plans/2026-08-20-p021-contract-test-deliberation-path-resolution-plan.md` (requires_plan_hardening: no)
* Review: `docs/reviews/2026-08-20-p021-contract-test-deliberation-path-resolution-review.md` (PASS, 0 unresolved P0/P1)

Ordering: this feature's shipment is an execution-blocking prerequisite. Deterministic order PREREQ -> 144-S -> 145-S.