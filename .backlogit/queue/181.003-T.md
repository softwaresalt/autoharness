---
artifact_type: task
created_at: 2026-09-19 02:53:45.014457+00:00
custom_fields:
  complexity: medium
  size: S
  size_ruleset_version: 2h-rule-v1
  size_source: agent
dependencies:
- 181.007-T
id: 181.003-T
labels:
- harness-surface:harness-architect
- ship-lifecycle
- fixtures
- structural
parent_id: 181-F
priority: medium
status: queued
title: 'P4 T5 (PREPARE): canonical agent-text fixtures and activation structural tests'
updated_at: 2026-09-21 03:01:30.383694+00:00
---

P4 T5 (PREPARE, inert). Author the canonical agent-text fixtures and the activation structural tests - TWO FILES: tests/fixtures/ship_harness_generation.md and tests/test_ship_harness_activation.py. Size S, complexity medium. Blocked by 181.007-T.

ORDERING IS DELIBERATE. This task runs AFTER the executable boundary exists, not before. Fixtures quote a CLI invocation, and a fixture may not quote a command whose flags, exit codes and JSON shape have not yet been fixed by 181.007-T. Revision 10's ordering, in which 181.001-T's tests depended on fixtures authored later, is corrected: 181.001-T creates its own fixtures programmatically and this task supplies ONLY agent text.

FIXTURES ARE CANONICAL. The fixture file is the SINGLE SOURCE for the Ship-facing text that 181.005-T installs. The structural tests assert against the fixture, and 181.005-T inserts the fixture content; the same bytes are therefore asserted and installed.

FIXTURE CONTENT. (a) The literal runnable invocation `autoharness harness resolve --workspace . --shipment {shipment_id} --json` with no <resolved-or-default> placeholder and no literal angle-bracket shell token. (b) Explicit handling of all three exit codes: 0 proceed to partition; 1 HALT before partition and report the missing or stale surfaces; 2 HALT before partition and report the reason code - 1 and 2 are reported DISTINCTLY. (c) The Ship-only crash-resume rule, stated in exactly the five ordered steps: load and validate the checkpoint payload; require shipment_id in context, halting when absent; RECOMPUTE the resolver from the current workspace; halt on exit 1 or 2; ONLY THEN restore the task cursor, phase and next-action intent. (d) An explicit statement that NO STORED TOKEN, CACHED STATE, PRIOR VERDICT OR RECORDED DIGEST EVER AUTHORIZES EXECUTION - a stored digest may be compared for drift reporting but never substituted for recomputation.

STRUCTURAL TESTS ASSERT EXACT SHAPE against the real files: the template's `### Step 2: Harness Generation (P-002 / P-004)` occurs EXACTLY ONCE; the mirror's `### Step 1.5: Harness Generation (P-002 / P-004)` occurs EXACTLY ONCE and appears strictly BETWEEN `### Step 1: Pre-Flight Checks` and `### Step 2: Task Execution Loop`; `### Step 2: Task Execution Loop` still occurs exactly once and is NOT renumbered; `### Crash-Resumption / Startup Recovery Protocol (fail-closed, owner-exclusive)` occurs exactly once in EACH file; the five restore steps appear in order in both; all three exit codes are handled in both; the runnable invocation is present and the literal `<resolved-or-default>` is ABSENT from both; the no-stored-token sentence is present in both; and the manifest contains exactly one entry whose path is .github/agents/_ship.agent.md.

THESE TESTS ARE RED WHEN WRITTEN AND STAY RED THROUGH 181.004-T. They go green only when 181.005-T performs the activation.

EXACT PATHS. The Ship surfaces this unit activates are templates/agents/_ship.agent.md.tmpl and .github/agents/_ship.agent.md, with .autoharness/harness-manifest.yaml refreshed at its single entry whose path is .github/agents/_ship.agent.md. templates/agents/ship.md.tmpl and .github/agents/ship.md DO NOT EXIST and are named nowhere in this unit.

REVIEW STATE IS POINTER-ONLY. The verdict manifest docs/reviews/2026-09-18-ship-harness-lifecycle-foundation-plan-review.md is the SOLE AUTHORITY for this unit's review state. This record asserts NO verdict, NO attempt number, NO publication eligibility and NO finding closure. Governing plan docs/plans/2026-09-18-ship-harness-lifecycle-foundation-plan.md (plan_id ship-harness-lifecycle-foundation, plan_role active, revision 11). Governing decision docs/decisions/2026-09-18-shared-execution-architecture-and-portfolio-reslicing-decision.md (revision 8). Source stash 76EBDE6D.
