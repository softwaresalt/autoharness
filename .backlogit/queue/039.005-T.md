---
artifact_type: task
created_at: 2026-05-17T17:49:14.1407417-07:00
id: 039.005-T
labels:
    - release
    - tag
    - ci-cd
    - pypi
parent_id: 039-F
priority: medium
status: queued
title: Push v1.4.3 annotated tag and confirm release workflow success
updated_at: 2026-05-17T17:49:14.1407417-07:00
---

Push the annotated `v1.4.3` tag to trigger the release workflow. Gate: the PR from 039.004-T must be fully merged to `main` before this task begins.

## Pre-condition (P-001 gate)

**Do NOT push this tag until**:
- 039.004-T is complete (PR merged, `main` updated)
- `git log --oneline -1` on main shows the merge commit from 039.004-T at HEAD

This is enforced by P-001 as strengthened in PR #86: full post-merge release closure (PR merged, branch clean) must precede tag push.

## Steps

### 1. Sync local main
```
git checkout main
git pull origin main
```

### 2. Confirm version at HEAD
```
python -c "import tomllib; f=open('pyproject.toml','rb'); print(tomllib.load(f)['project']['version'])"
# → 1.4.3
```

### 3. Push annotated tag
```
git tag -a v1.4.3 -m "Release v1.4.3"
git push origin v1.4.3
```

### 4. Monitor release workflow
Navigate to `https://github.com/softwaresalt/autoharness/actions` and confirm the release workflow (triggered by the `v1.4.3` tag) completes with all green steps:
- Validate tag format ✓
- Extract changelog section ✓
- Build wheel + sdist ✓
- Twine check ✓
- Pre-publish check ✓
- PyPI publish (OIDC) ✓
- Smoke test (`uv tool run autoharness version` returns `1.4.3`) ✓
- GitHub Release create/update ✓

### 5. Confirm GitHub Release published
```
gh release view v1.4.3
```
Expect: release exists, notes present, tag `v1.4.3`.

## No Trusted Publisher reconfiguration needed

The Trusted Publisher was configured during 033-S for `release.yml`. No changes to the workflow filename or environment mean it continues to work as-is.

## Width constraint

Touch only git tag operations. No code, doc, or backlog changes in this task.