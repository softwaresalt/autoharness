---
artifact_type: task
created_at: 2026-08-05T00:15:02.457583Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-two-axis-v1
    size_source: agent
id: 109.019-T
labels:
    - topology
    - orchestrator
    - ship-agent
    - sequencing
    - backlogit-1.8.0
    - p-001
parent_id: 109-F
priority: medium
status: queued
title: 'B8: correct Orchestrator/Ship serial-sequencing to queued-from-start (remove unsupported blocked->queued)'
updated_at: 2026-08-05T00:15:02.457583Z
---



<!-- BEGIN:acceptance-criteria -->
* Orchestrator and Ship agent templates NO LONGER instruct or attempt any `blocked -> queued` shipment transition. backlogit 1.8.0 isValidShipmentTransition supports ONLY queued->active, active->shipped, active->abandoned (blocked is not a valid shipment status and a blocked shipment can never transition — dead end).
* The serial-sequencing contract is corrected to the supported model: dependency-gated successor shipments remain `status: queued` from creation and are suppressed by the blocks hard-eligibility gate; a successor becomes eligible automatically when its blocking predecessor reaches `shipped` (the blocks edge clears). No status mutation is used to un-gate.
* Cursor-advance prose (dark-run) is corrected: advancing sets the completed/next cursor and relies on predecessor-ship to un-gate the successor; it does NOT perform `blocked -> queued`.
* Ordered-scope derivation prose is corrected: dependency-gated successors are discovered via blocks-edge traversal over `queued` shipments (not by a `blocked` status filter).
* Structural assertions confirm no `blocked -> queued` shipment-transition instruction remains in either template; a note cites backlogit 1.8.0 transition rules as the authority.
* Change confined to templates/agents/_orchestrator.agent.md.tmpl, templates/agents/_ship.agent.md.tmpl, and their installed copies (and any backlogit Shipment Sequencing Protocol instruction the templates cite); no backlogit source mutation.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
[Shipment B / contract correction] The installed Orchestrator (Step 2 ordered-scope derivation + dark-run cursor-advance) and the shipment-status compound guidance encode an IMPOSSIBLE handoff: they assume dependency-gated successor shipments sit at `status: blocked` and are later transitioned `blocked -> queued`. backlogit 1.8.0 does not support that transition (verified: internal/core/shipment.go isValidShipmentTransition allows only queued->active, active->shipped, active->abandoned; a `blocked` shipment is a dead end). Correct the templates to the supported dependency-suppression model: successors are `queued` from the start and gated purely by blocks edges. This is the durable fix behind the cycle-2 data repair that set 115-S/116-S to queued.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Authoritative evidence: C:/Source/GitHub/backlogit/internal/core/shipment.go lines 336-345 (isValidShipmentTransition) and 107-108 (MoveShipmentStatus doc). Update docs/compound/2026-05-07-backlogit-shipment-status-constraints.md is a separate learnings correction (handled in Stage memory this cycle); this task fixes the AGENT TEMPLATES + any cited backlogit Shipment Sequencing Protocol instruction. Do not mutate backlogit source.
<!-- END:implementation-notes -->