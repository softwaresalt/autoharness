---
artifact_type: task
created_at: 2026-08-05T00:15:02.457583Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-two-axis-v1
    size_source: agent
id: 109.019-T
labels:
    - topology
    - orchestrator
    - ship-agent
    - sequencing
    - backlogit-1.8.0
    - p-001
parent_id: 109-F
priority: medium
status: queued
title: 'B8: correct Orchestrator/Ship serial-sequencing to queued-from-start (remove unsupported blocked->queued)'
updated_at: 2026-08-05T03:11:00Z
---



<!-- BEGIN:acceptance-criteria -->
* Orchestrator and Ship agent templates NO LONGER instruct or attempt any `blocked -> queued` shipment transition. backlogit 1.8.0 isValidShipmentTransition supports ONLY queued->active, active->shipped, active->abandoned (blocked is not a valid shipment status and a blocked shipment can never transition — dead end).
* The serial-sequencing contract is corrected to the supported model: dependency-gated successor shipments remain `status: queued` from creation and are suppressed by the blocks hard-eligibility gate; a successor becomes eligible automatically when its blocking predecessor reaches `shipped` (the blocks edge clears). No status mutation is used to un-gate.
* Cursor-advance prose (dark-run) is corrected: advancing sets the completed/next cursor and relies on predecessor-ship to un-gate the successor; it does NOT perform `blocked -> queued`.
* Ordered-scope derivation prose is corrected: dependency-gated successors are discovered via blocks-edge traversal over `queued` shipments (not by a `blocked` status filter).
* MANDATORY PRE-SELF-CLOSE CONTEXT RELOAD (114-S's own closure): because 114-S is the shipment that MERGES the corrected non-cascading safe-close/terminal-marker contract (A7 = 109.016-T canonical skill + 109.020-T Ship agent pointer) into `main`, Ship MUST, after 114-S's PR merges to `main` and BEFORE it performs 114-S's own safe-close, RELOAD the freshly merged `main` Ship instructions + shipment-reconcile skill. This guarantees 114-S closes under the newly merged move-to-`shipped` -> verify -> archive -> verify sequence, NOT the stale in-context copy that archives the shipment record while it is still `active` (which would stamp `archived_status: active` and cause the newly installed 115-S protected-set check to keep 114-S's tasks protected and halt). This reload is REQUIRED in addition to the post-closure reload below.
* MANDATORY POST-PREDECESSOR-CLOSURE CONTEXT RELOAD: the corrected cursor-advance contract REQUIRES the Orchestrator, after a predecessor shipment is merged and its P-020 post-merge closure completes, to RELOAD the current `main` agent instructions (re-read the freshly merged _orchestrator/_ship templates) BEFORE advancing the cursor or selecting the successor shipment. This guarantees the corrected sequencing prose installed by THIS task is the version in effect for the 114-S -> 115-S transition, not a stale in-context copy loaded before 114-S merged.
* SHIPS IN SHIPMENT A (114-S): this correction is installed by 114-S so the corrected 114 -> 115 transition contract exists BEFORE the Orchestrator advances past 114-S. The fix must NOT depend on 115-S/116-S to take effect.
* Structural assertions confirm no `blocked -> queued` shipment-transition instruction remains in either template, that the pre-self-close context-reload step precedes the shipment's own safe-close, and that the post-closure context-reload step precedes cursor-advance/successor-selection; a note cites backlogit 1.8.0 transition rules as the authority.
* Change confined to templates/agents/_orchestrator.agent.md.tmpl, templates/agents/_ship.agent.md.tmpl, and their installed copies (and any backlogit Shipment Sequencing Protocol instruction the templates cite); no backlogit source mutation.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
[Shipment A / contract correction] The installed Orchestrator (Step 2 ordered-scope derivation + dark-run cursor-advance) and the shipment-status compound guidance encode an IMPOSSIBLE handoff: they assume dependency-gated successor shipments sit at `status: blocked` and are later transitioned `blocked -> queued`. backlogit 1.8.0 does not support that transition (verified: internal/core/shipment.go isValidShipmentTransition allows only queued->active, active->shipped, active->abandoned; a `blocked` shipment is a dead end). Correct the templates to the supported dependency-suppression model: successors are `queued` from the start and gated purely by blocks edges. This is the durable fix behind the cycle-2 data repair that set 115-S/116-S to queued. MOVED TO 114-S (cycle 3): the currently-installed Orchestrator attempts the invalid `blocked -> queued` cursor-advance immediately AFTER 114-S closes (when it tries to advance to 115-S) — before 115-S could ever run the correction. The fix must therefore be INSTALLED by 114-S, before the 114 -> 115 transition, and is paired with a mandatory post-114-closure Orchestrator context reload so the corrected contract is the one actually in effect at cursor-advance.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Authoritative evidence: C:/Source/GitHub/backlogit/internal/core/shipment.go lines 336-345 (isValidShipmentTransition) and 107-108 (MoveShipmentStatus doc). Update docs/compound/2026-05-07-backlogit-shipment-status-constraints.md is a separate learnings correction (handled in Stage memory this cycle); this task fixes the AGENT TEMPLATES + any cited backlogit Shipment Sequencing Protocol instruction. Do not mutate backlogit source. CYCLE-3 PLACEMENT: this task now ships in Shipment A (114-S), not B (115-S), so the corrected 114->115 transition contract + the post-closure context-reload step are installed before the Orchestrator advances past 114-S.
<!-- END:implementation-notes -->