---
artifact_type: task
created_at: 2026-09-26T08:17:02.566534Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: v1
    size_source: agent
dependencies:
    - 191.001-T
id: 191.002-T
labels:
    - harness-surface:none
    - ship-lifecycle
    - unit-d
parent_id: 191-F
priority: high
references:
    - docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md
    - docs/reviews/2026-09-25-ship-lifecycle-release-units-plan-review.md
status: queued
title: 'Ship lifecycle D2: Ship structural tests for the post-activation text (gap characterization)'
updated_at: 2026-09-26T08:19:47.3846397Z
---



<!-- BEGIN:acceptance-criteria -->
- Tests over both Ship surfaces (templates/agents/_ship.agent.md.tmpl and .github/agents/_ship.agent.md) for the post-activation text: the per-task pre-claim step (T1 resolve under D1's capture contract, including its capture path and cleanup, with exits 0/1/2 kept distinct and consumed only through D1's printed line; on resolver-not-observed or any D1 exit other than 0, the task halts with no T2, T3 or Claim; T2 harness-architect for the current task when it declares the surface; T3 the current task's valid RED record before Claim; a harness-surface:none task skips T2 and T3; T1 to T3 do not depend on a member being queued, because a backlogit 1.10 claim moves members to active).
- No Ship passage states an order that contradicts T1 -> T2 -> T3 -> Claim or FI-13; the template's run-once Step 2 and the harness-ready prefilter of Step 3 are removed; the template's session-start memory restore and '### Resumption Protocol' are replaced to meet restore contract rules 1 to 4; the recovery machine is still invoked at session start (E L6) and the C1-C6 wiring is intact (E O4); template and mirror are semantically equal at their own anchors (tests/fixtures/ship_activation/expected_anchors.json).
- Verification: the tests fail against the pre-activation surfaces, and each failure names the missing anchor. This is a recorded gap characterization, not P-004 RED evidence (R2 refuses assertion failures); the record is the input to D3 preflight (5).
- Manifest harvest carry CONST-II-F01 (E2): applied; D2's tests are structural (they reach no stub), so record their pre-implementation failing run against the pre-activation surfaces, and the audit test's run, as gap characterization (Principle II evidence), not as RED.
- Manifest harvest carry IM-14-F51 (E4, D2 part): D2's frozen 'The test files are added to the audit inventory' cell is read as: FLOOR grows only by B1 (harness_surfaces.py) and D1 (harness_verdict.py); D2's paths (tests/test_ship_harness_activation.py, tests/fixtures/ship_activation/expected_anchors.json and the audit test itself) are residue units under the Residue rule, not FLOOR entries; any LEDGER entry D2 adds is a non-claim confirmed by local review.
- harness-surface:none: no roster and no RED record (A1 restricts the label to budgets without src/ Python; D2's budget has none).
- Environment prechecks: git worktree list shows one worktree; git status --porcelain is empty for the task's files before the first write; the canonical command runs from the repository root. Over the estimate by more than 30 minutes -> stop and return to Stage for a split.
- IM-14: no added text claims race, TOCTOU or hardlink-alias resistance.
- IM-15: a test or proof PASS confers no claim authority; ordinary P-001/P-002/P-004/P-020 gates at claim and closure.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan task D2 (docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md, blob b7a77c76, section 'D2: Ship structural tests'; release unit S(D); review authority: manifest E5 Verdict under OP-5).

Files (budget): tests/test_ship_harness_activation.py; tests/fixtures/ship_activation/expected_anchors.json; tests/test_harness_noclaim_audit.py.

Change: structural tests over both Ship surfaces asserting the post-activation text (the per-task pre-claim step T1 -> T2 -> T3 -> Claim, the removals, the replaced restore passages, the intact recovery invocation and C1-C6 wiring, template and mirror semantic equality); the test files are added to the audit inventory (as residue units).

Trace: IM-07 (Ship refusal), IM-08, FI-13, IM-14, IM-15. Estimate 100 min; size S; complexity medium.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Label harness-surface:none (tests and fixtures only, no src/ Python). Blocked by D1 (the tests assert D1's capture contract and printed line); blocks D3. D2's tests stay red until D3 activates the text; the 196-S -> S(D) shipment edge keeps them from sitting in flight while IM-10 waits (IM-10-F01). Supersedes retired 181.003-T (plan Trace From the Retired Tasks). D2's test commit is reverted in a D3 rollback (after D4's commit and the activation commit), because it asserts the activated text.
<!-- END:implementation-notes -->