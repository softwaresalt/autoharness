---
artifact_type: task
created_at: 2026-07-30T20:20:14.5277866Z
id: 101.001-T
labels:
    - templates
    - orchestrator
    - dark-mode
parent_id: 101-F
priority: medium
references:
    - docs/plans/2026-07-30-multi-shipment-dark-sequencing-plan.md
    - templates/agents/_orchestrator.agent.md.tmpl
    - .github/agents/_orchestrator.agent.md
    - .autoharness/harness-manifest.yaml
status: queued
title: 'U1: Queue-ordering-aware Orchestrator shipment selection'
updated_at: 2026-07-30T20:21:15.4688252Z
---

U1 — Make the Orchestrator shipment-selection rule queue-ordering-aware. In templates/agents/_orchestrator.agent.md.tmpl, Step 2 'Route to Ship', replace rule #1 ('Select the highest-priority queued shipment.', ~line 257) with: select the next shipment via `queue view --type shipment --status queued` — which returns only READY shipments in execution order (custom_fields.queue_position first, then priority) and ALREADY EXCLUDES any shipment whose item_deps blocks-chain has an unshipped predecessor (backlogit's queue query performs the eligibility filtering; the Orchestrator does NOT re-implement suppression); claim the HEAD of that ready list so an item_deps blocks-chain becomes a self-enforcing sequence. State explicitly that `queue view` selects the next ELIGIBLE shipment only and cannot reconstruct the full ordered sequence (that needs a raw non-ready listing + item_deps traversal; see U2 / 101.002-T). Add reciprocal cross-references to the P-017 ordered scope (U2 / 101.002-T) and the backlogit playbook (U3 / 101.003-T). PRESERVE Step 2 rule #2 (P-001/P-016/P-020 closure guard) verbatim; touch only rule #1. Introduce NO new {{VARIABLE}} (reuse {{STATUS_QUEUED}}). ALSO update the source-controlled dogfood mirror `.github/agents/_orchestrator.agent.md` (the same rule #1 lives at line 196) and REGENERATE its checksum in `.autoharness/harness-manifest.yaml` (the .github/agents/_orchestrator.agent.md entry, ~lines 117-121) — editing only the template leaves this repo's active Orchestrator on the old behavior. NOTE: these edits are Ship execution work, not Stage.