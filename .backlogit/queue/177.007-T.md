---
artifact_type: task
created_at: 2026-09-20T05:30:30.4344909Z
custom_fields:
    complexity: low
    size: XS
    size_ruleset_version: v1
    size_source: agent
dependencies:
    - 177.003-T
id: 177.007-T
labels:
    - spike
    - cleanup
    - evidence
    - composed-state
parent_id: 177-F
priority: medium
status: queued
title: 'S2 T7: observe the committed removal and emit the CLEANUP_ evidence block'
updated_at: 2026-09-20T05:30:30.4344909Z
---

CLEANUP EVIDENCE EMISSION. THIS TASK DETERMINES NO PROPERTY AND REMOVES NOTHING. It exists because attempt 05 finding L1 (P2) established that the cleanup evidence block cannot be written in the commit that removes the probe workflow: two of its fields name values that only exist once that commit has been created, a commit's content cannot contain its own SHA, and a working tree cannot be observed clean from inside the staged change that makes it clean. THE LIFECYCLE IS THEREFORE TWO STEPS. 177.003-T removes .github/workflows/spike-177-isolation-probe.yml and lands the findings artifact in ONE COMMIT - THE REMOVAL COMMIT - and writes no CLEANUP_ line. THIS TASK RUNS AFTER THAT COMMIT ALREADY EXISTS, observes it, and writes the CLEANUP_ evidence block into docs/spikes/2026-09-18-conformance-isolation-findings.md in ITS OWN SEPARATE COMMIT - THE EVIDENCE COMMIT. Every value the block carries is ALREADY OBSERVABLE BEFORE IT IS RECORDED, and no field of the block ever refers to the commit that records it. THIS TASK IS THE SOLE WRITER of the cleanup evidence block; 177.003-T does not write it, 177.006-T is its SOLE EVALUATOR, and 181-S reads neither the block nor its checks, only the composed-state token. DERIVE AND OBSERVE FIRST, THEN WRITE, IN THIS ORDER. THE REMOVAL PROOF IS DURABLE, NOT POSITIONAL: THIS TASK NEVER REQUIRES THE REMOVAL COMMIT TO BE THE CURRENT TIP, and later unrelated forward commits - including the bookkeeping commits this repository's own tracked .backlogit records produce - NEVER invalidate it. (1) DERIVE the removal commit from committed history rather than assuming it is the tip, and verify it, by RC1-RC7. RC1: run "git rev-list --full-history --max-count=1 HEAD -- .github/workflows/spike-177-isolation-probe.yml" and require EXACTLY ONE line carrying ONE 40-character lowercase hex SHA; empty output, more than one line or any other shape FAILS CLOSED. --full-history IS MANDATORY - default history simplification PRUNES the deletion commit once the path is absent at HEAD and the command returns empty, which would otherwise read as "no removal commit exists". RC2: "git diff-tree --no-commit-id --name-status -r <sha> -- .github/workflows/spike-177-isolation-probe.yml" must output EXACTLY ONE line, D then a tab then that exact path; any other status letter, more than one line, or empty output FAILS CLOSED. RC3: "git diff-tree --no-commit-id --name-status -r <sha> -- .github/workflows/" must output EXACTLY the one line RC2 produced - the removal commit touches NO OTHER WORKFLOW FILE. RC4: "git ls-tree --full-tree -r --name-only <sha> -- .github/workflows/spike-177-isolation-probe.yml" must be EMPTY - the path is absent from the removal commit's own tree. RC5: "git merge-base --is-ancestor <sha> HEAD" must exit 0 - the removal commit is REACHABLE FROM THE CURRENT HEAD. Ancestor-or-equal, so a removal commit that happens still to be the tip also passes; being the tip is ACCEPTED, never REQUIRED. RC6: "git merge-base --is-ancestor <created-sha> <sha>" must exit 0 AND <created-sha> must not equal <sha> - 177.004-T's creation commit is IN THE REMOVAL COMMIT'S ANCESTRY. RC7 SELF-REFERENCE GUARD: <sha> is read from committed history BEFORE this task stages anything, so it can never be the evidence commit this task subsequently creates; NO FIELD OF THE BLOCK MAY NAME A COMMIT THAT DOES NOT ALREADY EXIST AT DERIVATION TIME. (2) OBSERVE THE CURRENT TIP AND CONFIRM THE EXACT PATH IS NOT TRACKED THERE. Record the current tip's 40-character lowercase hex SHA as observed_tip. Run PP1: "git ls-tree --full-tree -r --name-only HEAD -- .github/workflows/spike-177-isolation-probe.yml". EMPTY OUTPUT means the path is NOT TRACKED at the current tip and the check passes; exactly one line byte-identical to that path means it IS TRACKED and FAILS CLOSED; a non-zero exit, more than one line, or a line that is not byte-identical is AMBIGUOUS and FAILS CLOSED. observed_tip is the removal commit OR A DESCENDANT OF IT - both are correct, and no intervening commit forbids this write. (3) CONFIRM NO EXACT-PATH RESIDUE, BEFORE THIS TASK STAGES ANY EDIT OF ITS OWN. THE CHECK IS SCOPED TO THAT ONE PATH AND NOTHING ELSE. Run PP3: "git ls-files --cached --error-unmatch -- .github/workflows/spike-177-isolation-probe.yml" must exit NON-ZERO (the path is not in the index); exit 0 means INDEX RESIDUE and FAILS CLOSED. Run PP2: "git status --porcelain=v1 --untracked-files=all -- .github/workflows/spike-177-isolation-probe.yml" must be EMPTY (no staged, unstaged or untracked copy at that exact path); one or more lines whose path field is byte-identical to that path means RESIDUE and FAILS CLOSED; a non-zero exit, any reported path that is not byte-identical, or any directory form ending in / is AMBIGUOUS and FAILS CLOSED. --untracked-files=all IS MANDATORY so an untracked copy inside an otherwise-untracked directory is reported as the file rather than collapsed into a directory entry. THIS TASK NEVER OBSERVES, REQUIRES OR RECORDS WHOLE-TREE CLEANLINESS. Unrelated tracked files, untracked files, agent checkpoints, session memory notes and operator work are OUTSIDE every limb of this check and MUST NOT prevent this write (attempt 06 finding M2, P2). (4) Read 177.004-T's creation commit SHA and the four dispatch run IDs and URLs from the records already produced by the determining tasks. ONLY WHEN ALL FOUR STEPS HAVE BEEN COMPLETED, compose and write the block. THE BLOCK IS EXACTLY EIGHT LINES, IN THIS ORDER, each beginning at column zero with the literal prefix shown. "CLEANUP_WORKFLOW_PATH: .github/workflows/spike-177-isolation-probe.yml". "CLEANUP_CREATED_COMMIT: <40-lowercase-hex>" - 177.004-T's creation commit, the commit that ADDS that path. Four lines "CLEANUP_DISPATCH: <input> | run_id=<digits> | url=<run-url>" with <input> being exactly i1-credentials, i7-acquisition, i2-i3-egress and i4-i6-containment, one each, in that order. "CLEANUP_REMOVED_COMMIT: <40-lowercase-hex>" - 177.003-T's removal commit, the commit that DELETES that path, and NEVER this task's own evidence commit. "CLEANUP_TIP_OBSERVATION: WORKFLOW_ABSENT | observed_tip=<40-lowercase-hex> | path_clean_at_observation=yes | observed_at=<date>". WHAT observed_tip MEANS, STATED SO IT CANNOT BE READ AS SELF-REFERENCE: observed_tip is the branch tip THIS TASK OBSERVED at steps (2)-(3). It is the removal commit OR A DESCENDANT OF IT - both are correct and neither is preferred - and it is the PARENT of the evidence commit this task then creates. IT IS NEVER THE EVIDENCE COMMIT, and no field of the block names the evidence commit. path_clean_at_observation=yes records that AT THAT OBSERVATION, before this task staged its own edit, the EXACT PATH .github/workflows/spike-177-isolation-probe.yml was absent from observed_tip's tree and carried no staged, unstaged or untracked residue. IT MAKES NO CLAIM WHATSOEVER ABOUT ANY OTHER PATH and it makes and needs no claim about the tree after the block is written. THE EVALUATOR'S INDEPENDENT RE-OBSERVATION OF THE CURRENT STATE (177.006-T check C6) IS THE FINAL BRANCH-STATE AUTHORITY; this line is a recorded observation and is never that authority. ATOMIC, ALL-OR-NOTHING WRITE. Write the eight lines as ONE contiguous block in ONE commit. IF ANY REQUIRED VALUE CANNOT BE OBSERVED, WRITE NO BLOCK AT ALL - no partial block, no placeholder line, and NEVER GUESS, INVENT OR PRE-COMPUTE A SHA. A wholly absent block resolves to CLEANUP_FAILED | cleanup=EVIDENCE_MISSING and the unit composes to ISOLATION_CLEANUP_FAILED, which is fail-closed, non-pass and harvest-blocking, and is the correct outcome. A fabricated SHA would resolve EVIDENCE_INCONSISTENT and is forbidden outright. THE ARTIFACT CARRIES EXACTLY ONE CLEANUP_ BLOCK AT ALL TIMES: a re-emission REPLACES the whole block in place rather than appending beside it, and more than one CLEANUP_WORKFLOW_PATH:, CLEANUP_CREATED_COMMIT:, CLEANUP_REMOVED_COMMIT: or CLEANUP_TIP_OBSERVATION: line is EVIDENCE_MALFORMED. RE-EMISSION IS ALWAYS AVAILABLE AND NEEDS NO SECOND REMOVAL COMMIT (attempt 06 finding M1, P2). This task may re-run at any later point on the branch, after ANY NUMBER of unrelated forward commits: RC1-RC7 re-derive the SAME removal commit, because its diff and its ancestry are HISTORICAL FACTS and RC5 asks only for reachability, which later commits preserve. On a re-emission observed_tip names the tip actually observed at that later moment, which is a DESCENDANT of the removal commit and the parent of the new evidence commit, and C5 already accepts exactly that. NO SECOND DELETION OR REMOVAL COMMIT IS EVER REQUIRED - which is what makes the in-unit ISOLATION_CLEANUP_FAILED remedy executable rather than advertised. What re-emission still cannot do is manufacture a missing value: the atomic all-or-nothing rule above is unchanged. THE BLOCK CARRIES PATHS, COMMIT IDENTITIES, RUN IDENTIFIERS, URLS AND A DATE ONLY - no environment-variable value, token, key fragment or credential content appears in it, under the same binding evidence rule that governs the I1 inventory and the I1 gate line (R6). THIS TASK IS LOCAL, OFFLINE AND CREDENTIAL-FREE: it reads the local repository and already-recorded run identifiers, performs no network request, dispatches nothing, acquires nothing and probes nothing, so it neither weakens nor interacts with the I1 verdict gate or the acquisition-then-no-network model. IT CARRIES NO I1 GATE READ for that reason and is unaffected by whether the gate was OPEN or CLOSED; on a CLOSED gate 177.003-T still removes the workflow and this task still emits the block, so cleanup can resolve CLEANUP_PROVEN while the unit composes to ISOLATION_FLOOR_ONLY. IF THE SPIKE ABORTS BEFORE CLOSE, 177.003-T removes the workflow anyway and this task still runs to emit the block against that removal commit. Rollback is unchanged: git revert of 177.004-T's single creation commit, or deletion of that single added path. ELAPSED BOUND 30 min. Blocked by 177.003-T; blocks 177.006-T. Source 7F9CB5E9. Plan docs/plans/2026-09-18-conformance-isolation-spike-plan.md (plan_id conformance-isolation-spike, revision 7, awaiting independent attempt 07). Verdict manifest docs/reviews/2026-09-18-conformance-isolation-spike-plan-review.md.