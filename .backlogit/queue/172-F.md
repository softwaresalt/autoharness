---
artifact_type: feature
created_at: 2026-09-18T04:48:31.3703896Z
custom_fields:
    harness_status: pending
id: 172-F
labels:
    - checkpoint
    - recovery-contract
    - compatibility-policy
    - p-021
priority: medium
status: queued
title: Checkpoint resume_hint producer guarantee, author-time validation, and historical-record compatibility policy
updated_at: 2026-09-19T01:16:03.0991244Z
---

Close the checkpoint resume-hint gap with an ATOMIC, SAFE ROLLOUT: INERT BUILD-OUT FIRST, ACTIVATION LAST AND ALL AT ONCE.

THE ACTIVATION INVARIANT, STATED ONCE AND ENCODED IN THE DAG: at NO commit boundary does a contract prohibiting the raw backlogit create path exist while any producer still lacks a wired adapter. Every task before 172.009-T prohibits nothing, rewires nothing, and changes no registry; 172.009-T changes every affected surface in ONE change set; every task after it verifies what 172.009-T wired.

FIVE TOKENS: CHECKPOINT_RESUME_HINT_MISSING and CHECKPOINT_RESUME_HINT_EMPTY (harness origin), CHECKPOINT_LEGACY_HINTLESS_RESOLVED (reported) and CHECKPOINT_ACTIVE_HINTLESS (blocking) (historical origin), and CHECKPOINT_RECORD_MALFORMED (blocking, either origin).

CLASSIFICATION IS TOTAL. Accepted statuses are queued, active, shipped, abandoned and resolved; NON-TERMINAL = {active, queued}, TERMINAL = {shipped, abandoned, resolved}, with ABANDONED named explicitly so a deliberately stopped record never halts startup. CHECKPOINT_RECORD_MALFORMED is an EXPLICIT BLOCKING OUTCOME covering unparseable documents, duplicate keys, non-object roots, missing/wrong-typed/unrecognized status, missing or FUTURE schema_version, and wrong-typed required fields. A future schema_version BLOCKS rather than degrades. No record is ever unclassified.

THE SCAN IS EXECUTABLE. autoharness checkpoint scan --json is an invocable command whose JSON the startup surfaces consume; a Markdown agent cannot call a Python predicate by prose. CLI and the guarded MCP operation delegate to the SAME adapter and emit IDENTICAL structured tokens and JSON.

THE ADAPTER IS HARDENED. Two mutually exclusive input options (--state-dump-file / --state-dump-json) replace the ambiguous path-or-JSON option; origin is an INTERNAL CONSTANT with no public flag; paths are canonicalized then required to be workspace-contained regular files with link/junction/reparse/device components rejected; size, depth, string and collection bounds are enforced independently; the delegated call uses a fixed argv with shell=False, honours -- and leading-hyphen values; and EVERY validation failure exits non-zero, writes nothing, and never delegates.

GUARDED PARITY. After activation, raw create is reachable ONLY inside the adapter - exactly one raw-allowed entry in a TYPED PRODUCER INVENTORY - and every installed and template registry mapping and every agent tool declaration maps the guarded operation, derived by enumeration rather than hardcoded.

THE PRODUCER INVENTORY REPLACES THE BROAD TEXT SCAN. A free-text grep for the raw operation name cannot tell a prohibition document that names it in order to forbid it from a real producer declaration that reaches it. Bounded machine markers reconciled against a typed inventory as a bijection make that distinction decidable, and are proven by both a prohibition-reference fixture that must PASS and a synthetic third producer that must FAIL.

TEST-FIRST. Four RED tasks (172.008-T, 172.011-T, 172.012-T, 172.014-T) precede the implementations they constrain and depend on none of them; all RED modules are IMPORT-SAFE, loading not-yet-existing boundaries inside the test body so each test individually executes and fails on its own declared marker. GREEN is observed twice: 172.006-T over the complete inert build-out (the activation precondition) and 172.010-T over the wired system, followed by the invariant-based live-corpus audit 172.007-T.

PLANNED BUT NOT IMPLEMENTED HERE: the exact instruction/template/mirror/manifest checksum obligations are specified in the plan and carried out inside 172.009-T and asserted by 172.010-T; no separate implementation task precedes them.

FIFTEEN tasks, shipment 180-S. Stash 2A7C48A8. Plan docs/plans/2026-09-17-checkpoint-resume-hint-contract-plan.md (revision 7). Verdict manifest docs/reviews/2026-09-17-checkpoint-resume-hint-contract-plan-review.md (latest_attempt 07, verdict REMEDIATED-PENDING-REVIEW at plan revision 7, awaiting independent attempt 08).