---
artifact_type: feature
created_at: 2026-09-12T22:47:45.1005867Z
custom_fields:
    harness_status: pending
id: 165-F
labels:
    - topology-gate
    - gate-correctness
    - dag
    - contract-change
    - pipeline-topology
    - urgent
status: queued
title: DAG-authoritative predecessor derivation for the pipeline-topology pre_claim gate
updated_at: 2026-09-12T22:47:45.1005867Z
---

Make explicit backlogit `blocks` DAG dependencies the sole predecessor authority for `autoharness gate pipeline-topology --phase pre_claim`, retiring the implicit numeric-adjacency heuristic as a claim-blocking input, adding deterministic predecessor provenance to gate output, and aligning the `dag-readiness` advisory gate with the authoritative model (including the closure-evidence dimension) so operators are never shown contradictory readiness.

SOURCE ARTIFACTS:
- Deliberation: docs/decisions/2026-09-12-dag-authoritative-predecessor-derivation-deliberation.md
- Plan (hardened): docs/plans/2026-09-12-dag-authoritative-predecessor-derivation-plan.md
- Plan review (PASS, 0 P0 / 0 P1): docs/reviews/2026-09-12-dag-authoritative-predecessor-derivation-plan-review.md
- Original bug report (historical, unmodified): docs/bugs/2026-09-11-autoharness-pipeline-topology-numeric-predecessor-bug.md
- Stash origin: AF2890B7

SUPERSESSION: The bug report's §7 recommendation was presentation-only and labelled numeric-fallback removal a FUTURE option. Operator direction (2026-09-12) supersedes that; the deliberation records the new decision explicitly rather than rewriting the historical record.

CRITICAL CORRECTION CARRIED INTO EXECUTION: `163-S` declares `dependencies: [162-S]` EXPLICITLY, so the numeric heuristic is a NO-OP for it. Its block token is PREDECESSOR_CLOSURE_INCOMPLETE (`closure_complete: null`), caused by a missing `docs/closure/162-S-*-post-merge-closure.md` artifact. Retiring numeric adjacency will NOT unblock 163-S. No task may claim otherwise; the real cause is handled as a separate task.

CONTRACT: `pipeline-topology --phase pre_claim` remains the SOLE claim authority. `dag-readiness` remains advisory and non-authorizing. Migration is fail-closed and explicit: a shipment with no explicit predecessor is a legitimate DAG root, passing with `predecessor_source: "none"` plus an `UNSEQUENCED_SHIPMENT` advisory note; workspaces that relied on unstated numeric sequencing may opt into the legacy blocking posture via `gates.pipeline_topology.unsequenced_shipment: block` (default `warn`).

SCOPE FENCES: excludes branch-naming authority (stash 86498B64), the cost-per-unit-of-work epic (9B582824), verify-workspace/Ship branch drift (97B28746, 50434138), and file-lock test follow-up (58A85283). Does not change `--force` semantics and does not modify backlogit.