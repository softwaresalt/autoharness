---
artifact_type: feature
created_at: 2026-09-12T22:47:45.1005867Z
custom_fields:
    harness_status: pending
id: 165-F
labels:
    - topology-gate
    - gate-correctness
    - dag
    - contract-change
    - pipeline-topology
    - urgent
status: queued
title: DAG-authoritative predecessor derivation for the pipeline-topology pre_claim gate
updated_at: 2026-09-13T00:52:29.1799523Z
---

Make explicit backlogit `blocks` DAG dependencies the sole predecessor authority for `autoharness gate pipeline-topology --phase pre_claim`, retiring the implicit numeric-adjacency heuristic from the claim path, resolving edge-less shipments through an explicit four-state contract, adding deterministic predecessor provenance to gate output, providing a read-only sequencing audit as the migration path, and aligning the `dag-readiness` advisory gate with the authoritative model across every state.

SOURCE ARTIFACTS (all committed):
- Original intake bug report: docs/bugs/2026-09-11-autoharness-pipeline-topology-numeric-predecessor-bug.md — committed VERBATIM and UNMODIFIED in review-fix cycle 2 under operator authorization, so the durable record no longer references an untracked file. Its recommendation (presentation-only remedy) is SUPERSEDED by operator direction; see the deliberation's Historical context section.
- Deliberation (rev 3): docs/decisions/2026-09-12-dag-authoritative-predecessor-derivation-deliberation.md
- Plan (rev 3, hardened): docs/plans/2026-09-12-dag-authoritative-predecessor-derivation-plan.md
- Plan review (cycle 3): docs/reviews/2026-09-12-dag-authoritative-predecessor-derivation-plan-review.md
- Stash origin: AF2890B7 (archived)

ACTIVE TASK SET (8 tasks, all in shipment 173-S): 165.001-T (derivation test harness), 165.002-T (engine four-state derivation), 165.003-T (audit_sequencing phase + migration ledger), 165.004-T (advisory parity tests), 165.005-T (advisory engine parity), 165.006-T (CLI provenance + audit rendering), 165.008-T (agent template sources AND installed dogfood mirrors, atomic), 165.009-T (docs + migration guide).
ARCHIVED, NOT ACTIVE: 165.007-T (descoped to stash FD0CCB42) and 165.010-T (merged into 165.008-T). Neither may be re-created.

CONTRACT (decision D1). Predecessors derive EXCLUSIVELY from ShipmentState.blocking_predecessor_ids. When that set is empty, exactly one of three states applies, each with its own provenance value: DECLARED ROOT (record carries the `dag-root` label) passes as `declared_root`; GENESIS passes as `genesis` under the narrow rule below; UNSEQUENCED blocks with token UNSEQUENCED_SHIPMENT and a message naming both remedies. Non-empty edge sets evaluate as `explicit`. The four states are total and mutually exclusive, and `predecessor_source` appears on every payload, blocked and passed alike.

GENESIS IS NARROW AND NON-FAIL-OPEN (review-fix cycle 2, finding 2 — this REPLACES the cycle-1 "no shipped-terminal shipment exists" rule). Genesis is granted only when the candidate declares no edges, carries no `dag-root`, AND: (G2) no shipped-terminal shipment exists anywhere, live or archived; (G3) the candidate is the SOLE EXTANT NONTERMINAL shipment record, live or archived; (G4) no abandoned shipment record exists anywhere. Absence of shipped history alone is INSUFFICIENT: in a workspace with several queued shipments and nothing yet shipped, that weaker rule returns genesis for EVERY edge-less shipment simultaneously — including later-numbered candidates downstream of other pending work — which is an unearned pass in exactly the fail-open direction this feature exists to close. Sole-extancy makes genesis a cardinality-1 state that cannot hold for two shipments at once and ceases to be available the moment a workspace holds a second shipment record of any kind. From that point on, every intended root must be DECLARED or it blocks as `unsequenced`. The three workspace-level facts are computed ONCE per evaluation from one snapshot and shared by both gates.

LABEL PARSING IS ITS OWN VALIDATOR (review-fix cycle 2, finding 5). The `dag-root` declaration is read from the shipment record's `labels` frontmatter using a LABELS-SPECIFIC fail-closed validator — NOT the artifact-ID validator `_tuple_of_str`, whose `_ARTIFACT_ID_PATTERN` rejects `dag-root` and would turn every labelled shipment record, including 173-S itself, into a hard BacklogUnavailableError. Fail-closed container/member discipline is preserved; label syntax replaces artifact-ID syntax; reader-level tests cover both polarities.

WHY NOT A FLAT BLOCKING MODE (review-fix cycle 1, decision Finding 6): a flat `block` posture for edge-less shipments is NOT a legacy-compatibility mode. Legacy behaviour blocked only when a numerically lower unshipped shipment existed; a genuine root passed. A flat block deadlocks every legitimate DAG root with no in-contract escape. The declared-root state is what keeps intentional roots representable AND claimable while staying fail-closed.

NO CONFIG OR SCHEMA SURFACE (decision D2): the contract is carried by backlog record data that already exists. No `.autoharness/config.yaml` key, no root-schema change, no new versioned schema mirror, no schema_contracts.py work, no harness-config template change, no install/tune preservation, no dogfood config change, no config parity tests.

ROLLBACK IS DATA-ORDERED, NOT CODE-ONLY (review-fix cycle 2, P2 finding 11): migration mutates backlog data (`blocks` edges, `dag-root` labels) that a code revert cannot undo. The audit emits a MIGRATION LEDGER capturing pre-migration edge/label state; rollback reverts DATA FIRST using the ledger, THEN the engine. Without a ledger the rollback claim is narrowed to manual reconstruction.

CRITICAL CORRECTION CARRIED INTO EXECUTION: `163-S` declares `dependencies: [162-S]` EXPLICITLY, so the numeric heuristic is a NO-OP for it. Its block token is PREDECESSOR_CLOSURE_INCOMPLETE. The 162-S closure artifact EXISTS and is committed (docs/closure/2026-09-11-162-s-154-f-closure.md, c6ed877b) but is written under a naming contract the gate's glob cannot match. Retiring numeric adjacency will NOT unblock 163-S; no task may claim otherwise.

DESCOPED (decision D4): the closure-evidence producer/consumer naming defect is REMOVED from this feature and captured as DEFERRED SCOPE EXPANSION stash FD0CCB42 (kind bug, priority high, requires deliberation: yes — verified present and durable in .backlogit/stash.jsonl). Task 165.007-T, which previously carried it, is ARCHIVED with a forward reference. The closure gate must NOT be weakened, and no competing 162-S closure artifact may be authored.

BOOTSTRAP (decision D6, re-scoped in review-fix cycle 2, finding 3): operator-authorized EXACTLY TWO audited `pre_claim --force` invocations for 173-S ONLY — U1 before branch/worktree creation and U2 immediately before the claim — because the Ship protocol runs the gate twice and a "single-use" grant was unsatisfiable. Each invocation is valid only if the sole blocking token is PREDECESSOR_NOT_SHIPPED, the inferred predecessor is exactly 172-S, HEAD and the 173-S manifest match the reviewed state, and no other topology/check/closure/secrets violation exists. Authority expires immediately on successful claim or any mismatch; a third invocation (e.g. the CLAIM_NOT_OBSERVED retry path) is NOT authorized. No broader force authority; `--force` semantics unchanged. 173-S carries `labels: [dag-root]`, so it passes natively as `declared_root` once 165.002-T lands.

SCOPE FENCES: excludes branch-naming authority (stash 86498B64 — next but separate), the cost-per-unit-of-work epic (9B582824), verify-workspace/Ship branch drift (97B28746, 50434138), and the file-lock test follow-up (58A85283). Does not modify backlogit. Does not touch closure-evidence semantics.