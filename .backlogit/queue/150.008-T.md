---
artifact_type: task
created_at: 2026-08-30T05:21:51.8713244Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: autoharness-2h-rule-v1
    size_source: agent
dependencies:
    - 150.007-T
    - 150.006-T
id: 150.008-T
labels:
    - release
    - pr
parent_id: 150-F
priority: medium
references:
    - docs/plans/2026-08-29-v1_5_0-release-preparation-plan.md
status: active
title: Open release implementation PR, drive to green CI, and merge to main
updated_at: 2026-08-30T09:22:22.584617Z
---

Open the single release IMPLEMENTATION PR, drive it to green CI, and MERGE it. This task owns the transition all the way from "opened" to "merged on main".

CONSTRAINTS:
- INV-2 / P-016: exactly ONE ACTIVE IMPLEMENTATION worktree, branch, and PR. Do NOT create PARALLEL implementation worktrees — a second concurrent worktree breaks the pipeline topology gate globally (docs/compound/2026-08-16-multiple-implementation-worktrees-blocks-topology-gate-globally.md).
- *** THIS DOES NOT FORBID SHIP'S POST-MERGE CLOSURE BRANCH/PR (corrected after PR #422 review cycle 3). *** .github/agents/_ship.agent.md L624-L645 "Post-Merge Branch Protocol" is NON-NEGOTIABLE and has NO local-record bypass: closure commits (backlog archival, knowledge graduation, doc updates, compound refresh, compact-context) MUST NOT land on main and MUST go on a post-merge/{feature_slug} branch merged via a separate closure PR with its own operator approval. That branch is created FROM main AFTER this PR merges, so it is SEQUENTIAL, never parallel, and P-016 is preserved. An earlier version of this task said "ONE branch, ONE PR for the entire release", which would have forced Ship to choose between this plan and a non-negotiable protocol. It does not.
- Planning baseline was main @ 484da671. EXECUTE from the current synchronized origin/main HEAD, which will have advanced past 484da671 once the staging PR (#422) merged. Re-verify main == origin/main and a clean worktree at start; do NOT require equality with 484da671.

SCOPE:
1. Open the PR containing: the two blocker fixes, the curated CHANGELOG 1.5.0 section, and the synchronized six-surface version bump.
2. Confirm CI is green on the PR head.
3. Re-verify consistency at PR head: tag-target version (1.5.0) == pyproject.toml version, and a "## 1.5.0 - <date>" changelog section exists and is non-empty.
4. Address review findings within the standard fix-cycle limits.
5. *** MERGE THE PR TO main. ***
   - Requires EXPLICIT OPERATOR APPROVAL to merge.
   - Preconditions: CI green on the final head; dry run A and dry run B both PASSED; no unresolved review threads.
   - After merging, confirm main is green post-merge and record the MERGE COMMIT SHA — 150.009-T tags that commit.
   WHY THIS STEP EXISTS: without it no task owned the "merge-ready -> merged" transition, so the shipment dead-ended with 150.009-T unblocked but unable to proceed.

ACCEPTANCE:
- CI green, PR MERGED to main, merge commit SHA recorded and handed to 150.009-T.
- Dry run A and dry run B both PASSED before the PR was opened (they are gates, not parallel work).
- No unrelated changes in the diff — the "Out of scope" list in the plan enumerates what must NOT appear.

STOP CONDITIONS:
- CI red -> do NOT merge and do NOT tag.
- Operator approval to merge not obtained -> STOP.

NOTE: everything through this task is REVERSIBLE (a merge can be reverted). 150.009-T (tag push) arms an IRREVERSIBLE publish. Treat the 150.008-T -> 150.009-T boundary as the point of no return. Ship's post-merge closure protocol runs after 150.010-T and is expected, not a scope violation.