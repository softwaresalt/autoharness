---
artifact_type: task
created_at: 2026-07-29T11:57:58.5902624Z
dependencies:
    - 097.001-T
id: 097.002-T
parent_id: 097-F
priority: medium
status: queued
title: Sweep non-code cross-references (docs, instructions, gitattributes, workspace-profile, CHANGELOG)
updated_at: 2026-07-29T11:57:58.5902624Z
---

<!-- BEGIN:description -->
## Task 097.002 — Non-code cross-reference sweep

Width domain: docs/instructions/config prose. Est. ~1.5h. Depends on 097.001.

Replace old dot-prefixed filenames/handles with the new underscore names in these
files ONLY (see feature 097-F Group B for line hints):

* `.github/instructions/harness-architecture.instructions.md`
* `.github/copilot-review-instructions.md`
* `docs/capability-packs.md`
* `docs/getting-started.md`
* `docs/backlogit-operating-model.md`
* `docs/backlogit-compatibility-matrix.md`
* `docs/copilot-review-gate.md` (relative link `../.github/agents/.ship.agent.md`)
* `templates/skills/shipment-reconcile/SKILL.md.tmpl`
* `.autoharness/workspace-profile.yaml` (2 path refs)
* `.gitattributes`: rename the `.github/agents/.ship.agent.md text eol=lf` line to
  `_ship.agent.md`, AND add `.github/agents/_stage.agent.md text eol=lf` (closes a
  pre-existing LF-pin gap; required for portable checksums — see 097.005/H2).
* `CHANGELOG.md`: ADD one new Unreleased entry describing this rename. Do NOT
  rewrite the pre-existing historical entries.

Do NOT touch EXEMPT paths (see feature). Do NOT change prose display names
"Stage"/"Ship".
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
## Acceptance criteria

* Each listed file references only `_stage.agent.md`/`_ship.agent.md`
  (+ `.tmpl`) and `_Stage`/`_Ship`; zero old dot-name hits in these files —
  **EXCEPT** `CHANGELOG.md`, whose pre-existing historical entries legitimately
  retain the old dot-names (only a NEW entry is added). This matches the
  zero-residual gate exempt-paths list (H1), so the contract is satisfiable.
* `.gitattributes` contains eol=lf pins for BOTH `_ship.agent.md` and
  `_stage.agent.md`.
* CHANGELOG has a new entry; historical entries unchanged.
* markdownlint on changed `.md` files passes (heading hierarchy, fences).
<!-- END:acceptance-criteria -->