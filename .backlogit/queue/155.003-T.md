---
artifact_type: task
created_at: 2026-08-31T22:16:54.7283462Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: autoharness-stage-2h-v1
    size_source: agent
id: 155.003-T
parent_id: 155-F
priority: medium
status: queued
title: Extend the docs frontmatter guard to detect silent space-hash truncation (red before green)
updated_at: 2026-08-31T23:29:23.7914512Z
---

RE-SCOPED in review-fix cycle 1 (WIDTH constraint). This task previously repaired TEN documents AND extended a test guard - two different widths and two review lenses in one unit. It is now the GUARD ONLY; the ten repairs move to 155.007-T (batch 1 of 5) and 155.008-T (batch 2 of 5). Task ID preserved for traceability. SCOPE: tests/test_docs_frontmatter_decodes.py ONLY. No docs/ file is touched by this task. PROBLEM (11BCE865): the failure mode is distinct from, and nastier than, a decode failure - an UNQUOTED YAML scalar containing SPACE-HASH triggers YAML's comment rule, so the value is silently cut at the # and the document STILL PARSES. The existing guard asserts the frontmatter DECODES - which it does - so it cannot see this class at all. None of the ten were introduced by the P-020 compaction that found them. DELIVERABLE: extend the guard to detect silent space-hash TRUNCATION, not merely decode failure. RED BEFORE GREEN (mandatory, and the whole point of the split): the extended guard MUST be observed FAILING against the UNREPAIRED corpus, identifying the ten known-truncated files. Record the observed red result and the exact file list before any repair begins. A guard authored after the repair and never observed failing proves nothing. BLOCKS 155.007-T and 155.008-T - both repair batches are verified green by THIS guard, and both draw their file lists from the red result recorded here. ALSO CONSUMED BY SHIP-9: 159.003-T's acceptance requires this strengthened guard (SHIP-5 plan H4 / SHIP-9 plan H4), which is why SHIP-5 blocks SHIP-9 as a genuine artifact dependency rather than mere chain ordering. ACCEPTANCE: guard detects space-hash truncation; observed red against the unrepaired tree naming the ten files; no docs/ file modified by this task; PYTHONPATH=src python -m unittest discover -s tests runs the new assertion. SAFETY MODE: careful. Source: 11BCE865. Plan: docs/plans/2026-08-31-ship5-fail-closed-harness-gates-plan.md
