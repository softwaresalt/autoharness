---
artifact_type: task
created_at: 2026-08-21T20:26:27.8940635Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 142.005-T
    - 142.007-T
id: 142.006-T
labels:
    - bug
    - templates
    - parity
    - manifest
    - checksum
parent_id: 142-F
priority: medium
references:
    - docs/plans/2026-08-21-verify-workspace-variable-derivation-plan.md
    - docs/decisions/2026-08-20-template-dogfood-render-parity-spike.md
status: queued
title: Reconcile template/dogfood parity and refresh harness-manifest checksums
updated_at: 2026-08-21T20:26:27.8940635Z
---

Reconcile the template/dogfood parity contract and the harness manifest after the derivation changes.

**Plan:** `docs/plans/2026-08-21-verify-workspace-variable-derivation-plan.md` Task 4 + amendment B1

## Steps
1. Re-run `tests/test_scope_containment_policy_contract.py::test_clean_pairs_are_byte_identical_via_render_template`. If any of the four CLEAN pairs diverged as predicted by 142.001-T's AC0b intersection, update BOTH sides of the pair and refresh the manifest checksum ATOMICALLY (constraint C6).
   - **Amendment B1 STOP RULE.** Only a pure placeholder-to-same-literal substitution may be reconciled here — i.e. the newly rendered value is provably IDENTICAL to the literal already in the dogfood copy. Any other divergence is a NORMATIVE edit to live agent policy and must STOP and re-enter P-021 capture. Do not "make the dogfood copy match" a changed render.
2. Refresh `.autoharness/harness-manifest.yaml` checksums for every regenerated artifact.

## Acceptance criteria
- AC4a. `tests/test_scope_containment_policy_contract.py` fully green, including `test_clean_pairs_are_byte_identical_via_render_template`.
- AC4b. `autoharness verify-workspace --workspace .` reports 0 unresolved and NO new blockers or warnings versus the pre-change baseline (0 blockers / 0 warnings per the 145-S closure record).
- AC4c. Checksum scan clean — no manifest entry left stale.
- AC4d. Any reconciled pair is individually recorded with the literal-identity proof required by B1; if none were reconciled, that null result is recorded explicitly.

## Out of scope (P-021 C1 — hard boundary)
The F3/F4(2) SEMANTIC PROSE DRIFT identified by `docs/decisions/2026-08-20-template-dogfood-render-parity-spike.md` (~1,200 lines of bidirectional normative drift across the three agent pairs and `github-pr-automation`). That work was explicitly refused by the spike because it is a correctness-bearing editorial decision per sentence on the harness's own governing agent contracts. Touching it here would silently rewrite live agent policy under cover of a bug fix.