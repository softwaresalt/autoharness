---
artifact_type: task
created_at: 2026-09-03T08:24:31.6425707Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 160.004-T
labels:
    - packaging
    - python
    - build-support
parent_id: 160-F
priority: medium
status: queued
title: Manifest loader/resolver in build_support (excluded from payload)
updated_at: 2026-09-03T08:24:31.6425707Z
---

Implement the manifest loader/resolver that turns `.autoharness/payload-manifest.yaml` into a resolved per-channel file set.

PLACEMENT (plan-review finding P1-6): this module lives in `build_support/`, which is itself EXCLUDED from the payload. It must NOT live in `src/autoharness/` — that would ship the packaging rules inside the very artifact they trim, a coupling inversion contradicting the feature's own minimalism goal.

RESPONSIBILITIES:
- Load and schema-validate the manifest.
- Resolve include/exclude globs plus per-channel (wheel, plugin) overlays into a concrete file set.
- Enumerate tracked paths and report any path that is neither included nor explicitly excluded (drives the AC2/AC8 fail-closed guard).

ERROR HANDLING (plan-review finding P2-7): a malformed or schema-invalid manifest MUST fail closed with a specific, typed exception. It must never degrade to a permissive/default-inclusive payload — permissive failure reproduces the original defect.

TYPE SAFETY: explicit type signatures on public functions per Constitution Principle I (Safety-First Python).

ACCEPTANCE: AC1, AC2. Resolver output is deterministic and order-stable.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T4)