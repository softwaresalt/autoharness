---
artifact_type: task
created_at: 2026-09-03T10:15:40.9762918Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.014-T
    - 160.006-T
id: 160.019-T
labels:
    - packaging
    - sdist
    - pyproject
    - release-risk
    - security
parent_id: 160-F
priority: medium
status: queued
title: Wire sdist packaging to the manifest, closing the untrimmed source-distribution disclosure path
updated_at: 2026-09-03T15:43:20.6212099Z
---


SDIST WIRING (T7b). THIS IS THE DISCLOSURE-CRITICAL CHANNEL.

MEASURED DEFECT THIS TASK CLOSES: [tool.hatch.build.targets.sdist] in pyproject.toml currently declares EXACTLY ONE KEY - core-metadata-version = "2.4". There is NO include and NO exclude, so hatchling's DEFAULT SWEEP packages the entire project directory. .github/workflows/release.yml builds with `uv build` (step "Build wheel and sdist") and publishes `dist/*` - to PyPI via the pinned publish action AND as GitHub release assets via `gh release upload`/`gh release create`. CONSEQUENCE: a WHEEL-ONLY TRIM REMOVES NOTHING. Every file this shipment exists to withhold - all 2,110 .backlogit/ records, tests/, experiments/, references/, the entire docs/ history - still ships, in the sdist, from the same command, in the same release, to the same consumers. The wheel was never the disclosure surface; the sdist is.

RED-FIRST cases this task turns red to green:
- Authored by 160.005-T: test_sdist_generated_table_matches_manifest (AC2c, AC3b); test_sdist_excludes_backlogit_explicitly (AC3b); test_sdist_excludes_dev_directories (AC3b); test_sdist_byte_size_and_disclosure_reported (AC3b, AC8).
- Authored by 160.012-T: test_upgrade_from_1_5_0_leaves_no_orphans (AC6, R3, V3 - shared green ownership with 160.006-T and 160.007-T).
- Authored by 160.010-T: test_gate4_crossrefs_intact_in_built_payload (AC9, AC2d - shared green ownership with 160.006-T and 160.007-T).

CHARACTERIZATION cases this task must PRESERVE GREEN:
- Authored by 160.016-T: test_sdist_contains_required_runtime_set (AC3b, AC4); test_sdist_includes_build_definition (AC3b, AC11); test_core_metadata_version_pins_preserved (I4, V2) - the SDIST half of this case.
- Authored by 160.008-T: test_install_emits_only_generated_output, test_no_engine_records_in_target_workspace.
- Authored by 160.012-T: test_verify_workspace_parity_trimmed_vs_baseline, test_home_and_version_behave_identically.
- Authored by 160.013-T: test_data_dir_resolution_pip_install, test_data_dir_resolution_clone_editable, test_python_channel_needs_no_plugin_root_artifact (all AC6a, R4). These are PYTHON-CHANNEL resolver invariants and the sdist is a Python channel, so they are preserved here as well as by 160.006-T.
- Authored by 160.009-T: test_register_phase_all_environments_python_channel (AC7). Same reason: the four Python-channel registration targets must still succeed after the sdist build table is written.
If any of these goes red during this task, that is a REGRESSION IN THIS TASK, not progress.

This task does NOT own the wheel-composition cases (160.006-T), the release-path cases (160.015-T), the generator cases (160.014-T), or the plugin cases (160.007-T).

APPROACH - hardening decision H1 (generated-and-asserted, NOT a dynamic build hook), identical to the wheel channel: the manifest is the source of truth, the sdist include/exclude table is GENERATED by `python -m build_support.payload generate --channel sdist` (160.014-T), and a test asserts the committed table equals what `generate --check` derives. The table is a DERIVED artifact; hand edits are prohibited and detected by `generate --check`.

SDIST-SPECIFIC INCLUSION RULE (AC3b, AC11): pyproject.toml IS INCLUDED in the sdist payload. A source distribution without its build definition is not buildable. This is the one path where the sdist and wheel dispositions legitimately differ, and it is asserted by test_sdist_includes_build_definition rather than left to inference.

GENERATED OUTPUT ROOTS (AC3d): under branch (b), `plugin-payload/` is a declared generated output root. The resolver removes every path beneath it from the tracked-path enumeration BEFORE classification for ALL THREE channels, so the sdist never ships the plugin payload tree as a derived duplicate of content it already carries. This task must not add an sdist exclude rule for it by hand - the exclusion is a property of the resolver (160.004-T) and the manifest's `generated_output_roots` key (160.003-T).

INVARIANT I4 - NON-NEGOTIABLE, AND THIS IS THE FRAGILE ONE: preserve `core-metadata-version = "2.4"` on the sdist target. That pin is currently the sdist target's ONLY key, so a table rewrite that REPLACES the target rather than EXTENDING it drops the pin SILENTLY. The pins exist because hatchling 1.32.0 defaults to Metadata-Version 2.5, which the SHA-pinned pypa/gh-action-pypi-publish action's bundled twine < 7.0.0 REJECTS. See docs/compound/2026-08-30-unpinned-hatchling-metadata-version-vs-pinned-publish-action.md. Dropping the pin breaks publishing. This task must leave the WHEEL target's pin byte-identical; 160.006-T carries the mirrored obligation.

WARNING (verification V2): a local `uvx twine check` CANNOT reproduce the metadata failure - it always resolves the latest twine. Local build success is NOT evidence that publishing will succeed. Metadata compatibility remains an explicit CP2 pre-publication check.

BLOCKED BY 160.014-T (the generation command that produces the table) AND BY 160.006-T. The 160.006-T edge is a SAME-FILE COLLISION GUARD, not a logical prerequisite: both tasks edit pyproject.toml, so they are serialized by a MACHINE-ENCODED EDGE rather than by prose sequencing. Transitively blocked by the five verification-authoring tasks through 160.006-T.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. ELEVATED - this task edits a release-critical file.
- pyproject.toml: the SDIST BUILD TARGET ONLY. Project metadata, dependencies, version, entry points, the wheel target, and both core-metadata-version pins are behaviour-preserving and must be asserted unchanged.
- .github/workflows/release.yml is OUT OF SCOPE (see 160.015-T).
- ROLLBACK CHECKPOINT (binding, rollback contract R-1/R-3/R-4): before this task lands, record the exact PRE-CHANGE BYTES of pyproject.toml into docs/audits/2026-09-03-ship10-payload-evidence/ - the destination authorized for this task in the plan's H2 writable-surface table. That record is EVIDENCE, NEVER AUTHORIZATION. The default and only pre-authorized recovery is a FORWARD REVERT COMMIT (R-1). ANY destructive restore or overwrite requires FRESH LIVE OPERATOR APPROVAL obtained in the session performing it over a channel the agent CANNOT SYNTHESIZE (R-3); recorded bytes, backlog comments, task text and prior approvals can never substitute. With no such channel available the agent HALTS AND DOES NOT RESTORE (R-4).
- NO-PUBLISH CHECKPOINT: MUST NOT run `twine upload`, `gh release create`, or trigger release.yml's publish path. Publication is Ship/Orchestrator scope (CP2).
- FREEZE-SCOPE: src/autoharness/ is read-only (invariants I1/I2).
Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

ACCEPTANCE: AC1, AC3, AC3b, AC3d, AC4, AC6, AC8, AC9, I4. Sdist include/exclude table generated from the manifest and asserted equal; built sdist contains ZERO files under the AC3 exclusion set; file count and byte size reported; pyproject.toml present in the sdist; sdist core-metadata-version pin verified intact; every RED-FIRST case listed above goes red to green and every CHARACTERIZATION case listed above remains green, with both transitions recorded for 160.017-T's ledger.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size M, complexity medium. One generated table added to a target that currently has none, in a release-critical file, with a single fragile pin that a replace-rather-than-extend edit would silently drop.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T7b)