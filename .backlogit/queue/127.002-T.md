---
artifact_type: task
created_at: 2026-08-15T05:41:30.3705543Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: stage-2h-v1
    size_source: agent
id: 127.002-T
labels:
    - verification
    - tests
    - additive-only
owner: stage
parent_id: 127-F
priority: low
status: queued
title: Add an additive non-root-feature negative control to the topology verifier
updated_at: 2026-08-15T09:41:01.6822579Z
---

Strengthen the shipment-topology verifier negative control. In docs/spikes/2026-08-09-plan1-shipment-topology-proof/verify-plan1-shipment-topology.ps1 near line 584, Test-CascadeEligible is called with a bare task ID ('118.003-T'), so the case exercised is 'manifest has no feature member', not 'manifest has a non-root feature' as the variable name nonRoot implies. The non-root-feature rejection branch is never executed. STAGE RULING (operator-granted planning authority): the verifier is cited evidence for F14, so existing assertions MUST NOT be modified, reordered, or weakened -- the fix is PURELY ADDITIVE: add a genuine non-root-feature negative control alongside the existing one. CORRECTION (PR #339 Copilot review, comment 3788712405): an earlier draft of this task also asked for the existing assertion MESSAGE to be corrected. That instruction was WRONG and is WITHDRAWN -- line 585 already reads "NEGATIVE CONTROL: a manifest with no feature member -> REJECTED (nonRoot.Reason)", which already describes the no-feature-member case it actually tests, and editing it would contradict the MUST NOT be modified constraint above. The only misleading token is the IDENTIFIER nonRoot. Existing predicates, messages and ordering stay UNCHANGED; renaming nonRoot to noFeatureMember is OPTIONAL and, if done, is a pure local-variable rename that changes no assertion semantics and no assertion count. Source stash A5628E7E.