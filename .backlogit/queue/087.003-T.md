---
artifact_type: task
created_at: 2026-07-13T15:21:06.4409217-07:00
dependencies:
    - 087.001-T
    - 087.002-T
id: 087.003-T
parent_id: 087-F
priority: medium
status: queued
title: document-bun-runtime-and-audit-npx-launcher-refs
updated_at: 2026-07-13T15:33:35.5302826-07:00
---


<!-- BEGIN:acceptance-criteria -->
- `docs/environment-setup.md` states Bun and `bunx` are REQUIRED MCP-runtime prerequisites for MCP tools that need a JavaScript engine, and that both must be installed and available on `PATH` because `.mcp.json` and the backlog-md registry invoke `bunx` by bare command.
- `docs/environment-setup.md` includes cross-platform Bun install guidance for Windows/macOS/Linux and PATH verification commands such as `bun --version` and `bunx --version`.
- Docs state the actual Node requirement determined by 087.001-T and 087.002-T: do not assert Node is eliminated unless `bunx --bun` compatibility is verified for every JS-engine MCP server and backlog-md; document Bun-only where compatible and Bun+Node where bare `bunx` remains required.
- Docs explain that packages with `#!/usr/bin/env node` shebangs can make bare `bunx` delegate to Node, and that `bunx --bun` is the mechanism for forcing the Bun runtime; the Node-elimination claim is therefore contingent on empirical `--bun` initialization results.
- Other existing docs that enumerate tool/runtime prerequisites (for example prerequisites/requirements sections, README setup, install-harness/workspace-discovery guidance) list Bun/bunx-on-PATH and any still-required Node prerequisite alongside the existing native-binary/PATH requirements for backlogit/engram/graphtor-docs.
- MCP-launcher npx guidance in the audited surfaces is updated to bunx (or recorded N/A with rationale).
- Non-MCP npx references are explicitly left unchanged.
- No unresolved `{{VARIABLE}}` placeholders introduced.
- If any installed mirror is changed, Ship updates its `.autoharness/harness-manifest.yaml` checksum.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Ship documents Bun/bunx as the MCP-tool runtime prerequisite in `docs/environment-setup.md`: Bun and `bunx` are REQUIRED prerequisites for MCP tools that need a JavaScript engine, and both lightweight runtime executables must be installed and available on `PATH` because `.mcp.json` and the backlog-md registry invoke `bunx` by bare command. Include cross-platform Bun install guidance for Windows/macOS/Linux and verification commands such as `bun --version` and `bunx --version`. Document the actual runtime facts produced by 087.001-T and 087.002-T: bare `bunx` can still delegate to Node when a package ships a `#!/usr/bin/env node` shebang, while `bunx --bun` forces the Bun runtime. Do not assert Node is eliminated unless every JS-engine MCP server and backlog-md registry launcher has been verified to initialize under `bunx --bun` with Node unavailable; otherwise document Bun+Node where bare `bunx` remains necessary. Audit other existing docs that enumerate tool/runtime prerequisites (for example prerequisites/requirements sections, README setup, install-harness/workspace-discovery guidance) and list Bun/bunx-on-PATH, plus any retained Node prerequisite, alongside native-binary/PATH requirements already documented for backlogit/engram/graphtor-docs. Audit tracked npx references for MCP-launcher context and update launcher guidance in `.github/skills/install-harness/SKILL.md`, `.github/skills/workspace-discovery/SKILL.md`, `schemas/backlog-tool-registry.schema.json` (example/description), and any mirror templates. Do NOT change npx references unrelated to MCP launching (e.g., `templates/instructions/technology-typescript.instructions.md.tmpl` npx-for-TS-tooling, or npx in test fixtures) — record those as intentionally out of scope. Maintain dogfood template/mirror parity where an audited file has both.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Description-of-work only. Execute this docs task after 087.001-T and 087.002-T determine the real runtime facts, so docs reflect the empirical `bunx --bun` compatibility results rather than assuming Node is eliminated. Keep the audit tight to MCP-tool runtime/launcher prerequisites; avoid a blanket npx→bunx find-replace across the repo. Additional operator stash `DD1201D3` was folded into this task and archived during Stage so it is not pending work.
<!-- END:implementation-notes -->
