---
artifact_type: task
created_at: 2026-09-18T04:52:26.6443917Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 169.016-T
id: 169.007-T
labels:
    - docs
parent_id: 169-F
priority: high
status: queued
title: 'P-002.7 DOCS (verdict-gated): document the contract, its preserved distinction, and the explicitly-undelivered downstream detection'
updated_at: 2026-09-19T18:58:21.9533795Z
---

DOCS PHASE. DOCUMENTATION IS AUTHORIZED BY A VERDICT, NOT BY AN EDGE. FIRST ACTION, BEFORE ANY DOCUMENTATION FILE IS OPENED FOR WRITING: read the post-activation confirmation artifact .autoharness/gates/p002-7-status-contract-verdict.txt, written by 169.016-T, and FAIL CLOSED. THE GATE IS OPEN ONLY WHEN ALL OF THE FOLLOWING HOLD. WHOLE-FILE CONDITION: the file EXISTS, is READABLE, is NOT EMPTY, and its ENTIRE CONTENT IS EXACTLY ONE LINE - after stripping at most one trailing newline there is exactly one line and no other content, blank or otherwise. LINE CONDITIONS: that line begins with the literal prefix "COMPOSED_STATE: " and the token in its FIRST FIELD is the literal STATUS_CONTRACT_HELD, compared as an EXACT, CASE-SENSITIVE, BYTE-FOR-BYTE MATCH against that literal - no prefix match, no substring match, no normalization, no default. FRESHNESS CONDITIONS F1-F5, as declared in the plan's Shared gate primitives section and RECOMPUTED BY THIS TASK against the repository: F1 head_commit is present exactly once, is 40 lowercase hex, and EQUALS this task's own reading of HEAD at the instant it evaluates; F2 surface_digest is present exactly once, is 64 lowercase hex, and EQUALS this task's own CCD/v1 recomputation over EXACTLY the four declared surfaces in enumeration order - templates/policies/workflow-policies.md.tmpl, .github/policies/workflow-policies.md, templates/agents/_ship.agent.md.tmpl, .github/agents/_ship.agent.md; F3 resolved_surface_count is present exactly once and equals 4; F4 checked is present exactly once, matches the literal RFC 3339 UTC form YYYY-MM-DDThh:mm:ssZ, and is NOT EARLIER THAN the committer timestamp of the commit named by head_commit; F5 binding is present exactly once, is 64 lowercase hex, and EQUALS this task's own B/v1 recomputation from the line's own head_commit, surface_digest, resolved_surface_count and checked values under the tag p002-7-confirmation-binding/v1. EVERYTHING ELSE IS CLOSED, and each category below is a READING rather than an error. ABSENCE: the file does not exist; it exists but is unreadable; it is empty. MALFORMATION: the content is more than one line; there is no line beginning "COMPOSED_STATE: "; there is MORE THAN ONE such line; the first field is not one of the three declared tokens; or a required field of the HELD form is absent, duplicated or lexically malformed. STALENESS: any F1-F5 failure - a head_commit that is not the current HEAD, which is WHAT A REVERTED ACTIVATION ALWAYS PRODUCES because git revert necessarily creates a new commit; a surface_digest that does not match recomputation over the four declared surfaces; a resolved_surface_count other than 4; a checked that is absent, malformed or earlier than the head commit's committer timestamp; or a binding that does not match recomputation. FAILURE: the token is the literal STATUS_CONTRACT_DIVERGENT or the literal STATUS_CONTRACT_NOT_OBSERVED. FOREIGN VOCABULARY: a PREACTIVATION_* token, or a "PREACTIVATION_STATE: " prefix, appearing in this file - the two vocabularies are DISJOINT, so a readiness token can NEVER open the documentation gate. ON A CLOSED GATE THIS TASK TOUCHES NO DOCUMENTATION AT ALL: it creates, modifies and deletes NO FILE UNDER docs/ AND NO OTHER FILE, makes NO COMMIT, writes to NEITHER GATE ARTIFACT, EXITS NON-ZERO (EXIT CODE 1), and the unit RETURNS TO STAGE. It exits zero ONLY when the gate was OPEN and its documentation commit landed. THE EDGE IS ORDERING, THE TOKEN IS THE AUTHORIZATION: the dependency edge on 169.016-T guarantees only that the confirmation artifact EXISTS before it is read, and it clears on PREDECESSOR COMPLETION, which 169.016-T reaches on ALL THREE of its tokens - two of which are FAILURES. The edge is never offered as the safety argument, and neither is 169.016-T's exit code, which depends on the invoking process having observed and honoured it. THIS GATE EXISTS BECAUSE ATTEMPT-05 FINDING N1 (P2) recorded that this edge was a COMPLETION EDGE with no fail-closed verdict read, even though the plan asserted in FOUR places that DOCS must not proceed on a non-pass confirmation, and applied its own H18 gate-predicate principle to the structurally identical ACTIVATE edge and not to this one. THIS TASK NEITHER PRODUCES NOR REINTERPRETS EVIDENCE: it does not run the suite, does not re-derive the evidence set, does not re-observe any assertion, writes to neither gate artifact, and never reinterprets a CLOSED gate as open. RECOMPUTING F1-F5 COMPUTES CONTENT AND COMMIT IDENTITIES, NOT ASSERTION OUTCOMES - it executes no test and adds no assertion, so it introduces nothing that must be observed red first, and it belongs to this task's gate-read logic rather than to the conformance suite. WHAT TO DOCUMENT, once the gate is OPEN. Document the P-002.7 contract, the distinction it preserves, and the downstream detection that is EXPLICITLY NOT DELIVERED here. Record: the canonical post-claim member-status vocabulary and its THREE claim-to-admission transition rows; the FOUR declared surfaces and the enumeration rule that produces them, including its exclusion of .autoharness/staging/ as gitignored generated verify-workspace output rather than a mirror; the observed-version attribution and why an externally-observed contract carries one; and the unit's rollout order PREPARE, RED, VERIFY, ACTIVATE, CONFIRM, DOCS, stating how it maps onto decision D2's binding rollout invariant PREPARE then VERIFY then ACTIVATE. DOCUMENT THE TWO DISTINCT VERDICTS AND NEVER CONFLATE THEM - this is the documentation half of closing attempt-04 finding M1 (P1), which found the unit's only gate emitter placed AFTER the irreversible activation commit. (1) THE PRE-ACTIVATION READINESS VERDICT, emitted solely by 169.017-T BEFORE activation, to .autoharness/gates/p002-7-preactivation-readiness.txt, in one of three literal "PREACTIVATION_STATE: " line forms carrying PREACTIVATION_READY, PREACTIVATION_BLOCKED or PREACTIVATION_NOT_OBSERVED, with PREACTIVATION_READY the ONLY token that authorizes activation. Document that 169.015-T reads this artifact as its FIRST ACTION and FAILS CLOSED, that the dependency edge is ORDERING ONLY while the TOKEN is the authorization, and that on a CLOSED gate no declared surface is touched at all. (2) THE POST-ACTIVATION STATUS-CONTRACT VERDICT, emitted solely by 169.016-T AFTER activation, to .autoharness/gates/p002-7-status-contract-verdict.txt, in one of three literal "COMPOSED_STATE: " line forms carrying STATUS_CONTRACT_HELD, STATUS_CONTRACT_DIVERGENT or STATUS_CONTRACT_NOT_OBSERVED, naming STATUS_CONTRACT_HELD as the only pass token. DOCUMENT EXPLICITLY THAT THE POST-ACTIVATION VERDICT IS A CONFIRMATION OF INSTALLED/TEMPLATE PARITY AND ACTIVE-CONSUMER BEHAVIOUR AND IS NOT THE EVIDENCE GATE THAT AUTHORIZED ACTIVATION, and that the two artifacts have different paths, different line prefixes and DISJOINT token vocabularies, with neither emitter reading or writing the other's path. DOCUMENT THE SHARED GATE PRIMITIVES AND THE FRESHNESS BINDING (attempt-05 findings N1 and N2, both P2): the CCD/v1 canonical content digest with its CRLF-to-LF normalization, its repository-relative POSIX path preimage and its fully enumerated input lists - SEVEN paths for the readiness candidate_digest (the four declared surfaces plus tests/p002_7_candidate_definition.py, tests/p002_7_near_miss_fixtures.py and tests/test_p002_7_member_status_contract.py) and FOUR for the confirmation surface_digest (the declared surfaces alone); the head identity pair head_commit and head_committed_at, read from the repository; the B/v1 binding over the five-line tagged preimage that COVERS checked, which is what makes checked a CONSUMED field rather than a decorative one; the RFC 3339 UTC checked format; and the F1-F5 predicate that BOTH authorizing consumers recompute. DOCUMENT WHY ONLY THE AUTHORIZING LINE CARRIES THE BINDING - a not-observed verdict is reachable precisely when the inputs cannot be read, so its digest is undefined by rule and could not be emitted truthfully, and a non-authorizing line is never consumed as an authorization. DOCUMENT THE SCOPE HONESTLY: this is a STALENESS AND MISTAKE-DETECTION contract, NOT a tamper-proof security boundary, and no wall-clock future check is imposed because clock skew would make the contract environment-dependent. For BOTH artifacts document the single-writer atomic whole-file-replace rule, that a re-run therefore necessarily produces a FRESH BINDING, the requirement of exactly one verdict line at all times, the exit-code contract (zero only on the single authorizing or passing token), and the closed list of read outcomes that resolve to the respective NOT_OBSERVED token - missing, unreadable, empty, no prefixed line, more than one such line, or an unrecognised token - so absence is decidable rather than ambiguous (attempt 03 finding L1, P1). State that BOTH destinations sit under .autoharness/gates/, which is GITIGNORED at .gitignore:7, so neither verdict is ever committed and neither leaves the working tree dirty, and that neither is a declared surface: the surface-enumeration rule's search scope is templates/policies/, .github/policies/, templates/agents/ and .github/agents/, so declared_surface_count stays fixed at 4 and the two generated artifacts can never be counted as a fifth. The revision-6 freshness fields were added INSIDE those two existing lines at those two existing paths; no new artifact, directory or ignored path was introduced. DOCUMENT THE POST-ACTIVATION FAILURE PATH: on STATUS_CONTRACT_DIVERGENT or STATUS_CONTRACT_NOT_OBSERVED the unit HALTS, THIS DOCS TASK DOES NOT PROCEED - AND THAT HALT IS MECHANICAL, because this task's own first-action fail-closed read finds a token that is not the literal STATUS_CONTRACT_HELD and therefore touches no documentation file, makes no commit and exits 1, without depending on any process having observed 169.016-T's exit code - the single 169.015-T activation commit is REVERTED AS A UNIT returning all four surfaces to their pre-activation state simultaneously, and the unit returns to Stage, a guarantee that holds only because activation is one commit. DOCUMENT THAT THE REVERT RE-CLOSES BOTH GATES WITHOUT EITHER CONSUMER BEING TOLD, AND NAME WHICH PREDICATE DOES IT: git revert advances HEAD past every head_commit written before it, so a surviving PREACTIVATION_READY line fails F1 at 169.015-T - F1 ALONE, which is sufficient - and a surviving STATUS_CONTRACT_HELD line fails F1 AND F2 at this task. F4 FAILS ON NEITHER AND MUST NOT BE DOCUMENTED AS A POST-REVERT FRESHNESS GUARD: it is anchored to the commit the line itself names, which a stale line satisfies (attempt 06 finding O3, P2). DOCUMENT THE SUPPORTED RECOVERY PATH AS MERGE-FRIENDLY ONLY: git revert and new forward commits; git reset --hard to the pre-activation commit, and any rebase or amend restoring the pre-activation commit identity, are PROHIBITED and outside this contract and must never be documented as an allowed rollback route. DOCUMENT THIS TASK AS WRITING DOCUMENTATION ONLY: it commits no verdict artifact and does not modify .github/workflows/ci.yml, which is a PRODUCER of the unittest observations both verdicts are derived FROM rather than a consumer of either verdict line, so the unit is self-contained and adds no CI gate. The verify-workspace token implementation and its negative-case suite are NOT in this release unit; that scope belongs to the deferred typed-policy-representation entry E770139B, and the documentation must say so plainly rather than implying the detection exists. Scope: docs/ only - this task changes no declared surface and adds no assertion. THIS IS THE DOCS PHASE of the unit's PREPARE - RED - VERIFY - ACTIVATE - CONFIRM - DOCS order; attempt 02 finding G4 (P3) recorded that this record previously carried the retired T1-T6 label. Blocked by 169.016-T, the post-activation confirmation: documentation follows CONFIRMED behaviour, never asserted behaviour. Stash 3EF5AAF2. Plan docs/plans/2026-09-18-post-claim-member-status-contract-plan.md (plan_id post-claim-member-status-contract-v2, revision 7, reviewed and PASSED at independent terminal attempt 07). Verdict manifest docs/reviews/2026-09-18-post-claim-member-status-contract-v2-plan-review.md.