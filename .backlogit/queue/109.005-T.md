---
artifact_type: task
created_at: 2026-08-04T23:38:32.3701013Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-two-axis-v1
    size_source: agent
dependencies:
    - 109.002-T
id: 109.005-T
parent_id: 109-F
priority: medium
status: queued
title: 'A2: at-most-one-active-shipment check + detect-before consistency scan'
updated_at: 2026-08-04T23:38:32.3701013Z
---



<!-- BEGIN:acceptance-criteria -->
* PHASE-AWARE active-shipment invariant, evaluated against backlogit state as observed in the CURRENT SYNCHRONIZED CHECKOUT (see cross-machine limitation below), driven by an EXPLICIT phase value on the gate domain-input (never silently inferred):
  - PRE-CLAIM phase (before this checkout holds the claim; e.g. pre-claim eligibility / route-to-Ship): the workspace-global count of ACTIVE shipments MUST be ZERO. ANY active shipment (>=1) -> blocked with a specific token. A to-be-claimed target is still `queued` pre-claim, so a healthy pre-claim topology has zero active shipments; "one active is fine" is INVALID in this phase.
  - POST-CLAIM / LATER-LIFECYCLE phase (build / branch-worktree / PR / closure, after this checkout holds the claim): EXACTLY ONE active shipment is allowed AND it MUST be the resolved `target_shipment_id`. Zero active, two-or-more active, OR exactly-one-active-that-is-not-the-target -> blocked with a specific token.
* Detect-before inconsistency scan (queued/blocked record x active/done task) -> SHIPMENT_STATE_INCONSISTENT, task-artifact-filtered, runs BEFORE the phase-aware count check.
* Deterministic, testable phase x count matrix (each row a named unit case): PRE-CLAIM {0 -> pass; 1 -> blocked; 2+ -> blocked}; POST-CLAIM {0 -> blocked; exactly-1-and-target -> pass; exactly-1-not-target -> blocked; 2+ -> blocked}.
* State reader is injectable; no live backlogit call in unit tests.
* Read-only: performs NO backlogit mutation.
* CROSS-MACHINE LIMITATION: "workspace-global" means all shipment records visible in THIS synchronized checkout's backlogit state, NOT a real-time cross-machine lease (per 012-DL / 109-F). Ambiguous or unreachable shared state fails CLOSED (block), never fail-open.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
[Shipment A / core] Implement the PHASE-AWARE active-shipment topology check. Read shipment status from backlogit (CLI-preferred re-read; MCP is the flaky surface per 106-S) via an INJECTED state-reader so tests are deterministic/offline. The invariant is phase-sensitive and the phase is an EXPLICIT domain-input value, never inferred: PRE-CLAIM requires ZERO active shipments (a target still `queued` has not yet been claimed, so any active shipment is a topology violation); POST-CLAIM / later lifecycle allows EXACTLY ONE active shipment and only when it is the resolved target (`target_shipment_id`). Run the detect-before consistency scan (SHIPMENT_STATE_INCONSISTENT) BEFORE the phase-aware count check: if a queued/blocked shipment record has active/done manifest tasks, halt fail-closed. All counts are evaluated against backlogit state as observed in the current synchronized checkout (workspace-global, not cross-machine); ambiguous/unreachable state fails closed.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Reuse the 106-S SHIPMENT_STATE_INCONSISTENT semantics (docs/compound/106-S-claim-integrity-guards.md). This invariant is the mechanical single-active linearization of the shipment-blocks DAG (spike 001-SP), enforced within one synchronized checkout. The pre-claim ZERO-active vs post-claim exactly-one-same-target distinction is carried by the gate domain-input phase field (aligned with 109.001-T post-claim GLOBAL re-verify and 109.002-T domain-input). Cross-machine serialization is a documented limitation (012-DL / 109-F): degraded to detect-at-sync/CI + fail-closed-on-ambiguity, NOT a real-time lease.
<!-- END:implementation-notes -->