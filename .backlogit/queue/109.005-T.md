---
artifact_type: task
created_at: 2026-08-04T23:38:32.3701013Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-two-axis-v1
    size_source: agent
dependencies:
    - 109.002-T
id: 109.005-T
parent_id: 109-F
priority: medium
status: queued
title: 'A2: at-most-one-active-shipment check + detect-before consistency scan'
updated_at: 2026-08-04T23:38:32.3701013Z
---



<!-- BEGIN:acceptance-criteria -->
* PHASE-AWARE active-shipment invariant, evaluated against backlogit state as observed in the CURRENT SYNCHRONIZED CHECKOUT (see TOCTOU limitation below), driven by the EXPLICIT `--phase` flag carried on the gate domain-input (agent shipment-scoped mode: REQUIRED, never inferred; missing/invalid -> exit 2 via 109.002-T):
  - PRE-CLAIM phase (before this checkout holds the claim; e.g. pre-claim eligibility / route-to-Ship): the workspace-global count of ACTIVE shipments MUST be ZERO. ANY active shipment (>=1) -> blocked with a specific token. A to-be-claimed target is still `queued` pre-claim, so a healthy pre-claim topology has zero active shipments; "one active is fine" is INVALID in this phase.
  - POST-CLAIM / LATER-LIFECYCLE phase (build / branch-worktree / PR / closure, after this checkout holds the claim; carried by `--phase post_claim` OR `--phase lifecycle`, which share identical count semantics): EXACTLY ONE active shipment is allowed AND it MUST be the resolved `target_shipment_id`. Zero active, two-or-more active, OR exactly-one-active-that-is-not-the-target -> blocked with a specific token.
* Detect-before inconsistency scan (queued/blocked record x active/done task) -> SHIPMENT_STATE_INCONSISTENT, task-artifact-filtered, runs BEFORE the phase-aware count check.
* Deterministic, testable phase x count matrix (each row a named unit case): PRE-CLAIM {0 -> pass; 1 -> blocked; 2+ -> blocked}; POST-CLAIM {0 -> blocked; exactly-1-and-target -> pass; exactly-1-not-target -> blocked; 2+ -> blocked}.
* State reader is injectable; no live backlogit call in unit tests.
* Read-only: performs NO backlogit mutation.
* CROSS-MACHINE + LOCAL TOCTOU LIMITATION: "workspace-global" means all shipment records visible in THIS synchronized checkout's backlogit state; backlogit does NOT serialize claims at any scope (LOCAL same-checkout races via unlocked read/check/write + CROSS-MACHINE divergence, per 012-DL / 109-F), so this check is DETECTION-only, never a lock or lease. Ambiguous or unreachable shared state fails CLOSED (block), never fail-open.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
[Shipment A / core] Implement the PHASE-AWARE active-shipment topology check. Read shipment status from backlogit (CLI-preferred re-read; MCP is the flaky surface per 106-S) via an INJECTED state-reader so tests are deterministic/offline. The invariant is phase-sensitive and the phase is the EXPLICIT `--phase` flag value (pre_claim | post_claim | lifecycle) carried on the gate domain-input, never inferred from mode or claim state: PRE-CLAIM requires ZERO active shipments (a target still `queued` has not yet been claimed, so any active shipment is a topology violation); POST-CLAIM / later lifecycle (post_claim OR lifecycle) allows EXACTLY ONE active shipment and only when it is the resolved target (`target_shipment_id`). Run the detect-before consistency scan (SHIPMENT_STATE_INCONSISTENT) BEFORE the phase-aware count check: if a queued/blocked shipment record has active/done manifest tasks, halt fail-closed. All counts are evaluated against backlogit state as observed in the current synchronized checkout (workspace-global within this checkout; backlogit serializes nothing, so this is DETECTION-only, not a lock/lease); ambiguous/unreachable state fails closed.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Reuse the 106-S SHIPMENT_STATE_INCONSISTENT semantics (docs/compound/106-S-claim-integrity-guards.md). This invariant is the mechanical single-active linearization of the shipment-blocks DAG (spike 001-SP), realized by DETECTION (backlogit provides no serialization at any scope). The pre-claim ZERO-active vs post-claim/lifecycle exactly-one-same-target distinction is carried by the explicit `--phase` flag on the gate domain-input (aligned with 109.001-T post-claim GLOBAL re-verify and 109.002-T domain-input; agent scoped mode requires it, missing/invalid -> exit 2). No serialization exists at any scope (012-DL / 109-F): both local same-checkout and cross-machine claims are TOCTOU-exposed, degraded to detect-at-sync/CI + fail-closed-on-ambiguity, NOT a lock or lease.
<!-- END:implementation-notes -->