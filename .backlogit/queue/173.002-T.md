---
artifact_type: task
created_at: 2026-09-18T05:03:44.7717985Z
custom_fields:
    complexity: low
    size: XS
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 173.008-T
    - 173.011-T
id: 173.002-T
labels:
    - test
    - evidence-provenance
parent_id: 173-F
priority: high
status: queued
title: 'SAFE_CLOSE T2: conformance fixture - update --status shipped refusal, exit 9, exact message'
updated_at: 2026-09-19T03:17:56.6316459Z
---

HERMETIC CONFORMANCE FIXTURE 2 OF 4. Observe that `backlogit update --status shipped` REFUSES the transition: assert the exact non-zero EXIT CODE 9 and the EXACT REFUSAL MESSAGE emitted by the pinned binary. Assert the observation against the DECLARED version, so the assertion is bound to an identified binary and not to whatever build happens to be present.
SUITE LOCATION (BINDING): this fixture lives in tests_conformance/, OUTSIDE the ordinary discovery root, and runs only in the separately provisioned required conformance CI job. It must NEVER make PYTHONPATH=src python -m unittest discover -s tests fail after the approval-gated workspace binary is cleaned up. It is VERSION-AWARE and FAILS LOUDLY when the declared binary is absent or its version is undeclared - it NEVER SKIPS, because a silent skip passes green while observing nothing.

WORKSPACE ISOLATION (BINDING): the fixture operates against an EXPLICIT DISPOSABLE BACKLOG STORAGE ROOT, canonicalized, with a PRE-MUTATION ASSERTION RE-EVALUATED IMMEDIATELY BEFORE EVERY MUTATING CALL that the target is NEVER the live .backlogit/ tree. The binary is invoked by ABSOLUTE RESOLVED PATH under a SCRUBBED ENVIRONMENT, with symlink, junction, reparse-point, hardlink and out-of-root components rejected by resolved-component comparison. Cleanup is contained and pathspec-approved.

Blocked by 173.008-T (the approval-gated v1.9.0 observation baseline) and 173.011-T (the suite partition and the provisioning job). Scope: tests_conformance/. Stash 7F9CB5E9. Plan docs/plans/2026-09-17-safe-close-record-transition-disposition-plan.md (revision 8). Verdict manifest docs/reviews/2026-09-17-safe-close-record-transition-disposition-plan-review.md (latest_attempt 07, verdict REMEDIATED-PENDING-REVIEW at plan revision 8, awaiting independent attempt 08).