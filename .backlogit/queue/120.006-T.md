---
artifact_type: task
created_at: 2026-08-10T07:11:18.1805036Z
custom_fields:
    complexity: medium
    origin_feature: 117-F
    size: S
    size_ruleset_version: 2h-width-isolated-v1
    size_source: agent
dependencies:
    - 120.004-T
id: 120.006-T
labels:
    - shipment-3
    - P1
    - cli
    - adapter
    - additive
parent_id: 120-F
priority: high
references:
    - src/autoharness/cli.py
    - pyproject.toml
    - docs/plans/2026-08-09-copilot-cli-supervisor-control-plane-plan.md
status: queued
title: T17 `autoharness run` CLI adapter (parse -> call -> render -> exit, zero policy)
updated_at: 2026-08-10T08:12:23.1285355Z
---

SIZE: S. COMPLEXITY: medium. (Two independent axes; also emitted as structured backlog fields.) Shipment 3 (P1). Add a `run` top-level command to src/autoharness/cli.py and a matching console entry point, following the repository's existing hand-rolled parsing style (no argparse, no click/typer). The adapter is STRICTLY parse argv -> call supervise.app.run_session() -> render human or --json -> sys.exit(result.exit_code). NO POLICY IN THE ADAPTER: no config loading, no pass/fail definition, no verdict mutation, no audit-log authorship, no telemetry classification, and no synthesis of any outcome the service cannot produce. Exit codes are DERIVED from the single machine-readable contract table, never hardcoded at the call site. If a supervisor ToolTelemetryEvent is ever emitted it is emitted BY THE SERVICE with tool_surface supplied by this adapter - never synthesized in cli.py. ADDITIVE ONLY: the existing 10 top-level / 17 leaf command paths are unchanged, and no existing CLI test is modified. Tests cover argument parsing, human and --json rendering, and exit-code propagation for each error kind.