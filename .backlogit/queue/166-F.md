---
artifact_type: feature
created_at: 2026-09-15T22:14:58.3296406Z
custom_fields:
    harness_status: pending
id: 166-F
labels:
    - shipment-closure
    - safe-close
    - protected-set
    - p-015
    - contract-change
    - deliberation-outcome
references:
    - docs/decisions/2026-09-15-173-s-terminal-shipment-closure-deliberation.md
status: queued
title: Flat-manifest shipment closure (closure scope = manifest + shipment record)
updated_at: 2026-09-16T06:29:05.6543462Z
---

REFRAMED IN PLACE 2026-09-15 by operator architectural correction. This feature ID, its six task IDs, and shipment 174-S are RETAINED (not replaced) to preserve traceability and to avoid requiring a NEW operator dag-root authorization.

SUPERSESSION
- AUTHORITATIVE DECISION: docs/decisions/2026-09-15-flat-manifest-shipment-closure-deliberation.md
- AUTHORITATIVE REVIEWED PLAN: docs/plans/2026-09-15-flat-manifest-shipment-closure-plan.md (impl-plan body sections 1-8 + appended `## Plan Hardening` (P-006) + appended `## Plan Review` with dispatch_mode: single-agent-declared-degradation and decision: PASS). Ship MUST execute from that plan.
- SUPERSEDED DECISION (do not implement): docs/decisions/2026-09-15-173-s-terminal-shipment-closure-deliberation.md
- SUPERSEDED PLAN (do not implement): docs/plans/2026-09-15-terminal-shipment-closure-plan.md
Both superseded artifacts carry explicit in-file SUPERSEDED banners and frontmatter markers; neither was rewritten.

BINDING OPERATOR DIRECTIVE
A shipment manifest is a FLAT MANIFEST of exactly what is delivered. It is NOT a container and carries NO implicit hierarchical closure semantics. A feature MAY be decomposed across multiple shipments; the FINAL shipment for that feature carries the feature item itself as the FINAL manifest entry. Items under a feature that are excluded, removed, descoped, rejected, merged into other work, or otherwise absent from a shipment MUST NEVER block the feature record or shipment record from being marked shipped solely because of their ancestry. Closure applies ONLY to explicitly listed manifest items plus the shipment record; hierarchy remains useful for planning/traceability but MUST NOT expand closure scope.

WITHDRAWN: TERMINAL_CLOSE (prior E1) and the terminal-descope exemption (prior E2). E1 precondition 2 restated hierarchical closure; E1 precondition 3 made multi-shipment feature delivery unreachable; E2 replaced an ancestry gate with a disposition-note gate over artifacts that must not gate closure at all. NO third close verdict is added: ClosePath keeps exactly SAFE_CLOSE and CASCADE.

ROOT CAUSE: the autoharness overlay reasons about HIERARCHY MEMBERSHIP when the safety-relevant question is MUTABILITY. It is simultaneously too strict (blocks 173-S on provably inert artifacts) and too weak (relies on a returned_ids guard that does not fire on the real destructive case).

MEASURED ENGINE LAW (backlogit 1.10.1; four disposable %TEMP% spike workspaces, all destroyed; repository untouched):
- manifest HAS a feature member + out-of-manifest descendant declares status: archived -> INERT, skipped, byte-identical (hash, updated_at, archived_status, parent_id all preserved).
- manifest HAS a feature member + out-of-manifest descendant is status: done -> ARCHIVED (out-of-scope mutation).
- manifest HAS a feature member + out-of-manifest descendant is LIVE queued -> ARCHIVED, and returned_ids was EMPTY (the skill's Step 2 guard did NOT fire).
- manifest has NO feature member + live out-of-manifest sibling -> RETURNED, and its parent_id was SILENTLY CLEARED (orphaned).
- an ancestor/parent of a manifest item is never touched (no upward walk).
Therefore the descendant walk is RETAINED, but its verdict changes from "is it in the manifest?" (scope) to "can the engine mutate it?" (blast radius).

NORMATIVE INVARIANTS (INV-1..INV-10, full text in the decision): INV-1 closure scope = items(S) + {S}; INV-2 membership explicit/exhaustive; INV-3 exclusion never gates on ancestry and requires NO disposition note; INV-4 multi-shipment delivery with the feature last in the final manifest; INV-5 feature terminality over DELIVERED tasks only; INV-6 engine-inertness containment gate (out-of-manifest reachable artifacts must declare status: archived); INV-7 baseline INVARIANCE replaces baseline PRESENCE; INV-8 manifest ordering is a Stage assembly convention and NEVER a closure gate; INV-9 DAG orthogonality; INV-10 postconditions including byte-identity of everything outside closure scope.

BACKWARD COMPATIBILITY: every historical manifest lists the covering feature FIRST, not last. INV-8 makes ordering non-normative at closure, so NO historical manifest is invalidated and 173-S need not be touched. The existing classifier negative-test suite survives UNCHANGED because its _write_artifact helper writes no status field, making every existing out-of-manifest fixture non-inert under INV-6; migration is purely ADDITIVE.

173-S EXPECTED CLOSURE PATH (read-only dry-run executed 2026-09-15, NO writes): out-of-manifest descendants 165.007-T and 165.010-T both declare status: archived -> INERT -> INV-6 satisfied -> verdict CASCADE -> the engine op transitions the record to status: archived + archived_status: shipped. Safe-close Step 8's refused `move --status shipped` (2B42392E) is NEVER REACHED because safe-close is not the selected path. Predicted archived_ids = 11 manifest members + 173-S = 12; returned_ids = []; both excluded siblings absent from allowed_ids and required_ids and byte-identical.

CONSTRAINTS (non-negotiable): do NOT mutate 173-S, 165.007-T, 165.010-T, or the 160-F family. Do NOT clear parent_id. Do NOT add a P-001 overlap authority, a bootstrap grant, or any claimability beyond the EXISTING operator-authorized dag-root on 174-S. Do NOT special-case any feature/shipment ID or manifest shape.

BLAST RADIUS: classifier src/autoharness/gates/shipment_closure.py + policy .github/policies/workflow-policies.md + templates/policies/workflow-policies.md.tmpl + skill .github/skills/shipment-reconcile/SKILL.md + templates/skills/shipment-reconcile/SKILL.md.tmpl + tests. P-006 hardening performed; plan-review returned PASS (2 P0 + 3 P1 raised, all resolved in cycle 1).

TASK GRAPH: 166.002-T (red phase; no prerequisites) gates 166.001-T (classifier), 166.003-T (P-015 policy) and 166.004-T (skill Step 0(c)); 166.005-T (safe-close Steps 2/3/5/8) follows 166.003-T + 166.004-T; 166.006-T (parity, gates, read-only dry-runs) follows 166.001-T + 166.005-T.

RESIDUAL RISKS ROUTED OUT OF SCOPE: R1 — for a genuine SAFE_CLOSE shipment there is NO safe path to archived_status: shipped in 1.10.1 (move refused exit 9; cascade unsafe because it orphans returned siblings). Surfaced as a fail-closed halt (RECONCILE_FAIL_NO_SAFE_RECORD_TRANSITION), NOT worked around, and routed upstream. R2 — the engine's parent_id clearing on returned siblings is an upstream defect to report.

HANDOFF: carried by shipment 174-S. Planning gates (impl-plan, MANDATORY plan-harden, plan-review PASS) are SATISFIED against the NEW plan. The SOLE outstanding claim blocker remains OPERATOR-OWNED explicit P-001 authority for the sequencing overlap with the still-active 173-S. Stage does not grant it.