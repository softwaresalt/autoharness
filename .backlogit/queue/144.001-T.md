---
artifact_type: task
created_at: 2026-08-22T08:15:26.3194461Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 144.001-T
labels:
    - test-first
    - reproduction
    - windows
    - mechanism-a
parent_id: 144-F
priority: medium
references:
    - docs/plans/2026-08-22-git-config-env-containment-plan.md
    - docs/plans/2026-08-22-git-config-env-containment-hardening.md
    - tests/test_repo_root_artifacts.py
status: queued
title: Test-first reproduction of the ambient empty-valued-variable destruction (RED by design; halt if not reproduced)
updated_at: 2026-08-22T08:47:48.6648559Z
---

**TEST-FIRST REPRODUCTION. This task DELIVERS A RED SUITE ON WINDOWS BY DESIGN (hardening A2R).**

Plan: `docs/plans/2026-08-22-git-config-env-containment-plan.md` Task 1. Amendments **A11**, **A2R**, **R10**, R1, R3 BINDING.

## Deliverable

New module `tests/test_environ_restore_contract.py` with **four** tests.

## Process topology (A11, BINDING) — why the old wording was unusable

The previous version said "set a uniquely-named sentinel to `\"\"`" and "build a `GIT_CONFIG_*` triple **in-process** with the last `VALUE_n` empty". Both are **self-defeating on the platform they target**: `os.environ[name] = ""` IS the destructive operation under study — on Windows it reaches `SetEnvironmentVariableW(name, L"")` which DELETES the variable immediately. The sentinel would be gone BEFORE the round trip, so the test would observe "sentinel missing" for the WRONG REASON — a FALSE POSITIVE confirming a mechanism it never exercised.

A blank-valued variable can only be established by an **explicit environment block** handed to `CreateProcessW` (a `NAME=\0` entry is inherited as a genuinely empty value). Hence three levels:

```text
L0 CONTROLLER  (the unittest test method)
  sentinel  := "AUTOHARNESS_ENVTEST_EMPTY_" + uuid4().hex
  env_block := dict(os.environ) | {sentinel: ""} | {"PYTHONPATH": "src"}
  spawn L1 with env=env_block, argv carrying the sentinel name + variant

L1 RUNNER  (long-lived; the operation under test happens HERE)
  a. pre := probe()                    # L2 child, env=None
  b. PRECONDITION GATE: sentinel present in pre?
     no -> emit {"status":"INVALID_PRECONDITION"}; exit    (HALT at L0)
  c. operation under test (--variant):
       destructive : bare unittest.mock.patch.dict(os.environ, {...}) enter/exit
       fixed       : patched_environ(...) enter/exit       (from 144.002-T on)
  d. post := probe()                   # L2 child, env=None
  e. emit {"status":"OK","pre_has_sentinel":...,"post_has_sentinel":...}

L2 PROBE  (child of L1, env=None -> inherits L1's REAL block)
  python -c "import os,json,sys; sys.stdout.write(json.dumps(dict(os.environ), sort_keys=True))"
```

## R10 (BINDING) — ISOLATION

Every destructive environment operation is confined to the **L1 child**. The L0 test process MUST NEVER execute `patch.dict(os.environ, ...)`, `os.environ.clear()`, or an empty-value assignment. Otherwise this module becomes a **FOURTEENTH POLLUTING SITE**, corrupting the very suite it exists to measure — and is flagged by the `144.004-T` guard it is supposed to satisfy.

## Required tests

0. `test_blank_sentinel_survives_explicit_env_block_inheritance`
   - Runs L0 -> L1 and asserts ONLY the precondition: `status == "OK"` and `pre_has_sentinel is True`.
   - The **non-vacuity lock** for the whole module. **Expected GREEN on both platforms.**
   - Failure means the platform did not carry a blank through explicit-block inheritance: the scenario proves nothing and the design returns to Stage.

1. `test_bulk_environ_restore_preserves_empty_valued_variable_in_child_process`
   - Runs the `destructive` variant; asserts `post_has_sentinel is True`.
   - This is the DESIRED invariant, so RED on Windows today, GREEN on Linux today. It MUST NOT be platform-gated, skipped, or `expectedFailure`d.

2. `test_bulk_environ_restore_preserves_git_config_triple_for_child_git`
   - Same topology. L0 puts a COMPLETE triple into the **explicit block**: `GIT_CONFIG_COUNT=3`, `KEY_0=safe.bareRepository`/`VALUE_0=explicit`, `KEY_1=credential.interactive`/`VALUE_1=never`, `KEY_2=core.fsmonitor`/`VALUE_2=""`.
   - L1 precondition: run `git version` in L2, require exit 0. After the round trip: exit 0 again, no `missing config value` in stderr.
   - The triple is **NEVER** assembled by in-process assignment.
   - Skip ONLY if `shutil.which("git")` is None (L0), matching `tests/test_repo_root_artifacts.py:23`.

3. `test_sentinel_variables_are_removed_from_the_process_environment`
   - `tearDown`-verified: every sentinel this module introduces is gone from `os.environ` AND from a freshly spawned child's environment when the module finishes.
   - Because all seeding happens in L0's EXPLICIT BLOCK rather than in `os.environ`, this should hold trivially — which is exactly what it locks in. **Expected GREEN on both platforms.**

## Canonical invocation (R3 — `pytest` MUST NOT be substituted)

```powershell
$env:PYTHONPATH = 'src'; python -m unittest discover -s tests
```

## R1 — resolve the victim #1 ambiguity

`test_backlog_only_workspace_succeeds` exists in BOTH `tests/test_gate_dag_readiness_cli.py:182` and `tests/test_gate_pipeline_topology_cli.py:226`. The canonical signature is exactly FIVE failures with five distinct names, so exactly one is the victim. From the verbatim baseline output (which carries the module path), RECORD which module holds the failing one and pin it into the A2R enumeration below.

## A2R — expected failure-set equality gate (NOT `failures == 0`)

```
EXPECTED RED after this task, Windows only:
  test_environ_restore_contract :: test_bulk_environ_restore_preserves_empty_valued_variable_in_child_process
  test_environ_restore_contract :: test_bulk_environ_restore_preserves_git_config_triple_for_child_git
  + the five pre-existing canonical victims (unchanged):
      test_backlog_only_workspace_succeeds                      (module pinned by R1)
      test_empty_queue_and_archive_dirs_pass_as_zero_shipments  (test_gates_topology.py:112)
      test_root_tracked_json_matches_allowlist                  (test_repo_root_artifacts.py:23)
      test_git_check_ignore_matches_metrics_artifacts           (test_telemetry_gitignore_template.py:33)
      test_emitted_metrics_artifacts_are_never_tracked          (test_telemetry_gitignore_template.py:74)
EXPECTED GREEN on both platforms:
  test_environ_restore_contract :: test_blank_sentinel_survives_explicit_env_block_inheritance
  test_environ_restore_contract :: test_sentinel_variables_are_removed_from_the_process_environment
EXPECTED RED on Linux/CI: none
```

The gate is **failure-set EQUALITY against this enumeration**. Any failure outside the set, any missing expected failure, or any red on Linux is a HALT. The expected-red window is exactly one task wide (`144.002-T` closes it) and MUST NOT be closed by `skipTest`, `expectedFailure`, deletion, or assertion weakening.

**Why the precondition is a SEPARATE GREEN test, not a branch inside a RED one:** if it were folded in, `INVALID_PRECONDITION` would be indistinguishable from a successful reproduction and the equality gate would wave it through as "expected red". As its own expected-GREEN test, an invalid precondition is a green-to-red flip OUTSIDE the expected-red set -> automatic HALT, no special-casing.

## HALT CONDITIONS (MANDATORY)

1. If test 1 does NOT fail on Windows at the current head, the mechanism is UNCONFIRMED. STOP, record, return the shipment blocked. Do NOT proceed to `144.002-T`.
2. If test 0 fails (`INVALID_PRECONDITION`), the measurement apparatus is invalid. STOP and return to Stage. This is distinguishable from both pass and fail.

## Acceptance

- RED on Windows for tests 1 and 2; GREEN on Linux; tests 0 and 3 GREEN on both.
- Verbatim failure output recorded on the task.
- R1 module pinned.
- R10 isolation verified: no destructive env operation in the L0 test process.
- No `tests/__init__.py`, no `tests/conftest.py`.