---
artifact_type: task
created_at: 2026-08-22T08:15:26.3194461Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 144.001-T
labels:
    - test-first
    - reproduction
    - windows
    - mechanism-a
parent_id: 144-F
priority: medium
references:
    - docs/plans/2026-08-22-git-config-env-containment-plan.md
    - docs/plans/2026-08-22-git-config-env-containment-hardening.md
    - tests/test_repo_root_artifacts.py
status: queued
title: Test-first reproduction of the ambient empty-valued-variable destruction (RED by design; halt if not reproduced)
updated_at: 2026-08-22T08:15:26.3194461Z
---

**TEST-FIRST REPRODUCTION. This task DELIVERS A RED SUITE ON WINDOWS BY DESIGN (hardening A2).**

Plan: `docs/plans/2026-08-22-git-config-env-containment-plan.md` Task 1. Amendments A2, R1, R3, R6 BINDING.

## Deliverable

New module `tests/test_environ_restore_contract.py` with three tests.

1. `test_bulk_environ_restore_preserves_empty_valued_variable_in_child_process`
   - Set a uniquely-named sentinel (e.g. `AUTOHARNESS_ENVTEST_EMPTY_<uuid4hex>`) to `""`.
   - Enter and exit a bare `unittest.mock.patch.dict(os.environ, {"AUTOHARNESS_ENVTEST_OTHER": "x"})` block.
   - Spawn `subprocess.run([sys.executable, "-c", "import os,sys; sys.exit(0 if '<name>' in os.environ else 1)"])` with `env=None`.
   - Assert the child still sees the sentinel.
   - This asserts the DESIRED invariant, so it is RED on Windows today and GREEN on Linux today. It MUST NOT be platform-gated, skipped, or `expectedFailure`d.

2. `test_bulk_environ_restore_preserves_git_config_triple_for_child_git`
   - Build a self-consistent three-pair `GIT_CONFIG_*` triple in-process with the last `VALUE_n` empty.
   - Exercise the same bare `patch.dict` round-trip.
   - Run `git version` (or `git config --get-regexp`) in a child; assert exit 0 and no `missing config value` in stderr.
   - Skip ONLY if `shutil.which("git")` is None, matching the precedent at `tests/test_repo_root_artifacts.py:23`.

3. `test_sentinel_variables_are_removed_from_the_process_environment`
   - `tearDown`-verified: every sentinel this module introduces is gone from `os.environ` AND from a freshly spawned child's environment when the module finishes.
   - **Expected GREEN on both platforms (R6).** Its failure is a HALT, not an expected red.

## Canonical invocation (R3 — `pytest` MUST NOT be substituted)

```powershell
$env:PYTHONPATH = 'src'; python -m unittest discover -s tests
```

## R1 — resolve the victim #1 ambiguity

`test_backlog_only_workspace_succeeds` exists in BOTH `tests/test_gate_dag_readiness_cli.py:182` and `tests/test_gate_pipeline_topology_cli.py:226`. The canonical signature is exactly FIVE failures with five distinct names, so exactly one is the victim. From the verbatim baseline output (which carries the module path), RECORD which module holds the failing one and pin it into the A2 enumeration below.

## A2 — expected failure-set equality gate (NOT `failures == 0`)

```
EXPECTED RED after this task, Windows only:
  test_environ_restore_contract :: test_bulk_environ_restore_preserves_empty_valued_variable_in_child_process
  test_environ_restore_contract :: test_bulk_environ_restore_preserves_git_config_triple_for_child_git
  + the five pre-existing canonical victims (unchanged):
      test_backlog_only_workspace_succeeds                      (module pinned by R1)
      test_empty_queue_and_archive_dirs_pass_as_zero_shipments  (test_gates_topology.py:112)
      test_root_tracked_json_matches_allowlist                  (test_repo_root_artifacts.py:23)
      test_git_check_ignore_matches_metrics_artifacts           (test_telemetry_gitignore_template.py:33)
      test_emitted_metrics_artifacts_are_never_tracked          (test_telemetry_gitignore_template.py:74)
EXPECTED GREEN on both platforms:
  test_environ_restore_contract :: test_sentinel_variables_are_removed_from_the_process_environment
EXPECTED RED on Linux/CI: none
```

The gate for this task is **failure-set EQUALITY against this enumeration**. Any failure outside the set, any missing expected failure, or any red on Linux is a HALT. The expected-red window is exactly one task wide (`144.002-T` closes it) and MUST NOT be closed by `skipTest`, `expectedFailure`, deletion, or assertion weakening.

## HALT CONDITION (MANDATORY)

If test 1 does NOT fail on Windows at the current head, the deliberation's mechanism is UNCONFIRMED. STOP. Record the observation on this task and return the shipment blocked. Do NOT proceed to `144.002-T` on an unconfirmed hypothesis.

## Acceptance

- RED on Windows for tests 1 and 2; GREEN on Linux; test 3 GREEN on both.
- Verbatim failure output recorded on the task.
- R1 module pinned.
- No `tests/__init__.py`, no `tests/conftest.py`.