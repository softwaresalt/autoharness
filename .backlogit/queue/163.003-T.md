---
artifact_type: task
created_at: 2026-09-08T07:42:31.5455547Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 033-DL
id: 163.003-T
labels:
    - test
    - red
    - guard
    - dogfood-parity
    - structural
parent_id: 163-F
priority: high
status: queued
title: 'U1c RED: section-scoped contract guard and dogfood-parity guard'
updated_at: 2026-09-10T23:52:21.9362067Z
---

Guard module over BOTH .github/skills/shipment-reconcile/SKILL.md and templates/skills/shipment-reconcile/SKILL.md.tmpl. AC: (1) SECTION-SCOPED CONTRACT PRESENCE - the emission obligation, both halt tokens, the explicit-empty rule and the anti-reconstruction clause appear WITHIN the resolved Step 0(c) linked-deliberation extension block / Cascade Close Sub-Procedure block, located by heading/anchor and then asserted within that slice; (2) SINGLE-STATEMENT RULE (plan D-5) - the obligation is stated ONCE and the Cascade Sub-Procedure REFERENCES it rather than restating it; a second independent restatement FAILS the guard (two independent restatements drift - that is literally how 15A02E21 was born); (3) DOGFOOD PARITY - the two copies agree on the asserted region; (4) CONSTRUCTION CONSTRAINT (per docs/compound/2026-08-21-ast-based-structural-regression-guards-beat-line-regex.md) - assertions MUST be section-scoped structural checks, NEVER a bare file-wide regex, which passes as soon as the required phrase appears anywhere in an 1082-line document and cannot express the single-statement rule at all; (5) RED only. Plan: docs/plans/2026-09-08-shipment-reconcile-step-0c-live-pre-mutation-evidence-plan.md U1c.

--- REVISION R1 (2026-09-10, gate 033-DL, stash DDBF283E/EB23D1B9; plan section "Revision R1") --- R1 GOVERNS where it conflicts with the text above. Same SECTION-SCOPED technique, never a file-wide regex. Within the resolved Step 0(c) extension slice, the guard MUST additionally assert, over BOTH .github/skills/shipment-reconcile/SKILL.md and templates/skills/shipment-reconcile/SKILL.md.tmpl: (1) the ANCHOR EMISSION OBLIGATION (append a PRECASCADE_EVIDENCE_ANCHOR event carrying the evidence record's sha256 to .backlogit/logs/{shipment_id}.jsonl via backlogit's append operation, inside the existing lock, before the cascade invocation); (2) ALL FOUR token names present - RECONCILE_FAIL_PRECASCADE_EVIDENCE_MISSING, RECONCILE_FAIL_PRECASCADE_EVIDENCE_STALE, RECONCILE_FAIL_PRECASCADE_ANCHOR_MISSING, RECONCILE_FAIL_PRECASCADE_ANCHOR_NOT_PRE_MUTATION; (3) the COLLECTION_COMPLETED_AT DEMOTION SENTENCE stating it is corroborating metadata and NOT the ordering proof - this guard exists specifically so a future editor cannot silently re-promote it to load-bearing; (4) the STRENGTHENED ANTI-RECONSTRUCTION CLAUSE naming the engine-written anchor's append position as the enforcing mechanism. The SINGLE-STATEMENT RULE (plan D-5) EXTENDS to the anchor obligation: stated ONCE in the Step 0(c) extension, REFERENCED from the Cascade Close Sub-Procedure; a second independent restatement fails the guard. Dogfood-parity assertion covers the extended region.

--- AMENDMENT R1-11 (2026-09-10, review-fix cycle 1; plan section "R1-11", decision section "D-7") --- [PREMISE AND L2 WORDING SUPERSEDED IN PART BY AMENDMENTS R1-12 AND R1-13 BELOW, WHICH GOVERN; the force-add phrasing and the gitignore justification below are RETAINED AS HISTORY, NOT AS AN INSTRUCTION.] DURABILITY SPLIT, ADDITIVE TO R1. The guard MUST additionally assert, within the same section-scoped slice and in both copies, the L1/L2 DURABILITY-SPLIT SENTENCE: (a) that the ENGINE LOG (.backlogit/logs/{shipment_id}.jsonl) is the SOLE GATE INPUT - all four tokens read L1 and only L1; and (b) that the L2 CLOSURE OBLIGATION (commit the evidence record AND that shipment's own engine log, force-added past the workspace .gitignore rule, verbatim and byte-unmodified, at the closure commit) is EXPLICITLY NOT A GATE, NOT A HALT TOKEN, and NOT A FALLBACK. WHY THIS ASSERTION EXISTS: .backlogit/logs/ is gitignored, so the anchor is durable LOCALLY but invisible to a fresh-clone reviewer; the guard prevents a future editor from either (i) deleting the L2 obligation and silently re-narrowing the evidence to the local workspace, or (ii) promoting L2 into a fifth halt token, which would gate the close on an artifact that cannot exist until after the close. The SINGLE-STATEMENT RULE (plan D-5) EXTENDS to the L2 obligation: stated ONCE, referenced from the Cascade Close Sub-Procedure; a second independent restatement fails the guard. Token count remains FOUR - an assertion that finds a fifth PRECASCADE halt token MUST FAIL. Still RED only; no new file, no Python source change.

--- AMENDMENT R1-12 (2026-09-10, review-fix cycle 2; plan sections "R1-6" and "R1-12", redesign decision D-7/D-8) --- ADDITIVE TO R1 AND R1-11; SAME SECTION-SCOPED TECHNIQUE, NEVER A FILE-WIDE REGEX; STILL RED ONLY. (1) CORRECTED PREMISE FOR THE R1-11 ASSERTION ABOVE: the R1-11 block justifies its L1/L2 assertion with "the anchor is durable LOCALLY but invisible to a fresh-clone reviewer because .backlogit/logs/ is gitignored". That justification is FACTUALLY WRONG in its categorical form and is CORRECTED here - origin/main's .gitignore contains NO .backlogit/logs/ rule; git ls-files lists 993 of 1016 local logs as ALREADY TRACKED, including 150-S, 159-S, 169-S and 171-S itself; the .backlogit/logs/ ignore rule exists ONLY as an uncommitted working-tree edit; and a .gitignore rule NEVER hides an already-tracked path. The ASSERTION ITSELF STANDS - the guard must still assert the L1/L2 split sentence and the non-gating labelling of L2 - but the contract text it asserts against now expresses a CONDITIONAL publication rule (tracking-state Cases A/B/C per plan R1-12), not an unconditional force-add. The guard MUST NOT assert any wording that makes the obligation conditional on a .gitignore rule. (2) NEW ASSERTION - THE U6 PUBLICATION STEP: within the same section-scoped slice and in BOTH copies, assert that the Cascade Close Sub-Procedure closure section carries the POST-CASCADE PUBLICATION step (163.008-T / plan U6) and that it names the reportable condition RECONCILE_CLOSURE_INCOMPLETE_L2_EVIDENCE as a P-001 INCOMPLETE-CLOSURE REPORT, explicitly NOT a halt token and NOT a gate input. (3) THE NO-FIFTH-TOKEN ASSERTION IS UNCHANGED AND STILL MUST FIRE: RECONCILE_CLOSURE_INCOMPLETE_L2_EVIDENCE is deliberately named OUTSIDE the RECONCILE_FAIL_PRECASCADE_ namespace, so the assertion that FAILS on a fifth PRECASCADE halt token keeps its exact meaning and is not weakened by U6's existence. Assert both: exactly four RECONCILE_FAIL_PRECASCADE_ tokens, AND the U6 condition present under a different prefix. (4) The SINGLE-STATEMENT RULE (plan D-5) EXTENDS to the U6 step: stated ONCE in the Step 0(c) extension / closure section pairing, REFERENCED from elsewhere; a second independent restatement fails the guard. (5) SIZING RE-CONFIRMED AND CHANGED: size S -> M (redesign decision D-5 sizing table). S was correct for the R1 assertion set; it is no longer, because the guard now additionally carries the R1-11 L1/L2 split sentence, the negative no-fifth-PRECASCADE-token assertion, and this R1-12 U6 assertion - three additional section-scoped assertion groups across two files. Complexity stays medium: the technique is unchanged, only the asserted surface grew. Still within the 2-hour rule on both axes.

--- AMENDMENT R1-13 (2026-09-10, review-fix cycle 3; plan section "R1-13", redesign decision D-7/D-8) --- ASSERTED CONTRACT WORDING CORRECTED. ADDITIVE TO R1, R1-11 AND R1-12; SAME SECTION-SCOPED TECHNIQUE, NEVER A FILE-WIDE REGEX; STILL RED ONLY; NO SIZE CHANGE (stays M / medium).
(1) THE GUARD MUST NOT ASSERT A BYTE-IDENTICAL FIDELITY CLAIM. R1-12 criterion (ii) required the committed log blob to be BYTE-IDENTICAL to the engine log the gate read; that is UNSATISFIABLE, because the engine appends the cascade's own shipment_status_changed / commit_tracked / archived events to the SAME log after L1's read and before the closure commit. A guard that asserted the byte-identical wording would pin an unreachable criterion into the contract text. ASSERT INSTEAD that the L2 / U6 contract text states the APPEND-ONLY PREFIX RULE: the anchor line and all pre-cascade bytes L1 evaluated appear BYTE-UNMODIFIED and in UNCHANGED ORDER as the PREFIX (or a provably unchanged ordered leading segment) of the committed log; engine-written post-gate mutation lines are EXPECTED APPEND-ONLY EXTENSIONS; and any modification, deletion, truncation or reordering of pre-existing bytes is a FAILURE. ASSERT ALSO that the separate ordering criterion - the mutation lines FOLLOW the anchor in the COMMITTED bytes - is still stated SEPARATELY and NOT merged into the fidelity criterion (the B57F9E24 never-merge rule).
(2) ASSERT THE U6 REMEDIATION CLAUSE. Within the same section-scoped slice and in BOTH copies, assert that the closure section states what happens when L2 evidence is missing: closure REMAINS INCOMPLETE; it is remediated by a FOLLOW-UP CLOSURE COMMIT containing the exact engine log BEFORE P-001 release-unit completion; published history is NEVER amended, rewritten or force-pushed to insert the evidence; and closure is NEVER marked done while the evidence is missing.
(3) ASSERT THE TWO-ROLE DISTINCTION, OR AT MINIMUM DO NOT ASSERT WORDING THAT CONFLATES THEM. The installed text's "never Stage" clause attaches to the RUNTIME CLOSING ACTOR (Role 2), not to whoever implements the step in the build pipeline (Role 1). The guard MUST NOT require wording that reads as a prohibition on implementing 163.008-T.
(4) UNCHANGED AND STILL REQUIRED: every R1, R1-11 and R1-12 assertion above - the anchor emission obligation, all four token names, the collection_completed_at demotion sentence, the strengthened anti-reconstruction clause, the L1/L2 split sentence with L2 labelled non-gating, the U6 publication step with its non-PRECASCADE condition name, the NEGATIVE no-fifth-PRECASCADE-token assertion, the plan D-5 single-statement rule, and dogfood parity across the extended region.