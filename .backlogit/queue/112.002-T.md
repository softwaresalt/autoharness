---
artifact_type: task
created_at: 2026-08-05T23:50:58.3991495Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 1.0.0
    size_source: agent
dependencies:
    - 112.001-T
    - 112.004-T
id: 112.002-T
parent_id: 112-F
priority: medium
status: queued
title: 'B3: regression guard for no-auto-mutation default + verify/assertion-anchor + tests'
updated_at: 2026-08-06T04:50:00Z
---



<!-- BEGIN:acceptance-criteria -->
1. Explicit assertion/quality-criteria that pre/post/safe-close remain STRICTLY no-auto-mutation (unchanged by this feature). 2. verify_workspace / assertion-anchor coverage for the new mode's presence + the isolation guarantee. 3. Tests: repair PROCEEDS on the clean silently-dropped-claim signature — INCLUDING a MIXED-ROLE target whose manifest tasks span a live-`active` queue record, a live-`queued` queue record, AND an archived-COMPLETED record in EITHER valid archive representation ((a) a `status: done` archive record with NO provenance required, OR (b) `status: archived` + `archived_status: done` with valid `archived_from` provenance — NOT a live `status: done` in the QUEUE), which MUST proceed: this is the explicit regression guard against the unreachable single-`expected_status` all-`matched` gate (per-task ALLOWED-STATE role classification — each manifest task a UNIQUE, NON-CONFLICTING record for its role {live-queued | live-active | archived-completed(done)}; publication-review P2 2026-08-06 / 013-DL Addendum D + publication-review P1 2026-08-06 / 013-DL Addendum E); REFUSES on any PER-ITEM anomaly — a DUPLICATE record (same id in BOTH queue and archive); a CONFLICTING record (queue status disagreeing with role, an archive record alongside a live queue record, or a live `status: done` in the QUEUE — a completed task lives in `archive/` as either `status: done` or `status: archived`+`archived_status: done`, never live-done in `queue/`); a MISSING record (manifest id with no queue or archive file); MALFORMED/ABSENT archive PROVENANCE on a `status: archived` record (`archived_status: done`/`archived_from` missing or ill-formed; a `status: done` archive record needs no provenance) or ANY OTHER archived status/provenance shape; an `orphan`; malformed/out-of-role task status; or torn-partial; classifies a legacy/malformed `blocked` shipment status as MALFORMED LEGACY DATA and HALTS for OPERATOR manual remediation (no blocked->queued transition attempted — H3 malformed-status hard-exclusion; backlogit 1.8.0 has no blocked shipment status); REFUSES when another shipment is active; REFUSES when the target is NOT the sole queued-with-active-work inconsistency (a NON-target queued shipment also has active/done tasks — target-aware inspection fail-closed, H7); CONCURRENT-CLAIM / POST-CONDITION-FAILURE test (H8, second re-review 2026-08-05): a concurrent claim activates ANOTHER shipment in the window BETWEEN the read-only inspection and the ClaimShipment re-claim; the GLOBAL post-condition DETECTS the resulting two-active state and FAILS CLOSED — emits audit+telemetry (post-claim two-active), HALTS, surfaces operator remediation, and attempts NO active->queued rollback of the target (a repeat/racing claim on an already-active target is itself REJECTED single-shot with ErrShipmentConflict — the double-claim guard, never a silent re-activation); audits on refuse/degraded. 4. No existing shipment-reconcile behavior regressed.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Add explicit regression guards + verify/assertion-anchor wiring so the harness validates BOTH the new repair mode AND the preserved no-auto-mutation default on pre/post/safe-close paths.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on B1, B2. Source: hardening H1/H5, review 112.001-R P1-1 + publication-review P2 2026-08-06 (per-task allowed-state torn refusal; mixed-status proceed regression guard) + publication-review P1 2026-08-06 (archived-completed accepted in EITHER representation — `status: done` OR `status: archived`+`archived_status: done`+`archived_from`; per-task role predicate fails closed on duplicate/conflicting/missing/malformed-provenance/any-other-archived-status; mixed-ROLE proceed regression guard; 013-DL Addendum F).
<!-- END:implementation-notes -->