---
artifact_type: task
created_at: 2026-08-17T23:33:57.327005Z
custom_fields:
    complexity: low
    size: XS
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 129.002-T
labels:
    - commit-a
    - tests
    - resolver
    - BED0DDED
parent_id: 129-F
priority: medium
references:
    - docs/plans/2026-08-17-backlogit-self-migration-plan.md
    - docs/reviews/2026-08-17-backlogit-self-migration-review.md
status: queued
title: Route live-root test binding in test_gates_sizing.py through resolve_backlog_root
updated_at: 2026-08-17T23:33:57.327005Z
---

COMMIT A (root-agnostic prep). tests/test_gates_sizing.py line 72 binds to the LIVE repository root via a hardcoded literal: header_def = yaml.safe_load((_REPO_ROOT / ".backlogit" / "header-def.yaml").read_text()). A repo-wide scan confirms this is the ONLY such live-root binding across tests/, src/autoharness/, src/autoharness/gates/ and scripts/. Post-migration it raises FileNotFoundError, and because the PR's changed files (.gitignore, .engram/registry.yaml, tests/**) make the fail-closed changes filter report code == 'true', the unittest gate WILL run and WILL fail, blocking merge immediately after the irreversible step. Replace with: from autoharness.backlog_root import resolve_backlog_root; header_def = yaml.safe_load((resolve_backlog_root(_REPO_ROOT) / "header-def.yaml").read_text()) -- the same resolver-routing fix 126-F applied to topology.py FilesystemTopologyReaders.backlog_dir. Correct before AND after the rename, so it belongs in Commit A. ACCEPTANCE: python -m unittest tests.test_gates_sizing -v passes on the current .backlogit root; repo-wide scan for _REPO_ROOT / ".backlogit" returns zero hits. Plan T002; hardening H14; review finding F6.