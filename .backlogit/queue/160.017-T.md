---
artifact_type: task
created_at: 2026-09-03T10:14:13.1957024Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.015-T
id: 160.017-T
labels:
    - packaging
    - tests
    - verification
    - ledger
parent_id: 160-F
priority: medium
status: queued
title: 'Transition ledger: assert every test case transitioned exactly as its declared class requires'
updated_at: 2026-09-03T10:14:13.1957024Z
---

TRANSITION LEDGER (T16). NEW in review-fix cycle 2 (finding 1). This is the task that makes the two-class test contract MACHINE-ENFORCED rather than a labelling convention.

WHY THIS TASK EXISTS: the plan classifies every composition/verification case as either RED-FIRST or CHARACTERIZATION, and each class implies a REQUIRED PAIR OF OBSERVATIONS. Without a terminal task that checks the pairs actually occurred, the classification is unverified metadata - an agent could declare a case CHARACTERIZATION to excuse it from ever being red, or declare it RED-FIRST and never record the green. This task closes that loop and is the only task in the shipment that observes POST-CHANGE transitions.

DELIVERABLE: re-run the COMPLETE suite against the BUILT TRIMMED ARTIFACTS - the built wheel, the built sdist, AND the resolved plugin payload - and produce a durable ledger asserting, per case:
- RED-FIRST cases: a recorded RED observation from the authoring task AND a GREEN observation now. Assert the transition red -> green.
- CHARACTERIZATION cases: a recorded GREEN-ON-BASELINE observation from the authoring task AND a GREEN observation now. Assert the preservation green -> green.

FAIL THE SHIPMENT on any of the following - each is a distinct failure mode and each must be reported by name, never collapsed into a generic failure:
- a case with NO DECLARED CLASS (classification is mandatory and exhaustive);
- a case declaring BOTH classes (the classes are mutually exclusive);
- a declared class with NO RECORDED FIRST OBSERVATION (the authoring task did not do its job);
- a RED-FIRST case with no recorded red (it was green on arrival - it never exercised the not-yet-built behaviour);
- a CHARACTERIZATION case with no recorded green-on-baseline (the baseline claim was never verified);
- ANY case that is red now.
A DECLARED CLASS WITH NO RECORDED TRANSITION IS A FAILURE, NOT A PASS. Absence of evidence must never be read as evidence of success - that is the fail-open shape this shipment exists to remove.

VERIFICATION V6 (plan section Reinforced verification): this ledger IS V6. It is what stops the two-class contract from degrading into unverified labelling.

THIS TASK OBSERVES; IT DOES NOT FIX. If a case fails here, the fix belongs to the task that owns the surface, never to this task (SHIP-10 plan H2, freeze-scope closing rule). This task must not edit the manifest, the schemas, build_support/, pyproject.toml, marketplace.json, plugin-payload/, or release.yml to make a case pass. It may not weaken, skip, xfail, or delete a failing case.

BLOCKED BY 160.015-T, and transitively by every wiring task - the trimmed artifacts must exist before their transitions can be observed.
BLOCKS 160.011-T (docs + CHANGELOG): documentation of the payload boundary must not be written until the boundary is verified.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. Writes are confined to tests/ and the durable ledger evidence artifact. src/autoharness/ is read-only (invariants I1/I2). Builds and scratch installs run under gitignored temporary paths and must not mutate the developer's real workspace, ~/.autoharness/, installed interpreter, or Copilot CLI plugin registry. NO-PUBLISH: no twine upload, no gh release create, no copilot plugin publish, no dispatch of release.yml's publish path. Publication is Ship/Orchestrator scope (CP2). Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

ACCEPTANCE: complete suite re-run against built wheel, built sdist, and resolved plugin payload; ledger produced and recorded durably; every case's declared class present and its required observation pair verified; every one of the six named failure modes above covered by an assertion; zero unresolved transitions.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T16)