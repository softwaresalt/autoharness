---
artifact_type: task
created_at: 2026-09-03T08:25:32.2776242Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 160.008-T
labels:
    - tests
    - e2e
    - install
    - upgrade
parent_id: 160-F
priority: medium
status: queued
title: Install, upgrade, and verify-workspace parity e2e tests
updated_at: 2026-09-03T08:25:32.2776242Z
---

End-to-end install, upgrade, and verification parity against the TRIMMED built artifact.

Tests run against the built artifact, never the source tree (verification V1, risk R1).

TESTS:
- test_install_emits_only_generated_output (AC5) — /install-harness into a scratch workspace produces NO autoharness engine file copied verbatim. Assert the scratch workspace contains no engine .backlogit records, no engine docs/compound entries, no engine docs/decisions or docs/plans content. This enforces the existing install-harness contract: "The target workspace never contains autoharness engine files — only the output artifacts it needs to function."
- test_skill_docs_refs_resolve_to_workspace_not_payload (AC5, risk R2) — assert skill references to docs/compound, docs/plans, docs/memory, docs/decisions, docs/closure resolve against the TARGET WORKSPACE path, not the packaged data dir. Closes the false positive where a naive dependency scan reads these 77 references as required payload.
- test_verify_workspace_parity_trimmed_vs_baseline (AC6) — no new findings vs the captured untrimmed baseline.
- test_upgrade_from_1_5_0_leaves_no_orphans (AC6, risk R3, verification V3) — install v1.5.0 untrimmed into a scratch workspace, upgrade to the trimmed build, enumerate residual engine files, assert the set is empty or explicitly expected.
- test_data_dir_resolution_all_install_methods (AC6, risk R4) — cli.py _DATA_DIR resolution including the clone/editable fallback to repo root resolves templates/ and schemas/ under pip, clone, and plugin installs.
- Assert `autoharness home` and `autoharness version` behave identically pre/post change.
- Assert /install-harness output is byte-identical to the baseline (invariant I3).

ACCEPTANCE: AC5, AC6; invariants I1, I2, I3.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T8)