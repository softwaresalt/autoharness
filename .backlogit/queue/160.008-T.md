---
artifact_type: task
created_at: 2026-09-03T08:25:32.2776242Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.006-T
    - 160.007-T
id: 160.008-T
labels:
    - tests
    - e2e
    - install
    - upgrade
parent_id: 160-F
priority: medium
status: queued
title: Install-time payload boundary e2e tests (AC5, I3)
updated_at: 2026-09-03T09:37:12.4309712Z
---

INSTALL-TIME PAYLOAD BOUNDARY verification against the TRIMMED built artifact. Narrowed in Orchestrator review-fix cycle 1 (finding 7): this task previously combined install boundary, upgrade/orphan, and resolver failure domains in one unit, exceeding the 2-hour rule. Upgrade/parity moved to 160.012-T; channel resolver contracts moved to 160.013-T.

SINGLE FAILURE DOMAIN: does an /install-harness run against a scratch workspace leak engine payload into that workspace? Nothing else.

Tests run against the BUILT ARTIFACT, never the source tree (verification V1, risk R1).

TESTS:
- test_install_emits_only_generated_output (AC5) - /install-harness into a scratch workspace produces NO autoharness engine file copied verbatim. Assert the scratch workspace contains no engine .backlogit records, no engine docs/compound entries, and no engine docs/decisions or docs/plans content. This enforces the existing install-harness contract: "The target workspace never contains autoharness engine files - only the output artifacts it needs to function."
- test_skill_docs_refs_resolve_to_workspace_not_payload (AC5, risk R2) - assert skill references to the target-workspace docs prefixes resolve against the TARGET WORKSPACE path, not the packaged data dir. This test MUST obtain the prefix set by calling `build_support.payload.classify_target_workspace_path` (160.004-T); it must NOT re-list the prefixes locally. Closes the false positive where a naive dependency scan reads these 77 references as required payload.
- Assert /install-harness output is BYTE-IDENTICAL to the untrimmed baseline captured by 160.001-T (invariant I3). Byte-identity is the criterion; "equivalent" or "no meaningful difference" is not.

SCOPE BOUNDARY: upgrade-path and verify-workspace parity are 160.012-T. `_DATA_DIR` / plugin-root resolution contracts are 160.013-T. Cross-environment registration is 160.009-T. Gate 4 cross-references and version resolution are 160.010-T. Do not absorb any of them back into this task.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. Verification-only. Scratch workspaces are created under a gitignored temporary path; the test MUST NOT install into, mutate, or read secrets from the developer's real workspace or `~/.autoharness/`, and MUST NOT delete any tracked file (destructive-operation checkpoint). `src/autoharness/` is read-only. NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

ACCEPTANCE: AC5; invariant I3. GREEN RESPONSIBILITY: this task owns green for its own three assertions only.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T9)