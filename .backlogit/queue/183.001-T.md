---
artifact_type: task
created_at: 2026-09-20T21:41:29.5813378Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 183.001-T
labels:
    - bug
    - test-only
    - toolchain-unblocker
    - graphtor
    - pr457
parent_id: 183-F
priority: medium
status: queued
title: Replace fixed 300ms sleep with deterministic child-stdin-close synchronization in test_child_stdin_write_error_fails_requests_without_crashing
updated_at: 2026-09-20T21:41:29.5813378Z
---

SCOPE (test-only, single file): in tests/test_graphtor_mcp_shim.py, replace the unsynchronized fixed `time.sleep(0.3)` in test_child_stdin_write_error_fails_requests_without_crashing with a deterministic readiness synchronization that waits until the fake child ('close-stdin-only' mode) has actually closed its stdin read end before the first _write, then keeps the existing two assertions on the synthesized JSON-RPC errors for id 1 and id 2. Acceptable mechanisms: have the fake child emit an explicit readiness line on stdout after closing stdin and block on that line (bounded timeout), or retry/poll the write until the stdin stream raises, bounded by the existing 10s collector budget. Preserve the test's original intent (isolating the child.stdin stream 'error' path, distinct from the ChildProcess 'close' path). PROHIBITED: any change to scripts/graphtor-mcp-shim.cjs (it already installs child.stdin.on('error') -> handleChildTermination at lines 244-257 and is unchanged since merge-base); weakening/deleting/skipping the assertions; adding sleeps as the synchronization mechanism; touching any other test or source file. If execution evidence disproves the test-only diagnosis (e.g. a correctly synchronized test still observes no synthesized error), STOP and return the finding to Stage rather than editing production code under this task. VERIFICATION ORDER (mandatory, in order): (1) targeted test `python -m unittest tests.test_graphtor_mcp_shim.GraphtorMcpShimHandshakeTests.test_child_stdin_write_error_fails_requests_without_crashing`; (2) bounded repeat of that same targeted test under Windows/Node (fixed small repeat count, e.g. 10 consecutive runs, all green) to prove the race is closed rather than re-won; (3) canonical suite `PYTHONPATH=src python -m unittest discover -s tests`; (4) ordinary pre-push hook (no bypass, no --no-verify). CIRCUIT BREAKER: the universal breaker 'graphtor-mcp-shim-child-stdin-write-error-no-response' (docs/memory/2026-09-20/circuit-break-graphtor-mcp-shim-stdin-write-error-test.md, 3 attempts) is reopened by explicit operator authorization ONLY AFTER test code changes are made; do not re-run the failing operation before editing the test. TRACEABILITY: decision docs/decisions/2026-09-20-graphtor-shim-test-synchronization-defect-family-deliberation.md; stash 24A85BF8 (survivor) + AD3D41FA (merged duplicate); unblocks PR #457 Push B (local commit eabcecc8, unpushed). Size S / complexity medium (2h-rule-v1) - one test function, bounded, but timing-sensitive on Windows/Node.