---
artifact_type: task
created_at: 2026-09-12T22:51:26.4326628Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 165.005-T
    - 165.006-T
id: 165.008-T
labels:
    - templates
    - documentation
    - agent-instructions
    - dogfood
    - installed-mirror
    - parity
parent_id: 165-F
priority: medium
status: queued
title: 'Agent sequencing contract: template sources AND installed dogfood mirrors in one atomic commit'
updated_at: 2026-09-13T00:36:53.5715571Z
---

Update the agent sequencing contract in BOTH the TEMPLATE SOURCES and the INSTALLED DOGFOOD MIRRORS as ONE ATOMIC UNIT — one task, one commit.

ATOMICITY IS MANDATORY (review-fix cycle 2, finding 8). Cycle 1 split this work into 165.008-T (sources) -> 165.010-T (mirrors) joined by a dependency edge. That split is now REVERSED and 165.010-T is ARCHIVED into this task. A dependency-only ordering does NOT prevent drift: it merely schedules it. Between the two tasks the repository would contain template sources stating the four-state contract and installed `.github/agents/` mirrors still stating retired numeric-adjacency sequencing — and the installed mirrors are what the dogfooded agents in THIS repository actually read. That intermediate window is a live wrong-contract window for every agent session that starts inside it, and it is not acceptable at any duration. Sources and mirrors MUST be updated in the SAME commit; a commit that touches one without the other fails this task.

EDIT SURFACES — TEMPLATE SOURCES (verified by grep):
- `templates/agents/_orchestrator.agent.md.tmpl` — L312, L314, L339, L341
- `templates/agents/_ship.agent.md.tmpl` — L172-175, L200-201, L224-225, L235

EDIT SURFACES — INSTALLED DOGFOOD MIRRORS (same commit):
- `.github/agents/_orchestrator.agent.md`
- `.github/agents/_ship.agent.md`

REMOVED FROM SCOPE (corrected mapping): `.github/instructions/workflows.instructions.md` contains NO `pre_claim`, NO `PREDECESSOR_`, and NO `dag-readiness` token — verified by grep. The first-round plan listed it in error. DO NOT edit it, and do NOT manufacture a change there. Likewise `templates/policies/workflow-policies.md.tmpl` contains no `pre_claim`/`PREDECESSOR_` token and is NOT an edit surface. No other installed instruction file is in scope.

CONTENT REQUIREMENTS (identical in sources and mirrors):
1. Predecessors derive from EXPLICIT `blocks` DAG edges only; numeric adjacency is retired from the claim path.
2. The four derivation states and their provenance values (`explicit`, `declared_root`, `genesis`, `unsequenced`) are documented as agent-observable output, along with the UNSEQUENCED_SHIPMENT token and its two remedies.
3. GENESIS IS NARROW (review-fix cycle 2, finding 2): state that `genesis` applies ONLY to a workspace whose candidate shipment is the SOLE EXTANT shipment record with NO shipped-terminal and NO abandoned history, live or archived. An agent must NOT expect a second, third, or later-numbered edge-less shipment to pass as `genesis` — in a workspace holding more than one shipment record, every intended root must carry `dag-root` explicitly or it blocks as `unsequenced`.
4. `pre_claim` is the SOLE claim authority; `dag-readiness` is advisory and NON-AUTHORIZING and must never be treated as permission.
5. SHIP MUST NOT SELF-DECLARE A ROOT. Applying the `dag-root` label is an operator/Stage act on backlog data. Ship declaring a root to unblock its own claim is the same authority violation as self-authorizing `--force`.
6. The `audit_sequencing` phase is named as the migration path for workspaces with previously-implicit sequencing, together with the migration ledger it emits (165.003-T) and the rule that a code-only rollback does NOT undo migrated edges or root labels.
7. No unresolved template placeholder tokens introduced; markdownlint heading hierarchy clean (P-008).

RECORDED PARITY CHECK (carried over from archived 165.010-T, unchanged in force): for EACH of the two file pairs, confirm in the task record that the installed copy and its template source state the SAME contract, and record the confirmation. Drift found must be FIXED, not noted. Because both halves land in one commit, the parity check is performed against the final working tree before commit, not against a previous task's output.

MANDATORY MEMBER OF 173-S. This task may not be deferred, split back out, or partially delivered.

SIZE: M (~2h). COMPLEXITY: medium (one contract text authored once, then mirrored within the same working context; the merged estimate is below the previous 1.5h + 1h split because the contract text is derived ONCE rather than re-derived from a finished source in a second sitting).
Plan §T7. Depends on 165.005-T and 165.006-T.