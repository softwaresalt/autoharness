---
artifact_type: task
created_at: 2026-09-12T22:51:26.4326628Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 165.005-T
    - 165.006-T
id: 165.008-T
labels:
    - templates
    - documentation
    - agent-instructions
    - dogfood
parent_id: 165-F
priority: medium
status: queued
title: 'Agent template sources: sequencing contract text for the four-state derivation model'
updated_at: 2026-09-13T00:10:12.8207963Z
---

Update the AGENT TEMPLATE SOURCES so the agent-facing sequencing contract matches the new gate behaviour. Installed dogfood mirrors are handled by 165.010-T, which depends on this task so the mirror is written from the finished source.

MANDATORY MEMBER OF 173-S (review-fix cycle 1, finding 8). This task and 165.010-T are split ONLY to keep each under the 2-hour ceiling. NEITHER may be deferred to an optional follow-up outside this shipment. The first-round "split installed-copy updates into a follow-up if execution exceeds budget" escape hatch is REMOVED.

CONFIRMED EDIT SURFACES (verified by grep):
- `templates/agents/_orchestrator.agent.md.tmpl` — L312, L314, L339, L341
- `templates/agents/_ship.agent.md.tmpl` — L172-175, L200-201, L224-225, L235

REMOVED FROM SCOPE (corrected mapping): `.github/instructions/workflows.instructions.md` contains NO `pre_claim`, NO `PREDECESSOR_`, and NO `dag-readiness` token — verified by grep. The first-round plan listed it in error. DO NOT edit it, and do NOT manufacture a change there. Likewise `templates/policies/workflow-policies.md.tmpl` contains no `pre_claim`/`PREDECESSOR_` token and is NOT an edit surface.

CONTENT REQUIREMENTS:
1. Predecessors derive from EXPLICIT `blocks` DAG edges only; numeric adjacency is retired from the claim path.
2. The four derivation states and their provenance values (`explicit`, `declared_root`, `genesis`, `unsequenced`) are documented as agent-observable output, along with the UNSEQUENCED_SHIPMENT token and its two remedies.
3. `pre_claim` is the SOLE claim authority; `dag-readiness` is advisory and NON-AUTHORIZING and must never be treated as permission.
4. SHIP MUST NOT SELF-DECLARE A ROOT. Applying the `dag-root` label is an operator/Stage act on backlog data. Ship declaring a root to unblock its own claim is the same authority violation as self-authorizing `--force`.
5. The `audit_sequencing` phase is named as the migration path for workspaces with previously-implicit sequencing.
6. No unresolved template placeholder tokens introduced; markdownlint heading hierarchy clean (P-008).

SIZE: S (~1.5h). COMPLEXITY: medium (contract text across two template sources).
Plan §T7. Depends on 165.005-T and 165.006-T. Blocks 165.010-T.