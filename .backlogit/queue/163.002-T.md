---
artifact_type: task
created_at: 2026-09-08T07:42:26.3263813Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 163.001-T
    - 033-DL
id: 163.002-T
labels:
    - test
    - red
    - ordering
    - fail-closed
parent_id: 163-F
priority: high
status: queued
title: 'U1b RED: replay assertions - ordering, reconstruction rejection, explicit-empty'
updated_at: 2026-09-10T22:12:42.19086Z
---

Two assertion families plus the explicit-empty rule over the U1a fixture. AC: (1) ORDERING - a trace that reaches the cascade invocation with no pre-existing evidence record is REJECTED; a record whose collection_completed_at is absent, or is not STRICTLY BEFORE the cascade invocation timestamp, is REJECTED; (2) RECONSTRUCTION REJECTION - a trace producing a BYTE-IDENTICAL evidence record AFTER the mutation (the literal 159-S history) is REJECTED, and is rejected FOR THE ORDERING REASON, never for a content-mismatch reason. This is the load-bearing assertion: content equality is explicitly NOT sufficient, because 159-S's reconstruction WAS byte-identical and would pass any content comparison. A content-equality implementation MUST fail this test; (3) EXPLICIT-EMPTY - an evidence record with an empty validated set must record scan_performed: true with an explicit empty list; an omitted field is REJECTED, because an unrecorded empty scan is indistinguishable from a scan that never ran (the precise 159-S failure mode); (4) both new halt tokens asserted by name: RECONCILE_FAIL_PRECASCADE_EVIDENCE_MISSING and RECONCILE_FAIL_PRECASCADE_EVIDENCE_STALE, evaluated as two separately-labelled independently-failing conditions, never merged (merging two questions into one condition is the documented root cause of external defect B57F9E24); (5) RED only - no SKILL.md or template edits. Depends on U1a. Plan: docs/plans/2026-09-08-shipment-reconcile-step-0c-live-pre-mutation-evidence-plan.md U1b.

--- REVISION R1 (2026-09-10, gate 033-DL, stash DDBF283E/EB23D1B9; plan section "Revision R1") --- R1 GOVERNS where it conflicts with the text above. ADD the BACKDATED-RECONSTRUCTION REJECTION FAMILY (the negative case DDBF283E explicitly requires): a trace whose evidence record has collection_completed_at strictly BEFORE the cascade invocation and whose HEAD SHA and manifest item list both MATCH the state being closed - a PERFECT FORGERY under the pre-R1 mechanism - but whose PRECASCADE_EVIDENCE_ANCHOR event is ABSENT from the engine log, or is appended AFTER the mutation events, MUST be REJECTED. ASSERT THE TOKEN IDENTITY, NOT MERELY THE REJECTION: it must be rejected with RECONCILE_FAIL_PRECASCADE_ANCHOR_MISSING or RECONCILE_FAIL_PRECASCADE_ANCHOR_NOT_PRE_MUTATION and NEVER with RECONCILE_FAIL_PRECASCADE_EVIDENCE_STALE. Token identity is what proves the ORDERING PROOF - and not a content comparison - is doing the work; asserting only "rejected" would pass against a content-equality implementation and defeat the purpose of this task. This assertion MUST FAIL against the pre-R1 mechanism; that failure is the genuine RED for R1. Also assert digest binding: an anchor whose sha256 does not match the evidence record on disk fires ANCHOR_MISSING (closes the anchor-early/fabricate-record-later seam). The pre-existing ORDERING and EXPLICIT-EMPTY assertion families are UNCHANGED and still required. The ordering family now asserts APPEND ORDER in the engine log, not timestamp comparison.