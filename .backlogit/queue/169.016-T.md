---
artifact_type: task
created_at: 2026-09-19T04:23:57.6194298Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 169.015-T
id: 169.016-T
labels:
    - test
    - p-002.7
    - confirm
    - post-activation
    - tdd-green-observation
parent_id: 169-F
priority: medium
status: queued
title: 'P-002.7 CONFIRM (post-activation): confirm installed/template parity and active-consumer behaviour, and emit the status-contract verdict'
updated_at: 2026-09-19T07:04:38.338243Z
---

CONFIRM PHASE - POST-ACTIVATION CONFIRMATION. THIS IS NOT THE EVIDENCE GATE THAT AUTHORIZED ACTIVATION, AND IT MUST NEVER BE DESCRIBED AS ONE. The gate that authorizes activation is 169.017-T, which runs BEFORE 169.015-T and emits the pre-activation readiness verdict. This task runs AFTER the single ACTIVATE commit and confirms that what was activated actually holds on the INSTALLED surfaces. REPURPOSING PROVENANCE, STATED TRUTHFULLY: through revision 4 this record was the unit's VERIFY task AND its sole gate emitter, positioned after the irreversible ACTIVATE commit. Attempt-04 finding M1 (P1) recorded that this placement contradicted decision D2's rollout invariant - PREPARE then VERIFY then ACTIVATE, with the complete evidence set produced BEFORE any activation - because it left the unit's only gate on the far side of the mutation D2 positions it to guard. In the revision-5 remediation cycle THE PRE-ACTIVATION EVIDENCE-GATE ROLE WAS MOVED OUT of this record into the new 169.017-T, and THIS RECORD WAS RETAINED AND RETITLED as the distinct post-activation confirmation step rather than archived, because its substantive work - observing the five RED-proven assertion families against the SHIPPED text and resolving the STATUS_CONTRACT_* composed state - is genuinely post-activation work that must still happen and has no other owner. This record still ABSORBS the verification responsibilities of archived 169.005-T and 169.008-T, which attempt-01 A2 found introducing assertions in the GREEN phase with no prior red observation; those assertion families live in RED tasks 169.012-T, 169.013-T and 169.014-T, and what remains here is the observation that they hold once installed. WHAT THIS TASK CONFIRMS, AFTER THE ACTIVATE COMMIT. (1) INSTALLED/TEMPLATE PARITY: for BOTH authoritative/mirror pairs - templates/policies/workflow-policies.md.tmpl against .github/policies/workflow-policies.md, and templates/agents/_ship.agent.md.tmpl against .github/agents/_ship.agent.md - the P-002.7 blocks are BYTE-IDENTICAL as shipped. (2) ACTIVE-CONSUMER BEHAVIOUR: Ship's claim sequence at .github/agents/_ship.agent.md item 4, "Claim the shipment via backlogit_claim_shipment", resolves the P-002.7 clause as a LIVE DECLARATION on an installed surface rather than as inert test-owned fixture data, and the bidirectional cross-reference resolves in BOTH directions from the INSTALLED copies. This is the distinction that makes this step irreducible to 169.017-T: the pre-activation gate can only ever observe the candidate as inert data, and no inert observation can demonstrate installed-consumer behaviour. (3) THE FIVE RED-PROVEN FAMILIES, per assertion, now passing against the shipped text: (A) three transition rows present and correct (169.009-T), (B) bidirectional cross-reference wiring (169.010-T), (C) template/mirror byte-identity for both pairs (169.012-T), (D) observed-version attribution paragraph present (169.013-T), (E) exactly three transition rows and exactly four declaring surfaces, both closure assertions (169.014-T). THE EVIDENCE MUST BE PER-ASSERTION, NOT AGGREGATE: a green suite exit does not demonstrate that assertion D passed, it demonstrates that nothing failed, which is a weaker claim and is not what this contract requires. ADDS NO ASSERTION - the test module tests/test_p002_7_member_status_contract.py produces the OBSERVATIONS; this task READS them and emits the VERDICT LINE. THIS TASK IS THE SOLE EMITTER OF THE POST-ACTIVATION STATUS-CONTRACT VERDICT LINE AND EMITS EXACTLY ONE OF THREE TOKENS. Attempt 02 finding G1 (P1) found this record previously emitting an incompatible two-token CONTRACT_ACTIVE / CONTRACT_INCOMPLETE pair, which deleted the not-observed state decision D6 requires. DESTINATION - EXACTLY ONE PATH: .autoharness/gates/p002-7-status-contract-verdict.txt. That directory is GITIGNORED at .gitignore:7, which is deliberate: the verdict is a GENERATED OBSERVATION ABOUT the tracked surfaces, not a tracked surface itself. It is never committed, never appears in a diff, and CANNOT LEAVE THE WORKING TREE DIRTY after this task runs. THE TWO VERDICTS ARE NEVER CONFLATED: this task writes COMPOSED_STATE: lines carrying STATUS_CONTRACT_* tokens to p002-7-status-contract-verdict.txt; 169.017-T writes PREACTIVATION_STATE: lines carrying PREACTIVATION_* tokens to p002-7-preactivation-readiness.txt. Different path, different line prefix, DISJOINT token vocabularies. This task NEVER reads, writes, amends or deletes the pre-activation readiness artifact, and a STATUS_CONTRACT_* token is never valid in it. FORMAT - EXACTLY ONE LINE, ONE OF EXACTLY THREE LITERAL FORMS, prefix "COMPOSED_STATE: ", token as the first field, fields separated by " | ": "COMPOSED_STATE: STATUS_CONTRACT_HELD | families=5 | assertions_passed=<n> | declared_surface_count=4 | resolved_surface_count=4 | checked=YYYY-MM-DD"; "COMPOSED_STATE: STATUS_CONTRACT_DIVERGENT | families=5 | failed=<n> | surface=<declared-surface-path> | divergence=<short-name> | checked=YYYY-MM-DD"; "COMPOSED_STATE: STATUS_CONTRACT_NOT_OBSERVED | reason=<import_error|loader_errors|failed_test_placeholder|zero_assertions|family_unrecorded> | checked=YYYY-MM-DD". A DIVERGENT line naming more than one offending surface repeats the surface= field once per surface, still on the ONE line. Note that resolved_surface_count is 4 here and 0 in the pre-activation verdict, because this task observes the ACTIVATED state and 169.017-T observes the INERT state; that difference is a property of the two phases, not a discrepancy. OWNERSHIP AND WRITE BEHAVIOUR - SINGLE WRITER, ATOMIC, WHOLE-FILE REPLACE. THIS TASK IS THE SOLE WRITER of that path; no other task and not the test module writes it. The write is ATOMIC: write the line to a temporary file in the SAME DIRECTORY and RENAME it over the destination, so a reader sees either the previous verdict or the new one and NEVER A PARTIAL LINE. Each run REPLACES THE WHOLE FILE - never appended to, holding exactly one COMPOSED_STATE: line at all times; a pre-existing file from an earlier run is overwritten, not merged. ABSENCE EVALUATION - FAIL CLOSED, AND ABSENCE IS A READING RATHER THAN AN ERROR: a consumer resolves the artifact to STATUS_CONTRACT_NOT_OBSERVED if ANY of the following holds - the file does not exist; it exists but is unreadable; it is empty; it contains no line beginning "COMPOSED_STATE: "; it contains MORE THAN ONE such line; or the token in the first field is not one of the three declared. EVALUATE THE THREE CONDITIONS IN THIS ORDER. (1) STATUS_CONTRACT_NOT_OBSERVED - emit if ANY of the following holds: the test module raised on import under unittest.defaultTestLoader; loader.errors is non-empty; any _FailedTest placeholder is present; zero assertions executed; or any one of the five assertion families has no per-assertion executed result recorded. THIS IS CHECKED FIRST, because a suite that did not run cannot be distinguished from a suite that passed by looking at its exit code. (2) STATUS_CONTRACT_DIVERGENT - emit if the module imported cleanly, every family executed, and AT LEAST ONE EXECUTED ASSERTION FAILED; name the offending surface path and the specific divergence in the line. (3) STATUS_CONTRACT_HELD - emit ONLY if the module imported cleanly, all five families executed with a per-assertion PASSING result recorded, installed/template parity holds for both pairs, the surface-enumeration rule resolves to exactly declared_surface_count, and no assertion exists in the suite that was not observed red first. FAIL CLOSED: any state not affirmatively established by (2) or (3) is STATUS_CONTRACT_NOT_OBSERVED. NO-OBSERVATION IS NEVER A PASS: a green aggregate exit with no per-assertion record is STATUS_CONTRACT_NOT_OBSERVED, and an ABSENT verdict line is itself STATUS_CONTRACT_NOT_OBSERVED. EXIT CODE: EXIT ZERO ONLY when the token written is STATUS_CONTRACT_HELD; EXIT NON-ZERO for STATUS_CONTRACT_DIVERGENT and STATUS_CONTRACT_NOT_OBSERVED. The exit code and the artifact carry the same verdict by construction, because one step produces both. ROLLBACK AND HALT PATH ON FAILURE - EXPLICIT AND MANDATORY. On STATUS_CONTRACT_DIVERGENT or STATUS_CONTRACT_NOT_OBSERVED this task exits non-zero and THE UNIT HALTS. 169.007-T DOCS DOES NOT PROCEED - documentation follows CONFIRMED behaviour, never asserted behaviour. THE SINGLE 169.015-T ACTIVATE COMMIT IS REVERTED AS A UNIT with git revert, which returns all four declared surfaces to their pre-activation divergent state SIMULTANEOUSLY; that guarantee holds ONLY because activation is one commit, and it would not hold across two commits joined by an edge. The unit then RETURNS TO STAGE. THIS TASK NEVER REPAIRS A DECLARED SURFACE IN PLACE, never amends or re-runs the activation commit, never adds, weakens or re-scopes an assertion to reach STATUS_CONTRACT_HELD, and never edits either gate artifact's history. A divergence discovered here returns to a RED task for re-observation, and the unit RE-RUNS 169.017-T for a fresh PREACTIVATION_READY verdict before any re-activation; a stale readiness verdict never authorizes a second activation. ADDING ANY NEW ASSERTION IN THIS TASK IS A TDD VIOLATION and a finding. Scope: observation, evidence recording, and one generated gitignored artifact; no production surface is touched. .github/workflows/ci.yml IS NOT A CONSUMER of either verdict line: its test job runs PYTHONPATH=src python -m unittest discover -s tests and consumes an EXIT CODE, and that suite PRODUCES the observations this verdict is derived FROM - the reverse direction. Attempt 03 finding L1 (P1) found this record and the plan both naming ci.yml as reading the verdict line as a gate; NO TASK IN 177-S MODIFIES ci.yml and this unit adds no CI gate. Ship's claim sequence is a consumer of the P-002.7 CONTRACT TEXT that 169.015-T writes, not of either verdict line. Blocked by 169.015-T; blocks 169.007-T. Stash 3EF5AAF2. Plan docs/plans/2026-09-18-post-claim-member-status-contract-plan.md (plan_id post-claim-member-status-contract-v2, revision 5, disposition REMEDIATED-PENDING-REVIEW, awaiting independent attempt 05). Verdict manifest docs/reviews/2026-09-18-post-claim-member-status-contract-v2-plan-review.md.