---
artifact_type: task
created_at: 2026-09-19T04:23:57.6194298Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 169.015-T
id: 169.016-T
labels:
    - test
    - p-002.7
    - confirm
    - post-activation
    - tdd-green-observation
parent_id: 169-F
priority: medium
status: queued
title: 'P-002.7 CONFIRM (post-activation): confirm installed/template parity and active-consumer behaviour, and emit the status-contract verdict'
updated_at: 2026-09-21T00:17:00.0000000Z
---

CONFIRM PHASE - POST-ACTIVATION CONFIRMATION. THIS IS NOT THE EVIDENCE GATE THAT AUTHORIZED ACTIVATION, AND IT MUST NEVER BE DESCRIBED AS ONE. The gate that authorizes activation is 169.017-T, which runs BEFORE 169.015-T and emits the pre-activation readiness verdict. This task runs AFTER the single ACTIVATE commit and confirms that what was activated actually holds on the INSTALLED surfaces. REPURPOSING PROVENANCE, STATED TRUTHFULLY: through revision 4 this record was the unit's VERIFY task AND its sole gate emitter, positioned after the irreversible ACTIVATE commit. Attempt-04 finding M1 (P1) recorded that this placement contradicted decision D2's rollout invariant - PREPARE then VERIFY then ACTIVATE, with the complete evidence set produced BEFORE any activation - because it left the unit's only gate on the far side of the mutation D2 positions it to guard. In the revision-5 remediation cycle THE PRE-ACTIVATION EVIDENCE-GATE ROLE WAS MOVED OUT of this record into the new 169.017-T, and THIS RECORD WAS RETAINED AND RETITLED as the distinct post-activation confirmation step rather than archived, because its substantive work - observing the five RED-proven assertion families against the SHIPPED text and resolving the STATUS_CONTRACT_* composed state - is genuinely post-activation work that must still happen and has no other owner. This record still ABSORBS the verification responsibilities of archived 169.005-T and 169.008-T, which attempt-01 A2 found introducing assertions in the GREEN phase with no prior red observation; those assertion families live in RED tasks 169.012-T, 169.013-T and 169.014-T, and what remains here is the observation that they hold once installed. WHAT THIS TASK CONFIRMS, AFTER THE ACTIVATE COMMIT. (1) INSTALLED/TEMPLATE PARITY: for BOTH authoritative/mirror pairs - templates/policies/workflow-policies.md.tmpl against .github/policies/workflow-policies.md, and templates/agents/_ship.agent.md.tmpl against .github/agents/_ship.agent.md - the P-002.7 blocks are BYTE-IDENTICAL as shipped. (2) ACTIVE-CONSUMER BEHAVIOUR: Ship's claim sequence at .github/agents/_ship.agent.md item 4, "Claim the shipment via backlogit_claim_shipment", resolves the P-002.7 clause as a LIVE DECLARATION on an installed surface rather than as inert test-owned fixture data, and the bidirectional cross-reference resolves in BOTH directions from the INSTALLED copies. This is the distinction that makes this step irreducible to 169.017-T: the pre-activation gate can only ever observe the candidate as inert data, and no inert observation can demonstrate installed-consumer behaviour. (3) THE FIVE RED-PROVEN FAMILIES, per assertion, now passing against the shipped text: (A) three transition rows present and correct (169.009-T), (B) bidirectional cross-reference wiring (169.010-T), (C) template/mirror byte-identity for both pairs (169.012-T), (D) observed-version attribution paragraph present (169.013-T), (E) exactly three transition rows and exactly four declaring surfaces, both closure assertions (169.014-T). THE EVIDENCE MUST BE PER-ASSERTION, NOT AGGREGATE: a green suite exit does not demonstrate that assertion D passed, it demonstrates that nothing failed, which is a weaker claim and is not what this contract requires. ADDS NO ASSERTION - the test module tests/test_p002_7_member_status_contract.py produces the OBSERVATIONS; this task READS them and emits the VERDICT LINE. THIS TASK IS THE SOLE EMITTER OF THE POST-ACTIVATION STATUS-CONTRACT VERDICT LINE AND EMITS EXACTLY ONE OF THREE TOKENS. Attempt 02 finding G1 (P1) found this record previously emitting an incompatible two-token CONTRACT_ACTIVE / CONTRACT_INCOMPLETE pair, which deleted the not-observed state decision D6 requires. DESTINATION - EXACTLY ONE PATH: .autoharness/gates/p002-7-status-contract-verdict.txt. That directory is GITIGNORED at .gitignore:7, which is deliberate: the verdict is a GENERATED OBSERVATION ABOUT the tracked surfaces, not a tracked surface itself. It is never committed, never appears in a diff, and CANNOT LEAVE THE WORKING TREE DIRTY after this task runs. THE TWO VERDICTS ARE NEVER CONFLATED: this task writes COMPOSED_STATE: lines carrying STATUS_CONTRACT_* tokens to p002-7-status-contract-verdict.txt; 169.017-T writes PREACTIVATION_STATE: lines carrying PREACTIVATION_* tokens to p002-7-preactivation-readiness.txt. Different path, different line prefix, DISJOINT token vocabularies. This task NEVER reads, writes, amends or deletes the pre-activation readiness artifact, and a STATUS_CONTRACT_* token is never valid in it. FORMAT - EXACTLY ONE LINE, ONE OF EXACTLY THREE LITERAL FORMS, prefix "COMPOSED_STATE: ", token as the first field, fields separated by " | ", FIELD ORDER FIXED EXACTLY AS SHOWN: "COMPOSED_STATE: STATUS_CONTRACT_HELD | families=5 | assertions_passed=<n> | declared_surface_count=4 | resolved_surface_count=4 | head_commit=<40-hex> | surface_digest=<64-hex> | checked=<YYYY-MM-DDThh:mm:ssZ> | binding=<64-hex>"; "COMPOSED_STATE: STATUS_CONTRACT_DIVERGENT | families=5 | failed=<n> | surface=<declared-surface-path> | divergence=<short-name> | checked=<YYYY-MM-DDThh:mm:ssZ>"; "COMPOSED_STATE: STATUS_CONTRACT_NOT_OBSERVED | reason=<import_error|loader_errors|failed_test_placeholder|zero_assertions|family_unrecorded|digest_input_unreadable> | checked=<YYYY-MM-DDThh:mm:ssZ>". <40-hex> and <64-hex> are lowercase hexadecimal of exactly 40 and exactly 64 characters. A DIVERGENT line naming more than one offending surface repeats the surface= field once per surface, still on the ONE line. Note that resolved_surface_count is 4 here and 0 in the pre-activation verdict, because this task observes the ACTIVATED state and 169.017-T observes the INERT state; that difference is a property of the two phases, not a discrepancy. THE FRESHNESS BINDING, ADDED AT REVISION 6 SO THAT THE DOCUMENTATION GATE OPENED BY ATTEMPT-05 FINDING N1 (P2) HAS A DEFINED STALENESS SEMANTICS, using the same shared primitives as the readiness gate rather than a second rule. COMPUTE, IMMEDIATELY BEFORE THE ATOMIC WRITE AND ONLY FOR THE HELD FORM: (i) head_commit, the full 40-character lowercase hex commit ID of HEAD at the instant of evaluation - which in the normal path is the single ACTIVATE commit - read from the repository and never from an environment variable or from a file this unit writes; (ii) surface_digest, the CCD/v1 canonical content digest over EXACTLY the FOUR declared surfaces IN ENUMERATION ORDER - templates/policies/workflow-policies.md.tmpl, .github/policies/workflow-policies.md, templates/agents/_ship.agent.md.tmpl, .github/agents/_ship.agent.md - with no glob, no directory walk and no discovery; (iii) checked, an RFC 3339 UTC timestamp YYYY-MM-DDThh:mm:ssZ; and (iv) binding, the B/v1 digest over the five-line tagged preimage under tag p002-7-confirmation-binding/v1. CCD/v1 and B/v1 are defined once in the plan's Shared gate primitives section and are identical for both gates: CRLF-to-LF normalization only, lowercase-hex sha256 per file, a <path> LF <file-digest> LF UTF-8 preimage with / separators, and a five-line binding preimage whose key is ALWAYS the literal content_digest= even though the emitted field here is named surface_digest=. WHY THIS DIGEST IS THE FOUR SURFACES AND NOT THE READINESS SEVEN: the two gates observe different subjects. The readiness digest covers the test material because the readiness verdict is a statement about evidence produced from it; this digest covers the declared surfaces alone because THIS TASK'S SUBJECT IS THE SHIPPED TEXT and 169.007-T documents ACTIVATED BEHAVIOUR, so binding the documentation gate to test material would close it on a test-only edit that changes nothing DOCS describes. The asymmetry is deliberate and recorded, not an oversight. WHY ONLY THE HELD FORM CARRIES head_commit, surface_digest AND binding: a not-observed verdict is reachable precisely when the inputs cannot be read, so the digest is UNDEFINED BY RULE and could not be emitted truthfully, and a non-authorizing line is never consumed as an authorization so it has nothing to bind. checked= stays on all three forms as a diagnostic vintage. SCOPE STATED HONESTLY: staleness and mistake detection, NOT a tamper-proof security boundary, and no wall-clock future check, deliberately, because clock skew would make the contract environment-dependent. OWNERSHIP AND WRITE BEHAVIOUR - SINGLE WRITER, ATOMIC, WHOLE-FILE REPLACE, FRESH BINDING ON EVERY RUN. THIS TASK IS THE SOLE WRITER of that path; no other task and not the test module writes it. The write is ATOMIC: write the line to a temporary file in the SAME DIRECTORY and RENAME it over the destination, so a reader sees either the previous verdict or the new one and NEVER A PARTIAL LINE. Each run REPLACES THE WHOLE FILE - never appended to, holding exactly one COMPOSED_STATE: line at all times; a pre-existing file from an earlier run is overwritten, not merged. Because the whole file is replaced and head_commit, surface_digest and checked are recomputed from scratch on every run, A RE-RUN NECESSARILY PRODUCES A FRESH BINDING, and no previous run's binding can survive alongside a new verdict. ABSENCE EVALUATION - FAIL CLOSED, AND ABSENCE IS A READING RATHER THAN AN ERROR: a consumer resolves the artifact to STATUS_CONTRACT_NOT_OBSERVED if ANY of the following holds - the file does not exist; it exists but is unreadable; it is empty; it contains no line beginning "COMPOSED_STATE: "; it contains MORE THAN ONE such line; or the token in the first field is not one of the three declared. AN F1-F5 FRESHNESS FAILURE IS A GATE FAILURE, NOT AN ARTIFACT REWRITE: it resolves 169.007-T's gate CLOSED, does not change the token in this artifact, and NEVER authorizes 169.007-T to rewrite, amend or delete it; the only way to obtain a fresh binding is to RE-RUN THIS TASK. EVALUATE THE THREE CONDITIONS IN THIS ORDER. (1) STATUS_CONTRACT_NOT_OBSERVED - emit if ANY of the following holds: the test module raised on import under unittest.defaultTestLoader; loader.errors is non-empty; any _FailedTest placeholder is present; zero assertions executed; any one of the five assertion families has no per-assertion executed result recorded; or any of the four surface_digest inputs is missing or unreadable, or HEAD is unresolvable, so the freshness binding cannot be computed (reason=digest_input_unreadable). THIS IS CHECKED FIRST, because a suite that did not run cannot be distinguished from a suite that passed by looking at its exit code. (2) STATUS_CONTRACT_DIVERGENT - emit if the module imported cleanly, every family executed, and AT LEAST ONE EXECUTED ASSERTION FAILED; name the offending surface path and the specific divergence in the line. (3) STATUS_CONTRACT_HELD - emit ONLY if the module imported cleanly, all five families executed with a per-assertion PASSING result recorded, installed/template parity holds for both pairs, the surface-enumeration rule resolves to exactly declared_surface_count, no assertion exists in the suite that was not observed red first, AND head_commit, surface_digest and checked were all computed successfully and binding was derived from them under B/v1. The freshness fields are computed LAST, immediately before the atomic write. FAIL CLOSED: any state not affirmatively established by (2) or (3) is STATUS_CONTRACT_NOT_OBSERVED. NO-OBSERVATION IS NEVER A PASS: a green aggregate exit with no per-assertion record is STATUS_CONTRACT_NOT_OBSERVED, and an ABSENT verdict line is itself STATUS_CONTRACT_NOT_OBSERVED. EXIT CODE: EXIT ZERO ONLY when the token written is STATUS_CONTRACT_HELD; EXIT NON-ZERO for STATUS_CONTRACT_DIVERGENT and STATUS_CONTRACT_NOT_OBSERVED. The exit code and the artifact carry the same verdict by construction, because one step produces both. NOTE WHAT THIS EXIT CODE IS NOT ASKED TO DO ALONE: it is the WEAKER of TWO INDEPENDENT enforcement mechanisms on the CONFIRM-to-DOCS edge, because it depends on the invoking process having observed and honoured it. THE STRONGER MECHANISM IS 169.007-T'S OWN FIRST-ACTION FAIL-CLOSED READ of this artifact - a whole-file comparison against the literal STATUS_CONTRACT_HELD plus the F1-F5 freshness recomputation - which does not depend on any process having observed this exit at all. Through revision 5 only the weaker mechanism plus a blocks edge existed, which attempt-05 finding N1 (P2) recorded as a gap: the plan asserted in FOUR places that DOCS must not proceed on a non-pass confirmation while applying its own H18 gate-predicate principle only to the structurally identical ACTIVATE edge. THE EDGE ON 169.007-T IS ORDERING ONLY - it clears on PREDECESSOR COMPLETION, which this task reaches on ALL THREE of its tokens, two of which are FAILURES - and THE TOKEN PLUS ITS BINDING IS THE AUTHORIZATION. ROLLBACK AND HALT PATH ON FAILURE - EXPLICIT AND MANDATORY. On STATUS_CONTRACT_DIVERGENT or STATUS_CONTRACT_NOT_OBSERVED this task exits non-zero and THE UNIT HALTS. 169.007-T DOCS DOES NOT PROCEED, AND THAT HALT IS MECHANICAL: its own first-action read finds a token that is not the literal STATUS_CONTRACT_HELD, so it touches no documentation file, makes no commit and exits 1. Documentation follows CONFIRMED behaviour, never asserted behaviour. THE SINGLE 169.015-T ACTIVATE COMMIT IS REVERTED AS A UNIT with git revert, which returns all four declared surfaces to their pre-activation divergent state SIMULTANEOUSLY; that guarantee holds ONLY because activation is one commit, and it would not hold across two commits joined by an edge. THE REVERT RE-CLOSES BOTH GATES WITHOUT EITHER CONSUMER BEING TOLD, AND THE PREDICATE THAT DOES IT IS NAMED: git revert necessarily creates a NEW COMMIT, so HEAD advances past every head_commit written before it - a surviving PREACTIVATION_READY line fails F1 at 169.015-T, F1 ALONE being sufficient, and a surviving STATUS_CONTRACT_HELD line fails F1 AND F2 at 169.007-T, because the reverted surfaces no longer match its surface_digest. F4 FAILS ON NEITHER LINE AND IS NOT PART OF THIS GUARANTEE: it is anchored to the commit the line itself names, which a stale line satisfies (attempt 06 finding O3, P2). ONLY MERGE-FRIENDLY RECOVERY IS SUPPORTED - git revert and new forward commits; git reset --hard, rebase and amend restoring the pre-activation identity are PROHIBITED and outside this contract, because they would restore precisely the state a stale readiness line describes. The unit then RETURNS TO STAGE. THIS TASK NEVER REPAIRS A DECLARED SURFACE IN PLACE, never amends or re-runs the activation commit, never adds, weakens or re-scopes an assertion to reach STATUS_CONTRACT_HELD, and never edits either gate artifact's history. A divergence discovered here returns to a RED task for re-observation, and the unit RE-RUNS 169.017-T for a fresh PREACTIVATION_READY verdict before any re-activation; a stale readiness verdict never authorizes a second activation, and since revision 6 that is ENFORCED by 169.015-T's F1-F5 recomputation rather than only mandated in prose. ADDING ANY NEW ASSERTION IN THIS TASK IS A TDD VIOLATION and a finding. The digest and binding computations are EMITTER LOGIC, NOT ASSERTIONS: they produce no assertion outcome, belong to no assertion family, and must not enter the conformance suite. Scope: observation, evidence recording, and one generated gitignored artifact; no production surface is touched. .github/workflows/ci.yml IS NOT A CONSUMER of either verdict line: its test job runs PYTHONPATH=src python -m unittest discover -s tests and consumes an EXIT CODE, and that suite PRODUCES the observations this verdict is derived FROM - the reverse direction. Attempt 03 finding L1 (P1) found this record and the plan both naming ci.yml as reading the verdict line as a gate; NO TASK IN 177-S MODIFIES ci.yml and this unit adds no CI gate. Ship's claim sequence is a consumer of the P-002.7 CONTRACT TEXT that 169.015-T writes, not of either verdict line. MANIFEST CHECKSUM PARITY IS RE-OBSERVED HERE (decision D11, plan revision 8). Alongside installed/template parity, this task RE-DERIVES, for the two declared surfaces that are MANIFEST-TRACKED INSTALLED ARTIFACTS - .github/policies/workflow-policies.md and .github/agents/_ship.agent.md - that the .autoharness/harness-manifest.yaml artifacts: checksum recorded for each EQUALS a FRESH sha256 of the shipped file. THIS RE-DERIVATION IS OWNED GATE LOGIC BOUND TO THE EXACT ACTIVATION COMMIT SNAPSHOT, NOT THE GENERIC verify-workspace COMMAND, whose whole-workspace drift report is read from the WORKING TREE, is not an adjudication of a named digest against a named entry, and whose exit status is therefore not a checksum assertion. BOTH SIDES OF THE COMPARISON COME FROM THE COMMIT, NEVER THE WORKING TREE: for EACH of the two surfaces it recomputes SHA-256 over the artifact's SHIPPED BYTES - the LF-normalized committed blob read via git cat-file -p HEAD:<path> - and it PARSES THE MANIFEST FROM THE SAME COMMIT, as HEAD:.autoharness/harness-manifest.yaml, so a dirty or diverged working tree can neither manufacture a pass nor a spurious divergence. It then locates the EXACT MATCHING artifacts: entry by its literal path, requires that entry to be present EXACTLY ONCE with its path, primitive and template metadata intact, and compares BYTE-FOR-BYTE against the recorded checksum. A MISMATCH IS A DIVERGENCE: it resolves to STATUS_CONTRACT_DIVERGENT naming the offending surface, exactly as a template/mirror divergence does; a MISSING, DUPLICATED, EXTRA (an artifacts: entry the activation commit added or removed) or METADATA-DIVERGENT entry is likewise STATUS_CONTRACT_DIVERGENT naming that surface; and an UNREADABLE manifest resolves to STATUS_CONTRACT_NOT_OBSERVED under the existing digest_input_unreadable reason. FAIL CLOSED IN BOTH DIRECTIONS - neither outcome is ever a pass, and both exit non-zero. THIS TASK RE-DERIVES AND NEVER WRITES: 169.015-T owns the refresh, inside its own atomic activation commit, and this task rewrites no manifest entry and repairs nothing. THE SURFACE ARITHMETIC IS UNCHANGED: the manifest is a COMMIT MEMBER and not a fifth declared surface, .autoharness/ remains outside the enumeration rule's search scope, this task still ENUMERATES EXACTLY FOUR SURFACES, declared_surface_count stays 4 and resolved_surface_count stays 4, and the confirmation surface_digest continues to run over the FOUR DECLARED SURFACES ALONE with the manifest among NONE of its inputs. NO NEW ASSERTION IS ADDED TO THE CONFORMANCE SUITE: like the digest and binding computations, this is GATE LOGIC, it belongs to no assertion family, produces no assertion outcome, and therefore needs no prior red observation. Blocked by 169.015-T; blocks 169.007-T. Stash 3EF5AAF2. Plan docs/plans/2026-09-18-post-claim-member-status-contract-plan.md (plan_id post-claim-member-status-contract-v2, revision 8, disposition PASS, terminal attempt 08 scoped to the D11 delta - gate_result PASS with p0_open 0, p1_open 0 and p2_open 0 at reviewed_content_head a192e50c, artifact docs/reviews/review-history/2026-09-18-post-claim-member-status-contract-v2-plan-review-attempt-08.md, review_terminal true and awaiting_attempt null). Verdict manifest docs/reviews/2026-09-18-post-claim-member-status-contract-v2-plan-review.md.