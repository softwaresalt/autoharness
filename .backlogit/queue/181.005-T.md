---
artifact_type: task
created_at: 2026-09-21T06:00:10.7094833Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 181.004-T
id: 181.005-T
labels:
    - harness-surface:harness-architect
    - ship-lifecycle
    - activation
    - manifest-parity
parent_id: 181-F
priority: medium
status: queued
title: 'P4 T7: three-file Ship activation, manifest checksum and rollback gate'
updated_at: 2026-09-21T06:00:10.7094833Z
---

IMPLEMENTATION BUDGET: 110 minutes; exactly three repository files: templates/agents/_ship.agent.md.tmpl, .github/agents/_ship.agent.md and .autoharness/harness-manifest.yaml; depends on 181.004-T. Update the template's existing Step 2 between its own anchors; insert mirror Step 1.5 immediately before mirror Step 2. Keep numbering/placement intentionally asymmetric while preserving semantic rendered-body parity. Replace the early checkpoint restore path in both surfaces: validate checkpoint, extract shipment_id, run a fresh resolver and halt on 1/2 before restoring cursor/phase/intent; remove the old bypass rather than appending a later correction. Exit 0 resolves the actor, invokes harness-architect for applicable tasks and requires valid task-scoped marker-bearing RED evidence before partition. Refresh only the existing manifest entry/checksum for .github/agents/_ship.agent.md. Task-scoped harness acceptance marker AH181005_SHIP_ACTIVATION must be observed RED before edits and green after; imports/collection/syntax errors are NO_OBSERVATION. Run targeted and whole-suite commands with PYTHONPATH=src. Rollback is only `git revert <exact activation SHA>` after fresh live non-repository operator/session approval bound to that SHA immediately before revert; no fourth repository file or task-record mutation records approval. Without approval halt. Plan revision 12; verdict pending attempt 11; source stash 76EBDE6D.