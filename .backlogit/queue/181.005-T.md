---
artifact_type: task
created_at: 2026-09-19T02:54:43.9196366Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: v1
    size_source: agent
dependencies:
    - 181.004-T
id: 181.005-T
labels:
    - harness-surface:harness-architect
    - ship-lifecycle
    - activate
    - installed-mirror
parent_id: 181-F
priority: medium
status: queued
title: 'P4 T5 (ACTIVATE): one commit wiring the pre-task phase into both Ship surfaces'
updated_at: 2026-09-21T03:01:57.3712685Z
---

P4 T5 (ACTIVATE). One atomic commit wiring the pre-task harness-generation phase into both Ship surfaces. Size M, complexity high.

EXACTLY THREE FILES, EXACTLY ONE REFRESHED MANIFEST ENTRY: templates/agents/ship.md.tmpl, the rendered mirror .github/agents/ship.md, and .autoharness/harness-manifest.yaml. This is decision D11 applied - the manifest is a MEMBER of the commit and of the rollback unit, and checksum parity is verified against the on-disk mirror after the refresh. Templates are not manifest-tracked, so the template-and-mirror pair refreshes ONE entry, not two. The three-file / one-entry count is load-bearing and is stated identically in the governing plan, in 181-F and here.

BOTH SECTIONS ARE UPDATED in the template and in the installed mirror: the harness-generation section, which invokes the resolver before task partition, and the crash-recovery section, which re-invokes it after an operator-selected checkpoint is restored and BEFORE the task cursor and phase execution are restored. NO_HARNESS and UNRESOLVED both halt before partition and remain DISTINCT state, exit-code and reason-code classifications; neither is silently treated as ready.

PARITY CRITERIA PAR-1..PAR-6b govern the mirror: PAR-1 heading text and level match; PAR-2 identical ordinal position within the parent section; PAR-3 non-placeholder lines byte-identical after canonicalization; PAR-4 every uppercase placeholder rendered; PAR-5 no placeholder syntax survives; PAR-6a manifest checksum equals the SHA-256 of the mirror's canonical bytes; PAR-6b the manifest entry names the authoritative template the mirror was rendered from.

ROLLBACK REQUIRES FRESH, LIVE, SHA-BOUND OPERATOR APPROVAL. Rollback is a git revert of this single activation commit. Before the revert command is issued, the agent MUST obtain a fresh live operator approval that NAMES THE EXACT ACTIVATION COMMIT SHA, and MUST re-validate that approval in the moment the command is about to run. An approval obtained earlier in the session, or for any other SHA, IS NOT AN APPROVAL. If the operator is unreachable, dark or AFK, the agent HALTS AND REPORTS and does not revert. Proceeding without that approval is a P-005 violation. THERE IS NO UNCONDITIONAL REVERT PATH.

Blocked by 181.004-T.

REVIEW STATE IS POINTER-ONLY. The verdict manifest docs/reviews/2026-09-18-ship-harness-lifecycle-foundation-plan-review.md is the SOLE AUTHORITY for this unit's review state. This record asserts NO verdict, NO attempt number, NO publication eligibility and NO finding closure, and any such value read from this record is stale by construction. Governing plan docs/plans/2026-09-18-ship-harness-lifecycle-foundation-plan.md (plan_id ship-harness-lifecycle-foundation, plan_role active, revision 10).
