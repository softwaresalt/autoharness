---
artifact_type: task
created_at: 2026-09-19 02:54:43.919636+00:00
custom_fields:
  complexity: high
  size: M
  size_ruleset_version: 2h-rule-v1
  size_source: agent
dependencies:
- 181.004-T
id: 181.005-T
labels:
- harness-surface:harness-architect
- ship-lifecycle
- activation
- manifest-parity
parent_id: 181-F
priority: medium
status: queued
title: 'P4 T7 (ACTIVATE): wire harness generation and crash-resume recomputation into Ship template and installed mirror'
updated_at: 2026-09-21 03:01:57.371268+00:00
---

P4 T7 (ACTIVATE). Size M, complexity high. Blocked by 181.004-T. Final task of the chain.

EXACTLY THREE COMMIT FILES, ONE REFRESHED SHIP ENTRY - the D11 manifest-parity rule, preserved verbatim:
  1. templates/agents/_ship.agent.md.tmpl
  2. .github/agents/_ship.agent.md
  3. .autoharness/harness-manifest.yaml
templates/agents/ship.md.tmpl AND .github/agents/ship.md DO NOT EXIST. The revision-10 paths were wrong and every occurrence is corrected. No fourth file, no second manifest entry, no new artifacts entry.

THE ACTIVATION IS ASYMMETRIC, because the two files are not in the same state. (a) TEMPLATE - UPDATE IN PLACE. `### Step 2: Harness Generation (P-002 / P-004)` ALREADY EXISTS and occurs exactly once. Its body is replaced with the canonical fixture content. NO HEADING IS ADDED AND NOTHING IS RENUMBERED. (b) MIRROR - INSERT ONE SECTION. The installed mirror has NO harness-generation section; it runs `### Step 1: Pre-Flight Checks` straight into `### Step 2: Task Execution Loop`. Insert EXACTLY ONE new section `### Step 1.5: Harness Generation (P-002 / P-004)` immediately BEFORE `### Step 2: Task Execution Loop`. Step 1.5 is chosen because the mirror ALREADY uses decimal sub-steps (0.0, 0.1, 0.1b, 0.1c, 0.1d, 0.5) so it is idiomatic, and because `### Step 1.5` currently occurs ZERO times so there is no collision. NOTHING IS RENUMBERED and `### Step 2: Task Execution Loop` keeps its exact text and its count of one.

CRASH-RESUMPTION, BOTH FILES. Each file has EXACTLY ONE `### Crash-Resumption / Startup Recovery Protocol (fail-closed, owner-exclusive)` section; update BOTH. The Ship-only restore sequence is inserted in exactly five ordered steps: load and validate the checkpoint payload; require shipment_id, halting when absent; RECOMPUTE the resolver from the current workspace; halt on exit 1 or 2; only then restore cursor, phase and next-action intent. RECOMPUTATION PRECEDES CURSOR RESTORATION, so a checkpoint can never resume execution into a workspace whose harness has since been removed. No stored token, cached state, prior verdict or recorded digest ever authorizes execution.

SHIP ONLY. The text is inserted into SHIP SURFACES and describes SHIP BEHAVIOUR. Stage has no harness-generation surface, so NO STAGE BEHAVIOUR IS CLAIMED ANYWHERE - revision 10's promise of a Stage obligation was unimplementable and is removed.

MANIFEST. Refresh the checksum of the SINGLE EXISTING entry whose path is .github/agents/_ship.agent.md so it matches the new installed bytes. THAT ENTRY'S template FIELD IS THE LITERAL SENTINEL 'global agent definition', NOT a .tmpl path; LEAVE IT EXACTLY AS IT IS. Do not add an entry, do not remove one, do not touch the top-level variables_used mapping, do not touch any other entry.

THEN RUN, IN THIS ORDER: the targeted wiring tests `PYTHONPATH=src python -m unittest tests.test_ship_harness_activation`, which must now be GREEN having been proven RED in 181.004-T; then the canonical whole suite `python -m unittest discover -s tests`, which must show no regression against the 181.004-T baseline.

SAFETY MODE: CAREFUL + FREEZE-SCOPE. Only these three paths may change. Any diff outside them is a scope breach and halts the task.

ROLLBACK IS APPROVAL-GATED AND NEVER UNCONDITIONAL. `git revert` MAY ONLY BE EXECUTED after a FRESH, LIVE operator approval obtained AT THE TIME OF THE FAILURE and BOUND TO THE EXACT COMMIT SHA BEING REVERTED, copied verbatim into the task record before the command runs. A prior approval, a blanket approval, an approval naming a different SHA, or an approval predating the failure IS NOT VALID. Absent that approval the task HALTS and reports; it does not revert, does not retry and does not partially undo.

REVIEW STATE IS POINTER-ONLY. The verdict manifest docs/reviews/2026-09-18-ship-harness-lifecycle-foundation-plan-review.md is the SOLE AUTHORITY for this unit's review state. This record asserts NO verdict, NO attempt number, NO publication eligibility and NO finding closure. Governing plan docs/plans/2026-09-18-ship-harness-lifecycle-foundation-plan.md (plan_id ship-harness-lifecycle-foundation, plan_role active, revision 11). Governing decision docs/decisions/2026-09-18-shared-execution-architecture-and-portfolio-reslicing-decision.md (revision 8). Source stash 76EBDE6D.
