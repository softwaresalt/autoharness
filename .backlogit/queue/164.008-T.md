---
artifact_type: task
created_at: 2026-09-09T23:15:03.6920861Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 164.004-T
    - 164.005-T
    - 164.006-T
    - 164.007-T
id: 164.008-T
labels:
    - dogfood-parity
    - manifest
    - checksum
parent_id: 164-F
priority: medium
status: queued
title: Dogfood mirror parity and harness manifest checksum refresh
updated_at: 2026-09-09T23:15:03.6920861Z
---

Reconcile installed dogfood mirrors against their templates for every surface changed by this feature, then refresh .autoharness/harness-manifest.yaml checksums for every changed tracked .github artifact, and ADD a new manifest entry for the new instruction .github/instructions/shipment-documentation-audit.instructions.md (path / primitive / template / checksum / note).

CHECKSUM CONTRACT (verified, non-negotiable): SHA-256 over RAW LF BYTES, computed from the LF-normalized committed git blob via `git cat-file -p :<path>` for the staged index or `HEAD:<path>` post-commit. NEVER compute from a raw Windows working-tree read -- CRLF would silently produce a wrong checksum. This gotcha is already recorded in docs/compound and in the manifest's own notes.

WIDTH-ISOLATION CONSTRAINT: for .github/agents/_ship.agent.md, mirror only the regions this feature changed. That file carries substantial pre-existing template drift (835 installed vs 1111 template lines) unrelated to this work; a full re-render would sweep in unrelated changes and is explicitly out of scope. Record the width-isolation rationale in the manifest note, consistent with existing precedent for this file.

Also record in each touched note WHICH task changed the file and why, so the manifest remains an accurate provenance record (this is itself audit category 6, ownership/role attribution). Width isolation: parity reconciliation and manifest maintenance only -- no new behavioural content.