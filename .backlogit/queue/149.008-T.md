---
artifact_type: task
created_at: 2026-08-28T04:02:27.6374914Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 149.005-T
    - 149.006-T
    - 149.007-T
id: 149.008-T
labels:
    - S1
    - cli
    - gate
parent_id: 149-F
priority: medium
references:
    - docs/plans/2026-08-27-pre-review-detector-sdk-plan.md
    - src/autoharness/cli.py
status: queued
title: U8 — gate pre-review CLI wiring
updated_at: 2026-08-28T04:02:27.6374914Z
---

Wire `autoharness gate pre-review [--json] [--base <ref>]` into the gate dispatch, following the dag-readiness command shape including degraded-payload synthesis. Exit 0 always; 2 for invalid args or invalid registry. Domain: CLI. Files: 1.

<!-- BEGIN:acceptance-criteria -->
* `autoharness gate pre-review` runs; `--json` emits parseable JSON; `--base <ref>` is honored.
* `--help`/`-h` prints `GATE_USAGE` and returns without parsing.
* **Exit codes: 0 always; 2 for invalid arguments OR invalid registry.** Nothing else.
* Degraded path synthesizes a literal payload and never fabricates a graph.
* The gate is **read-only** — it performs no backlogit or git mutation.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan unit **U8**. Edit `src/autoharness/cli.py`.

* Register `pre-review` in the `_gate_command` dispatch (L351-L374).
* Add `_gate_pre_review_command(rest)` and `_parse_gate_pre_review_args`.
* Extend `GATE_USAGE` with the new subcommand and its options.
* Flags: `--json`, `--base <ref>`; `--help`/`-h` prints usage and returns **before** parsing.

Follow the `_gate_dag_readiness_command` shape (L944+) **exactly**, including its **degraded-payload synthesis** precedent — on a degraded path, construct the payload literally and **never fabricate a graph**.

**Execution posture**: characterization-first against the existing `dag-readiness` CLI tests.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
**Depends on U3, U5, U6, U7.**

**INV-3**: the report emitter has no return path into the exit code. Exit 0 is returned even when `ART-01` reports `failed` — this is the report-only guarantee and U11 asserts it.

Agent-Native Parity Reviewer: `--json` gives agents a machine-readable path at parity with the human report; the gate adds no agent-facing authority.

Width isolation: CLI-only unit. Do not add detector logic here.
<!-- END:implementation-notes -->