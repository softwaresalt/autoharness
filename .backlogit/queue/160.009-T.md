---
artifact_type: task
created_at: 2026-09-03T08:25:45.7261535Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 160.009-T
labels:
    - tests
    - cross-environment
    - deploy
parent_id: 160-F
priority: medium
status: queued
title: Cross-environment register-phase tests against the trimmed payload
updated_at: 2026-09-03T08:25:45.7261535Z
---

Verify cross-environment registration still succeeds against the trimmed payload.

TEST: test_register_phase_all_environments (AC7) — the deploy-harness.ps1 / deploy-harness.sh `register` phase succeeds for each supported environment against the trimmed payload:
- vscode
- copilot-cli
- claude
- codex

RATIONALE: the manifest ships only a SUBSET of scripts/ (deploy-harness.*, acquire_lock.*, release_lock.*, ci-topology-check.sh). If registration for any environment depends on a script or artifact the manifest excluded, it fails at consumer install time rather than in CI (risk R1). This test is the guard.

PRESERVED LIMITATION (not a regression): plugin-only consumers without the Python CLI still cannot run `setup-vscode`, `verify-workspace`, or register with Claude Code / Codex. This is the documented pre-existing state (docs/installation.md) and excluding src/ from the plugin channel preserves it rather than worsening it. Assert the limitation is unchanged, not newly introduced.

ACCEPTANCE: AC7.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T9)