---
artifact_type: task
created_at: 2026-09-03T08:25:45.7261535Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.001-T
    - 160.005-T
    - 160.016-T
id: 160.009-T
labels:
    - tests
    - cross-environment
    - deploy
parent_id: 160-F
priority: medium
status: queued
title: Cross-environment registration support matrix (channel x environment) against the trimmed payload
updated_at: 2026-09-03T17:34:36.7035727Z
---

CROSS-ENVIRONMENT REGISTRATION SUPPORT MATRIX (T12), under an EXPLICIT channel x environment contract. Authored and observed against the BASELINE build, then re-observed against the trimmed build by the transition ledger (160.017-T).

SEQUENCING (machine-encoded, not prose). This task is BLOCKED BY 160.001-T (baseline capture), 160.005-T (RED-FIRST harness) and 160.016-T (CHARACTERIZATION harness), and it BLOCKS 160.006-T and 160.007-T. The wiring tasks cannot begin until this family has authored its cases and recorded its baseline observations. A verification family that first exists after the behaviour it verifies cannot distinguish "this still works" from "this never worked".

TWO-CLASS CONTRACT (plan section Test strategy). Every case MUST DECLARE ITS CLASS IN THE TEST FILE ITSELF, PER CASE. THIS FAMILY IS MIXED - three CHARACTERIZATION cases and one RED-FIRST case - so there is NO family-wide class label and none may be introduced.
- RED-FIRST: asserts behaviour that DOES NOT YET EXIST. Observed FAILING on the baseline, passing after implementation.
- CHARACTERIZATION: asserts an EXISTING, PRESERVED behaviour that CORRECTLY PASSES ON THE BASELINE. Observed GREEN on the untrimmed baseline and GREEN AGAIN after trimming. NOT required to be red, and MUST NOT be weakened or inverted to manufacture one.
- If a CHARACTERIZATION case goes RED after the wiring tasks land, that is a REGRESSION CAUSED BY THE WIRING TASK, and the wiring task - not this one - is at fault.

BASELINE OBSERVATION IS A DELIVERABLE, not a side effect. Record, per case, the class and the observed baseline result against 160.001-T's artifacts - in particular the recorded v1.5.0 plugin-only registration failure modes - loading the structured (non-prose) half from tests/fixtures/payload-baseline/**.

WHY THE MATRIX IS TWO-DIMENSIONAL: registration support is a function of (channel, environment), not of environment alone. A flat list of "supported environments" plus a prose footnote that plugin-only consumers cannot use three of them mixes two channels' registration surfaces into one undifferentiated set, and cannot be asserted.

SUPPORT MATRIX (AC7). Rows are registration targets; columns are install channels. Every cell is asserted, positively or negatively.

| Registration target | pip / clone (Python CLI present) | plugin-only (no Python CLI) |
|---|---|---|
| `deploy-harness register --vscode` / `setup-vscode` | SUPPORTED | NOT SUPPORTED (pre-existing) |
| `deploy-harness register --copilot-cli` | SUPPORTED | SUPPORTED - the plugin channel's own target |
| `deploy-harness register --claude` / `setup-claude` | SUPPORTED | NOT SUPPORTED (pre-existing) |
| `deploy-harness register --codex` / `setup-codex` | SUPPORTED | NOT SUPPORTED (pre-existing) |
| `autoharness verify-workspace` | SUPPORTED | NOT SUPPORTED (pre-existing) |

EVIDENCE FOR THE "pre-existing" CLAIM: docs/installation.md states that the plugin path "gives Copilot CLI users built-in versioning and update management with no Python dependency" and that "the Python CLI is still needed for setup-vscode, verify-workspace, and registering with Claude Code or Codex." Excluding src/ from the plugin channel PRESERVES that documented state; it does not create it.

THE FOUR CASES, EACH WITH ITS OWN CLASS AND GREEN/PRESERVATION OWNER:

1. test_register_phase_all_environments_python_channel (AC7) - CLASS: CHARACTERIZATION. OBSERVED INITIAL STATE: GREEN on the baseline - all four Python-channel registration targets already succeed. ASSERTS: the deploy-harness.ps1 / deploy-harness.sh `register` phase succeeds for vscode, copilot-cli, claude and codex with the Python CLI present. BOTH script variants are covered; a PowerShell-only or POSIX-only pass is not evidence for the other. PRESERVATION OWNER: 160.006-T.

2. test_plugin_only_register_copilot_cli_succeeds (AC7) - CLASS: CHARACTERIZATION. OBSERVED INITIAL STATE: GREEN on the baseline - the one supported plugin-channel cell already succeeds. ASSERTS: that cell positively. PRESERVATION OWNER: 160.007-T.

3. test_plugin_only_unsupported_targets_fail_the_same_way_as_baseline (AC7) - CLASS: CHARACTERIZATION. OBSERVED INITIAL STATE: GREEN on the baseline - the four limitations are pre-existing and documented, and the case compares the baseline against its OWN recorded failure modes. ASSERTS: for vscode, claude, codex and verify-workspace, the plugin-only environment fails AND the failure is IDENTICAL IN KIND to the v1.5.0 untrimmed baseline's failure in the same environment. The assertion is "UNCHANGED", not merely "fails": a test that only asserts failure cannot distinguish a preserved limitation from a regression this shipment introduced. PRESERVATION OWNER: 160.007-T.

4. test_plugin_channel_excludes_python_cli_by_design (AC7) - CLASS: RED-FIRST. OBSERVED INITIAL STATE: RED by ASSERTION FAILURE. `marketplace.json` declares `source: "."`, so `src/` and `pyproject.toml` ARE present in the baseline plugin payload; the assertion that they are absent fails today. THE CLASS WAS CORRECTED IN REVIEW-FIX CYCLE 3: this record previously declared the whole family CHARACTERIZATION, which was false of this case - it asserts the trimmed state, which does not yet exist. ASSERTS: `src/` and `pyproject.toml` are ABSENT from the resolved plugin payload, so the unsupported cells are unsupported for the DECLARED reason (no Python distribution) rather than by accident. GREEN OWNER: 160.007-T.

RATIONALE FOR THE MATRIX: the manifest ships only a SUBSET of scripts/ (deploy-harness.*, acquire_lock.*, release_lock.*, ci-topology-check.sh). If registration for any supported cell depends on a script or artifact the manifest excluded, it fails at consumer install time rather than in CI (risk R1). This matrix is the guard.

SCOPE BOUNDARY: this task asserts REGISTRATION support per channel. It does NOT define or test the `_DATA_DIR` / plugin-root RESOLUTION contracts - those are 160.013-T (AC6a / AC6b). Consume those contracts; do not restate them.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. Verification-only. Registration tests run against scratch profiles under a gitignored temporary path and MUST NOT modify the developer's real VS Code settings, Claude Code config, Codex config, or Copilot CLI plugin registry. `src/autoharness/` and the deploy-harness scripts are READ-ONLY for this task - if a supported cell fails, the fix belongs to the manifest/wiring tasks, not here. BASELINE-FIDELITY CHECKPOINT: CHARACTERIZATION cases are observed green against the BASELINE build from 160.001-T, never against a partially-changed tree. ITS ONLY AUTHORIZED WRITE OUTSIDE ITS OWN TEST FILES IS ITS FIRST-OBSERVATION RECORD at docs/audits/2026-09-03-ship10-payload-evidence/observations/T12.json (see FIRST-OBSERVATION DESTINATION below); this is stated here, in the operative write-surface clause, because a destination named only further down the record would still read as unauthorized under freeze-scope (extended review-fix cycle 4, finding 10). NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

BOUNDED RAW LOG - SECOND AUTHORIZED EVIDENCE WRITE (added in review-fix cycle 5, findings 10 and 15; local field reference removed in cycle 8, item 7). Every observation this task records is backed by a RAW LOG this task also writes, into ITS OWN SUBDIRECTORY AND NOWHERE ELSE: docs/audits/2026-09-03-ship10-payload-evidence/observations/logs/T12/**. This path is authorized in the plan's H2 writable-surface table, so writing it does NOT violate freeze-scope, and 160.017-T can verify the evidence without following a reference outside the single audit directory. WHY THE LOG IS REQUIRED: cycle 4 had 160.017-T accept each producer's SELF-DECLARED outcome, which verifies nothing - a task that never ran would be indistinguishable from a clean pass. The log is the artifact that makes the claim checkable. The log also carries the source-provenance capture the plan's canonical contract requires before the test command runs, and the observation is taken on a CLEAN, FULLY COMMITTED TREE.
BOUNDS (binding, enforced by 160.017-T): PER LOG <= 256 KiB AND <= 2,000 lines - a run exceeding either limit is truncated head-and-tail with an explicit truncation marker (how the record signals truncation and its pre-truncation counts is defined in the plan's canonical contract, not here); PER CASE at most 3 logs (first observation, second observation, one retry); PER SHIPMENT at most 200 files under the logs root, none of them binary; RETENTION until 160.017-T completes and the shipment closes.
OBSERVATION-RECORD CONTENT IS DEFINED ONCE, IN THE PLAN, AND IS NOT RESTATED HERE (cycle 6, finding 23; TIGHTENED IN CYCLE 8, ITEM 7). The SINGLE CANONICAL definition is the field table in docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section "Evidence-verification contract for 160.017-T". Seven task records each carried their own restatement and the copies had ALREADY DIVERGED, so a producer reading only its own record would have written a record 160.017-T rejects. THIS RECORD STATES ONLY WHAT IS TASK-SPECIFIC: which cases it produces records for, and which log subdirectory is its own. THIS RECORD ENUMERATES NO FIELD, NAMES NO FIELD, AND DEFINES NO FIELD TYPE OR VALIDATION RULE - NOT EVEN PARTIALLY. Cycle 7 deleted the "ADDITIONAL RECORD FIELDS" restatement but left a second local enumeration standing under a different heading, which was the same defect wearing a different label; cycle 8 deletes that one too. A PARTIAL COPY DRIFTS FROM THE CANONICAL TABLE EXACTLY AS A FULL COPY DOES. FOR EVERY FIELD, ITS TYPE, AND ITS ACCEPTANCE RULE, READ THE PLAN'S CANONICAL TABLE - there is no second source. 160.017-T verifies each record INDEPENDENTLY against its evidence and REJECTS any record that is missing a field, malformed, or inconsistent; it does not warn and does not skip.
This task WRITES its own JSON and its own logs and NOTHING ELSE outside its test files; it MUST NOT write into another producer's subdirectory, and MUST NOT write the ledger.

ACCEPTANCE: AC7 - every cell of the matrix asserted, with the four NOT-SUPPORTED cells asserted as UNCHANGED versus the v1.5.0 baseline. All four cases authored with a per-case declared class; the three CHARACTERIZATION cases observed and recorded GREEN on the baseline; the one RED-FIRST case observed and recorded RED by assertion failure.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size S, complexity medium. Four cases over one matrix and two script variants; the failure-mode comparison against recorded baseline modes is the only non-mechanical part.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T12)

FIRST-OBSERVATION DESTINATION (authorized, binding; added in extended review-fix cycle 4, finding 7). This task AUTHORS cases and records their FIRST observation, and that observation is a NON-DEFERRABLE acceptance condition consumed by 160.017-T's transition ledger. Review-fix cycle 3 authorized 160.002-T's spike record, 160.001-T's baseline, the pre-change byte captures (SIX in the current contract: three owned by 160.020-T, one each by 160.006-T, 160.007-T and 160.015-T) and 160.017-T's ledger, but NOT the first observations of the authoring tasks - so under freeze-scope this task had NO LEGAL DESTINATION FOR ITS OWN MANDATORY DELIVERABLE, the same class of self-contradiction cycle 2 fixed for schema_contracts.py and cycle 3 fixed for the baseline. A red recorded only in a transcript is uncitable, and 160.017-T cannot verify a transition pair whose first half does not durably exist.
DESTINATION: docs/audits/2026-09-03-ship10-payload-evidence/observations/T12.json - a subdirectory of the SINGLE bounded evidence surface already authorized for this shipment. NO NEW STORE, no new tool, no new convention.
RECORD CONTENT IS NOT SPECIFIED HERE (CYCLE 8, ITEM 7). The observation-record field set, every field's type, and every acceptance rule are defined ONCE, in the plan's canonical Evidence-verification contract (docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md, section "Evidence-verification contract for 160.017-T"). READ THAT CONTRACT; there is no second source and no local list. This record contributes ONLY its exact case set (the four cases enumerated above), its destination path, and its log bounds.
CONSUMPTION: 160.017-T joins these records to the canonical case table, verifies each independently against its evidence, and rejects any record that is missing, malformed or inconsistent - all exactly as the canonical contract states, and not restated here. A ledger row with no record, or a record with no ledger row, FAILS THE SHIPMENT; this is what makes verification V6 a machine check rather than a promise.
BOUND: records are APPEND-ONLY within the shipment and written ONLY by this task. No task writes another task's record, and 160.017-T READS them without modifying them. This is the ONLY write this task performs outside tests/**.
