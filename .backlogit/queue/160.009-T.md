---
artifact_type: task
created_at: 2026-09-03T08:25:45.7261535Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.001-T
    - 160.005-T
    - 160.016-T
id: 160.009-T
labels:
    - tests
    - cross-environment
    - deploy
parent_id: 160-F
priority: medium
status: queued
title: Cross-environment registration support matrix (channel x environment) against the trimmed payload
updated_at: 2026-09-03T10:29:06.8384448Z
---

=== REVIEW-FIX CYCLE 2 RE-SCOPE (finding 1) - READ FIRST, IT CHANGES WHEN THIS TASK RUNS ===

WHAT CHANGED: cycle 1 sequenced this task AFTER the wheel wiring (160.006-T) and the plugin wiring (160.007-T), so its cases were authored against an ALREADY-CHANGED payload and had NO RED OWNER ANYWHERE. That is two defects in one edge. (a) A verification family that first exists after the behaviour it verifies cannot distinguish "this still works" from "this never worked" - there is no prior observation to compare against. (b) It let install, upgrade, resolver, registration and integrity behaviour change with no test having been observed in a known state beforehand, which the shipment's test-first contract forbids.

THE EDGE IS NOW REVERSED. This task is BLOCKED BY 160.001-T (baseline capture), 160.005-T (RED-FIRST harness) and 160.016-T (CHARACTERIZATION harness), and it BLOCKS 160.006-T and 160.007-T. The wiring tasks cannot begin until this family has authored its cases and recorded its baseline observations. This is enforced by machine dependency edges, not by prose.

TWO-CLASS CONTRACT (plan section Test strategy). Every case this task authors MUST DECLARE ITS CLASS in the test file itself. The classes are mutually exclusive and exhaustive; a case declaring neither or both is a failure detected by the transition ledger (160.017-T).
- RED-FIRST: the case asserts behaviour that DOES NOT YET EXIST. It must be observed FAILING on the baseline before the implementing task begins, and observed passing after. Required observation pair: (red on baseline, green after implementation).
- CHARACTERIZATION: the case asserts an EXISTING, PRESERVED behaviour or invariant that CORRECTLY PASSES ON THE BASELINE. It is observed GREEN on the untrimmed baseline and must be observed GREEN AGAIN after trimming. Required observation pair: (green on baseline, green after change). A CHARACTERIZATION case is NOT required to be red, and MUST NOT be weakened, inverted, or otherwise broken to manufacture a red observation. Manufactured red is worse than no red: it destroys the signal for cases where red is real.
- If a CHARACTERIZATION case goes RED after the wiring tasks land, that is a REGRESSION CAUSED BY THE WIRING TASK, and the wiring task - not this one - is at fault.

BASELINE OBSERVATION IS A DELIVERABLE OF THIS TASK, not a side effect. Record, per case, the class and the observed baseline result, against the baseline artifacts captured by 160.001-T (untrimmed wheel, untrimmed sdist, untrimmed plugin payload, recorded verify-workspace finding set, recorded v1.5.0 registration failure modes). An observation with no recorded baseline reference cannot be replayed and does not satisfy this task.

=== ORIGINAL TASK BODY (unchanged except where the re-scope above overrides sequencing) ===
CLASS ASSIGNMENT FOR THIS FAMILY: the channel x environment registration matrix is CHARACTERIZATION. The v1.5.0 plugin-only registration limitations are EXISTING baseline facts recorded by 160.001-T; the matrix asserts they are UNCHANGED by trimming, not that they are newly correct. Observe green on the baseline, green again after trimming. Any cell that is genuinely NEW behaviour must instead be authored as RED-FIRST by 160.005-T and named here explicitly - it must not be smuggled in as characterization.

Verify cross-environment registration against the trimmed payload, under an EXPLICIT channel x environment support matrix.

WHY THIS WAS REWRITTEN (Orchestrator review-fix cycle 1, finding 5): the original task listed vscode / copilot-cli / claude / codex as a flat set of "supported environments" and then, in prose, noted that plugin-only consumers cannot use three of them. That mixes two different channels' registration surfaces into one undifferentiated list. Registration support is a function of (channel, environment), not of environment alone, and it must be asserted as such.

SUPPORT MATRIX (AC7). Rows are registration targets; columns are install channels. This matrix is the acceptance contract - every cell is asserted, positively or negatively.

| Registration target | pip / clone (Python CLI present) | plugin-only (no Python CLI) |
| deploy-harness register --vscode / setup-vscode | SUPPORTED | NOT SUPPORTED (pre-existing) |
| deploy-harness register --copilot-cli | SUPPORTED | SUPPORTED - this is the plugin channel's own target |
| deploy-harness register --claude / setup-claude | SUPPORTED | NOT SUPPORTED (pre-existing) |
| deploy-harness register --codex / setup-codex | SUPPORTED | NOT SUPPORTED (pre-existing) |
| autoharness verify-workspace | SUPPORTED | NOT SUPPORTED (pre-existing) |

EVIDENCE FOR THE "pre-existing" CLAIM: docs/installation.md states that the plugin path "gives Copilot CLI users built-in versioning and update management with no Python dependency" and that "the Python CLI is still needed for setup-vscode, verify-workspace, and registering with Claude Code or Codex." Excluding src/ from the plugin channel PRESERVES that documented state; it does not create it.

POSITIVE TESTS (pip/clone column):
- test_register_phase_all_environments_python_channel (AC7) - the deploy-harness.ps1 / deploy-harness.sh `register` phase succeeds for vscode, copilot-cli, claude, and codex against the trimmed payload with the Python CLI present. BOTH script variants are covered; a PowerShell-only or POSIX-only pass is not evidence for the other.

NEGATIVE ASSERTIONS (plugin-only column) - MANDATORY, not optional prose:
- test_plugin_only_register_copilot_cli_succeeds - the one supported plugin-channel cell, asserted positively.
- test_plugin_only_unsupported_targets_fail_the_same_way_as_baseline - for vscode, claude, codex, and verify-workspace, assert the plugin-only environment fails, AND assert the failure is IDENTICAL IN KIND to the v1.5.0 untrimmed baseline's failure in the same environment. The assertion is "UNCHANGED", not merely "fails". A test that only asserts failure cannot distinguish a preserved limitation from a regression that this shipment introduced.
- test_plugin_channel_excludes_python_cli_by_design - assert src/ and pyproject.toml are absent from the resolved plugin payload, so the unsupported cells are unsupported for the DECLARED reason (no Python distribution) rather than by accident.

RATIONALE: the manifest ships only a SUBSET of scripts/ (deploy-harness.*, acquire_lock.*, release_lock.*, ci-topology-check.sh). If registration for any supported cell depends on a script or artifact the manifest excluded, it fails at consumer install time rather than in CI (risk R1). This matrix is the guard.

SCOPE BOUNDARY: this task asserts REGISTRATION support per channel. It does NOT define or test the `_DATA_DIR` / plugin-root RESOLUTION contracts - those are 160.013-T (AC6a / AC6b). Consume that task's contracts; do not restate them.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. Verification-only. Registration tests run against scratch profiles under a gitignored temporary path and MUST NOT modify the developer's real VS Code settings, Claude Code config, Codex config, or Copilot CLI plugin registry. `src/autoharness/` and the deploy-harness scripts are READ-ONLY for this task - if a supported cell fails, the fix belongs to the manifest/wiring tasks, not here. NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

ACCEPTANCE: AC7 - every cell of the matrix asserted, with the four NOT-SUPPORTED cells asserted as UNCHANGED versus the v1.5.0 baseline.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T12)