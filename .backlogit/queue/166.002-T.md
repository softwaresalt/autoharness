---
artifact_type: task
created_at: 2026-09-15T22:15:34.9305069Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 166.002-T
labels:
    - test-first
    - characterization
    - topology-gate
parent_id: 166-F
priority: medium
status: queued
title: 'Red-phase tests: engine-inertness classifier cases + flat-manifest doc-contract assertions'
updated_at: 2026-09-16T06:30:09.0669744Z
---

REFRAMED IN PLACE 2026-09-15. Supersedes the TERMINAL_CLOSE red-phase scope. Authoritative plan: docs/plans/2026-09-15-flat-manifest-shipment-closure-plan.md (unit U1). Decision: docs/decisions/2026-09-15-flat-manifest-shipment-closure-deliberation.md.

TEST-FIRST / RED PHASE for the WHOLE feature (Constitution Principle II, NON-NEGOTIABLE). Single red-phase entry point: 166.001-T, 166.003-T and 166.004-T all depend on it; 166.005-T/166.006-T inherit transitively. TESTS DOMAIN ONLY — no classifier, skill, or policy edits (width isolation).

PART A — CLASSIFIER TESTS (tests/test_shipment_closure_classification.py, extend)
A0. EXTEND THE FIXTURE HELPER: _write_artifact currently writes NO `status` field. Add an optional `status: str | None = None` parameter. This is the single most important migration detail — because the helper omits status today, EVERY existing out-of-manifest fixture is non-inert under INV-6 and therefore still yields SAFE_CLOSE. The existing negative suite MUST continue to pass UNCHANGED; migration is purely ADDITIVE.
A1. NEW POSITIVE (the 173-S shape; R03/R07): test_archived_out_of_manifest_child_selects_cascade — root feature + one manifest task + an out-of-manifest child declaring `status: archived` and NO archived_status. EXPECT CASCADE. This is the operator's core requirement: an archived/descoped sibling MUST NOT block closure, and NO disposition note may be demanded of it.
A2. NEW NEGATIVE (spike Arm 2): test_done_but_not_archived_out_of_manifest_child_falls_back_to_safe_close — out-of-manifest child declares `status: done`. EXPECT SAFE_CLOSE (the live engine WOULD archive it).
A3. NEW NEGATIVE (spike Arm 2): test_live_out_of_manifest_child_falls_back_to_safe_close — out-of-manifest child is live `queued`. EXPECT SAFE_CLOSE. Rationale to record in the test docstring: the engine archived exactly this shape while reporting returned_ids: [], so the post-mutation guard does NOT protect it.
A4. LOCATION IS NEVER SUFFICIENT (R08): test_out_of_manifest_child_in_archive_without_status_falls_back_to_safe_close — record sits in archive/ but declares no status. EXPECT SAFE_CLOSE. Inertness is read from the record's own frontmatter `status`, NEVER inferred from queue//archive/ location.
A5. DEPTH: archived out-of-manifest GRANDCHILD -> CASCADE; live out-of-manifest grandchild -> SAFE_CLOSE.
A6. FAIL-CLOSED PRESERVATION: retain/extend coverage that any index build failure, torn record (present in BOTH queue/ and archive/), missing record, or malformed frontmatter yields SAFE_CLOSE, NEVER CASCADE.
A7. EXTRAS NOT WEAKENED (plan-review P0-2): retain coverage that a manifest member which is neither a qualifying root feature nor a descendant of one still forces SAFE_CLOSE. Inert out-of-manifest descendants MUST NOT be absorbed into accounted_ids.
A8. ORDERING IS NOT A GATE (INV-8, plan-review P1-1): a manifest listing the covering feature FIRST (the shape of every historical manifest, including 173-S) must still classify CASCADE.

PART B — DOC-CONTRACT TESTS (new module tests/test_flat_manifest_closure_docs.py; plan-review P1-2)
Assert on BOTH the installed artifact AND its template mirror (.github/policies/workflow-policies.md + templates/policies/workflow-policies.md.tmpl; .github/skills/shipment-reconcile/SKILL.md + templates/skills/shipment-reconcile/SKILL.md.tmpl) that:
- INV-1..INV-10 are present and normative;
- closure scope is stated as the manifest's explicit item IDs PLUS the shipment record, with ancestry explicitly excluded from expanding it;
- multi-shipment feature delivery is permitted and the feature item is the FINAL manifest entry of the FINAL shipment (INV-4);
- excluded/removed/descoped/rejected/merged items never block closure on the ground of ancestry, and NO disposition note is required (INV-3);
- manifest ORDERING is stated as a Stage assembly convention and explicitly NOT a closure precondition (INV-8);
- the returned_ids guard is explicitly recorded as INSUFFICIENT on its own, citing the measured empty-returned_ids destructive case (R13);
- the RECONCILE_FAIL_NO_SAFE_RECORD_TRANSITION fail-closed halt exists with its explicit no-substitution prohibition (R18);
- NEGATIVE ASSERTIONS: the tokens `TERMINAL_CLOSE` and the phrase `fully covered` no longer appear as normative preconditions on any of the four surfaces; clearing parent_id remains explicitly rejected.

ACCEPTANCE: fixtures are declarative and shape-derived; NO test asserts on a literal feature or shipment ID; every new case fails RED against current main for the right reason; the pre-existing ShipmentClosureClassificationTests suite passes UNCHANGED. Canonical invocation: `python -m unittest discover -s tests`.

SIZE: M. COMPLEXITY: medium.