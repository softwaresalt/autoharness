---
artifact_type: task
created_at: 2026-09-15T22:15:34.9305069Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 166.002-T
labels:
    - test-first
    - characterization
    - topology-gate
parent_id: 166-F
priority: medium
status: queued
title: 'Red-phase tests: engine-inertness classifier cases + flat-manifest doc-contract assertions'
updated_at: 2026-09-16T08:32:24.2914312Z
---

REFRAMED IN PLACE 2026-09-15; CORRECTED 2026-09-16 (plan revision 2); CORRECTED AGAIN 2026-09-16 (plan revision 3 — red-matrix, evidence-class and fixture-encoding blockers); CORRECTED AGAIN 2026-09-16 (plan REVISION 4 — P1 group A test-granularity contradiction, group B evidence-honesty propagation, group C historical-evidence correctness, group D constitutional containment). Authoritative plan: docs/plans/2026-09-15-flat-manifest-shipment-closure-plan.md (unit U1, REVISION 4). Decision: docs/decisions/2026-09-15-flat-manifest-shipment-closure-deliberation.md (REVISION 4; D1b, D7a, D7b, D10, D11, D16, D17).

DEPENDENCIES (machine DAG, authoritative): NONE. This task is the red-phase entry point for the whole feature.

TEST-FIRST / RED PHASE for the WHOLE feature (Constitution Principle II, NON-NEGOTIABLE). Single red-phase entry point: 166.001-T, 166.003-T and 166.004-T all depend on it; 166.005-T/166.006-T inherit transitively. TESTS DOMAIN ONLY — no classifier, skill, or policy edits (width isolation).

=== RED-PHASE EXIT CRITERIA — REWRITTEN 2026-09-16 (plan revision 3, decision D11) ===
THE REVISION-2 THREE-CLASS SCHEME WAS STILL UNSATISFIABLE AND IS WITHDRAWN. It designated A1-A5, A9 and A10 as CLASS 1 "MUST FAIL RED against current main". Measured against the CURRENT src/autoharness/gates/shipment_closure.py, that is FALSE for most of them. The live predicate is:

    missing = tuple(d for d in descendants if d not in manifest_id_set)
    if missing:   # -> SAFE_CLOSE, reason naming the offending IDs

So EVERY out-of-manifest descendant — live, done, Archived, ARCHIVED, " archived ", torn, or status-less — ALREADY yields SAFE_CLOSE WITH THE OFFENDING ID NAMED IN `reason`. A2, A3, A4, the LIVE HALF of A5, all three A9 variants and both A10 cases are therefore ALREADY GREEN ON VERDICT. Demanding they go red would force an author to weaken a correct test — the exact failure mode revision 2 claimed to fix, reintroduced one level down.

Classify EVERY test into EXACTLY ONE of FOUR classes and declare the class in the test's docstring:

*** GRANULARITY RULE — NEW IN REVISION 4 (NON-NEGOTIABLE, plan D16/H19) ***
CLASS IS GATED AT TEST-FUNCTION GRANULARITY. Revision 3 demanded "exactly one class per test" while simultaneously assigning CLASS 4 (must start GREEN) to the verdict assertion and CLASS 1 (must start RED) to the reason-text assertion INSIDE THE SAME NAMED TEST, for A2, A3, all three A9 variants and A10. THAT IS A DIRECT CONTRADICTION AND IS UNSATISFIABLE: one test function produces ONE pass/fail result, so it cannot be simultaneously required to start green and to start red. It also DESTROYS the CLASS 4 before/after green pair, which is the ENTIRE containment proof (plan H16).
RESOLUTION (plan D16): SPLIT the verdict observable and the diagnostic/reason observable into SEPARATELY NAMED, SINGLE-CLASS test functions. The alternative — defining and gating class at ASSERTION granularity — was considered and REJECTED because a per-test pass/fail is directly observable by `python -m unittest discover -s tests` and by CI, whereas assertion-level attribution would need a bespoke harness (new surface, outside freeze-scope).
BINDING SUB-RULES:
  (i)  A CLASS 4 verdict test asserts the VERDICT ONLY and MUST NOT assert on `reason` text at all (not even a substring, not even a negative match).
  (ii) A CLASS 1 reason test asserts the REASON TEXT ONLY and MUST NOT assert a verdict.
  (iii) The two members of a pair MAY and SHOULD share a single fixture-builder helper, so the split costs fixture duplication of zero.
  (iv) No named test may carry two class labels. A docstring declaring two classes is a DEFECT.

MANDATORY SPLIT TABLE (revision 4) — six CLASS 4 verdict tests and six CLASS 1 reason tests replace the six dual-class tests of revision 3:

| # | CLASS 4 verdict test (start GREEN, stay GREEN) | CLASS 1 reason test (start RED) |
|---|---|---|
| A2  | test_done_but_not_archived_out_of_manifest_child_falls_back_to_safe_close | test_done_but_not_archived_out_of_manifest_child_reason_names_observed_status |
| A3  | test_live_out_of_manifest_child_falls_back_to_safe_close | test_live_out_of_manifest_child_reason_names_observed_status |
| A9a | test_titlecase_archived_out_of_manifest_child_falls_back_to_safe_close | test_titlecase_archived_out_of_manifest_child_reason_names_observed_status |
| A9b | test_uppercase_archived_out_of_manifest_child_falls_back_to_safe_close | test_uppercase_archived_out_of_manifest_child_reason_names_observed_status |
| A9c | test_whitespace_padded_archived_out_of_manifest_child_falls_back_to_safe_close | test_whitespace_padded_archived_out_of_manifest_child_reason_names_observed_status |
| A10 | test_torn_out_of_manifest_descendant_falls_back_to_safe_close | test_torn_out_of_manifest_descendant_reason_is_torn_specific |

FOUR-CLASS MEMBERSHIP (restated under the granularity rule):

- CLASS 1 BEHAVIOUR-CHANGING — asserts a reason or verdict the classifier does NOT yet produce. MUST FAIL RED against current main, each for its OWN SPECIFIC named reason, never an import/collection error. MEMBERS: A1 (archived out-of-manifest child -> CASCADE); A5a (archived out-of-manifest GRANDCHILD -> CASCADE); the SIX reason-only tests in the right-hand column of the split table.
- CLASS 2 DOC-CONTRACT — asserts contract text that does not yet exist. MUST FAIL RED against current main. MEMBERS: all of PART B.
- CLASS 3 CHARACTERIZATION — pins behaviour already correct and untouched by this change. MAY START GREEN; green is correct. MEMBERS: A0, A6, A7, A8.
- CLASS 4 CONTAINMENT-REGRESSION — pins a SAFE_CLOSE VERDICT that is correct today and that the NEW predicate could silently re-widen. MUST START GREEN AND MUST STAY GREEN. A red result here on current main means THE TEST IS WRONG, not the code. MEMBERS: the SIX verdict-only tests in the left-hand column of the split table, plus A4, A5b (live grandchild), test_duplicate_id_within_single_root_falls_back_to_safe_close, and A11's non-str fixtures.

WHY CLASS 4 IS NOT MERELY CLASS 3: a CLASS 3 test pins behaviour this feature does not touch. A CLASS 4 test pins behaviour whose ENTIRE JUSTIFICATION IS BEING REPLACED UNDERNEATH IT — today these shapes are rejected for being OUT OF MANIFEST; after 166.001-T they must be rejected for being NON-INERT. The verdict is identical, but a subtly wrong inertness predicate (a stray .strip(), a missing torn check, a str() coercion) flips them to CASCADE — a DESTRUCTIVE regression. They are the load-bearing guards of this change; their green start is POSITIVE EVIDENCE OF CONTAINMENT, not an absence of evidence.

WHERE THE GENUINE RED SIGNAL LIVES: 166.001-T requires the reason string to name the offending IDs AND THEIR OBSERVED STATUS, and requires a TORN-SPECIFIC containment reason. Current main emits NEITHER — it emits only "has descendants outside the manifest: (...)". Asserting that new text is a REAL, NON-CONTRIVED red observable: it fails today for a precise nameable reason and passes only after the intended change.

PROHIBITED: contriving a failure, weakening an assertion, or reshaping a fixture for the sole purpose of driving a CLASS 3 or CLASS 4 test red is RED-PHASE FALSIFICATION. Mandatory red applies ONLY to genuinely missing or behaviour-changing observables.

GATING: every CLASS 1 and CLASS 2 test must be individually demonstrated RED before 166.001-T/166.003-T/166.004-T begin. Every CLASS 4 test must be individually demonstrated GREEN before 166.001-T begins AND re-run GREEN after it lands — the before/after pair IS the containment proof (plan H16). The pre-existing suite must pass UNTOUCHED.

PART A — CLASSIFIER TESTS (tests/test_shipment_closure_classification.py, extend)
A0. [CLASS 3] EXTEND THE FIXTURE HELPER: _write_artifact currently writes NO `status` field. Add an optional `status` parameter that writes the line VERBATIM as given, so a test can control YAML quoting exactly (see A9's encoding requirement). Because the helper omits status today, EVERY existing out-of-manifest fixture is non-inert under INV-6 and still yields SAFE_CLOSE. The existing negative suite MUST continue to pass UNCHANGED; migration is purely ADDITIVE.
A1. [CLASS 1 — genuinely RED] NEW POSITIVE (the 173-S shape; R03/R07): test_archived_out_of_manifest_child_selects_cascade — root feature + one manifest task + an out-of-manifest child declaring `status: archived` and NO archived_status. EXPECT CASCADE. Current main returns SAFE_CLOSE, so this is a true verdict-mismatch red. This is the operator's core requirement: an archived/descoped sibling MUST NOT block closure, and NO disposition note may be demanded of it.
A2. [SPLIT IN REVISION 4 INTO TWO SINGLE-CLASS TESTS] (a) [CLASS 4 — start green, stay green] test_done_but_not_archived_out_of_manifest_child_falls_back_to_safe_close — out-of-manifest child declares `status: done`. EXPECT SAFE_CLOSE. ASSERTS VERDICT ONLY; MUST NOT assert on `reason`. (b) [CLASS 1 — genuinely RED] test_done_but_not_archived_out_of_manifest_child_reason_names_observed_status — same fixture via the shared builder; asserts `reason` NAMES THE OBSERVED STATUS `done` (not emitted today). ASSERTS REASON ONLY; MUST NOT assert a verdict.
A3. [SPLIT IN REVISION 4] (a) [CLASS 4] test_live_out_of_manifest_child_falls_back_to_safe_close — out-of-manifest child is live `queued`. EXPECT SAFE_CLOSE. VERDICT ONLY. (b) [CLASS 1 — RED] test_live_out_of_manifest_child_reason_names_observed_status — asserts observed-status reason text only. BOTH docstrings MUST record that the engine-archives-while-returning-[] rationale is INDICATIVE AND UNPROVEN ONLY (plan D7a/R24), is NOT a measured fact, is NOT proven by this or any classifier fixture, and CANNOT authorize a CASCADE.
A4. [CLASS 4] LOCATION IS NEVER SUFFICIENT (R08): test_out_of_manifest_child_in_archive_without_status_falls_back_to_safe_close — record sits in archive/ but declares no status. EXPECT SAFE_CLOSE. Inertness is read from the record's own frontmatter `status`, NEVER inferred from queue//archive/ location.
A5. DEPTH, SPLIT BY CLASS: A5a [CLASS 1 — RED] archived out-of-manifest GRANDCHILD -> CASCADE. A5b [CLASS 4 — start green] live out-of-manifest grandchild -> SAFE_CLOSE.
A6. [CLASS 3] FAIL-CLOSED PRESERVATION: retain/extend coverage that any index build failure, missing record, or malformed frontmatter yields SAFE_CLOSE, NEVER CASCADE.
A7. [CLASS 3] EXTRAS NOT WEAKENED (plan-review P0-2): retain coverage that a manifest member which is neither a qualifying root feature nor a descendant of one still forces SAFE_CLOSE. Inert out-of-manifest descendants MUST NOT be absorbed into accounted_ids.
A8. [CLASS 3] ORDERING IS NOT A GATE (INV-8, plan-review P1-1): a manifest listing the covering feature FIRST (the shape of every historical manifest, including 173-S — which was in fact successfully closed under feature-first ordering) must still classify CASCADE.
A9. [SPLIT IN REVISION 4 — THREE VERDICT TESTS + THREE REASON TESTS] EXACT PARSED-SCALAR MATCH (D1b / plan R19). VERDICT-ONLY [CLASS 4, start green]: test_titlecase_archived_out_of_manifest_child_falls_back_to_safe_close (`Archived`), test_uppercase_archived_out_of_manifest_child_falls_back_to_safe_close (`ARCHIVED`), test_whitespace_padded_archived_out_of_manifest_child_falls_back_to_safe_close (`" archived "`). ALL EXPECT SAFE_CLOSE; NONE may assert on `reason`. REASON-ONLY [CLASS 1, RED]: test_titlecase_archived_out_of_manifest_child_reason_names_observed_status, test_uppercase_archived_out_of_manifest_child_reason_names_observed_status, test_whitespace_padded_archived_out_of_manifest_child_reason_names_observed_status — each asserts the reason NAMES THE OBSERVED STATUS as parsed; NONE may assert a verdict. Each pair shares one fixture builder.
    *** FIXTURE-ENCODING REQUIREMENT (revision 3, NON-NEGOTIABLE, plan R25/D1b) *** the padded variant MUST be written as a YAML-QUOTED scalar — literally `status: " archived "` — so it survives yaml.safe_load as `" archived "`. Written UNQUOTED (`status:  archived  `), YAML resolves it to exactly `"archived"`, the fixture silently becomes an INERT record, and the test would be ASSERTING A FALSEHOOD. Record this in the docstring.
    *** WITHDRAWN CLAIM *** the revision-2 statement that unquoted `archived ` (trailing space) is a distinct non-inert value is FACTUALLY WRONG and is withdrawn: every record reaches the predicate through gates.topology._frontmatter, which parses with yaml.safe_load, and a YAML load ERASES lexical form. Do NOT write a fixture asserting that distinction.
A10. [SPLIT IN REVISION 4] TORN / DUPLICATE IDENTITY FAILS CLOSED (D1c / plan R20). (a) [CLASS 4 — start green] test_torn_out_of_manifest_descendant_falls_back_to_safe_close — an OUT-OF-MANIFEST descendant present in BOTH queue/ and archive/, INCLUDING the case where the archive/ copy declares `archived` and would otherwise read as inert. EXPECT SAFE_CLOSE. VERDICT ONLY; MUST NOT assert on `reason`. (b) [CLASS 1 — RED] test_torn_out_of_manifest_descendant_reason_is_torn_specific — same fixture; asserts a TORN-SPECIFIC containment reason (current main emits only "has descendants outside the manifest"). REASON ONLY. Plus [CLASS 4] test_duplicate_id_within_single_root_falls_back_to_safe_close (verdict only). Docstring rationale: a torn record has an AMBIGUOUS declared status, so granting CASCADE would make a destructive authorization depend on filesystem iteration order.
A11. [CLASS 4 — NEW IN REVISION 3] NON-STRING STATUS FAILS CLOSED (plan R25 / decision D1b): a fixture declaring `status: yes` (yaml.safe_load parses this to the BOOL True) and a fixture declaring a bare `status:` (parses to None) must BOTH yield SAFE_CLOSE. The predicate MUST NOT str()-coerce either value. Also cover an int, a list, and a mapping if cheap.

PART B — DOC-CONTRACT TESTS (new module tests/test_flat_manifest_closure_docs.py; ALL CLASS 2)
Assert on BOTH the installed artifact AND its template mirror (.github/policies/workflow-policies.md + templates/policies/workflow-policies.md.tmpl; .github/skills/shipment-reconcile/SKILL.md + templates/skills/shipment-reconcile/SKILL.md.tmpl) that:
- the D1a SET VOCABULARY is present and normative: manifest_scope = items(S); closure_scope = items(S) + {S}; allowed_ids = closure_scope + validated_linked_deliberations; required_ids = closure_scope members not already truly archived at the pre-close snapshot — and that validated_linked_deliberations adds ZERO work items (plan R21);
- INV-1..INV-11 are present and normative;
- closure scope is stated as the manifest's explicit item IDs PLUS the shipment record, with ancestry explicitly excluded from expanding it;
- multi-shipment feature delivery is permitted BY CONTRACT and the feature item is the FINAL manifest entry of the FINAL shipment (INV-4);
- INV-11 IS STATED WHEREVER INV-4/INV-5 ARE (plan R22): multi-shipment delivery is CONTRACT-COMPLETE but OPERATIONALLY BLOCKED on an EXTERNAL RUNTIME PREREQUISITE, tracked by durable active stash entry 7F9CB5E9;
- excluded/removed/descoped/rejected/merged items never block closure on the ground of ancestry, and NO disposition note is required (INV-3);
- manifest ORDERING is stated as a Stage assembly convention and explicitly NOT a closure precondition (INV-8);
- the returned_ids guard is explicitly recorded as INSUFFICIENT on its own (R13), and is stated as INDICATIVE RATIONALE rather than as a proven engine law (plan D7a);
- the RECONCILE_FAIL_NO_SAFE_RECORD_TRANSITION fail-closed halt exists with its explicit no-substitution prohibition (R18);
- the APPROVAL-GATED ROLLBACK SEQUENCE is stated in full (plan R23 / decision D6): capture evidence -> HALT -> emit P-005 -> request EXPLICIT operator approval -> REVALIDATE exact paths and state after approval -> execute ONLY the approved rollback;
- NEGATIVE ASSERTIONS: the tokens `TERMINAL_CLOSE` and the phrase `fully covered` no longer appear as normative preconditions on any of the four surfaces; clearing parent_id remains explicitly rejected; NO surface claims multi-shipment delivery is operationally complete or end-to-end supported; NO automatic, immediate, or notification-only destructive-revert language remains on any surface; and (NEW, plan R24/H17) NO surface claims that a classifier fixture proves an ENGINE behaviour.

=== EVIDENCE RULE — CORRECTED 2026-09-16 (plan R24, decision D7a/D7b) ===
THE REVISION-2 EVIDENCE RULE WAS UNSOUND AND IS WITHDRAWN. It said these fixtures ARE "the authoritative record of the engine law". THEY ARE NOT AND CANNOT BE. These are CLASSIFIER fixtures: they write synthetic Markdown and call classify_shipment_close_path, a pure Python function. They NEVER invoke the backlogit Go engine, so they CANNOT re-derive archived_ids, returned_ids, descendant archival, or parent_id clearing. Asserting that they do would be FABRICATED EVIDENCE.

TWO DISJOINT EVIDENCE CLASSES:
- CLASSIFIER LAW — what classify_shipment_close_path returns for a record shape. Authoritative evidence: THESE fixtures. Fully provable here.
- ENGINE LAW — what backlogit 1.10.1 actually mutates/returns/skips. Authoritative evidence: a real, observed, in-repository engine execution. NOT provable by ANY fixture in this task.

THE ONE ENGINE PROPOSITION AUTHORITATIVELY HELD — RE-SOURCED IN REVISION 4 (plan D7a / D10 G3 / H20). *** THE REVISION-3 SOURCING IS WITHDRAWN AS FACTUALLY WRONG. *** Revision 3 said: "`git show --stat e4ca20e5` shows the real 173-S close changed EXACTLY 12 paths and did NOT touch 165.007-T / 165.010-T". VERIFIED AND FALSE AS WRITTEN: the UNFILTERED diffstat of e4ca20e5 contains 34 CHANGED PATHS, and e4ca20e5 (`chore(stage): publish flat-manifest closure package`) is the COMBINED PUBLICATION COMMIT, NOT a close-only commit. Do NOT cite the unfiltered diffstat, and do NOT describe e4ca20e5 as close-only, on any surface.

CORRECT SOURCING — PATH-SCOPED before/after evidence between 358b63b4 (parent of e4ca20e5; the immutable pre-close tree, merge of PR #451) and e4ca20e5:
  (a) `git diff --stat 358b63b4 e4ca20e5 -- <the 11 manifest member paths>` -> 11 files changed;
  (b) `git diff --stat -M --find-renames 358b63b4 e4ca20e5 -- .backlogit/queue/173-S.md .backlogit/archive/173-S.md` -> EXACTLY ONE entry, `.backlogit/{queue => archive}/173-S.md` (rename PLUS modify);
  (c) `git diff --stat 358b63b4 e4ca20e5 -- .backlogit/archive/165.007-T.md .backlogit/archive/165.010-T.md` -> EMPTY;
  (d) `git rev-parse 358b63b4:.backlogit/archive/165.007-T.md e4ca20e5:.backlogit/archive/165.007-T.md` (and the same for 165.010-T) -> IDENTICAL blob OIDs (544c2377b38e9ba2df123f5b8d9bf359dacb89ea and 609ad8bc7839fca8b5273d777a6acdc590aacb6f respectively).
(c)+(d) together establish EXACTLY ONE law: an out-of-manifest sibling declaring `status: archived` is SKIPPED by the cascade. That is precisely and only the law the INV-6 inertness grant rests on. This git-derivable evidence is DISTINCT FROM, and must not be conflated with, Ship's separately adjudicated in-workspace verification of the 173-S close.

STILL UNPROVEN, NEVER CITABLE AS FACT, NEVER "MEASURED", AND NEVER SUFFICIENT TO AUTHORIZE A CASCADE: Arm 2's returned_ids: [] while archiving (usable as RATIONALE in a docstring, never as proof, never worded as a measurement); Arm 3/4's parent_id clearing (BLOCKED, follow-up 63363CF5); status: done descendant archival (NOT LOAD-BEARING — done != archived, so it is non-inert and forces SAFE_CLOSE regardless). Where an engine behaviour is unproven, the dependent autoharness behaviour IS THE FAIL-CLOSED ONE.

=== CONTAINMENT RULE — REPLACED 2026-09-16 IN REVISION 4 (decision D7b/D17, plan R27/H21) ===
*** THE REVISION-3 "PERMITTED" CARVE-OUT IS WITHDRAWN IN FULL. *** Revision 3 declared `tempfile.TemporaryDirectory()` "EXPLICITLY PERMITTED and need NOT be relocated" for hermetic unit tests. That is wrong on TWO independent constitutional grounds:
  (i)  CONSTITUTION IV (workspace containment): an OS %TEMP%/TMPDIR path resolves OUTSIDE this repository and OUTSIDE cwd. Hermeticity and tool-freeness describe WHAT THE TEST READS; containment governs WHERE THE TEST WRITES. A hermetic writer outside cwd is still a containment breach.
  (ii) CONSTITUTION PRINCIPLE VII (destructive-operation approval): TemporaryDirectory() performs an AUTOMATIC, UNAPPROVED, RECURSIVE DELETE on context exit. "Self-cleaning" NAMES an unapproved destructive operation; it does not excuse one.

- PROHIBITED FOR EVERY PURPOSE IN THIS TASK, INCLUDING PURE-CLASSIFIER UNIT TESTS: tempfile.TemporaryDirectory(), tempfile.mkdtemp(), tempfile.mkstemp(), any TMPDIR/%TEMP%-rooted path, any path resolving outside cwd, ANY WRITE OUTSIDE CWD, out-of-repository workspaces in which the backlogit engine is executed, and ANY automatic/implicit/on-exit/atexit/teardown/"best-effort" deletion (including shutil.rmtree in tearDown) of a scratch workspace.
- REQUIRED — canonical repository-internal scratch root: <repo_root>/.autoharness/staging/tmp/<unique-run-or-test-nonce>/ . Already git-ignored via .gitignore line 6 (`.autoharness/staging/`), confirmed by `git check-ignore -v .autoharness/staging/tmp/probe.txt` (exit 0). NO .gitignore CHANGE IS REQUIRED AND NONE IS AUTHORIZED.
- REQUIRED — canonical containment check, run BEFORE THE FIRST WRITE and FAILING CLOSED, using FULLY RESOLVED REAL PATHS on both operands (os.path.commonpath or Path.is_relative_to; a raw string-prefix test is NOT sufficient):
      root = Path(os.getcwd()).resolve(strict=True)
      scratch = (root / ".autoharness" / "staging" / "tmp" / nonce).resolve()
      if os.path.commonpath([str(root), str(scratch)]) != str(root):
          raise AssertionError(f"containment violation: {scratch} escapes {root}")
  This check is itself asserted by a test (CLASS 3 for the positive path, CLASS 2 for the escape-rejection assertion).
- REQUIRED — CLEANUP IS OPERATOR-CONTROLLED. Scratch directories are PERSISTENT and UNIQUELY NAMED; leaving them in place is the CORRECT TERMINAL STATE. No test, fixture, or teardown may delete, prune, truncate, or rmtree them. Deletion occurs ONLY through the D6 sequence: capture evidence -> HALT -> emit P-005 -> request EXPLICIT OPERATOR APPROVAL -> REVALIDATE exact paths and state -> execute ONLY the approved deletion. Accumulated scratch is an OPERATOR HYGIENE MATTER; this task invents NO deletion authority.
- CONDITIONAL / STILL NOT AUTHORIZED HERE: a real engine characterization additionally requires an explicit time-boxed P-016 declaration and EXPLICIT OPERATOR DESTRUCTIVE APPROVAL BEFORE CREATION. NOT in 174-S; follow-up P2-3.

ACCEPTANCE: fixtures are declarative and shape-derived; NO test asserts on a literal feature or shipment ID; NO named test carries more than one class label (revision 4 / H19); every CLASS 1 and CLASS 2 case fails RED against current main FOR ITS OWN SPECIFIC NAMED REASON; every CLASS 4 case passes GREEN against current main (a red CLASS 4 means the test is wrong); CLASS 3 cases pass; the pre-existing ShipmentClosureClassificationTests suite passes UNCHANGED; no test claims a classifier fixture proves an engine behaviour and no docstring states returned_ids or parent_id effects as measured fact; every scratch path resolves under .autoharness/staging/tmp/ with the containment check green and NO automatic deletion anywhere. Canonical invocation: `python -m unittest discover -s tests`.

SIZE: M. COMPLEXITY: medium.