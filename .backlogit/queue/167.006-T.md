---
artifact_type: task
created_at: 2026-09-17T19:58:46.5473135Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 167.005-T
id: 167.006-T
labels:
    - closure
    - agent-template
    - documentation
    - producer-contract
parent_id: 167-F
priority: medium
references:
    - docs/plans/2026-09-17-closure-evidence-naming-contract-plan.md
    - docs/decisions/2026-09-17-closure-evidence-naming-contract-deliberation.md
status: queued
title: Align Ship agent post-merge closure references to the canonical pattern and both gate-relevant keys (template + installed mirror, atomic)
updated_at: 2026-09-17T22:12:37.6362347Z
---

PLAN UNIT U6 (plan revision 5). Domain: agent documentation. Posture: migration-first.

FILES (2, BOTH IN ONE COMMIT — ATOMIC, same R4 / 165.008-T precedent as 167.005-T):
- templates/agents/_ship.agent.md.tmpl  (the PRODUCT)
- .github/agents/_ship.agent.md          (the INSTALLED DOGFOOD MIRROR)

CHANGES
1. The post-merge closure step (_ship.agent.md.tmpl:863 and the corresponding installed step) names the CANONICAL closure path and the MANDATORY `autoharness gate closure-evidence` invocation, instead of naming only the closure directory. The TEMPLATE retains its `{{DOCS_CLOSURE}}` placeholder and the INSTALLED MIRROR retains the rendered literal; that difference is correct and is preserved.
2. The step's gate-relevant metadata list names BOTH `compaction_status` AND `closure_status`. The source bug report identifies the omission of `closure_status` from the Ship-side guidance as the COMPOUNDING half of the metadata axis: an artifact written against that partial guidance satisfies half the consumer predicate and still fails. This closes it.

SCOPE GUARD
NO change to Ship's Role Boundary, closure procedure, P-015 handling, shipment-reconcile invocation, or any other step. This task edits ONLY the closure-artifact naming and metadata references in the post-merge closure step.

ACCEPTANCE
- The template retains `{{DOCS_CLOSURE}}` and introduces NO other unresolved placeholder; the installed mirror carries the rendered literal and NO `{{...}}`.
- Template and installed mirror agree with each other (asserted by 167.009-T test 2).
- Touches NOTHING under docs/closure/ (D8).

DEPENDS ON: 167.005-T (the agent wording must match the producer skill's reconciled wording).