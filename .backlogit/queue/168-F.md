---
artifact_type: feature
created_at: 2026-09-18T04:48:26.0526293Z
custom_fields:
    harness_status: pending
id: 168-F
labels:
    - policy
    - tdd-gate
    - p-004
    - fail-closed
priority: high
status: queued
title: P-004 red-phase precondition scoped to the declared shipment harness set
updated_at: 2026-09-19T00:25:03.9820135Z
---

Replace the P-004 unsatisfiable whole-suite every-function-red precondition with a declared-harness-set precondition admitting two disjoint expected-outcome classes under exact set equality. The declaration is a TYPED per-entry shape ({test_id, marker} for expected_red, {test_id} for expected_green_characterization), never a bare identifier list, stored SHIPMENT-SCOPED at .autoharness/harness-sets/{shipment_id}.yaml under schemas/declared-harness-set.schema.json - NOT in .autoharness/harness-manifest.yaml, which is a singleton install inventory with no shipment dimension. Observation is defined against unittest.TestResult PER TEST, never against merged runner output. THIRTEEN fail-closed gate tokens, each carrying exactly one STRUCTURED SUBJECT - TestSubject, CollectionSubject, EntrySubject or DocumentSubject - because a collection-level token such as P004_EMPTY_RED_SET has no test_id to name and a malformed entry may carry none at all. P004_DECLARATION_SCHEMA_ERROR is a hard halt, not a fourteenth token. ONE ATOMIC PUBLIC ENTRYPOINT owns the whole gate: run_p004_gate() / `autoharness gate p004 --shipment <id> --json`, with an equivalent agent-facing P004_GATE_REQUEST parity block; a bare `python -m unittest <ids>` invocation produces no P004GateResult and does NOT satisfy P-004. Delivered TEST-FIRST throughout: the storage lifecycle suite (168.010-T) precedes the loader (168.011-T), and the thirteen-token contract suite (168.005-T) precedes the observation boundary (168.007-T) and the entrypoint (168.012-T), with green verification (168.008-T) last. RED tests are IMPORT-SAFE: a module that raises at import yields a _FailedTest placeholder classified P004_MISSING_OBSERVATION, which is a missing observation and not valid red, so not-yet-existing boundaries are imported inside the test body. This feature contains NO bootstrap task: harness generation is a harness-architect skill step at Ship Step 2, which runs before any task claim and is never itself a claimable task. Stash: 76EBDE6D. Decision: docs/decisions/2026-09-17-seven-entry-contract-defect-staging-portfolio-deliberation.md (revision 3). Plan: docs/plans/2026-09-17-p004-red-phase-precondition-scoping-plan.md (plan_id p004-red-phase-precondition-scoping, plan_role active, revision 7). Verdict manifest: docs/reviews/2026-09-17-p004-red-phase-precondition-scoping-plan-review.md (latest_attempt 07, verdict REMEDIATED-PENDING-REVIEW at plan revision 7, awaiting independent attempt 08).

<!-- BEGIN:dod -->
Typed schema landed as schemas/declared-harness-set.schema.json (per-item shape only; no uniqueness and no disjointness rule expressed there). Shipment-scoped storage at .autoharness/harness-sets/{shipment_id}.yaml has a creation path (harness-architect Step 6), a single authoritative reader (load_declared_harness_set()), archive-on-close retention, and three fail-closed staleness checks - absent document, shipment_id mismatch, manifest_digest drift - each emitting P004_HARNESS_SET_MISSING or P004_HARNESS_SET_STALE with a DocumentSubject. compute_manifest_digest() is imported from bootstrap_grant.py, not copied. Policy rewritten in template and installed mirror BYTE-IDENTICALLY, carrying the harness-scoped quantifier, all thirteen tokens with their subject kinds, the structured subject model, and the normative statement that a bare `python -m unittest <ids>` invocation does not satisfy P-004. harness-architect writes the shipment-scoped declaration with explicit test ids and obtains its verdict ONLY from the gate entrypoint, recording P004GateResult verbatim, with the P004_GATE_REQUEST agent-facing parity block. Thirteen-token contract suite authored RED before the boundary exists and verified GREEN against the shipped entrypoint. RED modules import cleanly with ZERO loader.errors, asserted directly; not-yet-existing boundaries load inside the test body; each RED test individually fails with its own declared marker. validate_declared_harness_set() is the SOLE declaration-validation entry point: Draft-07 schema, jsonschema errors mapped by the structural (absolute_path, validator) pair and never by message text, P004_DECLARATION_SCHEMA_ERROR raised and HALTING on any unmapped pair, then the two relational rules the schema cannot express (P004_DUPLICATE_DECLARATION, P004_SET_OVERLAP). P004Result reuses the INHERITED TestResult._exc_info_to_string formatting and overrides addSubTest so a subtest-only red path is never a pass. Declared names load INDEPENDENTLY one per loadTestsFromName call, each suite RECURSIVELY FLATTENED, load errors attributed by a len(loader.errors) snapshot slice, results correlated through an id(test_object) identity map; NO positional correspondence anywhere. Loaded TestCase.id() is asserted equal to the requested test_id as a SEPARATE declaration-conformance check emitting P004_DECLARED_ID_MISMATCH, independent of identity-based attribution. The declared-class x observed-outcome matrix is TOTAL with no undefined cell; expectedFailure and unexpectedSuccess both fail closed as P004_INCONCLUSIVE_OUTCOME. run_p004_gate() is the single atomic public entrypoint owning load, validate, run, compare, tokens and JSON, with no partial entry; the no-bypass property is asserted end-to-end. Whole-suite CI gate proven still green and byte-unchanged, with the CI/P-004 distinction recorded: CI requires exit zero on the default branch, P-004 requires non-zero at the red phase on a feature branch.
<!-- END:dod -->