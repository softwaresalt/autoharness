---
artifact_type: feature
created_at: 2026-09-18T04:48:26.0526293Z
custom_fields:
    harness_status: pending
id: 168-F
labels:
    - policy
    - tdd-gate
    - p-004
    - fail-closed
priority: high
status: queued
title: P-004 red-phase precondition scoped to the declared shipment harness set
updated_at: 2026-09-18T20:44:15.7437553Z
---

Replace the P-004 unsatisfiable whole-suite every-function-red precondition with a declared-harness-set precondition admitting two disjoint expected-outcome classes under exact set equality. The declaration is a TYPED per-entry shape ({test_id, marker} for expected_red, {test_id} for expected_green_characterization), never a bare identifier list, and observation is defined against unittest.TestResult per test, never against merged runner output. Nine fail-closed tokens, each naming the offending test_id(s). Delivered TEST-FIRST: the nine-token contract suite is authored and observed RED (168.005-T) before the P004Result boundary is implemented (168.007-T), and green verification (168.008-T) follows the implementation. This feature contains NO bootstrap task: harness generation is a harness-architect skill step at Ship Step 2, which runs before any task claim and is never itself a claimable task. Stash: 76EBDE6D. Decision: docs/decisions/2026-09-17-seven-entry-contract-defect-staging-portfolio-deliberation.md (revision 3). Plan: docs/plans/2026-09-17-p004-red-phase-precondition-scoping-plan.md (plan_id p004-red-phase-precondition-scoping, plan_role active, revision 6). Verdict manifest: docs/reviews/2026-09-17-p004-red-phase-precondition-scoping-plan-review.md (latest_attempt 06, verdict REMEDIATED-PENDING-REVIEW at plan revision 6, 0 P0/P1 open; awaiting independent attempt 07).

<!-- BEGIN:dod -->
Typed schema fields landed (per-item shape only). Policy rewritten in template and installed mirror byte-identically. harness-architect skill declares both typed sets and addresses tests by explicit id. Nine-token contract suite authored RED before the P004Result boundary exists, and verified GREEN against the shipped runner. validate_declared_harness_set() is the SOLE declaration-validation entry point: it runs the Draft-07 schema, maps every jsonschema error to its token by the structural (absolute_path, validator) pair - never by parsing message text - emitting P004_MARKER_MISPLACED and P004_EMPTY_RED_SET with the offending test_id recovered from the parsed manifest, raises P004_DECLARATION_SCHEMA_ERROR and halts on any unmapped pair, and then evaluates the two relational rules the schema cannot express (P004_DUPLICATE_DECLARATION, P004_SET_OVERLAP). P004Result reuses the INHERITED TestResult._exc_info_to_string formatting from the exc_info tuples handed to addFailure/addError, and overrides addSubTest so a subtest-only red path is never recorded as a pass. Declared names are loaded INDEPENDENTLY, one per loadTestsFromName call, each returned suite RECURSIVELY FLATTENED to leaf tests, load errors attributed by a len(loader.errors) snapshot slice taken before and after each call, and results correlated to requested names through an id(test_object) identity map built at load time. NO positional correspondence is used anywhere: loadTestsFromNames returns one SUITE per name rather than one test, so positional correlation is unsound in both the success and the failure case. Whole-suite CI gate proven still green and byte-unchanged.
<!-- END:dod -->

<!-- BEGIN:goals -->
Make the P-004 red-phase precondition satisfiable within the EXISTING Ship lifecycle - shipment claim precedes harness generation, and harness-architect authors failing tests and assigns harness-ready before any implementation task is claimed - by scoping the precondition to the declared harness set with two disjoint expected-outcome classes under exact set equality, and prove it test-first.
<!-- END:goals -->