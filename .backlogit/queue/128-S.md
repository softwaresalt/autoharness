---
artifact_type: shipment
created_at: 2026-08-10T08:13:30.0105463Z
custom_fields:
    items:
        - 119-F
        - 119.001-T
        - 119.002-T
        - 119.003-T
        - 119.004-T
        - 119.005-T
        - 119.006-T
dependencies:
    - 127-S
id: 128-S
links:
    - target_id: 125-S
      link_type: supersedes
priority: critical
status: queued
title: 'S2 Copilot supervisor: supervision core library, unwired (P0)'
updated_at: 2026-08-10T08:18:39.1905988Z
---

Shipment 2 of the strict serial chain `127-S` -> `128-S` -> `129-S` for Plan 1. Supersedes the retired `125-S`. BLOCKED until `127-S` ships.

*** ADDITIONAL GATES — OPEN P1s F18, F19, F22, F23 AND F24 (2026-08-10). *** Open P1 **F24** ALSO gates this shipment: `119.005-T` requires adding `.autoharness/sessions/` to "the gitignore template", and NO SUCH TEMPLATE EXISTS — `templates/` contains no gitignore artifact, and workspace ignore rules are handled procedurally by the install-harness skill. The criterion is UNSATISFIABLE BY VACUITY (it is "met" by finding nothing to change, and unlike F17/F23 it fails SILENTLY), and it falsifies the H6 claim that the session journal is "gitignored local operational state": in generated workspaces nothing installs that rule, so every supervised session's JSONL would be git-tracked. Resolution likely moves scope OUT of S2 and INTO the installer/tuner surfaces plus a real `git check-ignore` test — a SHIPMENT-BOUNDARY question, not a wording fix. Stage adopted no resolution and changed no dependency edge. Open P1 F19 ALSO gates this shipment: 119.003-T must emit SessionPhaseChanged but that event type is only defined in 119.004-T, which depends BACK on 119.003-T - a circular ordering that makes the declared sequence unimplementable.  The predecessor is NOT the only gate on this shipment. Open P1 **F18** must also be dispositioned before `128-S` is claimed: plan §3.2's diagram routes `CANCELLING -> EXITED`, bypassing `DRAINING`, contradicting the plan's own prose rule and `119.006-T`, and `119.003-T`'s transition table omits `LOCKING -> REFUSED`, the launch-phase failure edges to `FAILED`, and `RESTARTING -> LAUNCHING`. Implemented literally, the state machine would reject valid outcomes (absent edges must raise `ILLEGAL_TRANSITION`) and could skip journal flush, lock release, and child reaping on cancellation. Requires operator confirmation that cancellation routes through `DRAINING`, plus replacement of the shorthand with the complete reviewed transition table. See `docs/reviews/2026-08-09-copilot-cli-supervisor-control-plane-review.md` Cycle 4.

Open P1 **F22** ALSO gates this shipment: plan §3.2 routes THREE post-`LOCKING` failure edges (`BOOTSTRAPPING —(fatal)→ FAILED`, `RESOLVING —(no copilot exe)→ FAILED`, `LAUNCHING —(spawn error)→ FAILED`) straight to terminal states WITHOUT passing through `DRAINING`, which is where the plan places lock release; §3.4 then requires an explicit operator `--force-unlock` for a stale lock and never auto-breaks one. The most likely first-run failure of all — no Copilot CLI on `PATH` — therefore leaves the workspace permanently locked, with every retry returning `REFUSED`. NOTE: if the operator places the guaranteed-release obligation on `118.005-T` (T5) rather than on `119.003-T`'s transition table, the fix reaches into **`127-S`**, not just this shipment.

Open P1 **F23** ALSO gates this shipment: `119.006-T` requires a `cancel during launch` test, but §3.2 defines exactly one cancellation edge (`RUNNING → CANCELLING`) and Rule 1 rejects out-of-table transitions with `ILLEGAL_TRANSITION`, so a cancel arriving in `LAUNCHING` can only be refused — contradicting the cancellation contract stated in that same task. Same defect class as F17: acceptance criteria the fixed contract cannot satisfy.

**ONE RULING DISCHARGES F18, F22 AND F23.** All three are the same missing invariant — cleanup and cancellation are guaranteed only from `RUNNING`. An operator ruling that *every terminal exit after `LOCKING` routes through `DRAINING`, and operator cancel is legal from every post-`LOCKING` phase* resolves all three at once and makes §3.2 Rule 2 unconditional. Stage has NOT adopted it.

MANIFEST CONTRACT (H10.5 — fully-covered root covering feature). Lists the covering feature `119-F` FIRST, then all six of its children (`119.001-T`..`119.006-T`) in dependency order. Same two invariants as `127-S`: (i) FULL COVERAGE — `119-F` has exactly these six children, so `returnUnreleasedFeatureItems(119-F)` returns the empty set at close; (ii) ROOT PLACEMENT — `119-F` has no parent feature, so `featureScopeRoots` cannot escape into `120-F` or `117-F`.

SYMMETRIC GUARANTEE FROM THE PREDECESSOR: nothing in this shipment is reachable from the `127-S` close either. No `127-S` member has `119-F` as an ancestor, so `featureScopeRoots` never discovers `119-F` and `returnUnreleasedFeatureItems` is never invoked for it. When Ship claims `128-S`, all six tasks are guaranteed to still be `queued` with `parent_id: 119-F` intact — no repair, no re-adoption, no verification-then-fix step. The cross-shipment `blocks` edges (`119.001-T` -> `118.003-T`, `119.004-T` -> `118.004-T`) are dependency edges, not parent edges, and the close path never traverses them.

NO SHIP-SIDE MITIGATION IS REQUIRED OR PERMITTED. CLOSE-PATH CONTRACT: this manifest intentionally lists its covering feature, which is the FULLY-COVERED ROOT exception to the task-only rule in docs/compound/097-S-shipment-task-only-safe-close.md - see the "Reconciliation - the FULLY-COVERED ROOT exception" section there. The task-only rule targets PARTIAL-feature shipments; task-only membership does NOT make this topology safe (ARM A orphaned 14/14 tasks). Close with a single `backlogit shipment ship`. Expected `ShipShipment` result: `returned_ids: []`; `archived_ids` = the six tasks + `119-F` + `128-S`.

SCOPE: the supervision core library (P0) — `process.py` / `process_pty.py` behind one `ChildProcess` Protocol, `session.py` state machine, `events.py` typed bus, `journal.py` redacted append-only journal, `recovery.py` cancellation / bounded restart budget / resume. UNWIRED (H10.2): nothing here is reachable from `start.ps1`, `start.sh`, or the CLI, so this shipment is still zero observable behavior change.

*** ADDITIONAL GATE - OPEN P1 F26 (2026-08-11), WHICH GATES ALL THREE SHIPMENTS. *** The close-path contract in docs/compound/097-S-shipment-task-only-safe-close.md instructs Ship to close this shipment with a single `backlogit shipment ship`, but .github/agents/_ship.agent.md prohibits that operation UNCONDITIONALLY ("NEVER the cascade backlogit_ship_shipment, P-015" / "Do NOT call backlogit shipment ship"), with no fully-covered-root carve-out; P-015 in templates/policies/workflow-policies.md.tmpl scopes its Applies-when to partial-feature shipments but states its prohibition and postcondition absolutely. Under fail-closed P-010/P-015 the instructed close command is UNEXECUTABLE, and a Stage planning artifact cannot grant Ship an exemption from Ship's own operative prohibition. THE MANIFEST SHAPE IS NOT AFFECTED: under safe-close this fully-covered-root manifest is still correct, because the covering feature is itself a manifest item and no unshipped siblings exist, so the protected set is empty and safe-close archives exactly the release unit. What is contested is the CLOSE COMMAND, and with it the scope of the 64/64 evidence, which proves cascade safety rather than safe-close correctness. OPERATOR RULING REQUIRED: either amend P-015 + the Ship agent + shipment-reconcile coherently for a verified fully-covered-root exception, or keep safe-close and revise the compound contract and its expected evidence (returned_ids: [] would no longer be the artifact to expect). Stage adopted NO resolution. See docs/reviews/2026-08-09-copilot-cli-supervisor-control-plane-review.md.
