---
artifact_type: task
created_at: 2026-09-08T07:42:21.2778771Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 033-DL
id: 163.001-T
labels:
    - test
    - red
    - replay
    - 159-s
parent_id: 163-F
priority: high
status: queued
title: 'U1a RED: 159-S-shaped replay fixture for the pre-cascade evidence gate'
updated_at: 2026-09-10T23:52:35.2041307Z
---

Build the synthetic 159-S-shaped fixture in tests/test_shipment_reconcile_precascade_evidence.py. AC: (1) fixture workspace = one shipment record + one covering ROOT feature declaring status: active (the only valid pre-close state - the cascade is what archives it) + its FULL descendant task set at every depth declaring status: done; (2) variant A = covering feature with NO linked deliberation (the exact 159-S/151-F shape whose correct validated set is EMPTY); (3) variant B = one validated linked deliberation reachable via each of the three engine-defined sources in turn (literal custom_fields.source_deliberation_id, description scan, references scan) using the engine matcher \\b(?:DL\\d+|[0-9]+(?:\\.[0-9]+)*-DL)\\b verbatim - never a wording-level approximation; (4) the fixture models ORDERING explicitly as a recorded sequence of (step, timestamp) events so assertions can ask 'did the evidence emission precede the cascade invocation', not merely 'is a file present'; (5) RED only - this task lands the fixture and does NOT edit SKILL.md or the template. Plan: docs/plans/2026-09-08-shipment-reconcile-step-0c-live-pre-mutation-evidence-plan.md U1a.

--- REVISION R1 (2026-09-10, gate 033-DL, stash DDBF283E/EB23D1B9; plan section "Revision R1") --- R1 GOVERNS where it conflicts with the text above. Fixture must model an APPEND-ORDERED ENGINE EVENT LOG per shipment (.backlogit/logs/{shipment_id}.jsonl), not merely a (step, timestamp) sequence and not merely file presence, so an assertion can ask "does the PRECASCADE_EVIDENCE_ANCHOR event precede the engine-written mutation events (shipment_status_changed / commit_tracked / archived) in APPEND ORDER?". Timestamp comparison alone is exactly what R1 removes from the load-bearing path, so a fixture that only records timestamps cannot support 163.002-T's required assertions. Add a fixture variant carrying a BACKDATED RECONSTRUCTION: evidence record whose collection_completed_at is strictly BEFORE the cascade and whose HEAD SHA and manifest list both MATCH the closed state (a perfect forgery under the pre-R1 mechanism), but whose anchor event is absent or appended AFTER the mutation events. Real-data reference shapes verified 2026-09-10: .backlogit/logs/159-S.jsonl (nine engine events, NO anchor before archived - the true negative) and .backlogit/logs/150-S.jsonl (full lifecycle through archived). Anchor payload carries the literal token PRECASCADE_EVIDENCE_ANCHOR, shipment_id, the record's repo-relative path, and the record's sha256.

--- AMENDMENT R1-13 (2026-09-10, review-fix cycle 3; plan sections "R1-4"/"U1a"/"R1-13(b)", redesign decision D-5) --- NEW POSITIVE FIXTURE VARIANT: LATE_BUT_ANCHORED. ADDITIVE TO R1; STILL RED ONLY; NO SIZE CHANGE (stays M / medium - one further variant inside a fixture set this task already builds).
WHY IT EXISTS: R1-2 / R1-12 removed the "collection_completed_at strictly before the cascade invocation" comparison from the LOAD-BEARING PATH, and 163.002-T's R1-12 amendment states that a PRESENT-BUT-LATE value MUST NOT by itself reject. No fixture variant EXERCISED that statement - every existing variant either carried a compliant timestamp or was a rejection case - so the supersession existed only as prose.
THE VARIANT: an evidence record whose collection_completed_at is PRESENT BUT NOT STRICTLY BEFORE the cascade invocation (equal to it, or after it), whose HEAD SHA and manifest item list BOTH MATCH the closed state, whose scan_performed and linked_deliberation_ids fields are EXPLICITLY RECORDED, and whose PRECASCADE_EVIDENCE_ANCHOR event is PRESENT, DIGEST-MATCHING, and appended BEFORE EVERY engine-written mutation event (shipment_status_changed / commit_tracked / archived) in APPEND ORDER. EXPECTED OUTCOME: PASS - no token fires. This is the exact mirror of the BACKDATED_RECONSTRUCTION negative variant (valid-looking timestamp, missing or late anchor => reject); together the pair proves from both sides that the ANCHOR, not the timestamp, is load-bearing.
RED-FIRST BOUNDARY, EXPLICIT: this task lands the VARIANT AS FIXTURE DATA ONLY - no assertion over it, and no SKILL.md or template edit. The acceptance assertion belongs to 163.002-T (U1b). That assertion is genuine RED against the pre-R1 mechanism, which rejects the late timestamp, and goes GREEN only when 163.004-T (U2) lands the anchor contract.