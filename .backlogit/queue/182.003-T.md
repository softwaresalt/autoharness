---
artifact_type: task
created_at: 2026-09-20T07:44:33.6302518Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: v1
    size_source: agent
dependencies:
    - 182.002-T
id: 182.003-T
labels:
    - bootstrap
    - activate
    - harness-architect
parent_id: 182-F
priority: critical
status: queued
title: 'BOOTSTRAP T3 (ACTIVATE): one commit generating .github/skills/harness-architect/SKILL.md and registering it in the harness manifest'
updated_at: 2026-09-20T19:20:00.0000000Z
---

ACTIVATE - ONE TASK, ONE COMMIT. Generate .github/skills/harness-architect/SKILL.md from templates/skills/harness-architect/SKILL.md.tmpl, binding EVERY template variable from its NAMED AUTHORITATIVE SOURCE below so the generated file contains NO unresolved {{...}} double-brace variable and NO unresolved single-brace {SUFFIX_FEATURE} or {SUFFIX_TASK} token. This is the SINGLE ACTIVATION COMMIT of the unit and it makes 182.001-T's assertion GREEN.

VARIABLE SOURCES ARE NOT ALL THE SAME KIND OF SOURCE - RESOLVE EACH FROM THE SOURCE NAMED HERE. FOUR are STORED in .autoharness/harness-manifest.yaml under variables_used: BUILD_CHECK_COMMAND = "python -m py_compile src/autoharness/cli.py"; TEST_COMMAND = "PYTHONPATH=src python -m unittest discover -s tests"; SOURCE_DIR = "src/autoharness"; TEST_DIR = "tests". The two SINGLE-BRACE suffix tokens in the template's frontmatter argument-hint (L4) bind from config.backlog.suffix_map in .autoharness/config.yaml - feature F, task T - per .github/skills/install-harness/SKILL.md:262 and :264; they are DEFINED BOUND VARIABLES, not retained exemplars, and every other carrier in the repository spells them in double-brace form.

UNIMPLEMENTED_MARKER - DERIVED, NOT STORED, AND NOT TO BE IMPROVISED. It is absent from .autoharness/ entirely: it appears in neither harness-manifest.yaml variables_used nor anywhere else under that directory. TWO install-harness surfaces are involved and they are COMPLEMENTARY, NOT COMPETING; naming only one of them is what leaves an executor without a referent. (1) KEYING RULE ONLY - .github/skills/install-harness/SKILL.md:335 is a row of the Review Persona Variables table headed at :327 with columns "Template Variable | Source | Purpose". That table is keyed ONE ROW PER VARIABLE and has NO language columns and NO language rows; its Purpose cell holds an abbreviated illustrative "e.g." list. It supplies ONLY the rule "Derived from languages.primary" and is NEVER a value source - its e.g. list MUST NOT be transcribed as a marker. (2) VALUE SOURCE - .github/skills/install-harness/SKILL.md:130 is a row of the table headed at :100 with columns "Template Variable | Source | Example (Rust) | Example (TypeScript) | Example (Python)". This is the ONLY table in the file carrying per-language UNIMPLEMENTED_MARKER values.

DERIVATION IN ORDER: read languages.primary from .autoharness/workspace-profile.yaml (line 7, primary: "python"); use it to select the matching "Example (...)" column of the :100 table; read the {{UNIMPLEMENTED_MARKER}} row at :130 in that column; transcribe the cell VERBATIM.

EXACT VALUE FOR THIS WORKSPACE: languages.primary is "python", so the Example (Python) column applies and the bound value of {{UNIMPLEMENTED_MARKER}} is EXACTLY the literal: raise NotImplementedError("...")  -- transcribed character-for-character from :130. THE MESSAGE SLOT IS NOT A PROMPT TO INVENT TEXT. Bind the literal as written: do NOT re-spell it, do NOT shorten it to "raise NotImplementedError", and do NOT substitute a stub-specific message. Varying the message is a template-authoring decision this unit does not own.

WHY THE TWO SURFACES DO NOT DISAGREE: :335's e.g. list shows "raise NotImplementedError" and :130's Python column shows raise NotImplementedError("..."). They differ ONLY in whether the message slot is displayed. The DETECTABLE TOKEN - the substring 182.002-T's red-phase marker check asserts against the failure set - is "NotImplementedError", identical in both. :130 is the value source; :335 is used solely to confirm the key and corroborate the token.

FAIL CLOSED ON ANY UNRESOLVABLE OR AMBIGUOUS MAPPING - five mechanically evaluable triggers, each a check against a real artifact rather than against a structure that does not exist. F1: .autoharness/workspace-profile.yaml missing or unreadable, or languages.primary absent or empty - HALT, key unresolvable. F2: languages.primary does not case-insensitively match any "Example (...)" column header of the :100 table (today exactly Rust, TypeScript, Python) - HALT, UNSUPPORTED LANGUAGE MAPPING; do NOT fall back to another column, to :335's e.g. list, or to a plausible idiom for that language. F3: the {{UNIMPLEMENTED_MARKER}} row is absent from the :100 table, or its cell for the selected column is empty or _(N/A)_ - HALT, value unresolvable. F4: the selected cell and :335's e.g. entry for the same language share NO common detectable token - HALT, AMBIGUOUS LANGUAGE MAPPING; two authoritative surfaces disagreeing on the token is a repository defect, not a choice for the executor to arbitrate. F5: config.backlog.suffix_map is missing a required key - HALT, suffix binding unresolvable. ON ANY HALT: DO NOT IMPROVISE, DO NOT GUESS, DO NOT SUBSTITUTE A PLAUSIBLE VALUE. Touch no file, make no commit, exit non-zero and return the unit to Stage. NO TRIGGER FIRES FOR THIS WORKSPACE: python is a declared column, :130's Python cell is non-empty, both surfaces carry NotImplementedError, and suffix_map supplies F and T - so the guard is a real check that passes here, not a condition that is unconditionally true and halts every run. An improvised UNIMPLEMENTED_MARKER would silently weaken 182.002-T's red-phase marker check, which is this unit's core evidence.

REGISTER THE GENERATED ARTIFACT IN THE HARNESS MANIFEST, IN THIS SAME COMMIT (decision D11). .github/skills/harness-architect/SKILL.md is a MANIFEST-TRACKED INSTALLED ARTIFACT: .autoharness/harness-manifest.yaml already carries an artifacts: entry for every other installed skill surface, and today carries NONE for harness-architect. Generating the file without registering it would leave the manifest asserting the actor does not exist while the actor exists - a parity hole in the exact file 182.004-T re-derives parity from, and in the exact file the P-004 postcondition is read from. THEREFORE, IN THE SAME COMMIT AND THE SAME ROLLBACK UNIT: ADD an artifacts: entry for path .github/skills/harness-architect/SKILL.md recording its primitive, its source template templates/skills/harness-architect/SKILL.md.tmpl, and the sha256 CHECKSUM OF THE FILE AS GENERATED IN THIS COMMIT; then VERIFY CHECKSUM PARITY by re-digesting the on-disk file and comparing it to the recorded value. A commit carrying the generated skill without its manifest entry, or the manifest entry without the generated skill, is an IMMEDIATE REVERT and not a fixup commit. The entry is ADDED, never appended as a note, and no OTHER manifest entry is created, refreshed or rewritten by this task - this is a REGISTRATION of one new artifact, not a manifest-wide reconciliation. This does NOT change any declared_surface_count anywhere in the portfolio: the manifest is a COMMIT MEMBER of this activation, not a declared surface of it.

THE COMMIT INSTALLS AND REGISTERS THE ACTOR, AND NOTHING ELSE - EXACTLY TWO FILES: the generated .github/skills/harness-architect/SKILL.md and .autoharness/harness-manifest.yaml. It MUST NOT add, edit or remove any Python module; MUST NOT touch templates/agents/_ship.agent.md.tmpl or .github/agents/_ship.agent.md; MUST NOT touch .github/policies/workflow-policies.md or templates/policies/; MUST NOT edit templates/skills/harness-architect/SKILL.md.tmpl (the template's own single-brace suffix spelling is a NON-BLOCKING FOLLOW-UP carried in the stash and OUT OF SCOPE here); MUST NOT edit .github/skills/install-harness/SKILL.md (its two-surface variable reference is READ-ONLY input here); MUST NOT generate any other absent skill surface; and MUST NOT modify the pre_claim topology gate or any bootstrap-grant mechanism. The Ship pre-task harness-generation lifecycle, the harness-surface resolver and the HARNESS_READY/NO_HARNESS contract belong to 181-F/187-S and are OUT OF SCOPE here - installing them in this commit would re-create the deadlock in a new place.

CLAIM ADMISSION - NOT ADMITTED. This task DOES NOT CARRY the harness-ready label. The label is deliberately ABSENT from this record because the P-004 evidence that earns it has not yet been produced and recorded, and P-002's ordinary ready-queue filter therefore does not admit this task at queue building or at claim. That is the correct state, not a defect: the label is applied only after both P-004 channels have been observed and their postcondition durably recorded. No exception, no carve-out, no grant, no --force, no force-audit entry, no policy-text edit, no gate edit and no self-authorization is used to substitute for it.

FIRST ACTION IS A GATE READ, FAIL-CLOSED: confirm 182.002-T recorded Compilation: PASS and Red Phase: CONFIRMED in .autoharness/harness-manifest.yaml. If that postcondition is absent, unreadable or not affirmatively both, TOUCH NO FILE, make NO COMMIT, exit non-zero and return the unit to Stage. If the generated skill is missing or non-conforming, or if the manifest entry and the on-disk file fail checksum parity, the unit resolves to HARNESS_ARCHITECT_ABSENT at 182.004-T and HALTS. ROLLBACK: git revert of this single commit removes BOTH the generated skill AND its manifest entry together - they are one rollback unit - and returns 182.001-T's assertion to failing, which is its authored state. Blocked by 182.002-T. Blocks 182.004-T. Plan docs/plans/2026-09-20-harness-architect-bootstrap-plan.md (plan_id harness-architect-bootstrap, revision 4, disposition REMEDIATED-PENDING-REVIEW, awaiting independent attempt 04). Verdict manifest docs/reviews/2026-09-20-harness-architect-bootstrap-plan-review.md. Decision docs/decisions/2026-09-18-shared-execution-architecture-and-portfolio-reslicing-decision.md (revision 4, D9 and D11). Source 76EBDE6D.