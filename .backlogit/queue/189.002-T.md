---
artifact_type: task
created_at: 2026-09-26T07:47:31.5147623Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: v1
    size_source: agent
dependencies:
    - 189.001-T
id: 189.002-T
labels:
    - harness-surface:harness-architect
    - ship-lifecycle
    - unit-b
    - unit-b-entry
parent_id: 189-F
priority: high
references:
    - docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md
    - docs/reviews/2026-09-25-ship-lifecycle-release-units-plan-review.md
status: queued
title: 'Ship lifecycle B4b: reducer, FI-5 early return and resolve_shipment entry'
updated_at: 2026-09-26T07:47:38.0446676Z
---



<!-- BEGIN:acceptance-criteria -->
- API: the keyword-only entry resolve_shipment(*, workspace_root, shipment_id) calls the private _resolve(*, workspace_root, shipment_id, limits) with the FI-2 defaults; _resolve's limits is the only B patch point (IM-06) and no public caller can pass limits; _resolve returns the result and the reader's ReadUsage; resolve_shipment returns only the result.
- Reducer order 1, 1b, 2 to 8 with first-applicable selection (FI-6). Early return (FI-5): the first read-limit error stops all further read and recheck requests.
- IM-16 and IM-04 tests call _resolve with small ReadLimits so every read-limit code is reachable (with the defaults, FILE_COUNT_LIMIT is not).
- IM-16 (a): a completed disagreeing ledger recheck, and separately a completed disagreeing surface recheck, each followed by a read-limit error -> INPUT_CHANGED_DURING_RESOLUTION.
- IM-16 (b): for each of the three read-limit codes and each feasible stage, a read-limit error first with a later recheck arranged to disagree -> that code, native code and stage in diagnostics, and usage.files_claimed unchanged after the error; infeasible stages recorded with the reason (for example, byte codes at an absent candidate).
- IM-04: injection at each of the 7 ReadStage values (trace matrix); two read-limit errors in both orders -> the first-occurring code at the reducer; byte codes never raised at an absent candidate; each higher class beats every lower class, with the lower-class fact first in both encounter and task-ID order, without short-circuiting the collection of lower-class facts.
- All fixtures are temporary workspaces; no test reads the real repository surface (IM-10).
- RED record before implementation (IM-13, per the A1 wording): task ID, exact P-004 canonical command, native exit, and each roster test's outcome with its own marker AHLC_B4B_REDUCER_EARLY_RETURN:<t> per the Marker Convention (pairwise distinct; a shared marker is refused under FI-9). Any structural test that reaches no stub (for example an API signature test) is recorded outside the roster, like characterization tests.
- Environment prechecks: git worktree list shows one worktree; git status --porcelain is empty for the task's files before the first write; the canonical command runs from the repository root. Over the estimate by more than 30 minutes -> stop and return to Stage for a split.
- IM-14: no added text claims race, TOCTOU or hardlink-alias resistance.
- IM-15: a test or proof PASS confers no claim authority; ordinary P-001/P-002/P-004/P-020 gates at claim and closure.
- Manifest harvest carry IM-14-F52 (E4): B4b's frozen Files cell omits tests/test_harness_noclaim_audit.py; when B4b adds or changes a line of harness_surfaces.py that is a detector hit (a trigger) or a LEDGER-ed line, B4b adds the audit test to its Files and adds or updates the LEDGER entry (a non-claim confirmed by local review); otherwise the Files budget stands.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan task B4b (docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md, blob b7a77c76, section 'B4b: Reducer, early return and resolver entry'; release unit S(B-entry); review authority: manifest E5 Verdict under OP-5).

Files (budget): src/autoharness/harness_surfaces.py; tests/test_harness_surfaces_reducer.py (plus tests/test_harness_noclaim_audit.py only under the IM-14-F52 carry).

Change: The keyword-only entry resolve_shipment(*, workspace_root, shipment_id), which calls the private _resolve(*, workspace_root, shipment_id, limits) with the FI-2 defaults. _resolve's limits is the only B patch point (IM-06); no public caller can pass limits. _resolve returns the result and the reader's ReadUsage; resolve_shipment returns only the result. Reducer order 1, 1b, 2 to 8 with first-applicable selection (FI-6). Early return (FI-5): the first read-limit error stops all further read and recheck requests.

Marker: AHLC_B4B_REDUCER_EARLY_RETURN (prefix). Trace: IM-04 (B4b stages and the three tests), IM-06 (private limits hook), IM-16 (both orders), PE-DATA-01 to 03, IM-10, IM-13, IM-14, IM-15. Estimate 110 min; size S; complexity medium.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Label harness-surface:harness-architect (Python production module under src/): roster and RED obligation apply. Blocked by B4a; blocks B5 (same module). Supersedes retired 181.016-T (resolver and reducer; early return, IM-16). At 110 minutes this is the largest B task: the 30-minute overrun stop applies strictly (return to Stage for a split; never a stretched estimate).
<!-- END:implementation-notes -->