---
artifact_type: shipment
created_at: 2026-08-10T08:13:25.0325622Z
custom_fields:
    items:
        - 118-F
        - 118.003-T
        - 118.001-T
        - 118.002-T
        - 118.004-T
        - 118.005-T
        - 118.006-T
        - 118.007-T
id: 127-S
links:
    - target_id: 124-S
      link_type: supersedes
priority: critical
status: queued
title: 'S1 Copilot supervisor: safety contracts + characterization baseline (P0)'
updated_at: 2026-08-11T02:11:09.0959443Z
---

Shipment 1 of the strict serial chain `127-S` -> `128-S` -> `129-S` for Plan 1 (Local Copilot CLI supervisor / control-plane runtime). Supersedes the retired `124-S`.

MANIFEST CONTRACT (H10.5 — fully-covered root covering feature). This manifest lists the covering feature `118-F` FIRST, followed by all SEVEN of its children (`118.003-T`, `118.001-T`, `118.002-T`, `118.004-T`, `118.005-T`, `118.006-T`, `118.007-T`) in dependency order (`118.006-T` and `118.007-T` were added on 2026-08-11 by the F27 and F26 rulings respectively). Two invariants make this close provably safe on backlogit 1.8.0:

(i) FULL COVERAGE — `118-F` has exactly these SEVEN children and no others; the invariant was RE-VERIFIED after `118.006-T` and `118.007-T` were added, because adding a child to a covering feature without adding it to the manifest is precisely what would break it. At close, `releaseScopeItemIDs` expands to the feature plus every descendant, so `returnUnreleasedFeatureItems(118-F)` finds every descendant already in release scope and returns the empty set. NO `parent_id` is cleared, anywhere. Because the covering feature is fully covered, `setArtifactStatus(118-F, done)` and the `collectArchiveCandidateIDs` sweep act only on items this shipment is already releasing — the F13 hazard (member feature with unshipped children) is structurally impossible here.

(ii) ROOT PLACEMENT — `118-F` has NO parent feature. `featureScopeRoots` walks `parent_id` upward only, so scope discovery terminates at `118-F` and can never reach `119-F`, `120-F`, or the umbrella `117-F`. Closing this shipment cannot touch any artifact outside its own manifest.

WHY THE OLD TOPOLOGY FAILED: `124-S` was task-only over the shared covering feature `117-F`, whose other 14 children lived in `125-S`/`126-S`. `returnUnreleasedFeatureItems` is NOT gated by `explicitScope` and runs for non-member ancestor features too, so closing `124-S` would have cleared `parent_id` on all 14. The proposed remedy (Ship re-adopting via `adopt_item`) is REJECTED: re-parenting is outside Ship's Role Boundary (fail-closed P-010) and post-close manual repair is not reliability-first. The defect is now eliminated structurally rather than mitigated procedurally.

NO SHIP-SIDE MITIGATION IS REQUIRED OR PERMITTED. CLOSE-PATH CONTRACT: this manifest intentionally lists its covering feature, which is the FULLY-COVERED ROOT exception to the task-only rule in docs/compound/097-S-shipment-task-only-safe-close.md - see the "Reconciliation - the FULLY-COVERED ROOT exception" section there. The task-only rule targets PARTIAL-feature shipments; task-only membership does NOT make this topology safe (ARM A orphaned 14/14 tasks). Close with a single `backlogit shipment ship` - which becomes an EXECUTABLE instruction only after `118.007-T` (a member of this shipment) lands the verified fully-covered-root exception in P-015 and the Ship agent. Until that task is complete the cascade operation remains prohibited, so `118.007-T` is a hard prerequisite of this shipment's own close path. Ship claims, executes, and closes. There is no `adopt_item` step, no parentage verification step, no feature reactivation, and no operator intervention after closure. Expected `ShipShipment` result: `returned_ids: []`; `archived_ids` = the seven tasks + `118-F` + `127-S`.

PROOF: `docs/spikes/2026-08-09-plan1-shipment-topology-proof/sim-shipment-closure.ps1` exercises the real backlogit engine on a disposable fixture; the run that produced the published totals was attributed to v1.8.0-dirty commit fd8d2c9d, and a later re-run on the clean release v1.9.0 commit 39528a4 reproduced them. Those identities are EVIDENCE LABELS recording which build produced which run - they are NOT an allowlist and NOT a required build. ARM A reproduces the old topology and orphans 14/14 downstream tasks; ARM B reproduces this topology and passes 66/66 assertions with `returned_ids: []` on all three closes.

SCOPE: safety contracts + characterization baseline (P0). ZERO observable behavior change — pure additions plus characterization tests pinning today's `start.ps1` / `start.sh` contract (H1 characterize-before-migrate). This is the only shipment in the chain with no blocking predecessor; `128-S` blocks-on `127-S`.

RESOLUTION - ALL GATES ON THIS SHIPMENT ARE CLOSED (2026-08-11, operator rulings accepted). Every P1 previously recorded against 127-S has an accepted ruling and the owning task text has been amended to implement it. F17 (RULING 4 - re-inventory and correct completely): 118.001-T now pins start.ps1's REAL contract, including that GITHUB_TOKEN is guarded and non-fatal while GITHUB_PERSONAL_ACCESS_TOKEN is assigned unguarded at line 65, plus the observable sidecar side effects and TTY attachment; 118.002-T is restated as start.sh's OWN five-dimension contract with the four absences (no ENGRAM_DATA_DIR, no PAT, no COPILOT_USE_REMOTE/--remote, no sidecars) asserted AS ABSENT, and cross-platform normalization is explicitly deferred to Shipment 3. The false 'same seven dimensions' parity premise is withdrawn, so the baseline is now SATISFIABLE. F22 (RULING 1 - DRAINING invariant): the guaranteed-release obligation is placed on the 119.003-T state machine as the sole terminal gateway, and 118.005-T supplies the idempotent, crash-safe release contract it calls; this shipment's exposure is therefore an API-shape requirement that is now explicitly stated, not an open question. F27 (RULING 9 - atomic OS-backed lock, AS AMENDED BY THE F34 RULING 2026-08-11): 118.005-T now REQUIRES atomic acquisition via a SINGLE OS advisory lock (msvcrt.locking / fcntl.flock) held on a STABLE, NEVER-DELETED GUARD FILE, with holder metadata in a SEPARATE REMOVABLE RECORD file; O_CREAT|O_EXCL is REMOVED as a permitted backend, leaving no backend ambiguity. It explicitly PROHIBITS check-then-write, demotes the PID+start-time record to stale-lock diagnosis only, and mandates a real parallel-contender suite (>=8 simultaneous contenders x >=50 iterations) - a sequential contention test is declared insufficient evidence. Stale-record lifecycle, --force-unlock and recycled-PID rejection were split into the new 118.006-T to keep both tasks inside the 2-hour box. F26 (RULING 8 - amend P-015): the new 118.007-T is a MEMBER OF THIS SHIPMENT and amends P-015, the Ship agent template, the shipment-reconcile skill and the compound close-path doc coherently, adding a machine-checkable verified fully-covered-root exception. It is deliberately placed here because it must land BEFORE the first close, and it aligns the permitted close operation with the 64/64 executable evidence WITHOUT requiring Ship to perform any P-010-forbidden operation. NO P0 OR P1 REMAINS OPEN AGAINST 127-S.

GATE STATE REVERTED 2026-08-11: the Cycle-16 GATE-CLEAR declaration is WITHDRAWN. The confirmatory current-HEAD Copilot review of 90a011c5 returned three surviving P1s (F30, F31, F32/F33) - see docs/reviews/2026-08-09-copilot-cli-supervisor-control-plane-review.md. Both F30 (the 118.007-T P-015 exception as drafted would reject 129-S, so the permitted close operation is not actually established for all three manifests) and F31 (--force-unlock stale cleanup remains a check-then-act race that can delete a live holder, in 118.005-T/118.006-T) land INSIDE this shipment. This shipment remains the only structurally eligible cursor, but eligibility is not clearance. Stage HALTED per the operator's one-bounded-pass standing instruction and attempted no remediation. NOT gate-clear.

GATE STATE 2026-08-11 (Cycle 18): the F30/F31/F32-F33 gate is CLEARED by three final operator rulings, validated structurally (verifier V13 quantified close-path predicate with negative controls; V14 approval-reachability from the runtime chain tail). F31 lands here (118.005-T exposes the reusable exclusion primitive; 118.006-T holds it across inspection AND removal with compare-and-delete plus a real contender-vs-cleanup race test). AMENDED 2026-08-11 BY THE F34 RULING: that reusable primitive is the STABLE, NEVER-DELETED GUARD FILE lock, holder metadata lives in a SEPARATE REMOVABLE RECORD file, O_CREAT|O_EXCL is removed as a backend, and 118.006-T performs compare-and-REPAIR of the RECORD under the guard (never deleting the guard). The F31 safety property is preserved; only the primitive and the removal target changed, which is what made the protocol implementable. F30 also lands here: 118.007-T's P-015 predicate is now quantified over every feature member and admits a verified-childless terminal umbrella, so it selects the cascade path for ALL THREE manifests including 129-S - the manifest the withdrawn wording would have rejected. 118.007-T must still land before ANY shipment closes. All three findings are RESOLVED; 0 unresolved P0 / 0 unresolved P1. GATE-CLEAR IS NOT AN INSTRUCTION TO CLAIM - claiming remains Ship's decision under Ship's own Role Boundary.

GATE STATE RE-CLEARED 2026-08-11 (Cycle 19 F34 remediation, operator ruling accepted and applied): the Cycle-19 withdrawal is now DISCHARGED and this shipment is GATE-CLEAR again. F34 (P1) landed HERE, in 118.005-T/118.006-T: the F31 cleanup protocol required force-unlock to acquire the same primitive as acquisition, which was UNIMPLEMENTABLE under the permitted O_CREAT|O_EXCL backend (a stale lock means the path already exists, so exclusive-create can never succeed and --force-unlock becomes permanently unreachable, leaving the workspace strandable) and UNSAFE under the advisory-lock-then-unlink backend (inode/path race in which two processes both believe they hold exclusion). THE ACCEPTED F34 RULING, APPLIED TO THE OWNING TASK SPECS: a STABLE NEVER-DELETED OS-LOCKED GUARD FILE is the SOLE exclusion primitive; holder/diagnostic metadata lives in a SEPARATE REMOVABLE RECORD file; BOTH normal acquisition AND force-unlock/stale cleanup must acquire the SAME guard lock BEFORE reading, validating, replacing or removing metadata; O_CREAT|O_EXCL is REMOVED as a backend so no backend ambiguity remains; a LIVE HOLDER PREVENTS CLEANUP, proven by REAL contender/race tests (cleanup can neither remove nor replace a live holder's metadata; stale metadata is repairable only under the guard; the guard survives every force-unlock outcome with unchanged file identity) each backed by a positive control; prior F27/F31 requirements are PRESERVED and acceptance criteria are EXECUTABLE ON WINDOWS AND POSIX via a standard-library OS-lock mechanism (msvcrt.locking / fcntl.flock) or an explicitly scoped platform-adapter contract. The ruling is propagated to 118.005-T, 118.006-T and (for operator reachability of --force-unlock) 120.006-T in 129-S - not merely summarised here. See docs/reviews/2026-08-09-copilot-cli-supervisor-control-plane-review.md. This shipment is structurally eligible AND gate-clear; eligibility and clearance are still not an instruction to claim, which remains Ship's decision.

ENGINE VERSION BINDING 2026-08-11, SUPERSEDED IN PART 2026-08-12 (recorded for Ship; does NOT change gate state). HISTORICAL RECORD - the identities below are EVIDENCE LABELS, not a required build; the dynamic-attestation guard at the end of this entry is what governs close. The operator asked whether backlogit had been upgraded before F30 was finalised. At that time it had NOT: both the CLI and the MCP daemon reported commit fd8d2c9d, and as re-verified on 2026-08-11 during the F34 pass BOTH surfaces also reported the SAME build timestamp 2026-08-11T01:25:43Z (CLI v1.8.0-dirty). This RETIRED the earlier two-builds-of-one-commit caveat, which recorded the MCP daemon at the older 2026-08-02T07:27:31Z build; the daemon had since been restarted onto the same build the evidence was produced against. UPDATE 2026-08-12: the installed CLI was subsequently upgraded to the CLEAN RELEASE v1.9.0, commit 39528a4, and the verifier (221/221) and simulation (66/66) were re-run and PASS on it - which DISCHARGES the -dirty caveat recorded below, because the proof now also holds on a build whose exact behaviour IS reproducible from a commit. During that same session the long-lived MCP process still reported the pre-upgrade v1.8.0-dirty/fd8d2c9d because it held that binary in memory, so the "both surfaces report the same build" statement above is HISTORICAL and was not true at the end of that session; that is a session artifact rather than a workspace defect, since the artifacts are plain Markdown read identically by both builds and every structural assertion cited was produced by the CLI-driven harnesses. All close evidence was produced against the installed CLI of its day; the simulation includes two assertions that FAIL CLOSED if the engine build cannot be identified. Compatibility was not inferred from the version number: at the time of the fd8d2c9d evidence, backlogit's source was 128 commits ahead with heavy changes to the close surface (shipment_lifecycle.go, dependencies.go, a new shipment_gate_manifest.go, shipment_covering.go, shipment_verify.go, archive.go), so that unreleased code was read directly - DeriveCoveringFeature still selects the first parent-first root feature and still tolerates an additional root, and the new strict variant is fail-closed only on lookup errors and reachable only from the opt-in formal-gate digest (disabled here). The F30 premise holds on both builds; no new finding. FORMER CAVEAT, NOW DISCHARGED: the fd8d2c9d build was -dirty, so its exact behaviour was not reproducible from any commit; the 2026-08-12 re-run on released v1.9.0/39528a4 removes that limitation. (The earlier SECOND caveat - that backlog mutations went through the MCP build while evidence went through the CLI build - was RETIRED as of 2026-08-11 and applied to the earlier mutations, which remain trustworthy because the CLI independently read back and validated every edge the MCP surface wrote.) SHIP-FACING GUARD - DYNAMIC ENGINE ATTESTATION (REPLACES THE FORMER EXACT-COMMIT PIN, P1-1 ruling 2026-08-12): Ship closes against whatever is installed AT CLOSE TIME, so the guard attests the ACTUAL INSTALLED ENGINE rather than comparing it to a historical commit. THE FORMER REQUIREMENT TO CONFIRM COMMIT fd8d2c9d IS REMOVED AND MUST NOT BE REINSTATED - and it MUST NOT BE REPLACED BY AN EXACT PIN TO 39528a4 OR ANY OTHER COMMIT. Historical commit identities recorded in this workspace are EVIDENCE LABELS describing which build produced which run; they are never a future allowlist. IMMEDIATELY BEFORE CLOSING, Ship MUST: (1) identify the actual installed backlogit engine(s) across every surface it will rely on - CLI and, if used for a relied-upon mutation or read, the MCP daemon - and RECORD the version, commit and build identity of each; (2) require that identity be DETERMINABLE and COHERENT for the exercised surfaces, meaning every surface resolves an identity and the surfaces do not disagree about a behaviour being relied upon; (3) re-run docs/spikes/2026-08-09-plan1-shipment-topology-proof/sim-shipment-closure.ps1 UNMODIFIED against that installed engine - the harness self-attributes via its ENGINE UNDER TEST block and already fails closed when identity cannot be resolved; (4) proceed ONLY when that current-engine run PASSES and all structural assertions hold. FAIL CLOSED - close is BLOCKED - on any of: engine identity unknown or unresolvable on any exercised surface; a CLI/MCP mismatch touching a behaviour the close relies on; or a simulation failure. A newer or older build is NOT itself a blocker; an unattested or incoherent one is.
