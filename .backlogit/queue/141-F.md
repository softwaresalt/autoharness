---
artifact_type: feature
created_at: 2026-08-21T20:21:04.1402002Z
custom_fields:
    harness_status: pending
id: 141-F
labels:
    - bug
    - tests
    - isolation
    - regression-guard
    - p-021
    - windows
priority: high
references:
    - docs/plans/2026-08-21-full-suite-test-isolation-plan.md
    - docs/plans/2026-08-21-full-suite-test-isolation-hardening.md
    - docs/reviews/2026-08-21-full-suite-test-isolation-review.md
    - .backlogit/queue/024-DL.md
status: queued
title: Full-suite test-order/global-state pollution — bisect to a minimal reproducing pair, then fix at source
updated_at: 2026-08-21T20:28:51.0920239Z
---

Make the canonical local test gate GREEN by removing the cross-test global-state pollution that fails five tests only in a full-suite run — fixing the POLLUTER, not the victims, and leaving all five victim assertions intact.

**Source stash:** E8158860 (DEFERRED SCOPE EXPANSION, P-021)
**Deliberation:** 024-DL
**Plan:** `docs/plans/2026-08-21-full-suite-test-isolation-plan.md`
**Hardening:** `docs/plans/2026-08-21-full-suite-test-isolation-hardening.md` — HARDENED (A1–A3)
**Review:** `docs/reviews/2026-08-21-full-suite-test-isolation-review.md` (PASS, 0 unresolved P0/P1; A4–A5 applied)

**Victims** (3 failures + 2 errors; the 2 errors are the two `check=True` git-subprocess paths):
1. `test_gate_pipeline_topology_cli.py::PipelineTopologyStorageRootResolutionTests::test_backlog_only_workspace_succeeds` (`git init` exit 128)
2. `test_gates_topology.py::FilesystemTopologyReadersTests::test_empty_queue_and_archive_dirs_pass_as_zero_shipments`
3. `test_repo_root_artifacts.py::RepoRootTrackedJsonAllowlistTest::test_root_tracked_json_matches_allowlist`
4. `test_telemetry_gitignore_template.py::MetricsGitignoreTests::test_git_check_ignore_matches_metrics_artifacts`
5. `test_telemetry_gitignore_template.py::MetricsEmissionHardGateTests::test_emitted_metrics_artifacts_are_never_tracked`

**Established facts (024-DL):** reproduces on the CANONICAL gate (`python -m unittest discover -s tests`) and on pytest; does NOT reproduce on hosted Linux CI (146-S/147-S closure records) — i.e. Windows-local, CI-invisible; pre-existing on merge-base `b9d91b18`; all five pass when the three `tests/test_scope_containment_*` modules are excluded.

**Stage static findings that REFUTE the entry's own hypotheses:** no `os.chdir` anywhere in `tests/`; no bare `os.environ[...]` assignment anywhere in `tests/`; every `patch.dict(os.environ, ...)` is context-managed with no unstopped `.start()`; the three suspected polluter modules write nothing into the live working tree. Leading structural suspect: 58 `tempfile.TemporaryDirectory(dir=Path.cwd())` sites creating temp trees INSIDE the live working tree.

**Feature acceptance criteria**
- AC-F1. Canonical full-suite gate GREEN on Windows: zero failures, zero errors (skips unchanged).
- AC-F2. All five victim tests retain their original assertions VERBATIM — proven by diff (changes confined to setUp/tearDown/imports, zero assertion-line edits) and by identical in-isolation pass/fail semantics (amendment A1).
- AC-F3. Zero `dir=Path.cwd()` sites remain under `tests/`; the AST structural guard passes with an EMPTY allowlist.
- AC-F4. The guard is GREEN after every subtask (shrinking allowlist, amendment A4) — no intermediate commit carries a deliberately-red test.
- AC-F5. Hosted CI remains green.

**Hard stop (amendment A2):** if the bisect task cannot isolate a minimal reproducing pair inside its time box, it returns blocked with the narrowed candidate set and the feature closes with the diagnosis + hygiene tasks only. No speculative fix.

**Out of scope (P-021 C1):** any change under `src/autoharness/`. If the bisect implicates production code, that is a different contract surface and re-enters P-021 capture (024-DL R5).