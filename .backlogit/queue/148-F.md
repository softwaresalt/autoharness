---
artifact_type: feature
created_at: 2026-08-28T03:45:18.9204891Z
custom_fields:
    harness_status: pending
id: 148-F
labels:
    - S0
    - prerequisite
    - policy-registry
    - review-personas
    - 336F3AB7
priority: critical
references:
    - docs/plans/2026-08-27-policy-registry-and-review-persona-layer-plan.md
    - docs/decisions/2026-08-27-pre-review-evidence-dag-shipment-portfolio-deliberation.md
status: queued
title: S0 — Policy registry and review-persona layer install/restore (336F3AB7)
updated_at: 2026-08-28T07:27:43.0620162Z
---

Install/restore the missing policy registry (.github/policies/workflow-policies.md) and the review-persona layer (.github/agents/subagents/) so policy IDs P-001..P-021 resolve and policy/persona routing becomes executable. Prerequisite release unit S0; blocks S1.

<!-- BEGIN:description -->
**Source stash lineage**: critical bug `336F3AB7` — "Policy registry and review-persona layer are cited repo-wide but were never installed." This feature is the SOLE downstream owner of that entry.

**Stash disposition — PAST TENSE, ALREADY COMPLETE (restated 2026-08-28, review-fix cycle 2).** `336F3AB7` **WAS consumed and archived during STAGING on 2026-08-28T04:14:26Z**, non-destructively via `backlogit stash archive`, after lineage was preserved here and shipment verification passed. **This happened BEFORE execution begins. It is NOT a pending shipment step.** **Ship MUST NOT attempt to archive it again** — `stash get 336F3AB7` already returns `not found`, a re-archive attempt will fail, and no archive action is owed by this shipment.

**Reverse-lineage caveat PRESERVED.** The archived record in `.backlogit/archive/stash.jsonl` carries only `id`, `priority`, `kind`, `text`, `created_at`, `archived_at`, and `reason: "archived"`. It has **no forward disposition field**, and backlogit exposes **no official operation to update an archived stash entry**. Lineage is therefore recorded in the successors and is reconstructible only in REVERSE:

```text
336F3AB7  ->  031-DL (deliberation, done)
          ->  docs/plans/2026-08-27-policy-registry-and-review-persona-layer-plan.md
          ->  148-F (feature, queued)  ->  148.001-T .. 148.008-T
          ->  156-S (shipment, queued)
```

**Authoritative portfolio**: `031-DL` `docs/decisions/2026-08-27-pre-review-evidence-dag-shipment-portfolio-deliberation.md` section 'Shipment Portfolio' -> **S0 — Prerequisite closure**, and section R3 (hidden prerequisites, must not be duplicated). The backing `031-DL` work item was created 2026-08-28 to close a dangling-ID defect and is linked to this feature.

**Reviewed plan**: `docs/plans/2026-08-27-policy-registry-and-review-persona-layer-plan.md` (impl-plan -> plan-harden -> plan-review). **Re-reviewed 2026-08-28** after a current-HEAD local review of `1bafd85e` returned BLOCKED (P0=0, P1=1, P2=6, P3=2). **Amended 2026-08-28 (review-fix cycle 2)** after a second local review of `f54152ec`; the PASS was RETAINED, not re-run — see the plan's 'Amendment record'. Operative verdict: `decision: PASS`, `dispatch_mode: single-agent-declared-degradation`.

**Operator authority consumed**: **Q7 APPROVED** — clear S0, do not waive it; S0 must be staged before S1. **Q5 APPROVED** — authoritative test command is `PYTHONPATH=src python -m unittest discover -s tests`, which is directly load-bearing because the policy-registry template carries a `{{TEST_COMMAND}}` placeholder. **Q1 APPROVED** — report persistence allowed with named consumers.

**GAP 1 — policy registry absent**: template `templates/policies/workflow-policies.md.tmpl` exists (83,467 bytes, 27 `P-0NN` headings); `.github/policies/` does not exist; `verify_workspace.py` `DARK_FACTORY_ASSERTIONS` declares `dark_factory_policy_contract` on that path with `required: True` (L866).

**GAP 2 — review-persona layer absent**: `.github/agents/subagents/` does not exist.

**GAP 2 audit — CORRECTED 2026-08-28.** The prior description claimed `python-reviewer` had no template anywhere and that 3 templates had no citing reader. Both claims were false:

* **`python-reviewer` is a RENDER TARGET, not a missing template.** Its canonical source is `templates/agents/review/technology-reviewer.agent.md.tmpl`, which installed `.github/skills/install-harness/SKILL.md` L1203 maps to `.github/agents/subagents/{{PRIMARY_LANGUAGE_LOWER}}-reviewer.agent.md`. `.autoharness/harness-manifest.yaml` L394 binds `PRIMARY_LANGUAGE_LOWER: "python"`, so that mapping renders exactly `python-reviewer.agent.md` here. **No new template is authored.**
* **`correctness-reviewer` and `maintainability-reviewer` DO have named readers**, cited by bare filename rather than installed path: `install-harness/SKILL.md` L1200-L1201 designates both "Always-on", and `tune-harness/SKILL.md` L462-L469 treats a review layer lacking either as real local-first review drift. A path-shaped grep cannot see bare-filename citations — that measurement-shape defect caused the original misclassification.
* **Law-2 exclusion set is now EMPTY.** All 12 `templates/agents/review/` templates plus `learnings-researcher` install, yielding **13 persona artifacts** (**14 installed artifacts** including the policy registry).

**SYNTHESIZED-BINDING GAP — CLOSED 2026-08-28 (review-fix cycle 2, P2-2).** Five placeholders across two persona templates are classed by installed `install-harness/SKILL.md` L329-L333 as *"Synthesized from language ... model"* — i.e. the installer is expected to INVENT their content. A "mechanical render" over a synthesized placeholder would force Ship either to emit unresolved `{{...}}` (failing the DoD) or to write **unreviewed persona content into a normative review surface**. All five are now pinned with reviewed in-repo derivations in plan decision **D8** (owned by U3 / `148.003-T`), consumed verbatim by U5 and U6, and asserted by U8 scenario 5:

| Variable | Template | Pinned value |
|---|---|---|
| `{{CONCURRENCY_PATTERNS}}` | `concurrency-reviewer.agent.md.tmpl` | `asyncio, task, queue, thread, process` (copied from `verify_workspace.py` `_language_defaults("python")`) |
| `{{LANGUAGE_SAFETY_CHECKS}}` | `technology-reviewer.agent.md.tmpl` | D8-B bullet list |
| `{{LANGUAGE_IDIOM_CHECKS}}` | `technology-reviewer.agent.md.tmpl` | D8-B bullet list |
| `{{LANGUAGE_ERROR_HANDLING_CHECKS}}` | `technology-reviewer.agent.md.tmpl` | D8-B bullet list |
| `{{LANGUAGE_PERFORMANCE_CHECKS}}` | `technology-reviewer.agent.md.tmpl` | D8-B bullet list (weakest derivation — RK-J) |

Templates remain technology-agnostic: values bind at render time into installed artifacts only; **no `.tmpl` is edited** (D8-C).

**Not re-owned here**: `8AC574F1` (residual skill install gap) is untouched.
<!-- END:description -->

<!-- BEGIN:dod -->
* `.github/policies/workflow-policies.md` exists, zero unresolved `{{...}}`, 27 `P-0NN` headings, all `dark_factory_policy_contract` `must_contain` tokens present, body byte-identical to template modulo the 9 bound placeholders (INV-1).
* Every persona path cited by any installed skill/agent under `.github/agents/subagents/` resolves (route-resolution test, not file-presence test), with the literal `{{PRIMARY_LANGUAGE_LOWER}}` citation **expanded** (preferred) or **exempted** by a named rule — never asserted to resolve raw.
* All 13 personas installed; each has a **named reader** (installed-path OR bare-filename citation). No test asserts the absence of `correctness-reviewer`, `maintainability-reviewer`, or `technology-reviewer`.
* `correctness-reviewer.agent.md` and `maintainability-reviewer.agent.md` present, so `tune-harness` local-first review drift does not trigger.
* `python-reviewer.agent.md` rendered from `technology-reviewer.agent.md.tmpl`, with that template source recorded in its manifest entry; no `python-reviewer.agent.md.tmpl` is created.
* **ADDED (P2-2)**: all five synthesized persona variables bound to their **verbatim D8 pins**; `{{CONCURRENCY_PATTERNS}}` cross-checks equal to the live `_language_defaults("python")` value; **no synthesized/improvised persona content in any installed artifact**; both source templates (`technology-reviewer.agent.md.tmpl`, `concurrency-reviewer.agent.md.tmpl`) **unmodified**, retaining their placeholders (D8-C).
* **ADDED (D8-D)**: the zero-`{{...}}` scan exempts fenced-code output-schema exemplars (`{{file_path}}`, `{{line_number}}`) by a named commented rule; no other placeholder is exempted.
* All three stale DANGLING manifest notes reconciled (plan-review, review, shipment-reconcile), each retaining its original gap history; no note deleted. **No stash archive action is performed — `336F3AB7` was already archived during staging.**
* `_resolve_policy_registry` template-fallback branch preserved (INV-2).
* `verify-workspace` exit-code behavior unchanged pre/post (INV-3) — a change is a hard stop, not a finding.
* All 14 manifest checksums round-trip from the installed files (LF-only, INV-5).
* Gate: `PYTHONPATH=src python -m unittest discover -s tests` green.
* Carried-forward findings recorded, not fixed: `workspace-profile.yaml` stale `test.runner: pytest`; `8AC574F1` residual; **RK-J** (`LANGUAGE_*_CHECKS` are Stage-reviewed prose, not resolver-derived; follow-up is to add those keys to `_language_defaults` — out of S0 scope).
* **Accepted bounded residual risk RK-K**: resolved checkpoint `checkpoint-20260828-041509.json` records the pre-correction S0 state and **cannot be amended** — backlogit exposes no official amendment path for a resolved checkpoint, and hand-editing tool-owned state is forbidden. It is left byte-intact. Contained because it is **resolved** (the recovery protocol scans active checkpoints only, so it can never be selected), it is named in the superseding checkpoint's `supersedes_checkpoint`, and the correction is recorded in `docs/memory/2026-08-28-stage-156s-blocked-review-repair.md` and the plan (RK-K). Not a gate on execution.
<!-- END:dod -->

<!-- BEGIN:goals -->
1. Render and install the policy registry so P-001..P-021 resolve from an installed authoritative surface.
2. Reconcile the in-repo contradiction between the tolerant `_resolve_policy_registry` and the `required: True` dark-factory assertion.
3. Install the 13 persona identities, rendering `python-reviewer` from the **existing** `technology-reviewer` template rather than authoring a Python-specific duplicate.
4. Bind every synthesized persona variable to a **reviewed, pre-pinned** value (D8) so no unreviewed persona content is improvised into a normative review surface, while keeping the source templates technology-agnostic and unmodified.
5. Register all 14 installed artifacts in the harness manifest with LF-only SHA-256 checksums, and reconcile **all three** stale DANGLING notes (plan-review, review, and shipment-reconcile).
6. Prove both gaps closed by route-resolution (with declared placeholder handling), named-reader tests, and pinned-binding conformance.
<!-- END:goals -->