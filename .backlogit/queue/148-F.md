---
artifact_type: feature
created_at: 2026-08-28T03:45:18.9204891Z
custom_fields:
    harness_status: pending
id: 148-F
labels:
    - S0
    - prerequisite
    - policy-registry
    - review-personas
    - 336F3AB7
priority: critical
references:
    - docs/plans/2026-08-27-policy-registry-and-review-persona-layer-plan.md
    - docs/decisions/2026-08-27-pre-review-evidence-dag-shipment-portfolio-deliberation.md
status: queued
title: S0 — Policy registry and review-persona layer install/restore (336F3AB7)
updated_at: 2026-08-28T03:45:44.3848067Z
---

Install/restore the missing policy registry (.github/policies/workflow-policies.md) and the review-persona layer (.github/agents/subagents/) so policy IDs P-001..P-021 resolve and policy/persona routing becomes executable. Prerequisite release unit S0; blocks S1.

<!-- BEGIN:description -->
**Source stash lineage**: critical bug `336F3AB7` — "Policy registry and review-persona layer are cited repo-wide but were never installed." This feature is the SOLE downstream owner of that entry; `336F3AB7` is consumed and archived only after shipment verification succeeds.

**Authoritative portfolio**: `031-DL` `docs/decisions/2026-08-27-pre-review-evidence-dag-shipment-portfolio-deliberation.md` section 'Shipment Portfolio' -> **S0 — Prerequisite closure**, and section R3 (hidden prerequisites, must not be duplicated).

**Reviewed plan**: `docs/plans/2026-08-27-policy-registry-and-review-persona-layer-plan.md` (impl-plan -> plan-harden -> plan-review, `decision: PASS`, `dispatch_mode: single-agent-declared-degradation`).

**Operator authority consumed**: **Q7 APPROVED** — clear S0, do not waive it; S0 must be staged before S1. **Q5 APPROVED** — authoritative test command is `PYTHONPATH=src python -m unittest discover -s tests`, which is directly load-bearing because the policy-registry template carries a `{{TEST_COMMAND}}` placeholder.

**GAP 1 — policy registry absent**: template `templates/policies/workflow-policies.md.tmpl` exists (83,467 bytes, 27 `P-0NN` headings); `.github/policies/` does not exist; `verify_workspace.py` `DARK_FACTORY_ASSERTIONS` declares `dark_factory_policy_contract` on that path with `required: True` (L866).

**GAP 2 — review-persona layer absent**: `.github/agents/subagents/` does not exist. 11 personas are cited by installed skills/agents; 10 have templates (9 under `templates/agents/review/`, plus `learnings-researcher` under `templates/agents/research/`); **`python-reviewer` has no template anywhere**. 3 templates (`correctness-reviewer`, `maintainability-reviewer`, `technology-reviewer`) have no citing reader and are deliberately NOT installed per Law 2.

**Not re-owned here**: `8AC574F1` (residual skill install gap) is untouched.
<!-- END:description -->

<!-- BEGIN:dod -->
* `.github/policies/workflow-policies.md` exists, zero unresolved `{{...}}`, 27 `P-0NN` headings, all `dark_factory_policy_contract` `must_contain` tokens present, body byte-identical to template modulo the 9 bound placeholders (INV-1).
* Every persona path cited by any installed skill/agent under `.github/agents/subagents/` resolves (route-resolution test, not file-presence test).
* The 3 Law-2-excluded personas are absent and asserted absent.
* `_resolve_policy_registry` template-fallback branch preserved (INV-2).
* `verify-workspace` exit-code behavior unchanged pre/post (INV-3) — a change is a hard stop, not a finding.
* All manifest checksums round-trip from the installed files (LF-only, INV-5).
* Gate: `PYTHONPATH=src python -m unittest discover -s tests` green.
* Carried-forward findings recorded, not fixed: `workspace-profile.yaml` stale `test.runner: pytest`; `8AC574F1` residual.
<!-- END:dod -->

<!-- BEGIN:goals -->
1. Render and install the policy registry so P-001..P-021 resolve from an installed authoritative surface.
2. Reconcile the in-repo contradiction between the tolerant `_resolve_policy_registry` and the `required: True` dark-factory assertion.
3. Author the one missing persona template (`python-reviewer`) and install the 11 cited persona identities.
4. Register all 12 installed artifacts in the harness manifest with LF-only SHA-256 checksums, and amend the two stale DANGLING notes.
5. Prove both gaps closed by route-resolution and placeholder tests.
<!-- END:goals -->