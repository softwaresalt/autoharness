---
artifact_type: task
created_at: 2026-09-03T08:26:16.4281139Z
custom_fields:
    complexity: trivial
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.008-T
    - 160.012-T
    - 160.013-T
    - 160.009-T
    - 160.010-T
    - 160.015-T
    - 160.017-T
id: 160.011-T
labels:
    - docs
    - changelog
parent_id: 160-F
priority: medium
status: queued
title: Document payload boundary and record measured reduction
updated_at: 2026-09-03T15:38:45.4159734Z
---

Document the payload boundary and record the change (T15, terminal task).

DOCS:
- docs/installation.md - document what each channel delivers and what it deliberately does not, for ALL THREE channels (wheel, sdist, plugin). Make the "target workspaces receive only generated output, never autoharness engine files" boundary explicit for consumers. Reproduce the AC7 channel x environment support matrix (160.009-T) so the plugin channel's registration limits are stated, not implied.
- README.md - update the install section if payload/channel wording changes.
- CHANGELOG.md - record the packaging change and the MEASURED payload reduction.

MEASURED FIGURES, PER CHANNEL, NEVER AGGREGATED: the CHANGELOG entry and docs/installation.md must record the MEASURED reduction for the WHEEL, the SDIST and the PLUGIN payload SEPARATELY. A single aggregate figure would hide the channel that mattered most - the sdist was fully untrimmed at baseline, so its reduction is the largest and the most security-relevant. Use the actual per-channel counts and byte sizes reported by the AC8 composition test against the baseline captured by 160.001-T. Do NOT reuse the projected ~350 files / ~3 MB; a projection recorded as a measurement is a false record.

DOCUMENT THE SDIST DISCLOSURE CLOSURE (AC3b): docs/installation.md must state what the source distribution does and does not contain, because a consumer who downloads the sdist from PyPI or the GitHub release gets a different file set than the wheel consumer, and that difference is now intentional and specified rather than accidental.

DOCUMENT THE PLUGIN SOURCE STRATEGY (AC3c, AC3d): state which spike branch was selected by 160.002-T and where the plugin `source` resolves. If branch (b) was selected, document that `plugin-payload/` is TRACKED and COMMITTED, is regenerated by the single generation command `python -m build_support.payload generate --channel plugin`, and is fetched by consumers directly from a repository checkout; and that it is a GENERATED OUTPUT ROOT excluded from classifier inputs while remaining the published directory. Do NOT document any `dist/`-rooted plugin path: `dist/.gitignore` contains `*`, so such a path is absent from every consumer checkout and could never be fetched.

FORWARD-ONLY FRAMING: removing .backlogit/ from the payload is FORWARD-ONLY. Already-published v1.5.0 artifacts still contain those 2,110 files and remain public; invariant I5 correctly forbids retraction. The documentation written by THIS task MUST NOT imply retroactive remediation - state plainly that the change affects future releases only.

SCOPE REMOVALS. Two items previously required by this task were out-of-scope knowledge work with no compliant P-021 disposition. Both are captured as deferred stash entries and are NOT deliverables here:
- Content sensitivity assessment of what v1.5.0 already published -> deferred entry 00C2B1F9. P-021 C1 discrimination: assessing the sensitivity of ALREADY-PUBLISHED historical records is a security-analysis activity over a different artifact set (published release contents), not a change to the packaging contract this shipment defines. It cannot be verified by any acceptance criterion of this plan and would fail the same-contract-surface test.
- docs/compound/ learning capture on allowlist-over-denylist packaging and "referenced != required payload" -> deferred entry F73A04A2. P-021 C1 discrimination: authoring a compound learning is a knowledge-library contribution whose surface is docs/compound/, which this shipment explicitly EXCLUDES from the payload and does not otherwise modify. It is also a post-completion retrospective activity - it cannot be written truthfully until the shipment has actually shipped and its outcome is known.
Neither entry blocks this task, this shipment, or its completion.

RELEASE VERSION IS NOT RESOLVED HERE: the target release version for the trimmed payload is resolved at release time by Ship/Orchestrator.

DEPENDENCIES: blocked by the five baseline verification families (160.008-T, 160.012-T, 160.013-T, 160.009-T, 160.010-T), by 160.015-T (release-path gate), and by 160.017-T (transition ledger). This task is the LAST task in the shipment and must not describe the change as complete until the ledger has confirmed every declared test case transitioned exactly as its class required.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. Documentation-only. Writes are confined to docs/installation.md, README.md, and CHANGELOG.md. This task MUST NOT modify the manifest, schema, build_support/, tests/, pyproject.toml, marketplace.json, plugin-payload/, or release.yml - if the docs disagree with the built payload, the defect belongs to the task that built it. NO-PUBLISH: recording a version in CHANGELOG.md is not publication and must not trigger a release. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

ACCEPTANCE: docs accurately describe the delivered payload for all three channels; the AC7 support matrix is reproduced in docs/installation.md; the plugin source strategy and the selected spike branch are stated; CHANGELOG records MEASURED (not projected) per-channel reduction; no documentation sentence implies retroactive remediation of already-published artifacts.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size S, complexity trivial. Transcription of figures and decisions that every upstream task has already produced and recorded.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T15)