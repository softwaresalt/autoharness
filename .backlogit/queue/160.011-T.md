---
artifact_type: task
created_at: 2026-09-03T08:26:16.4281139Z
custom_fields:
    complexity: trivial
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.008-T
    - 160.012-T
    - 160.013-T
    - 160.009-T
    - 160.010-T
    - 160.015-T
id: 160.011-T
labels:
    - docs
    - changelog
parent_id: 160-F
priority: medium
status: queued
title: Document payload boundary and record measured reduction
updated_at: 2026-09-03T09:37:46.042888Z
---

Document the payload boundary and record the change.

DOCS:
- docs/installation.md - document what each channel delivers and what it deliberately does not. Make the "target workspaces receive only generated output, never autoharness engine files" boundary explicit for consumers. Reproduce the AC7 channel x environment support matrix (160.009-T) so the plugin channel's registration limits are stated, not implied.
- README.md - update the install section if payload/channel wording changes.
- CHANGELOG.md - record the packaging change and the MEASURED payload reduction (actual figures from the composition test's reported counts per AC8, not the projected ~350 files / ~3 MB).

FORWARD-ONLY FRAMING (plan-review finding P2-8, in-scope portion): removing .backlogit/ from the payload is FORWARD-ONLY. Already-published v1.5.0 artifacts still contain those 2,110 files and remain public; invariant I5 correctly forbids retraction. The documentation written by THIS task MUST NOT imply retroactive remediation - state plainly that the change affects future releases only. This is a wording constraint on the docs this task writes, and is therefore same-contract-surface work.

SCOPE REMOVALS (Orchestrator review-fix cycle 1, finding 8). Two items previously required by this task were out-of-scope knowledge work with no compliant P-021 disposition. Both are now captured as deferred entries and are NOT deliverables here:
- Content sensitivity assessment of what v1.5.0 already published -> deferred entry 00C2B1F9. P-021 C1 discrimination: assessing the sensitivity of ALREADY-PUBLISHED historical records is a security-analysis activity over a different artifact set (published release contents), not a change to the packaging contract this shipment defines. It cannot be verified by any acceptance criterion of this plan and would fail the same-contract-surface test.
- docs/compound/ learning capture on allowlist-over-denylist packaging and "referenced != required payload" -> deferred entry F73A04A2. P-021 C1 discrimination: authoring a compound learning is a knowledge-library contribution whose surface is docs/compound/, which this shipment explicitly EXCLUDES from the payload and does not otherwise modify. It is also a post-completion retrospective activity - it cannot be written truthfully until the shipment has actually shipped and its outcome is known.
Neither entry blocks this task, this shipment, or its completion.

P3-10: the target release version for the trimmed payload is resolved at release time by Ship/Orchestrator, not here.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. Documentation-only. Writes are confined to docs/installation.md, README.md, and CHANGELOG.md. This task MUST NOT modify the manifest, schema, build_support/, tests/, pyproject.toml, marketplace.json, or release.yml - if the docs disagree with the built payload, the defect belongs to the task that built it. NO-PUBLISH: recording a version in CHANGELOG.md is not publication and must not trigger a release. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

ACCEPTANCE: docs accurately describe the delivered payload for all three install methods; the AC7 support matrix is reproduced in docs/installation.md; CHANGELOG records MEASURED (not projected) reduction; no documentation sentence implies retroactive remediation of already-published artifacts.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T15)