---
artifact_type: task
created_at: 2026-09-26T07:16:43.6792958Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: v1
    size_source: agent
dependencies:
    - 187.002-T
id: 187.003-T
labels:
    - harness-surface:harness-architect
    - ship-lifecycle
    - unit-c
parent_id: 187-F
priority: high
references:
    - docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md
    - docs/reviews/2026-09-25-ship-lifecycle-release-units-plan-review.md
status: queued
title: 'Ship lifecycle C3: harness_read bounded reads and read budget'
updated_at: 2026-09-26T07:23:59.8573438Z
---



<!-- BEGIN:acceptance-criteria -->
- G25 to G29 on each host: exactly 64 B accepted; 65 B and 4096 B over a 64 B cap -> FILE_SIZE_LIMIT after reading at most cap+1 bytes; total 64+64 accepted then TOTAL_SIZE_LIMIT; three accepts then FILE_COUNT_LIMIT with zero opens (open counter spies on os.open only).
- Patching _read_chunk records every requested size (each within the bound; at most 65 bytes requested in total for the 4096 B file) and returns short reads, which the loop continues through.
- A fixture of b"\r\n\x1a" plus padding is returned byte-exact on Windows (no text mode).
- usage never decreases; explicit exhaustion codes, no silent truncation or partial success.
- RED record before implementation (IM-13, per the A1 wording): task ID, exact P-004 canonical command, native exit, and each roster test's outcome with its own marker AHLC_C3_READ_BOUNDS:<t> per the Marker Convention (pairwise distinct; a shared marker is refused under FI-9).
- Environment prechecks: git worktree list shows one worktree; git status --porcelain is empty for the task's files before the first write; the canonical command runs from the repository root. Over the estimate by more than 30 minutes -> stop and return to Stage for a split.
- IM-14: no added text claims race, TOCTOU or hardlink-alias resistance.
- IM-15: a test or proof PASS confers no claim authority; ordinary P-001/P-002/P-004/P-020 gates at claim and closure.
- Manifest harvest carry NOROW-F03 (E2 deferral, optional): IO stays a closed code with a defined mapping; a portable IO fixture needs a per-OS mechanism (POSIX permission bits, or a Windows byte-range lock) that Proof G does not cover. C3 may add it if cheap; its absence does not block C3.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan task C3 (docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md, blob b7a77c76, section 'C3: Bounded reads and read budget'; release unit S(C); review authority: manifest E5 Verdict under OP-5).

Files (budget): src/autoharness/harness_read.py; tests/test_harness_read_bounds.py.

Change: open unbuffered and binary on both hosts: os.open(path, os.O_RDONLY | getattr(os, "O_BINARY", 0) | getattr(os, "O_NONBLOCK", 0)); read only through the private _read_chunk(fd, n) (os.read), never through a buffered file object. Take the size from os.fstat on the open handle; over max_file_bytes -> FILE_SIZE_LIMIT; over the remaining total -> TOTAL_SIZE_LIMIT; both before reading. Reserve that size (never refunded). Read in a loop until EOF or until min(max_file_bytes, remaining) + 1 bytes are held, never requesting more than that bound minus the bytes held; if more bytes arrive than were reserved, charge them and re-apply both limits. Return data only when the loop reached EOF within both limits. Every failure discards the bytes.

Marker: AHLC_C3_READ_BOUNDS (prefix). Trace: IM-05, IM-06 / IM-16 (usage seam), PE-SAFETY-04, PE-SAFETY-05, IM-13, IM-14, IM-15. Estimate 100 min; size S; complexity medium.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Label harness-surface:harness-architect. Blocked by C2; blocks C4 (C4 relies on C3's O_NONBLOCK open and post-open fstat). _read_chunk is the second private patch point (IM-06).
<!-- END:implementation-notes -->