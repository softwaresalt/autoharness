---
artifact_type: task
created_at: 2026-08-31T22:19:09.6210247Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 158.002-T
parent_id: 158-F
priority: medium
status: queued
title: Author the red regression tests for both sizing gates and record them observed failing
updated_at: 2026-09-03T15:58:40.5967857Z
---

RE-SCOPED in review-fix cycle 2 (SHIP-8 plan H3 TDD sequencing). SPLIT: this task is now the RED-TEST HALF ONLY and it COMPLETES FIRST; the post-implementation shipment-assembly/green half is 158.003-T. WHY THE SPLIT (cycle-2 P1 blocker): cycle 1 left 158.001-T depending on the WHOLE of 158.002-T while 158.002-T owned BOTH the red tests AND the post-implementation assembly work - an impossible dependency model, because 158.001-T could not begin until work that must FOLLOW it had finished. The order is now explicit, acyclic and satisfiable: 158.002-T (red tests) -> 158.001-T (harvest-gate implementation) -> 158.003-T (assembly implementation + green). SCOPE: tests/ ONLY. No template, skill, agent, or Stage-contract text is edited by this task. DELIVERABLE: three regression tests, each OBSERVED AND RECORDED RED against the unmodified tree before any implementation begins - (T1) harvest a task with no complexity MUST halt; (T2) assemble a shipment whose size_composition.unsized > 0 MUST fail; (T3) a backlog-md-shaped registry WITHOUT features.sizing MUST complete with an explicitly REPORTED degradation rather than a halt.

=== BOUNDARY-CASE OWNERSHIP: ALL OF B1-B11 ARE AUTHORED HERE (BINDING; extended review-fix cycle 4, finding 2) ===

WITHDRAWN WORDING: the cycle-3 sentence "The remaining boundary cases B1, B3, B4, B5, B7-B11 are the implementation contract of 158.003-T and are authored there" is WITHDRAWN and MUST NOT be reintroduced. It handed authorship of nine of the eleven behaviour-defining boundary cases to the SAME task that implements the predicate they constrain. A case authored by its own implementer is written against code that already exists, is green the moment it is written, and can NEVER be observed red - so it produces ZERO red-first evidence and, worse, is shaped by whatever the implementation happens to do rather than by the contract. Cycle 3 correctly identified B6 as the discriminator and then left the six other equally dangerous discriminators (B7 float, B8 string, B9 null, B4 missing, B10 negative, B11 malformed histogram) unobserved - every one of which can defeat a naive predicate exactly as B6 does.

BINDING OWNERSHIP: THIS TASK AUTHORS ALL ELEVEN CASES B1-B11, as ONE table-driven case set over INJECTED size_composition rollup values, against the UNMODIFIED tree. 158.003-T AUTHORS NO BOUNDARY CASE AT ALL. The eleven cases split into exactly two observation classes, and every case belongs to exactly one:

CLASS R - RED-FIRST (EIGHT cases; each expects a FAILURE that the unmodified tree does not produce, so each is observed RED here):
  B2  unsized: 1 (int), non-empty histogram  => must FAIL, naming the unsized member IDs.
  B4  unsized field ABSENT from the rollup   => must FAIL CLOSED, named SHAPE error (an absent field is not zero).
  B6  unsized: false (bool)                  => must FAIL CLOSED, named TYPE error. Assert BOTH false AND true, so an implementation that special-cases only false is caught.
  B7  unsized: 0.0 (float)                   => must FAIL CLOSED, named TYPE error (0.0 == 0 is True).
  B8  unsized: "0" (string)                  => must FAIL CLOSED, named TYPE error; never coerced with int().
  B9  unsized: null                          => must FAIL CLOSED, named TYPE error.
  B10 unsized: -1 (negative int)             => must FAIL CLOSED, named RANGE error (not merely "not equal to zero").
  B11 histogram ABSENT, and separately histogram PRESENT BUT NOT A MAPPING => must FAIL CLOSED, named SHAPE error. Cover the absent case and at least one non-mapping case as SEPARATE deterministic cases.

CLASS C - CHARACTERIZATION (THREE cases; each expects a PASS, which the unmodified tree already produces because no gate exists yet, so NONE of them can be red and NONE may be recorded as red):
  B1  unsized: 0 (int), non-empty histogram        => PASS.
  B3  unsized: 0 (int), EMPTY histogram            => PASS (an empty shipment is not an unsized one).
  B5  unsized: 0 (int), ruleset_version: null      => PASS (binding H4).
  These three are recorded GREEN-AT-BASELINE and their acceptance obligation is that they REMAIN GREEN after 158.003-T lands. They are the over-rejection guard: they are what catches a fail-closed implementation that overreaches and starts refusing legitimate shipments. Recording a Class C case as "red" would be a FALSE red-first claim and FAILS this task.

CLASS TOTALS (must reconcile exactly): 11 cases total = 8 Class R + 3 Class C. Every one of B1-B11 appears in exactly one class; no case is unassigned and none appears twice.

RED EVIDENCE IS THE ACCEPTANCE, PER CASE. Record the observed failure output and the exact assertion each Class R case fails on, SEPARATELY FOR EACH INJECTED VALUE. A single aggregated red over the table is NOT evidence for the individual cases and FAILS acceptance - the whole point is that each malformed value defeats a different naive predicate, so each needs its own observation. Record each Class C case's green-at-baseline observation the same way. A test authored against an already-conforming implementation proves nothing (SHIP-8 plan H3).

SEQUENCING (machine-encoded, acyclic): this task BLOCKS 158.001-T (T1 and T3 turn green there) and BLOCKS 158.003-T DIRECTLY as well as transitively. The direct 158.003-T -> 158.002-T blocks edge is declared so that "all eleven cases were authored and observed BEFORE the assembly predicate was implemented" is machine-checkable rather than a prose promise. The full order remains 158.002-T -> 158.001-T -> 158.003-T with no cycle.

FINDING 4 (preserved): the halt these tests assert is a DECOMPOSITION halt, not a session halt - it rejects one malformed task and reports it while other independent items continue. FINDING 2 (preserved): SHIP-8 is the PRODUCER and portfolio unit S2 is the DETECTOR of the same invariant; this task adopts S2 constraints verbatim - enforce-on-new-only, no auto-split, no schema change - and records that S2 is the authority if they ever diverge. SEQUENCING NOTE, TASK-LEVEL vs SHIPMENT-LEVEL EDGE (corrected in cycle 2): SHIP-8's dependence on SHIP-7 having installed features.sizing is a SHIPMENT-level blocks edge (166-S blocked by 165-S), NOT a task-level dependency - no 158-* task declares a dependency on any 157-* task. Cycle 1's wording ("real blocks edge, not a comment") was true of the shipment edge but read as though a task edge existed. Both edges are real; they sit at different levels and must not be conflated.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size S, complexity low. RE-CONFIRMED under the cycle-4 scope increase: the nine added cases are nine further INJECTED VALUES in the SAME single table-driven fixture built for B2/B6 - no new fixture, no new failure domain, no new dependency, no production code. The work grows by rows in a table, not by surface, so the two-axis assessment is unchanged.

SAFETY MODE: careful (SHIP-8 plan H5). Source: 2E67938C. Plan: docs/plans/2026-08-31-ship8-stage-size-complexity-enforcement-plan.md