---
artifact_type: task
created_at: 2026-08-31T22:19:09.6210247Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 158.002-T
parent_id: 158-F
priority: medium
status: queued
title: Author the red regression tests for both sizing gates and record them observed failing
updated_at: 2026-09-03T16:59:12.8922893Z
---

RE-SCOPED in review-fix cycle 2 (SHIP-8 plan H3 TDD sequencing). SPLIT: this task is the RED-TEST HALF ONLY and it COMPLETES FIRST; the post-implementation shipment-assembly/green half is 158.003-T. WHY THE SPLIT (cycle-2 P1 blocker): cycle 1 left 158.001-T depending on the WHOLE of 158.002-T while 158.002-T owned BOTH the red tests AND the post-implementation assembly work - an impossible dependency model, because 158.001-T could not begin until work that must FOLLOW it had finished. The order is now explicit, acyclic and satisfiable: 158.002-T (red tests) -> 158.001-T (harvest-gate implementation) -> 158.003-T (assembly clause authoring + green). SCOPE: tests/ ONLY. No template, skill, agent, or Stage-contract text is edited by this task.

=== TEST SEAM (BINDING; REWRITTEN IN REVIEW-FIX CYCLE 5, FINDING 7; PLAN H6) ===

WITHDRAWN WORDING: the cycle-2/3 framing of T2 as "assemble a shipment whose size_composition.unsized > 0 MUST fail", and of B1-B11 as a table over "INJECTED size_composition rollup values", is WITHDRAWN and MUST NOT be reintroduced. IT NAMED A CALLABLE ASSEMBLY-GATE RUNTIME THAT DOES NOT EXIST. Measured at HEAD: src/autoharness/gates/topology.py's ShipmentState dataclass carries exactly shipment_id, title, live_status, archived_status, archived_record_present, manifest_item_ids and blocking_predecessor_ids - NO size_composition, NO unsized, NO histogram - and `autoharness gate pipeline-topology` never reads the rollup. There is no function, CLI entry point, or module boundary into which a rollup could be injected. A test written to that wording would have had to CONSTRUCT the very interface it claims to be testing and then assert against its own construction, which proves nothing about the product and yields a red that is an artifact of the mock rather than of a real defect.

ACTUAL SEAM: RENDERED-TEMPLATE STATIC CONTRACT TESTS over the Stage agent instruction. The product surface that 158.003-T changes is templates/agents/_stage.agent.md.tmpl and its installed mirror .github/agents/_stage.agent.md, "Step 5: Shipment Assembly". This task asserts, against the UNMODIFIED tree, that that section carries the eleven required fail-closed clauses. MECHANISM: render the template with autoharness.verify_workspace._derive_template_variables + _render_template and assert over the rendered text, following the established pattern in tests/test_circuit_breaker_policy_contract.py. This is a REAL, ALREADY-EXISTING, DETERMINISTIC harness - no new engine, no mock, no fabricated interface. Also assert TEMPLATE/MIRROR PARITY: the .tmpl and the installed .github/agents/_stage.agent.md must agree on the edited section, so a change to one without the other is caught.

NO NEW PYTHON RUNTIME GATE IS AUTHORIZED for this shipment. If a future shipment wants a callable gate that reads size_composition, that is a separate deliberated change to a different surface; this task must not anticipate it by testing an interface that has no implementer.

DELIVERABLE: three regression tests, each OBSERVED AND RECORDED RED against the unmodified tree before any implementation begins - (T1) harvest a task with no complexity MUST halt; (T2) the rendered Stage agent Step 5 section MUST carry the eleven fail-closed size_composition clauses B1-B11, and the .tmpl and mirror MUST agree; (T3) a backlog-md-shaped registry WITHOUT features.sizing MUST complete with an explicitly REPORTED degradation rather than a halt.

=== BOUNDARY-CASE OWNERSHIP: ALL OF B1-B11 ARE AUTHORED HERE (BINDING; extended review-fix cycle 4, finding 2; REINFORCED cycle 5, finding 9) ===

WITHDRAWN WORDING: the cycle-3 sentence "The remaining boundary cases B1, B3, B4, B5, B7-B11 are the implementation contract of 158.003-T and are authored there" is WITHDRAWN and MUST NOT be reintroduced. It handed authorship of nine of the eleven behaviour-defining boundary cases to the SAME task that implements the contract they constrain. A case authored by its own implementer is written against text that already exists, is green the moment it is written, and can NEVER be observed red - so it produces ZERO red-first evidence and, worse, is shaped by whatever the implementation happens to say rather than by the contract. Cycle 3 correctly identified B6 as the discriminator and then left the six other equally dangerous discriminators (B7 float, B8 string, B9 null, B4 missing, B10 negative, B11 malformed histogram) unobserved - every one of which can defeat a naive predicate exactly as B6 does. Cycle 5 finding 9 removed tests/ from 158.003-T's surface so this ownership is now enforced by the task boundary itself, not only by prose.

BINDING OWNERSHIP: THIS TASK AUTHORS ALL ELEVEN CASES B1-B11, as ONE table-driven case set over the RENDERED Stage agent Step 5 section, against the UNMODIFIED tree. 158.003-T AUTHORS NO BOUNDARY CASE AND NO TEST AT ALL. Each case asserts that the rendered instruction carries an INDIVIDUALLY LOCATABLE clause specifying the named disposition for that rollup shape; assertions must be specific enough that a section missing one clause fails exactly one case, never all eleven at once. The eleven cases split into exactly two observation classes, and every case belongs to exactly one:

CLASS R - RED-FIRST (EIGHT cases; each asserts a FAIL-CLOSED clause that the unmodified Stage template does not contain, so each is observed RED here):
  B2  unsized: 1 (int), non-empty histogram  => clause must require FAIL, naming the unsized member IDs.
  B4  unsized field ABSENT from the rollup   => clause must require FAIL CLOSED, named SHAPE error (an absent field is not zero).
  B6  unsized: false (bool)                  => clause must require FAIL CLOSED, named TYPE error. Assert BOTH false AND true are covered, so a clause set that special-cases only false is caught.
  B7  unsized: 0.0 (float)                   => clause must require FAIL CLOSED, named TYPE error (0.0 == 0 is True).
  B8  unsized: "0" (string)                  => clause must require FAIL CLOSED, named TYPE error; never coerced with int().
  B9  unsized: null                          => clause must require FAIL CLOSED, named TYPE error.
  B10 unsized: -1 (negative int)             => clause must require FAIL CLOSED, named RANGE error (not merely "not equal to zero").
  B11 histogram ABSENT, and separately histogram PRESENT BUT NOT A MAPPING => clause must require FAIL CLOSED, named SHAPE error. Cover the absent case and at least one non-mapping case as SEPARATE deterministic cases.

CLASS C - CHARACTERIZATION (THREE cases; each asserts a PASS disposition that the unmodified tree already satisfies vacuously because no gate clause exists yet, so NONE of them can be red and NONE may be recorded as red):
  B1  unsized: 0 (int), non-empty histogram        => PASS.
  B3  unsized: 0 (int), EMPTY histogram            => PASS (an empty shipment is not an unsized one).
  B5  unsized: 0 (int), ruleset_version: null      => PASS (binding H4).
  These three are recorded GREEN-AT-BASELINE and their acceptance obligation is that they REMAIN GREEN after 158.003-T lands. They are the over-rejection guard: they are what catches a clause set that overreaches and starts refusing legitimate shipments. Recording a Class C case as "red" would be a FALSE red-first claim and FAILS this task.

CLASS TOTALS (must reconcile exactly): 11 cases total = 8 Class R + 3 Class C. Every one of B1-B11 appears in exactly one class; no case is unassigned and none appears twice.

RED EVIDENCE IS THE ACCEPTANCE, PER CASE. Record the observed failure output and the exact assertion each Class R case fails on, SEPARATELY FOR EACH CASE. A single aggregated red over the table is NOT evidence for the individual cases and FAILS acceptance - the whole point is that each malformed value defeats a different naive predicate, so each needs its own observation. Record each Class C case's green-at-baseline observation the same way. A test authored against an already-conforming implementation proves nothing (SHIP-8 plan H3).

SEQUENCING (machine-encoded, acyclic): this task BLOCKS 158.001-T (T1 and T3 turn green there) and BLOCKS 158.003-T DIRECTLY as well as transitively. The direct 158.003-T -> 158.002-T blocks edge is declared so that "all eleven cases were authored and observed BEFORE the assembly clauses were written" is machine-checkable rather than a prose promise. The full order remains 158.002-T -> 158.001-T -> 158.003-T with no cycle.

FINDING 4 (preserved): the halt these tests assert is a DECOMPOSITION halt, not a session halt - it rejects one malformed task and reports it while other independent items continue. FINDING 2 (preserved): SHIP-8 is the PRODUCER and portfolio unit S2 is the DETECTOR of the same invariant; this task adopts S2 constraints verbatim - enforce-on-new-only, no auto-split, no schema change - and records that S2 is the authority if they ever diverge. SEQUENCING NOTE, TASK-LEVEL vs SHIPMENT-LEVEL EDGE (corrected in cycle 2): SHIP-8's dependence on SHIP-7 having installed features.sizing is a SHIPMENT-level blocks edge (166-S blocked by 165-S), NOT a task-level dependency - no 158-* task declares a dependency on any 157-* task. Cycle 1's wording ("real blocks edge, not a comment") was true of the shipment edge but read as though a task edge existed. Both edges are real; they sit at different levels and must not be conflated.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size S, complexity low. RE-CONFIRMED under the cycle-5 seam move: the case count is unchanged at eleven and the fixture becomes a rendered-template string produced by an EXISTING helper pair with an existing worked example in tests/test_circuit_breaker_policy_contract.py, which is strictly simpler than standing up an injected-rollup harness. No new fixture family, no new failure domain, no new dependency, no production code.

SAFETY MODE: careful (SHIP-8 plan H5). Source: 2E67938C. Plan: docs/plans/2026-08-31-ship8-stage-size-complexity-enforcement-plan.md