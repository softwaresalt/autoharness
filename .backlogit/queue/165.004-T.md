---
artifact_type: task
created_at: 2026-09-12T22:49:33.1643993Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 165.002-T
id: 165.004-T
labels:
    - test-first
    - red-phase
    - dag-readiness
    - advisory-semantics
parent_id: 165-F
priority: medium
status: queued
title: 'RED: dag-readiness advisory alignment and closure-evidence modelling tests'
updated_at: 2026-09-12T22:49:33.1643993Z
---

RED-FIRST. Author failing tests asserting that `dag-readiness` and `pipeline-topology --phase pre_claim` cannot report CONTRADICTORY readiness for the same shipment on the closure-evidence dimension.

MOTIVATING DIVERGENCE (reproduced in-workspace, deliberation Finding 3): `dag-readiness` places `163-S` in `ready_set` while `pre_claim` blocks it with PREDECESSOR_CLOSURE_INCOMPLETE. Both gates AGREE on the edge `163-S -> 162-S`; they diverge because `_dag_all_predecessors_finished` (L1873) tests only shipped-terminal status and does NOT model closure evidence at all.

REQUIRED COVERAGE:
1. A shipment whose explicit predecessor is shipped-terminal but lacks closure evidence must NOT appear ready in advisory output while `pre_claim` blocks it.
2. Advisory output is explicitly labelled advisory / non-authorizing.
3. `next_eligible` output is disambiguated so it cannot be read as claim authorization (bug report acceptance criterion 4; see also docs/compound/2026-08-09-next-eligible-detail-scoping-and-vacuous-tiebreak-tests.md).
4. `dag-readiness` remains NON-AUTHORIZING under every configuration — it never grants a claim.

Tests must fail against current `main`.

SIZE: S (~1.5h). COMPLEXITY: medium.
Plan §T4. Depends on T2 (165.002-T).