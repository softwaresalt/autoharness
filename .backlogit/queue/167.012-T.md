---
artifact_type: task
created_at: 2026-09-17T20:36:50.2451232Z
custom_fields:
    complexity: low
    size: XS
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 167.008-T
    - 167.002-T
id: 167.012-T
labels:
    - closure
    - tests
    - fail-closed-design
    - pipeline-topology
    - conditional-state
parent_id: 167-F
priority: medium
references:
    - docs/plans/2026-09-17-closure-evidence-naming-contract-plan.md
    - docs/reviews/2026-09-17-closure-evidence-naming-contract-plan-review.md
status: queued
title: Add conditional-state and case-fold regression battery proving recognition never admits an invalid verdict
updated_at: 2026-09-17T22:09:07.0616907Z
---

PLAN UNIT U12 (plan revision 5). Domain: tests. Posture: test-first.

FILES (1): tests/test_gates_topology.py (EXTENDED, after 167.008-T — same file, sequenced to avoid a concurrent edit).

FIXTURE POLICY (Contract Specification C6)
ALL fixtures are TEMPORARY. R1 names come from build_closure_path(); R2 names come from tests/_closure_legacy_names.py::legacy_closure_filename, the single test-only legacy filename helper created by 167.007-T. The canonical builder CANNOT emit an R2 name, so builder creation of R2 fixtures is explicitly NOT required and must not be asserted. docs/closure/ is NEVER read or written by any durable test.

TESTS (3)
1. BLOCKED UNDER A RECOGNIZED NAME STILL BLOCKS — an artifact matching R2 whose closure_status is BLOCKED must still fail the gate, proving read-side widening did not weaken the validity predicate: RECOGNITION ADMITS THE FILE, NEVER THE VERDICT.
2. READY_WITH_CONDITIONS WITHOUT SATISFIED CONDITIONS STILL BLOCKS — exercises the _closure_conditions_satisfied path for BOTH an absent and an unsatisfied conditions: block. This is the read-time counterpart of 167.011-T's write-side parity matrix.
3. CASE-FOLDED LEGACY ID SEGMENT — a lowercase 162-s identifier in an R2 name is recognized through R2's [Ss] kind segment and case-folded comparison, WHILE a foreign identifier in the same case form is still rejected by attribution. BOTH HALVES ARE ASSERTED: case tolerance must not become case blindness, and IT MUST NOT LEAK ONTO THE R1 PATH — a canonical-shaped name with a lowercase kind letter is still not R1.

ACCEPTANCE
- No scenario claims or requires that the canonical builder produce an R2 name.
- Scenario 3 asserts the R1 non-leakage half explicitly.
- All fixtures temporary; docs/closure/ untouched (D8).

DEPENDS ON: 167.002-T, 167.008-T.