---
artifact_type: task
created_at: 2026-07-30T02:22:40.5845466Z
custom_fields:
    size: S
id: 099.002-T
labels:
    - agent-engram
    - documentation
parent_id: 099-F
priority: medium
status: queued
title: Align .claude/instructions.md Engram Available-Tools table + Recommended Workflow with canonical tool surface
updated_at: 2026-07-30T02:22:40.5845466Z
---

Edit the `<!-- engram:start -->` block of `.claude/instructions.md` (lines 6-24 only) so its Engram tool surface matches the canonical instruction `.github/instructions/agent-engram.instructions.md`. Do NOT touch line 4 (transport, already fixed by 094-F/612f47d) or any other file. Not template-generated — edit the file directly. Single-file, doc-only edit; ~15-30 min, well within the 2-hour rule.

<!-- BEGIN:acceptance-criteria -->
1. Available Tools table contains no `create_task`, `update_task`, or `query_changes` entries.
2. Table makes the full lifecycle check explicit (`get_daemon_status`, `get_workspace_status` [required binding verification], `set_workspace`) and includes the full applicable indexing surface (`index_workspace`, `sync_workspace`, `flush_state`) plus canonical code-graph/search tools (`list_symbols`, `map_code`, `impact_analysis`, `query_graph`, `query_memory`, `unified_search`).
3. Recommended Workflow no longer instructs use of `create_task`/`update_task`/`query_changes`; it references only canonical Engram tools and explicitly includes the lifecycle check (`get_daemon_status` + `get_workspace_status`) at session start and the full indexing surface (`sync_workspace`/`index_workspace`/`flush_state`) for freshness.
4. `grep -E 'create_task|update_task|query_changes' .claude/instructions.md` returns no matches.
5. Tool set consistent with `.github/instructions/agent-engram.instructions.md`.
6. No other files modified; Markdown table/frontmatter valid; engram block markers preserved (P-008 unaffected).
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Suggested target table + workflow are in docs/plans/2026-07-29-engram-claude-tool-surface-fix-plan.md (Fix section). Implementer may refine wording but must preserve the canonical tool set and intent. Task management is backlogit's job, not Engram's — remove the Engram task-management workflow step rather than re-pointing it (a backlogit pointer is out of scope).
<!-- END:implementation-notes -->