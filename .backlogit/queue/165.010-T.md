---
artifact_type: task
created_at: 2026-09-13T00:10:42.9745241Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 165.008-T
id: 165.010-T
labels:
    - dogfood
    - installed-mirror
    - agent-instructions
    - parity
parent_id: 165-F
priority: medium
status: queued
title: Installed dogfood agent mirrors + source-vs-mirror parity check
updated_at: 2026-09-13T00:10:42.9745241Z
---

Mirror 165.008-T's template-source contract text into the INSTALLED dogfood agent copies, and record an explicit source-versus-mirror parity check.

MANDATORY MEMBER OF 173-S (review-fix cycle 1, finding 8). This task is NOT an optional follow-up and MUST NOT be deferred outside this shipment. It is split from 165.008-T only to keep each task under the 2-hour ceiling, and it DEPENDS on 165.008-T so the mirror is written from the finished source rather than in parallel with it (hardening F8 — drift prevention).

EDIT SURFACES (installed dogfood copies only):
- `.github/agents/_orchestrator.agent.md`
- `.github/agents/_ship.agent.md`

EXPLICITLY OUT OF SCOPE: `.github/instructions/workflows.instructions.md` — verified by grep to contain NO `pre_claim`, NO `PREDECESSOR_`, and NO `dag-readiness` token. The first-round plan mapped it in error. Do NOT edit it. No other installed instruction file is in scope.

DELIVERABLES:
1. The installed copies carry the SAME contract statements delivered in 165.008-T: explicit-`blocks` derivation; the four states and their provenance values; UNSEQUENCED_SHIPMENT and its two remedies; `pre_claim` as SOLE claim authority with `dag-readiness` advisory/non-authorizing; SHIP MUST NOT SELF-DECLARE a `dag-root`; `audit_sequencing` as the migration path.
2. A recorded PARITY CHECK in the task record: for each of the two file pairs, confirm the installed copy and its template source state the same contract, and record the confirmation. Drift found must be fixed, not noted.
3. No unresolved template placeholder tokens; markdownlint heading hierarchy clean (P-008).

SIZE: S (~1h). COMPLEXITY: medium (mechanical mirroring, but parity must be provably exact).
Plan §T8. Depends on 165.008-T.