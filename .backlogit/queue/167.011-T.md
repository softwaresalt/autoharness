---
artifact_type: task
created_at: 2026-09-17T20:36:30.9309316Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 167.004-T
id: 167.011-T
labels:
    - closure
    - cli
    - tests
    - validation
    - fail-closed-design
parent_id: 167-F
priority: medium
references:
    - docs/plans/2026-09-17-closure-evidence-naming-contract-plan.md
    - docs/reviews/2026-09-17-closure-evidence-naming-contract-plan-review.md
status: queued
title: Add closure-evidence semantic validation battery proving write-time and read-time verdicts are identical
updated_at: 2026-09-17T22:07:20.5402947Z
---

PLAN UNIT U11 (plan revision 5). Domain: tests. Posture: test-first.

FILES (1): tests/test_cli_gate_closure_evidence.py (EXTENDED, after 167.004-T).

PURPOSE
Proving that the write-time gate and the read-time consumer agree on validity needs more scenarios than 167.004-T's 2-hour envelope allows, and those scenarios are the evidence for the single-validity-definition decision (D6) rather than for the CLI surface itself.

NORMATIVE SOURCE: plan revision 5, unit U11, and Contract Specification C5.

PREDICATE BRANCH ENUMERATION (from live source)
_closure_artifact_complete / _closure_conditions_satisfied have exactly these branches: compaction_status present and in {done, degraded} (case-insensitive, stripped); the LEGACY `compaction` ALIAS KEY the consumer falls back to; compaction missing or out-of-enum; closure_status missing, non-string, or blank; closure_status == READY; closure_status == READY_WITH_CONDITIONS routed through the conditions block; BLOCKED and any other value; and inside the conditions block: non-list, empty list, non-mapping entry, `satisfied` not the literal boolean True (including the truthy string "true"), and missing / non-string / blank `evidence`.

TESTS (4) — scenarios 1 and 3 are parametrized
1. CONSUMER-BRANCH PARITY MATRIX — one parametrized scenario whose rows cover EVERY branch above, each row asserting that the write-time CLI verdict and the read-time _closure_artifact_complete verdict ON THE SAME ARTIFACT are IDENTICAL. Rows: accepted READY + compaction_status: done; accepted READY + degraded; accepted READY via the LEGACY `compaction:` alias key (included because the consumer supports it, and a write-time gate that rejected it would be a STRICTER second definition — the same defect mirrored); accepted READY_WITH_CONDITIONS with every condition satisfied: true and non-empty evidence; rejected READY_WITH_CONDITIONS with absent conditions:; rejected with empty conditions: list; rejected with malformed conditions: (not a list; a non-mapping entry); rejected with satisfied: "true" (truthy string, not the literal boolean); rejected with missing, non-string, or blank evidence; rejected closure_status: BLOCKED; rejected closure_status missing / blank / non-string; rejected closure_status: MAYBE (out-of-enum); rejected compaction_status missing entirely; rejected compaction_status: partial (out-of-enum). THE ASSERTION IS EQUALITY OF THE TWO VERDICTS, never that each is independently "correct". A row where the two verdicts differ is the exact failure mode under repair and fails the suite.
2. GENERIC PREDICATE-REJECTION DIAGNOSTIC (C5) — a rejected artifact (a READY_WITH_CONDITIONS record with unsatisfied conditions) exits 1 with a message that names the artifact path, names topology._closure_artifact_complete as the deciding authority, and quotes CLOSURE_PREDICATE_REQUIREMENT_DOC. The scenario ALSO ASSERTS THE NEGATIVE FORM: the message and the --json payload carry NO field-level or reason-level cause for the frontmatter refusal, and failed_check is `frontmatter_predicate`. This is the guard against a future contributor re-deriving a per-field explanation and thereby reintroducing a second validity definition.
3. CLI-OWNED DIAGNOSTIC FIDELITY (parametrized) — for each check the CLI itself owns, the diagnostic IS specific: a legacy filename names the filename and the canonical pattern (failed_check: filename); a canonically-named, predicate-acceptable artifact whose filename encodes a DIFFERENT shipment than --shipment declares fails with exit 1 naming the path, the filename-encoded shipment, and the declared shipment (failed_check: discoverability); an absent or unreadable path exits 2 naming the path (failed_check: input).
4. PREDICATE REUSE IS STRUCTURAL — the CLI module imports _closure_artifact_complete and contains NO independent membership test over closure_status or compaction_status values, asserted by SOURCE INSPECTION of src/autoharness/cli.py. This converts "no second definition" from a review observation into a machine check.

ACCEPTANCE
- Scenario 1 covers every enumerated branch; no branch is omitted.
- Scenario 2's negative assertion is present and is not merely a positive message check.
- Scenario 4 inspects source, not behaviour.
- All fixtures are temporary; docs/closure/ is never read or written (D8).

DEPENDS ON: 167.004-T.