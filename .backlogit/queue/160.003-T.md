---
artifact_type: task
created_at: 2026-09-03T08:24:17.7206958Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.005-T
    - 160.018-T
id: 160.003-T
labels:
    - packaging
    - manifest
    - allowlist
parent_id: 160-F
priority: medium
status: queued
title: Author payload-manifest.yaml allowlist with complete path classification
updated_at: 2026-09-03T20:24:26.4848337Z
---

Author `.autoharness/payload-manifest.yaml` - the single source of truth for BOTH IN-SCOPE distribution channels (wheel, plugin).

GREEN RESPONSIBILITY - ONE CANONICAL STATEMENT (assigned by the RED-FIRST harness 160.005-T). THIS TASK IS THE GREEN OWNER OF EXACTLY FOUR MANIFEST-SCOPED RED-FIRST CASES, all authored and observed red by 160.005-T:
1. test_manifest_validates_against_schema (AC1) - red because neither the manifest nor the 1.0.0 schema exists. Also asserts AC1a DESTINATION INJECTIVITY (no two rules resolve to the same dest within one channel) and rejects the glob-src/file-dest shape.
2. test_every_tracked_path_is_classified (AC11, AC3d) - red because no manifest exists, so no path is classified. Asserts EVERY tracked path IN THE CLASSIFIER INPUT DOMAIN resolves to EXACTLY ONE rule PER CHANNEL under the plan's AC11 DETERMINISTIC RESOLUTION MODEL. THE MODEL IS PLAN AC11'S AND IS NOT RESTATED HERE BEYOND ITS SHAPE (cycle 8, item 6): build the CANDIDATE SET of all rules whose pattern matches the path; ZERO candidates FAILS (unclassified); with one or more candidates, select the MOST SPECIFIC by the plan's pattern-text-only, ORDER-INDEPENDENT specificity ordering; an EQUAL-SPECIFICITY TIE FAILS CLOSED as genuinely ambiguous. THE WITHDRAWN CYCLE-3..7 CONTRACT - "maps to EXACTLY ONE RAW MATCHING RULE ... rule precedence MUST NOT be used" - IS DELETED AS FALSE ON ARRIVAL, NOT MERELY REWORDED: the AC11 table DELIBERATELY carries broad-parent/explicit-descendant pairs (`schemas/**` with `schemas/payload-manifest/**`, `.autoharness/**` with `.autoharness/payload-manifest.yaml`, `build_support/**` with `build_support/pypi_probe.py`, `.github/plugin/**` with `marketplace.json`, `dist/**` with `dist/plugin/**`, and the named `scripts/` subset with its remainder), so under a raw-count rule the check FAILED THE SHIPMENT'S OWN MANIFEST THE MOMENT IT RAN. Cycle 8 replaces "no precedence" with A DECLARED, DETERMINISTIC, ORDER-INDEPENDENT SELECTION RULE - which is what "precedence silently hides the second match" was actually reaching for. NOTHING IS HIDDEN: the candidate set is reported, the selected rule is reported, and a tie is a hard failure rather than a silent win. BROAD PARENTS AND EXPLICIT DESCENDANTS MAY THEREFORE COEXIST, and ONLY under this declared rule. GENERATED OUTPUT ROOTS ARE REMOVED FROM THE INPUT DOMAIN BEFORE MATCHING and so never enter a candidate set at all - `plugin-payload/**` and `dist/.autoharness-scratch/**` are `generated_output_roots` entries and MUST NOT ALSO CARRY A CHANNEL EXCLUSION RULE, because a rule for a path the classifier never sees is dead configuration that reads as live protection. NO NEW TEST ROW IS ADDED BY THIS CORRECTION.
3. test_start_scripts_and_workspace_config_excluded (AC11) - red because no manifest exists.
4. test_target_workspace_prefix_derivation_is_centralized (AC2d) - red because the manifest and its `target_workspace_paths` key do not exist, so P1 has no declaration to find and P2 has no classifier to route through. See AC2d PREDICATE below.
IT DOES NOT OWN GREEN for resolver, wheel-composition, plugin-composition, generator, or release-path cases.
(CYCLE 6, FINDING 16: this record previously stated THREE cases here and appended a trailing `GREEN RESPONSIBILITY UPDATED IN CYCLE 4` block stating FOUR. Two contradictory counts in one record means an executor reading top-down implements the wrong scope. The correction is folded into this single canonical statement and the trailing block is DELETED - a correction that leaves the wrong text in place is not a correction.)

BLOCKED BY 160.018-T: the 1.0.0 schema this manifest validates against is published and registered there. Authoring the manifest before the schema exists means test_manifest_validates_against_schema cannot be evaluated at all.

ALLOWLIST SEMANTICS (AC2): a path not matched by an `include` rule is excluded. A tracked path that is neither allowlisted nor matched by an explicit `exclude` rule FAILS THE BUILD. Silent default-inclusion is prohibited (denylist fails open; that is how the current state arose). EXACTLY ONE *SELECTED* RULE PER PATH PER CHANNEL, UNDER PLAN AC11'S DETERMINISTIC RESOLUTION MODEL (cycle 8, item 6): zero candidates is UNCLASSIFIED and FAILS; two or more candidates are resolved by MOST-SPECIFIC SELECTION over pattern text (order-independent), and an EQUAL-SPECIFICITY TIE FAILS CLOSED. The withdrawn sentences - "two or more matches is AMBIGUOUS and also fails" and "rule precedence must not be used to break ties" - are DELETED: they contradicted this shipment's own AC11 table, which intentionally pairs broad parents with explicit descendants. Selection by declared pattern specificity is NOT file-order precedence and hides nothing - the full candidate set and the selected rule are both reported.

EVERY RULE IS A SOURCE-TO-DESTINATION MAPPING, NOT A FILE SET (ADDED IN CYCLE 6, FINDING 3). The withdrawn manifest shape expressed only WHICH files ship. That is strictly less information than the wheel target already requires: pyproject.toml's `force-include` is a MAPPING of ten `source = destination` entries, so a set-shaped manifest cannot generate it and AC1's "single source of truth" claim would be FALSE - the destinations would still live, unmanaged and unasserted, in pyproject.toml. THE 1.0.0 SCHEMA THEREFORE DEFINES EACH RULE AS AN OBJECT:
    { "src": "<repo-relative source path or glob>", "dest": "<destination path, relative to the channel install root>" }
- IDENTITY SHORTHAND: a bare string rule is sugar for {src: X, dest: X}. Most rules are identity mappings and must not be forced to restate the path twice.
- PER-CHANNEL INSTALL ROOT: each channel declares `install_root`, and every `dest` in that channel resolves beneath it. For the WHEEL that is the installed package data root, `src/autoharness/data/`. For the PLUGIN it is the EMPTY STRING `""` - the PAYLOAD ROOT ITSELF (corrected in cycle 10, operator item 9). THE EARLIER FORMULATION - "for the plugin it is the plugin payload root declared in `generated_output_roots`" - IS WITHDRAWN AS FALSE AND MUST NOT RETURN. `plugin-payload/**` is a BRANCH-(b) MATERIALIZATION OUTPUT ROOT, i.e. the directory the generator WRITES INTO, and it is NEVER an install-root PREFIX baked into a `dest`. Treating it as an install root prefixes every plugin `dest` with the same directory the generator then writes beneath, which is precisely the RECURSIVE NESTING defect this shipment exists to close; the classifier's removal of `generated_output_roots` from its input BEFORE matching is the complementary half of that guard. A `dest` that escapes its install root (absolute, or containing `..`) is a SCHEMA ERROR; with `install_root: ""` the plugin's `dest` values are payload-root-relative and the escape check still applies unchanged.
- A GLOB `src` WITH A FILE `dest` IS A SCHEMA ERROR, not a silently-flattened many-to-one write. A glob `src` may only pair with a directory `dest` (or the identity shorthand).
- DESTINATION INJECTIVITY: within a channel, no two rules may resolve to the same `dest`. Asserted by test_manifest_validates_against_schema.
- `dest` VALUES ARE FORWARD-SLASH POSIX STRINGS on every platform.

THE WHEEL'S `packages` DECLARATION IS CLASSIFIED, NOT IGNORED (CYCLE 6, FINDING 3). pyproject.toml's wheel target carries `packages = ["src/autoharness"]` IN ADDITION to `force-include`. It is not a force-include mapping and has no `dest`, so it does not fit the rule shape above - but leaving it unmodelled leaves AC1 FALSE, because a second, unmanaged input to wheel composition would survive. The manifest therefore declares a SEPARATE, NARROWLY-SCOPED KEY:
    channel_package_declarations: { wheel: ["src/autoharness"] }
This is a DECLARED, ASSERTED EXEMPTION from the src/dest rule shape - not an omission and not an untracked exception. test_wheel_generated_table_matches_manifest asserts the generated `packages` array equals this key VERBATIM, so the whole of the wheel target - both `force-include` and `packages` - is generated from and asserted against this manifest. Do NOT invent a synthetic `dest` for it; the backend derives the install location from the package name, and fabricating one would misstate the semantics.

INCLUDE (engine runtime):
- plugin.json, .github/plugin/marketplace.json (both MANDATORY - see AC10)
- .github/agents/** (incl. .github/agents/subagents/ - reviewer personas break for consumers without it)
- .github/skills/**, .github/instructions/**, .github/prompts/**, .github/policies/**
- .github/copilot-instructions.md, .github/copilot-review-instructions.md
- templates/**, schemas/**
- scripts/ subset referenced by skills: deploy-harness.*, acquire_lock.*, release_lock.*, ci-topology-check.sh
- AGENTS.md - INCLUDE IN BOTH CHANNELS. It is the root agent contract and is runtime payload for the wheel and the plugin alike. IT IS THE ONLY TOP-LEVEL ENTRY DOCUMENT COMMON TO BOTH CHANNELS.
- README.md, LICENSE, CHANGELOG.md - WHEEL CHANNEL ONLY, AND EXPLICITLY EXCLUDED FROM THE PLUGIN PAYLOAD. This is the ONE CANONICAL INCLUDE STATEMENT for these three paths and it matches plan AC4 and plan AC11 EXACTLY. (CYCLE 7 CORRECTION: the withdrawn wording - "AGENTS.md, README.md, LICENSE, CHANGELOG.md - ALL FOUR, IN BOTH CHANNELS" - contradicted AC4 and AC11, both of which classify these three as wheel-Include/plugin-Exclude. Under the three-way exact set equality stated immediately below, a manifest asserting the withdrawn rule could not agree with AC4 or AC11, so the contradiction was not cosmetic: it made the reconciliation assertion unsatisfiable.) README.md and LICENSE are standard distribution metadata a wheel consumer expects, and LICENSE in particular must ship in the wheel for distribution compliance; CHANGELOG.md is referenced by the release process. The plugin carries its own plugin.json metadata and requires none of the three, so each is an EXPLICIT plugin-channel EXCLUSION ROW carrying that stated reason - never an unexplained absence and never a superset/subset difference left unremarked.
- docs/ ROOT GUIDES ONLY - the 21 tracked files at the `docs/` root, MEASURED not estimated. The count is a derived fact, and test_docs_root_guides_only asserts EXACT SET EQUALITY against the enumerated set, so a guide added or removed at the docs/ root fails the gate rather than silently changing the payload.

INCLUDE-SET RECONCILIATION IS A THREE-WAY EXACT SET EQUALITY (CYCLE 6, FINDING 20). Plan AC4 (completeness), plan AC11 (classification), and THIS MANIFEST must resolve to the IDENTICAL include set per channel. Any path present in one and absent from another is either (a) a defect to fix, or (b) an EXPLICIT EXCLUSION ROW carrying a stated reason - never an unexplained difference and never a superset/subset relation left unremarked. The reconciliation is asserted, not reviewed by eye: the composition suite computes AC11's classification, the manifest's resolved include set, and AC4's required set, and asserts all three are equal per channel.
- src/autoharness/** - WHEEL CHANNEL ONLY (excluded from the plugin channel; see the plugin-root resolver contract in 160.013-T)

EXCLUDE: .backlogit/**, tests/**, experiments/**, references/**, .githooks/**, .vscode/**, .claude/**, .engram/**, .graphtor/**, .autoharness/**, dist/**, and docs/ history subdirs (archive, plans, memory, decisions, closure, compound, reviews, spikes, audits, exec-plans, telemetry, design-docs, research, deferred, product-specs).

PER-CHANNEL OVERLAYS - THE CLOSED TWO-CHANNEL SET {wheel, plugin}. The manifest declares a base classification plus a per-channel overlay for `wheel` and `plugin`.

  CHANNEL SET NARROWED IN REVIEW-FIX CYCLE 5 (FINDING 1). The withdrawn text declared a closed THREE-channel set including `sdist` and asserted "THE SDIST OVERLAY IS NOT OPTIONAL". The sdist is a THIRD DISTRIBUTION CHANNEL DISCOVERED DURING PLAN REVIEW, outside the deliberated source request E9E5E6CC (the Copilot plugin payload); it shares neither the plugin's allowlist mechanism nor its docs/ over-inclusion failure mode, so absorbing it was a P-021 C1 scope expansion. The measured sdist disclosure evidence is preserved IN FULL in deferred entry 99818C6D, not discarded. DO NOT re-add an `sdist` overlay, an `sdist` enum member, or an sdist trim rule to this manifest; doing so would silently re-import the de-scoped channel through the single source of truth.

  Channel-specific divergences the overlays must express:
- pyproject.toml -> EXCLUDE from BOTH in-scope channels. It is the build definition, not engine payload; the wheel does not need it at runtime and the plugin never did. (The withdrawn INCLUDE-in-sdist rule existed only to keep a source distribution buildable and has no meaning once the sdist is out of scope.)
- src/autoharness/** -> INCLUDE in wheel, EXCLUDE from plugin. Carried by `channel_package_declarations` for the wheel (above), NOT by a force-include mapping.
- build_support/** -> EXCLUDE from BOTH channels, EXPLICITLY (cycle 6, finding 9). This includes build_support/payload.py (160.014-T's generator) and build_support/pypi_probe.py (SHIP-2's release-time helper). They are BUILD- AND RELEASE-TIME TOOLING, never engine runtime: nothing the installed package or the deployed plugin executes imports them. The exclusion is an EXPLICIT rule, not a default, so AC11 classifies these paths rather than leaving them unclassified - and so that a future runtime dependency on them fails the gate loudly instead of quietly enlarging the payload.
- tests/** -> EXCLUDE from BOTH channels, explicitly.
- .backlogit/**, .autoharness/**, .claude/**, .engram/**, .githooks/**, experiments/**, references/** -> EXCLUDE from BOTH channels, explicitly. These are workspace/development state, not shipped payload.
- The unreferenced remainder of scripts/** -> EXCLUDE from BOTH channels, explicitly; only the skill-referenced subset enumerated above is included.
The channel enum is CLOSED and must match the schema published by 160.018-T exactly. `all` means wheel and plugin - never a subset, and never a third channel.

GENERATED OUTPUT ROOTS ARE A SEPARATE KEY FROM INCLUDE/EXCLUDE (plan AC3d). The manifest declares `generated_output_roots`, naming per channel the directory the resolved payload is MATERIALIZED INTO. For the plugin channel that is `plugin-payload/`.

  `plugin-payload/**` IS DECLARED IN `generated_output_roots` AND IS NOT AN `include` RULE IN ANY CHANNEL. This is a correction of a real defect, not a re-wording. Classifying it as "INCLUDE in the plugin channel" makes the plugin generator's own output part of its next run's INPUT: run two resolves `plugin-payload/**` as source and writes it INSIDE `plugin-payload/`, producing `plugin-payload/plugin-payload/...`; run three nests again. `generate --check` then reports drift that regenerating can NEVER clear, because every regeneration deepens the tree - PERMANENT DRIFT failing the release gate on every subsequent run. Prose saying the tree is "excluded from itself" cannot fix an `include` rule that says otherwise; the manifest must express the distinction STRUCTURALLY.

  BINDING SEMANTICS THIS MANIFEST MUST ENCODE:
  1. Paths under a declared output root are REMOVED FROM THE TRACKED-PATH ENUMERATION BEFORE any classification runs. They are therefore NEITHER classified NOR unclassified, so AC2's unclassified-path build failure is NOT triggered by generated content, and the generator can never read its own output.
  2. The removal is UNCONDITIONAL AND APPLIES TO BOTH IN-SCOPE CHANNELS. That is also what keeps the tree out of the wheel - it is a derived duplicate of payload content, not source. No separate wheel exclude rule is needed or permitted for it.
  3. EXCLUSION FROM CLASSIFIER INPUT IS NOT EXCLUSION FROM PUBLICATION. `plugin-payload/` remains TRACKED and COMMITTED and remains the directory `marketplace.json`'s `source` points at (AC3c). Input role and output role are independent properties of the same path; conflating them is exactly what produced the defective classification.
  4. The plugin channel's payload is the resolved SOURCE file set written into the output root. A channel whose resolved source set is empty produces an EMPTY output root; it never falls back to shipping whatever the output root already contains.
  PROHIBITED: adding `plugin-payload/**` (or any declared output root) to an `include` rule in any channel; relying on rule ORDERING to make an exclusion "win"; or leaving the self-exclusion to the generator instead of the resolver - the latter would leave every other resolver consumer (the AC2 classification check, the size report, `--check`) still seeing the output tree as input.
  SPIKE DEPENDENCE: `plugin-payload/` exists only under spike branch (b) (160.002-T). Under branch (a) the `generated_output_roots` entry names a surface that is never materialized and the key is inert; under branch (c) the shipment halts to the operator before this classification is exercised.
  dist/** REMAINS A PLAIN EXCLUDE, NOT AN OUTPUT ROOT. dist/.gitignore contains `*`, so nothing under dist/ is tracked or present in a consumer checkout. A gitignored build-output directory is not a publishable `source` and is never a payload source.

AC11 - COMPLETE CLASSIFICATION: every tracked path in the classifier's INPUT DOMAIN must be classified or the manifest fails its own AC2. (The input domain is the tracked-path set MINUS every path under a declared output root, per the semantics above.) Explicitly classify: start.ps1, start.sh, .mcp.json, uv.lock, autoharness.code-workspace, .gitattributes, .gitignore, .gitmodules, .markdownlint.json, .markdownlintignore, .github/workflows/**, .github/copilot/**, pyproject.toml, dist/**.

PATHS CREATED BY THIS SHIPMENT (these would otherwise fail AC2 against the shipment's own output):
- build_support/** -> EXCLUDE. Build-time only, created by 160.004-T; shipping it would put the packaging rules inside the artifact they trim.
- .autoharness/payload-manifest.yaml -> covered by the .autoharness/** exclude, but classify it EXPLICITLY BY NAME so the manifest's own exclusion is intentional rather than incidental.
- docs/spikes/**, docs/audits/** -> already covered by the docs/ history-subdir excludes; the evidence artifacts written by 160.001-T, 160.002-T, 160.006-T, 160.007-T, 160.015-T and 160.017-T are therefore never payload.
- tests/fixtures/payload-baseline/** -> already covered by the tests/** exclude.

NOTE: start.ps1 / start.sh are GENERATED dogfood output from templates/scripts, not engine payload - shipping them would violate AC5.

TARGET-WORKSPACE PATH CENTRALIZATION: the manifest declares ONE `target_workspace_paths` key naming the docs/* prefixes that engine files legitimately REFERENCE but that resolve against the TARGET WORKSPACE rather than the payload. THIS TASK IS THE SINGLE AUTHORED SOURCE OF THOSE VALUES; AC5, AC9 and the R2 reference allow-list all consume THAT KEY BY NAME and MUST NOT re-list the prefixes.

  THE AC2d BINDING IS THE FOUR CANONICAL PREDICATES P1-P4 STATED BELOW - NOT A TEXTUAL OCCURRENCE COUNT OF ANY KIND, OVER ANY SURFACE. (CYCLE 7: the cycle-4/cycle-5 "exactly ONE authored occurrence" rule - scoped to build_support/**, src/**, tests/** excluding tests/fixtures/**, pyproject.toml, .github/plugin/marketplace.json, .github/workflows/** and the manifest - was WITHDRAWN AS UNSATISFIABLE in cycle 6, finding 19, and the restatement of it that survived here is DELETED. It contradicted the AC2d PREDICATE block in this same record, so an executor reading top-down implemented the withdrawn rule.) NO REPOSITORY-WIDE AND NO SURFACE-SCOPED TEXTUAL COUNT BINDS THIS TASK OR ANY OTHER TASK IN THIS SHIPMENT. WHAT BINDS: this manifest declares `target_workspace_paths` ONCE and that key is the single authored DECLARATION; every other consumer resolves it SYMBOLICALLY through that key rather than re-listing values; and the drift guarantee is enforced BY DERIVATION - P1 declaration uniqueness across build_support/**, P2 call-site routing through classify_target_workspace_path, P3 no fixture ingestion, and P4 frozen non-increase over src/**. tests/fixtures/** is OUTSIDE every predicate surface: 160.001-T's baseline fixtures legitimately RECORD prefixes as inert data, and P3 - not textual absence - is what keeps that carve-out honest, because a fixture literal something EXECUTABLE READS is a second source of truth while an inert one is not. Documentation, plans, deliberations and backlog records are likewise irrelevant to the predicate, since nothing reads them and they therefore cannot cause the drift the rule exists to prevent. The authoritative predicate text is the AC2d PREDICATE block below; plan AC2d is the canonical statement.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. This task authors ONE new declarative file. It MUST NOT modify pyproject.toml, marketplace.json, release.yml, plugin-payload/, or src/autoharness/. NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2 (writable-surface table).

AC2d PREDICATE - WHAT CASE 4 ACTUALLY ASSERTS (rewritten in cycle 6, finding 19). THE `EXACTLY ONE AUTHORED OCCURRENCE` RULE IS UNSATISFIABLE AS MEASURED and is WITHDRAWN: at the current tip there are 57 occurrences of the target-workspace prefixes across 23 files on the declared surface, 8 of them in src/** - which freeze-scope makes READ-ONLY - including TWO GENUINELY EXECUTABLE LITERALS in src/autoharness/verify_workspace.py (the `DOCS_COMPOUND`/`docs/compound` and `DOCS_CLOSURE`/`docs/closure` workspace-relative defaults). A test asserting `exactly one` over that surface is RED ON ARRIVAL AND CAN NEVER GO GREEN, because satisfying it requires editing frozen src/** and rewriting nineteen unrelated test modules. NO SOURCE EDIT IS AUTHORIZED to make the old rule true. THE CASE IS NAMED `test_target_workspace_prefix_derivation_is_centralized`, WHICH IS THE CURRENT AND ONLY NAME (CYCLE 11, ITEM 9). **THE STALE "THE CASE NAME IS RETAINED" RATIONALE IS DELETED.** It was written when the name was an inherited artefact that no longer matched what the case asserts, and it argued for keeping a mismatched name to protect 160.017-T's verbatim ledger-to-suite bijection. That argument is now MOOT AND MISLEADING: the case WAS renamed atomically in cycle 9, item 10 - across the ledger row, every referring task record, and the plan - so the bijection holds on the CURRENT name, and the current name STATES the property (centralized derivation) rather than merely labelling it. There is no historical name in the shipped suite; `test_target_workspace_prefix_derivation_predicate_holds` MUST NOT appear anywhere. What the case enforces is AC2d AS REWRITTEN - FOUR MECHANICALLY DECIDABLE AST/IMPORT/CALL-SITE PREDICATES, not a textual count and not a whole-program semantic proof:
  P1 DECLARATION UNIQUENESS (AST): parsing every module under build_support/**, ZERO string constants equal to or prefixed by a target-workspace prefix may appear; the manifest's `target_workspace_paths` key is the single authored declaration. Decidable by walking ast.Constant nodes.
  P2 CALL-SITE ROUTING (AST): every AC5/AC9/R2 target-workspace classification decision in build_support/** reaches that decision by CALLING classify_target_workspace_path. Decidable by collecting ast.Call nodes and asserting the set of deciding modules equals {the classifier module}.
  P3 NO FIXTURE INGESTION (AST): no module under build_support/** imports from, opens, or path-references tests/fixtures/**. This is what keeps the fixture carve-out honest - a fixture literal is inert DATA; a fixture literal something EXECUTABLE reads is a second source of truth.
  P4 FROZEN PRE-EXISTING ALLOW-LIST, NON-INCREASE (symbol-level): the two verify_workspace.py executable literals are recorded in a named allow-list keyed BY ENCLOSING SYMBOL, not by line number. Assert the allow-list contains EXACTLY those two AND that NO ADDITIONAL executable target-workspace literal appears anywhere in src/**. This is a NON-INCREASE predicate - true today and enforceable - rather than an `exactly one` predicate that is false today and unfixable under freeze-scope.
RESIDUAL RISK, RECORDED NOT CONCEALED: P4 accepts two pre-existing executable literals in a frozen module. If DOCS_COMPOUND/DOCS_CLOSURE were renamed in the manifest without updating those defaults, the two would diverge. Risk is LOW - they are fallback defaults on a target-workspace path, not payload classification - and is bounded by P4, which fails the moment a THIRD literal appears. Unifying them is a src/** change outside this shipment's authority and is NOT attempted.

ACCEPTANCE: AC1, AC2, AC2d, AC3, AC3d, AC3e, AC4, AC11. (AC3b is DELETED - it was the sdist acceptance criterion, withdrawn with the channel in cycle 5.) Manifest validates against the 1.0.0 schema. BOTH in-scope channel overlays present and NO third channel declared, plus `generated_output_roots`, plus exactly one `target_workspace_paths` key. The FOUR manifest-scoped harness cases go from red to green and the transitions are recorded for 160.017-T's ledger.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size M, complexity medium. RE-CONFIRMED under the cycle-5 channel narrowing: one declarative file, one fewer overlay to author, but it must still classify every tracked path across both in-scope channels and encode the output-root distinction correctly; the judgement is in the classification, not the volume, so the axis assessment is unchanged.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T4)