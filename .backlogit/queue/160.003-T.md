---
artifact_type: task
created_at: 2026-09-03T08:24:17.7206958Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.005-T
    - 160.018-T
id: 160.003-T
labels:
    - packaging
    - manifest
    - allowlist
parent_id: 160-F
priority: medium
status: queued
title: Author payload-manifest.yaml allowlist with complete path classification
updated_at: 2026-09-03T10:20:07.0416356Z
---

Author `.autoharness/payload-manifest.yaml` - the single source of truth for ALL THREE distribution channels (wheel, sdist, plugin).

GREEN RESPONSIBILITY (assigned by the RED-FIRST harness 160.005-T): this task turns the MANIFEST-SCOPED cases green - test_manifest_validates_against_schema (AC1), test_every_tracked_path_is_classified (AC11), test_start_scripts_and_workspace_config_excluded (AC11). It does NOT own green for resolver, wheel-composition, sdist-composition, plugin-composition, or release-path cases.

BLOCKED BY 160.018-T: the 1.0.0 schema this manifest validates against is published and registered there. Authoring the manifest before the schema exists means test_manifest_validates_against_schema cannot be evaluated at all.

ALLOWLIST SEMANTICS (AC2): a path not matched by an `include` rule is excluded. A tracked path that is neither allowlisted nor matched by an explicit `exclude` rule FAILS THE BUILD. Silent default-inclusion is prohibited (denylist fails open; that is how the current state arose).

INCLUDE (engine runtime):
- plugin.json, .github/plugin/marketplace.json (both MANDATORY - see AC10)
- .github/agents/** (incl. .github/agents/subagents/ - reviewer personas break for consumers without it)
- .github/skills/**, .github/instructions/**, .github/prompts/**, .github/policies/**
- .github/copilot-instructions.md, .github/copilot-review-instructions.md
- templates/**, schemas/**
- scripts/ subset referenced by skills: deploy-harness.*, acquire_lock.*, release_lock.*, ci-topology-check.sh
- AGENTS.md, README.md, LICENSE, CHANGELOG.md
- docs/ ROOT GUIDES ONLY (21 files)
- src/autoharness/** - WHEEL AND SDIST CHANNELS ONLY (excluded from the plugin channel; see the plugin-root resolver contract in 160.013-T)

EXCLUDE: .backlogit/**, tests/**, experiments/**, references/**, .githooks/**, .vscode/**, .claude/**, .engram/**, .graphtor/**, .autoharness/**, and docs/ history subdirs (archive, plans, memory, decisions, closure, compound, reviews, spikes, audits, exec-plans, telemetry, design-docs, research, deferred, product-specs).

PER-CHANNEL OVERLAYS - THREE CHANNELS, NOT TWO (extended in review-fix cycle 2, finding 3). The manifest declares a base classification plus a per-channel overlay for `wheel`, `sdist` and `plugin`. The SDIST OVERLAY IS NEW AND IS NOT OPTIONAL: `[tool.hatch.build.targets.sdist]` currently declares only `core-metadata-version`, so hatchling's default sweep packages the ENTIRE PROJECT and `uv build` publishes that sdist to PyPI and to the GitHub release alongside the wheel. A manifest with no sdist overlay cannot trim it, and the excluded set would remain fully disclosed through the source distribution no matter how well the wheel is trimmed. Channel-specific divergences the overlays must express:
- pyproject.toml -> INCLUDE in sdist (a source distribution without its build definition is not buildable), EXCLUDE from wheel and plugin.
- src/autoharness/** -> INCLUDE in wheel and sdist, EXCLUDE from plugin.
- plugin-payload/** -> INCLUDE in plugin, EXCLUDE from wheel and sdist (see below).

AC11 - COMPLETE CLASSIFICATION (plan-review finding P1-4; extended in Orchestrator review-fix cycle 1, finding 6): every tracked path must be classified or the manifest fails its own AC2. Explicitly classify: start.ps1, start.sh, .mcp.json, uv.lock, autoharness.code-workspace, .gitattributes, .gitignore, .gitmodules, .markdownlint.json, .markdownlintignore, .github/workflows/**, .github/copilot/**, pyproject.toml, dist/**.

MANDATORY ADDITIONS IN CYCLE 1 (these paths are CREATED BY THIS SHIPMENT and would otherwise fail AC2 against the shipment's own output - the same self-contradiction class as finding P1-4):
- build_support/** -> EXCLUDE. Build-time only, created by 160.004-T; shipping it would put the packaging rules inside the artifact they trim (plan-review finding P1-6).
- .autoharness/payload-manifest.yaml -> covered by the .autoharness/** exclude, but classify it EXPLICITLY BY NAME so the manifest's own exclusion is intentional rather than incidental.

CORRECTED AND ADDED IN CYCLE 2 (finding 5):
- dist/** -> EXCLUDE (all of it, including any dist/plugin/). dist/.gitignore contains `*`, so nothing under dist/ is tracked or present in a consumer checkout. The cycle-1 plan pointed the plugin `source` there, which no consumer could fetch; that strategy is withdrawn. dist/ is build output and is NEVER a payload source.
- plugin-payload/** -> GENERATED, TRACKED, INCLUDE-IN-PLUGIN-CHANNEL, EXCLUDE from wheel and sdist. Materialized only under spike branch (b) (160.002-T). Being TRACKED is precisely what makes marketplace `source` consumer-fetchable (AC3c). Because it is both tracked and derived, it inherits H1's generate-and-assert contract: `generate --channel plugin --check` detects drift between the committed tree and the manifest. If the spike selects branch (a), this key classifies an empty/absent surface and no tree is materialized; if it selects branch (c), the shipment halts to the operator before this classification is exercised.

NOTE: start.ps1 / start.sh are GENERATED dogfood output from templates/scripts, not engine payload - shipping them would violate AC5.

TARGET-WORKSPACE PATH CENTRALIZATION (cycle 1 finding 6; SCOPE CORRECTED IN CYCLE 2, FINDING 15): the manifest declares ONE `target_workspace_paths` key naming the docs/* prefixes that engine files legitimately REFERENCE but that resolve against the TARGET WORKSPACE rather than the payload. THIS TASK IS THE SINGLE AUTHORED SOURCE OF THOSE VALUES; AC5, AC9 and the R2 reference allow-list all consume THAT KEY BY NAME and MUST NOT re-list the prefixes. Cycle 1 demanded exactly one authored occurrence ANYWHERE IN THE REPOSITORY, which was unsatisfiable: the plan, the deliberation, this task record and every reviewer note necessarily name the prefixes to be intelligible, so the contract could never be met and the test could only be passed by deleting documentation. The requirement is therefore SCOPED TO EXECUTABLE AND CONFIG SURFACES ONLY - build_support/**, src/**, tests/**, pyproject.toml, marketplace.json, .github/workflows/**, and the manifest itself. Documentation, plans, deliberations and backlog records are EXCLUDED from the single-occurrence test: an illustrative mention in prose cannot cause the drift the rule exists to prevent, because nothing reads it. Within the scoped surfaces the rule is absolute - exactly ONE authored occurrence, in this manifest, and every other consumer resolves it symbolically through `target_workspace_paths`.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. This task authors ONE new declarative file. It MUST NOT modify pyproject.toml, marketplace.json, release.yml, plugin-payload/, or src/autoharness/. NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2 (writable-surface table).

ACCEPTANCE: AC1, AC2, AC3, AC3b, AC4, AC11. Manifest validates against the 1.0.0 schema. All three channel overlays present. The three manifest-scoped harness cases go from red to green and the transitions are recorded for 160.017-T's ledger.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T4)