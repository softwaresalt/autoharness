---
artifact_type: task
created_at: 2026-09-03T08:24:17.7206958Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.005-T
id: 160.003-T
labels:
    - packaging
    - manifest
    - allowlist
parent_id: 160-F
priority: medium
status: queued
title: Author payload-manifest.yaml allowlist with complete path classification
updated_at: 2026-09-03T09:20:48.6758264Z
---

Author `.autoharness/payload-manifest.yaml` - the single source of truth for both distribution channels.

GREEN RESPONSIBILITY (assigned by the RED harness 160.005-T): this task turns the MANIFEST-SCOPED cases green - test_manifest_validates_against_schema (AC1), test_every_tracked_path_is_classified (AC11), test_start_scripts_and_workspace_config_excluded (AC11). It does NOT own green for resolver, wheel-composition, plugin-composition, or release-path cases.

ALLOWLIST SEMANTICS (AC2): a path not matched by an `include` rule is excluded. A tracked path that is neither allowlisted nor matched by an explicit `exclude` rule FAILS THE BUILD. Silent default-inclusion is prohibited (denylist fails open; that is how the current state arose).

INCLUDE (engine runtime):
- plugin.json, .github/plugin/marketplace.json (both MANDATORY - see AC10)
- .github/agents/** (incl. .github/agents/subagents/ - reviewer personas break for consumers without it)
- .github/skills/**, .github/instructions/**, .github/prompts/**, .github/policies/**
- .github/copilot-instructions.md, .github/copilot-review-instructions.md
- templates/**, schemas/**
- scripts/ subset referenced by skills: deploy-harness.*, acquire_lock.*, release_lock.*, ci-topology-check.sh
- AGENTS.md, README.md, LICENSE, CHANGELOG.md
- docs/ ROOT GUIDES ONLY (21 files)
- src/autoharness/** - WHEEL CHANNEL ONLY (excluded from the plugin channel; see the plugin-root resolver contract in 160.013-T)

EXCLUDE: .backlogit/**, tests/**, experiments/**, references/**, .githooks/**, .vscode/**, .claude/**, .engram/**, .graphtor/**, .autoharness/**, and docs/ history subdirs (archive, plans, memory, decisions, closure, compound, reviews, spikes, audits, exec-plans, telemetry, design-docs, research, deferred, product-specs).

AC11 - COMPLETE CLASSIFICATION (plan-review finding P1-4; extended in Orchestrator review-fix cycle 1, finding 6): every tracked path must be classified or the manifest fails its own AC2. Explicitly classify: start.ps1, start.sh, .mcp.json, uv.lock, autoharness.code-workspace, .gitattributes, .gitignore, .gitmodules, .markdownlint.json, .markdownlintignore, .github/workflows/**, .github/copilot/**, pyproject.toml, dist/**.

MANDATORY ADDITIONS IN CYCLE 1 (these paths are CREATED BY THIS SHIPMENT and would otherwise fail AC2 against the shipment's own output - the same self-contradiction class as finding P1-4):
- build_support/** -> EXCLUDE. Build-time only, created by 160.004-T; shipping it would put the packaging rules inside the artifact they trim (plan-review finding P1-6).
- dist/plugin/** -> EXCLUDE. Build output materialized only under spike branch (b) (160.002-T); it is a generated staging directory, never payload.
- .autoharness/payload-manifest.yaml -> covered by the .autoharness/** exclude, but classify it EXPLICITLY BY NAME so the manifest's own exclusion is intentional rather than incidental.

NOTE: start.ps1 / start.sh are GENERATED dogfood output from templates/scripts, not engine payload - shipping them would violate AC5.

TARGET-WORKSPACE PATH CLASSIFICATION (cycle 1, finding 6): the manifest declares ONE `target_workspace_paths` key naming the docs/* prefixes that engine files legitimately REFERENCE but that resolve against the TARGET WORKSPACE rather than the payload (docs/compound, docs/plans, docs/memory, docs/decisions, docs/closure). AC5, AC9, and the R2 reference allow-list all consume THAT SINGLE KEY. Re-listing those prefixes anywhere else is prohibited - duplicated classification is how the "referenced != required payload" rule drifts.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. This task authors ONE new declarative file. It MUST NOT modify pyproject.toml, marketplace.json, release.yml, or src/autoharness/. NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

ACCEPTANCE: AC1, AC2, AC3, AC4, AC11. Manifest validates against the 1.0.0 schema. The three manifest-scoped harness cases go from red to green and the transition is recorded.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T4)