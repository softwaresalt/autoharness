---
artifact_type: task
created_at: 2026-05-17T23:02:35.0450566-07:00
id: 041.003-T
labels:
    - release
    - changelog
    - docs
parent_id: 041-F
priority: medium
status: active
title: Author CHANGELOG entry for v1.4.4 (P-014 merge gate + 036-S closure)
updated_at: 2026-05-17T23:12:26.1360183-07:00
---

Insert a new `## 1.4.4` section at the top of `CHANGELOG.md` (above `## 1.4.3`). The release workflow's changelog-extraction step uses awk to find the section by version; the section must be non-empty and must be separated from adjacent version headers by blank lines.

## Pre-condition
✅ `041.001-T` done — PR #93 merged closure branch to `main` (commit `e52e785`). Working on `chore/release-1.4.4` branch started in `041.002-T`.

## CHANGELOG section to insert

```markdown
## 1.4.4 - YYYY-MM-DD

### Changed

- Hardened the Copilot Review Merge Gate (P-014) across the full Ship pipeline:
  added defense-in-depth pre-merge verification (§1.9) to `ship.agent.md`,
  `pr-lifecycle/SKILL.md.tmpl`, and `workflow-policies.md.tmpl`. Every PR —
  including post-merge closure PRs — must pass the paginated GraphQL thread-
  resolution check against the current HEAD before merge is presented or
  executed (PR #90, shipment 036-S follow-up).
- Installed `github-pr-automation.instructions.md` with §1.9 pre-merge
  readiness gate and §1.10 post-merge closure PR Copilot surveillance protocol.
  Ship agents now enforce Copilot review freshness and zero-unresolved-thread
  requirements as a non-negotiable pre-merge step (PR #90).

### Fixed

- Corrected `035-S` archive frontmatter: status set to `shipped`, commit SHA
  aligned to the actual merge SHA (`38a6c77`). Reconciled post-merge closure
  artifacts from PR #89 follow-up (PR #91, shipment 036-S).
- Archived 036-S shipment artifacts and session memory; corrected
  `archived_from` queue paths in frontmatter (chore/036-S-post-merge-closure).
```

Replace `YYYY-MM-DD` with today's date at authoring time.

## Verification

Confirm the release workflow awk step would extract non-empty notes:
```powershell
$lines = Get-Content CHANGELOG.md
$inSection = $false
$notes = @()
foreach ($line in $lines) {
    if ($line -match '^## 1\.4\.4') { $inSection = $true; continue }
    if ($inSection -and $line -match '^## ') { break }
    if ($inSection) { $notes += $line }
}
$notes -join "`n"
# → must not be empty
```

## Commit
```
docs: add CHANGELOG entry for v1.4.4 (P-014 merge gate hardening, 036-S closure)
```

## Width constraint
Touch only `CHANGELOG.md`. Do NOT modify version files or any other file in this commit.