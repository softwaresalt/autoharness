---
artifact_type: task
created_at: 2026-08-21T00:44:18.4438962Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 136.001-T
    - 136.002-T
id: 136.003-T
labels:
    - tests
    - regression-guard
parent_id: 136-F
priority: medium
references:
    - docs/plans/2026-08-20-docline-lint-restoration-plan.md
status: queued
title: 'Regression guard: every docs frontmatter block must decode'
updated_at: 2026-08-21T00:44:18.4438962Z
---

Add a regression guard so a malformed frontmatter block can never again silently disable the workspace-wide docline lint.

RATIONALE
The one-line fix restores the lint today; this guard is what closes the failure class. The real severity of 395EBE60 is that a single malformed file is a latent denial-of-service on a whole-repo quality gate — the lint aborts on first decode error and emits no report at all.

REQUIREMENTS
- Assert that the YAML frontmatter of every docs/**/*.md file decodes.
- Discover files DYNAMICALLY so a newly added doc is covered with no test edit.
- On failure, name the offending file path and line, so the next occurrence is diagnosed in one step rather than by bisecting a silent whole-repo lint abort.
- Files with NO frontmatter block are skipped, not failed. Frontmatter is not universally required here; this guard is about malformed frontmatter only.

ACCEPTANCE
- Test passes after 136.001-T and 136.002-T.
- Test demonstrably FAILS if the 136.001-T quoting is reverted. Verify by temporary local revert; do not commit the revert.

Plan: docs/plans/2026-08-20-docline-lint-restoration-plan.md (Task 3)