---
artifact_type: task
created_at: 2026-08-24T07:18:32.4750665Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: 1.0.0
    size_source: agent
dependencies:
    - 147.001-T
id: 147.002-T
labels:
    - skill-template
    - shipment-reconcile
    - cascade-close
    - safety-gate
parent_id: 147-F
priority: medium
status: queued
title: Rewrite shipment-reconcile cascade-close step 3 as the two-set allowed/required gate
updated_at: 2026-08-24T07:18:32.4750665Z
---

File: `templates/skills/shipment-reconcile/SKILL.md.tmpl`

Implement the corrected contract that 147.001-T states normatively.

1. PRE-ARCHIVED PREAMBLE (~L535-L567): classification records DECLARED STATUS, not directory location. Explicitly distinguish a true `status: archived` from a merely relocated `status: done`. Remove the citation of `docs/spikes/2026-08-18-cascade-close-pre-archived-member-behavior.md` as authority for byte-identical `archived_ids`, and remove "must never be relaxed" as applied to full-set equality.

2. STEP 3 -- REWRITE AS THE TWO-SET GATE:
   - `allowed_ids` = manifest task items + qualifying feature members + the shipment record.
   - `required_ids` = allowed artifacts NOT truly `status: archived` in the Step 0(b) snapshot.
   - FAIL when `archived_ids - allowed_ids` is non-empty -> `HALT - cascade archived unexpected artifact {id}` + P-005.
   - FAIL when `required_ids - archived_ids` is non-empty -> `HALT - cascade did not archive required artifact {id}` + P-005.
   - An already-archived allowed member MAY be included or omitted; neither fails the gate.

3. STEP 6 gate decision (~L618-L621): reference BOTH set relations instead of "matches exactly".

4. STEP 5 cascade-close report: record the Step 0(b) snapshot, `allowed_ids`, `required_ids`, and both set differences (so a vacuous `required_ids` is VISIBLE rather than silent -- review F1).

BINDING AMENDMENTS (P-006 hardening)
- A1: carry an explicit SUPERSESSION NOTE at the skill's point of change (the paragraph that currently pins the never-relax rule).
- A2: state the two set relations as TWO SEPARATELY LABELLED, INDEPENDENTLY FAILING conditions with two DISTINCT halt messages. Neither may be evaluated as a precondition of the other, and they MUST NOT be merged into a single combined test. (Conflating two questions into one condition is the documented root cause of external defect B57F9E24.)
- A3: define "qualifying feature members" at the point of use by reference to the Step 0(c) classifier's own qualifying-feature determination, and state that the definition is deliberately independent of HOW the engine transitions them. Assert SET MEMBERSHIP, never a mechanism.
- A4: specify that the declared-status snapshot is the STEP 0(b) snapshot captured BEFORE the cascade invocation, carrying the same "never a freshly-read or assumed value" warning as step 4, WITH the reason stated: `status` is the very field the cascade mutates, so a post-close read would report `archived` for everything just archived, collapsing `required_ids` to empty and silently disabling the completeness check.

5. SWEEP: grep `templates/`, `docs/`, `.github/` for restatements of the false claim (`nothing more, nothing less`, `never be relaxed`, `full item set`, `archived_ids`) and correct any stale copy found (027-DL Q2).

PRESERVE UNCHANGED: step 2 (`returned_ids` empty), step 4 (parent_id preservation against the Step 0(b) snapshot), the protected-set rules and their absence of any pre-archived exemption, the no-substitution rule, and persisted final-state verification.

ACCEPTANCE
- Step 3 states both set relations as independent conditions with distinct halt strings.
- No surviving full-set-equality claim.
- All preserved checks textually intact.
- Frontmatter valid; no unresolved `{{...}}` beyond legitimate template variables.

WIDTH ISOLATION: skill template family only.