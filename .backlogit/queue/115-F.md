---
artifact_type: feature
created_at: 2026-08-09T20:36:49.1656466Z
custom_fields:
    harness_status: pending
id: 115-F
labels:
    - dag
    - cli
    - reporting
    - read-only
    - resumption
    - 33CC445C
    - phase-2
status: queued
title: Deterministic next-eligible resumption ADVISORY over the shipped dag-readiness substrate (33CC445C Phase 2)
updated_at: 2026-08-09T20:36:49.1656466Z
---



<!-- BEGIN:description -->
Phase 2 of the DAG-visibility follow-up from spike 001-SP (stash 33CC445C), layered ADDITIVELY on the shipped Phase 1 substrate (110-F / 117-S, `autoharness gate dag-readiness`). Adds a deterministic, READ-ONLY, ADVISORY-ONLY resumption cursor answering the single question `ready_set` cannot: "what is the ONE shipment to pick up right now?"

VERIFIED GAP (read-only evidence, 2026-08-09): `compute_dag_readiness` (src/autoharness/gates/topology.py:1837) returns a lexically-sorted `ready_set` with NO cursor semantics and NO representation of in-flight work — an `active` shipment is never a ready candidate. After a crash/restart/context loss, a reader of `ready_set` alone can wrongly conclude a NEW shipment should be started while an `active` one is still in flight: exactly the P-001 single-active failure mode. Repo-wide grep confirms NO `next_eligible` implementation exists in autoharness, and none in external backlogit 1.8.0 (HEAD fd8d2c9d, internal/ scanned read-only, NOT mutated). Non-duplicative and not upstream-owned.

IMPL-PLAN (source of truth): docs/plans/2026-08-09-dag-next-eligible-resumption-advisory-plan.md. Deliberation: 014-DL (Option C).

RESOLUTION ORDER (resumption-first, evaluated against the UNFILTERED enumeration): (1) DEGRADED backlog -> null/`degraded`; (2) cycle detected -> null/`cycle_detected`; (3) ambiguous live/archive provenance ANYWHERE -> null/`ambiguous_provenance` (anomaly-first, before any partitioning); (4) >1 `active` -> null/`multi_active_anomaly` (fail closed; pipeline-topology owns the BLOCK); (5) exactly 1 `active` -> THAT shipment is the cursor, reason `resume_active`; (6) 0 `active` -> deterministic head of `ready_set`, reason `ready_set_head`; (7) 0 `active` + empty ready_set -> null/`no_candidates`.

DETERMINISTIC TIE-BREAK: sort candidates by DESC transitive `downstream_dependents` count (unblock the most work), then ASC shipment id. Ids are unique so the order is TOTAL — never ambiguous, never dependent on dict/filesystem iteration order. `downstream_dependents` is already computed by the same call; no new traversal.

INTEROP CONTRACT: `next_eligible` is NOT guaranteed to be a member of `ready_set` (under `resume_active` the cursor is an `active` shipment and `ready_set` holds only live `queued`). Consumers MUST use `next_eligible_reason` as the discriminator and MUST NOT treat non-membership as an error.

ADVISORY-ONLY INVARIANT: the cursor RECOMMENDS. It never claims, activates, mutates, creates a branch/worktree, or authorizes a claim; a null cursor never authorizes a claim either. Ship's claim authority is untouched. P-001/P-016 fully preserved.

P-006 HARDENING CONCLUSION: Requires plan hardening = YES (field explicitly set, not absent). REQUIRED + DONE (H1-H6 incl. H3b) — elevated blast radius because the change edits gates/topology.py, the shared module backing the pipeline-topology gate (autoharness's P-001/P-016 enforcement surface), and because it reconciles a documented PERMANENT NON-GOAL (policy-adjacent).

OUT OF SCOPE / DEFERRED: agent-template weaving (Orchestrator/Stage/Ship consulting the cursor) and the consequent installed dogfood mirror refresh (the 111.007-T lesson) — deferred, re-triage only after the cursor ships and proves stable. Any scheduler, auto-claim, or parallel execution remains a PERMANENT NON-GOAL under P-001/P-016. No schema change, no template change, no new gate family, no exit-code change, no external/backlogit change.
<!-- END:description -->

<!-- BEGIN:dod -->
(1) A pure `compute_next_eligible` analyzer in gates/topology.py implements the 7-branch resolution order and the DESC-fan-out/ASC-id total-order tie-break, performing NO I/O; (2) anomaly-first structure verified — the unfiltered enumeration is checked for DEGRADED/cycle/ambiguous-provenance/multi-active BEFORE any active-or-ready partitioning, and every null carries a populated machine-readable reason (a reasonless or silently-empty null is a defect); (3) `next_eligible`, `next_eligible_reason`, `next_eligible_detail` are emitted UNCONDITIONALLY as ADDITIVE `--json` fields and as at most one added line in the human report; (4) BACKWARD COMPATIBILITY proven — every Phase 1 JSON field keeps its exact meaning and shape, exit codes remain 0/2, dag-readiness still has NO BLOCK verdict, and the pre-existing Phase 1 tests pass UNMODIFIED; (5) regression tests cover each of the 7 branches, INCLUDING dedicated tests that multi-active fails closed without picking a winner, that single-active-BUT-ambiguous reports `ambiguous_provenance` rather than `resume_active`, and that shuffled input ordering yields an identical cursor; (6) read-only proven — no backlogit/git mutation on ANY branch including the anomaly branches; (7) docs explicitly RECONCILE the existing permanent NON-GOAL, distinguishing prohibited automatic selection-for-execution from the delivered read-only recommendation, WITHOUT deleting or quietly softening the non-goal sentence, and state the `next_eligible`-not-in-`ready_set` interop contract. PLAN-REVIEW VERDICT: PASS after 1 fix cycle, P0/P1 clear (115.001-R).
<!-- END:dod -->

<!-- BEGIN:goals -->
Give the crash/restart/context-loss path a single deterministic, read-only answer to "what do I pick up next?" that CANNOT induce a P-001 violation — by recommending resumption of in-flight work ahead of any new start, and by failing closed with a named reason on every anomaly. Materially improve pipeline reliability and composability by reusing the shipped Phase 1 analyzer wholesale, with zero new mutation surface and zero payload breakage.
<!-- END:goals -->