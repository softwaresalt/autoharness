---
artifact_type: task
created_at: 2026-09-03T09:19:04.6154592Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.001-T
    - 160.005-T
    - 160.016-T
id: 160.013-T
labels:
    - tests
    - resolver
    - channels
    - contracts
parent_id: 160-F
priority: medium
status: queued
title: 'Channel resolver contract tests: Python _DATA_DIR and plugin-root'
updated_at: 2026-09-03T10:29:29.8607802Z
---

=== REVIEW-FIX CYCLE 2 RE-SCOPE (finding 1) - READ FIRST, IT CHANGES WHEN THIS TASK RUNS ===

WHAT CHANGED: cycle 1 sequenced this task AFTER the wheel wiring (160.006-T) and the plugin wiring (160.007-T), so its cases were authored against an ALREADY-CHANGED payload and had NO RED OWNER ANYWHERE. That is two defects in one edge. (a) A verification family that first exists after the behaviour it verifies cannot distinguish "this still works" from "this never worked" - there is no prior observation to compare against. (b) It let install, upgrade, resolver, registration and integrity behaviour change with no test having been observed in a known state beforehand, which the shipment's test-first contract forbids.

THE EDGE IS NOW REVERSED. This task is BLOCKED BY 160.001-T (baseline capture), 160.005-T (RED-FIRST harness) and 160.016-T (CHARACTERIZATION harness), and it BLOCKS 160.006-T and 160.007-T. The wiring tasks cannot begin until this family has authored its cases and recorded its baseline observations. This is enforced by machine dependency edges, not by prose.

TWO-CLASS CONTRACT (plan section Test strategy). Every case this task authors MUST DECLARE ITS CLASS in the test file itself. The classes are mutually exclusive and exhaustive; a case declaring neither or both is a failure detected by the transition ledger (160.017-T).
- RED-FIRST: the case asserts behaviour that DOES NOT YET EXIST. It must be observed FAILING on the baseline before the implementing task begins, and observed passing after. Required observation pair: (red on baseline, green after implementation).
- CHARACTERIZATION: the case asserts an EXISTING, PRESERVED behaviour or invariant that CORRECTLY PASSES ON THE BASELINE. It is observed GREEN on the untrimmed baseline and must be observed GREEN AGAIN after trimming. Required observation pair: (green on baseline, green after change). A CHARACTERIZATION case is NOT required to be red, and MUST NOT be weakened, inverted, or otherwise broken to manufacture a red observation. Manufactured red is worse than no red: it destroys the signal for cases where red is real.
- If a CHARACTERIZATION case goes RED after the wiring tasks land, that is a REGRESSION CAUSED BY THE WIRING TASK, and the wiring task - not this one - is at fault.

BASELINE OBSERVATION IS A DELIVERABLE OF THIS TASK, not a side effect. Record, per case, the class and the observed baseline result, against the baseline artifacts captured by 160.001-T (untrimmed wheel, untrimmed sdist, untrimmed plugin payload, recorded verify-workspace finding set, recorded v1.5.0 registration failure modes). An observation with no recorded baseline reference cannot be replayed and does not satisfy this task.

=== ORIGINAL TASK BODY (unchanged except where the re-scope above overrides sequencing) ===
CLASS ASSIGNMENT FOR THIS FAMILY IS SPLIT - this family carries both classes and must declare per case. CONTRACT A (AC6a, Python-channel _DATA_DIR resolution for pip and clone) is CHARACTERIZATION: it works on the baseline and must keep working. CONTRACT B (AC6b, the plugin-root resolver and the {{AUTOHARNESS_VERSION}} resolution path for a channel with no Python package) is RED-FIRST: that resolver DOES NOT EXIST YET, its case must be authored by 160.005-T, observed FAILING on the baseline, and turned green by 160.007-T. Declaring Contract B as characterization would let a genuinely new resolver land with no red phase - exactly the defect this cycle exists to remove.

CHANNEL RESOLVER CONTRACT TESTS. Split out of 160.008-T in Orchestrator review-fix cycle 1 (finding 7 sizing, finding 5 channel contradiction). This task owns the two SEPARATE resolver contracts that AC6 previously conflated into a single "all three install methods" claim.

WHY THE OLD AC6 WAS WRONG: AC6 required Python `_DATA_DIR` resolution to work for pip, clone AND plugin. The plugin channel EXCLUDES `src/` and `pyproject.toml`, so a plugin-only install has NO Python package and therefore NO `_DATA_DIR` at all. The old criterion demanded a Python resolver in a channel that by construction has no Python. The contracts are now split.

CONTRACT A - PYTHON-CHANNEL RESOLVER (AC6a). Applies to the PIP and CLONE/EDITABLE install methods ONLY, i.e. exactly the environments where the `autoharness` Python distribution is importable.
- test_data_dir_resolution_pip_install - `_DATA_DIR` (cli.py:14-21) resolves `templates/` and `schemas/` from the installed wheel's packaged data directory.
- test_data_dir_resolution_clone_editable - the clone/editable fallback to repo root resolves `templates/` and `schemas/`.
- NEGATIVE ASSERTION A1: the Python-channel contract MUST NOT require any plugin-root artifact. Assert resolution succeeds with `plugin.json` and `.github/plugin/marketplace.json` absent from the environment.
- The `_DATA_DIR` resolution ORDER is invariant I1/I2 and is OBSERVED here, never modified (freeze-scope).

CONTRACT B - PLUGIN-ROOT RESOLVER (AC6b). Applies to the PLUGIN channel ONLY: no Python CLI on PATH, no `src/`, no `pyproject.toml` in the payload.
- test_plugin_root_resolves_templates_and_schemas - `templates/` and `schemas/` resolve relative to the INSTALLED PLUGIN ROOT (the directory the marketplace payload materializes), with no Python import involved.
- test_plugin_version_resolves_from_plugin_manifest - version resolves from `plugin.json` / `.github/plugin/marketplace.json` per AC10, not from `pyproject.toml` or `src/autoharness/__init__.py`.
- NEGATIVE ASSERTION B1: assert NO import of the `autoharness` Python package occurs on this path, and that the contract holds with no Python interpreter-visible `autoharness` distribution installed.
- NEGATIVE ASSERTION B2: assert `src/`, `pyproject.toml`, and any `_DATA_DIR`-bearing package are ABSENT from the resolved plugin payload. Their presence is a payload-composition regression, not a convenience.

CONTRACT DISJOINTNESS (mandatory): a single test must assert that the two contracts are DISJOINT by channel - no environment is expected to satisfy both, and neither contract is a fallback for the other. This is the assertion that prevents AC6a and AC6b silently re-merging into the old contradictory single claim.

SCOPE BOUNDARY: install-time payload boundary assertions are 160.008-T; upgrade/parity is 160.012-T; the registration support matrix is 160.009-T. This task covers RESOLUTION ONLY.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. Verification-only; `src/autoharness/` runtime behaviour is READ-ONLY. Test fixtures create isolated environments under a gitignored temporary path and MUST NOT mutate the developer's installed interpreter, the real `~/.autoharness/`, or any tracked file. NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

ACCEPTANCE: AC6a, AC6b, and the disjointness assertion. Invariants I1, I2 observed unbroken.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T11)