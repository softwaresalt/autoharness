---
artifact_type: task
created_at: 2026-09-03T09:19:04.6154592Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.001-T
    - 160.005-T
    - 160.016-T
id: 160.013-T
labels:
    - tests
    - resolver
    - channels
    - contracts
parent_id: 160-F
priority: medium
status: queued
title: 'Channel resolver contract tests: Python _DATA_DIR and plugin-root'
updated_at: 2026-09-03T17:34:21.365036Z
---

CHANNEL RESOLVER CONTRACT CASES (T11). Authored and observed against the BASELINE build, then re-observed against the trimmed build by the transition ledger (160.017-T).

SEQUENCING (machine-encoded, not prose). This task is BLOCKED BY 160.001-T (baseline capture), 160.005-T (RED-FIRST harness) and 160.016-T (CHARACTERIZATION harness), and it BLOCKS 160.006-T and 160.007-T. A verification family that first exists after the behaviour it verifies cannot distinguish "this still works" from "this never worked".

TWO-CLASS CONTRACT (plan section Test strategy). Every case MUST DECLARE ITS CLASS IN THE TEST FILE ITSELF, PER CASE. THIS FAMILY IS MIXED - three CHARACTERIZATION cases and five RED-FIRST cases - so there is NO family-wide class label and none may be introduced.
- RED-FIRST: asserts behaviour that DOES NOT YET EXIST. Observed FAILING on the baseline, passing after implementation.
- CHARACTERIZATION: asserts an EXISTING, PRESERVED behaviour that CORRECTLY PASSES ON THE BASELINE. Observed GREEN on the untrimmed baseline and GREEN AGAIN after trimming. NOT required to be red, and MUST NOT be weakened or inverted to manufacture one.
- If a CHARACTERIZATION case goes RED after the wiring tasks land, that is a REGRESSION CAUSED BY THE WIRING TASK, and the wiring task - not this one - is at fault.

BASELINE OBSERVATION IS A DELIVERABLE, not a side effect. Record, per case, the class and the observed initial result against 160.001-T's artifacts, loading the structured (non-prose) half from tests/fixtures/payload-baseline/**.

THIS TASK OWNS THE TWO SEPARATE RESOLVER CONTRACTS THAT AC6 ORIGINALLY CONFLATED. WHY THE OLD AC6 WAS WRONG: it required Python `_DATA_DIR` resolution to work for pip, clone AND plugin. The plugin channel EXCLUDES `src/` and `pyproject.toml`, so a plugin-only install has NO Python package and therefore NO `_DATA_DIR` at all. The old criterion demanded a Python resolver in a channel that by construction has no Python.

CONTRACT A - PYTHON-CHANNEL RESOLVER (AC6a). Applies to the PIP and CLONE/EDITABLE install methods ONLY, i.e. exactly the environments where the `autoharness` Python distribution is importable. All three Contract A cases are CHARACTERIZATION; all three are GREEN on the baseline; PRESERVATION OWNER for all three is 160.006-T.

1. test_data_dir_resolution_pip_install (AC6a, R4) - CLASS: CHARACTERIZATION. OBSERVED INITIAL STATE: GREEN - `_DATA_DIR` (cli.py:14-21) already resolves from the installed wheel. ASSERTS: `templates/` and `schemas/` resolve from the installed wheel's packaged data directory.
2. test_data_dir_resolution_clone_editable (AC6a, R4) - CLASS: CHARACTERIZATION. OBSERVED INITIAL STATE: GREEN - the clone/editable fallback already resolves. ASSERTS: the fallback to repo root resolves `templates/` and `schemas/`.
3. test_python_channel_needs_no_plugin_root_artifact (AC6a, negative assertion A1) - CLASS: CHARACTERIZATION. OBSERVED INITIAL STATE: GREEN - Python-channel resolution already succeeds with plugin-root artifacts absent. ASSERTS: resolution succeeds with `plugin.json` and `.github/plugin/marketplace.json` ABSENT from the environment. The Python-channel contract MUST NOT require any plugin-root artifact.

The `_DATA_DIR` resolution ORDER is invariant I1/I2 and is OBSERVED by these three cases, never modified (freeze-scope).

CONTRACT B - PLUGIN-ROOT RESOLVER (AC6b). Applies to the PLUGIN channel ONLY: no Python CLI on PATH, no `src/`, no `pyproject.toml` in the payload. THE RESOLVER DOES NOT EXIST YET, so every Contract B case is RED-FIRST. Declaring Contract B as characterization would let a genuinely new resolver land with no red phase. CONTRACT B IS AUTHORED BY THIS TASK, not by 160.005-T: these cases are channel-resolution contracts and belong with their sibling Contract A cases, in one failure domain with one diagnosis path. GREEN OWNER for all five is 160.007-T.

4. test_plugin_root_resolves_templates_and_schemas (AC6b) - CLASS: RED-FIRST. OBSERVED INITIAL STATE: RED - no plugin-root resolver exists and no trimmed plugin root exists (missing artifact). ASSERTS: `templates/` and `schemas/` resolve relative to the INSTALLED PLUGIN ROOT (the directory the marketplace payload materializes), with no Python import involved.
5. test_plugin_version_resolves_from_plugin_manifest (AC6b, AC10) - CLASS: RED-FIRST. OBSERVED INITIAL STATE: RED by ASSERTION FAILURE - the baseline plugin payload is the WHOLE REPOSITORY (`source: "."`), so version resolves from `pyproject.toml` / `src/autoharness/__init__.py`; the manifest-only path is never exercised and the assertion that it is the resolving source fails. ASSERTS: version resolves from `plugin.json` / `.github/plugin/marketplace.json` per AC10, NOT from `pyproject.toml` or `src/autoharness/__init__.py`. THIS CASE WAS ADDED TO THE PLAN'S LEDGER IN REVIEW-FIX CYCLE 3 - it was named here but absent there, so it had no ledger row and no verifiable transition. It is complementary to, not a duplicate of, 160.010-T's test_version_resolves_on_plugin_install_without_cli: that case asserts the INSTALL-HARNESS layer behaviour, this one asserts the RESOLVER behaviour.
6. test_plugin_channel_resolution_imports_no_python_package (AC6b, negative assertion B1) - CLASS: RED-FIRST. OBSERVED INITIAL STATE: RED - the baseline plugin payload contains `src/`, so the no-import path cannot be exercised. THE CASE WAS NAMED IN REVIEW-FIX CYCLE 3: it previously existed only as an unnamed "negative assertion B1", which made it invisible to the plan's ledger and unverifiable by the transition ledger. ASSERTS: NO import of the `autoharness` Python package occurs on this path, and the contract holds with no interpreter-visible `autoharness` distribution installed.
7. test_plugin_payload_has_no_python_distribution (AC6b, negative assertion B2) - CLASS: RED-FIRST. OBSERVED INITIAL STATE: RED by ASSERTION FAILURE - the baseline plugin payload contains `src/` and `pyproject.toml`. ASSERTS: `src/`, `pyproject.toml`, and any `_DATA_DIR`-bearing package are ABSENT from the resolved plugin payload. Their presence is a payload-composition regression, not a convenience.
8. test_channel_resolver_contracts_are_disjoint (AC6a, AC6b) - CLASS: RED-FIRST. OBSERVED INITIAL STATE: RED - the AC6b resolver does not exist, so disjointness is unassertable. ASSERTS: the two contracts are DISJOINT BY CHANNEL - no environment is expected to satisfy both, and neither contract is a fallback for the other. THIS IS THE ASSERTION THAT PREVENTS AC6a AND AC6b SILENTLY RE-MERGING into the old contradictory single claim, so it is mandatory rather than a nicety.

SCOPE BOUNDARY: install-time payload boundary assertions are 160.008-T; upgrade/parity is 160.012-T; the registration support matrix is 160.009-T; install-harness-layer version resolution is 160.010-T. This task covers RESOLUTION ONLY.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. Verification-only; `src/autoharness/` runtime behaviour is READ-ONLY. Test fixtures create isolated environments under THE SINGLE DECLARED SCRATCH ROOT `dist/.autoharness-scratch/<run-id>/` (plan AC3e; CORRECTED IN CYCLE 11, ITEM 3 - the withdrawn generic "gitignored temporary path" wording named no root and admitted an OS temporary directory, and is DELETED). `<run-id>` is a per-run UUID4 hex string; CONTAINMENT IS VALIDATED CANONICALLY - the resolved scratch path MUST be a strict descendant of the resolved workspace root, and a validation failure FAILS CLOSED. The root is NEVER taken from `TMPDIR`, `TEMP`, `TMP`, or any OS temporary-directory API, and `tempfile.TemporaryDirectory()` and the pytest `tmp_path` / `tmp_path_factory` fixtures ARE PROHIBITED for it. It is gitignored by the existing `dist/.gitignore`, and it is EXCLUDED FROM CLASSIFIER AND PAYLOAD INPUT BEFORE MATCHING. IT IS NEVER AUTOMATICALLY DELETED - cleanup is a separate operation requiring destructive approval, and MUST NOT mutate the developer's installed interpreter, the real `~/.autoharness/`, or any tracked file. BASELINE-FIDELITY CHECKPOINT: the three CHARACTERIZATION cases are observed green against the BASELINE build, never against a partially-changed tree. ITS ONLY AUTHORIZED WRITE OUTSIDE ITS OWN TEST FILES IS ITS FIRST-OBSERVATION RECORD at docs/audits/2026-09-03-ship10-payload-evidence/observations/T11.json (see FIRST-OBSERVATION DESTINATION below); this is stated here, in the operative write-surface clause, because a destination named only further down the record would still read as unauthorized under freeze-scope (extended review-fix cycle 4, finding 10). **EVIDENCE WRITE SURFACE - TWO AUTHORIZED DESTINATIONS, BOTH OPERATIVE (CYCLE 12, ITEM 8).** This task is an EVIDENCE PRODUCER and is authorized to write BOTH: (1) its first-observation record at `docs/audits/2026-09-03-ship10-payload-evidence/observations/T11.json`, AND (2) its bounded raw logs under its OWN subdirectory `docs/audits/2026-09-03-ship10-payload-evidence/observations/logs/T11/**` and nowhere else. **ANY "ONLY AUTHORIZED WRITE ... IS ITS OBSERVATION RECORD" WORDING IS WITHDRAWN AS A CONTRADICTION:** the evidence contract REQUIRES a raw log for every observation (`evidence_ref`/`evidence_sha256`/`evidence_bytes` are mandatory fields and 160.017-T validates the referenced log), so a safety clause authorizing only the JSON would have made this task's own mandatory evidence an unauthorized write under freeze-scope. BOTH destinations sit inside the SAME single bounded evidence surface; NEITHER is a second store. Log bounds are FAIL-CLOSED per 160.017-T (<=256 KiB AND <=2,000 lines per log; halt and report rather than truncate). THIS AUTHORIZES DESTINATIONS ONLY: this task DOES NOT redefine, restate, or extend the canonical evidence field list, which lives once in the plan's "Evidence-verification contract for 160.017-T" table. NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

BOUNDED RAW LOG - SECOND AUTHORIZED EVIDENCE WRITE (added in review-fix cycle 5, findings 10 and 15; local field reference removed in cycle 8, item 7). Every observation this task records is backed by a RAW LOG this task also writes, into ITS OWN SUBDIRECTORY AND NOWHERE ELSE: docs/audits/2026-09-03-ship10-payload-evidence/observations/logs/T11/**. This path is authorized in the plan's H2 writable-surface table, so writing it does NOT violate freeze-scope, and 160.017-T can verify the evidence without following a reference outside the single audit directory. WHY THE LOG IS REQUIRED: cycle 4 had 160.017-T accept each producer's SELF-DECLARED outcome, which verifies nothing - a task that never ran would be indistinguishable from a clean pass. The log is the artifact that makes the claim checkable. The log also carries the source-provenance capture the plan's canonical contract requires before the test command runs, and the observation is taken on a CLEAN, FULLY COMMITTED TREE.
BOUNDS (binding, enforced by 160.017-T): PER LOG <= 256 KiB AND <= 2,000 lines - **A LOG EXCEEDING EITHER LIMIT IS REJECTED. ONE RULE, FAIL CLOSED (CYCLE 11, ITEM 6).** THE WITHDRAWN TRUNCATION PATH - truncate head-and-tail, write a truncation marker, and record `truncated: true` with pre-truncation byte and line counts - IS DELETED AND MUST NOT RETURN. It added three extension fields nothing else consumed, self-reported by the producer whose output the bound exists to constrain, and it CONTRADICTED 160.017-T, which now rejects an over-bound log outright - so a producer following the old rule would have truncated compliantly and still failed the shipment. WHAT THIS TASK DOES INSTEAD: keep the run bounded at source - scope the command, or split the observation across the per-case log allowance. IF AN OBSERVATION GENUINELY CANNOT FIT INSIDE THE BOUND, HALT AND REPORT; do not truncate and do not proceed. PER CASE at most 3 logs (first observation, second observation, one retry); PER SHIPMENT at most 200 files under the logs root, none of them binary; RETENTION until 160.017-T completes and the shipment closes.
OBSERVATION-RECORD CONTENT IS DEFINED ONCE, IN THE PLAN, AND IS NOT RESTATED HERE (cycle 6, finding 23; TIGHTENED IN CYCLE 8, ITEM 7). The SINGLE CANONICAL definition is the field table in docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section "Evidence-verification contract for 160.017-T". Seven task records each carried their own restatement and the copies had ALREADY DIVERGED, so a producer reading only its own record would have written a record 160.017-T rejects. THIS RECORD STATES ONLY WHAT IS TASK-SPECIFIC: which cases it produces records for, and which log subdirectory is its own. THIS RECORD ENUMERATES NO FIELD, NAMES NO FIELD, AND DEFINES NO FIELD TYPE OR VALIDATION RULE - NOT EVEN PARTIALLY. Cycle 7 deleted the "ADDITIONAL RECORD FIELDS" restatement but left a second local enumeration standing under a different heading, which was the same defect wearing a different label; cycle 8 deletes that one too. A PARTIAL COPY DRIFTS FROM THE CANONICAL TABLE EXACTLY AS A FULL COPY DOES. FOR EVERY FIELD, ITS TYPE, AND ITS ACCEPTANCE RULE, READ THE PLAN'S CANONICAL TABLE - there is no second source. 160.017-T verifies each record INDEPENDENTLY against its evidence and REJECTS any record that is missing a field, malformed, or inconsistent; it does not warn and does not skip.
This task WRITES its own JSON and its own logs and NOTHING ELSE outside its test files; it MUST NOT write into another producer's subdirectory, and MUST NOT write the ledger.

ACCEPTANCE: AC6a, AC6b, AC10 (resolver layer), and the disjointness assertion. Invariants I1, I2 observed unbroken. All eight cases authored with a per-case declared class; the three CHARACTERIZATION cases observed and recorded GREEN on the baseline; the five RED-FIRST cases observed and recorded RED with the failure mode stated above.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size S, complexity medium. Eight cases across exactly two fixtures - an importable-distribution environment and a no-Python plugin-root environment - and the two cases named in review-fix cycle 3 were already required work carried as unnamed negative assertions, so naming them adds a ledger row, not effort.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T11)

FIRST-OBSERVATION DESTINATION (authorized, binding; added in extended review-fix cycle 4, finding 7). This task AUTHORS cases and records their FIRST observation, and that observation is a NON-DEFERRABLE acceptance condition consumed by 160.017-T's transition ledger. Review-fix cycle 3 authorized 160.002-T's spike record, 160.001-T's baseline, the pre-change byte captures (SIX in the current contract: three owned by 160.020-T, one each by 160.006-T, 160.007-T and 160.015-T) and 160.017-T's ledger, but NOT the first observations of the authoring tasks - so under freeze-scope this task had NO LEGAL DESTINATION FOR ITS OWN MANDATORY DELIVERABLE, the same class of self-contradiction cycle 2 fixed for schema_contracts.py and cycle 3 fixed for the baseline. A red recorded only in a transcript is uncitable, and 160.017-T cannot verify a transition pair whose first half does not durably exist.
DESTINATION: docs/audits/2026-09-03-ship10-payload-evidence/observations/T11.json - a subdirectory of the SINGLE bounded evidence surface already authorized for this shipment. NO NEW STORE, no new tool, no new convention.
RECORD CONTENT IS NOT SPECIFIED HERE (CYCLE 8, ITEM 7). The observation-record field set, every field's type, and every acceptance rule are defined ONCE, in the plan's canonical Evidence-verification contract (docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md, section "Evidence-verification contract for 160.017-T"). READ THAT CONTRACT; there is no second source and no local list. This record contributes ONLY its exact case set (the eight cases enumerated above), its destination path, and its log bounds.
CONSUMPTION: 160.017-T joins these records to the canonical case table, verifies each independently against its evidence, and rejects any record that is missing, malformed or inconsistent - all exactly as the canonical contract states, and not restated here. A ledger row with no record, or a record with no ledger row, FAILS THE SHIPMENT; this is what makes verification V6 a machine check rather than a promise.
BOUND: records are APPEND-ONLY within the shipment and written ONLY by this task. No task writes another task's record, and 160.017-T READS them without modifying them. This is the ONLY write this task performs outside tests/**.
