---
artifact_type: task
created_at: 2026-09-12T22:50:30.3064Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 165.002-T
    - 165.003-T
id: 165.006-T
labels:
    - cli
    - topology-gate
    - observability
parent_id: 165-F
priority: medium
status: queued
title: 'CLI: surface predecessor provenance in pipeline-topology JSON and human output'
updated_at: 2026-09-13T00:49:04.4425751Z
---

Surface deterministic predecessor provenance and the migration surfaces in `pipeline-topology` CLI output so operators and agents can audit WHY a shipment is claimable or blocked.

DELIVERABLES:
1. `--json` output exposes `predecessor_source` (one of `explicit` / `declared_root` / `genesis` / `unsequenced`) and the selected predecessor IDs on the `shipment_readiness` check, on BLOCKED and PASSED paths alike.
2. Human-readable output states the provenance alongside the outcome.
3. UNSEQUENCED_SHIPMENT rendering NAMES BOTH REMEDIES explicitly (record the real `blocks` edge, or declare the shipment a root). An operator reading only CLI output must be able to resolve the block without reading the source — a block with no visible exit is the deadlock defect this design corrects. Where the engine reports WHICH genesis disqualifier applied (G2 shipped history live or archived / G3 a second extant nonterminal shipment / G4 an abandoned record), render that too, so an operator in a workspace that has never shipped understands why the shipment is nevertheless not `genesis`.
4. `--phase audit_sequencing` output (165.003-T) renders readably in both JSON and human modes, INCLUDING the migration ledger reference that run emits.
5. Assertions added in `tests/test_gate_pipeline_topology_cli.py` covering each provenance value, the remedy text, the disqualifier text, and the audit rendering.

DEPENDENCY CORRECTION (review-fix cycle 2, finding 4): this task depends on 165.003-T AS WELL AS 165.002-T. Deliverable 4 renders the `audit_sequencing` output that 165.003-T produces — it CONSUMES that task's output surface. The cycle-1 edge set omitted it, which would have permitted this task to start (and its audit-rendering assertions to be authored) against a phase that does not yet exist. 165.002-T supplies `predecessor_source` for deliverables 1-3; 165.003-T supplies the audit payload and ledger for deliverable 4.

HARDENING H4: output changes must be ADDITIVE so existing agent consumers do not break. Do NOT rename or remove existing fields.

SIZE: S (~1.5h). COMPLEXITY: low (output surfacing over already-computed values).
Plan §T6. Depends on 165.002-T and 165.003-T. Blocks 165.008-T, 165.009-T.