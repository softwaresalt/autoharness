---
artifact_type: task
created_at: 2026-09-18T14:50:17.9962206Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 172.001-T
id: 172.008-T
labels:
    - test
    - tdd
    - red-phase
    - validation
    - checkpoint
parent_id: 172-F
priority: medium
status: queued
title: 'Resume-hint T5a (RED): five-token contract and the total classification matrix observed failing'
updated_at: 2026-09-19T03:14:52.8150238Z
---

TDD RED PHASE. Author the FIVE-TOKEN contract tests and the TOTAL HISTORICAL-CLASSIFICATION MATRIX, and OBSERVE THEM FAILING before either surface exists.

FIVE TOKENS, NOT THREE: CHECKPOINT_RESUME_HINT_MISSING and CHECKPOINT_RESUME_HINT_EMPTY on the harness origin; CHECKPOINT_LEGACY_HINTLESS_RESOLVED (reported) and CHECKPOINT_ACTIVE_HINTLESS (blocking) on the historical origin; and CHECKPOINT_RECORD_MALFORMED (blocking) for a structurally unclassifiable record on either origin.

SURFACE 1 - THE PREDICATE validate_checkpoint_payload(payload, *, origin) -> ValidationOutcome. Assert the origin partition: harness can emit the two harness tokens and NEITHER historical token; historical can emit the two historical tokens and NEITHER harness token; malformed is reachable from both. Assert the predicate is TOTAL - every input returns a ValidationOutcome, never None and never an unclassified result - and that the outcome is DATA, not an exception, so the scan can report while the adapter halts.

THE TOTAL CLASSIFICATION MATRIX (every cell asserted): EVERY ACCEPTED STATUS x HINT USABILITY. Statuses: queued, active, shipped, abandoned, resolved. ABANDONED IS ASSERTED EXPLICITLY as terminal, so a deliberately stopped record never halts startup. Hint usability: present-and-specific, missing, empty, whitespace-only. Plus EVERY MALFORMED CLASS, each its own case: unparseable document; duplicate keys; non-object root; missing status; wrong-typed status; unrecognized status value; missing schema_version; FUTURE schema_version; and each required field present with the wrong type. A future schema_version BLOCKS rather than degrades.

SURFACE 2 - THE EXECUTABLE ADAPTER COMMAND autoharness checkpoint create. Assert it by INVOKING IT AS A COMMAND, not by importing a function: that is the only assertion form that distinguishes "a producer called the gate" from "a gate exists somewhere in the package". Assert that on any blocking outcome it EXITS NON-ZERO AND WRITES NOTHING - validation is strictly PRE-WRITE, because a written checkpoint cannot be repaired through the official create operation - and that on a clean outcome it performs the official backlogit create call and ADDS NO CAPABILITY beyond it. The adapter's argument-hardening surface is asserted separately in 172.011-T.

IMPORT SAFETY (BINDING): this module imports cleanly under unittest.defaultTestLoader with zero loader.errors and zero _FailedTest placeholders. The not-yet-existing autoharness.checkpoint boundaries are loaded INSIDE the test body through a _load_boundary() helper, never at module scope. Every declared test individually executes and fails with its own declared marker in its own detail_text; a module that fails at import is a MISSING OBSERVATION, not a valid red.

Every case is RED because neither the predicate nor the adapter exists. Tests invoke the real boundaries and MUST NOT re-implement them. Blocked by 172.001-T only. IT DOES NOT DEPEND ON 172.005-T; that asymmetry is what makes this a genuine red phase. Blocks 172.005-T. Scope: tests/. Stash 71200CBB. Plan docs/plans/2026-09-17-checkpoint-resume-hint-contract-plan.md (revision 7). Verdict manifest docs/reviews/2026-09-17-checkpoint-resume-hint-contract-plan-review.md (latest_attempt 07, verdict REMEDIATED-PENDING-REVIEW at plan revision 7, awaiting independent attempt 08).