---
artifact_type: task
created_at: 2026-07-27T03:00:38.4487882Z
id: 090.001-T
parent_id: 090-F
priority: high
status: queued
title: Genericize 092-S sizing-readiness gate + explicit close timestamp
updated_at: 2026-07-27T03:00:38.4487882Z
---

## Description

<!-- BEGIN:description -->
Genericize the 092-S ship-agent sizing-readiness execution-ready gate and add explicit close-timestamp guidance (source: stash 638AA991, A465162F).

Files (5): templates/agents/.ship.agent.md.tmpl, .github/agents/.ship.agent.md, tests/test_telemetry_ship_lifecycle.py, tests/test_telemetry_record_cli.py, .autoharness/harness-manifest.yaml

Current state: both agent docs hard-code 079.013-T/079.015-T as the literal execution-ready gate condition; ratified test test_092s_execution_ready_guardrails_are_documented_in_ship_agents (test_telemetry_ship_lifecycle.py:156-165) asserts those literals are present. Neither doc instructs Ship to pass an explicit close timestamp in the telemetry record payload (.ship.agent.md:242); cli.py:887-888 falls back to context captured_at (begin time) when omitted.

TDD (red->green): first update the ratified test to replace the two literal-ID assertions with an assertion on a generic dependency-derivation phrase (keep task-only manifests / parent_id / execution-ready assertions), and add an assertion proving both docs instruct an explicit close timestamp. Confirm the test fails against current doc content (red). Then edit both docs: (1) replace the literal 079.013-T/079.015-T sentence with a generic rule deriving execution-readiness from the shipment's own declared task dependencies, never embedded ID literals; (2) add explicit-close-timestamp guidance at the telemetry record step that requires Ship to capture the close timestamp ONCE and reuse that exact value on every retry (never regenerate `now` per attempt), so a retried record keeps a stable payload_digest and replays as idempotent_replay rather than conflict_rejected; the close-invariant captured_at fallback (cli.py:879-888) remains the safe default when no explicit close timestamp is supplied. Also add test_record_replays_idempotently_with_same_explicit_close_timestamp to test_telemetry_record_cli.py (two context-ref record calls sharing one explicit close timestamp -> created then idempotent_replay) to lock the capture-once/reuse contract. Because editing .github/agents/.ship.agent.md changes the dogfood mirror, also refresh its sha256 entry (artifact path .github/agents/.ship.agent.md) in .autoharness/harness-manifest.yaml, or test_manifest_tracks_dogfood_ship_agent_checksum (test_telemetry_ship_lifecycle.py:45-52) fails. Confirm tests green.
<!-- END:description -->

## Acceptance Criteria

<!-- BEGIN:acceptance-criteria -->
- Ratified test no longer asserts hard-coded 079.013-T/079.015-T literals; asserts a generic dependency-derivation phrase instead.
- Ratified test asserts explicit close-timestamp guidance present in both docs.
- task-only manifests / parent_id / execution-ready assertions remain unchanged and passing.
- Both templates/agents/.ship.agent.md.tmpl and .github/agents/.ship.agent.md updated identically in substance.
- Explicit close-timestamp guidance specifies capture-once/reuse across retries (never regenerate `now` per attempt) so a retried record keeps a stable payload_digest and replays as idempotent_replay; a regression test records two context-ref telemetry records with the same explicit close timestamp and asserts created -> idempotent_replay.
- .autoharness/harness-manifest.yaml checksum for .github/agents/.ship.agent.md is refreshed after the mirror edit (test_manifest_tracks_dogfood_ship_agent_checksum stays green).
<!-- END:acceptance-criteria -->

## Implementation Notes

<!-- BEGIN:implementation-notes -->
Verification: python -m pytest tests/test_telemetry_ship_lifecycle.py tests/test_telemetry_record_cli.py -v

Note: this is an elevated-blast-radius task (ratified test contract change) — plan-harden sequencing applies: update the test contract FIRST, confirm red, then edit docs, confirm green. Do not weaken _merge_telemetry_context_payload's protected captured_at fallback (test_record_context_idempotency_and_conflict_outcomes, test_telemetry_record_cli.py:199) — work around it via explicit timestamp guidance, not by modifying it.
<!-- END:implementation-notes -->
