---
artifact_type: task
created_at: 2026-08-19T02:49:21.7061Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 1.0.0
    size_source: agent
dependencies:
    - 134.002-T
    - 134.003-T
    - 134.004-T
    - 134.005-T
    - 134.006-T
    - 134.007-T
    - 134.008-T
    - 134.009-T
    - 134.010-T
id: 134.011-T
labels:
    - tests
    - contract
    - verification
parent_id: 134-F
priority: medium
references:
    - tests/test_scope_containment_policy_contract.py
    - tests/test_circuit_breaker_policy_contract.py
    - .autoharness/harness-manifest.yaml
status: queued
title: 'Contract test: P-021 clause-to-carrier coverage matrix + byte identity + checksum'
updated_at: 2026-08-19T02:49:21.7061Z
---

Add a contract test that proves clause coverage across carriers, not merely byte identity, for the P-021 surface set.

<!-- BEGIN:acceptance-criteria -->
- [ ] New file tests/test_scope_containment_policy_contract.py exists and runs under the canonical unittest gate.
- [ ] Byte-identity test: for every dogfooded pair touched by this feature (_ship, _stage, _orchestrator agents; circuit-breaker, role-enforcement, github-pr-automation, copilot-code-review instructions; feature-flow-dark prompt), the LF-normalized rendered template equals the dogfood bytes.
- [ ] Checksum test: each of those artifacts' `checksum:` in .autoharness/harness-manifest.yaml matches the LF-normalized generated bytes.
- [ ] Clause-coverage tests: one distinct test method per clause C1-C7 asserting its designated marker text is present on EVERY carrier in the matrix above (hardening H11), asserting the marker appropriate to each carrier's declared ROLE (`[A]`/`[N]`/`[R]`/`[P]`/`[G]`) rather than one identical string across the whole row. A `[R]` carrier's marker MUST be its policy-ID/clause-label CITATION plus the recognition or enforcement behaviour it encodes -- never clause prose, since a reference-only carrier is contracted not to carry any. The matrix is EXHAUSTIVE: a carrier that authors a clause but is absent from the matrix is a defect in this task, because an under-listed carrier lets that clause regress silently while this test stays green.
- [ ] Carrier-completeness guard, derived from EVERY row C1 through C7 rather than from C2/C3 alone: the test declares an explicit `AUTHORING_TASKS` constant mapping each content task (134.001-T through 134.009-T) to the (surface, clause, role) triples its acceptance criteria author, and asserts that the matrix equals the INVERSION of that constant for all seven clauses. 134.010-T is deliberately EXCLUDED from the constant and carries no clause: it only widens the `HARNESS_ENFORCED_SUMMARY` range to `P-001 through P-021`, which REFERENCES the policy's existence rather than carrying any clause text -- recorded here so a future reader can tell a considered exclusion from a forgotten task. Inverting a declared authoring set - rather than reading carriers ad hoc - is what makes omission detectable, and it is the method hardening H11 now mandates. The guard MUST fail if any of these known-omitted entries regress: 134.005-T on C1/C2/C3, 134.006-T and 134.007-T on C4, and 134.004-T / 134.006-T / 134.007-T / 134.008-T on C5. A future carrier added to the feature cannot be omitted from the matrix without failing this guard.
- [ ] Negative-guard test: the Ship Role Boundary contains NO blanket stash prohibition - it must not forbid 'stash operations' unqualified - while still forbidding, BY NAME and matching the verb list its owner 134.002-T declares: triage, prioritize/re-prioritize, re-classify, edit, harvest, deliberate on, and discretionary removal/ARCHIVAL of stash entries (hardening H1). The archival verb is not optional in this list: a guard that checks removal but not archival passes against a Role Boundary that lets Ship discretionarily archive a Stage-owned entry, which is the same authority transfer the removal verb exists to block.
- [ ] Preservation test: Ship post-merge Step 7 source-artifact-cleanup language (backlogit_stash_remove on custom_fields.source_stash_id) is still present and unweakened (hardening H2).
- [ ] Literal-clause test (behaviour B2, owned by this file per the CROSS-FILE BEHAVIOUR ALLOCATION, asserted across the FULL B2 subset {001[A], 005[N]}): asserts BOTH carriers state the C1 test text -- the authoritative wording in workflow-policies.md.tmpl and the designated operational restatement in circuit-breaker.instructions.md.tmpl. On each carrier the assertion covers all FIVE not-sufficient phrases ('same file', 'same function', 'same PR', 'same subsystem', 'related'; a test checking only some of them would let the omitted ones regress silently, and the count MUST be resolved from the authoritative C1 in 134.001-T rather than from the operational restatement, since the restatement is the surface that can drift) AND all THREE worked discrimination cases named in 134.005-T. Hardening H5 forbids any other surface from restating this text, so a companion NEGATIVE guard asserts no third carrier paraphrases it.
- [ ] Precedence test (behaviour B13, owned by this file, asserted across the FULL B13 subset {001[A], 008}): asserts BOTH carriers state the C6 Stage intake obligation -- the authoritative registry wording that a DEFERRED SCOPE EXPANSION entry MUST route to the deliberate skill before any planning regardless of shape, size, priority or apparent triviality, AND the `_stage.agent.md.tmpl` procedural form that evaluates the DEFERRED SCOPE EXPANSION marker BEFORE shape classification (hardening H8). Asserting only the Stage agent would leave the authoritative clause itself unguarded, which is the carrier-subset under-coverage this guard exists to prevent.
- [ ] Policy-registry test (behaviour B15, owned by this file, subset {001[A]} ONLY): asserts workflow-policies.md.tmpl contains the P-021 section, its Relationship to P-010/P-017/P-018 subsections, and Amendment Log row 1.20.0, AND -- the behaviour B15 assertion proper -- that the C7 Violation Action states BOTH of its mandated consequences: that implementing an out-of-scope expansion inside a fix cycle, or closing an out-of-scope finding with no captured deferred entry, records a P-021 violation via P-005 telemetry (`violation_policy: P-021`) AND halts. A structural section-presence check alone does not discharge B15: it would pass against a registry whose violation action had been weakened to telemetry-without-halt, which is precisely the regression B15 must catch.
- [ ] Full canonical gate green: $env:PYTHONPATH='src'; python -m unittest discover -s tests. Root `python -m pytest` is NOT the canonical gate here (docs/compound/097-S-canonical-unittest-gate.md).
- [ ] TEST-DIRECTION NOTE (mirrors the identical rule in 134.012-T and 134.013-T, which applies to those files' guards; it is restated here because this file now owns behaviours B2/B13/B15 and carries its own negative guards): a negative guard asserting an absent phrasing is easy to write in a way that passes vacuously. Each negative guard in THIS file -- including the B2 companion guard that no third carrier paraphrases the C1 test text -- MUST be validated by temporarily reintroducing the defective wording and confirming the test fails. A guard that cannot be shown to fail against the defect it names does not satisfy its criterion. This is the one duplication between the two contract-test files that is deliberate: it is a testing-practice rule each file must independently honour, not a carrier mapping, so restating it creates no drift surface of the kind the single-source matrix exists to prevent.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Create tests/test_scope_containment_policy_contract.py, modeled on tests/test_circuit_breaker_policy_contract.py (which supplies the _render_template / _derive_template_variables helpers, the LF-normalization approach, and the manifest-checksum assertion pattern).

Hardening H11: a test that only checks byte identity and checksums would PASS even if a carrier silently omitted its clause, because the dogfood would faithfully mirror the omission. The test must therefore assert a clause-to-carrier COVERAGE MATRIX with one distinct test method per clause, so a single missing carrier fails a distinct, diagnosable test rather than one opaque aggregate assertion.

AUTHORITATIVE CLAUSE-TO-CARRIER MATRIX. This block is the SINGLE SOURCE OF TRUTH for the matrix across the whole feature. Hardening H11 and tasks 134.012-T and 134.013-T REFERENCE it by name and MUST NOT restate it; a restated copy is what allowed the matrix to drift out of agreement with the tasks that actually author the clauses.

Registry rule: `workflow-policies.md.tmpl` (authored by 134.001-T) is the AUTHORITATIVE carrier of ALL clauses C1-C7 and is therefore a carrier in EVERY row below. The per-clause lists enumerate the additional restating/carrying surfaces.

CARRIER ROLES. A clause is not carried the same way on every surface, and asserting one identical marker across a whole row would demand text that some carriers are contractually forbidden to contain (hardening H5 requires most surfaces to REFERENCE the registry rather than paraphrase it) or that their subject matter cannot support (a CI surface has no review thread to reply on). Every carrier below therefore declares a ROLE, and the clause-coverage test asserts the marker appropriate to that role:

- `[A]` AUTHORITATIVE - authors the full normative clause text. Only `workflow-policies.md.tmpl`, for all seven clauses.
- `[N]` NORMATIVE RESTATEMENT - restates clause text in operational form. Permitted ONLY where a task explicitly designates it (circuit-breaker for C1; the Ship Role Boundary for C5).
- `[R]` REFERENCE-ONLY - CITES the clause by policy ID and clause label and encodes the behaviour that recognizing the clause requires (recognition, classification, fail-closed enforcement), WITHOUT reproducing the clause text in any form. Introduced in the owner-contract reconciliation review because `[N]` had wrongly claimed role-enforcement for C5: hardening H5 contracts 134.003-T to reference C5 rather than restate it, so a test resolving that carrier's marker through `[N]` demands prose a CORRECTLY authored template must not contain. A reference-only carrier is asserted on its CITATION plus its ENFORCEMENT behaviour, never on clause prose, and a marker assertion that would be satisfied only by restated policy text is a defect against this role.
- `[P]` PROCEDURAL - implements the clause as a required step, gate, or Stop-Conditions annotation in a workflow, citing P-021 by ID and clause label per H5. A procedural carrier is asserted on the STEP it must contain, never on the registry's clause prose.
- `[G]` GUARD-ONLY - carries only the C3 symmetric sub-guard (H12) and nothing else of that clause.

- C1 -> workflow-policies.md.tmpl [A] (001); circuit-breaker.instructions.md.tmpl [N] (005, designated operational restatement; carries the full C1 test text and all three worked discrimination cases); `_ship.agent.md.tmpl` [P] (004, BOTH the review-fix loop and the build/CI-fix loop gate on C1 classification before any fix); github-pr-automation.instructions.md.tmpl [P] (006, comment-handling loop classification gate); pr-lifecycle/SKILL.md.tmpl [P] (007, review-fix-push iteration gate); fix-ci/SKILL.md.tmpl [P] (007, CI/build remediation loop gate)
- C2 -> workflow-policies.md.tmpl [A] (001); `_ship.agent.md.tmpl` [P] (004); github-pr-automation.instructions.md.tmpl [P] (006); pr-lifecycle/SKILL.md.tmpl [P] (007); fix-ci/SKILL.md.tmpl [P] (007); circuit-breaker.instructions.md.tmpl [P] (005, capture-before-close requirement ONLY - deliberately NOT a six-field-payload carrier, because hardening H7 names 004, 006 and 007 as the payload carriers and 005 carries an operational summary)
- C3 -> workflow-policies.md.tmpl [A] (001); `_ship.agent.md.tmpl` [P] (004, BOTH the thread-present and threadless dispositions); github-pr-automation.instructions.md.tmpl [P] (006, thread-present disposition only - every finding on that surface IS a PR review comment and therefore always has a thread); pr-lifecycle/SKILL.md.tmpl [P] (007, thread-present disposition); fix-ci/SKILL.md.tmpl [P] (007, BOTH dispositions - see the DUAL-PATH note below - plus the symmetric guard); circuit-breaker.instructions.md.tmpl [G] (005, the C3 symmetric guard per hardening H12 and nothing else)
- C4 -> workflow-policies.md.tmpl [A] (001); `_ship.agent.md.tmpl` Stop Conditions [P] (004); github-pr-automation.instructions.md.tmpl Stop Conditions [P] (006, the review-fix-push row states that cycle exhaustion does not authorize expansion); pr-lifecycle/SKILL.md.tmpl Stop Conditions [P] (007, the advisory-shadow-review row states the same); `_orchestrator.agent.md.tmpl` [P] (009); feature-flow-dark.prompt.md.tmpl [P] (009)
- C5 -> workflow-policies.md.tmpl [A] (001); `_ship.agent.md.tmpl` Role Boundary [N] (002, the carve-out itself, including the provenance-based cleanup exception); role-enforcement.instructions.md.tmpl [R] (003, fail-closed recognition of the carve-out, cited by policy ID and clause label per H5 -- NOT a restatement); `_ship.agent.md.tmpl` defer-capture section [P] (004, provisional priority + Stage-only reprioritization); github-pr-automation.instructions.md.tmpl [P] (006, same); pr-lifecycle/SKILL.md.tmpl [P] (007, same); fix-ci/SKILL.md.tmpl [P] (007, same); `_stage.agent.md.tmpl` [P] (008, the Stage-side authority statement that reconciliation needs no change to the carve-out)
- C6 -> workflow-policies.md.tmpl [A] (001); `_stage.agent.md.tmpl` [P] (008, deferred-expansion precedence route AND late-identifier reconciliation)
- C7 -> workflow-policies.md.tmpl [A] (001) - authoritative only. No restating carrier exists, and that is deliberate: the Violation Action is a single telemetry+halt contract with no per-surface procedure to implement.

CLAUSE -> BEHAVIOR -> CARRIER-SUBSET MAPPING. The rows above establish PRESENCE (which surfaces carry a clause at all). The behaviours below establish WHICH ASSERTION each carrier must satisfy, and are the subsets the semantic suite 134.012-T resolves by behaviour ID. Each subset is justified, not defaulted; a subset that silently equalled its whole clause row would re-create the false equivalence this block exists to prevent. B1-B18 is the COMPLETE authoritative range: a semantic assertion that carries no behaviour ID is outside the audit and cannot be checked for subset fidelity, so every assertion in 134.012-T must name one.

- B1 `c1-classification-gate` -> 001[A], 005[N], 004, 006, 007-pr-lifecycle, 007-fix-ci
- B2 `c1-test-text` (the literal 'same file' / 'same function' / 'same PR' / 'same subsystem' / 'related' not-sufficient wording -- all FIVE discriminators the authoritative C1 declares insufficient -- plus the three worked cases) -> 001[A], 005[N] ONLY. Every other carrier is forbidden by H5 to restate it.
- B3 `c2-capture-before-close` -> 001[A], 004, 005, 006, 007-pr-lifecycle, 007-fix-ci
- B4 `c2-six-field-payload` -> 001[A], 004, 006, 007-pr-lifecycle, 007-fix-ci. NOT 005 (H7 payload carriers only).
- B5 `c2-per-field-source-ids` (independent availability; no fused `PR/thread` token) -> 001[A], 004, 007-fix-ci. NOT 006 and NOT 007-pr-lifecycle: on a PR-review-comment surface both identifiers always exist at capture, so the per-field `N/A` case cannot arise there and asserting it would demand text the surface has no occasion to carry.
- B6 `c2-single-write-invariant` -> 004 (the invariant's authoritative carrier - it is a Ship ROLE property derived from C5, not a registry clause) and 008 (its designated consumer). Deliberately NOT asserted on 001, 006 or 007.
- B7 `c3-thread-present-reply-ordering` (capture -> reply citing the entry ID -> resolve -> PR/closure record) -> 001[A], 004, 006, 007-pr-lifecycle, 007-fix-ci. NOT 005: the circuit-breaker instruction governs cycle counting and scope classification and performs no PR thread operation at all (verified read-only against `templates/instructions/circuit-breaker.instructions.md.tmpl`, which contains no thread/reply/resolve step), so it is the ONLY surface legitimately excluded from this behaviour.
- B8 `c3-threadless-discharge` (task-level + run-level + closure residual-risk citation; no reply required or possible) -> 001[A], 004, 007-fix-ci (its CI-finding path only). NOT 006 and NOT 007-pr-lifecycle.
- B9 `c3-symmetric-guard` (H12), asserted in its COMPLETE TWO-PART form -- (i) a same-contract-surface completion IS in scope and must be fixed, not deferred; AND (ii) deferring one without a captured entry AND a residual-risk record is itself a P-021 violation, actioned per C7 -- because part (i) alone forbids only under-fixing and leaves silent deferral unpenalized, so a carrier stating half the guard does not carry B9 -> 001[A], 004, 005[G], 007-fix-ci. NOT 006 and NOT 007-pr-lifecycle. Subset UNCHANGED by the two-part clarification: the correction is to what each mapped carrier must state, not to which carriers state it, and it creates no obligation on the excluded surfaces.
- B10 `c4-non-bypass` -> 001[A], 004, 006, 007-pr-lifecycle, 009-orchestrator, 009-feature-flow-dark
- B11 `c5-carve-out-boundary` (capture-only creation grant; forbidden verbs by name -- discretionary removal AND discretionary archival, resolved from owner 134.002-T; manifest-derived cleanup exception preserved) -> 001[A], 002[N]. NOT 003: hardening H5 contracts 134.003-T to REFERENCE C5 by policy ID and clause label rather than restate it, so 003 never carries the creation grant or the manifest-derived cleanup exception, and asserting the complete boundary semantics there would demand text its task is contracted NOT to author. 003's genuinely contracted behaviour is B18.
- B12 `c5-stage-only-reprioritization` (provisional priority at capture; triage and re-prioritization remain Stage-only) -> 001[A], 004, 006, 007-pr-lifecycle, 007-fix-ci
- B13 `c6-stage-intake-precedence` -> 001[A], 008
- B14 `c6-late-identifier-reconciliation` -> 008 ONLY (Stage-owned; no other surface performs it). This behaviour ALSO discharges 008's C5 carriage: the STAGE AUTHORITY assertion inside the reconciliation workflow is where 008 states that reconciling under Stage's own stash authority requires no change to Ship's capture-only carve-out. Recorded explicitly because 008 appears in the C5 row but in neither B11 nor B12, and an unstated cross-clause discharge is indistinguishable from a coverage hole during audit.
- B15 `c7-violation-action` -> 001[A] ONLY
- B16 `c3-dual-disposition-carriage` (a carrier states BOTH the thread-present and the threadless C3 disposition and selects between them at runtime) -> DERIVED, not hand-listed: the subset is exactly `B7 INTERSECT B8` = 001[A], 004, 007-fix-ci. It is defined as a derivation rather than a literal list so it can never drift from B7/B8 the way a hardcoded copy would; if a future carrier gains the second disposition, it enters B16 automatically the moment it enters both parent subsets. NOT 006 and NOT 007-pr-lifecycle (thread-present only, so there is no second disposition to select). NOT 005 (carries neither disposition, only the B9 guard). The SHARED behaviour is dual carriage; the SELECTOR is carrier-specific and MUST NOT be flattened into one universal rule: (i) 001[A] and 004 select on ACTUAL THREAD AVAILABILITY at the moment the finding is classified, which is the AUTHORITATIVE selector -- 001 states it as `WHERE A REVIEW THREAD EXISTS` / `WHERE NO REVIEW THREAD EXISTS` and 004 states its threadless path as `no review thread exists for the finding at the moment it is classified`; (ii) 007-fix-ci expresses selection as a PATH split (CI-check-failure path -> threadless, review-comment path -> thread-present), which is a faithful IMPLEMENTATION of the authoritative selector on that surface because path there determines thread availability, not a competing rule. Asserting a finding-kind selector on 001 or 004 is a DEFECT, not a stricter test: a pre-PR local-review finding is a REVIEW-kind finding with NO thread, so a finding-kind selector would route it to the thread-present disposition and demand a reply on a thread that does not exist.
- B17 `c2-existing-entry-reuse` (a finding already captured -- in the CURRENT run OR a PRIOR run -- that later surfaces on a review thread reuses the EXISTING deferred entry by ID; no second entry is created) -> 004, 007-fix-ci, 008. The prior-run case is not a corner case: a CI finding captured with `review-thread ID: N/A` commonly gains a thread only when a later run's review reaches that code, and a run-scoped reuse rule would let that path create a duplicate. Reuse is keyed on the EXPANSION's stable deferred-entry ID, never on run boundaries. Derived as `B6 UNION {007-fix-ci}`: reuse is a direct consequence of the SINGLE-WRITE CAPTURE INVARIANT, so it inherits B6's carriers and B6's declared exemption from the clause-row subset check for the same reason (a Ship ROLE property derived from C5, not a registry clause row - which is why 008 may appear here despite being absent from the C2 row). 007-fix-ci is ADDED to the inherited set because fix-ci Step 6.5 re-queries for threads opened since Step 2.5, so a thread can surface AFTER the capture write inside a single run - the only surface where that is mechanically possible without a second Ship invocation. NOT 006 and NOT 007-pr-lifecycle: every finding there is already a review comment whose thread exists AT capture, so a late-surfacing thread cannot arise and asserting reuse would demand text those surfaces have no occasion to carry.
- B18 `c5-reference-role-recognition` -> 003[R] ONLY. This is the REFERENCE-ONLY half of the C5 boundary, split out of B11 in the owner-contract reconciliation because 134.003-T is contracted (H5) to REFERENCE C5 rather than restate it; the role marker was corrected `[N]` -> `[R]` in review cycle 2, because classifying a reference-only carrier as a NORMATIVE RESTATEMENT reintroduced through the role legend exactly the false requirement the B11 split existed to remove -- the marker would have resolved to restated clause prose that a correctly authored template must not contain. What 003 actually authors, and therefore all this behaviour asserts: (a) a capture-only stash write under C2/C5 matches the Allowed column and does NOT trigger the fail-closed unclassified-mutation halt; (b) any OTHER stash operation by Ship -- triage, prioritize, re-classify, edit, harvest, deliberate, discretionary remove/archive -- remains a P-010 violation and MUST halt; (c) the passage cites P-021 C5 by policy ID and clause label instead of restating the policy text; (d) the pre-existing fail-closed rationale paragraph is unchanged, so the carve-out reads as an enumeration in the Allowed column and never as a weakening of fail-closed semantics. It deliberately does NOT assert the creation grant or the manifest-derived cleanup exception -- those remain tested on their actual owners under B11.

AUTHORITY-OWNER MAP. For every claim the three contract-test files assert, this names the ONE artifact that owns it. Resolve each assertion from its owner, never from a surface that restates it -- three of the last five defects were counts or clauses read off a procedural restatement instead of the authority. Ship implements against this table without re-deriving it.

- Clause TEXT, phrase COUNTS, ORDERING, EXCEPTIONS, VIOLATION ACTION for C1-C7 -> 134.001-T. Includes: C1's FIVE insufficient discriminators; C2's SIX payload fields and per-field source-ref availability; C3's thread-availability selector, its two-part symmetric guard, and the three threadless residual-risk records; C4's ACTIVE-CYCLE boundary (no authorization, operator authorization included, expands the cycle that discovered the expansion; authorization is FORWARD-acting only, opening a separate work unit through C2 capture plus C6 deliberation); C5's discretionary-vs-manifest-derived distinction; C7's telemetry-AND-halt action; Amendment Log version 1.20.0 (registry ends at P-020/1.19.0).
- Behaviour MAP, carrier SUBSETS, carrier ROLES `[A]/[N]/[R]/[P]/[G]`, B16/B17 DERIVATIONS, exemption set, cross-file allocation -> THIS block in 134.011-T.
- Ship Role Boundary carve-out, the forbidden VERB LIST (triage, prioritize/re-prioritize, re-classify, edit, harvest, deliberate on, discretionary removal/archival) -> 134.002-T (+ hardening H1).
- Ship post-merge STEP 7 `backlogit_stash_remove` on `custom_fields.source_stash_id` (post-merge Step 6 is entry CREATION -- do not conflate) -> hardening H2.
- role-enforcement fail-closed recognition of the carve-out -> 134.003-T.
- Ship defer-capture procedure, SINGLE-WRITE CAPTURE INVARIANT, LATE-SURFACING THREAD rule, both C3 dispositions, and the DEFERRED-ENTRY DISCOVERY lookup plus its DISCOVERY FAIL-SAFE (sources, join keys, the FOUR-CASE disposition truth table over candidate-count x identity-confirmation, and the two discovery-failure paths -- ambiguous/unconfirmed identity and unavailable lookup -- BOTH of which CAPTURE and differ only in the evidence token recorded) -> 134.004-T. Reuse requires POSITIVE identity confirmation; capture is never suppressed by a discovery failure. 134.007-T REFERENCES this procedure for fix-ci and MUST NOT restate it.
- circuit-breaker operational restatement and the THREE worked discrimination cases -> 134.005-T (H5 forbids any other surface restating C1 test text; H6 governs the worked cases).
- github-pr-automation ORDERED sequence (capture -> reply citing the deferred ID -> resolve -> PR/closure residual-risk record) -> 134.006-T.
- pr-lifecycle and fix-ci contracts, fix-ci DUAL-PATH carriage and its path-split selector -> 134.007-T (surface evidence: `templates/skills/fix-ci/SKILL.md.tmpl` Steps 2.5/3/6/6.5, read-only).
- Stage intake precedence, UNCONDITIONAL DUPLICATE DETECTION, and LATE-IDENTIFIER RECONCILIATION -> 134.008-T. These are TWO independently triggered obligations, not one: detection runs over EVERY triaged deferred entry regardless of field population, while reconciliation fires on an `N/A` source ref. Do not read either trigger off the other -- 134.004-T's discovery fail-safe depends on the UNCONDITIONAL one, and gating detection on `N/A` leaves every fully-populated duplicate undetected. Includes the duplicate disposition: duplicates are ARCHIVED via backlogit's stash archive operation, never destructively removed (the CLI offers `stash archive` and no `stash remove`; the `backlogit_stash_remove` MCP tool is deprecated in favour of `backlogit_stash_archive`). Do NOT read this verb off hardening H2, which governs SHIP's manifest-derived post-merge Step 7 -- a different actor, a different provenance and a different operation.
- Orchestrator and feature-flow-dark C4 non-bypass -> 134.009-T.
- `HARNESS_ENFORCED_SUMMARY` range `P-001 through P-021` -> 134.010-T (carries NO clause text; deliberately excluded from AUTHORING_TASKS).
- Which surfaces are DOGFOODED (byte-identity + checksum scope) -> the plan's surface map S1-S11. S1 workflow-policies and S7 skills are NOT dogfooded and are correctly absent from the byte-identity list; assert those two by template render only.
- `checksum:` values -> `.autoharness/harness-manifest.yaml`. Canonical unittest gate -> `docs/compound/097-S-canonical-unittest-gate.md`.
- Hardening constraints H1-H15 -> `docs/plans/2026-08-18-bounded-fix-cycle-scope-containment-hardening.md`.

CROSS-FILE BEHAVIOUR ALLOCATION. The eighteen behaviours are discharged across the feature's THREE contract-test files, and the split is declared here so no file has to infer it. 134.012-T (capture-and-discharge semantics, and the allocation guards themselves) owns B3, B4, B5, B6, B7, B8, B9, B16, B17. 134.013-T (clause-boundary semantics) owns B1, B10, B11, B12, B14, B18. 134.011-T (policy contract) owns B2 via its literal-clause test, B13 via its precedence test, and B15 via its policy-registry test. The three sets MUST be PAIRWISE DISJOINT and their union MUST equal B1-B18 exactly; a behaviour claimed by more than one file is duplication of the kind that caused the original matrix drift, and one claimed by none is the invisible-assertion failure that MATRIX CORRECTION 5 exists to prevent. Two allocation invariants are load-bearing and are stated here rather than left to be rediscovered: DERIVED behaviours sit with their PARENTS (B16 = `B7 INTERSECT B8` and B17 = `B6 UNION {007-fix-ci}` are in 012 because B6/B7/B8 are, so the DERIVED-SUBSET guard never has to recompute an intersection whose operands live in another module), and behaviours SPLIT from one another stay together (B18 was split out of B11 and moved with it to 013). DISCHARGE MEANS FULL COVERAGE, NOT ATTRIBUTION: an owning file discharges a behaviour only when its test asserts that behaviour's complete mapped semantics across its COMPLETE declared carrier subset. Allocation and coverage are separate properties, and a file may legitimately own a behaviour it under-covers -- which is a coverage defect, not an allocation defect, and must be reported as such rather than passing because the ID appears in the right constant. The RANGE-COMPLETENESS guard in 134.012-T enforces pairwise disjointness, union completeness AND per-ID subset sufficiency against the subsets declared above, IMPORTING each sibling's `OWNED_BEHAVIOURS` rather than hardcoding a copy; that import direction is one-way (012 reads 011 and 013; neither reads 012), which is what keeps the three-way guard acyclic and why the dependency edges run 011 -> 013 -> 012. OWNER INVENTORIES ARE DERIVED, NEVER DECLARED: each split file's list of AUTHORING TASKS -- the tasks whose text its marker strings resolve against -- is the UNION of the authoring tasks appearing in the carrier subsets of that file's `OWNED_BEHAVIOURS`, computed from the mapping above. It is a convenience listing for the implementer, not a second source of truth, and where it disagrees with the mapping THE MAPPING WINS and the inventory is the defect. A pre-split inventory copied across a split is stale by construction, because the split moves behaviours and the union moves with them; PR #372 review-fix cycle 2 found exactly that in both directions at once (012 retained 134.002-T after B11/B12 left it, and 013 omitted 134.005-T even though B1 includes the circuit-breaker `[N]` carrier and 013's own C1-GATE test asserts `both policy surfaces`). The current derived unions are: 011 -> {001, 005, 008}; 012 -> {001, 004, 005, 006, 007, 008}; 013 -> {001, 002, 003, 004, 005, 006, 007, 008, 009}. Recompute them whenever a behaviour moves between files.
DUAL-PATH SURFACES. A carrier's path set is determined by the FINDING KINDS it actually handles, never by the loop's name or its primary purpose. This rule governs PRESENCE -- which dispositions a surface must carry -- and it does NOT determine SELECTION, which is how the surface chooses between them at runtime. Conflating the two is what produced the B16 selector defect: finding kinds tell you a surface needs both dispositions; ACTUAL THREAD AVAILABILITY is what tells it which one applies to the finding in hand. Two carriers handle both kinds and therefore carry BOTH C3 dispositions:

- `_ship.agent.md.tmpl` (004) - review-fix findings (thread-present) and build/CI findings plus pre-PR local-review findings (threadless).
- `fix-ci/SKILL.md.tmpl` (007) - CI check failures (threadless: an open PR exists, so the PR number is known and only the review-thread ID is `N/A`) AND PR review comments (thread-present: the skill maintains a full review-thread inventory, replies per template, resolves Copilot threads via GraphQL, and enforces a NON-NEGOTIABLE reply gate requiring every open thread to be replied to before the local quality gate).

`github-pr-automation.instructions.md.tmpl` (006) and `pr-lifecycle/SKILL.md.tmpl` (007) are thread-present ONLY, because every finding they handle is a PR review comment. `circuit-breaker.instructions.md.tmpl` (005) performs no thread operation at all and carries neither disposition, only the symmetric guard.

MATRIX CORRECTION (Stage contract replanning, 2026-08-19): the previous matrix listed C2 without github-pr-automation and C3 with only github-pr-automation and pr-lifecycle. Both were under-listed against the tasks that actually author them: 134.006-T criterion (b)-(d) makes github-pr-automation a full C2 AND C3 carrier, and 134.007-T makes fix-ci a C3 carrier via the threadless discharge. Most importantly, _ship.agent.md.tmpl was absent from C3 entirely even though 134.004-T authors BOTH C3 dispositions there. The practical consequence is that the coverage matrix as previously written would not have detected ANY of the three defects found in the prior fix cycles, because the surfaces those defects lived on were not listed as carriers of the clause they broke.

MATRIX CORRECTION 2 (Stage readiness fix cycle 1, 2026-08-19, current-HEAD review of 1dad725a, staging finding P1.1): the correction above was itself still incomplete, in the same way and for the same reason. C2 omitted BOTH authoritative `workflow-policies.md.tmpl` and `circuit-breaker.instructions.md.tmpl`, even though 134.001-T authors C2 authoritatively and 134.005-T acceptance criterion 6 states the C2 capture-before-close requirement outright; C3 omitted `circuit-breaker.instructions.md.tmpl`, whose criterion 7 carries the C3 symmetric guard mandated by hardening H12. The registry was also listed inconsistently -- named in the C3 and C7 rows but absent from C2, C4, C5 and C6 -- despite authoring all seven clauses. Root cause of BOTH corrections: the matrix was derived by reading the carrier tasks rather than by systematically inverting the authoring set, and it existed in three separately maintained copies (here, hardening H11, and 134.012-T's carrier lists) that could drift independently. Fixed by deriving the matrix from a full sweep of every task that authors C1-C7, adding the explicit registry rule, and designating THIS block as the single source of truth that H11 and 134.012-T now reference instead of restating.
MATRIX CORRECTION 3 (Stage readiness fix cycle 2, 2026-08-19, current-HEAD review of b449dcbf, staging findings P1.1/P1.2/P1.3/P1.4): the first two corrections both re-inverted only the clauses whose defects had already been observed -- C2 and C3 in correction 1, C2/C3/C5 in correction 2 -- so C1 and C4 were never re-derived at all and stayed at their original, narrowest form. A full inversion across all seven rows found: C1 listed only the registry and circuit-breaker even though 134.004-T, 134.006-T and 134.007-T all gate their loops on C1 classification; C4 listed only 004 and 009 even though 134.006-T and 134.007-T both carry the cycle-exhaustion-does-not-authorize-expansion annotation in their Stop Conditions rows; and C5 listed only the two carve-out surfaces even though four procedural surfaces (004, 006, 007 x2) carry the Stage-only reprioritization rule and 008 carries the Stage-side authority statement. The carrier-completeness guard was itself the deepest instance of the same fault: it checked only C2 and C3, and its expected set omitted 134.005-T, so the guard could not have detected the very omission that correction 2 had just repaired by hand. Root cause: a completeness guard scoped to the clauses where defects were previously FOUND cannot detect defects in the clauses nobody has looked at yet. Fixed by re-deriving all seven rows from a full sweep of every content task's acceptance criteria, and by rewriting the guard to invert a declared AUTHORING_TASKS constant covering C1-C7 for tasks 001-009.

CARRIER-ROLE RATIONALE (same cycle, staging finding P1.4): the corrections above expanded the rows correctly, but an expanded row is only safe if the coverage test stops demanding one identical marker from every carrier in it. Three of the newly added pairings would otherwise have been unsatisfiable or unfaithful: hardening H5 forbids 004/006/007 from restating the C1 test text that 005 is specifically designated to carry; github-pr-automation has no threadless case to describe because every finding on that surface is a PR review comment; and fix-ci has no thread to reply on. Demanding B7 reply-ordering from fix-ci, or B8 threadless discharge from github-pr-automation, would have forced a contract that contradicts the surface's own subject matter -- the precise failure mode that produced the paired-`N/A` defect in the first place. The `[A]`/`[N]`/`[P]`/`[G]` roles and the B1-B15 behaviour subsets exist to keep every pairing both asserted and satisfiable. [SUPERSEDED IN PART, readiness fix cycle 3: the clause "and fix-ci has no thread to reply on" in this paragraph is FACTUALLY WRONG and is corrected by MATRIX CORRECTION 4 below. The reasoning about role-appropriate markers stands and is unaffected; only the fix-ci example was mistaken. The original wording is preserved here because it is the recorded rationale that the next correction had to overturn.]

MATRIX CORRECTION 4 (Stage readiness fix cycle 3, 2026-08-19, current-HEAD review of 904c46b9, staging finding P1): fix-ci was modelled as a THREADLESS-ONLY carrier across the matrix, this task's B7/B8 subsets, 134.008-T and 134.012-T. That is false, and it was verified false by reading `templates/skills/fix-ci/SKILL.md.tmpl` directly rather than reasoning from the skill's name. The template's own description is "Detect CI pipeline failures AND REVIEW COMMENTS"; it carries Step 2.5 (Copilot review-comment detection, building a full thread inventory with per-thread reply status), Step 3 (categorize review comments valid/partial/invalid), Step 6 (reply to the thread per the github-pr-automation reply templates, then resolve Copilot-authored threads via the GraphQL `resolveReviewThread` mutation), and Step 6.5 (a NON-NEGOTIABLE reply gate that forbids pushing any commit while any open thread is unreplied). A surface with a mandatory reply gate is the LAST surface that should have been excluded from thread-present reply ordering.

Root cause, and it is a new one rather than another instance of the previous three: every earlier correction re-derived the matrix from the BACKLOG TASKS' acceptance criteria, which is the right method for detecting an under-listed carrier but inherits any factual error those criteria already contain. 134.007-T asserted "a CI/build failure has no review thread", which is true of a CI FINDING and was silently generalized into a claim about the fix-ci SURFACE. Inverting the authoring set cannot catch that, because the authoring set is exactly where the wrong premise lives. The correction is the DUAL-PATH SURFACES rule above: a carrier's path set is determined by the finding kinds it actually handles, verified against the template, never inferred from the loop's name.

MATRIX CORRECTION 5 (Stage correction operation, 2026-08-19, checkpoint-20260819-165602): correction 4 added the dual-path and entry-reuse assertions to 134.012-T with correct substance and correct carriers, but did NOT assign them behaviour IDs. The mapping therefore still ended at B15 while the suite exercised seventeen behaviours, and the two newest -- precisely the ones guarding the most recently discovered defect class -- sat OUTSIDE the audit: not checked by SUBSET-FIDELITY, not counted by any completeness assertion, and free to drift from the matrix without failing anything. Registered as B16 `c3-dual-path-selection` and B17 `c2-existing-entry-reuse`, both DERIVED from existing subsets (`B7 INTERSECT B8` and `B6 UNION {007-fix-ci}`) rather than hand-listed, so they cannot drift from their parents.

Root cause, distinct again from the previous four: every prior correction fixed the CONTENT of the map and its guards, but the guards only ever policed entries that were already IN the map. Nothing asserted that the map was COMPLETE with respect to the suite -- that every semantic assertion carries an ID at all. An unnumbered assertion is not a wrong entry, it is a missing one, and a guard that validates existing entries is structurally blind to it. This is the same shape as MATRIX CORRECTION 3's finding (a guard scoped to the clauses where defects were already found cannot see the clauses nobody looked at), applied one level up: a guard scoped to the behaviours already registered cannot see the assertions nobody registered. Fixed by the RANGE-COMPLETENESS guard in 134.012-T, which asserts the range is exactly B1-B17, that every ID in it is exercised, and that no assertion in the suite lacks an ID -- closing the bidirectional gap rather than the one direction that was already covered.

MATRIX CORRECTION 6 (Stage B16/B17 correction review cycle 1, 2026-08-19, current-HEAD review of 77d1a7e0, staging findings P1.1/P1.2): two defects, both introduced by correction 5 itself.

P1.1 -- B16 was defined as selecting between the two C3 dispositions by FINDING KIND, which contradicts the authoritative selector. 001 states C3 as `WHERE A REVIEW THREAD EXISTS` / `WHERE NO REVIEW THREAD EXISTS` and 004 states its threadless path as `no review thread exists for the finding at the moment it is classified`: both select on ACTUAL THREAD AVAILABILITY. A pre-PR local-review finding is a REVIEW-kind finding with no thread, so a finding-kind selector routes it to the thread-present disposition and demands a reply on a thread that does not exist -- the exact class of error this feature exists to prevent, reintroduced by the guard meant to protect against it. Root cause: correction 4 established that a surface's PATH SET is determined by the finding kinds it handles, and correction 5 over-generalized that presence rule into a SELECTION rule. fix-ci's path split is a faithful implementation of thread availability on that surface, not a competing selector, and it was wrong to lift it into the shared behaviour. Fixed by defining B16's shared behaviour as dual CARRIAGE with explicitly carrier-specific SELECTOR semantics, and by stating the presence/selection distinction in the DUAL-PATH SURFACES rule so the two cannot be conflated again.

P1.2 -- the cross-file allocation counted B2, B13 and B15 as discharged by 134.011-T when those tests covered only part of each behaviour: B2 was asserted on circuit-breaker alone with three of its four not-sufficient phrases and none of the three worked cases, though its subset is {001[A], 005[N]}; B13 was asserted on the Stage agent alone, though its subset is {001[A], 008}; and B15 was discharged by a structural section-presence check that would pass against a registry whose C7 violation action had been weakened to telemetry-without-halt. Root cause: an ASYMMETRY between the two halves of the audit. For behaviours a file OWNS, SUBSET-FIDELITY already checks the asserted carriers against the declared subset -- but for behaviours marked DISCHARGED_ELSEWHERE, nothing did; the allocation guard treated the ID appearing in the constant as proof of coverage. A partially-covered behaviour reported green is worse than an uncovered one, because the uncovered case at least fails a count. Fixed by making discharge a COVERAGE claim cross-checked against the declared subset and mapped clause text on both sides of the allocation, and by expanding the three tests to their full subsets.

MATRIX CORRECTION 7 (Stage correction cycle 2, 2026-08-19, current-HEAD review of 663205a6, staging findings P1.1/P1.2): two carrier-contract shortfalls where the SEMANTIC TEST was correct and the CARRIER TASK it tests was not -- the inverse of every earlier correction, which fixed tests against correct carriers.

P1.1 -- the authoritative C1 declares FIVE insufficient discriminators ('same file', 'same function', 'same PR', 'same subsystem', 'related') but 134.005-T's operational restatement carried only four, omitting 'related', and the B2 assertion demanded four. Root cause: correction 6 raised B2 from three phrases to four by reading the RESTATEMENT (005) and counting what was there, rather than reading the AUTHORITATIVE clause (001) and counting what should be. That is the same defect the restatement/authority split exists to prevent, committed inside the fix for it: the restating carrier is precisely the surface that can drift, so it can never be the source of truth for its own completeness. B2 now resolves the phrase count from 001 explicitly.

P1.2 -- the C3 symmetric guard is a TWO-PART clause in 001: (i) a same-contract-surface completion IS in scope and must be fixed, not deferred; AND (ii) deferring one without a captured entry and a residual-risk record is itself a violation. 134.004-T and 134.007-T carried only part (i); 134.005-T carried part (ii) but omitted the residual-risk record from it. The B9 semantic test in 134.012-T already required the complete two-part form, so the suite was UNSATISFIABLE against its own carrier contracts -- a contradiction that would surface only at implementation time, when whoever wrote the tests would have had to either weaken the test or edit contracts mid-build. Root cause: a compound clause was treated as its memorable half. Part (i) forbids under-fixing and is the half that reads like the point of the guard; part (ii) forbids silent deferral and is what makes it SYMMETRIC. Fixed by stating both parts in all four mapped carrier contracts, with the B9 subset deliberately UNCHANGED -- the correction is to what each mapped carrier must state, not to which carriers state it, and it creates no obligation on 006 or 007-pr-lifecycle.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on ALL content tasks: 134.002-T, 134.003-T, 134.004-T, 134.005-T, 134.006-T, 134.007-T, 134.008-T, 134.009-T, 134.010-T. It asserts clauses that must already exist, so it is the final task in the chain (each dependency edge is enumerated discretely per review finding R3).

Reuse the import surface from tests/test_circuit_breaker_policy_contract.py: `from autoharness.verify_workspace import _derive_template_variables, _render_template`.

Marker text for each clause should be chosen as a stable, distinctive substring authored in 134.001-T (e.g. the literal token `DEFERRED SCOPE EXPANSION` and the phrase `same contract surface`), not a fragile long sentence.

Commit with an explicit enumerated path list only (hardening H13).

SCOPE SPLIT (Stage contract replanning, 2026-08-19; extended in PR #372 review-fix cycle 1): this task covers STRUCTURAL coverage only -- byte identity, manifest checksums, and the clause-to-carrier presence matrix. The SEMANTIC assertions are carried by two sibling tasks in two separate test files: 134.012-T (capture-and-discharge semantics -- threadless discharge, per-field source-ID availability, single-write capture, task/run/closure citations, and the no-PR/no-thread assumption guards -- plus the behaviour-map allocation guards) and 134.013-T (clause-boundary semantics -- the C1 gate, C4 non-bypass, C5 boundary and its reference-only carrier, Stage-only reprioritization, and the reconciliation consumer). The original two-way split was required by the 2-hour rule: adding roughly a dozen behavioural assertions -- each needing its own stable marker strings across eight-plus template surfaces -- to this task's existing byte-identity, checksum and seven-clause matrix work would exceed the bound. The three-way split was required for the same reason one level down, once the full C1-C7 inversion had grown the semantic suite past the bound on its own. The three files are disjoint, so they can be reviewed and diagnosed independently, and a structural, semantic and boundary failure each stay distinguishable.
MATRIX CORRECTION 8 (Stage authority audit, correction cycle 3, 2026-08-19, operator-directed): rather than fixing another reported instance, this cycle audited every acceptance criterion in 134.011-T and 134.012-T against the artifact that OWNS each claim. Two mismatches were proven and fixed, two negative guards were strengthened, and one stale ordering was corrected. (a) The H1 role-boundary guard checked Ship's forbidden verbs but omitted ARCHIVAL, which its owner 134.002-T declares as "discretionary removal/archival" -- the guard would have passed against a Role Boundary permitting Ship to discretionarily archive a Stage-owned deferred entry. (b) 134.012-T's marker-provenance criterion named three authoring tasks while the suite asserts text authored across eight; it now enumerates all eight with their behaviours, plus the rule that a marker resolves from the task that AUTHORS the text and never from a surface that restates it. (c) The SINGLE-WRITE and NO-PR/NO-THREAD negative guards named verbs and assumptions but no historically defective wording, so neither was demonstrably non-vacuous; each now names the exact wording it must fail against -- the original thread-present "capture first and only afterward record the PR and review-thread IDs" flow, and the original "(PR number, review-thread ID when applicable, ...)" enumeration that qualified only the thread ID. (d) 019-DL's S6 surface row still carried the pre-capture-first "reply/resolve/capture/reference" ordering.

The audit also confirmed, and records here so no later cycle re-derives them: the byte-identity surface list correctly EXCLUDES workflow-policies (S1) and the two skills (S7) because the plan surface map marks them dogfooded: no; Amendment Log 1.20.0 is correct against the registry's 1.19.0/P-020 end state; the six-field payload and B4's exclusion of 005 match 134.001-T and H7; post-merge Step 7 is the cleanup step and 134.002-T's "Step 6" reference is about entry creation, not a mismatch; 134.010-T is correctly outside AUTHORING_TASKS because it carries no clause text; and the B3, B7, B11 and B12 subsets enumerated in 134.012-T match this map exactly.

The findings share the shape named in CORRECTION 7 -- a claim resolved from a convenient restatement instead of its authority -- so the durable remedy is the AUTHORITY-OWNER MAP added above, which names the owning artifact for every class of claim these two test files assert. Resolve from that table first; it is the antidote to this defect class, not another instance fix.

MATRIX CORRECTION 9 (Stage owner-contract reconciliation, 2026-08-19, checkpoint-20260819-183625, six terminal P1 findings): the first correction driven entirely by resolving claims against the AUTHORITY-OWNER MAP rather than by reacting to a symptom, and the first to change the behaviour RANGE. (1) C2 APPLICABILITY -- the FUSED-REF guard was declared UNIVERSAL while carrying B5's ID, so it demanded an independent per-field applicability qualifier on 006 and 007-pr-lifecycle, the two surfaces B5 deliberately excludes because both identifiers always exist there. Split into a genuinely universal NEGATIVE (no fused `PR/thread` token anywhere -- correct universally) and a B5-subset POSITIVE (independent qualifiers and per-field `N/A`). Scope is now a property of the assertion, not of the behaviour ID it happens to carry. (2) C5 ARCHIVAL -- authoritative C5 prohibited discretionary REMOVAL only, while owner 134.002-T had always declared "discretionary removal/archival"; archival reaches the same loss by a second verb. Both are now named under one DISCRETIONARY qualifier on the authoritative clause, the plan clause table, hardening H2, 019-DL clause 5 and B11, with the manifest-derived Step 7 exception unchanged. (3) B11 ROLE-ENFORCEMENT OWNERSHIP -- B11 listed 003 and asserted the creation grant and the manifest-derived cleanup exception there, but hardening H5 contracts 134.003-T to REFERENCE C5 by ID and clause label rather than restate it. The test therefore demanded text a correctly authored template is contracted NOT to carry. Split: B11 keeps the complete boundary semantics on their real owners 001/002, and the new B18 `c5-reference-role-recognition` asserts exactly what 003 authors -- Allowed-column recognition, the forbidden verbs incl. remove/archive as halting P-010, citation-not-restatement, and the untouched fail-closed rationale -- with a negative guard forbidding the boundary semantics from being demanded there. (4) B6 STAGE CONSUMER -- the SINGLE-WRITE test asserted only 004, leaving 008 (the mapped consumer) undischarged; a suite proving Ship writes once without proving anyone may close the resulting gap would pass on a policy that permanently strands every `N/A`. Both halves are now asserted together, neither weakening the other. (5) B14 COMPLETENESS -- B14's subset is 008 ALONE, so its test is the only place the workflow is exercised; it asserted mere existence. It now asserts all six owner-defined obligations (trigger, retrieval-by-join-key, Stage authority, anti-duplication, non-blocking, idempotence), and the Stage-authority obligation is flagged as what discharges 008's cross-clause C5 carriage, so omitting it silently reopens the C5 row. (6) B17 PRIOR-RUN REUSE -- reuse was scoped to "this run", but the commonest real path is a CI finding captured in an EARLIER run with `review-thread ID: N/A` that gains a thread when a later run's review reaches that code; the run-scoped rule permitted exactly the duplicate the behaviour forbids. Reuse is now keyed on the stable deferred-entry ID, never on run boundaries. Range extended B1-B17 -> B1-B18 in the allocation block, hardening H11, and the CARRIER-SET/SUBSET-FIDELITY/RANGE-COMPLETENESS guards.

Root cause, and why it differs from CORRECTIONS 1-8: those were wrong CONTENT resolved from the wrong source. These six are wrong APPLICABILITY -- each assertion was individually true of SOME carrier and was then applied to a set the owner never contracted. Findings 1 and 3 are the same error mirrored: one demanded a qualifier on surfaces that cannot carry it, the other demanded boundary text on a surface contracted only to reference it. Findings 4, 5 and 6 are its complement -- a subset or a lifetime narrower than the owner's, leaving a mapped carrier or a real path untested. The durable rule this adds to the AUTHORITY-OWNER MAP: resolve not only WHAT a claim says but WHERE the owner says it applies, and never let a behaviour ID silently widen or narrow an assertion's scope.

MATRIX CORRECTION 10 (Stage owner-reconciliation review cycle 2, 2026-08-19, current-HEAD review of 53b080fa, P1x2): both findings were introduced by the two immediately preceding passes, and both are VERIFICATION failures rather than reasoning failures. (1) B14 OWNER COVERAGE, third attempt. Review cycle 1 fixed a missing-obligation defect by mapping each owner criterion LINE to one obligation and asserting zero unmapped -- a check that passes while a compound clause hides INSIDE a line, which is exactly where the next defect was: owner criterion L42 carries FIVE distinct obligations (in-place update, stable lifetime identity, DETECT duplicates, reconcile into earliest-captured and REMOVE them, RECORD the merge), of which only the first two and half the fourth were asserted. The remediation half of anti-duplication was missing entirely, which matters because a rule that only forbids creating duplicates has nothing to say once duplicates exist -- and they do, since a prior-run capture whose entry is not found on re-query is precisely how a second entry appears. Re-resolving the owner ATOMICALLY rather than patching the count also surfaced a third omission nobody had flagged: L40's negative half, that Stage MUST NOT ask Ship to supply the identifiers by editing the entry, without which obligation (2) silently reauthorizes a second Ship write. The completeness rule is now atomic by construction: every distinct MUST/MUST NOT in L39-L46 maps to a numbered or lettered obligation, a sentence carrying two obligations counts as two, and the audit is 22 atomic obligations with zero unmapped. (2) B18 CARRIER ROLE. The B11/B18 split correctly removed the false requirement that role-enforcement carry the complete C5 boundary semantics, but left the carrier tagged `[N]` NORMATIVE RESTATEMENT -- a role whose legend explicitly designated role-enforcement for C5, and whose marker resolution demands restated clause prose. H5 and 134.003-T L34 contract that surface to REFERENCE C5 by policy ID and clause label instead, so the role marker reintroduced through the legend exactly the false requirement the split had just removed, by the back door. Added the `[R]` REFERENCE-ONLY role: asserted on its citation plus its recognition/enforcement behaviour, never on clause prose, with an explicit rule that a marker satisfiable only by restated policy text is a defect against the role. C5 row, B18 subset, the role-marker rule, the AUTHORITY-OWNER MAP role list, hardening H11 and the plan's role narrative all re-inverted; the `[N]` legend no longer claims role-enforcement.

Root cause shared by both, and the reason this correction changes METHOD rather than content: each was a correct fix whose VERIFICATION operated one level coarser than the defect. Cycle 1 verified a range by line when the duplicate lived within a line; cycle 1 verified obligations by criterion when the omission lived within a criterion; the B11 split verified subset membership when the false requirement lived in the role marker. The durable rule this adds: verify at the granularity the defect can occupy, not the granularity the artifact is organized in. Occurrence counts, not line matches; atomic obligations, not criterion lines; role semantics, not subset membership.

MATRIX CORRECTION 11 (Staging PR #372 Copilot review-fix cycle 1, 2026-08-19, five review threads, HEAD 1c7e2458): the first review of this feature by an agent that had NOT written it, and the finding profile is correspondingly different from the eight self-reviews before it. None of the five is a matrix or subset defect; three are places where the artifacts stated a rule that could not be EXECUTED, which every prior cycle's internal-consistency checking was structurally unable to see because the rules were mutually consistent -- just not actionable. (1) C4 read as a bypass. The clause listed the insufficient authorizations and closed with "only explicit operator authorization ... does", naming an authorization sufficient to expand the ACTIVE cycle -- the exact route around C1/C2/C6 the Statement forbids. The deliberated intent was always that authorization opens SEPARATE work; the wording never said so. Corrected on the authority (001), the deliberation (019-DL clause 4), the plan clause table, and the three Stop-Conditions carriers (004/006/007) whose halt-and-prompt is where an in-cycle "go ahead" is actually solicited. 009 was NOT touched: its dark surfaces already state the correct forward-acting rule, and editing them would have been the "same clause, therefore in scope" error this policy exists to prevent. (2) Prior-run entry reuse had no discovery procedure. B17 and 134.007-T required reuse of an entry captured in a PRIOR run while providing no way to FIND it: in-run reuse holds the ID returned by the capture just performed, and across runs that handle does not exist, so the "MUST NOT create a second entry" rule was unenforceable in precisely the case that produces duplicates. Added DEFERRED-ENTRY DISCOVERY and a two-direction DISCOVERY FAIL-SAFE to 134.004-T, authored ONCE and referenced by 007. [SUPERSEDED IN PART by MATRIX CORRECTION 13: the fail-safe's ambiguous-match direction described in the remainder of this paragraph was itself defective and no longer describes the contract. It is preserved here as the dated record of what cycle 1 did, not as current semantics.] The fail-safe is deliberately asymmetric: an ambiguous match fails CLOSED (no entry, cite all candidates, defer to Stage), while an unavailable lookup CAPTURES -- because a duplicate is recoverable through 008's reconciliation and a dropped finding is not. (3) Duplicate remediation said "removes". Corrected to ARCHIVE on two independent grounds: backlogit's CLI exposes `stash archive` and no `stash remove`, and the MCP `backlogit_stash_remove` is deprecated in favour of `backlogit_stash_archive`; and a duplicate is EVIDENCE that a discovery lookup returned a false absence, so destroying it destroys the diagnostic. (4) A breaker record counted a SUCCESS as a failed attempt. (5) Task 012 was knowingly oversized with a split line declared but not taken -- the review's point being that a known-necessary split deferred to implementation lands mid-execution with the bound already breached. Split taken; see the three-file CROSS-FILE BEHAVIOUR ALLOCATION above.

Root cause, and why it is new: every prior correction tested the artifacts against EACH OTHER. Findings (1)-(3) are all cases where the artifacts agreed with each other perfectly and none of them could be carried out by the agent that would have to execute it -- a bypass an agent would reasonably invoke, a lookup with no source or key, and a verb the tool no longer offers. Internal consistency is not executability, and no amount of cross-artifact auditing detects the difference. The durable rule this adds to the atomic/occurrence-granularity lessons of the prior cycles: for every MUST, ask WHO performs it, WITH WHAT INPUTS, and WHETHER THOSE INPUTS EXIST AT THAT MOMENT. A rule whose inputs are unavailable at the moment it fires is not a strict rule; it is an unenforceable one, and it will be silently skipped rather than loudly failed.

MATRIX CORRECTION 12 (PR #372 review-fix cycle 2, 2026-08-19, HEAD 47d5ad3e): the split's OWNER PROVENANCE INVENTORIES were contradictory in both directions at once, and the two errors have the same cause. 134.012-T still listed 134.002-T as an authoring task for a "provisional-priority carve-out reference", which the mapping has never supported: B12's subset is `001[A], 004, 006, 007-pr-lifecycle, 007-fix-ci` and 002 is not in it, so that entry was not made stale by the cycle-1 split -- it was WRONG BEFORE IT, and the split merely made it conspicuous. 002's only mapped behaviour anywhere is B11 (the Role Boundary forbidden-verb list), which 134.013-T owns. Separately, 134.013-T omitted 134.005-T even though B1's subset includes the circuit-breaker `[N]` carrier and 013's own C1-GATE test already asserts the gate across "both policy surfaces". So one file claimed an owner the mapping never gave it while the other disowned an owner its own test depended on. The two have DIFFERENT causes and only the second is a staleness case: 013's omission came from carrying the pre-split list across a split, which is stale by construction because a split moves behaviours and the owner union moves with them, whereas 012's 002 entry was a never-valid attribution that no split created and no re-derivation had ever been run against. Corrected by DERIVING both unions from the mapping above rather than editing the lists: 012 -> {001, 004, 005, 006, 007, 008} (six) and 013 -> {001, 002, 003, 004, 005, 006, 007, 008, 009} (all nine), each task now annotated with the specific behaviours it authors so a future reader can check an entry without recomputing the whole union. Two narrowings that a hand-edit reliably gets wrong are now written down: 005 owns only B3 and the B9 `[G]` guard in 012 (H7 excludes it from B4, and it performs no thread operation so it is excluded from B7), and 007 contributes B10 to 013 through pr-lifecycle ONLY, not fix-ci.

Why this is the same failure the matrix consolidation was supposed to end: the CARRIER-SET RESOLUTION rule already forbids hardcoding a carrier list, and these inventories WERE hardcoded carrier lists -- they just described carriers by their AUTHORING TASK rather than by their surface, so they did not look like the thing the rule prohibits. The rule was read as being about the shape of the data rather than about its provenance. The general lesson, and the reason OWNER INVENTORIES ARE DERIVED, NEVER DECLARED is now stated in the allocation block above: a derived value that is stored rather than computed is a cache, and every cache needs an invalidation rule. The split was the invalidation event and nothing recomputed. Any future move of a behaviour between the three files MUST recompute all three unions, and where an inventory and the mapping disagree the MAPPING WINS -- the inventory is the defect, never the evidence.

MATRIX CORRECTION 13 (PR #372 review-fix cycle 3, 2026-08-19, HEAD 5b8b91c9): two coupled P1 defects, both introduced by cycle 1's own discovery fail-safe, and both of the same shape -- a SAFETY MECHANISM THAT DEPENDED ON A GUARANTEE NOTHING PROVIDED. (1) The ambiguous-match direction suppressed capture, justified by the claim that the finding was "provably already captured". That claim is FALSE for the case the same sentence admits: a single candidate whose expansion statement CANNOT BE CONFIRMED to describe the same expansion. If it is a different expansion, the finding was never captured at all, and suppressing capture drops it silently -- the unrecoverable outcome the fail-safe's own asymmetry rationale says must never be traded for a recoverable one. Enumerating candidates is not identifying one. Corrected by making capture UNCONDITIONAL on both discovery-failure paths: the paths now differ only in the EVIDENCE TOKEN recorded (`DISCOVERY-STATUS: AMBIGUOUS` with every candidate ID, or `DISCOVERY-STATUS: LOOKUP-UNAVAILABLE`), written INSIDE field (2) of the fixed six-field payload so no seventh field is added. The path still fails closed -- but on the IDENTITY DECISION, which Ship cannot safely make and now defers to Stage with full evidence, rather than on the CAPTURE, which Ship always can and must make. The disposition is now a complete four-case truth table over (candidate count x identity confirmation), with POSITIVE CONFIRMATION a required predicate for reuse, because an unconfirmed reuse is an unrecoverable MIS-ATTACHMENT while an unnecessary capture is a recoverable duplicate. (2) That fail-safe rested on 134.008-T "requiring Stage to run duplicate detection over every deferred entry it triages, unconditionally" -- and 134.008-T did no such thing. Its single trigger fired only when a source-ref field was `N/A`, so a duplicate captured with every identifier populated -- the COMMON case on a PR-review-comment surface, where both PR and thread ID always exist -- would never be looked at. One artifact asserted a guarantee the other did not provide, and the assertion was load-bearing for the first defect's remedy. Corrected by splitting 008's triage step into TWO INDEPENDENTLY TRIGGERED obligations: (A) UNCONDITIONAL duplicate detection over every triaged entry regardless of field population, and (B) `N/A`-triggered late-identifier reconciliation. A discovery-status token ACCELERATES the scan by seeding it with candidate IDs but is explicitly NOT its trigger, since the worst duplicates -- those from a lookup that silently returned a false absence -- carry no token. B14's obligation (1) is now two-part with a negative guard against `N/A`-gated detection, and the audit output grew a FOURTH case, the clean scan, for the same reason the no-result case exists: an unrecorded clean scan under an unconditional trigger is indistinguishable from a scan that never ran.

Also corrected: 134.007-T's fix-ci criterion said "This skill MUST NOT restate that procedure" and then restated it in the immediately preceding clause, carrying the superseded ambiguous-match semantics -- the duplicated-source-of-truth failure occurring inside the sentence forbidding it, and a demonstration that a prohibition placed AFTER the text it governs does not police it. Replaced with a by-name reference only. 134-F's DoD enumerated Ship's forbidden verb set as "triage/prioritize/re-classify/edit/harvest/deliberate/discretionary-remove", omitting RE-PRIORITIZE and DISCRETIONARY ARCHIVAL from the set its owner 134.002-T declares; since the DoD is the feature's acceptance checklist, an implementation could satisfy it while leaving Ship free to archive a Stage-owned entry, so it is acceptance-authoritative and was completed to match the owner and made to cite it. And MATRIX CORRECTION 12's account of the 002 entry was itself wrong: 002 was never a B12 carrier (B12's subset is `001[A], 004, 006, 007-pr-lifecycle, 007-fix-ci`), so that inventory entry was not made stale by the split -- it was never map-supported, which makes it a different defect class from 013's genuine staleness and one that re-derivation would have caught at any point in the feature's life.

The durable lesson, and it is narrower than cycle 1's: A SAFETY MECHANISM MAY NOT ASSERT THE GUARANTEE IT DEPENDS ON. Cycle 1's fail-safe cited 008's unconditional scan as its justification, and that citation was accepted as evidence because it was written in the artifact doing the depending. Neither the AUTHORITY-OWNER MAP audit nor the owner-obligation audits caught it, because both check that a claim MATCHES its owner -- and this claim had no owner entry at all; it was a bare assertion ABOUT another artifact, which is exactly the shape no audit was looking for. When artifact X's correctness depends on artifact Y providing G, the check must be performed against Y and recorded on Y, not asserted on X.

MATRIX CORRECTION 14 (fresh Stage correction operation after the PR #372 review-fix budget was exhausted, 2026-08-19, terminal review of HEAD d52ab147): the B3/B5 source-ref rule had accumulated THREE negative guards across three cycles -- against the fused `PR/thread` token, against marking both refs `N/A` as a path-level default, and against making a PR or thread a precondition for capture -- and the terminal P1 was a FOURTH SHAPE that matched none of them. `PR number, review-thread ID (when applicable)` uses TWO separate tokens (so it passes the fused-ref guard), carries NO paired-`N/A` instruction (so it passes the pairing guard), and states no precondition (so it passes the root-cause guard) -- yet the single trailing qualifier attaches to the review-thread ID ALONE, which states the PR number UNCONDITIONALLY and contradicts C2's independent per-field availability. It survived in hardening H7 item (4) and `134.007-T` criterion (4) through three fix cycles for exactly that reason: each guard had been written to reject the specific string the review in front of it had produced, making the guard set a LIST OF PAST INSTANCES rather than a PARTITION OF THE FAILURE SPACE. Corrected by deriving both surfaces from authoritative C2 in `134.001-T` (both refs qualified, availability judged independently per field, explicit per-field `N/A`, fusion and joint qualification prohibited) and by adding an ASYMMETRIC-QUALIFIER guard whose test is SYMMETRY rather than presence -- both qualified or neither, never one -- with an explicit note that a grep for the literal `(when applicable)` is vacuous, since the same defect can be written `if one exists` or as a footnote.

The second half of the finding was worse, and is the shape H15 now names. `134.007-T` was a MAPPED B5 CARRIER THAT NEVER AUTHORED THE BEHAVIOUR AT ALL. Absence trips no negative guard: a surface that simply omits the per-field availability rule passes every prohibition ever written, because prohibitions can only reject text that is present. That is why fix-ci -- the ONE surface where the split case actually arises, since `fix-ci` cannot run without an open PR and a CI finding has no thread, so its PR number is KNOWN while its thread ID is UNAVAILABLE -- carried the defective enumeration and no rule. Corrected by adding an explicit B5 authoring criterion to `134.007-T` stating the rule in fix-ci's own terms, including that `N/A` is written ONLY for the specific unavailable field and is NEVER a path-level default, and by pairing the negative guards with a LIVE-CARRIER AGREEMENT test that resolves the required form FROM `134.001-T` at assertion time -- so a carrier can no longer pass by staying silent, and the authority and the test cannot drift together in the same direction and agree with each other while both diverge from C2.

Also corrected in the same operation: the B17 truth-table assertion ran "for every carrier in the B17 subset", which would have forced reference-only `134.007-T` to RESTATE `134.004-T`'s discovery procedure -- re-creating on that exact surface the duplicated-and-then-stale summary the owner-reconciliation cycle had just removed from it. B17 is now scoped by CARRIER ROLE: the full truth table on the authoring carrier (004), REFERENCE INTEGRITY (cites by name, resolves at point of use) on the reference-only carrier (007-fix-ci), the reconciliation form on 008, and the no-suppression negative guard universal across all three. A test that passes only when a reference-only carrier restates its owner is not a stricter test but a WRONG one. `134.006-T` and `007-pr-lifecycle` were deliberately NOT changed: B5 excludes them because both identifiers always exist on a PR-review-comment surface, so their unqualified enumeration is correct and adding qualifiers would demand text those surfaces have no occasion to carry.

The durable lesson is recorded as H15: NEGATIVE GUARDS MUST ENUMERATE DEFECT SHAPES, NOT DEFECT INSTANCES. For a rule over two independently-available fields the shapes are fused-into-one-token, both-qualified-together, ONE-qualified-and-not-the-other, NEITHER-qualified-where-required, and RULE-ABSENT-ENTIRELY. The last is the most dangerous because it trips nothing, and it is precisely what let a mapped carrier go five cycles without authoring its behaviour. Every negative-guard set now needs a positive agreement test resolved from the authoritative owner.

<!-- END:implementation-notes -->
