---
artifact_type: task
created_at: 2026-09-17T19:59:41.8954724Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 167.006-T
    - 167.001-T
    - 167.005-T
id: 167.009-T
labels:
    - closure
    - tests
    - anti-drift
    - invariant-guard
parent_id: 167-F
priority: medium
references:
    - docs/plans/2026-09-17-closure-evidence-naming-contract-plan.md
    - docs/compound/2026-09-06-composed-workflow-protocol-state-machine-validation.md
status: queued
title: Add non-drift guard tests binding producer prose, installed mirrors, and consumer-site enumeration to the contract constants
updated_at: 2026-09-17T22:11:38.8756197Z
---

PLAN UNIT U9 (plan revision 5). Domain: tests. Posture: test-first.

FILES (1): tests/test_closure_contract_nondrift.py (NEW).

PURPOSE
Make prose/code drift a test failure instead of a future incident. This is the guard that makes decision D1 ("no second definition anywhere") machine-enforced.

NORMATIVE SOURCE: plan revision 5, unit U9.

TESTS (4)
1. DOCUMENTED PATTERN EQUALS CODE CONSTANT — parse the Output bullet from templates/skills/operational-closure/SKILL.md.tmpl and .github/skills/operational-closure/SKILL.md, and assert each equals CANONICAL_CLOSURE_PATTERN_DOC, modulo the {{DOCS_CLOSURE}} <-> docs/closure substitution, which is THE ONLY difference the template and its installed mirror may have. Changing the pattern in prose alone fails; changing it in code alone fails.
2. TEMPLATE/INSTALLED PARITY — the two producer skill files agree with each other, and the two Ship agent files agree with each other, modulo the same placeholder substitution.
3. CONSUMER-SITE REGISTRY COMPLETENESS, SCOPED TO RUNTIME SURFACES — every path listed in CONSUMER_SITES exists, and no file under RUNTIME_SCAN_ROOTS outside the REGISTERED SET contains a closure-filename pattern definition. The registered set is (CONTRACT_DEFINITION_SITE,) + CONSUMER_SITES, BOTH READ FROM THE CONTRACT MODULE. Registering the authoritative module is required for satisfiability: closure_contract.py lives under src/autoharness/ and necessarily IS the pattern definition, so a scan recognizing only CONSUMER_SITES could never pass. Keeping the registration inside the contract — rather than as an exclusion list in the test — is what prevents it from becoming a second inventory of the very thing CONSUMER_SITES exists to be the only copy of. Narrative docs/ prose and test code are out of scope BY CONSTRUCTION, because the scan roots are read from the contract module rather than hard-coded here.
4. PATTERN-DOC DERIVATION GUARD — assert CANONICAL_CLOSURE_PATTERN_DOC == _render_pattern_doc(CANONICAL_CLOSURE_FILENAME_TEMPLATE), i.e. the documented text is DERIVED FROM the machine template rather than maintained beside it. Editing the template alone, or the doc constant alone, fails. Without this, test 1 would only pin prose to a constant that could itself drift away from the pattern the code actually matches.

NOTE ON BOUNDARIES
The RECOGNIZED_CLOSURE_PATTERNS cardinality pin lives in 167.001-T test 4, not here. This task owns drift detection; 167.001-T owns the closed-set invariant.

ACCEPTANCE
- No hard-coded scan root or exclusion list appears in this test module; both come from the contract.
- Touches NOTHING under docs/closure/ (D8).

DEPENDS ON: 167.001-T, 167.005-T, 167.006-T.