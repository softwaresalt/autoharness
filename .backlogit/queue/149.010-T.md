---
artifact_type: task
created_at: 2026-08-28T04:03:55.717397Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 149.005-T
    - 149.007-T
id: 149.010-T
labels:
    - S1
    - tests
parent_id: 149-F
priority: medium
references:
    - docs/plans/2026-08-27-pre-review-detector-sdk-plan.md
    - tests/
status: queued
title: 'U10 — Tests: assembler cycle detection, report emitter, freshness'
updated_at: 2026-08-28T04:03:55.717397Z
---

Unit tests for the DAG assembler (cycle -> zero nodes evaluated, blocked_upstream propagation) and the report emitter (flat list, epoch-keyed, non-overwrite, freshness). Domain: tests. Files: 2.

<!-- BEGIN:acceptance-criteria -->
* **INV-4**: the cycle test asserts the evaluation **COUNT is exactly zero**, not merely that the exit code is 2. A gate that exits 2 while having already evaluated nodes has auto-broken the cycle.
* **INV-1**: the on-disk report artifact is asserted to be a flat list with no edge/topology data.
* `blocked_upstream` propagation asserted for both `failed` and `insufficient_evidence` upstreams.
* Non-overwrite asserted at the same epoch.
* No network access.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan unit **U10**. Create `tests/test_detectors_assembler.py`, `tests/test_detectors_report.py`.

**4 scenarios**:
1. **Cycle injection** -> `invalid`, exit 2, and **zero nodes evaluated**.
2. **Upstream `failed`** -> downstream `blocked_upstream` (asserted NOT `passed`, NOT `skipped`).
3. **Freshness**: stale `head_sha` OR changed `tool_version_dims` -> `insufficient_evidence`, never a reused `passed`.
4. **Report shape**: flat list, epoch-keyed; a second run at the same epoch does **not** overwrite.

**Gate command (Q5)**: `PYTHONPATH=src python -m unittest discover -s tests`
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
**Depends on U5 and U7.**

Learnings-Researcher P2: `docs/compound/2026-08-21-ship-executable-set-must-wire-into-actual-loop-variable.md` — the 'defined but not wired' class. A registry whose nodes are never actually evaluated would present as passing. The zero-evaluation COUNT assertion (scenario 1) and U11's real-verdict end-to-end run are the two guards against it.

RK5: this suite is what makes the D-3 cycle-detection duplication safe — it tests the behavior independently of `topology.py`.
<!-- END:implementation-notes -->