---
artifact_type: task
created_at: 2026-07-29T13:46:34.9941485Z
dependencies:
    - 098.001-T
    - 098.002-T
    - 098.003-T
    - 098.004-T
    - 098.005-T
    - 098.006-T
    - 098.007-T
id: 098.008-T
labels:
    - workflow-policy
    - verify
    - tests
    - manifest
    - gate
parent_id: 098-F
priority: medium
references:
    - src/autoharness/verify_workspace.py
    - tests/test_verify_workspace.py
    - .autoharness/harness-manifest.yaml
    - .github/workflows/ci.yml
status: queued
title: Add ship_post_merge_compaction_gate verify assertion + tests + manifest + gates
updated_at: 2026-07-29T13:46:34.9941485Z
---



<!-- BEGIN:acceptance-criteria -->
- Add a `ship_post_merge_compaction_gate` entry to `FOUNDATION_ASSERTIONS` in `src/autoharness/verify_workspace.py` (path `.github/agents/_ship.agent.md`, `must_contain` compact-context closure markers, e.g. `compact-context` and `target: all`), modeled on `ship_index_sync_gate` / `ship_merge_confirmation_gate`. This assertion FAILS until 098.006-T fixes the installed Ship drift — intended coupling.
- Extend `tests/test_verify_workspace.py` to cover the new assertion (positive present-case + a missing-marker negative case), matching existing assertion-test patterns.
- Update `.autoharness/harness-manifest.yaml`: refresh checksums for every changed installed artifact (`.github/agents/_ship.agent.md`, `.github/agents/_orchestrator.agent.md`, `.github/instructions/harness-architecture.instructions.md`, `AGENTS.md`, and any other installed copy touched) and any assertion-text lines, using the repo's normalize-then-hash (LF, raw-bytes) method.
- GATE (canonical): `autoharness verify` passes; `PYTHONPATH=src python -m unittest discover -s tests` (CI ci.yml:89) is green. Root `pytest` is NONCANONICAL — do not use it as the gate.
- No unresolved `{{VARIABLE}}` in any installed output; template markdown/frontmatter/cross-references valid.
<!-- END:acceptance-criteria -->

<!-- BEGIN:estimate -->
~2h — CLI/test/manifest source; canonical unittest gate APPLICABLE here (this task changes Python source + tests).
<!-- END:estimate -->