---
artifact_type: feature
created_at: 2026-09-19T02:35:08.0730586Z
custom_fields:
    harness_status: pending
id: 180-F
labels:
    - foundation
    - review-authority
    - manifest
    - compatibility
    - precursor
priority: critical
status: queued
title: 'FOUNDATION: review-authority compatibility and atomic recording'
updated_at: 2026-09-19T02:35:08.0730586Z
---

FOUNDATION (P3). ABSORBS the scope previously carried by 179-S. Make the verdict manifest the single authority for latest attempt and verdict, compatibly with the records that already exist. Delivers: a VERSIONED, TOTAL manifest normalizer that accepts all three live shapes - Family A attempt-roster (six manifests, 21 keys), Family B cycle-record (one manifest, 17 keys, governing 175-S), and Family C pre-review (null latest_attempt, empty attempts) - and emits a canonical record that is OPEN at the boundary and CLOSED at the core; strict canonical validation downstream of normalization only; and an ATOMIC review-result recorder built on the P2 atomic writer so the immutable artifact, the roster entry, the top-level selection fields and the plan source_history can never be partially written. Migrates ALL FOUR review-verdict consumer surfaces in ONE activation commit: templates/skills/harvest, templates/skills/plan-review, and both installed mirrors. Harvest is the load-bearing consumer - it reads an inline decision: PASS marker from the plan body today and never opens the manifest. SELF-HOSTING: this unit is authored and reviewed under the CURRENT inline-marker system; its activation commit is what switches the workspace onto the manifest system, and the new gate never admits its own plan. MANIFEST PARITY IS ATOMIC (decision D11, plan revision 2). Two of the four migrated consumer surfaces - .github/skills/harvest/SKILL.md and .github/skills/plan-review/SKILL.md - are MANIFEST-TRACKED INSTALLED ARTIFACTS; the two templates are not, because .autoharness/harness-manifest.yaml tracks no template. 180.010-T therefore refreshes EXACTLY TWO manifest artifacts: checksums IN ITS SINGLE ACTIVATION COMMIT AND THE SAME ROLLBACK UNIT, rewriting each to the sha256 of the installed file AS WRITTEN BY THAT COMMIT, and then VERIFIES CHECKSUM PARITY by re-digesting both. THE COMMIT CONTAINS SIX FILES AND MIGRATES FOUR CONSUMERS: the refreshes are COMMIT MEMBERS, not consumer surfaces, so the consumer count stays FOUR, the mechanical consumer-inventory rule is unchanged, and no gate arity or fixture set moves. A commit that migrates either installed skill WITHOUT its manifest refresh leaves the manifest asserting a digest of a file that commit has already rewritten - a parity hole in the very unit making the manifest the single review authority - and is an IMMEDIATE REVERT rather than a fixup commit. This binds a FUTURE implementation commit and authorizes no staging-time edit to the live manifest; none has occurred. Source: C9CD24F3. Governing plan: docs/plans/2026-09-18-review-authority-foundation-plan.md (revision 2, disposition REMEDIATED-PENDING-REVIEW). Decision: docs/decisions/2026-09-18-shared-execution-architecture-and-portfolio-reslicing-decision.md (revision 4; D2, D3, D11, SM-2).