---
artifact_type: task
created_at: 2026-09-19T04:18:33.2683631Z
custom_fields:
    complexity: low
    size: XS
    size_ruleset_version: v1
    size_source: agent
dependencies:
    - 177.003-T
    - 177.007-T
id: 177.006-T
labels:
    - spike
    - gate
    - composed-state
    - coverage
parent_id: 177-F
priority: medium
status: queued
title: 'S2 T6: coverage and composed-state validation - every property has a determining task and captured evidence'
updated_at: 2026-09-20T05:32:55.762573Z
---

COVERAGE AND COMPOSED-STATE GATE (decision D6). Attempt 01 found that the plan's declared pass state was unreachable because a declared property had no determining task, and that the previous hardening pass never asked whether one existed. This task makes the coverage check STRUCTURAL rather than incidental, and executes the state transition mechanically so the gate is a check rather than a claim. FOUR CHECKS, IN ORDER. (1) COVERAGE: every property I1-I7 has an assigned DETERMINING task in the plan's Property coverage table, and that task is a determining task - 177.004-T, 177.005-T, 177.001-T or 177.002-T - NEVER the authoring task 177.003-T alone and never the cleanup-evidence task 177.007-T, which determines no property. A property whose only assignment is an authoring or evidence-emission task FAILS THIS CHECK OUTRIGHT, because such a task cannot produce an observation and an authoring-derived verdict rests on assertion, which R3 and H2 have already ruled insufficient. (2) EVIDENCE: resolve every property's row in the coverage ledger of docs/spikes/2026-09-18-conformance-isolation-findings.md to exactly one of three values. DETERMINED - a verdict is present AND the evidence shape named in the Property coverage table is present in the artifact. NOT DETERMINED - FLOOR INVOKED - the determining task reached its elapsed bound or was blocked AND the artifact records ALL THREE of what was attempted, what blocked it, and an explicit written statement that the property is UNPROVEN and that 181-S is held at the evidence-and-documentation floor for that property's scope. ABSENT - anything else, including a verdict supported only by a documentation citation and including a bare "not determined" carrying fewer than all three parts. THEN EMIT EXACTLY ONE OF FIVE COMPOSED STATES, and write a single verdict line to the findings artifact. PRECEDENCE MAKES THE RESOLUTION A TOTAL FUNCTION - evaluate in this order and emit the FIRST that matches: (1) ISOLATION_NOT_OBSERVED, (2) ISOLATION_UNDETERMINED, (3) ISOLATION_CLEANUP_FAILED, (4) ISOLATION_FLOOR_ONLY, (5) ISOLATION_CHARACTERIZED. ISOLATION_CHARACTERIZED, when every property is DETERMINED AND cleanup resolves CLEANUP_PROVEN - "COMPOSED_STATE: ISOLATION_CHARACTERIZED | determined=7 | floor_invoked=0 | absent=0 | cleanup=PROVEN | checked=<date>". THIS IS THE ONLY PASS STATE. ISOLATION_FLOOR_ONLY, when no property is ABSENT, at least one is NOT DETERMINED - FLOOR INVOKED, and cleanup resolves CLEANUP_PROVEN - "COMPOSED_STATE: ISOLATION_FLOOR_ONLY | determined=<n> | floor_invoked=<m> | absent=0 | floor_properties=I<x>,I<y> | cleanup=PROVEN | checked=<date>". THIS IS NOT A PASS: it records that the named properties are UNPROVEN, and it asserts NOTHING about the isolation. ISOLATION_CLEANUP_FAILED, when cleanup resolves CLEANUP_FAILED for any reason, WHATEVER THE LEDGER SAYS - "COMPOSED_STATE: ISOLATION_CLEANUP_FAILED | determined=<n> | floor_invoked=<m> | absent=0 | cleanup=<reason> | workflow_path=.github/workflows/spike-177-isolation-probe.yml | checked=<date>", where <reason> is exactly one of WORKFLOW_PRESENT_AT_TIP, WORKFLOW_PATH_RESIDUE, TIP_UNOBSERVABLE, EVIDENCE_MISSING, EVIDENCE_MALFORMED or EVIDENCE_INCONSISTENT. THIS IS NOT A PASS: it is the state that expresses EVIDENCE COMPLETE, SPIKE NOT CLOSED. ISOLATION_UNDETERMINED, when one or more properties is ABSENT - "COMPOSED_STATE: ISOLATION_UNDETERMINED | absent=<n> | properties=I<x>,I<y>". ISOLATION_NOT_OBSERVED - the findings artifact does not exist, or exists with no completed coverage ledger, no verdict line, or MORE THAN ONE COMPOSED_STATE: line; distinct from ISOLATION_UNDETERMINED and NEVER a pass. THE ARTIFACT CARRIES EXACTLY ONE COMPOSED_STATE: LINE AT ALL TIMES - a re-run REPLACES that line rather than appending beside it. SUCCESSOR ELIGIBILITY IS STATED, NOT INFERRED: under ISOLATION_CHARACTERIZED, 181-S may harvest every task the findings support; under ISOLATION_FLOOR_ONLY, 181-S may harvest ONLY the evidence-and-documentation floor tasks and NO task that assumes, relies on or asserts an unproven property; under ISOLATION_CLEANUP_FAILED, 181-S may harvest NOTHING - the spike is unclosed, and the remedy is inside this unit and cheap: remove the workflow from the branch tip if it is still tracked there, clear any exact-path staged, unstaged or untracked residue, have 177.007-T RE-EMIT a complete and consistent CLEANUP_ evidence block against the SAME REACHABLE REMOVAL COMMIT - no second removal or deletion commit is required and later unrelated commits do not prevent it - and re-run this task, which REPLACES the verdict line; unlike ISOLATION_UNDETERMINED this needs NO further determining run and NO waiver, because no property observation is missing; under ISOLATION_UNDETERMINED and ISOLATION_NOT_OBSERVED, 181-S may harvest NOTHING. Attempt 02 finding F2 (P2) found the previous revision asserting that ISOLATION_UNDETERMINED both blocked 181-S harvest and left the degradation floor standing, which cannot both be operative; the per-state eligibility rule above replaces that pair. FAIL CLOSED: any row not affirmatively DETERMINED or NOT DETERMINED - FLOOR INVOKED is ABSENT, any cleanup result not affirmatively CLEANUP_PROVEN is CLEANUP_FAILED, and any missing, unparseable or verdict-less artifact is ISOLATION_NOT_OBSERVED. Silence never produces eligibility, and NO state presents the isolation floor as PROVEN - 002-C stays blocked under all five. A verdict line whose counts disagree with the ledger is ITSELF A FAIL, and the counts must sum to 7. THIRD CHECK - THE I1 GATE AND ITS FORCED STATE (attempt 03 finding K1, P1). Read the observed I1_GATE token transcribed from docs/spikes/2026-09-18-conformance-isolation-i1-gate.md. THE I1 GATE IS CLOSED unless that file existed, was readable, carried EXACTLY ONE I1_GATE: line, and that line's token was ACHIEVABLE; NOT_ACHIEVABLE, FLOOR_INVOKED, an unrecognised token, a missing or unreadable file, no I1_GATE: line and MORE THAN ONE I1_GATE: line are all CLOSED. IF THE GATE WAS CLOSED, THE COMPOSED STATE MUST BE ISOLATION_FLOOR_ONLY - or ISOLATION_CLEANUP_FAILED if cleanup also fails - AND MUST NEVER BE ISOLATION_CHARACTERIZED. This is structural rather than a rule to remember: a CLOSED gate leaves at least one of I7, I4, I5, I6 or I2 recorded NOT DETERMINED - FLOOR INVOKED naming I1, and ISOLATION_CHARACTERIZED requires ALL SEVEN properties DETERMINED, so the pass state is unreachable. I3 is NOT gated and may still be DETERMINED. IF ANY GATED PROPERTY CARRIES A DETERMINED VERDICT WHILE THE GATE WAS CLOSED, THAT IS ITSELF A FAIL - it means a gated task ran its untrusted acquisition or containment probe against a job environment whose credential absence was unverified or affirmatively falsified, which H7, H9, H12, H13, R8 and R10 all forbid. Record it as a defect and emit no pass. IF A GATED TASK RECORDED FEWER THAN THE THREE REQUIRED FLOOR-INVOCATION PARTS, its row is ABSENT and the unit composes to ISOLATION_UNDETERMINED, which blocks harvest outright. Do NOT treat the 177.004-T blocks edge as evidence that the gate was open: a blocks edge clears on PREDECESSOR COMPLETION, and 177.004-T completes on all three I1 outcomes. THE VERDICT PREDICATE IS THE TOKEN, NOT THE EDGE. FOURTH CHECK - CLEANUP AND BRANCH STATE, AN EXECUTABLE PREDICATE (attempt 03 finding K2, P2; made executable under attempt 04 finding K3, P2; made FIRST-PASS SATISFIABLE under attempt 05 finding L1, P2; made DURABLY RE-EMITTABLE and EXACT-PATH-SCOPED under attempt 06 findings M1 and M2, both P2). THE BLOCK THIS CHECK READS IS WRITTEN BY 177.007-T, NOT BY 177.003-T. 177.003-T removes the workflow and commits - THE REMOVAL COMMIT - and writes no CLEANUP_ line; 177.007-T then runs against that already-existing commit, observes it, and lands the eight-line block in its own separate EVIDENCE COMMIT. Through plan revision 5 the block was mandated to be written in the removal commit itself while two of its fields named values that only that commit's creation defines, so no conformant first emission existed and a spurious CLEANUP_FAILED cycle was forced. NO FIELD OF THE BLOCK EVER NAMES THE COMMIT THAT RECORDS IT, and THIS TASK'S OWN INDEPENDENT RE-OBSERVATION OF THE CURRENT BRANCH STATE IS THE FINAL BRANCH-STATE AUTHORITY. Resolve cleanup to exactly one of CLEANUP_PROVEN or CLEANUP_FAILED by evaluating six checks, with C6 FIRST because it is the only limb that does not depend on the artifact's own word. THE EXACT-PATH PROBES PP1-PP3, USED BY C6 AND BY 177.007-T ALIKE, ARE FIXED-ARGV, SCOPED TO ONE PATH, AND REJECT AMBIGUOUS OUTPUT RATHER THAN INTERPRETING IT (attempt 06 finding M2, P2). PP1: "git ls-tree --full-tree -r --name-only HEAD -- .github/workflows/spike-177-isolation-probe.yml" - EMPTY output means NOT TRACKED at the current tip; exactly one line byte-identical to that path means TRACKED AT THE TIP; a non-zero exit, more than one line, or a line that is not byte-identical is AMBIGUOUS. PP2: "git status --porcelain=v1 --untracked-files=all -- .github/workflows/spike-177-isolation-probe.yml" - EMPTY output means NO staged, unstaged or untracked residue at that exact path; one or more lines whose path field is byte-identical to that path means RESIDUE PRESENT; a non-zero exit, any reported path that is not byte-identical, or any directory form ending in / is AMBIGUOUS. --untracked-files=all IS MANDATORY so an untracked copy inside an otherwise-untracked directory is reported as the file rather than collapsed into a directory entry. PP3: "git ls-files --cached --error-unmatch -- .github/workflows/spike-177-isolation-probe.yml" - a NON-ZERO exit means the path is not in the index; exit 0 means INDEX RESIDUE PRESENT. C6 CURRENT-STATE RE-OBSERVATION - observe the CURRENT branch state DIRECTLY, BEFORE STAGING THIS TASK'S OWN VERDICT-LINE EDIT, and find .github/workflows/spike-177-isolation-probe.yml neither tracked at the tip nor present as exact-path residue: PP1 reports the path ABSENT, and PP3 then PP2 each report NO RESIDUE. DO NOT take this from the recorded CLEANUP_TIP_OBSERVATION line, and DO NOT take it from CLEANUP_REMOVED_COMMIT. THIS CHECK IS SCOPED TO THAT ONE PATH: NO OTHER TRACKED FILE, UNTRACKED FILE, AGENT CHECKPOINT, SESSION MEMORY NOTE OR PIECE OF OPERATOR WORK IS OBSERVED BY IT AND NONE CAN FAIL IT. WHOLE-TREE CLEANLINESS IS NEVER OBSERVED, REQUIRED OR RECORDED ANYWHERE IN THIS UNIT. C1 PATH - exactly one CLEANUP_WORKFLOW_PATH: line, byte-identical to .github/workflows/spike-177-isolation-probe.yml. C2 CREATION - exactly one CLEANUP_CREATED_COMMIT: line carrying a 40-character lowercase hex SHA naming a commit that ADDS exactly that path and adds or modifies no other workflow file. C3 DISPATCH - exactly four CLEANUP_DISPATCH: lines whose input fields are exactly the set i1-credentials, i7-acquisition, i2-i3-egress and i4-i6-containment, with no duplicate, no omission and no unrecognised input, each carrying a non-empty run_id and a non-empty url. C4 REMOVAL - exactly one CLEANUP_REMOVED_COMMIT: line carrying a 40-character lowercase hex SHA that satisfies RC1-RC7 WHEN THIS TASK RE-DERIVES THEM INDEPENDENTLY. RC1: "git rev-list --full-history --max-count=1 HEAD -- .github/workflows/spike-177-isolation-probe.yml" must yield EXACTLY ONE 40-character lowercase hex SHA, and THE RECORDED SHA MUST EQUAL IT; a mismatch is EVIDENCE_INCONSISTENT. --full-history IS MANDATORY - default history simplification prunes the deletion commit once the path is absent at HEAD and the command returns empty. RC2: "git diff-tree --no-commit-id --name-status -r <sha> -- .github/workflows/spike-177-isolation-probe.yml" must be exactly one D line for that exact path. RC3: "git diff-tree --no-commit-id --name-status -r <sha> -- .github/workflows/" must be exactly that same one line - NO OTHER WORKFLOW FILE TOUCHED. RC4: "git ls-tree --full-tree -r --name-only <sha> -- .github/workflows/spike-177-isolation-probe.yml" must be EMPTY. RC5: "git merge-base --is-ancestor <sha> HEAD" must exit 0 - the removal commit is REACHABLE FROM THE CURRENT BRANCH TIP. RC6: "git merge-base --is-ancestor <created-sha> <sha>" must exit 0 with <created-sha> not equal to <sha> - the C2 creation commit is IN THE REMOVAL COMMIT'S ANCESTRY. RC7: no field may name a commit that did not already exist when 177.007-T derived it. IT NAMES 177.003-T'S REMOVAL COMMIT AND NEVER THE EVIDENCE COMMIT THAT RECORDS THE BLOCK, AND IT IS NEVER REQUIRED TO EQUAL THE CURRENT TIP - later unrelated forward commits DO NOT invalidate a removal that already happened, and no second removal commit is ever required to re-prove it (attempt 06 finding M1, P2). C5 RECORDED OBSERVATION - exactly one CLEANUP_TIP_OBSERVATION: line whose first field is the literal WORKFLOW_ABSENT, carrying observed_tip=<40-character lowercase hex>, path_clean_at_observation=yes and observed_at=<date>; observed_tip must be reachable from the current branch tip, must be the C4 removal commit or a descendant of it, and the exact CLEANUP_WORKFLOW_PATH must be ABSENT from observed_tip's tree. observed_tip is the tip 177.007-T observed before it wrote, which is the removal commit OR A DESCENDANT of it and which is the PARENT of the evidence commit; it is NEVER required to equal the current tip, because C6 - not this line - is the current-state authority. path_clean_at_observation asserts EXACT-PATH cleanliness only and is NOT a whole-tree claim. CLEANUP_PROVEN REQUIRES ALL SIX. Otherwise CLEANUP_FAILED, carrying the reason token of the FIRST check that failed in the order C6, C1, C2, C3, C4, C5. THE SIX REASON TOKENS REPORT DIFFERENT FACTS AND ARE NEVER INTERCHANGEABLE: WORKFLOW_PRESENT_AT_TIP (C6, PP1 reports the exact path TRACKED AT THE CURRENT BRANCH TIP - this token means that and NOTHING ELSE and is NEVER raised for a working-tree or index condition), WORKFLOW_PATH_RESIDUE (C6, the path is ABSENT at the tip but PP3 or PP2 reports a STAGED, UNSTAGED OR UNTRACKED COPY AT THAT EXACT PATH - distinct from WORKFLOW_PRESENT_AT_TIP so the diagnostic never claims the workflow is committed when only exact-path residue exists, and still fail-closed, still non-pass, still harvest-blocking), TIP_UNOBSERVABLE (C6, the current tip cannot be resolved at all, or any of PP1, PP2, PP3 returns output outside its accepted shape - AMBIGUITY IS NEVER RESOLVED IN THE PASSING DIRECTION), EVIDENCE_MISSING (a required line absent or a required dispatch input omitted), EVIDENCE_MALFORMED (a required line present but not in the declared form - wrong field count, non-hex SHA, empty run_id or url, unrecognised token, or more than one of a single-instance line), EVIDENCE_INCONSISTENT (lines well-formed but disagreeing with the repository or each other - a creation commit that does not add the path, a removal commit that does not delete it, that touches another workflow file, that does not have the creation commit in its ancestry, that is not reachable from the current tip, or that is not the SHA this task's own RC1 re-derivation yields, an observed_tip that is unreachable, is not the removal commit or a descendant of it, or still carries the path, or a duplicate dispatch input). WITHIN C6 EVALUATE TRACKED-AT-TIP FIRST, THEN RESIDUE, and raise TIP_UNOBSERVABLE at the point any probe returns an unaccepted shape. Attempt 05 finding L2 (P3) recorded that a single token was raised on two different facts; this split is made for consistency with the M2 remediation and NO CLAIM OF L2 CLOSURE IS MADE OR IMPLIED - that judgement belongs to an independent reviewer. ABSENCE OF THE WHOLE BLOCK IS CLEANUP_FAILED | cleanup=EVIDENCE_MISSING AND NEVER ISOLATION_NOT_OBSERVED - the ledger may be complete while the cleanup evidence is simply missing, and those are different facts. THIS CHECK IS LOCAL, OFFLINE AND CREDENTIAL-FREE: every limb reads the local repository, performs no network request and requires no credential, so it neither weakens nor interacts with the I1 gate or the acquisition-then-no-network model. ELAPSED BOUND 30 min. Blocked by 177.003-T and by 177.007-T; runs last in the unit. Source 7F9CB5E9. Plan docs/plans/2026-09-18-conformance-isolation-spike-plan.md (plan_id conformance-isolation-spike, revision 7, awaiting independent attempt 07). Verdict manifest docs/reviews/2026-09-18-conformance-isolation-spike-plan-review.md.