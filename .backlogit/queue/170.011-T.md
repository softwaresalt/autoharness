---
artifact_type: task
created_at: 2026-09-18T07:51:34.7409665Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 170.011-T
labels:
    - test
    - tdd
    - red-phase
    - branch
    - pipeline-topology
parent_id: 170-F
priority: high
status: queued
title: 'Branch T0 (RED): import-safe validator, precedence and gate-JSON contract tests observed failing'
updated_at: 2026-09-19T00:39:25.3523533Z
---

TDD RED PHASE, SINGLE ENTRY POINT of 178-S. Author the validator, precedence and gate-JSON contract tests and OBSERVE THEM FAILING before any implementation exists. COVERAGE: branch short-name validation rules V1-V11; the standalone option-like invariant; the `git check-ref-format --branch` SUPERSET corpus with divergence set D enumerated as exactly TWO shapes (bare `@` and resolvable `@{-N}`), the `@{-N}` arm confined to a hermetic fixture repository; the TWO-RUNG precedence (explicit_contract then title_alias) with the no-fallback-on-malformed asymmetry - a malformed explicit value FAILS CLOSED and never silently falls through to the title alias; and the gate-JSON shape carrying resolution_source, selected_branch and ordered expected_branches on BOTH the pass and the block paths.
IMPORT SAFETY (BINDING): this module must import cleanly under unittest.defaultTestLoader with ZERO loader.errors and ZERO _FailedTest placeholders, asserted directly. A module that raises at IMPORT time yields a _FailedTest placeholder classified P004_MISSING_OBSERVATION - a MISSING OBSERVATION, NOT valid red - so a RED task whose tests cannot be collected FAILS ITS OWN GATE. Every not-yet-existing boundary is therefore imported INSIDE the test body through a small _load_boundary() helper, never at module top level, and each declared RED test INDIVIDUALLY executes and fails or errors on its own, producing its OWN detail_text containing its OWN declared marker.
Blocked by NOTHING. Does NOT depend on 170.010-T or any other implementation task: red precedes implementation, and reversing that asymmetry would make this a test-after task wearing a red-phase label. Scope: tests/. Stash 86498B64 + 14F4D6F3. Plan docs/plans/2026-09-17-workspace-authoritative-branch-resolution-plan.md (plan_id workspace-authoritative-branch-resolution, plan_role active, revision 7). Verdict manifest docs/reviews/2026-09-17-workspace-authoritative-branch-resolution-plan-review.md (latest_attempt 07, verdict REMEDIATED-PENDING-REVIEW at plan revision 7, awaiting independent attempt 08).