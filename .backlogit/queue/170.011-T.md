---
artifact_type: task
created_at: 2026-09-18T07:51:34.7409665Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 170.011-T
labels:
    - test
    - tdd
    - red-phase
    - branch
    - pipeline-topology
parent_id: 170-F
priority: high
status: queued
title: 'Branch T0 (RED): author the validator, precedence and gate-JSON contract tests and observe them failing'
updated_at: 2026-09-18T17:15:27.3889243Z
---

TDD RED PHASE. Author the branch-resolution contract tests BEFORE any implementation exists, and OBSERVE THEM RED. Covers: (a) validator rules V1-V11 as a table-driven accept/reject corpus, with V2/V3 (leading hyphen; all-hyphen) enumerated explicitly so --force, -D, --all, -x and -- cannot validate; (b) the two-rung precedence ladder, explicit_contract then title_alias, with NO workspace_convention rung existing in any form; (c) the fail-closed asymmetry: a present-but-malformed explicit value emits IMPLEMENTATION_BRANCH_MALFORMED and NEVER falls back to the title-alias rung, asserted by proving the alias path was not consulted; (d) the resolver-exit invariant that no selected_branch begins with '-' on any rung from any source; (e) the gate JSON fields resolution_source, selected_branch and ordered expected_branches on both the pass and the block path; (f) the no-field compatibility invariant. At this task every case is red because the validator and resolver do not exist. Tests MUST import the real boundary from src/autoharness/gates/topology.py and MUST NOT re-implement it. Scope: tests/test_gates_topology.py + tests/test_gate_pipeline_topology_cli.py. No dependencies: this is the entry point of 178-S. Stash 86498B64 MERGED WITH 14F4D6F3 (both source IDs preserved). Plan docs/plans/2026-09-17-workspace-authoritative-branch-resolution-plan.md (revision 5). Verdict manifest docs/reviews/2026-09-17-workspace-authoritative-branch-resolution-plan-review.md (attempt 05, REMEDIATED-PENDING-REVIEW at plan revision 5 — independent review cycle 3, the final authorized review-fix cycle, returned BLOCKED at plan revision 4; Stage remediation cycle 3 closed every finding and raised the plan to revision 5. Stage does not review its own remediation, so no PASS is asserted at revision 5 and the next reviewer pass is attempt 06). REVISION 5 CORRECTION (attempt-05 finding): divergence assertions must use the corrected TWO-SHAPE D = { bare @ } union { resolvable @{-N}, N >= 1 }; the wider @{...} family is a SHARED rejection (exit 128, agreeing with V10) and must be asserted as such, not as divergence. Because the @{-N} arm resolves against the REFLOG, it MUST be asserted inside a HERMETIC FIXTURE REPOSITORY whose scripted checkout history creates the known-resolvable N values. Asserting @{-N} against the ambient repository reflog is PROHIBITED: the result would depend on the checkout history of whoever runs the suite.