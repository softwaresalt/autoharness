---
artifact_type: task
created_at: 2026-07-29T10:14:24.9191942Z
dependencies:
    - 096.002-T
id: 096.003-T
parent_id: 096-F
priority: high
references:
    - tests/test_deploy_harness_scripts.py
status: queued
title: Add deploy-wrapper opt-in tests (omitted vs explicit all, per preset)
updated_at: 2026-07-29T10:24:36.0966171Z
---

<!-- BEGIN:description -->
Add deploy-wrapper tests that lock in the opt-in resolution AND the uniform precedence contract from task 096.002-T. Extend tests/test_deploy_harness_scripts.py (existing rendering + parity + behavior suite).

Cover these cases for BOTH scripts (ps1 + sh) — or assert on the shared logic so both mirrors are exercised. The cases are organized by the precedence contract: EXPLICIT input is honored on every preset (incl. starter); OMITTED input resolves to preset default_in_preset members.

EXPLICIT input honored regardless of preset (guards against re-introducing 082-S FU-1, where starter discarded explicit selections):
- starter + explicit `all` -> every registry pack selected, agent-intercom INCLUDED.
- starter + explicit subset (e.g. two known packs) -> exactly that subset selected; starter does NOT drop them.
- full + explicit `all` -> every registry pack selected, agent-intercom INCLUDED.

OMITTED input resolves to preset defaults:
- omitted input + preset `full` -> exactly the registry packs whose default_in_preset includes "full"; agent-intercom NOT selected.
- omitted input + preset `standard` -> the standard default_in_preset members; agent-intercom NOT selected.
- omitted input + preset `starter` -> no packs selected (esp. agent-intercom absent).

Structural:
- Enumerator parses default_in_preset correctly from the plain-YAML registry.
- Keep the existing script<->template parity assertion green (the 4-file change must not break rendering parity).
- Reference the installer-contract parity: wrapper precedence must match `install-harness` semantics (.github/skills/install-harness/SKILL.md:551 — explicit capability_packs honored regardless of preset; omitted resolves to preset defaults).

## Acceptance criteria
- New tests fail against the pre-fix wrappers and pass against the 096.002-T fix.
- The three starter cases above are present (explicit all, explicit subset, omitted) and assert the correct honored-vs-empty behavior; the explicit-subset case would FAIL on the old starter-discards-explicit defect.
- The full/standard omitted-excludes-agent-intercom and explicit-all-includes cases are present.
- tests/test_deploy_harness_scripts.py passes in full, including parity/rendering.
<!-- END:description -->