---
artifact_type: feature
created_at: 2026-08-21T00:43:15.3968356Z
custom_fields:
    harness_status: pending
id: 136-F
labels:
    - docs
    - toolchain
    - bug
    - P-021
references:
    - docs/decisions/2026-08-20-docline-lint-hard-abort-malformed-frontmatter-deliberation.md
    - docs/plans/2026-08-20-docline-lint-restoration-plan.md
status: queued
title: 'Restore workspace-wide docline lint: docs/ frontmatter sweep + regression guard'
updated_at: 2026-08-21T05:04:51.0484493Z
---

Covering feature for stash 395EBE60 (P-021 C2 DEFERRED SCOPE EXPANSION, medium, bug).

docs/plans/2026-08-02-structural-navigation-benchmark-suite-plan.md line 12 carries an unquoted YAML scalar containing ': ' (blast_radius: elevated (result-integrity + multi-family: eval code / tests / docs / fixtures)). mdfront.Decode fails, and because `backlogit docs lint` aborts on the first decode error the ENTIRE repo-wide lint emits no report and exits 1 — one malformed file masks docline conformance across all of docs/.

SCOPE CHANGE (2026-08-20, Stage bounded correction)
The single-file repair originally held here as 136.001-T has been RELOCATED, not dropped. It is SUPERSEDED BY 138.001-T (feature 138-F, shipment 146-S), which repairs it atomically together with the stale P-021 test fixture resolution.

WHY: a circular mandatory-gate dependency. Ship evaluates ALL mandatory gates before completing EVERY task. 136.001-T owned the only repair for Ship Gate 1 (frontmatter validity) but sat in 144-S, which is blocked by 146-S; 146-S repaired only the pytest blocker, so its own first task would still have completed with Gate 1 red, and running 144-S first would still have hit the P-021 setUpClass collapse. There was no executable first task anywhere in the chain. Merging both repairs into one gate-atomic first task (138.001-T) dissolves the cycle.

136-F THEREFORE NOW OWNS: the repo-wide docs/ sweep (136.002-T) and the frontmatter regression guard (136.003-T). Both remain in 144-S, in that order, and 144-S continues to depend on 146-S (blocks) — which is what sequences the sweep after the gate-atomic repair. No scope is lost: the known scalar fix is preserved verbatim as Scope A of 138.001-T.

Scope: sweep docs/ for the same hazard, and add a regression guard so the failure class cannot recur. (The local defect fix is now in 138.001-T.)

Explicitly OUT of scope (width isolation, P-021 C1): whether the linter SHOULD hard-abort versus report-and-continue is an external backlogit product decision, not an autoharness one. It is recorded in the deliberation and captured as a separate [EXTERNAL / backlogit-owned] stash entry, following the precedent of 84D8E6AB and 3C7AAC71. Also out of scope: remediating any docline findings that the restored lint newly surfaces — those are captured as their own deferred entries.

Stage verification: root cause confirmed by direct file inspection (re-confirmed 2026-08-20: line 12 still unquoted). The lint itself was deliberately NOT run — Stage's Role Boundary forbids running linters; empirical before/after lint evidence is delegated to Ship.

Sequencing: 146-S (gate-atomic baseline repair) -> 144-S (this feature) -> 145-S (harness consistency). Predecessor of the harness-consistency shipment, so that shipment's documentation can be validated workspace-wide.

Superseded: 136.001-T -> 138.001-T (archived, not deleted; removed from 144-S membership).