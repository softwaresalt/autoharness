---
artifact_type: task
created_at: 2026-09-12T22:51:56.7917488Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 165.005-T
    - 165.006-T
id: 165.009-T
labels:
    - documentation
    - migration
    - contract-hygiene
parent_id: 165-F
priority: medium
status: queued
title: Gate documentation and numeric-sequencing migration guide
updated_at: 2026-09-12T22:51:56.7917488Z
---

Document the new gate contract and provide a migration path for workspaces that relied on unstated numeric sequencing (bug report §7 acceptance criterion 6 — contract-change hygiene).

DELIVERABLES:
1. Gate documentation in `docs/` describing: explicit-DAG predecessor derivation; `predecessor_source` provenance values; the `UNSEQUENCED_SHIPMENT` signal; and the `gates.pipeline_topology.unsequenced_shipment` switch (`warn` default | `block` opt-in).
2. MIGRATION GUIDE covering HARDENING H5 residual risk: in a workspace with NO explicit `blocks` edges anywhere, ALL shipments become claimable under default `warn` mode. This is the intended contract change, is signalled by provenance, and operators wanting the legacy posture must set `block`. State this plainly — it is the single most consequential behavioural change.
3. Document the REMOVED safety behaviour (numeric adjacency) and why it was retired, citing the three prior defect cycles in docs/compound/.
4. Rollback posture (H4): `block` restores legacy behaviour without a code revert; provenance fields are additive and safe for older consumers to ignore.
5. Cross-reference the deliberation, plan, and the historical bug report. The bug report remains UNMODIFIED.

SIZE: S (~1.5h). COMPLEXITY: low (documentation over decided, already-reviewed content).
Plan §T9. Depends on T5 (165.005-T) and T6 (165.006-T).