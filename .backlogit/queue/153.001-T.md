---
artifact_type: task
created_at: 2026-08-31T22:15:33.6931106Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 153.004-T
id: 153.001-T
parent_id: 153-F
priority: medium
status: queued
title: Enforce workspace-root containment and symlink-escape prevention in both acquire scripts
updated_at: 2026-09-03T09:11:43.7106572Z
---

Covers findings 1, 2, 3. acquire_lock.ps1 has no containment (Resolve-Path accepts absolute paths and ../ traversal); acquire_lock.sh L22 resolves symlinks via cd/pwd -P but never verifies containment; both resolve THROUGH links before computing the lock path, so a symlinked directory redirects lock creation outside the workspace (Constitution III and IV). DECISION (i): explicit workspace-root parameter, defaulting to git rev-parse --show-toplevel, with the resolved root itself validated. PLAN REVIEW FINDING 2 (P1): when the explicit parameter is absent AND the git-derived root is not an ancestor-or-equal of the script's own installed location, FAIL CLOSED and demand the explicit parameter rather than guessing - otherwise a nested checkout silently resolves to a PARENT repository and widens containment. BINDING H2: enforce containment only after FULL symlink/junction resolution of BOTH the candidate path and the root, comparing fully-resolved real paths. BINDING H4: prefix-string containment is forbidden - /repo must not be treated as containing /repo-evil; use path-segment comparison. H5: PowerShell and POSIX variants implement identical semantics. TEMPLATE-FIRST (H1): edit templates/skills/file-lock/scripts/ only. Do not redesign the FileMode::CreateNew acquire race handling - it is correct. SEQUENCING: BLOCKED BY 153.004-T (binding H9 - the two-platform behaviour matrix supplies this task's test vectors). This task BLOCKS 153.002-T, because 153.002-T also edits the ACQUIRE scripts (its O2 token is generated at acquire time) and the two would otherwise collide on the same files. SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size M, complexity high. The complexity:high axis is satisfied not by a further split - binding H3 forbids shipping containment without ownership - but by the explicit de-risking prerequisite 153.004-T, per the two-axis gate. SAFETY MODE (added in review-fix cycle 2; SHIP-3 plan H8 declared it but cycle 1 never propagated it into the executable task, and review flagged this task as high/production-impact): careful + freeze-scope bounded to templates/skills/file-lock/scripts/. This task rewrites PATH-RESOLUTION AND CONTAINMENT LOGIC in security-sensitive shipped scripts, where an over-broad edit is the dominant risk and a wrong containment fix is worse than none. The installed scripts/ copies are re-copied by 153.003-T, never hand-edited here; no skill, instruction, manifest, or test file is in scope for this task. Source: 74C62374 findings 1-3. Plan: docs/plans/2026-08-31-ship3-file-lock-script-security-hardening-plan.md