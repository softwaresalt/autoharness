---
artifact_type: task
created_at: 2026-09-01T00:00:26.1022982Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: autoharness-stage-2h-v1
    size_source: agent
dependencies:
    - 158.001-T
id: 158.003-T
parent_id: 158-F
priority: medium
status: queued
title: Implement the shipment-assembly size-composition budget check and turn the assembly test green
updated_at: 2026-09-01T00:00:26.1022982Z
---

NEW in review-fix cycle 2 - the POST-IMPLEMENTATION half SPLIT out of 158.002-T to repair an impossible dependency model. Executes LAST in SHIP-8: 158.002-T (red tests) -> 158.001-T (harvest-gate implementation) -> THIS TASK (assembly implementation + green). DEPENDS ON 158.001-T via a task-level blocks edge, so it cannot begin before the harvest gate exists. DELIVERABLE: at shipment-assembly time, read the shipment's size_composition rollup BACK FROM THE TOOL after assembly and FAIL the assembly when unsized > 0 or when the composition exceeds the declared budget. This is the genuine MACHINE gate of this shipment - it reads state back from the tool and no amount of agent goodwill can fake it. PLAN REVIEW FINDING 3 (P1, honestly bounded): label the overall mechanisation PARTIAL rather than overclaiming - the harvest-time gate delivered by 158.001-T is prose plus a regression test asserting documented behaviour; full mechanisation of harvest-time validation is portfolio unit S2's ART-03 and duplicating it here would violate finding 2. BINDING H4 / NON-GOAL N6 - MANDATORY ACCEPTANCE (measured in cycle 1, re-affirmed in cycle 2): the budget check MUST read size_composition.unsized AND size_composition.histogram, which are populated and trustworthy. It MUST NOT gate on the SHIPMENT-LEVEL size_composition.ruleset_version, which is OBSERVED NULL on every shipment measured in this run's own portfolio even though every member task carries size_ruleset_version: autoharness-stage-2h-v1; gating on the observed-null shipment-level field would fail every shipment. Ship a POSITIVE test asserting that a shipment with unsized == 0 and a NULL shipment-level ruleset_version PASSES the budget check. The rollup-provenance gap is deferred scope DSE-S8-1 and is NOT fixed here - N1 forbids schema change. GREEN EVIDENCE: 158.002-T's test T2 (assemble a shipment with unsized > 0 must fail) is observed RED there and MUST be observed GREEN here; T1 and T3 are already green from 158.001-T and must remain green - re-run and record all three. FINDING 2 (preserved): SHIP-8 is the PRODUCER and S2 is the DETECTOR of the same invariant; this task adopts S2 constraints verbatim - enforce-on-new-only, no auto-split, no schema change - and records that S2 is the authority if they ever diverge. SEQUENCING NOTE, TASK-LEVEL vs SHIPMENT-LEVEL EDGE (corrected in cycle 2): SHIP-8's dependence on SHIP-7 having installed features.sizing is a SHIPMENT-level blocks edge (166-S blocked by 165-S), NOT a task-level dependency; no 158-* task declares a dependency on any 157-* task. Both edges are real; they sit at different levels and must not be conflated. NON-GOALS: no backlogit schema change and no new sizing field (N1); no auto-splitting - the gate halts and reports (N4); no threshold derivation (N2, spike 6A2D62DD stays active and unconsumed); no retroactive sweep of the existing corpus (H1, zero day-one blast radius). ACCEPTANCE: budget check implemented and reading unsized + histogram only; observed-null shipment-level ruleset_version explicitly NOT gated on and covered by a positive test; T2 turns green; T1/T3 remain green; PYTHONPATH=src python -m unittest discover -s tests green; verify-harness passes. SIZE/COMPLEXITY (two-axis, ruleset autoharness-stage-2h-v1): size M, complexity medium. SAFETY MODE: careful (SHIP-8 plan H5). Source: 2E67938C. Plan: docs/plans/2026-08-31-ship8-stage-size-complexity-enforcement-plan.md