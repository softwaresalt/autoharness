---
artifact_type: task
created_at: 2026-08-21T06:40:11.051275Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 139.001-T
labels:
    - bug
    - agents
    - contract
    - templates
parent_id: 139-F
priority: high
references:
    - docs/plans/2026-08-20-ship-pre-archived-manifest-member-execution-plan.md
    - docs/plans/2026-08-20-ship-pre-archived-manifest-member-execution-hardening.md
status: queued
title: Derive Ship's executable task set from live task records and exclude pre-archived manifest members (both agent files + atomic harness-manifest checksum)
updated_at: 2026-08-21T06:42:09.4250453Z
---

Write the executable-task-set derivation contract into BOTH Ship agent files and refresh the installed artifact's harness-manifest checksum in the SAME change.

FILES (exactly three):
1. `.github/agents/_ship.agent.md`
2. `templates/agents/_ship.agent.md.tmpl`
3. `.autoharness/harness-manifest.yaml`

CONTRACT TEXT (identical semantics in both agent files; place by SEMANTIC ANCHOR, never by line number - the two files diverge structurally, installed is ~798 lines with the loop at "Step 2: Task Execution Loop", template is ~1073 lines with a Step 3 ready-queue construction and the loop at "Step 4: Execute Task Loop"; do NOT re-render either file):

C1. The shipment manifest (`custom_fields.items`) is the CLOSURE MEMBERSHIP record. It is never the executable task set and is never mutated to make execution proceed.
C2. Before the loop, derive the EXECUTABLE TASK SET: first filter the manifest to task artifacts (IDs ending `-T`; the covering feature is resolved through `parent_id` and is never executed - 097-S task-only-manifest precedent), THEN read each task record's status. Artifact-type filtering precedes any status read.
C3. Exhaustive, positive status rule: KEEP `queued` and `active`; SKIP-AND-REPORT `archived` as `pre_archived_skipped` (the `pre-archived` classification already defined by `shipment-reconcile`); REPORT an already-`done` member separately as `already_done`; ANY OTHER, MISSING, OR UNREADABLE status is a FAIL-CLOSED HALT, never a skip. `already_done` and `pre_archived_skipped` are distinct reported outcomes - a `done` member must never be laundered as a tolerated pre-archived skip.
C4. A `pre-archived` member is EXPECTED AND TOLERATED, not an error. It must not halt the run. Never claim it, never move it to active, never unarchive it, never remove it from the manifest.
C5. The Step 0.5 item 1a queued-with-active-work early-warning is UNCHANGED and runs strictly BEFORE this derivation, exactly where it runs today. The derivation is a work-SELECTION step, never an integrity-guard step; it never suppresses, replaces, softens, or pre-empts item 1a's `SHIPMENT_STATE_INCONSISTENT` halt.
C6. If the derived executable set is EMPTY while the manifest is non-empty: halt and report. Do NOT advance to build or PR. Do NOT trigger any closure path. Operator disposition only.
C7. INSTALLED FILE ONLY: add to Step 0.5 the `shipment-reconcile` `mode: pre` intake reconciliation reference the template already carries at its Step 0.5 item 6, adapted with this repository's self-hosting note - the skill is read from `templates/skills/shipment-reconcile/SKILL.md.tmpl` because `.github/skills/shipment-reconcile/SKILL.md` is not installed here. Leave the template's existing item 6 as-is.

Also reword the installed loop header so it iterates the DERIVED EXECUTABLE TASK SET rather than "each task in the shipment/feature".

TEMPLATE VARIABLE HYGIENE (hardening H6 / amendment A2): the template defines exactly `{{STATUS_QUEUED}}`, `{{STATUS_ACTIVE}}`, `{{STATUS_DONE}}`. There is NO `{{STATUS_ARCHIVED}}`. Express the archived state through the `pre-archived` classification vocabulary already defined by `shipment-reconcile` (record archived / archive file present). Introduce NO new variable. The template must contain zero unresolved placeholders after the edit.

CHECKSUM (hardening H2, atomic): refresh `.autoharness/harness-manifest.yaml`'s `checksum` for the `.github/agents/_ship.agent.md` artifact entry (currently `9535bb87f55748d1457429765e8c6f2e3e918137c36003de1bf3af95801ec57d`) computed from the LF-normalized COMMITTED GIT BLOB via `git cat-file -p :<path>` (staged) or `HEAD:<path>` (post-commit). NEVER `Get-FileHash`/`sha256sum` a raw Windows working-tree read - see the recorded CRLF/LF gotcha. APPEND a provenance sentence to that artifact's existing `note` naming this shipment and task; do not replace the note.

ATOMICITY: all three files move together. The workspace is never left with the mirror edited but its checksum stale, nor with one agent file declaring the contract and the other not.

MUST-STAY-GREEN (review P2-2): the insertion must not relocate, reword, or break any anchor phrase asserted by `tests/test_ship_safe_close_pointer.py`, `tests/test_ship_claim_integrity_guards.py`, or `tests/test_pipeline_topology_gate_agent_wiring.py`.

ACCEPTANCE CRITERIA:
- Both agent files state C1-C6 in their own structure and vocabulary.
- Neither file retains an unconditional "iterate every manifest entry and move it to active" formulation.
- Installed Step 0.5 gains C7 with the self-hosting note.
- Artifact-type filtering is stated to precede status reads.
- The exhaustive status rule and the distinct `already_done` / `pre_archived_skipped` reporting are stated.
- The unchanged-item-1a ordering is stated.
- No new `{{VARIABLE}}`; zero unresolved placeholders in the template.
- Manifest checksum matches the LF-normalized committed blob; note appended.
- No shipment manifest, backlog record, or archived task touched. No `src/`, schema, or CLI change.
- Frontmatter parses; LF line endings preserved.
- Configured suite green at task completion.

DO NOT: weaken closure membership, remove pre-archived members from any manifest, unarchive or requeue 136.001-T / 137.005-T / 137.006-T, modify the Step 0.5 item 1a halt condition, or add an `autoharness gate` subcommand (deferred, 022-DL open question 1).