---
artifact_type: task
created_at: 2026-09-15T22:16:18.7308761Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 166.002-T
id: 166.004-T
labels:
    - skill
    - shipment-reconcile
    - protected-set
    - template-parity
parent_id: 166-F
priority: medium
status: queued
title: 'shipment-reconcile Step 0(c): engine-inertness close-path classification (+ template mirror)'
updated_at: 2026-09-16T08:03:00.7966802Z
---

REFRAMED IN PLACE 2026-09-15; CORRECTED 2026-09-16 (plan revision 2); CORRECTED AGAIN 2026-09-16 (plan revision 3 — evidence-class labelling); CORRECTED AGAIN 2026-09-16 (plan REVISION 4 — group B "measured fact" demotions + group C path-scoped re-sourcing). Supersedes the Steps 2-3 terminal-descope exemption scope (that work now lives in 166.005-T under a different design). Authoritative plan: docs/plans/2026-09-15-flat-manifest-shipment-closure-plan.md (unit U4, REVISION 4). Authoritative decision: docs/decisions/2026-09-15-flat-manifest-shipment-closure-deliberation.md (REVISION 4).

DEPENDENCIES (machine DAG, authoritative): depends on 166.002-T (red phase). No other prerequisites.

SURFACES (identical edits to BOTH, byte-parity required):
- .github/skills/shipment-reconcile/SKILL.md
- templates/skills/shipment-reconcile/SKILL.md.tmpl

SCOPE: Safe-Close Mode Step 0(c) close-path classification ONLY. Steps 2/3/5/8 belong to 166.005-T (width isolation).

CHANGES
1. REWRITE Step 0(c) to the INV-6 ENGINE-INERTNESS predicate. Delete the "fully covered (every one of its descendants ... is also a manifest member)" wording and the "manifest must contain nothing beyond the qualifying root feature(s) and their descendants" coverage framing. The cascade path is permitted only when EVERY artifact reachable by the engine's own scope expansion from the manifest but OUTSIDE closure scope declares `status: archived` in its own frontmatter.
1a. REVISION-2 CORRECTION, RE-CORRECTED IN REVISION 3 (D1b / plan R19 + R25) — STATE THE EXACT-PARSED-SCALAR RULE IN THE SKILL TEXT. The revision-2 wording "the EXACT canonical literal `archived`, compared VERBATIM" is WITHDRAWN as UNIMPLEMENTABLE: every record reaches the predicate through gates.topology._frontmatter, which uses yaml.safe_load, and a YAML load ERASES lexical whitespace and quoting, so there is no byte-level value left to compare. CORRECT RULE: inertness is granted ONLY when the PARSED status is a str EXACTLY EQUAL to `archived` — `isinstance(status, str) and status == "archived"`. Ship MUST NOT lower-case, upper-case, casefold, strip, alias, or str()-coerce the parsed value before comparing, and MUST NOT accept `Archived`, `ARCHIVED`, the YAML-QUOTED `" archived "`, or any other DISTINCT PARSED value as inert. NON-STRING VALUES FAIL CLOSED: `status: yes` parses to the bool True and a bare `status:` parses to None; NEITHER is inert, and both route to SAFE_CLOSE. STATE THE ACCEPTED LIMITATION HONESTLY: spellings that are YAML-EQUIVALENT after parsing — notably UNQUOTED trailing whitespace, which parses to exactly `archived` — are INDISTINGUISHABLE post-parse and ARE treated as canonical. The revision-2 claim that an unquoted `archived ` is a distinct non-inert value is WITHDRAWN AS FACTUALLY WRONG. Rationale to state inline: this comparison AUTHORIZES A DESTRUCTIVE CASCADE, so any post-parse normalization BROADENS that authorization; and the backlogit engine ALSO YAML-parses, so the PARSED SCALAR is the domain in which autoharness and the engine agree. A raw-scalar lexical parser was considered and REJECTED (new surface outside freeze-scope, drifts from _frontmatter, diverges from engine semantics) — do NOT implement one. A variant may only be admitted after its engine behavior is SEPARATELY AND INDEPENDENTLY PROVEN. Anything not exactly canonical routes to SAFE_CLOSE.
1b. REVISION-2 ADDITION (D1c / plan R20) — STATE THE TORN/DUPLICATE FAIL-CLOSED RULE: if ANY artifact ID encountered during the scan resolves to MORE THAN ONE record — duplicated within a root, or TORN across queue/ and archive/ — the scan result is AMBIGUOUS and Ship MUST route to SAFE_CLOSE, naming the offending ID. This applies to EVERY record scanned, EXPLICITLY INCLUDING OUT-OF-MANIFEST DESCENDANTS, and applies even when one copy declares an exact canonical `archived`. Inertness must be POSITIVELY established against a complete, unambiguous scan; it may never be inferred from a partial, aborted, or ambiguous one, and a destructive authorization must never depend on which copy was read last.
2. STATE EXPLICITLY that inertness is read from the record's own frontmatter `status` field and NEVER inferred from queue//archive/ location — consistent with the existing declared-status discipline already established in Step 0(b).
3. STATE EXPLICITLY that this is a BLAST-RADIUS CONSTRAINT ON AN INSTRUMENT, not a definition of closure scope: it never adds a member to closure scope, and it never requires an out-of-manifest artifact to join the manifest or to carry a disposition note. Reference the D1a vocabulary (manifest_scope / closure_scope / allowed_ids / required_ids) so the reader can see that an inert out-of-manifest descendant is in NONE of those sets.
4. EMBED the engine-behavior table as the stated rationale (backlogit 1.10.1), BUT LABEL EACH ROW WITH ITS EVIDENCE CLASS — REVISION-3 CORRECTION, RE-CORRECTED IN REVISION 4 (D7a/D7b/D13 / plan R24 / H17 / H20). The revision-2 instruction to "cite the in-workspace, hermetic, checked-in fixtures from 166.002-T as the authoritative source" for these rows is WITHDRAWN AS UNSOUND: those fixtures call the PURE PYTHON CLASSIFIER and NEVER INVOKE THE BACKLOGIT ENGINE, so they CANNOT re-derive any engine effect (returned_ids, descendant archival, parent_id clearing). CLASSIFIER LAW and ENGINE LAW are DISJOINT evidence classes and NO surface may cite a classifier fixture as proof of an engine behavior. Rows and their true evidence status:
   (a) feature member present + out-of-manifest descendant `status: archived` -> INERT/byte-identical — PROVEN, and the ONLY proven row. *** REVISION-4 RE-SOURCING: the revision-3 citation "`git show --stat e4ca20e5`, which shows exactly 12 paths changed with 165.007-T and 165.010-T ABSENT from the diffstat" IS WITHDRAWN AS FACTUALLY WRONG — the UNFILTERED diffstat of e4ca20e5 contains 34 CHANGED PATHS and e4ca20e5 is the COMBINED PUBLICATION COMMIT (`chore(stage): publish flat-manifest closure package`), NOT a close-only commit. Do not cite it and do not call it close-only. *** CORRECT AUTHORITATIVE SOURCE — PATH-SCOPED evidence between 358b63b4 (parent of e4ca20e5; immutable pre-close tree) and e4ca20e5: `git diff --stat 358b63b4 e4ca20e5 -- .backlogit/archive/165.007-T.md .backlogit/archive/165.010-T.md` is EMPTY, and `git rev-parse` returns IDENTICAL blob OIDs at both commits (544c2377b38e9ba2df123f5b8d9bf359dacb89ea for 165.007-T, 609ad8bc7839fca8b5273d777a6acdc590aacb6f for 165.010-T). This is exactly, and only, the law the INV-6 inertness grant rests on, and it is DISTINCT from Ship's separately adjudicated in-workspace verification of the close.
   (b) `status: done` -> ARCHIVED, (c) live `queued` -> ARCHIVED with returned_ids EMPTY, (d) no feature member + live sibling -> RETURNED with parent_id CLEARED, (e) ancestors never touched — ALL FOUR ARE INDICATIVE AND UNPROVEN. State them as UNPROVEN, NEVER as MEASURED FACTS, keep them FAIL-CLOSED, and state explicitly that AN UNPROVEN ROW MAY NEVER AUTHORIZE A CASCADE — it may only justify refusing one. Cite the durable follow-ups 7F9CB5E9 (SAFE_CLOSE record transition) and 63363CF5 (returned-sibling parent_id clearing) rather than asserting them as measured fact.
   The table's ORIGINAL source was a Stage spike run in an EXTERNAL %TEMP% workspace, which was a P-005 containment/destructive-approval violation and is NON-AUTHORITATIVE — do NOT cite or re-run the external spike. A real engine-characterization harness is NOT AUTHORIZED in 174-S (deferred follow-up P2-3; would require repo-internal git-ignored scratch under .autoharness/staging/tmp/ per plan R27, explicit operator destructive approval, and D6 operator-controlled cleanup — never automatic deletion). DO NOT FABRICATE ENGINE EVIDENCE.
5. RECORD THAT returned_ids IS INSUFFICIENT (plan-review; R13): the Cascade Close Sub-Procedure Step 2 returned_ids guard is RETAINED but explicitly demoted to a SECONDARY check. REVISION-4 CORRECTION: the revision-3 justification "because the MEASURED engine archived a live out-of-manifest task while reporting returned_ids: []" is WITHDRAWN as a measured-fact claim — that observation came from the NON-AUTHORITATIVE external spike and is INDICATIVE AND UNPROVEN. State it as an UNPROVEN RISK that the guard is demoted AGAINST (a guard may be weakened on suspicion; an authorization may not be granted on it). The PRE-MUTATION inertness gate is the PRIMARY protection, and it does not depend on any unproven row.
5a. REVISION-2 ADDITION — cross-reference INV-11 where Step 0(c) routes a manifest with NO feature member: such a shipment can never qualify for CASCADE and is therefore a genuine SAFE_CLOSE shipment, for which backlogit 1.10.1 provides NO safe record transition to archived_status: shipped. Point to 166.005-T Step 8's RECONCILE_FAIL_NO_SAFE_RECORD_TRANSITION halt and to durable ACTIVE stash entry 7F9CB5E9. Do NOT state or imply that split/multi-shipment delivery is operationally complete.
6. PRESERVE UNCHANGED: Step 0(a) manifest load; Step 0(b) pre-close parent_id/declared-status snapshot; the linked-deliberation snapshot extension; the CASCADE/SAFE_CLOSE routing; the no-substitution rule; and the ENTIRE Cascade Close Sub-Procedure including its two-set allowed_ids/required_ids gate and its Step 4 parent_id-preservation check (already flat-manifest-shaped; it is the post-mutation guard for INV-10). NOTE (REVISION-4 CORRECTION): the Step 4 parent_id-preservation check is load-bearing against the separate upstream defect tracked by durable ACTIVE stash entry 63363CF5. Word that defect as SUSPECTED/UNPROVEN — the revision-3 phrasing "the engine silently CLEARS parent_id on returned out-of-manifest siblings", stated as fact, is WITHDRAWN; it originates from the NON-AUTHORITATIVE external spike. The check is retained BECAUSE the behaviour is unproven (fail-closed), not because it has been measured. Do not weaken or remove it.
7. REMOVE the three-way verdict framing entirely — there are exactly TWO verdicts, SAFE_CLOSE and CASCADE.

ACCEPTANCE: 166.002-T Part B doc-contract tests pass against BOTH mirrors, including the exact-canonical, torn/duplicate, INV-11 and multi-shipment-honesty assertions; both mirrors byte-identical on the changed block; no unresolved {{...}} placeholders introduced.

SIZE: M. COMPLEXITY: medium.