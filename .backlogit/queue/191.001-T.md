---
artifact_type: task
created_at: 2026-09-26T08:16:54.1256473Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: v1
    size_source: agent
id: 191.001-T
labels:
    - harness-surface:harness-architect
    - ship-lifecycle
    - unit-d
parent_id: 191-F
priority: high
references:
    - docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md
    - docs/reviews/2026-09-25-ship-lifecycle-release-units-plan-review.md
status: queued
title: 'Ship lifecycle D1: Ship-side validated-document consumer (accept_resolution, harness_verdict entry)'
updated_at: 2026-09-26T08:19:40.7115009Z
---



<!-- BEGIN:acceptance-criteria -->
- Capture contract: Ship runs 'harness resolve ... --json' with stdout and stderr each redirected as raw bytes to its own file under the Git-ignored .proof-scratch/harness-resolve/, records the native exit status, then runs 'python -m autoharness.harness_verdict --shipment <id> --status <int> --stdout-file <path> --stderr-file <path>'; the entry reads both files as bounded bytes and prints exactly one line: 'accepted <state> <reason_code> <exit_code>' (exit 0) or 'resolver-not-observed <sub-code>' (exit 1); an entry usage error (exit 2) is also not observed; Ship deletes both files after the entry returns.
- accept_resolution(*, requested_shipment, process_status, stdout: bytes, stderr: bytes) -> Accepted | NotObserved: Accepted only when stdout holds exactly one UTF-8 JSON document followed by at most one LF, schema-valid against B1 (harness-resolution 1.0.0), with shipment, state, reason, exit_code and process status all consistent, and stderr empty; otherwise the literal outcome resolver-not-observed (CR-B2) with a diagnostic sub-code. Sub-codes are diagnostics only; nothing branches on them (CR-B1).
- exit_code must be a JSON integer token, never a float such as 1.0 (CR-B3). Parsing is total: bounded input size, nesting depth bounded before json.loads, and RecursionError or MemoryError -> resolver-not-observed (CR-B4); object_pairs_hook rejects duplicate keys and parse_constant rejects NaN and Infinity.
- Verification: the Proof B impostor table (startup failure, unknown command, usage error, help text, empty stdout, malformed JSON, two documents, leading or trailing output, status/document mismatch, shipment mismatch, schema-invalid), plus float exit_code, deep nesting, oversized input, duplicate keys, NaN and a second trailing LF -> resolver-not-observed; one genuine document per state accepted. Handoff test: the real B5 CLI runs in a subprocess over fixture workspaces giving exits 0, 1 and 2, and an impostor ('harness bogus', argparse exit 2) is also run; each capture goes through the entry in a subprocess, and the printed line and exit code are asserted.
- RED record before implementation (IM-13, per the A1 wording): task ID, exact P-004 canonical command, native exit, and each roster test's outcome with its own marker AHLC_D1_VERDICT_CONSUMER:<t> per the Marker Convention (pairwise distinct; a shared marker is refused under FI-9). Structural tests that reach no stub (API, docstring, audit) are recorded outside the roster, like characterization tests.
- Audit inventory: src/autoharness/harness_verdict.py is added to FLOOR in tests/test_harness_noclaim_audit.py (the only FLOOR growth in S(D)); the other D1 paths (tests/test_harness_verdict.py, tests/test_harness_verdict_handoff.py and the audit test itself) are residue units under the Residue rule, not FLOOR entries (IM-14-F51 reading); any LEDGER entry D1 adds is a non-claim confirmed by local review.
- Manifest harvest carry NOROW-F18 (E2, repeat on D1): .proof-scratch/ is ignored in this repository but not guaranteed in installed workspaces; before any capture write under .proof-scratch/harness-resolve/ (D1's handoff test and any capture D1 performs), verify the ignore rule covers the path (for example, git check-ignore) and halt rather than write into a path Git does not ignore.
- Manifest harvest carry CONST-II-F01 (E2): structural tests 'may pass'; the audit test's FLOOR addition (harness_verdict.py) reaches no stub, so record the pre-implementation failing run of that structural audit test as gap characterization (Principle II evidence), not as RED; the roster tests carry the RED record above.
- Environment prechecks: git worktree list shows one worktree; git status --porcelain is empty for the task's files before the first write; the canonical command runs from the repository root. Over the estimate by more than 30 minutes -> stop and return to Stage for a split.
- IM-14: no added text claims race, TOCTOU or hardlink-alias resistance.
- IM-15: a test or proof PASS confers no claim authority; ordinary P-001/P-002/P-004/P-020 gates at claim and closure.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan task D1 (docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md, blob b7a77c76, section 'D1: Ship-side validated-document consumer'; release unit S(D); review authority: manifest E5 Verdict under OP-5).

Files (budget): src/autoharness/harness_verdict.py; tests/test_harness_verdict.py; tests/test_harness_verdict_handoff.py; tests/test_harness_noclaim_audit.py.

Change: accept_resolution(*, requested_shipment, process_status, stdout: bytes, stderr: bytes) -> Accepted | NotObserved and the 'python -m autoharness.harness_verdict' entry under the capture contract; total, bounded parsing; the literal outcome resolver-not-observed for every impostor; the module is added to the audit inventory (FLOOR).

Marker: AHLC_D1_VERDICT_CONSUMER (prefix). Trace: IM-07 (CR-B1 to CR-B4), IM-13, IM-14, IM-15, NOROW-F18. Estimate 110 min; size S; complexity medium.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Label harness-surface:harness-architect (Python production module under src/): roster and RED obligation apply. First task of S(D); it follows B5 (189.003-T, S(B-entry)), and that order is held by the shipment edge (this shipment is blocked by 195-S), not a task edge. D1 validates against B1's schema, so a B rollback rolls back D first, including D1. Blocks D2. Supersedes the Ship-side consumer part of retired 181.007-T (plan Trace From the Retired Tasks: 'The Ship-side consumer (IM-07) is D1'). D1 stays in place on a D3 rollback (inert without the activated text).
<!-- END:implementation-notes -->