---
artifact_type: shipment
created_at: 2026-09-19T03:02:58.3047598Z
custom_fields:
    items:
        - 177-F
        - 177.004-T
        - 177.005-T
        - 177.001-T
        - 177.002-T
        - 177.003-T
        - 177.007-T
        - 177.006-T
id: 183-S
status: queued
title: PRECURSOR SPIKE-2 - network-denied Linux container conformance isolation (7F9CB5E9)
updated_at: 2026-09-20T05:34:35.5967835Z
---

MANIFEST ORDER IS DEPENDENCY ORDER. Items are listed so that every prerequisite appears above the task it unblocks. 177.004-T leads the task list because the credential-environment inspection (I1) is a SAFETY PREREQUISITE, not merely an early finding: it must be observed before any untrusted acquisition or probe runs, so an explicit blocks edge gates 177.005-T and 177.002-T on it (attempt 02 finding F1, P1). THE BLOCKS EDGE IS THE ORDERING MECHANISM AND IS NOT THE SAFETY MECHANISM (attempt 03 finding K1, P1). A blocks edge clears on PREDECESSOR COMPLETION, and 177.004-T completes on three outcomes - DETERMINED/ACHIEVABLE, DETERMINED/NOT ACHIEVABLE, and NOT DETERMINED - FLOOR INVOKED - two of which leave credential absence unverified or affirmatively falsified. THE SAFETY MECHANISM IS AN EXECUTABLE VERDICT PREDICATE: 177.004-T emits a single non-secret I1_GATE: line to docs/spikes/2026-09-18-conformance-isolation-i1-gate.md, and 177.005-T and 177.002-T read it as their FIRST action. The gate is OPEN only on exactly one readable I1_GATE: line whose token is ACHIEVABLE; everything else is CLOSED. ON A CLOSED GATE THOSE TASKS DO NOT RUN THE UNTRUSTED ACQUISITION OR PROBE AT ALL and record I7 and I4/I5/I6 as NOT DETERMINED - FLOOR INVOKED naming I1 as the blocker; 177.001-T records I2 the same way via the acquisition that never ran, while I3 is not gated. THE COMPOSED STATE IS THEN ISOLATION_FLOOR_ONLY - or ISOLATION_CLEANUP_FAILED if cleanup also fails - AND NEVER ISOLATION_CHARACTERIZED. 177.001-T follows 177.005-T. THE CLOSING SEQUENCE IS THREE TASKS, NOT TWO (attempt 05 finding L1, P2). 177.003-T authors the findings artifact once its four determining tasks have reported and removes the probe workflow in that same commit - THE REMOVAL COMMIT - and writes no CLEANUP_ line. 177.007-T then runs against that ALREADY-EXISTING commit, observes it, and is the SOLE WRITER of the eight-line machine-readable CLEANUP_ evidence block, which it lands in its OWN SEPARATE EVIDENCE COMMIT recording the creation commit and path, the four dispatch run IDs and URLs, the removal commit, and a tip observation. 177.006-T runs last and is the SOLE EVALUATOR of that block and the sole emitter of the gate verdict. THE MANIFEST ORDER 177.003-T, 177.007-T, 177.006-T IS THE DEPENDENCY ORDER, and the edges are 177.003-T blocks 177.007-T, 177.007-T blocks 177.006-T, with the pre-existing 177.003-T blocks 177.006-T edge retained. THE SPLIT EXISTS BECAUSE A SINGLE-COMMIT EMISSION WAS UNSATISFIABLE. Through plan revision 5 the block was mandated to be written IN the removal commit while two of its eight lines - the removal commit SHA and a clean-working-tree tip observation - named values that only that commit's own creation defines. A commit's identity is a function of its content, so a commit cannot contain its own SHA, and a tree cannot be observed clean from inside the staged change that makes it clean. No spec-conformant first emission existed, C4 and C5 could not be satisfied on a first pass, and at least one spurious ISOLATION_CLEANUP_FAILED cycle was forced on every execution of the unit as written. Splitting removal from evidence makes EVERY RECORDED VALUE OBSERVABLE BEFORE IT IS RECORDED: observed_tip names the tip that was actually observed - the removal commit OR A DESCENDANT OF IT, and the PARENT of the evidence commit - and NO FIELD OF THE BLOCK EVER NAMES THE COMMIT THAT RECORDS IT. THE REMOVAL PROOF IS DURABLE, NOT POSITIONAL (attempt 06 finding M1, P2). Through plan revision 6 177.007-T could write only while the removal commit was the CURRENT TIP, which was strictly stronger than C4's reachability requirement and C5's explicit removal-commit-or-descendant tolerance, so one intervening commit - and .backlogit is tracked, so ordinary bookkeeping produces them - forbade the write under the atomic rule, while no second removal commit could ever exist because the path was already deleted. Revision 7 requires instead that the named removal commit EXIST, be REACHABLE FROM THE CURRENT HEAD, carry 177.004-T's creation commit in its ancestry, and delete EXACTLY the probe-workflow path while touching no other workflow file, derived deterministically from committed history by RC1-RC7 and NEVER required to be the tip. Later unrelated forward commits therefore cannot invalidate that historical fact, the block is RE-EMITTABLE from the same reachable removal commit after them, and NO SECOND DELETION OR REMOVAL COMMIT IS EVER REQUIRED - which is what makes the in-unit ISOLATION_CLEANUP_FAILED remedy executable rather than advertised. CLEANLINESS IS EXACT-PATH-SCOPED, NEVER WHOLE-TREE (attempt 06 finding M2, P2). Through plan revision 6 both 177.007-T and 177.006-T check C6 required an EMPTY WHOLE-TREE git status --porcelain, in a workspace whose backlog records are tracked and whose agents emit untracked checkpoints and session memory notes in normal operation, and C6 runs first, so one unrelated untracked note short-circuited the entire predicate. Revision 7 scopes every limb to .github/workflows/spike-177-isolation-probe.yml alone, using fixed-argv probes PP1-PP3 that reject ambiguous output: unrelated tracked files, untracked files, agent checkpoints, session memory notes and operator work neither block evidence emission nor fail the predicate, while a staged, unstaged or untracked copy of the exact path STILL FAILS CLOSED. No waiver, no guessed SHA and no corrective cycle is required, and first-pass success is reachable. CLEANUP IS AN EXECUTABLE LIMB OF THE FINAL PREDICATE (attempt 04 finding K3, P2). Through plan revision 4 the branch-cleanliness check was mandated in 177.006-T's record while the plan defined its composed states EXHAUSTIVELY over the seven-entry coverage ledger, in which branch state appeared nowhere, so an all-DETERMINED ledger on a branch still carrying the probe workflow forced the passing state and no token could express an unclosed spike. Revision 5 closed this and revisions 6 and 7 keep it: 177.006-T resolves cleanup to CLEANUP_PROVEN or CLEANUP_FAILED by six checks C1-C6, with C6 INDEPENDENTLY RE-OBSERVING THE CURRENT BRANCH STATE rather than trusting the recorded observation and never taking branch state from CLEANUP_REMOVED_COMMIT - THE EVALUATOR'S OWN CURRENT-STATE OBSERVATION IS THE FINAL BRANCH-STATE AUTHORITY - and CLEANUP_FAILED carrying one of SIX reason tokens: WORKFLOW_PRESENT_AT_TIP (the exact path is TRACKED AT THE CURRENT TIP, and nothing else raises this token), WORKFLOW_PATH_RESIDUE (absent at the tip but present as a staged, unstaged or untracked copy AT THAT EXACT PATH - a separate token precisely so a diagnostic never claims the workflow is committed when only exact-path residue exists), TIP_UNOBSERVABLE (the tip cannot be resolved, or an exact-path probe returns output outside its accepted shape - ambiguity is never resolved in the passing direction), EVIDENCE_MISSING, EVIDENCE_MALFORMED or EVIDENCE_INCONSISTENT. The token split touches text attempt 05's P3 L2 also concerns; L2 IS NOT CLAIMED CLOSED by this cycle and remains open for independent judgement. A FIFTH COMPOSED STATE, ISOLATION_CLEANUP_FAILED, carries its own verdict line form and blocks 181-S harvest outright; it expresses EVIDENCE COMPLETE, SPIKE NOT CLOSED, and its remedy is inside the unit rather than requiring a further determining run or a waiver. THE FIVE STATES RESOLVE IN A DECLARED PRECEDENCE - ISOLATION_NOT_OBSERVED, ISOLATION_UNDETERMINED, ISOLATION_CLEANUP_FAILED, ISOLATION_FLOOR_ONLY, ISOLATION_CHARACTERIZED - making the resolution a TOTAL FUNCTION and placing the cleanup test ABOVE the pass state, so ISOLATION_CHARACTERIZED is structurally unreachable while the exact workflow path is TRACKED AT THE CURRENT HEAD or exists as EXACT-PATH STAGED, UNSTAGED OR UNTRACKED RESIDUE, or while its lifecycle evidence is missing, malformed or inconsistent, and is reachable in ONE NORMAL RUN when it is not - and equally reachable on any later re-emission, because no limb depends on the branch tip standing still or on the rest of the tree being clean. The cleanup check is local, offline and credential-free, so it neither weakens nor interacts with the I1 gate or the acquisition-then-no-network model; 177.007-T is likewise local, offline and credential-free, carries no I1 gate read, and determines no property. Reordering the manifest changed no dependency edge - it made the existing DAG readable top to bottom (attempt 02 finding F3, P3). COMMITTED PROBE WORKFLOW LIFECYCLE IS DECLARED (attempt 03 finding K2, P2): GitHub runs only workflows that exist on a branch, so the hosted-runner probe workflow IS COMMITTED. 177.004-T creates exactly one at .github/workflows/spike-177-isolation-probe.yml, workflow_dispatch-only, minimal permissions, no secrets; all four determining tasks reuse it by dispatch input; 177.003-T removes it at spike close in the findings commit; rollback is git revert of the single creation commit, and removal is VERIFIED by the cleanup predicate rather than assumed; and the branch must end carrying no probe workflow. Blast radius and rollback in the plan now state this truthfully rather than describing the spike as mutating no tracked surface. THIS SHIPMENT IS A DAG ROOT: it has no precursor shipment, and successor shipments depend on it. Attempt 05's three open P3 findings K5, K6 and L2 were not in scope for the revision-6 or revision-7 cycles and remain open and unaddressed, as do attempt 06's three new P3 findings M3, M4 and M5; all six are carried as non-blocking follow-ups in the backlogit stash and must not enter this shipment's scope. Attempt 04's P3 K4 was independently closed at attempt 05 and is no longer carried, and attempt 05's P2 L1 was independently closed at attempt 06. Attempt 06's two P2 findings M1 and M2 were addressed by the revision-7 cycle and are recorded ADDRESSED PENDING REVIEW, not closed. Governing plan docs/plans/2026-09-18-conformance-isolation-spike-plan.md (plan_id conformance-isolation-spike, revision 7, disposition REMEDIATED-PENDING-REVIEW, awaiting independent attempt 07). Verdict manifest docs/reviews/2026-09-18-conformance-isolation-spike-plan-review.md. Source 7F9CB5E9.