---
artifact_type: task
created_at: 2026-09-26T07:35:48.9056881Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: v1
    size_source: agent
id: 188.001-T
labels:
    - harness-surface:harness-architect
    - ship-lifecycle
    - unit-b
    - unit-b-core
parent_id: 188-F
priority: high
references:
    - docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md
    - docs/reviews/2026-09-25-ship-lifecycle-release-units-plan-review.md
status: queued
title: 'Ship lifecycle B1: harness_surfaces contracts, 47-code reason registry and harness-resolution schema mirrors'
updated_at: 2026-09-26T07:53:42.1393567Z
---



<!-- BEGIN:acceptance-criteria -->
- Registry test (IM-03, IM-17): the registry equals a test-local constant copied from the hash-pinned block, and the governing 8 codes equal a second test-local constant, never derived from the registry; the derived counts 47 and 2/4/41 are recorded; each code carries its class, state and exit in listed order (FI-6).
- Schema tests: schemas/harness-resolution.schema.json and schemas/harness-resolution/1.0.0.schema.json are both new and equal after removing $id (the root file's $id names the unversioned path, the mirror's names harness-resolution/1.0.0); the 1.0.0 mirror's SHA-256 over LF-normalized bytes is pinned in the test; additionalProperties: false and closed enums; every code has exactly one oneOf branch binding its state and exit; the result field set is that of the proven c3_schema.json (FI-7), with declarations as ratified.
- The contract is registered in src/autoharness/schema_contracts.py with current_version 1.0.0; no published mirror is edited in place (docs/compound/2026-08-08-schema-mirror-mutated-in-place-without-version-bump.md).
- The 9 Proof C mutants are rejected, including READ_BUDGET_EXHAUSTED and a class 1b code replaced by MEMBERS_TOO_MANY.
- tests/test_harness_noclaim_audit.py lists src/autoharness/harness_surfaces.py in FLOOR (B1's paths are added to the audit inventory; see the IM-14-F51 carry below) and stays green; any LEDGER entry B1 adds is a non-claim confirmed by local review.
- RED record before implementation (IM-13, per the A1 wording): task ID, exact P-004 canonical command, native exit, and each roster test's outcome with its own marker AHLC_B1_RESOLUTION_CONTRACT:<t> per the Marker Convention (pairwise distinct; a shared marker is refused under FI-9). Structural tests that reach no stub (schema parity, pinned hash, audit) are recorded outside the roster, like characterization tests.
- Environment prechecks: git worktree list shows one worktree; git status --porcelain is empty for the task's files before the first write; the canonical command runs from the repository root. Over the estimate by more than 30 minutes -> stop and return to Stage for a split.
- IM-14: no added text claims race, TOCTOU or hardlink-alias resistance.
- IM-15: a test or proof PASS confers no claim authority; ordinary P-001/P-002/P-004/P-020 gates at claim and closure.
- Manifest harvest carry IM-14-F51 (E4): the frozen 'B1's paths are added to the audit inventory' cell is read as: FLOOR grows only by B1 (harness_surfaces.py) and D1 (harness_verdict.py); B1's other paths (the two schema files, schema_contracts.py and the test modules) are residue units under the Residue rule, not FLOOR entries.
- Manifest harvest carry CONST-II-F01 (E2): structural tests 'may pass'; record the pre-implementation failing run of the schema-parity, pinned-hash and audit structural tests as gap characterization (Principle II evidence), not as RED.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan task B1 (docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md, blob b7a77c76, section 'B1: Contracts, reason registry and schema mirrors'; release unit S(B-core); review authority: manifest E5 Verdict under OP-5).

Files (budget): src/autoharness/harness_surfaces.py; schemas/harness-resolution.schema.json; schemas/harness-resolution/1.0.0.schema.json; src/autoharness/schema_contracts.py; tests/test_harness_resolution_contract.py; tests/test_harness_noclaim_audit.py.

Change: frozen result types, the state/exit table, ReadStage (SHIPMENT_CANDIDATE, MEMBER_CANDIDATE, MANIFEST, TEMPLATE, INSTALLED, CANDIDATE_RECHECK, SURFACE_RECHECK; diagnostics only), and the 47-code registry with class, state and exit, in listed order (FI-6). The result field set is that of the proven c3_schema.json (FI-7), with declarations as ratified. Both schema files are new and equal except $id (the root file's $id names the unversioned path, the mirror's names harness-resolution/1.0.0), additionalProperties: false, closed enums, one oneOf branch per code binding state and exit; register the contract with current_version 1.0.0. B1 adds harness_surfaces.py to the non-claim audit FLOOR.

Marker: AHLC_B1_RESOLUTION_CONTRACT (prefix). Trace: IM-03 (B1 half), IM-17, PE-DATA-01 to 03, IM-13, IM-14 / PE-SAFETY-06 (FLOOR addition), IM-15. Estimate 85 min; size M; complexity medium.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Label harness-surface:harness-architect (Python production module under src/): roster and RED obligation apply. First task of S(B-core); blocks B2 (B1 to B5 all edit harness_surfaces.py, serial). Risky action (plan Plan Hardening): registering a new public schema contract at 1.0.0; mirror parity or registration test red -> B does not ship. Rollback: no release tag includes S(B-core) before S(B-entry) closes, so a schema defect found by B-entry reverts B-core, never a 1.1.0 bump.
<!-- END:implementation-notes -->