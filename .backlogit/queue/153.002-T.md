---
artifact_type: task
created_at: 2026-08-31T22:15:43.762564Z
custom_fields:
    complexity: high
    queue_position: 19
    size: M
    size_ruleset_version: autoharness-stage-2h-v1
    size_source: agent
id: 153.002-T
parent_id: 153-F
priority: medium
status: queued
title: Enforce lock-ownership verification and consistent lock-path computation in both release scripts
updated_at: 2026-08-31T22:33:30.7351477Z
---

Covers findings 4, 5, 6 - the most serious, because the shipped scripts contradict concurrency.instructions.md ("Agents MUST NOT delete lock files they did not create"). release_lock.ps1 removes with Remove-Item -Force unconditionally and release_lock.sh L35 uses rm -f, neither reading the agent/pid already recorded in the lock file, so any agent can silently break any other agents lock. Finding 6 travels here because it is a release-side lock-path bug and ownership cannot be read without the correct path: Split-Path -Parent returns empty for a root-level relative path such as AGENTS.md when the target is missing, Join-Path then throws under Set-StrictMode, and the missing-file branch uses the raw relative path while acquire used Resolve-Path, so the two compute DIFFERENT lock paths for the same target. DECISION (ii): REFUSE, honouring the existing 1-hour staleness heuristic only behind an explicit --force flag the operator supplies; never automatic. DECISION (iii): exit NON-ZERO on refusal. PLAN REVIEW FINDING 1 (P0 - DoS risk): the refusal message MUST state the operator remedy - lock path, recorded agent and pid, lock age, and the explicit --force invocation - so a crashed-agent lock is immediately actionable. PLAN REVIEW FINDING 4 (P1): ownership is decided on recorded AGENT IDENTITY as primary; pid is corroborating evidence for the staleness report only, never sole authorisation, because pid reuse on Windows can match an unrelated live process. BINDING H3: this task and the containment task must land in the SAME shipment - a containment-only intermediate state is worse than none. Source: 74C62374 findings 4-6.