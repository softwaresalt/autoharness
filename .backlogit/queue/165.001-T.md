---
artifact_type: task
created_at: 2026-09-12T22:48:13.9164174Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
id: 165.001-T
labels:
    - test-first
    - red-phase
    - topology-gate
parent_id: 165-F
priority: medium
status: queued
title: 'RED: authoritative predecessor-derivation test matrix (six regression dimensions)'
updated_at: 2026-09-12T22:48:13.9164174Z
---

RED-FIRST (Constitution Principle II, NON-NEGOTIABLE). Author failing tests in `tests/test_gates_topology.py` for DAG-authoritative predecessor derivation. Every test MUST fail against current `main` for the stated reason, and the red observation MUST be recorded before T2 implements.

REQUIRED COVERAGE (all six operator-mandated dimensions):
1. Independent numerically adjacent DAG roots — two roots with no explicit `blocks` edges (fixture mirroring the 148-S/149-S case) each derive `predecessor_source: "none"` and are NOT blocked by numeric inference.
2. Explicit dependency chains — a linear explicit chain blocks correctly with `predecessor_source: "explicit"`.
3. Closure evidence demanded ONLY for actual explicit predecessors — asserted for an explicit predecessor and asserted NEVER demanded for a merely numerically-adjacent non-predecessor.
4. Converging DAGs — two explicit predecessors converging on one successor; every explicit predecessor is evaluated, none skipped.
5. Missing/invalid dependency data — unresolvable or malformed dependency IDs fail CLOSED and never silently drop a predecessor.
6. Backward-compatibility / migration — `unsequenced_shipment: warn` (default) passes with provenance; `block` restores the legacy blocking posture. BOTH modes asserted (hardening F2).

HARDENING F1: `predecessor_source` must be asserted on a PASSING case, not only on blocked payloads.

SIZE: S (~1.5h). COMPLEXITY: medium (fixture construction across six dimensions; no production-code change).
Plan: docs/plans/2026-09-12-dag-authoritative-predecessor-derivation-plan.md §T1