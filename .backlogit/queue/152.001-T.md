---
artifact_type: task
created_at: 2026-08-31T22:13:53.3356401Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: autoharness-stage-2h-v1
    size_source: agent
dependencies:
    - 152.002-T
id: 152.001-T
parent_id: 152-F
priority: medium
status: queued
title: Make the release pre-publish PyPI probe fail closed on an already-published version
updated_at: 2026-09-01T00:09:22.4172523Z
---

Replace the else: branch at .github/workflows/release.yml L102-L106 with an explicit non-zero exit naming the version and the remedy. BINDING H2: three outcomes distinguished - 404 proceed; version present exit non-zero; any other error re-raise, including URLError, never silently treated as "not present". PLAN REVIEW FINDING 2 (P1): the else: branch is currently reached on ANY 2xx, so assert the response is from pypi.org AND the decoded body identifies the requested version; if the body cannot be parsed treat it as a transport error and re-raise rather than as present. PLAN REVIEW FINDING 1 (P0): the failure message MUST name the supported remedy (re-run post-publish jobs only, or bump and re-tag) so an operator does not disable the gate. Keep skip-existing: true at L112 unchanged WITH an inline comment recording it is deliberate defence in depth (finding 3). No change to the pinned action SHA, trigger, or permissions. SEQUENCING: BLOCKED BY 152.002-T, which carries the hermetic red test that must be observed failing against the pre-fix workflow content first. SIZE/COMPLEXITY (two-axis, ruleset autoharness-stage-2h-v1): size S, complexity medium. SAFETY MODE (added in review-fix cycle 2; SHIP-2 plan H6 declared it but cycle 1 never propagated it into the executable task, and review flagged this task as high/production-impact): careful + freeze-scope bounded to the PRE-PUBLISH PROBE STEP of .github/workflows/release.yml. This task edits the IRREVERSIBLE PUBLISH PATH, where an over-broad edit is the dominant risk: the pinned action SHA, the workflow trigger, permissions, secrets, and the skip-existing setting are ALL out of scope and must not be touched (binding H3). Any other defect noticed in the release workflow is a P-021 capture, not a fix. Source: 8E10B13B. Plan: docs/plans/2026-08-31-ship2-release-ci-fail-closed-gates-plan.md