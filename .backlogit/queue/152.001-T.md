---
artifact_type: task
created_at: 2026-08-31T22:13:53.3356401Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 152.002-T
id: 152.001-T
parent_id: 152-F
priority: medium
status: queued
title: Make the release pre-publish PyPI probe fail closed on an already-published version
updated_at: 2026-09-03T15:11:53.2450233Z
---

Replace the else: branch at .github/workflows/release.yml L102-L106 with an explicit non-zero exit naming the version and the remedy. BINDING H2: three outcomes distinguished - 404 proceed; version present exit non-zero; any other error re-raise, including URLError, never silently treated as "not present". PLAN REVIEW FINDING 2 (P1): the else: branch is currently reached on ANY 2xx, so assert the response is from pypi.org AND the decoded body identifies the requested version; if the body cannot be parsed treat it as a transport error and re-raise rather than as present. PLAN REVIEW FINDING 1 (P0), RESOLUTION CORRECTED IN ORCHESTRATOR REVIEW-FIX CYCLE 1 (FINDING 15): the failure message MUST name the supported remedy so an operator does not disable the gate, but the previously named remedy "re-run post-publish jobs only" DOES NOT EXIST IN THIS WORKFLOW. .github/workflows/release.yml declares exactly ONE job (release), and GitHub Actions cannot re-run an individual STEP - re-running the job re-enters the publish path and now correctly fails closed at the probe. The message MUST therefore name the remedies actually available in this workflow structure, in order: (R1) BUMP THE VERSION AND RE-TAG - the primary supported remedy and the only one that legitimately re-enters this workflow; (R2) if the upload already succeeded and only a later step failed, COMPLETE THE REMAINING POST-PUBLISH WORK OUT OF BAND (for example gh release create against the already-published artifacts) rather than re-running the job; (R3) NEVER disable or bypass the gate. ACCEPTANCE: assert the emitted message text contains R1 and R2 and does NOT contain any instruction to re-run post-publish jobs. Restructuring release.yml into separately re-runnable pre-publish and post-publish jobs is a DIFFERENT CONTRACT SURFACE and is out of scope here. Keep skip-existing: true at L112 unchanged WITH an inline comment recording it is deliberate defence in depth (finding 3). No change to the pinned action SHA, trigger, or permissions.

BINDING H2a - "VERSION PRESENT" MEANS THE BODY SAYS SO, FROM THE RIGHT HOST (made explicit in review-fix cycle 1). "Present" requires ALL of: the FINAL RESOLVED response host is pypi.org; the body decodes as JSON; the decoded body identifies THE REQUESTED VERSION. Host mismatch, undecodable body, or unexpected body shape => RE-RAISED TRANSPORT ERROR (never "present", never "absent").

BINDING H2b - A VERSION MISMATCH IS AN INTEGRITY ANOMALY, NOT EVIDENCE OF ABSENCE (this is the SINGLE authoritative statement of the mismatch outcome in this task; the cycle-1 "body decodes cleanly but names a DIFFERENT version => PROCEED" rule is WITHDRAWN and MUST NOT be reintroduced). The probe requests the EXACT-VERSION endpoint https://pypi.org/pypi/autoharness/{version}/json, which names the version IN THE PATH. A conforming PyPI can return only two things from that URL: 404 when the version does not exist, or 200 with a body identifying THAT version. A 200 whose decoded body names a DIFFERENT version is a response the protocol does not permit - it is POSITIVE EVIDENCE that something between the requester and PyPI is not behaving as PyPI (a cache serving a stale or mismatched object, a mirror synthesising responses, or an interception proxy). The REQUIRED OUTCOME is therefore RE-RAISE AS A TRANSPORT/INTEGRITY ERROR: the probe FAILS CLOSED, does NOT proceed, does NOT report "present", and publication does not occur. ABSENCE IS PROVED BY 404 AND NOTHING ELSE. Cycle 1's "proceed" detected an anomaly and then discarded it, which is fail-open reasoning inside a fail-closed shipment and is WORSE than the original else: branch because the anomaly had already been observed before being thrown away.

DISCRIMINATING POWER IS PRESERVED: C2 and C6 remain distinguishable because their outcomes differ IN KIND - C2 exits non-zero with an "already published, bump and re-tag" message, while C6 raises a transport/integrity error naming the mismatch. A probe that merely checks "did the request succeed" still passes C2 and STILL FAILS C6, because such a probe would proceed or report "present" instead of raising an integrity error. The body-versus-request-success discrimination H2a demands is fully retained; only the verdict for the anomalous branch changed.

SEQUENCING: BLOCKED BY 152.002-T, which carries the hermetic red tests that must be observed failing against the pre-fix workflow content first. The corresponding assertions live in 152.002-T as cases C4 (wrong-host redirect), C5 (malformed JSON, both undecodable-bytes and missing-info.version shapes) and C6 (mismatched version => RAISES, does not proceed, does not exit with C2's already-published code); this task is not green until those cases pass.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size S, complexity medium. SAFETY MODE (added in review-fix cycle 2; SHIP-2 plan H6 declared it but cycle 1 never propagated it into the executable task, and review flagged this task as high/production-impact): careful + freeze-scope bounded to the PRE-PUBLISH PROBE STEP of .github/workflows/release.yml. This task edits the IRREVERSIBLE PUBLISH PATH, where an over-broad edit is the dominant risk: the pinned action SHA, the workflow trigger, permissions, secrets, and the skip-existing setting are ALL out of scope and must not be touched (binding H3). Any other defect noticed in the release workflow is a P-021 capture, not a fix. Source: 8E10B13B. Plan: docs/plans/2026-08-31-ship2-release-ci-fail-closed-gates-plan.md