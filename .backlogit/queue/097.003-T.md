---
artifact_type: task
created_at: 2026-07-29T11:58:02.6130336Z
dependencies:
    - 097.001-T
id: 097.003-T
parent_id: 097-F
priority: medium
status: queued
title: Update installer/tuner logic + emit surfaces (verify_workspace CANONICAL_AGENTS, install/tune SKILL alias tables)
updated_at: 2026-07-29T11:58:02.6130336Z
---

<!-- BEGIN:description -->
## Task 097.003 — Installer/tuner logic + emit surfaces

Width domain: installer/tuner code + skill emit tables. Est. ~2h. Depends on 097.001.

Files (see feature 097-F Group C):

* `src/autoharness/verify_workspace.py`:
  - `CANONICAL_AGENTS` (~lines 113-126): set `canonical_file` to
    `_stage.agent.md`/`_ship.agent.md`; `canonical_name` to `_Stage`/`_Ship`;
    ADD the OLD dot-names to `legacy_files` (`.stage.agent.md`, keep
    `stage.agent.md`) and `legacy_names` (`.Stage`, keep `Stage`) — likewise ship.
    This lets tune migrate existing dot-prefixed installs. Retaining the old
    dot-names HERE is intentional (allow-listed in the zero-residual gate).
  - Update every hard-coded `.github/agents/.stage.agent.md` /
    `.ship.agent.md` path string (~26 occurrences).
* `.github/skills/install-harness/SKILL.md`: update prose refs AND the
  canonical/legacy alias table (~1079-1080). Keep the OLD name in the alias
  "legacy" column (intentional).
* `.github/skills/tune-harness/SKILL.md`: update prose ref (~247) AND the alias
  table (~327-328); keep OLD name in the legacy column.
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
## Acceptance criteria

* `CANONICAL_AGENTS` canonical_file/name use underscore forms; legacy tuples
  contain the old dot-names.
* All non-legacy path strings in verify_workspace.py use `_stage`/`_ship`.
* `rg "\.stage\.agent\.md|\.ship\.agent\.md" src/autoharness/verify_workspace.py`
  returns ONLY the legacy_files entries.
* Skill alias tables show `_stage.agent.md`/`_ship.agent.md` as canonical.
* `python -c "import autoharness.verify_workspace"` imports cleanly.
<!-- END:acceptance-criteria -->
