---
artifact_type: task
created_at: 2026-08-31T22:19:30.4374445Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: autoharness-stage-2h-v1
    size_source: agent
id: 159.002-T
parent_id: 159-F
priority: medium
status: queued
title: Correct the PR 411 closure record forward and add a per-directory P-020 assessment guard
updated_at: 2026-09-01T00:13:27.452813Z
---

docs/closure/pr411-p020-context-compaction-closure.md asserts "docs/plans | 65 | 642 KB | 0" further candidates and states "no further file qualifies". That is WRONG: re-assessment after the PR #412 merge found three genuine plan-consolidation candidates carrying an appended "## Plan Review" section, and docs/plans exceeds BOTH the max_files and max_size_kb triggers. The same counting error then recurred in the #412 floor artifact's own first draft - three instances of one root cause: assessing one target directory's criterion and generalizing the no-op to the others when each directory has its own Phase 2 criterion, and counting docs/memory root-only rather than recursively. BINDING H1: CORRECT FORWARD ONLY - do not silently rewrite the merged artifact; the correction is an appended, dated, clearly-marked, explicitly-superseding note that preserves the original claim verbatim alongside it (finding 6). PLAN REVIEW FINDING 2 (P1): the guard must assert the PER-DIRECTORY, RECURSIVE counting property against the SAME code path the compact-context skill uses rather than reimplementing the count, or - if no shared code path exists - as an assertion on the skill's own reported output, so guard and skill cannot drift into disagreement. SIZE/COMPLEXITY (two-axis, ruleset autoharness-stage-2h-v1): size M, complexity medium. SAFETY MODE (SHIP-9 plan H5, propagated into this executable task in review-fix cycle 2): careful - binding H1 forbids rewriting the merged closure artifact, so the edit must be strictly ADDITIVE (an appended superseding note preserving the original claim verbatim); no history rewrite, no deletion, and no destructive command. The guard addition is bounded to the per-directory recursive counting property and must not change any compact-context threshold or trigger. Source: 7645AE19. Plan: docs/plans/2026-08-31-ship9-backlog-docs-traceability-hygiene-plan.md