---
artifact_type: task
created_at: 2026-09-19T01:02:11.9446616Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 172.014-T
id: 172.015-T
labels:
    - implementation
    - inventory
    - schema
    - checkpoint
parent_id: 172-F
priority: high
status: queued
title: 'Resume-hint T8b (IMPLEMENTATION, inert): producer inventory, bounded markers and reconciling verifier'
updated_at: 2026-09-19T03:16:52.0189049Z
---

IMPLEMENTATION (INERT) - THE PRODUCER INVENTORY, THE BOUNDED MARKERS, AND THE RECONCILING VERIFIER.

Author schemas/checkpoint-producer-inventory.json as the TYPED inventory of every checkpoint producer surface: each entry declares a surface path, a producer kind, and a raw-allowed flag. EXACTLY ONE entry - the adapter implementation - is raw-allowed; every other producer must reach the create operation through the adapter.

Place the THREE BOUNDED MACHINE MARKERS at the real producer sites and implement the reconciling structural verifier in src/autoharness/checkpoint/. The verifier reconciles markers against inventory entries as a BIJECTION - a marker without an entry fails, an entry without a marker fails - so the inventory cannot silently rot and a newly added producer cannot pass by having been omitted from a hand-maintained list.

THE VERIFIER DISTINGUISHES PROHIBITION FROM PRODUCTION. It never treats a document that names the raw operation in order to forbid it as a producer; only a marked, inventoried surface is a producer. This is what replaces the broad free-text scan, which could not make that distinction in either direction.

THIS SHIPS INERT: the reconciliation assertion is present and passing, but the PROHIBITION ASSERTION IS NOT YET SWITCHED ON, because until the atomic activation the raw create path is still the legitimate producer path for both agents. Switching it on here would commit an intermediate state that forbids a path no producer has yet been wired away from.

Blocked by 172.014-T (an observed-failing suite exists first). Blocks 172.006-T. Scope: schemas/ and src/autoharness/checkpoint/. Stash 71200CBB. Plan docs/plans/2026-09-17-checkpoint-resume-hint-contract-plan.md (revision 7). Verdict manifest docs/reviews/2026-09-17-checkpoint-resume-hint-contract-plan-review.md (latest_attempt 07, verdict REMEDIATED-PENDING-REVIEW at plan revision 7, awaiting independent attempt 08).