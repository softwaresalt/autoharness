---
artifact_type: task
created_at: 2026-09-03T10:13:27.2042915Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.001-T
    - 160.020-T
id: 160.016-T
labels:
    - packaging
    - tests
    - characterization
    - baseline
parent_id: 160-F
priority: medium
status: queued
title: Characterization test harness for preserved payload invariants (completes at green-on-baseline)
updated_at: 2026-09-03T20:26:06.6645001Z
---

CHARACTERIZATION harness (T3b). TEST-FIRST (Constitution Principle II, NON-NEGOTIABLE) under the two-class contract in the plan section Test strategy.

WHY THIS TASK EXISTS: requiring EVERY composition case to be observed RED before implementation is FALSE for a whole class of cases. The wheel's [tool.hatch.build.targets.wheel.force-include] table already lists only templates, schemas, four .github/ subtrees, two instruction files, docs, and AGENTS.md, and `packages = ["src/autoharness"]` - so `.backlogit/`, `tests/` and `experiments/` are ALREADY ABSENT from the wheel today. Both `core-metadata-version = "2.4"` pins ALREADY EXIST. Demanding a red observation on these forces an author either to fabricate a failure or to weaken a correct assertion until it breaks. MANUFACTURED RED IS WORSE THAN NO RED: it destroys the signal for the cases where red is real.

COMPLETION CRITERION - GREEN ON BASELINE, NOT RED: this task is COMPLETE when every CHARACTERIZATION case listed below is AUTHORED and OBSERVED PASSING AGAINST THE BASELINE (UNTRIMMED) BUILD captured by 160.001-T, and that green-on-baseline evidence is RECORDED in the task's completion evidence. A CHARACTERIZATION case that is RED on arrival is a DEFECT OF THIS TASK - it means the baseline claim is false and must be corrected before anything is built on it. It is NOT a finding about the baseline and MUST NOT be "fixed" by changing production code.

CASES AUTHORED HERE - EXACTLY FOUR, all class CHARACTERIZATION, each naming its preservation owner. This is the complete and closed set for this task; the plan's canonical case table attributes exactly these four rows to T3b and no others.

COUNT RECONCILIATION, SIX -> SEVEN -> FOUR: cycle 4 (finding 5) moved test_plugin_source_path_is_tracked_and_fetchable here from 160.005-T, taking the count to seven. CYCLE 5 REMOVES THREE: the two sdist cases (test_sdist_contains_required_runtime_set, test_sdist_includes_build_definition) are DELETED because the sdist channel was de-scoped as a P-021 C1 expansion (finding 1) - their content is preserved as evidence in deferred stash entry 99818C6D; and test_wheel_contains_required_runtime_set is MOVED OUT to 160.005-T because cycle-5 finding 12 established it is NOT baseline-true. Arithmetic: 7 - 2 (sdist) - 1 (reclassified out) = 4.
1. test_wheel_excludes_backlogit_explicitly (AC3) -> preservation owner 160.006-T. Green on baseline: force-include already lists only the runtime set, so .backlogit/ is already absent.
2. test_wheel_excludes_dev_directories (AC3) -> preservation owner 160.006-T. Green on baseline: tests/, experiments/, references/ are already absent from the wheel.
3. test_core_metadata_version_pins_preserved (I4, V2) -> preservation owner 160.006-T, which is now the SOLE editor of pyproject.toml. Assert BOTH the wheel target's pin AND the sdist target's pin in this one case. Green on baseline: both pins already exist. WHY THE SDIST PIN IS STILL ASSERTED THOUGH THE SDIST CHANNEL IS OUT OF SCOPE: the sdist target lives in the SAME FILE 160.006-T edits, and `core-metadata-version` is currently that target's ONLY key - so a wheel-table rewrite that replaces rather than extends could drop it silently and break publishing for BOTH artifacts. This assertion is an UNCHANGED-GUARD protecting out-of-scope content from an in-scope edit; it is not sdist work and does not re-import the sdist channel.
4. test_plugin_source_path_is_tracked_and_fetchable (AC3c) -> preservation owner 160.007-T. RECLASSIFIED FROM RED-FIRST AND MOVED HERE FROM 160.005-T IN CYCLE 4, FINDING 5. Green on baseline: the baseline marketplace.json `source: "."` resolves to the repository ROOT, which IS tracked in git and IS present in every consumer checkout, so the tracked-and-fetchable property ALREADY HOLDS. It could not have been RED-FIRST: the property is baseline-true, and branch (a) of the 160.002-T spike PRESERVES `source: "."` unchanged, so the case would have been green at authoring, green throughout, and would never have transitioned - and forcing a red would have required weakening the assertion until the baseline broke it. Its real job is PRESERVATION and it is BRANCH-NEUTRAL: whatever branch the spike selects, the committed `source` must still resolve to a TRACKED, consumer-fetchable path. This is the guard against repointing `source` at a gitignored or untracked build-output path - `dist/.gitignore` contains `*`, so a `source` under dist/ is absent from every consumer checkout and the install simply fails. Assert the path is TRACKED, not merely that it exists locally.

THIS TASK AUTHORS NO RED-FIRST CASE. All 32 RED-FIRST cases are authored elsewhere: 22 by 160.005-T (T3a), and 10 by the verification families 160.008-T (T9), 160.012-T (T10), 160.013-T (T11), 160.009-T (T12) and 160.010-T (T13). THE CANONICAL LEDGER TOTAL IS 46 = 32 RED-FIRST + 14 CHARACTERIZATION, distributed per author (RED-FIRST/CHARACTERIZATION), stated with BOTH the plan's T-label AND the task ID so the two can never drift apart:
- T3a / 160.005-T: 22/0
- T3b / 160.016-T (this task): 0/4
- T9  / 160.008-T: 1/2
- T10 / 160.012-T: 1/2
- T11 / 160.013-T: 5/3
- T12 / 160.009-T: 1/3
- T13 / 160.010-T: 2/0
Column totals: 22+0+1+1+5+1+2 = 32 RED-FIRST; 0+4+2+2+3+3+0 = 14 CHARACTERIZATION; 46 cases.
THE CYCLE-5 MAPPING PRINTED HERE WAS WRONG AND IS CORRECTED IN CYCLE 6 (finding 15). It read `160.009-T 1/2, 160.010-T 1/2, 160.011-T 5/3, 160.012-T 1/3, 160.013-T 2/0` - which attributed 5 RED-FIRST and 3 CHARACTERIZATION cases to 160.011-T, THE DOCUMENTATION TASK (T15), WHICH AUTHORS NO TEST CASE AT ALL, and simultaneously misattributed the counts of 160.009-T, 160.010-T, 160.012-T and 160.013-T by rotating them against the wrong IDs. The defect was invisible because the COLUMN TOTALS still summed to 32/14/46; only a per-author join against the plan's canonical case table exposes it. USING T-LABEL PLUS TASK ID, as above, is what makes the same rotation detectable next time. (The cycle-4 figures - 52 = 35 + 17, with 25 red from 160.005-T - are SUPERSEDED and must not be cited.) If a case being authored here cannot be observed green on the baseline, it does not belong in this task - route it to 160.005-T instead of weakening it.

TESTS RUN AGAINST THE BUILT ARTIFACT, NOT THE SOURCE TREE (verification V1). Unpack the ACTUAL BUILT WHEEL and resolve the ACTUAL PLUGIN PAYLOAD; a source-tree assertion cannot detect a packaging defect (risk R1). The one exception is case 3, which asserts pyproject.toml TOML keys and is therefore necessarily a source-file assertion - it guards the BUILD DEFINITION rather than a built payload, and that is why it is the only source-tree case in this set.

CLASSIFICATION IS AUTHORED DATA, NOT COMMENTARY: each case carries its declared class in the test file itself, so 160.017-T's transition ledger can assert the pair of observations the class implies. A case with no declared class is a failure. Class is declared PER CASE; there is no family-level class label.

BLOCKED BY 160.001-T AND 160.020-T. 160.001-T: a green-on-baseline observation is meaningless without the baseline build to observe it against. 160.020-T (ADDED IN CYCLE 9): a green-on-baseline observation is equally meaningless if it cannot be TAKEN - this task's completion condition is an OBSERVED PASS recorded under the plan's evidence contract, and that observation runs through `uv run python -m pytest`. Measured before cycle 9, `pytest` was configured in `pyproject.toml` but ABSENT FROM `uv.lock`, and ci.yml invoked `unittest discover`; 160.020-T declares the pinned dependency, regenerates the lock, and moves CI to the canonical runner. Together with 160.005-T, this task is one of the two EARLIEST NODES IN THE DAG THAT MUST EXECUTE A TEST, so blocking it on 160.020-T places the toolchain fix before every downstream test author and consumer. THIS TASK INVENTS NO PYTEST SETUP OF ITS OWN and MUST NOT edit `pyproject.toml`, `uv.lock`, or any workflow file; it RELIES on its 160.020-T prerequisite.

BLOCKS 160.008-T, 160.012-T, 160.013-T, 160.009-T, 160.010-T and, transitively, every wiring task - so no invariant can be broken before it has been pinned.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. This task authors test files under tests/ ONLY. It MUST NOT author or modify the manifest, either schema file, schema_contracts.py, build_support/, pyproject.toml, marketplace.json, plugin-payload/, or release.yml. BASELINE-FIDELITY CHECKPOINT: cases are observed against the baseline untrimmed build, never against a partially-changed tree. ITS ONLY AUTHORIZED WRITE OUTSIDE ITS OWN TEST FILES IS ITS FIRST-OBSERVATION RECORD at docs/audits/2026-09-03-ship10-payload-evidence/observations/T3b.json (see FIRST-OBSERVATION DESTINATION below); this is stated here, in the operative write-surface clause, because a destination named only further down the record would still read as unauthorized under freeze-scope (extended review-fix cycle 4, finding 10). NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

BOUNDED RAW LOG - SECOND AUTHORIZED EVIDENCE WRITE (added in review-fix cycle 5, findings 10 and 15; local field reference removed in cycle 8, item 7). Every observation this task records is backed by a RAW LOG this task also writes, into ITS OWN SUBDIRECTORY AND NOWHERE ELSE: docs/audits/2026-09-03-ship10-payload-evidence/observations/logs/T3b/**. This path is authorized in the plan's H2 writable-surface table, so writing it does NOT violate freeze-scope, and 160.017-T can verify the evidence without following a reference outside the single audit directory. WHY THE LOG IS REQUIRED: cycle 4 had 160.017-T accept each producer's SELF-DECLARED outcome, which verifies nothing - a task that never ran would be indistinguishable from a clean pass. The log is the artifact that makes the claim checkable. The log also carries the source-provenance capture the plan's canonical contract requires before the test command runs, and the observation is taken on a CLEAN, FULLY COMMITTED TREE.
BOUNDS (binding, enforced by 160.017-T): PER LOG <= 256 KiB AND <= 2,000 lines - a run exceeding either limit is truncated head-and-tail with an explicit truncation marker (how the record signals truncation and its pre-truncation counts is defined in the plan's canonical contract, not here); PER CASE at most 3 logs (first observation, second observation, one retry); PER SHIPMENT at most 200 files under the logs root, none of them binary; RETENTION until 160.017-T completes and the shipment closes.
OBSERVATION-RECORD CONTENT IS DEFINED ONCE, IN THE PLAN, AND IS NOT RESTATED HERE (cycle 6, finding 23; TIGHTENED IN CYCLE 8, ITEM 7). The SINGLE CANONICAL definition is the field table in docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section "Evidence-verification contract for 160.017-T". Seven task records each carried their own restatement and the copies had ALREADY DIVERGED, so a producer reading only its own record would have written a record 160.017-T rejects. THIS RECORD STATES ONLY WHAT IS TASK-SPECIFIC: which cases it produces records for, and which log subdirectory is its own. THIS RECORD ENUMERATES NO FIELD, NAMES NO FIELD, AND DEFINES NO FIELD TYPE OR VALIDATION RULE - NOT EVEN PARTIALLY. Cycle 7 deleted the "ADDITIONAL RECORD FIELDS" restatement but left a second local enumeration standing under a different heading, which was the same defect wearing a different label; cycle 8 deletes that one too. A PARTIAL COPY DRIFTS FROM THE CANONICAL TABLE EXACTLY AS A FULL COPY DOES. FOR EVERY FIELD, ITS TYPE, AND ITS ACCEPTANCE RULE, READ THE PLAN'S CANONICAL TABLE - there is no second source. 160.017-T verifies each record INDEPENDENTLY against its evidence and REJECTS any record that is missing a field, malformed, or inconsistent; it does not warn and does not skip.
This task WRITES its own JSON and its own logs and NOTHING ELSE outside its test files; it MUST NOT write into another producer's subdirectory, and MUST NOT write the ledger.

FIRST-OBSERVATION DESTINATION (authorized, binding; added in extended review-fix cycle 4, finding 7). The green-on-baseline observations this task produces are a NON-DEFERRABLE acceptance condition consumed by 160.017-T, so they need a durable, authorized home; cycle 3 left them with no legal destination under freeze-scope. They are written to docs/audits/2026-09-03-ship10-payload-evidence/observations/T3b.json - a subdirectory of the SINGLE bounded evidence surface already authorized for this shipment, NOT a new store. RECORD CONTENT IS NOT SPECIFIED HERE (CYCLE 8, ITEM 7). The observation-record field set, every field's type, and every acceptance rule are defined ONCE, in the plan's canonical Evidence-verification contract (docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md, section "Evidence-verification contract for 160.017-T"). READ THAT CONTRACT; there is no second source and no local list. This record contributes ONLY its exact case set (the four CHARACTERIZATION cases enumerated above), its destination path, and its log bounds. Records are APPEND-ONLY within the shipment and written ONLY by this task. 160.017-T joins, verifies and rejects per that canonical contract.

ACCEPTANCE: all four CHARACTERIZATION cases above authored; every one observed GREEN against the baseline build; green-on-baseline evidence recorded per case EXACTLY AS THE PLAN'S CANONICAL EVIDENCE-VERIFICATION CONTRACT REQUIRES (cycle 8, item 7 - the earlier wording here pointed at a local field list that no longer exists and must not be reintroduced); the preservation-owner mapping above reproduced in the completion note so no case is orphaned.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size S, complexity low. RE-CONFIRMED IN CYCLE 5 AT FOUR CASES: two exclusion assertions over one already-built artifact, one TOML-key assertion, and one git-tracked-path check, with no judgement calls beyond artifact unpacking. Removing three cases lowers volume within the same size band and removes no failure domain, so the two-axis assessment is unchanged.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T3b)
