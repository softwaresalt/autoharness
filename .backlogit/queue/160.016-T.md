---
artifact_type: task
created_at: 2026-09-03T10:13:27.2042915Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 160.001-T
id: 160.016-T
labels:
    - packaging
    - tests
    - characterization
    - baseline
parent_id: 160-F
priority: medium
status: queued
title: Characterization test harness for preserved payload invariants (completes at green-on-baseline)
updated_at: 2026-09-03T15:36:47.9229835Z
---

CHARACTERIZATION harness (T3b). TEST-FIRST (Constitution Principle II, NON-NEGOTIABLE) under the two-class contract in the plan section Test strategy.

WHY THIS TASK EXISTS: requiring EVERY composition case to be observed RED before implementation is FALSE for a whole class of cases. The wheel's [tool.hatch.build.targets.wheel.force-include] table already lists only templates, schemas, four .github/ subtrees, two instruction files, docs, and AGENTS.md, and `packages = ["src/autoharness"]` - so `.backlogit/`, `tests/` and `experiments/` are ALREADY ABSENT from the wheel today. Both `core-metadata-version = "2.4"` pins ALREADY EXIST. Demanding a red observation on these forces an author either to fabricate a failure or to weaken a correct assertion until it breaks. MANUFACTURED RED IS WORSE THAN NO RED: it destroys the signal for the cases where red is real.

COMPLETION CRITERION - GREEN ON BASELINE, NOT RED: this task is COMPLETE when every CHARACTERIZATION case listed below is AUTHORED and OBSERVED PASSING AGAINST THE BASELINE (UNTRIMMED) BUILD captured by 160.001-T, and that green-on-baseline evidence is RECORDED in the task's completion evidence. A CHARACTERIZATION case that is RED on arrival is a DEFECT OF THIS TASK - it means the baseline claim is false and must be corrected before anything is built on it. It is NOT a finding about the baseline and MUST NOT be "fixed" by changing production code.

CASES AUTHORED HERE - EXACTLY SIX, all class CHARACTERIZATION, each naming its preservation owner. This is the complete and closed set for this task; the plan's canonical case table attributes exactly these six rows to T3b and no others.
1. test_wheel_excludes_backlogit_explicitly (AC3) -> preservation owner 160.006-T. Green on baseline: force-include already lists only the runtime set, so .backlogit/ is already absent.
2. test_wheel_excludes_dev_directories (AC3) -> preservation owner 160.006-T. Green on baseline: tests/, experiments/, references/ are already absent from the wheel.
3. test_wheel_contains_required_runtime_set (AC4) -> preservation owner 160.006-T. Green on baseline: the runtime set is already force-included.
4. test_core_metadata_version_pins_preserved (I4, V2) -> preservation owners 160.006-T (WHEEL target) AND 160.019-T (SDIST target). Assert BOTH targets in this one case. Green on baseline: both pins already exist. The sdist pin is the fragile one: it is currently the sdist target's ONLY key, so a table rewrite that REPLACES rather than EXTENDS the target drops it silently and breaks publishing.
5. test_sdist_contains_required_runtime_set (AC3b, AC4) -> preservation owner 160.019-T. Green on baseline: hatchling's default sweep already contains the runtime set.
6. test_sdist_includes_build_definition (AC3b, AC11) -> preservation owner 160.019-T. Green on baseline: pyproject.toml is in the default sweep. A source distribution without its build definition is not buildable.

THIS TASK AUTHORS NO RED-FIRST CASE. All 35 RED-FIRST cases are authored elsewhere: 25 by 160.005-T, and 10 by the verification families 160.008-T, 160.012-T, 160.013-T, 160.009-T and 160.010-T. If a case being authored here cannot be observed green on the baseline, it does not belong in this task - route it to 160.005-T instead of weakening it.

TESTS RUN AGAINST THE BUILT ARTIFACT, NOT THE SOURCE TREE (verification V1). Unpack the actual built wheel and the actual built sdist. A source-tree assertion cannot detect a packaging defect (risk R1), and a wheel-only assertion cannot detect an sdist defect.

CLASSIFICATION IS AUTHORED DATA, NOT COMMENTARY: each case carries its declared class in the test file itself, so 160.017-T's transition ledger can assert the pair of observations the class implies. A case with no declared class is a failure. Class is declared PER CASE; there is no family-level class label.

BLOCKED BY 160.001-T: a green-on-baseline observation is meaningless without the baseline build to observe it against.

BLOCKS 160.008-T, 160.012-T, 160.013-T, 160.009-T, 160.010-T and, transitively, every wiring task - so no invariant can be broken before it has been pinned.

SAFETY MODE (SHIP-10 plan H2): careful + freeze-scope. This task authors test files under tests/ ONLY. It MUST NOT author or modify the manifest, either schema file, schema_contracts.py, build_support/, pyproject.toml, marketplace.json, plugin-payload/, or release.yml. BASELINE-FIDELITY CHECKPOINT: cases are observed against the baseline untrimmed build, never against a partially-changed tree. NO-PUBLISH. Full mode text: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md section Plan Hardening -> H2.

ACCEPTANCE: all six CHARACTERIZATION cases above authored; every one observed GREEN against the baseline build; green-on-baseline evidence recorded per case; the preservation-owner mapping above reproduced in the completion note so no case is orphaned.

SIZE/COMPLEXITY (two-axis, ruleset ah-stage-sizing-v1): size S, complexity low. Six assertions over two already-built artifacts, with no judgement calls beyond artifact unpacking.
Plan: docs/plans/2026-09-03-minimal-copilot-plugin-payload-plan.md (T3b)