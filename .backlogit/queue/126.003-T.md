---
artifact_type: task
created_at: 2026-08-15T05:20:20.1705431Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: stage-2h-v1
    size_source: agent
dependencies:
    - 126.002-T
    - 126.001-T
id: 126.003-T
labels:
    - schema
    - backward-compatible
owner: stage
parent_id: 126-F
priority: high
status: queued
title: Align backlog storage-root schema examples, descriptions and defaults
updated_at: 2026-08-15T09:39:41.5545442Z
---

Align the backlog storage-root REFERENCES in schemas/backlog-tool-registry.schema.json, schemas/harness-config.schema.json and schemas/workspace-profile.schema.json with the dual-root reality. RECLASSIFIED (PR #339 Copilot review, comment 3788712399): the original 'widen validation with a version bump' premise was FALSE. Verified against main: harness-config.schema.json:107-114 declares 'directory' as an unconstrained 'type: string' whose examples ALREADY list .backlog; backlog-tool-registry.schema.json:22-26 is likewise an unconstrained string; workspace-profile.schema.json:224 mentions .backlogit/** only as prose inside the docs_only_paths description. No occurrence restricts the storage root, so every .backlog document already validates and there is NO acceptance contract to widen. This task is therefore limited to DESCRIPTIVE/DEFAULT updates (examples, descriptions, stated defaults) and MUST NOT carry a schema version bump on its own -- a bump that changes no accepted-document set is pure migration/compatibility churn. CONDITIONAL ESCALATION: if and only if 126.001-T finds a genuine validation constraint on the storage root, the widening becomes real and MUST then follow docs/compound/2026-08-08-schema-mirror-mutated-in-place-without-version-bump.md in full -- additive widening, a versioned schema mirror, AND a corresponding src/autoharness/schema_contracts.py update. Removing .backlogit from any enum or example set remains forbidden either way. EXCLUSION (H5, verbatim): this task MUST NOT run 'backlogit migrate --workspace-dir', MUST NOT rename this repository's .backlogit directory, and MUST NOT change .autoharness/backlog-registry.yaml's directory value. That migration is operator-gated and must never run under dark-mode automation.