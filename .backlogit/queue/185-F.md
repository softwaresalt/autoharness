---
artifact_type: feature
created_at: 2026-09-21T01:38:51.2789951Z
custom_fields:
    harness_status: pending
id: 185-F
labels:
    - harness-architect
    - p004-conformance
    - prerequisite
    - stage-2026-09-20-portfolio
status: queued
title: Harness-architect P-004 conformance release (prerequisite for 187-S lifecycle)
updated_at: 2026-09-21T01:38:51.2789951Z
---

PREREQUISITE FEATURE. Correct the harness-architect actor so that it is BEHAVIOURALLY conformant with policy P-004, not merely installed. P-004 requires that the harness produce a build check that exits 0 and a test command that exits non-zero with expected failure markers for every test function; the installed actor currently prescribes a runner P-004 does not accept. Until this feature ships, NO unit in this portfolio may claim P-004 is satisfiable, and 187-S (covering feature 181-F, the Ship harness lifecycle) is BLOCKED behind it.

ROOT CAUSE, VERIFIED AGAINST THE REPOSITORY AND NOT TRANSCRIBED FROM THE REVIEW. Two premises in the review's remediation sketch were STALE and are corrected here. (1) THE MANIFEST ALREADY REGISTERS THE ACTOR: .autoharness/harness-manifest.yaml already carries an artifacts entry for .github/skills/harness-architect/SKILL.md naming template skills/harness-architect/SKILL.md.tmpl at a matching checksum, so 'register the actor in the manifest' is ALREADY DONE and MUST NOT be re-done. (2) THE TEMPLATE IS ALREADY CORRECT: templates/skills/harness-architect/SKILL.md.tmpl line 126 already reads the placeholder {{TEST_COMMAND}}, not a hard-coded runner, so the template is NOT the defect and MUST NOT be rewritten to hard-code any ecosystem runner. THE ACTUAL DEFECT IS INSTALL-TIME VARIABLE RESOLUTION PRECEDENCE: the render performed by external commit 07b4be79263252b1820701fd123d0aed85c1db2a resolved {{TEST_COMMAND}} from the STALE .autoharness/workspace-profile.yaml field test.command ('pytest') instead of from the AUTHORITATIVE manifest variables_used.TEST_COMMAND ('PYTHONPATH=src python -m unittest discover -s tests'), which contradicts decision D9-C. The installed .github/skills/harness-architect/SKILL.md line 126 therefore reads pytest, a runner this workspace does not configure at all - the manifest itself records that the profile fields are deliberately overridden. SECOND, INDEPENDENT DEFECT: the template emits a SINGLE {{UNIMPLEMENTED_MARKER}}, while P-004 requires expected failure markers FOR EVERY TEST FUNCTION, so per-generated-test marker correlation is a genuine gap and not a restatement of the first defect.

07b4be79263252b1820701fd123d0aed85c1db2a IS STRUCTURAL INSTALLATION ONLY AND IS NOT BEHAVIORAL P-004 SATISFACTION. It made the actor PRESENT and manifest-registered. PRESENCE IS NOT CONFORMANCE. No record in this unit, in 181-F/187-S, or in any plan may claim the actor is P-004-compliant until this unit has shipped and verification has been observed.

SCOPE BOUNDARY. This feature owns the ACTOR CONFORMANCE correction ONLY. It does NOT own the Ship pre-task harness lifecycle, the harness-surface resolver, or the HARNESS_READY/NO_HARNESS state contract - those belong to 181-F/187-S and are strictly downstream. It edits NO policy text, consumes NO bootstrap grant, uses NO --force, and writes NO waiver or force-audit entry. The canonical dogfood TEST_COMMAND for this workspace is exactly 'PYTHONPATH=src python -m unittest discover -s tests' and the canonical build check is exactly 'python -m py_compile src/autoharness/cli.py'; NO ecosystem-specific runner may be hard-coded into any template.

DELIVERS: a RED contract test for exact P-004 command and marker behaviour (185.001-T); the install-time resolution-precedence correction that makes manifest variables_used outrank the workspace profile (185.002-T); the stale executable-gate correction in the workspace profile (185.003-T); per-generated-test marker correlation in the template's red-phase contract (185.004-T); the atomic installed-actor re-render plus manifest checksum refresh (185.005-T); and final verification (185.006-T).

Shipment 191-S. Blocks 187-S. Provenance: independent terminal plan-review attempt 07 finding S14 (P0) against docs/plans/2026-09-18-ship-harness-lifecycle-foundation-plan.md revision 8, artifact docs/reviews/review-history/2026-09-18-ship-harness-lifecycle-foundation-plan-review-attempt-07.md. Created by Stage at plan revision 9 under fresh explicit operator authorization. Review state for the governing plan is POINTER-ONLY: sole authority is docs/reviews/2026-09-18-ship-harness-lifecycle-foundation-plan-review.md.

HARNESS-SURFACE DECLARATION LABELS ARE DELIBERATELY ABSENT ON THIS FEATURE'S TASKS, AND THAT IS NOT AN OVERSIGHT. The harness-surface:<id> label vocabulary is consumed by the resolver resolve_harness_surfaces, which does not yet exist: it is built by 181-F and shipped by 187-S, which is DOWNSTREAM of this unit. At the time 191-S executes there is no resolver to read a declaration, so the labels would be inert. Labelling these tasks harness-surface:harness-architect would also be circular - they cannot require a CONFORMANT harness-architect actor in order to be allowed to MAKE it conformant - and labelling them harness-surface:none would be false, since they do write Python under src/. The honest position is that this unit predates the declaration mechanism. Once 187-S has shipped, any RE-harvested successor of this unit must carry an explicit declaration.
