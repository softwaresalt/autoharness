---
artifact_type: feature
created_at: 2026-08-10T07:07:43.8829951Z
custom_fields:
    harness_status: pending
id: 117-F
labels:
    - supervisor
    - control-plane
    - copilot-cli
    - candidate-a
    - 34D50F2D
    - plan-1
    - fast-track
    - P-006
links:
    - link_type: related_to
      target_id: 118-F
    - link_type: related_to
      target_id: 119-F
    - link_type: related_to
      target_id: 120-F
references:
    - docs/plans/2026-08-09-copilot-cli-supervisor-control-plane-plan.md
    - docs/plans/2026-08-09-copilot-cli-supervisor-control-plane-hardening.md
    - docs/reviews/2026-08-09-copilot-cli-supervisor-control-plane-review.md
    - .backlogit/archive/004-SP.md
    - docs/decisions/2026-08-09-composability-single-source-of-truth-spike.md
    - start.ps1
    - start.sh
status: queued
title: Local Copilot CLI supervisor / control-plane runtime (Plan 1, fast-track)
updated_at: 2026-08-10T08:17:00.5243211Z
---

PRODUCT UMBRELLA (childless) for Plan 1. Turn autoharness into a LOCAL, operator-driven supervisor / control-plane runtime for long-horizon Copilot CLI workloads. Copilot CLI is PRESERVED as the reasoning/agent-execution engine; autoharness owns lifecycle, policy, and supervision only and implements NO model action/observation reasoning loop. Move the orchestration policy currently duplicated and untested across start.ps1 and start.sh into tested, typed shared Python application services under src/autoharness/supervise/, with start.ps1 / start.sh reduced to compatibility shims over a new `autoharness run` CLI entry point. Scope: workspace bootstrap, sidecar lifecycle/preflight, child process/PTY management (ConPTY abstraction on Windows), session state machine, local structured commands/approvals, progress/events, cancellation, controlled restart/resume, logs/checkpoints, secret redaction, workspace/session locking, and recovery. Consumes stash 34D50F2D candidate (a) under the reconciled spike disposition (004-SP: CONDITIONAL PROCEED -> PROCEED under the clarified scope "supervising an external agent runtime is in scope; implementing a new agent runtime is not"). EXCLUDED / DEFERRED to Plan 2: Gradio, Microsoft devtunnel, remote UI/control, remote authentication, remote approvals, browser terminal streaming, and any remote service.

ROLE CHANGE (2026-08-10, plan-review cycle 3 of 3, F14 structural elimination). 117-F NO LONGER PARENTS ANY TASK. All 19 implementation tasks were re-parented, via the supported AdoptItem lifecycle operation, onto three ROOT per-shipment covering features — 118-F (S1), 119-F (S2), 120-F (S3) — each of which is FULLY COVERED by its own shipment manifest. 117-F is retained purely as the product-level record and grouping anchor, connected to the three implementation features by NON-HIERARCHICAL `related_to` semantic links. This is deliberate and load-bearing: backlogit's featureScopeRoots walks parent_id ONLY, so as a childless, non-ancestor artifact 117-F is never discovered by any shipment close and is structurally incapable of triggering, or suffering, a cascade. 117-F is an explicit member of the FINAL shipment 129-S only, so the program record is marked done and archived by the engine when the last shipment ships — no post-close operator action, no adopt_item, no manual repair anywhere in the chain. P-006 plan-harden HARDENED (H1-H10, incl. H10.5 per-shipment fully-covered root covering features); plan-review CURRENT VERDICT: BLOCKED - 0 unresolved P0, FOURTEEN unresolved P1 (F16-F29), 3 of 3 cycles used. (The cycles-1-3 PASS at 0 P0 / 0 P1 is HISTORICAL and applies only to findings F1-F15; F16-F29 were raised AFTER the budget was exhausted by successive PR #325 Copilot reviews and each BLOCKS rather than consuming a cycle.) NO SHIPMENT IS SAFELY CLAIMABLE - see the Review section below for the full gate map. DO NOT READ THIS SUMMARY AS AN APPROVAL TO CLAIM.

<!-- BEGIN:Hardening -->
P-006 plan-harden HARDENED. H1 characterize-before-migrate is a hard ordering constraint enforced by blocks edges. H2 fail-closed invariants (lock contention REFUSED, path containment abort, redaction drop-not-passthrough, illegal-transition raise, non-interactive approval never auto-approves, restart budget default 0, PTY degrade-with-warning, unresolvable Copilot fatal). H3 verbatim exit-status fidelity with a {0,1,2,42,130} round-trip test; `|| true` and -ErrorAction SilentlyContinue prohibited around the child launch. H4 argv-array spawn only, no shell=True, verbatim passthrough. H5 secret redaction is a single choke point with no raw-write API and registers resolved secret VALUES, not just patterns. H6 authority containment (journal is not a checkpoint; backlogit sole backlog/checkpoint authority; Engram read-only and never read by a supervisor decision; config.yaml routing authority). H7 anti-drift guard: zero event subscribers beyond journal + console renderer, no bind/listen in supervise/, import ban on gradio/fastapi/flask/uvicorn/aiohttp/devtunnel clients. H8 per-task de-risking for the FOUR complexity-high tasks (T7, T11, T15, T18 = 119.002-T, 119.006-T, 120.004-T, 120.007-T; the earlier 'three' was an undercount contradicted by H8's own table and by the queued task fields, corrected 2026-08-10). H9 explicit backward-compatibility contract. H10 strict serial shipment gating S1 -> S2 -> S3 with S1 zero-behavior-change and S2 unwired-library constraints.

H10.5 (2026-08-10 cycle-3 re-hardening, SUPERSEDES H10.4) PER-SHIPMENT FULLY-COVERED ROOT COVERING FEATURES. The single-covering-feature topology is structurally unsafe on backlogit 1.8.0 and is abandoned. Every shipment now owns a dedicated ROOT covering feature (118-F / 119-F / 120-F) whose children are EXACTLY that shipment's tasks, and the feature is an EXPLICIT member of its own manifest. Two invariants must both hold for every shipment: (i) FULL COVERAGE - every descendant of the covering feature is in that shipment's release scope, so returnUnreleasedFeatureItems finds nothing to return and clears no parent_id; (ii) ROOT PLACEMENT - the covering feature has no parent feature, so featureScopeRoots' upward walk cannot escape the shipment and reach a sibling shipment's scope. Nesting the per-shipment features under 117-F would violate (ii) and reintroduce the cascade in a worse form. The product umbrella 117-F is childless and grouped by non-hierarchical related_to links, which featureScopeRoots does not traverse; it is a member of the FINAL shipment 129-S only, so it closes with the program. Consequence: adopt_item, manual re-parenting, feature reactivation, and any post-close repair are ELIMINATED from Ship's path entirely - nothing in the close sequence needs repairing. Proven against the real backlogit 1.8.0 engine by docs/spikes/2026-08-09-plan1-shipment-topology-proof/sim-shipment-closure.ps1 (64/64 assertions; ARM A control reproduces the old defect at 14/14 orphaned, ARM B redesign returns zero items on all three closes).
<!-- END:Hardening -->

<!-- BEGIN:Non-Goals -->
1. No Gradio, Microsoft devtunnel, remote UI/control, remote authentication, remote approvals, browser terminal streaming, or remote service of any kind (DEFERRED to Plan 2, which has no implementation feature/tasks/shipment and is NOT a dependency of this feature). 2. No model action/observation reasoning loop, sequential model pipelining, or stderr-routed-back-to-the-model correction. 3. No native autoharness MCP server - explicit non-goal absent a concrete consumer. 4. No persistent daemon, multi-workspace scheduler, database, web framework, or plugin registry. 5. No Python+Go split now; the ChildProcess Protocol keeps the supervisor replaceable and Go is re-evaluated only if a future persistent multi-workspace daemon is actually required. 6. NOT candidate (c) - no background Verification and Compaction layer; the event bus is a hook surface only and Engram gains no authority. 7. No changes to backlogit, Engram, or graphtor. 8. No hardcoded model names; .autoharness/config.yaml remains model-routing authority. 9. No refactor of the existing 10 top-level / 17 leaf CLI commands; `autoharness run` is additive. 10. No parallel execution or extra worktree (P-001 / P-016 preserved). 11. No adopt_item, manual re-parenting, feature reactivation, or any post-close backlog repair by Ship - the decomposition is structured so none is ever required.
<!-- END:Non-Goals -->

<!-- BEGIN:Review -->
plan-review CURRENT VERDICT: **BLOCKED** - 0 unresolved P0, **14 unresolved P1s (F16-F29)**, 3 of 3 cycles used (limit reached). NO SHIPMENT IS SAFELY CLAIMABLE: F17 AND F27 gate 127-S; F18 + F19 + F22 + F23 + F24 gate 128-S (F22 may also touch 127-S); F16 + F20 + F21 + F25 gate 129-S; F28 and F29 gate 128-S; and F26 gates ALL THREE SHIPMENTS, because closure is on every shipment's path. The PASS below is HISTORICAL - it describes cycles 1-3 only and stands for findings F1-F15; it was superseded when PR #325 Copilot review raised F16-F18 (see CYCLE 4 at the end of this section). HISTORICAL CYCLES 1-3 VERDICT WAS PASS, 0 unresolved P0, 0 unresolved P1. CYCLE 1 (2026-08-09): F1/F2 P0 (un-reconciled spike NO-GO; unguarded network-listener drift into deferred Plan 2 scope) RESOLVED; F3-F7 P1 RESOLVED; F8-F12 P2 RESOLVED. CYCLE 2 (2026-08-10, post-harvest review-fix from PR #325): F13 P0 cascade-close hazard (covering feature 117-F was an explicit member of the 124-S manifest) RESOLVED by removing it; F14 P1 residual parent_id-clearing asymmetry accepted with an adopt_item mitigation. CYCLE 3 (2026-08-10, FINAL): the F14 disposition was REOPENED and REJECTED - re-parenting is not in Ship's Role Boundary (fail-closed P-010 makes it forbidden/unclassified) and fragile post-close repair violates reliability-first design, so F14 was an unresolved P1 and the PASS was not truthful. F14 is now STRUCTURALLY ELIMINATED, not mitigated: per-shipment fully-covered ROOT covering features (H10.5) mean no feature ever has an unreleased child when its shipment closes, so returnUnreleasedFeatureItems has nothing to return. Proven by a disposable-fixture lifecycle simulation against the real backlogit 1.8.0 engine. CYCLE 4 (2026-08-10, POST-BUDGET): the PR #325 Copilot reviews of HEAD 48368657 and 91538e74 raised new P1 findings AFTER the 3-cycle budget was exhausted (ten in total across cycles 4-12), so the verdict is now BLOCKED, NOT PASS. F16 (P1, OPEN): T18's rollback requirements are mutually exclusive - the shim may contain no legacy shell policy (hard DoD #2) yet must honor an AUTOHARNESS_SUPERVISOR=0 escape hatch executing the legacy inline path without redeploy; affects 120.007-T/120.008-T, gates 129-S. F17 (P1, OPEN): the plan's "two divergent implementations of the same policy" premise is factually wrong for start.sh, which lacks the ENGRAM_DATA_DIR default, GITHUB_PERSONAL_ACCESS_TOKEN handling, COPILOT_USE_REMOTE/--remote logic, backlogit sync and Engram pre-warm; start.ps1:65 also sets the PAT unguarded. This makes the S1 characterization acceptance criteria in 118.001-T and 118.002-T unsatisfiable and therefore BLOCKS THE ELIGIBLE CURSOR 127-S. F18 (P1, OPEN): Plan section 3.2's diagram routes cancellation CANCELLING -> EXITED, bypassing DRAINING, contradicting its own prose rule and 119.006-T, and 119.003-T's transition table is incomplete; gates 128-S. F19 (P1, OPEN): 119.003-T must emit SessionPhaseChanged but that event type is only defined in 119.004-T, whose dependency edge points back to 119.003-T, so the declared order is unimplementable as written; gates 128-S. F20 (P1, OPEN): 120.002-T and plan section 3.1 forbid mutation/sidecar writes while mandating a backlogit sync and Engram bind/sync pre-warm; gates 129-S. F21 (P1, OPEN, raised on HEAD 66f1220f): the fail-closed approval channel can be omitted from the shipped runtime without any task failing - 120.005-T (T16, approvals) DEPENDS ON 120.004-T (T15, run_session) and has ZERO reverse dependencies, so the runtime chain T15 -> T17 -> T18 -> T19 is fully satisfiable with approvals never started, even though T15 is specified as the single orchestrator and plan section 3.6 places the approval exchange inside supervisor runtime behaviour; the H2 fail-closed guarantee is therefore droppable silently. Gates 129-S. F22 (P1, OPEN, raised on HEAD 914cb214): post-LOCKING failures terminate WITHOUT releasing the workspace lock. Plan section 3.2 routes BOOTSTRAPPING -(fatal)-> FAILED, RESOLVING -(no copilot exe)-> FAILED and LAUNCHING -(spawn error)-> FAILED straight to a terminal state, none of them through DRAINING, while Rule 2 scopes the cleanup guarantee to 'the only path to a terminal state FROM RUNNING' and DRAINING is where lock release lives. Section 3.4 then makes the leak durable: a stale lock is NEVER auto-broken and requires an explicit operator --force-unlock. The most likely first-run failure of all - no copilot exe on PATH - therefore leaves the operator locked out of their own workspace, with every retry returning REFUSED. Owners 119.003-T (transition table) and 118.005-T (lock lifecycle); because 118.005-T is a 127-S member, a fix that makes release structural in T5 touches the FIRST shipment. Gates 128-S. F23 (P1, OPEN, raised on HEAD 914cb214): 119.006-T requires a 'cancel during launch' test, but section 3.2 defines exactly one cancellation edge (RUNNING -> CANCELLING) and Rule 1 rejects any transition outside the table with ILLEGAL_TRANSITION, so a cancel in LAUNCHING can only be refused - contradicting the cancellation contract in that same task. Same defect class as F17: acceptance criteria the fixed contract cannot satisfy. Gates 128-S. F24 (P1, OPEN, raised on HEAD a20c5b50): 119.005-T requires adding .autoharness/sessions/ to "the gitignore template", but NO GITIGNORE TEMPLATE EXISTS - templates/ contains no such artifact and workspace ignore rules are handled procedurally by the install-harness skill, which merely CONFIRMS an existing .gitignore covers .env.local. The criterion is therefore UNSATISFIABLE BY VACUITY (it is "met" by finding nothing to change) and, unlike F17/F23, it fails SILENTLY. It also falsifies the H6 property that the session journal is "gitignored local operational state": in generated workspaces nothing installs that rule, so every supervised session's JSONL would be git-tracked (redacted, so not a direct secret leak, but operational state committed to every consumer repo and a documented hardening guarantee left unbacked). Resolution likely moves scope OUT of S2 into the installer/tuner surfaces plus a real git check-ignore test - a shipment-boundary question. Gates 128-S. F25 (P1, OPEN, raised on HEAD a20c5b50): 120.006-T is the ONLY task touching the CLI and never enumerates the autoharness run option contract, while --force-unlock is owned by 118.005-T (locking.py) and --max-restarts N/resume by 119.006-T (recovery.py), neither of which touches cli.py. Every task can pass its DoD with those controls UNREACHABLE FROM THE SOLE PUBLIC SURFACE - structurally identical to F21. It COMPOUNDS F22: --force-unlock is the documented remedy for the very lockout F22 creates, so resolving F22 without F25 leaves the lockout with no reachable exit. Gates 129-S. F26 (P1, OPEN, raised on HEAD 045bbb7f): the close-path contract in docs/compound/097-S-shipment-task-only-safe-close.md instructs Ship to close with a single `backlogit shipment ship`, but .github/agents/_ship.agent.md prohibits that operation UNCONDITIONALLY ("NEVER the cascade backlogit_ship_shipment, P-015"), with no fully-covered-root carve-out, and P-015 states its prohibition and postcondition absolutely even though its Applies-when is scoped to partial-feature shipments. Under fail-closed P-010/P-015 the instructed close command is UNEXECUTABLE, and a Stage planning artifact cannot grant Ship an exemption from Ship's own operative prohibition. SECOND-ORDER: the 64/64 simulation proves cascade-close safety - a property of an operation Ship may never call - so the evidence is aimed at the wrong operation if safe-close is the real close path. The MANIFEST SHAPE is unaffected (under safe-close the protected set is empty and safe-close archives exactly the release unit) and F14's structural elimination stands. Gates ALL THREE shipments. F27 (P1, OPEN, raised on HEAD 1f14795e): 118.005-T defines the single-active-session invariant (the core H2/H4 safety property) but never requires ATOMIC ACQUISITION - only a PID+start-time liveness check and SEQUENTIAL contention tests. Two supervisors starting simultaneously can both observe no live lock and both write the lockfile, producing exactly the concurrent sessions the module exists to prevent, while every listed acceptance criterion passes. Needs an O_CREAT|O_EXCL / OS-lock primitive plus a SIMULTANEOUS-CONTENDER test. 118.005-T IS A 127-S MEMBER, so this is the SECOND P1 landing on the eligible cursor. Gates 127-S. F28 (P1, OPEN, raised on HEAD 1f14795e): 119.004-T's H7 anti-drift guard is LEXICAL and bypassable - it asserts supervise/ contains no bind/listen call and imports no banned framework, but socket.create_server, socketserver.TCPServer, asyncio.start_server and http.server.HTTPServer all open listeners with neither token nor a banned import. This guard is the control that discharges F2, a CYCLE-1 P0 about network-listener drift into deferred Plan-2 scope, so the P0 mitigation is weaker than recorded. Needs a BEHAVIOURAL check (instrument socket creation / audit hook), with the denylist demoted to a fast pre-filter. Gates 128-S. F29 (P1, OPEN, raised on HEAD 1f14795e): 119.001-T makes redirected pipes the DEFAULT child-process stdio, but the real contract is TTY-attached - start.sh:66 is `exec "$copilot_exe" "$@"` and start.ps1 inherits terminal handles. subprocess.PIPE makes stdin/stdout/stderr non-TTY, changing interactive prompts, input handling, colour and buffering; T1/T2 never characterize terminal attachment, so the migration can pass EVERY assertion while breaking normal interactive Copilot sessions - directly contradicting the zero-observable-change premise of S1/S2. Same defect class as F17/F23. Needs inherited stdio or a PTY for interactive launches plus a TTY characterization case, and an explicit ruling on how ChildOutput journaling (119.005-T) behaves under inherited stdio. Gates 128-S. TEN RULINGS, NOT FOURTEEN - AND NOT THREE: clustering reduces fourteen findings to TEN operator rulings, namely the three clusters below PLUS F16, F17, F20, F26, F27, F28 and F29, which remain INDEPENDENT and must each be decided separately; clearing only the clusters leaves the gate CLOSED. The clusters: F18 + F22 + F23 are one missing invariant (cleanup and cancellation guaranteed only from RUNNING; a single ruling that EVERY terminal exit after LOCKING routes through DRAINING and that operator cancel is legal from EVERY post-LOCKING phase discharges all three); F19 + F21 are one CONTRACT-PLACEMENT ruling (where the shared event/approval contract lives and which task owns it); F24 + F25 are one REACHABILITY ruling (every specified capability needs a task obligated to make it reachable - an ignore rule needs an installing surface, a CLI flag needs an exposing task). F16, F17 and F20 remain independent. NOTE ON STABILITY: cycles 5-7 raised no new P0/P1 and that was briefly recorded as evidence the blocking set was complete; cycle 8 raised F21, so that inference is WITHDRAWN - and cycle 10 then raised F22 and F23, cycle 12 raised F24 and F25, and cycle 14 raised F26, and cycle 15 raised F27, F28 and F29, demonstrating it a second, THIRD, FOURTH and FIFTH time. Quiet cycles are not proof of completeness, and FINDING DISCOVERY HAS NOT CONVERGED - the only defensible claim is "no new findings in that window". All FOURTEEN require operator disposition; NONE affects the F14 structural elimination or the shipment topology. F26 does narrow the EVIDENCE: the 64/64 run proves CASCADE-close safety, and if safe-close is the operative path that proof concerns an operation Ship will not call. See docs/reviews/2026-08-09-copilot-cli-supervisor-control-plane-review.md (Cycle 4).
<!-- END:Review -->

<!-- BEGIN:Architecture -->
New package src/autoharness/supervise/ containing: result.py (one typed SupervisorResult envelope), errors.py (one AutoharnessError base with machine-readable kind + exit-code contract), redact.py (single secret-redaction choke point), locking.py (single-active workspace/session lock), process.py (ChildProcess Protocol + PipeChildProcess default), process_pty.py (ConPTY / POSIX pty behind the same Protocol, optional extra), session.py (explicit state machine), events.py (typed event bus; candidate-(c) hook surface only), journal.py (append-only redacted JSONL session journal), recovery.py (cancellation, bounded restart budget, resume), bootstrap.py (.env.local, COPILOT_HOME/ENGRAM_DATA_DIR, token resolution), sidecar.py (backlogit sync + Engram direct-then-daemon prewarm), resolve.py (Copilot exe resolution + argv composition incl. opt-in --remote), approvals.py (local console-only approval channel), app.py (run_session() - the single orchestrator). Adapter: `autoharness run` in cli.py, parse -> call -> render -> exit, zero policy. Delivery is split across three ROOT per-shipment covering features: 118-F (S1 result/errors/redact/locking + characterization), 119-F (S2 process/process_pty/session/events/journal/recovery), 120-F (S3 bootstrap/sidecar/resolve/app/approvals + CLI adapter + shim migration + docs).
<!-- END:Architecture -->

<!-- BEGIN:Definition-of-Done -->
1. ./start.ps1 <args> and ./start.sh <args> behave identically to today, proven by characterization suites written BEFORE migration and re-run BYTE-IDENTICAL after. 2. No orchestration policy remains in PowerShell or bash. 3. Copilot CLI is spawned, supervised, and reaped by autoharness with VERBATIM exit-code propagation, and autoharness implements no reasoning loop. 4. Supervisor state is confined to <workspace>/.autoharness/sessions/, redacted, and contains no secret substring of length >= 8. 5. Only one supervised session per workspace can run; contention yields REFUSED and never breaks a live lock. 6. Zero network listeners, zero remote transports, zero Gradio/devtunnel code. 7. Base install introduces no new REQUIRED dependency (PTY is an optional extra). 8. All three shipments (127-S, 128-S, 129-S) close with returned_ids empty - no task is ever orphaned and no post-close repair is performed.
<!-- END:Definition-of-Done -->

<!-- BEGIN:Objective -->
autoharness becomes a local supervisor / control plane for long-horizon Copilot CLI workloads, preserving Copilot CLI as the external reasoning/agent-execution engine. Simplest viable architecture: typed shared Python application services with thin CLI / start.ps1 / start.sh adapters. No daemon, no database, no web framework, no async framework, no plugin registry, no Go component.
<!-- END:Objective -->

<!-- BEGIN:State-Machine -->
INIT -> LOCKING -> BOOTSTRAPPING -> PREFLIGHT -> RESOLVING -> LAUNCHING -> RUNNING -> {CANCELLING | RESTARTING | DRAINING} -> {EXITED | FAILED | REFUSED}. Transitions not in the legal-transition table are rejected with ErrorKind.ILLEGAL_TRANSITION; there are no implicit transitions. DRAINING is the only path from RUNNING to a terminal state and always flushes the journal, releases the lock, and reaps the child. REFUSED (lock contention) is a distinct terminal state from FAILED.
<!-- END:State-Machine -->