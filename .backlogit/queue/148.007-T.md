---
artifact_type: task
created_at: 2026-08-28T03:52:31.8337551Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 148.004-T
    - 148.005-T
    - 148.006-T
id: 148.007-T
labels:
    - S0
    - config
    - manifest
parent_id: 148-F
priority: medium
references:
    - docs/plans/2026-08-27-policy-registry-and-review-persona-layer-plan.md
    - .autoharness/harness-manifest.yaml
status: queued
title: U7 — Register the 11 persona artifacts and amend the stale DANGLING notes
updated_at: 2026-08-28T03:52:31.8337551Z
---

Add manifest entries for the 11 installed personas with LF-only SHA-256 checksums, and amend the two existing notes (L255, L265) that declare these references DANGLING. Domain: config. Files: 1.

<!-- BEGIN:acceptance-criteria -->
* 11 persona entries present, each with `path`, `primitive`, `template`, `checksum`, `note`.
* **INV-5 / checksum round-trip**: every checksum recomputed from the installed file matches (SHA-256 over raw LF bytes) — this is the CRLF-drift catch.
* Manifest L255 and L265 notes amended to record resolution while retaining the original gap history.
* No manifest note deleted.
* Manifest remains valid YAML and parses.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan unit **U7**. Register all 11 persona artifacts in `.autoharness/harness-manifest.yaml`.

One entry per installed persona with `path`, `primitive`, `template`, `checksum` (SHA-256 over raw LF bytes), and a `note` citing `336F3AB7`.

**Also — amend the two stale DANGLING notes.** Manifest L255 (`plan-review/SKILL.md`) and L265 (`review/SKILL.md`) currently declare the persona references DANGLING and NOT installed. After U4-U6 those references **resolve**. Leaving a note that asserts a now-false condition is the same stale-normative-surface defect U2 fixes (D6).

**Provenance is amended, never erased**: the notes must record the resolution AND retain the original gap history.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
**Depends on U4, U5, U6** — register once over a complete set.

U1 also edits this file; serialization is by ordering (execution strictly serial, P-001), not by locking. Architecture Strategist rated this P3/acceptable.

Forbidden: deleting any manifest note; changing `workspace-profile.yaml`.
<!-- END:implementation-notes -->