---
artifact_type: task
created_at: 2026-08-28T03:52:31.8337551Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 148.004-T
    - 148.005-T
    - 148.006-T
    - 148.001-T
id: 148.007-T
labels:
    - S0
    - config
    - manifest
parent_id: 148-F
priority: medium
references:
    - docs/plans/2026-08-27-policy-registry-and-review-persona-layer-plan.md
    - .autoharness/harness-manifest.yaml
status: queued
title: U7 — Register the 13 persona artifacts and reconcile the three stale DANGLING notes
updated_at: 2026-08-28T06:41:26.6835975Z
---

Add manifest entries for the 13 installed personas with LF-only SHA-256 checksums, and reconcile ALL THREE stale DANGLING notes (L255, L265, L275). Domain: config. Files: 1.

<!-- BEGIN:acceptance-criteria -->
* **13** persona entries present, each with `path`, `primitive`, `template`, `checksum`, `note`.
* The `python-reviewer.agent.md` entry records `template: "agents/review/technology-reviewer.agent.md.tmpl"` (differing basename is intentional, not drift).
* **INV-5 / checksum round-trip**: every checksum recomputed from the installed file matches (SHA-256 over raw LF bytes) — the CRLF-drift catch.
* **All three** notes reconciled: L255, L265, **and L275**. L275 must no longer assert either that `.github/policies/workflow-policies.md` is dangling or that `336F3AB7` is a still-open gap.
* Each reconciled note retains its original gap history alongside the resolution.
* No manifest note deleted.
* Manifest remains valid YAML and parses.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan unit **U7** — **CORRECTED 2026-08-28** (was 11 personas + 2 notes; now 13 personas + 3 notes).

Register all **13** persona artifacts in `.autoharness/harness-manifest.yaml`. One entry per installed persona with `path`, `primitive`, `template`, `checksum` (SHA-256 over raw LF bytes), and a `note` citing `336F3AB7`.

**Special case — `python-reviewer.agent.md`**: its entry MUST record `template: "agents/review/technology-reviewer.agent.md.tmpl"`. The installed filename and the template basename intentionally differ, per the `install-harness/SKILL.md` L1203 mapping. A future checksum/provenance check must not treat that as drift (RK-I).

**Reconcile ALL THREE stale DANGLING notes** — the prior revision named only two:

1. **L255** (`plan-review/SKILL.md`) — declares seven personas (constitution-reviewer, python-reviewer, architecture-strategist, scope-boundary-auditor, learnings-researcher, security-lens-reviewer, agent-native-parity-reviewer) NOT installed. **Resolved by U4-U6.**
2. **L265** (`review/SKILL.md`) — declares `security-reviewer`, `template-integrity-reviewer`, `schema-cli-docs-coupling-reviewer` NOT installed. **Resolved by U4-U6.**
3. **L275** (`shipment-reconcile/SKILL.md`) — **ADDED by the correction.** Its `DANGLING (partial, narrowed)` note states that the `.github/policies/workflow-policies.md` citation *"remains dangling -- the policy registry layer is a separate, still-open install gap tracked by stash 336F3AB7."* **U1 installs that exact file, and this shipment archives `336F3AB7`** — so BOTH halves of that sentence become false on landing. Leaving it is the same stale-normative-surface defect U2 fixes (D6).

**Provenance is amended, never erased**: each note records the resolution AND retains the original gap history.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
**Depends on U4, U5, U6** — register once over a complete persona set.

**Depends on U1** (`148.001-T`) — **edge ADDED by the 2026-08-28 correction.** Two independent reasons:

1. *Ordering*: U1 and U7 both write `.autoharness/harness-manifest.yaml`; the manifest migration/install in U1 must precede the reconciliation pass in U7.
2. *Truth*: the L275 note cannot be truthfully reconciled until U1 has actually installed `.github/policies/workflow-policies.md`. Reconciling it first would replace one false statement with another.

The prior revision left this as an implicit convention ("serialization is by ordering, not locking"). It is now an explicit dependency edge, which also resolves the Architecture Strategist's earlier P3.

**Note-line drift caution**: L255/L265/L275 are line numbers as measured at commit `1bafd85e`. U1 appends a manifest entry ahead of them, so locate each note by its owning `template:` key (`skills/plan-review/SKILL.md.tmpl`, `skills/review/SKILL.md.tmpl`, `skills/shipment-reconcile/SKILL.md.tmpl`) rather than by line number.

Forbidden: deleting any manifest note; changing `workspace-profile.yaml`.
<!-- END:implementation-notes -->