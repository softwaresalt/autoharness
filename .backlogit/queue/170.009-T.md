---
artifact_type: task
created_at: 2026-09-18T04:55:47.1334487Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 170.004-T
id: 170.009-T
labels:
    - docs
parent_id: 170-F
priority: high
status: queued
title: 'Branch T9: gate documentation - two-rung precedence table, V1-V11 rule set, token table, enumerated divergence note'
updated_at: 2026-09-18T20:03:24.1770793Z
---

Gate documentation: the two-rung precedence table (explicit_contract, title_alias -- no third rung), the V1-V11 validation rule set plus the standalone option-like invariant, the token table, and an accurate statement of the relationship to `git check-ref-format --branch`.

That relationship is documented as a STRICT SUPERSET OF REJECTIONS with ONE ENUMERATED DIVERGENCE SET D CONTAINING EXACTLY TWO SHAPES: (1) the bare @, and (2) @{-N} for N >= 1 that actually RESOLVES in the invoking repository. Only these two are accepted by git and deliberately rejected here under V10, with the reason stated (a context-dependent revision expression evaluated against the reflog is not a durable branch identity, and a gate that persisted it would record an expectation whose meaning changes with checkout history) and the explicit note that normalization was considered and NOT chosen.

The documentation MUST NOT describe the wider @{...} family as a divergence. foo@{1}, main@{0}, a@{b}, @{u}, @{upstream}, foo@{upstream} and @{-0} are SHARED REJECTIONS: git exits 128 on each, agreeing with V10. The docs state this explicitly so a reader cannot infer a broader divergence than the two shapes above. The docs also record that the @{-N} arm is verified only inside a hermetic fixture repository with a scripted checkout history, never against the ambient reflog.

The docs must not claim identical accept/reject sets, and must describe the absence of the third precedence rung using the scoped executable criterion (no occurrence under src/autoharness/, schemas/ or .autoharness/config.yaml key paths; frozen two-member resolution_source enum; remaining occurrences confined to the identified historical set) rather than a repository-wide literal ban.

Blocked by 170.004-T so the emitted-field table documents what actually shipped. Scope: docs/. Stash 86498B64 MERGED WITH 14F4D6F3 (both source IDs preserved). Plan docs/plans/2026-09-17-workspace-authoritative-branch-resolution-plan.md (plan_id workspace-authoritative-branch-resolution, plan_role active, revision 6, task T9). Verdict manifest docs/reviews/2026-09-17-workspace-authoritative-branch-resolution-plan-review.md (latest_attempt 06, verdict REMEDIATED-PENDING-REVIEW, awaiting independent attempt 07).