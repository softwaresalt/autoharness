---
artifact_type: feature
created_at: 2026-08-23T23:49:34.98386Z
custom_fields:
    harness_status: pending
id: 146-F
labels:
    - docs
    - compound
    - docline
    - conformance
    - p-021
references:
    - docs/plans/2026-08-23-docs-compound-source-value-semantics-plan.md
    - docs/reviews/2026-08-23-docs-compound-source-value-semantics-review.md
    - .backlogit/archive/026-DL.md
status: queued
title: 'docs/compound `source` value semantics: correct the provenance-string outlier and ratchet the conformance check to value shape'
updated_at: 2026-08-23T23:49:34.98386Z
---

Make `source` mean exactly one thing across `docs/compound/`, and make that meaning CHECKABLE rather than merely documented.

## Context

`templates/skills/compound/SKILL.md.tmpl` Phase 3 (landed by 140.002-T under 025-DL) already states normatively that "`source` is this document's own repo-relative path ... It is never a description of where the *problem* came from". Stage measured the corpus read-only on 2026-08-23 at HEAD `259ddeb1`: 75 markdown files, 75/75 with non-empty `source`/`doc_type`, **74/75 self-referential**, and **exactly one outlier** — `docs/compound/2026-08-09-next-eligible-detail-scoping-and-vacuous-tiebreak-tests.md` carries `source: "123-S / 115-F (PR #323 Copilot review)"`.

The gap survived because `tests/test_docs_compound_frontmatter_contract.py` asserts NON-EMPTINESS only, so it reports 75/75 GREEN on a corpus containing a known violation — a false-green structural guard. The template's own Quality Criteria bullet mirrors that weaker framing, so the checkable criterion is strictly weaker than the normative prose eight lines above it.

## Predecessor and preserved acceptance contracts

Predecessor: `025-DL` -> feature `140-F` / shipment `148-S` (PR #387, merged 2026-08-22T01:47:17Z, closure `docs/closure/148-S-140-F-post-merge-closure.md`, READY).

SAME CONTRACT SURFACE, SEPARATE SUCCESSOR FEATURE — 140-F is archived and 148-S is shipped, so it cannot absorb new work and re-opening a closed acceptance record is not permitted.

**140.001-T AC3 (verbatim preservation of pre-existing frontmatter keys) IS NOT VIOLATED AND IS NOT AMENDED.** AC3 bound what 140.001-T was authorized to do; it is satisfied and merged. This feature proceeds under a NEW authorization and does not re-run, re-open, or retroactively reinterpret it. The displaced provenance string is preserved VERBATIM in `citations`, the key the authoring contract already designates for it, so the change is value-relocating rather than value-destroying.

Carried forward from 025-DL: R3 (path scope = `docs/compound/` only), R4 (proven body invariance via `git diff`), R5 (existing frontmatter keys survive verbatim), R6 (width isolation, P-003).

## Artifacts

* Deliberation: `026-DL`
* Plan: `docs/plans/2026-08-23-docs-compound-source-value-semantics-plan.md`
* Review: `docs/reviews/2026-08-23-docs-compound-source-value-semantics-review.md` — verdict PASS, 3 P1 + 2 P2 all resolved by binding amendments A1–A5, 0 unresolved P0/P1
* Stash: `FAE1E7B7` (P-021 deferred scope expansion; duplicate scan CLEAN over 190 entries; late-identifier reconciliation performed with NO RESULT)

## P-006

`requires_plan_hardening: no` — considered, not defaulted. One corpus file (frontmatter-only, body-invariant), one test module, one prose bullet in one template family. No schemas, no CLI distribution. No installed dogfood counterpart under `.github/skills/` and no `.autoharness/harness-manifest.yaml` checksum entry, both re-verified this session.

## Scope guard

Explicitly OUT: any other `docs/` subdirectory; any `doc_type` value-shape assertion; any new authoring contract; any change to backlogit or the docline base schema.