---
artifact_type: task
created_at: 2026-09-18T05:02:11.5021221Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 172.001-T
    - 172.002-T
    - 172.008-T
id: 172.005-T
labels:
    - validation
    - fail-closed
parent_id: 172-F
priority: medium
status: queued
title: 'Resume-hint T5 (IMPLEMENTATION): author-time validation boundary implementing the three tokens'
updated_at: 2026-09-18T17:23:41.7058591Z
---

TDD GREEN/IMPLEMENTATION PHASE -- BOUNDARY ONLY, ENFORCEMENT NOT YET ON. Implement validate_checkpoint_payload(payload, origin) as a real callable boundary implementing the three tokens: CHECKPOINT_RESUME_HINT_MISSING, CHECKPOINT_RESUME_HINT_EMPTY, CHECKPOINT_LEGACY_HINTLESS_RESOLVED. Validation is AUTHOR-TIME on harness-produced payloads and NEVER retroactively invalidates records it did not author. This task turns the tests authored in 172.008-T from red to green. IT DOES NOT WIRE THE VALIDATOR INTO ANY WRITE PATH -- that is 172.009-T, which is separately blocked on both producer tasks. Splitting implementation from enablement is what makes "the validator cannot be enabled before both producers are updated" a dependency fact rather than a prose promise. Blocked by 172.008-T (red tests exist and fail first), and by 172.001-T and 172.002-T (token definitions). Blocks 172.006-T and 172.009-T. Scope: src/autoharness/. Stash 71200CBB (DEFERRED SCOPE EXPANSION; PR 452, thread PRRT_kwDORzpWpM6i_UrE; RECONCILED in remediation cycle 2: feature 172-F, shipment 180-S, task 172.005-T). Plan docs/plans/2026-09-17-checkpoint-resume-hint-contract-plan.md (revision 5). Verdict manifest docs/reviews/2026-09-17-checkpoint-resume-hint-contract-plan-review.md (attempt 05, REMEDIATED-PENDING-REVIEW at plan revision 5 — independent review cycle 3, the final authorized review-fix cycle, returned BLOCKED at plan revision 4; Stage remediation cycle 3 closed every finding and raised the plan to revision 5. Stage does not review its own remediation, so no PASS is asserted at revision 5 and the next reviewer pass is attempt 06). Upstream backlogit validator/schema/CLI-help change EXCLUDED. REVISION 5 CORRECTION (attempt-05 finding): validate_checkpoint_payload(payload, origin) remains THE SINGLE PREDICATE, but it is NOT an enforceable boundary on its own. Stage and Ship are MARKDOWN AGENT TEMPLATES that call backlogit MCP/CLI directly and import no Python, so a package-local function sitting beside that call path cannot intercept it. The enforceable boundary is the EXECUTABLE ADAPTER autoharness checkpoint create --state-dump <path-or-json> [--origin harness], delivered and wired by 172.009-T, which validates STRICTLY PRE-WRITE, writes NOTHING on a failing outcome, and performs the OFFICIAL backlogit create call itself on a clean one - so backlogit.instructions.md rule 2 is preserved, because the adapter IS how that operation is reached. This task still ships the predicate inert; the adapter and the startup scan are its only two callers.