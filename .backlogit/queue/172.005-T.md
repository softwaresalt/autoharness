---
artifact_type: task
created_at: 2026-09-18T05:02:11.5021221Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 172.008-T
    - 172.011-T
id: 172.005-T
labels:
    - validation
    - fail-closed
parent_id: 172-F
priority: medium
status: queued
title: 'Resume-hint T5 (IMPLEMENTATION, inert): total decision procedure and hardened adapter command'
updated_at: 2026-09-19T01:09:42.6754667Z
---

IMPLEMENTATION (INERT) - THE DECISION PROCEDURE AND THE HARDENED ADAPTER COMMAND.

Implement validate_checkpoint_payload(payload, *, origin) -> ValidationOutcome in src/autoharness/checkpoint/ as THE SINGLE DECISION PROCEDURE, with the FIVE tokens and the structural origin partition. It is TOTAL over every input: every record returns a ValidationOutcome - never None, never unclassified - across every accepted status (queued, active, shipped, abandoned, resolved) and every enumerated malformed class, with CHECKPOINT_RECORD_MALFORMED as the explicit blocking outcome. Outcome is DATA, not an exception, so the scan can report while the adapter halts.

Implement the EXECUTABLE ADAPTER COMMAND autoharness checkpoint create. WHY A COMMAND AND NOT A LIBRARY CALL: the two producers this contract guards are MARKDOWN AGENT TEMPLATES. They import no Python and call backlogit directly, so a package-local function beside that call path cannot intercept it and a structural test would have no wiring to assert. Because the adapter is a command, "did this surface invoke the adapter or the raw tool?" is a decidable property of the surface text.

ARGUMENT SURFACE (full hardening contract, asserted by 172.011-T): input arrives through TWO MUTUALLY EXCLUSIVE OPTIONS, --state-dump-file <path> and --state-dump-json <json>; supplying both or neither is an error and no value is ever guessed at by shape. THERE IS NO PUBLIC --origin: the create path binds the internal constant "harness" and the historical origin is reachable only from the internal scanner. Path inputs are canonicalized, then required to be workspace-contained and a REGULAR FILE, with symlink, junction, reparse-point, directory, FIFO, device and socket components REJECTED anywhere in the resolved chain. The parsed payload is bounded independently on serialized size, nesting depth, string length and collection length. The delegated backlogit invocation uses a FIXED ARGUMENT VECTOR with shell=False, honours the -- option terminator, and carries leading-hyphen values through as values. ON EVERY VALIDATION FAILURE the adapter exits non-zero, WRITES NOTHING, and NEVER REACHES THE DELEGATED CREATE CALL.

The adapter ADDS NO CAPABILITY beyond backlogit's own create operation and introduces no second checkpoint store; it validates, then performs the official create call. The rule that checkpoints are written only through the official create operation is preserved, because the adapter IS how that operation is reached.

THIS SHIPS INERT. No agent template, no registry, and no instruction references it yet; nothing is prohibited and the raw create path still works. Wiring is the single atomic activation 172.009-T.

Blocked by 172.008-T and 172.011-T - observed-failing suites exist first. Blocks 172.013-T and 172.006-T. Scope: src/autoharness/checkpoint/ only. Stash 2A7C48A8. Plan docs/plans/2026-09-17-checkpoint-resume-hint-contract-plan.md (revision 7). Verdict manifest docs/reviews/2026-09-17-checkpoint-resume-hint-contract-plan-review.md (latest_attempt 07, verdict REMEDIATED-PENDING-REVIEW at plan revision 7, awaiting independent attempt 08).