---
artifact_type: feature
created_at: 2026-09-08T07:41:24.3338019Z
custom_fields:
    harness_status: pending
dependencies:
    - 033-DL
id: 163-F
labels:
    - shipment-reconcile
    - p-005
    - pre-mutation-gate
    - gate
    - contract
    - dogfood-parity
priority: high
references:
    - docs/plans/2026-09-08-shipment-reconcile-step-0c-live-pre-mutation-evidence-plan.md
    - docs/decisions/2026-09-07-step-0c-pre-mutation-guard-reconstruction-disposition.md
status: queued
title: 'Shipment-reconcile Step 0(c): provable live pre-mutation evidence gate (856B6770)'
updated_at: 2026-09-10T23:23:35.6334011Z
---

Implements the operator's 2026-09-08 accepted-with-remediation P-005 disposition of stash 856B6770. Step 0(c)'s linked-deliberation collection is REQUIRED to run before the destructive P-015 cascade invocation (SKILL.md L473-475, L487-492, L747-749) but is not EVIDENCED, so a live scan and a post-hoc reconstruction are indistinguishable - the 159-S/151-F history. Makes live execution PROVABLE. Forward-only: 159-S's mechanical outcome stands final and its deviation stays permanently disclosed; no retroactive compliance is claimed. SEPARATE from 161-F/169-S (what Pre-Mode compares) and 162-F/170-S; sequencing dependency on 169-S only, to avoid conflicting edits to the same SKILL.md Step 0 region.

CURRENT CONTRACT (R1 + R1-11, 2026-09-10 - supersedes the original two-token formulation): (1) EVIDENCE RECORD - Step 0(c) writes a durable pre-mutation record at .backlogit/reconcile/{shipment_id}-precascade-snapshot-{timestamp}.md, inside the existing single-writer lock, before the cascade invocation, recording the validated deliberation ID set EXPLICITLY EVEN WHEN EMPTY. (2) ENGINE-WRITTEN ORDERING ANCHOR (R1) - the record's sha256 is appended as a PRECASCADE_EVIDENCE_ANCHOR event to the engine-owned .backlogit/logs/{shipment_id}.jsonl; the ENGINE, not the agent, writes the timestamp, actor and APPEND POSITION, so ordering is proved by relative append position in an engine-owned file rather than by any agent-authored field. collection_completed_at is DEMOTED to corroborating metadata. (3) FOUR separately labelled, independently failing halt tokens, never merged (the B57F9E24 rule): RECONCILE_FAIL_PRECASCADE_EVIDENCE_MISSING, RECONCILE_FAIL_PRECASCADE_EVIDENCE_STALE, RECONCILE_FAIL_PRECASCADE_ANCHOR_MISSING, RECONCILE_FAIL_PRECASCADE_ANCHOR_NOT_PRE_MUTATION. All four emit P-005 and halt; none authorizes a safe-close fallback. (4) DURABILITY SPLIT (R1-11) - L1 LOCAL RUNTIME ORDERING PROOF is the engine log and is the SOLE GATE INPUT; L2 REPOSITORY AUDIT EVIDENCE commits, at the closure commit, the evidence record AND that shipment's own engine log force-added past the workspace .gitignore rule, verbatim and byte-unmodified, so a fresh-clone reviewer can verify the closure claim without trusting any agent-authored timestamp. L2 is NEVER a gate, NEVER a halt token, NEVER a fallback; a missing L2 makes the closure PUBLICATION incomplete (a reportable P-001 condition) and never authorizes or weakens a close. (5) A 159-S-pattern replay regression test rejects post-hoc reconstruction on the ORDERING axis, not the content axis, and asserts TOKEN IDENTITY so a content-equality implementation cannot satisfy it.

GATE: 033-DL (Step 0(c) evidence-anchor mechanism redesign, DDBF283E/EB23D1B9) is a blocking predecessor of this feature and of tasks 163.001-T through 163.005-T. The gate is encoded at FEATURE/TASK level only: backlogit shipment claim-eligibility evaluates SHIPMENT predecessors only, so a 171-S -> 033-DL shipment edge was correctly rejected by the tool and was NOT recorded; inventing one would have created a permanently unsatisfiable shipment edge. 033-DL reached status done on 2026-09-10, satisfying this gate. Shipment 171-S remains queued and NOT claimable on its own shipment-level blocks dependency on 169-S, which is still unshipped.

Plan hardened (two hardening passes: original + R1). Plan-review PASS, three cycles, zero unresolved P0/P1. Plan: docs/plans/2026-09-08-shipment-reconcile-step-0c-live-pre-mutation-evidence-plan.md. Decisions: docs/decisions/2026-09-07-step-0c-pre-mutation-guard-reconstruction-disposition.md and docs/decisions/2026-09-10-step-0c-evidence-anchor-mechanism-redesign-deliberation.md.

AMENDMENT R1-12 (2026-09-10, review-fix cycle 2) - THREE CORRECTIONS AND ONE NEW UNIT. R1-12 GOVERNS where it conflicts with the CURRENT CONTRACT text above.
(1) TIMESTAMP-ORDERING SUPERSESSION. The pre-R1 comparison "collection_completed_at is not strictly before the cascade invocation => REJECT" is SUPERSEDED AND REMOVED FROM THE LOAD-BEARING PATH. An ABSENT collection_completed_at may still fire EVIDENCE_STALE as a CONTENT/COMPLETENESS failure, but a value that is PRESENT and LATE MUST NOT by itself reject when a valid digest-bound anchor precedes the mutation events in APPEND ORDER. THE ORDERING ASSERTION USES ENGINE-LOG APPEND ORDER AND NOTHING ELSE. Every earlier sentence describing the ordering family as "unchanged" is narrowed accordingly: the EXPLICIT-EMPTY family is unchanged; the ORDERING family is NARROWED.
(2) CORRECTED GIT-STATE PREMISE FOR L2. Item (4) DURABILITY SPLIT above says the engine log is "force-added past the workspace .gitignore rule". Direct inspection on 2026-09-10 refutes the categorical premise behind that instruction: origin/main's .gitignore contains NO .backlogit/logs/ rule; git ls-files lists 993 of 1016 local logs as ALREADY TRACKED including 150-S, 159-S, 169-S and 171-S ITSELF; the .backlogit/logs/ ignore rule exists ONLY as an UNCOMMITTED working-tree edit; and a .gitignore rule NEVER hides an already-tracked path. THE CONDITIONAL CONTRACT: publication follows the path's TRACKING STATE - CASE A already tracked, commit its mutation NORMALLY (this is 171-S's actual state); CASE B untracked and not ignored, ordinary git add; CASE C untracked and ignored, git add -f bounded to that ONE shipment log. THE DURABLE POSTCONDITION IS IDENTICAL IN ALL THREE CASES AND IS THE OBLIGATION: the engine-authored bytes L1 evaluated are present in the closure commit, byte-unmodified. Boundedness is asserted against the CLOSURE COMMIT'S OWN CONTENTS (at most one newly-tracked .backlogit/logs/ path, no .gitignore modification in the commit), so it holds in every tracking case without reading any ignore rule. This feature depends on NO .gitignore state and changes NONE.
(3) L2 NOW HAS AN EXECUTOR - NEW UNIT U6 / TASK 163.008-T. R1-11 declared a missing L2 a reportable P-001 incomplete-closure condition but assigned no implementation surface. Redesign decision D-8 keeps the obligation NORMATIVE and gives it an owner rather than demoting it to advisory (which would have required stripping every P-001 and fail-closed claim and would have reopened the fresh-clone auditability gap). OWNER: the closing agent on the cascade path - Ship, at operational closure. UNIT: U6, a paired prose edit to the SAME two files U2 edits, adding a POST-CASCADE PUBLICATION step that detects the tracking case, stages accordingly, verifies five criteria against the closure commit (presence, byte fidelity, anchor-precedes-mutation append order in the COMMITTED bytes, boundedness, case record), records the outcome and case in the reconcile report, and on failure emits RECONCILE_CLOSURE_INCOMPLETE_L2_EVIDENCE - a P-001 INCOMPLETE-CLOSURE REPORT, NOT a halt token, named OUTSIDE the RECONCILE_FAIL_PRECASCADE_ namespace so the no-fifth-token guard assertion keeps firing. THE PRE-CASCADE GATE SURFACE STAYS AT EXACTLY FOUR TOKENS.
(4) SCOPE AND SIZE COMPOSITION. TASK COUNT 7 -> 8 (163.001-T through 163.008-T). Sizes: 163.001-T M/medium, 163.002-T M/medium, 163.003-T M/medium (S -> M, re-confirmed for the accumulated R1-11 and R1-12 guard assertions), 163.004-T M/high (INDIVISIBLE, de-risked not split), 163.005-T S/low, 163.006-T S/low, 163.007-T XS/low, 163.008-T S/medium (new). Every task remains inside the 2-hour rule on both axes. NO new file, NO Python source change, NO schema, NO CLI, NO .gitignore change; the edited FILE SET is unchanged.
(5) HARDENING AND REVIEW COUNTS CORRECTED. The line above stating "Plan hardened (two hardening passes: original + R1). Plan-review PASS, three cycles" is STALE. Current state: FOUR hardening passes (original, R1, R1-11, R1-12) and FOUR plan-review cycles, all PASS with zero unresolved P0/P1. Cycle 4 was run under EXPLICIT OPERATOR AUTHORIZATION extending the 3-cycle review-fix budget for SAME-CONTRACT findings only (P-021 C4: an extension never makes an out-of-scope expansion in-scope).
(6) UNCHANGED: 033-DL gate satisfied (done, 2026-09-10), encoded at feature/task level only; 171-S REMAINS QUEUED AND NOT CLAIMABLE on its shipment-level blocks dependency on 169-S, which is still unshipped; forward-only, with 159-S's deviation permanently disclosed and no retroactive-compliance claim; separate from 161-F/169-S and 162-F/170-S.