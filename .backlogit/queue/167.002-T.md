---
artifact_type: task
created_at: 2026-09-17T19:57:40.4955675Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 167.001-T
id: 167.002-T
labels:
    - closure
    - pipeline-topology
    - python
    - fail-closed-design
parent_id: 167-F
priority: medium
references:
    - docs/plans/2026-09-17-closure-evidence-naming-contract-plan.md
    - docs/decisions/2026-09-17-closure-evidence-naming-contract-deliberation.md
status: queued
title: Rewire pipeline-topology closure reader onto the contract module, preserving the validity predicate byte-for-byte
updated_at: 2026-09-17T19:57:40.4955675Z
---

PLAN UNIT U2. Domain: Python source. Posture: characterization-first (pin current behaviour for the ID-anchored corpus BEFORE changing discovery).

FILES (2): src/autoharness/gates/topology.py, tests/test_gates_topology.py.

DELIVERABLES
1. FilesystemTopologyReaders.closure_complete() (topology.py:714-725) delegates DISCOVERY to closure_contract.classify_closure_candidates(). It KEEPS its existing `bool | None` return type for backward compatibility (plan decision P-D2 — minimises blast radius on a fail-closed gate; every existing caller and test double keeps working).
2. New sibling method closure_discovery(shipment_id) -> ClosureDiscovery | None, used by the gate layer (167.003-T) for diagnostics. The richer outcome is opt-in, not a breaking signature change.
3. Evaluation order per D5: evaluate CANONICAL matches first and accept on the first candidate satisfying _closure_artifact_complete; ONLY if the canonical partition is empty, evaluate LEGACY matches the same way.
4. Protocol/implementer updates (finding F-1, risk R10): add the matching closure_discovery member to the structural protocol declaration at topology.py:131 and to _NullReaders (topology.py:751), AND update the in-repo test doubles in tests/test_gates_topology.py that structurally implement closure_complete (approx. lines 91, 161, 1690, 1705, 1731, 1746, 1787, 1818, 1835, 1944, 1955, 2151, 2538, 3024, 3068, 3334). These change mechanically and identically, so the unit stays within its 2-hour envelope.

HARD ACCEPTANCE CRITERION (hardening H1.1 — resolves plan-review P1 finding F-2)
_closure_artifact_complete (topology.py:303-337) and _closure_conditions_satisfied (topology.py:281-300) MUST be BYTE-IDENTICAL after this task. EVIDENCE REQUIRED: a diff over those exact line ranges showing ZERO changes. This is the property that keeps the gate fail-closed and is a BLOCKING criterion, not a review observation.

EXPLICITLY UNCHANGED
- _frontmatter's BacklogUnavailableError raise path. Malformed frontmatter MUST still fail closed as BACKLOG_UNAVAILABLE; regressing it into a silent skip is an explicit non-goal carried from the source bug report.
- The "any valid match wins" semantics within a partition.

TESTS (3)
1. A legacy-named valid artifact now returns True (the 162-S / 174-S shape).
2. Canonical takes precedence when BOTH namings exist (the 173-S shape).
3. Malformed frontmatter under a RECOGNIZED name still raises BacklogUnavailableError.

DEPENDS ON: 167.001-T (MERGE-ORDER CONSTRAINT, hardening H2 — this task MUST NOT merge ahead of 167.001-T; doing so creates the transient split-contract state this feature exists to eliminate).

SCOPE GUARD: touches NOTHING under docs/closure/. Does not change any gate token in this task (that is 167.003-T).