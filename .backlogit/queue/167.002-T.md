---
artifact_type: task
created_at: 2026-09-17T19:57:40.4955675Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
dependencies:
    - 167.001-T
id: 167.002-T
labels:
    - closure
    - pipeline-topology
    - python
    - fail-closed-design
parent_id: 167-F
priority: medium
references:
    - docs/plans/2026-09-17-closure-evidence-naming-contract-plan.md
    - docs/decisions/2026-09-17-closure-evidence-naming-contract-deliberation.md
status: queued
title: Rewire pipeline-topology closure reader onto the contract module, preserving the validity predicate byte-for-byte
updated_at: 2026-09-17T22:11:10.6509314Z
---

PLAN UNIT U2 (plan revision 5). Domain: Python source. Posture: characterization-first — pin current behaviour for the ID-anchored corpus BEFORE changing discovery.

FILES (2): src/autoharness/gates/topology.py, tests/test_gates_topology.py.

NORMATIVE SOURCE: plan revision 5, unit U2.

CHANGES
- FilesystemTopologyReaders gains a READER-INTERNAL structured discovery method closure_discovery(shipment_id) -> ClosureDiscovery that delegates to 167.001-T's classify_closure_candidates. This is the SINGLE SOURCE OF FILESYSTEM OBSERVATION for closure evidence: closure_complete() is reimplemented as a THIN PREDICATE over closure_discovery(...)'s result and performs NO directory listing of its own, so the two can never disagree about what is on disk.
- closure_complete() KEEPS its existing bool | None return type and its existing signature.
- NO CHANGE to the TopologyReaders protocol (topology.py:118-131) and NO CHANGE to _NullReaders (topology.py:728-751). Adding a member to the broad structural protocol would force a mechanical edit across the ~16 in-repo test doubles in tests/test_gates_topology.py, which both exceeds this unit's granularity envelope and contradicts the compatibility this change claims. 167.003-T reaches the richer diagnostics through a CAPABILITY ACCESSOR instead, which duck-types the optional method and returns None for any reader that does not expose it. Every existing protocol implementer remains valid UNMODIFIED.
- EVALUATION ORDER per D5: evaluate canonical (R1) matches first and accept on the first candidate satisfying _closure_artifact_complete; ONLY IF the canonical partition is empty, evaluate legacy (R2) matches the same way.
- Candidate collection is gated by 167.001-T's DESIGNATED-POSITION attribution: the shipment identifier is parsed from the position the anchored grammar reserves for it and that single parsed group is compared. R1 comparison is EXACT and case-sensitive; R2 comparison is case-folded. A requested token is never searched for elsewhere in a filename.

EXPLICITLY UNCHANGED (hard acceptance criterion)
_closure_artifact_complete (topology.py:303-337) and _closure_conditions_satisfied (topology.py:281-300) are BYTE-IDENTICAL after this unit. Verify with an explicit diff of those two function bodies before the task is considered done; any change to them is a defect, not an improvement. _frontmatter's BacklogUnavailableError raise path is untouched, so malformed frontmatter still fails closed as BACKLOG_UNAVAILABLE. The protocol declaration, _NullReaders, and every existing test double in tests/test_gates_topology.py are likewise untouched.

SCOPE GUARD
Read-side discovery only. This unit NEVER writes, renames, or repairs a closure artifact, and touches NOTHING under docs/closure/ (D8).

TESTS (3)
1. A legacy-named valid artifact now returns True.
2. Canonical takes precedence when both exist.
3. Malformed frontmatter under a recognized name still raises BacklogUnavailableError.

ONE-TIME RUNTIME EVIDENCE (D9)
The REAL-CORPUS proof that closure_complete('162-S') and closure_complete('174-S') return True against the committed corpus is captured ONCE at execution time into this work's post-merge closure artifact. It is NOT a durable test assertion: durable tests use temporary fixtures only and never read docs/closure/.

DEPENDS ON: 167.001-T.