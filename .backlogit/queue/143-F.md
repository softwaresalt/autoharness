---
artifact_type: feature
created_at: 2026-08-21T21:19:03.9055004Z
custom_fields:
    harness_status: pending
id: 143-F
labels:
    - bug
    - tests
    - isolation
    - remediation
    - p-021
    - windows
priority: high
references:
    - docs/plans/2026-08-21-full-suite-test-isolation-plan.md
    - docs/plans/2026-08-21-full-suite-test-isolation-hardening.md
    - docs/reviews/2026-08-21-full-suite-test-isolation-review.md
    - .backlogit/queue/024-DL.md
status: queued
title: Post-diagnosis polluter remediation and git-subprocess self-diagnosis — executable successor to 141-F
updated_at: 2026-08-21T21:20:21.7489614Z
---

Carry the E8158860 test-isolation work past diagnosis: make the two `check=True` git subprocess sites self-diagnosing (UNCONDITIONAL), and remediate the confirmed polluter identified by 141.001-T (CONDITIONAL, but with an ALWAYS-TERMINATING recorded disposition).

**Source stash:** E8158860 (DEFERRED SCOPE EXPANSION, P-021) — same entry as 141-F
**Deliberation:** 024-DL
**Plan:** `docs/plans/2026-08-21-full-suite-test-isolation-plan.md` Task 3 (+ amendments A1, A2R)
**Hardening:** `docs/plans/2026-08-21-full-suite-test-isolation-hardening.md` — H1, H2R
**Review:** `docs/reviews/2026-08-21-full-suite-test-isolation-review.md`
**Predecessor feature:** 141-F (diagnosis + unconditional cwd-decoupling hygiene)
**Supersedes:** 141.005-T (archived; its scope is split across 143.001-T and 143.002-T with no loss)

## Why this feature exists (review-fix cycle 2, PR #386)

141.005-T was originally a member of 149-S whose documented failure path was "return the task BLOCKED and close the feature partially". That path is NOT EXECUTABLE: `.github/agents/_ship.agent.md:325-340` makes any manifest task whose status is not `queued`/`active`/`done`/pre-archived a FAIL-CLOSED HALT, and the manifest is explicitly "never mutated to make execution proceed". A blocked member therefore deadlocks the whole shipment instead of allowing a partial close.

The lifecycle fix is structural: 149-S now carries ONLY unconditionally executable work, and the conditional remediation moves here, into a successor shipment, expressed so that EVERY branch terminates in a legitimate `done` with recorded evidence. No task in this feature can ever need `blocked` status, shipment abandonment, or manifest mutation.

## Terminal dispositions (all reach `done`; none blocks a shipment)

* **R1 — polluter isolated and still reproduces.** 141.001-T recorded `VERDICT: PAIR-ISOLATED`, and the minimal reproducing command still fails at this shipment's head. Remediate at source. Canonical gate green.
* **R2 — polluter isolated but no longer reproduces.** The pair no longer fails at this shipment's head. Admissible causes are restricted to work that can PRECEDE 151-S under the graph `148-S -> 149-S -> 151-S -> 150-S`: the 141.002-T-141.004-T anchor work in 149-S (the primary candidate: it removes all 58 `dir=Path.cwd()` sites), 143.001-T's git-subprocess change earlier in THIS shipment, 148-S's docs/compound migration, or an intervening merge from `main`. **150-S is NOT an admissible cause** — it is ordered AFTER this shipment (corrected in review-fix cycle 3). Record the evidence; make NO source edit; the git self-diagnosis work still lands.
* **R3 — no polluter isolated.** 141.001-T recorded `VERDICT: INCONCLUSIVE`. Re-measure the canonical gate. If green, record it. If still red, record the residual defect plus 141.001-T's narrowed candidate set and capture a NEW deferred stash entry under P-021 C2. No speculative fix is permitted in any branch.

## Feature acceptance criteria

- AC-G1. Both `check=True` git subprocess sites surface captured stderr on failure (unconditional; 143.001-T).
- AC-G2. The disposition (R1, R2 or R3) is recorded explicitly on 143.002-T with its evidence. An unrecorded disposition is not acceptable.
- AC-G3. In R1 only: the canonical full-suite gate is GREEN on Windows (zero failures, zero errors, skips unchanged) and the 141.001-T AC1 reproducing command now passes.
- AC-G4. All five victim tests retain their original assertions VERBATIM, proven by the **assertion-integrity gate (AIG)** defined in `docs/plans/2026-08-21-full-suite-test-isolation-plan.md`: AIG-1 (AST-extracted assertion callsite sets exactly equal), AIG-2 (no assertion semantic drift), AIG-3 (EXHAUSTIVE N1–N6 allowlist — for this feature the relevant classes are N5 subprocess diagnostics at the two `check=True` git sites, plus N3/N6 in support) and AIG-4 (every changed non-assertion line cited by class); plus identical in-isolation pass/fail semantics (amendment A1R). Binding in every branch. *(Corrected in review-fix cycle 4: the previous "confined to setUp/tearDown/imports" wording forbade 143.001-T's own subprocess edits, which occur inside victim #1's test method and the `_git` helper in victim #5's module.)*
- AC-G5. No source edit under `tests/` is made in any branch without citing 141.001-T's recorded minimal reproducing pair. This is the structural replacement for the old "hard stop" — a speculative fix cannot produce the citation.
- AC-G6. Hosted CI remains green.
- AC-G7. In R3-still-red only: a new deferred stash entry exists carrying the residual defect and the narrowed candidate set.

## Out of scope (P-021 C1)

Any change under `src/autoharness/`. If remediation implicates production code, that is a different contract surface and re-enters P-021 capture (024-DL R5).