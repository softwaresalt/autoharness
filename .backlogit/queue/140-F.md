---
artifact_type: feature
created_at: 2026-08-21T20:20:42.4178635Z
custom_fields:
    harness_status: pending
id: 140-F
labels:
    - bug
    - docs
    - docline
    - conformance
    - p-021
    - compound
priority: medium
references:
    - docs/plans/2026-08-21-docs-compound-docline-conformance-plan.md
    - ' docs/reviews/2026-08-21-docs-compound-docline-conformance-review.md'
    - ' .backlogit/queue/025-DL.md'
status: queued
title: docs/compound docline conformance — backfill source/doc_type and align the authoring contract
updated_at: 2026-08-21T20:29:08.5685929Z
---

Make every document under `docs/compound/` satisfy the docline base frontmatter contract (`source` + `doc_type`), and amend the compound authoring template so new learnings are born conformant.

**Source stash:** F73BA065 (DEFERRED SCOPE EXPANSION, P-021)
**Deliberation:** 025-DL
**Plan:** `docs/plans/2026-08-21-docs-compound-docline-conformance-plan.md`
**Review:** `docs/reviews/2026-08-21-docs-compound-docline-conformance-review.md` (PASS, 0 unresolved P0/P1)
**Hardening:** not required (`requires_plan_hardening: no`, reasoned in 025-DL, not defaulted)

**Baseline (Stage-measured, read-only, 2026-08-21):** 73 markdown files under `docs/compound/`; 72 lack `source`, 73 lack `doc_type`, 0 have both, 4 have no frontmatter block at all.

**Decision (025-DL):** option (e) — backfill via the official idempotent `backlogit docs migrate` (dry-run proves `body_bytes_changed: false` for every file) PLUS align `templates/skills/compound/SKILL.md.tmpl`. Rejected: local profile opt-out and descoping (no repo-local docline lever exists — scope/profiles are tool-owned); external classification per the 90F2A9F8 precedent (that precedent covers a backlogit *behaviour* only backlogit can change; this is a conformance gap in autoharness's own corpus that autoharness can close today).

**Feature acceptance criteria**
- AC-F1. `backlogit docs lint --path docs/compound` reports ZERO required-field errors of ANY kind (amendment C1 — not merely zero `source`/`doc_type` errors).
- AC-F2. `tests/test_docs_compound_frontmatter_contract.py` is green: all 73 `*.md` files carry non-empty `source` and `doc_type`.
- AC-F3. Body invariance is PROVEN by diff, not asserted: no changed file has a diff hunk below its frontmatter's closing `---`.
- AC-F4. Migration idempotence proven (second dry-run plans zero changes).
- AC-F5. No file outside `docs/compound/` and `templates/skills/compound/SKILL.md.tmpl` is modified.

**Explicitly out of scope (P-021 C1):** every other `docs/` subdirectory (`decisions/`, `plans/`, `reviews/`, `closure/`, `spikes/`, `research/`, `design-docs/`, `product-specs/`) — all in docline scope and possibly sharing the gap, but a separate surface. Does not resolve active external entry 90F2A9F8.