---
artifact_type: task
created_at: 2026-09-15T22:15:57.2595035Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 166.002-T
id: 166.003-T
labels:
    - policy
    - p-015
    - contract-change
    - template-parity
parent_id: 166-F
priority: medium
status: queued
title: P-015 policy rewrite to flat-manifest closure scope (+ template mirror)
updated_at: 2026-09-16T08:02:20.984797Z
---

REFRAMED IN PLACE 2026-09-15; CORRECTED 2026-09-16 (plan revision 2); CORRECTED AGAIN 2026-09-16 (plan REVISION 4 — group B evidence-honesty propagation). Supersedes the TERMINAL_CLOSE policy amendment. Authoritative plan: docs/plans/2026-09-15-flat-manifest-shipment-closure-plan.md (unit U3, REVISION 4). Authoritative decision: docs/decisions/2026-09-15-flat-manifest-shipment-closure-deliberation.md (revision 2).

DEPENDENCIES (machine DAG, authoritative): depends on 166.002-T (red phase, Part B doc-contract tests). No other prerequisites.

SURFACES (identical edits to BOTH, byte-parity required):
- .github/policies/workflow-policies.md
- templates/policies/workflow-policies.md.tmpl

CHANGES
1. REWRITE the P-015 Statement in flat-manifest terms: a shipment manifest is a FLAT MANIFEST of exactly what is delivered. Closure scope is the manifest's explicit item IDs PLUS the shipment record. A manifest is NOT a container and carries NO implicit hierarchical closure semantics. ANCESTRY NEVER EXPANDS CLOSURE SCOPE.
2. REPLACE the "VERIFIED FULLY-COVERED-ROOT EXCEPTION" heading and its preconditions 1-5 with INV-1..INV-11, transcribed verbatim from the decision's section D1/D1d. Retain precondition 7's two-set allowed_ids/required_ids gate and its 155-S supersession note unchanged — that gate is ALREADY flat-manifest-shaped and remains the post-mutation guard (INV-10).
2a. REVISION-2 ADDITION — STATE THE D1a SET VOCABULARY NORMATIVELY, immediately before INV-1, so INV-1 and INV-2 cannot be read as contradictory: manifest_scope = items(S), the flat manifest and NOTHING else; closure_scope = manifest_scope + {S}, i.e. the shipment RECORD ITSELF is in closure scope while never appearing in its own manifest; allowed_ids = closure_scope + validated_linked_deliberations (a permission set — IDs that MAY appear in archived_ids without failing the guard); required_ids = the closure_scope members not already TRULY archived at the pre-close snapshot (an obligation set — IDs that MUST appear). State explicitly that validated_linked_deliberations widens ONLY allowed_ids, NEVER manifest_scope, NEVER closure_scope, NEVER required_ids, and therefore adds ZERO work items to what is delivered or archived. State its four admission conditions verbatim from D1a. INV-2's "in scope iff in items(S)" is then scoped explicitly to WORK-ITEM membership (manifest_scope), which is what INV-1 also means — the apparent conflict in revision 1 was a vocabulary collision, not a semantic one.
3. REWRITE the Precondition and Required Check paragraphs from baseline-PRESENCE to baseline-INVARIANCE (INV-7): record each out-of-manifest artifact's baseline location and content hash; require that NOTHING outside closure scope CHANGES during the run. An artifact already archived, descoped, or missing AT BASELINE is recorded as baseline state and is explicitly NOT a halt — it carries no signal about THIS run.
4. STATE INV-8 EXPLICITLY (plan-review P1-1): manifest ORDERING (feature last in the final shipment) is a STAGE ASSEMBLY convention. Ship MUST NOT evaluate manifest ordering as a closure precondition. Without this, every historical manifest — all of which list the covering feature FIRST, including 173-S, which was in fact successfully closed under feature-first ordering on 2026-09-16 — would be invalidated.
5. STATE INV-4/INV-5: a feature MAY be decomposed across an ordered sequence of shipments; intermediate shipments carry NO feature member; exactly one shipment (the final) carries the feature item, placed last. Only tasks ACTUALLY DELIVERED by the sequence are subject to the terminality precondition.
6. REVISION-2 ADDITION — STATE INV-11 WHEREVER INV-4/INV-5 APPEAR (plan R22, decision D1d), IN THE SAME NORMATIVE BLOCK so a reader can never take INV-4/INV-5 as an operational capability claim: multi-shipment feature delivery is CONTRACT-COMPLETE but OPERATIONALLY BLOCKED. An intermediate shipment S1..Sn-1 carries no feature member, therefore cannot qualify for CASCADE, therefore is a genuine SAFE_CLOSE shipment; backlogit 1.10.1 offers NO safe record transition to archived_status: shipped for such a shipment (`move --status shipped` is refused, exit 9). This is an EXTERNAL RUNTIME PREREQUISITE — backlogit is a third-party Go binary and the capability CANNOT be implemented in this repository — tracked by durable ACTIVE stash entry 7F9CB5E9 (successor to the too-broadly-archived 2B42392E). Until 7F9CB5E9 is discharged upstream, an intermediate shipment halts with RECONCILE_FAIL_NO_SAFE_RECORD_TRANSITION and NO substitution, workaround, hand-edit, or cascade escalation is permitted. The policy text MUST NOT claim end-to-end split-delivery support is operationally complete.
7. STATE INV-3 in normative prose: items excluded, removed, descoped, rejected, merged into other work, or otherwise absent from a manifest MUST NEVER block the feature record or the shipment record from reaching a terminal state ON THE GROUND OF ANCESTRY. No disposition note, no archived_status value, and no provenance may be demanded of such an artifact.
8. REVISION-2 ADDITION, CORRECTED IN REVISION 3 — STATE THE D1b EXACT-PARSED-SCALAR RULE AND THE D1c TORN/DUPLICATE FAIL-CLOSED RULE as normative policy text, since both govern when a DESTRUCTIVE cascade is authorized. D1b (REVISION-3 WORDING — the revision-2 "compared verbatim / byte-for-byte" wording is WITHDRAWN as UNIMPLEMENTABLE, because every record reaches the predicate through gates.topology._frontmatter, which uses yaml.safe_load, and a YAML load ERASES lexical form): engine-inertness is granted ONLY when the PARSED frontmatter status is a str EXACTLY EQUAL to `archived`, i.e. `isinstance(status, str) and status == "archived"`, with NO post-parse lower-casing, stripping, casefolding, aliasing, or str() coercion. NON-STRING VALUES FAIL CLOSED: `status: yes` parses to the bool True and a bare `status:` parses to None, and NEITHER is inert. State the accepted limitation explicitly: YAML-equivalent spellings that parse to the same scalar (e.g. unquoted trailing whitespace) are INDISTINGUISHABLE after parsing and are treated as the canonical value; only genuinely distinct parsed values such as the YAML-QUOTED `" archived "` remain non-inert. State the rationale inline: the backlogit engine ALSO YAML-parses, so the parsed scalar is the domain in which autoharness and the engine AGREE. D1c: any ID resolving to more than one record (duplicate within a root, or TORN across queue/ and archive/) invalidates the whole index and forces SAFE_CLOSE, INCLUDING for out-of-manifest descendants.
9. ADD a SUPERSESSION NOTE (2026-09-15, amended 2026-09-16) recording: the operator architectural correction; the withdrawal of the hierarchical-closure preconditions; the withdrawal of TERMINAL_CLOSE and the terminal-descope exemption; and the superseded decision path (docs/decisions/2026-09-15-173-s-terminal-shipment-closure-deliberation.md, retained as SUPERSEDED PROVENANCE ONLY — do not execute). Do NOT delete or rewrite the existing 155-S supersession note.
10. PRESERVE the P-015 generality rule: the exception MUST NOT be narrowed or special-cased to any particular feature ID, shipment ID, or manifest shape.

EVIDENCE RULE — REPLACED IN REVISION 4 (D7a / D13 / plan R24 / H17). *** THE PRIOR RULE ("engine-behavior claims cited in this policy text MUST be grounded in the in-workspace checked-in fixtures from 166.002-T") IS WITHDRAWN AS UNSOUND. *** 166.002-T's fixtures are CLASSIFIER fixtures: they write synthetic Markdown and call a pure Python function, never invoking the backlogit Go engine. Grounding an ENGINE claim in them would be FABRICATED EVIDENCE. The two laws are DISJOINT:
- CLASSIFIER LAW — what classify_shipment_close_path returns for a record shape. Authoritative evidence: 166.002-T's fixtures. Citable as fact in this policy text.
- ENGINE LAW — what backlogit 1.10.1 mutates/returns/skips. The ONLY authoritatively held engine proposition is sourced to the PATH-SCOPED git comparison between 358b63b4 and e4ca20e5 (empty diff and IDENTICAL blob OIDs for .backlogit/archive/165.007-T.md and .backlogit/archive/165.010-T.md), NOT to any fixture and NOT to the unfiltered `git show --stat e4ca20e5` diffstat (which has 34 paths; e4ca20e5 is the COMBINED PUBLICATION COMMIT, not close-only).
EVERY OTHER ENGINE BEHAVIOUR (returned_ids contents, descendant archival, parent_id clearing) is INDICATIVE AND UNPROVEN: state it as rationale, NEVER as a measured fact, and NEVER let it authorize a CASCADE — the dependent policy behaviour is the FAIL-CLOSED one. The external %TEMP% spike evidence remains NON-AUTHORITATIVE (P-005 containment/destructive-approval violation) and MUST NOT be cited.

ACCEPTANCE: 166.002-T Part B doc-contract tests pass against BOTH mirrors, including the D1a-vocabulary, INV-11, D1b/D1c and multi-shipment-honesty assertions; the tokens `TERMINAL_CLOSE` and the phrase `fully covered` no longer appear as normative preconditions; frontmatter/markdown/cross-reference validation clean; no unresolved {{...}} placeholders introduced in the .tmpl.

SIZE: M. COMPLEXITY: medium.