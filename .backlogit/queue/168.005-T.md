---
artifact_type: task
created_at: 2026-09-18T04:50:22.2743212Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 168.001-T
id: 168.005-T
labels:
    - test
    - p-004
parent_id: 168-F
priority: high
status: queued
title: 'P-004 T5a (RED): author the nine-token contract tests and observe them failing before P004Result exists'
updated_at: 2026-09-18T19:56:06.8795642Z
---

TDD RED PHASE. Author the composed state-machine contract tests for all NINE P-004 tokens BEFORE P004Result and validate_declared_harness_set() exist, and OBSERVE THEM RED. One concrete legitimate passing state and one concrete failing state per token: P004_UNEXPECTED_GREEN, P004_UNEXPECTED_RED, P004_MISSING_OBSERVATION, P004_UNDECLARED_OBSERVATION, P004_MARKER_ABSENT, P004_EMPTY_RED_SET, P004_SET_OVERLAP, P004_DUPLICATE_DECLARATION, P004_MARKER_MISPLACED. Tests import the real boundary from src/autoharness/ and MUST NOT re-implement the comparison. At this task the import fails, so every case is red with the declared marker. REQUIRED CASES: (1) the crossed-marker two-red-test case, where each test fails with the OTHER test's declared marker, emitting P004_MARKER_ABSENT naming BOTH ids; (2) a @unittest.skip-ped declared id emitting P004_MISSING_OBSERVATION; (3) a module- or class-addressed declared name yielding several leaf tests, emitting P004_MISSING_OBSERVATION for the declared id and P004_UNDECLARED_OBSERVATION for each surplus id; (4) a _FailedTest whose id() does not equal the requested name, still attributed to the requested name, proving correlation is by test-object identity and not by string comparison; (5) a two-name load in which only the SECOND name fails, attributing the load error to that name alone, proving per-name loader.errors slice attribution; (6) SUBTEST CASES - a declared expected_red test whose only failing path is a self.subTest block must be observed as a FAILURE, a test with three subtests of which the second and third fail must produce a detail_text concatenating both failing subtests' formatted text in call order with a marker declared against the third matched, and a test whose subtests all pass must remain success; (7) SCHEMA-ERROR-MAPPING CASES - validate_declared_harness_set() converts the jsonschema required error for a missing marker into P004_MARKER_MISPLACED naming the offending test_id, the additionalProperties error for a surplus marker into the same token naming its entry, and the minItems error into P004_EMPTY_RED_SET, with every assertion made on the returned token value and never on exception message text; plus a synthetic ValidationError whose (absolute_path, validator) pair matches no mapping row raising P004_DECLARATION_SCHEMA_ERROR and halting. Token definitions are broad, not additive - the gate token set is exactly NINE: P004_MISSING_OBSERVATION covers zero tests, more than one test, a load error, and a skip for a declared name; P004_UNDECLARED_OBSERVATION covers surplus tests arriving because a declared test_id was addressed at module or class granularity. P004_DECLARATION_SCHEMA_ERROR is a hard halt, deliberately NOT a tenth gate token. Scope: tests/. Blocked by 168.001-T (declaration shape) only. This task does NOT depend on 168.007-T: red precedes implementation, and reversing that would make this a test-after task wearing a red-phase label. Stash 76EBDE6D. Plan docs/plans/2026-09-17-p004-red-phase-precondition-scoping-plan.md (plan_id p004-red-phase-precondition-scoping, plan_role active, revision 6). Verdict manifest docs/reviews/2026-09-17-p004-red-phase-precondition-scoping-plan-review.md (latest_attempt 06, verdict REMEDIATED-PENDING-REVIEW, awaiting independent attempt 07).