---
artifact_type: task
created_at: 2026-09-26T07:17:34.3644019Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: v1
    size_source: agent
dependencies:
    - 187.004-T
id: 187.005-T
labels:
    - harness-surface:none
    - ship-lifecycle
    - unit-c
parent_id: 187-F
priority: high
references:
    - docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md
    - docs/reviews/2026-09-25-ship-lifecycle-release-units-plan-review.md
status: queued
title: 'Ship lifecycle C5: Linux-native CI gate, Proof G case coverage and non-claim audit'
updated_at: 2026-09-26T07:24:13.3257427Z
---



<!-- BEGIN:acceptance-criteria -->
- The pull request's CI log for the new step shows the job URL, runner OS, kernel, the unittest exit code, the effective fixture root under $RUNNER_TEMP, both filesystem types and zero skips; Ship records it as IM-01 evidence. The test job is path-gated and 'ci gate' treats a skipped test job as OK, so the evidence must show the step itself executed; a green 'ci gate' alone is not IM-01 evidence. Stage runs none of it.
- Coverage test: every Proof G case ID G01 to G32 appears in a test name; G31 is Linux-only; every other ID has a variant that runs on both hosts (skip accounting: Windows permits only the symlink sub-cases without privilege and G31; Linux none).
- Audit test implements FLOOR, Reading, Detector, Clearing, the required-sentence assertion and all Controls from the Non-Claim Audit Inventory verbatim: must fail on each claim form in IM-14-F05, F10 and F11, race_free, test_toctou_safe, TOCTTOU, hard-linked, race-/free, hard-/link and TOC-/TOU split over two lines, a required-sentence line ending TOC- then TOU resistance, a LEDGER entry for one path used on another, invalid UTF-8; a CRLF copy hashes as LF; must not hit trace-free, embrace-safe, grace period; the required sentence hits and clears.
- Structural tests (coverage, audit) reach no stub and are recorded outside any roster; harness-surface:none (workflow step plus structural tests; no Python production module under src/), so no RED obligation.
- Environment prechecks: git worktree list shows one worktree; git status --porcelain is empty for the task's files before the first write; the canonical command runs from the repository root. Over the estimate by more than 30 minutes -> stop and return to Stage for a split.
- IM-14: no added text claims race, TOCTOU or hardlink-alias resistance.
- IM-15: a test or proof PASS confers no claim authority; ordinary P-001/P-002/P-004/P-020 gates at claim and closure.
- Manifest harvest carry IM-14-F57 (E4): strip trailing whitespace before the ends-in-'-' test of the hyphen join, and add a 'TOC- ' (trailing space) / 'TOU' control that must fail.
- Manifest harvest carry IM-14-F47, in part (E4): the plan fixes the hyphen join to lines whose first line ends in '-'; the 'replace with nothing' reading of required-sentence removal is carried to C5: the audit test pins how removal is applied (the sentence is replaced with nothing) and covers it with a control.
- Manifest harvest carry IM-14-F49 (E4): add controls for a missing listed FLOOR file and for an unused LEDGER entry (both must fail; the rules are already stated in the inventory).
- Manifest harvest carry IM-14-F53 (E4): the LEDGER path key is the repo-relative POSIX string used in FLOOR.
- Manifest harvest carry IM-14-F35 (E3, C5 note): camelCase claims (for example raceFree) are not caught by the detector and remain residue; camelCase in floor lines is reviewed in the shipment merge diffs by the residue audit.
- Manifest harvest carry IM-14-F26 (E3, informational): recorded that tests.test_harness_read_case_coverage is not in the C5 CI step's module list as the plan writes it; the step's log is therefore not coverage-test evidence (the coverage test runs in the canonical suite).
- Manifest harvest carry IM-14-F67 (E5; C5 and harvest note): lines created by resolving a conflict in a harvest merge commit fall outside every non-merge harvest commit's diff; harvest merges are conflict-free, or the combined diff of a conflicted merge is audited and recorded.
- Manifest harvest carry CONST-II-F01 (E2): the coverage and audit structural tests 'may pass'; record their pre-implementation failing run as gap characterization (Principle II evidence), not RED.
- Manifest harvest carry NOROW-F18 (E2; shared with D1): .proof-scratch/ is Git-ignored here but not guaranteed in installed workspaces; verify the ignore rule before the IM-02 Windows evidence run writes fixtures under .proof-scratch/tmp.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan task C5 (docs/plans/2026-09-25-ship-lifecycle-release-units-plan.md, blob b7a77c76, section 'C5: Linux-native gate and case coverage'; release unit S(C); review authority: manifest E5 Verdict under OP-5).

Files (budget): .github/workflows/ci.yml (one new step in the existing ubuntu-latest test job); tests/test_harness_read_case_coverage.py; tests/test_harness_noclaim_audit.py.

Change: the CI step uses shell: bash, set -euo pipefail and export TMPDIR="$RUNNER_TEMP". It runs PYTHONPATH=src python -m unittest -v tests.test_harness_read_lexical tests.test_harness_read_containment tests.test_harness_read_bounds tests.test_harness_read_nonregular tests.test_harness_noclaim_audit logging output, and fails on a non-zero exit or unless the summary is OK with no skipped=. It prints platform.system(), uname -r, and stat -f -c %T of the checkout and of the effective fixture root (python -c "import tempfile; print(tempfile.gettempdir())" under the same environment), asserts that root is under $RUNNER_TEMP, and fails unless both types are ext2/ext3 (ext4), xfs or btrfs (tmpfs, overlayfs, 9p and fuse fail). The coverage test asserts every Proof G case ID G01 to G32 appears in a test name, G31 is Linux-only, and every other ID has a variant that runs on both hosts. The audit test implements the plan's Non-Claim Audit Inventory: FLOOR (a closed list read from the working tree, no git, no skip path; C5 lists harness_read.py; a listed file missing fails), Reading (strict UTF-8, CR removed, lowercase, _ and - to spaces, whitespace collapsed, adjacent-pair joins with the hyphen rule, SHA-256 of normalized UTF-8 per unit), the Detector regex, Clearing (required-sentence removal; closed LEDGER of (path, SHA-256) non-claim entries, C5 adds C's entries confirmed by local review; any other hit fails, as does an unused entry), the required-sentence assertion, and the listed must-fail and must-not-hit Controls. -v appears only in this CI evidence step; it is not the P-004 canonical command and is never RED evidence.

Trace: IM-01 (P1, non-waivable), IM-02 (Linux half), IM-14 / PE-SAFETY-06 (P2-critical), IM-15. Estimate 80 min; size S; complexity low.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Label harness-surface:none for the workflow step; the coverage and audit tests are structural. Blocked by C4; last task of S(C). B1 later adds harness_surfaces.py and D1 harness_verdict.py to FLOOR (IM-14-F51); C5 lists only harness_read.py. The audit's Residue rule is agent text audit outside FLOOR (no operator duty).
<!-- END:implementation-notes -->