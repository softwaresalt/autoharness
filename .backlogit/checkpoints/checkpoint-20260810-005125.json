{"schema_version":1,"agent":"stage","session_id":"stage-2026-08-09-34d50f2d-candidate-a-composability-spike","phase":"spike-complete","status":"resolved","created_at":"2026-08-10T00:52:00Z","updated_at":"2026-08-10T00:51:28.1189547Z","context":{"task_ids":["004-SP"],"branch":"main"},"progress":{"tasks_remaining":["OPERATOR: confirm the CONDITIONAL PROCEED condition (candidate (a) scoped as CONSOLIDATION only; non-goals accepted) before any impl-plan. NOT CONFIRMED.","ORCHESTRATOR PUBLICATION: commit/push the UNCOMMITTED Stage artifact set. NOT DONE - Stage deliberately left everything uncommitted.","IF PROCEEDING on (a): impl-plan -\u003e plan-harden (REQUIRED, P-006, elevated blast radius) -\u003e plan-review -\u003e harvest over proposed T1-T8. NOT STARTED; no implementation feature/task/shipment created this session.","CANDIDATE (c) background Verification and Compaction: still DEFERRED; needs its own spike -\u003e impl-plan -\u003e plan-review -\u003e harvest. NOT STARTED."],"decisions":["Bounded Stage-owned research spike for stash 34D50F2D candidate (a) executed as backlogit spike 004-SP (status done, auto-archived to .backlogit/archive/004-SP.md), with durable findings/decision doc docs/decisions/2026-08-09-composability-single-source-of-truth-spike.md.","CLASSIFICATION = PARTIAL GAP. VERDICT = CONDITIONAL PROCEED (confidence medium-high). Condition: (a) must be scoped as CONSOLIDATION of logic that already exists. NO-GO if product-spec section 3 is wanted literally (runtime action/observation executor, sequential pipelining, stderr-to-model routing) - that is AGENT-RUNTIME territory (Copilot CLI / Claude Code), not autoharness, which is a harness-COMPOSITION tool.","SURFACE INVENTORY: the CLI is the ONLY real surface (single entry point autoharness.cli:main, 11 commands, hand-rolled parsing). autoharness exposes NO MCP server of its own - no MCP SDK dependency and zero FastMCP/mcp.server/stdio_server hits in src/; its only mcp strings are backlog-REGISTRY VALIDATION codes (verify_workspace.py:140-159) for EXTERNAL tools. The Python library surface is NOMINAL not contractual: zero `from autoharness` consumers outside src/ and tests/, no __all__. The de-facto second surface is stringly-typed agent shell prose (argv + exit codes + ad-hoc JSON).","GAP INVENTORY D1-D10. D1 (highest severity) install-vs-verify template-variable derivation is genuinely DUPLICATED across the install-harness/tune-harness PROSE skills and Python (_render_template, _derive_template_variables, _language_defaults, _resolve_source_template, _scan_uninstalled_templates) with nothing forcing agreement - prose-vs-code, not surface-vs-surface. D2 the adapter MUTATES the pipeline-topology verdict (--force rewrites BLOCK to PASS via dataclasses.replace in cli.py). D3 one of seven dag-readiness outcomes (`degraded`) plus the whole `status` field is CLI-EXCLUSIVE and unreachable from any non-CLI caller. D4 the ONLY ToolTelemetryEvent construction site in the product is cli.py:789 incl. outcome classification and redaction policy. D5 verify-workspace pass/fail is DEFINED in the adapter (_report_has_failures). D6 ~150 lines of telemetry payload validation/merge semantics sit in the adapter. D7 setup-* is ~250 lines with NO core module at all. D8 divergent result shapes, no shared envelope. D9 divergent error/exit-code semantics specified only in CLI prose. D10 no declared public API and no second consumer proving core sufficiency.","ALREADY GOOD (preserve, do not rebuild): gate/telemetry cores are genuinely surface-independent and dependency-injected (TopologyReaders Protocol, run_fn, fetch_fn) and heavily tested. The gap is NOT testability - it is that policy, observability, and degraded-mode synthesis sit ABOVE the core in the adapter.","MINIMAL TARGET: one thin application/service function per capability owning the stranded policy; ONE shared typed result envelope (status/exit_code/data/messages/warnings/artifacts) that WRAPS rather than rewrites existing results incl. the 4453-line verify_workspace dict; ONE error taxonomy plus machine-readable exit-code contract; cli.py reduced to parse-\u003ecall-\u003erender-\u003eexit with NO policy; declared public API. Do NOT build an MCP server or HTTP API now.","AUTHORITY CONTRACTS: adapters may translate transport and render output but MUST NOT own policy - no verdict mutation, no audit-log authorship, no telemetry classification, no synthesis of outcomes the core cannot produce. Every observable outcome must be core-reachable. Telemetry emitted by the core with tool_surface supplied BY the adapter. Engram READ-ONLY with NO authority. .autoharness/config.yaml remains the model-routing authority; no hardcoded spec model names.","CANDIDATE (c) DEPENDENCY VERDICT: (c) BENEFITS FROM (a) but DOES NOT DEPEND ON IT. (c) already has both substrates - Python telemetry/log parsing (telemetry/reader.py, aggregation.py, tool_event_compose.py, _jsonl_segments.py, gaps.py) and prose-side compaction (P-020, context-efficiency, prune-on-restore shipped in 002-SP/111-F). Keep the operator (a)-then-(c) sequence but do NOT declare a blocking dependency.","OPERATOR DECISIONS PERSISTED to living tracker 34D50F2D via the SUPPORTED `backlogit stash edit` path (append-only, prior text preserved; entry REMAINS ACTIVE because candidate (c) is outstanding): (1) sequencing (a) then (c); (2) Engram read-only sidecar with no execution/mutation authority, (c) must not assign it authority; (3) spec model names illustrative/non-authoritative, config.yaml authoritative, no work to hardcode spec names - this CLOSES the long-standing spec model-pick reconciliation open question in favor of config with NO backlog work; (4) composability intent - one source of truth behind thin API/MCP/CLI surfaces.","HOUSEKEEPING: a first create_item call failed on a section-name whitespace constraint and left an orphaned partial artifact 003-SP (frontmatter only, no findings, stuck active). It was DELETED to preserve backlog integrity before the final sync. The successful artifact is 004-SP.","VALIDATION: backlogit sync = 747 indexed; git diff --check exit 0 for worktree and cached (only an informational CRLF notice on .backlogit/stash.jsonl); single clean worktree at 6dbdde67 [main], no spike/research worktree created (P-016 exception NOT exercised); all 26 file paths cross-referenced by the new docs verified to EXIST; backlogit doctor reports only 3 PRE-EXISTING unrelated orphans (048.001-T, 048.002-T, 048.003-T) that this session did not create and deliberately did not fix.","ROLE BOUNDARY HELD (Stage-only, research-only): no source/template/schema/config mutation, no branch or worktree creation, no commit/push/PR, no shipment created or claimed, no implementation feature/task created, no Ship work. External sidecars backlogit/Engram/graphtor treated as READ-ONLY and not mutated. All artifacts left UNCOMMITTED for the Orchestrator. Route claude-opus-5/anthropic/high; base HEAD = origin/main = 6dbdde67a8932b0765af4af664f2d92c318a6ccc. No stop conditions hit."]},"resume_hint":"NO RECOVERY RESUME REQUIRED - Stage COMPLETED CLEANLY; this is a completion record, not a crash-recovery cursor, so no Stage session should restore or resume from it. Bounded read-only Stage spike for stash 34D50F2D candidate (a) (unified library/CLI/MCP composability): executed as spike 004-SP with decision doc docs/decisions/2026-08-09-composability-single-source-of-truth-spike.md. RESULT: PARTIAL GAP / CONDITIONAL PROCEED (medium-high) - proceed only as CONSOLIDATION of existing logic; NO-GO for a literal spec-section-3 runtime executor. autoharness has NO MCP server of its own and the CLI is the only real surface; the core is well-factored but policy, audit, telemetry emission, degraded synthesis and pass/fail definitions are stranded in cli.py, setup-* has no core at all, and install-vs-verify template-variable derivation is genuinely duplicated across prose and Python. Candidate (c) BENEFITS FROM but does NOT depend on (a). FILES CHANGED (all UNCOMMITTED, left for Orchestrator): .backlogit/archive/004-SP.md, .backlogit/stash.jsonl, docs/decisions/2026-08-09-composability-single-source-of-truth-spike.md, docs/memory/2026-08-09-stage-34d50f2d-candidate-a-composability-spike.md. NEXT: Orchestrator publishes the uncommitted set; operator confirms the CONDITIONAL PROCEED condition before any impl-plan (which REQUIRES plan-harden, P-006). No implementation feature/task/shipment was created and stash 34D50F2D REMAINS ACTIVE as the living tracker for candidate (c)."}