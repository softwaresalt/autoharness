---
artifact_type: task
created_at: 2026-07-13T15:21:04.9179106-07:00
id: 087.001-T
parent_id: 087-F
priority: medium
status: done
title: track-and-bunx-normalize-root-mcp-json
updated_at: 2026-07-13T16:33:01.6846755-07:00
---


<!-- BEGIN:acceptance-criteria -->
- `.mcp.json` is git-tracked.
- `.gitignore` no longer ignores `.mcp.json` and its comment is consistent.
- Each JS-engine server (context7, tavily, github) launches via `bunx`; where `bunx --bun` is verified to initialize the server with Node unavailable, `--bun` is used to force the Bun runtime; where a server is not Bun-compatible, bare `bunx` is retained and the retained Node dependency is recorded in the docs task. Zero `npx` remnants for JS-engine servers either way.
- No literal secret is committed.
- `pwsh`/github-server changes are NOT made (deferred to 077-F).
- `git ls-files --error-unmatch .mcp.json` succeeds, `git check-ignore .mcp.json` matches nothing, and the final `git status` is clean for `.mcp.json`.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Ship removes `.mcp.json` from `.gitignore` (delete the `.mcp.json` ignore line ~24; keep editor-local `.vscode/.cursor/.claude` mcp.json ignores) so the root file is tracked, matching the existing gitignore comment. `git add .mcp.json`. Verify every JS-engine MCP server (context7, tavily, github) launches via `bunx` with NO `npx` remnants; native-binary servers (backlogit/engram/graphtor-docs) unchanged. Per JS-engine server, test `bunx --bun` initialization, including with Node unavailable, to determine whether Node can truly be dropped. Prefer `bunx --bun` where it initializes successfully under the Bun runtime; where `--bun` breaks a Node-only server, fall back to bare `bunx` and record that Node remains required for that server in the docs task. LEAVE the `pwsh` wrappers and the deprecated `@modelcontextprotocol/server-github` target to 077-F.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Description-of-work only; Ship performs the gitignore edit + `git add` + verification. Do not templatize `.mcp.json`.
<!-- END:implementation-notes -->
