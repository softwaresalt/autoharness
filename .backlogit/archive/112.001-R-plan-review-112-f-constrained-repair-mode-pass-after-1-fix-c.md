---
artifact_type: review
created_at: 2026-08-05T23:49:05.9743051Z
id: 112.001-R
parent_id: 112-F
status: done
title: 'Plan-review: 112-F constrained repair mode — PASS after 1 fix cycle + 2026-08-06 re-review + 2026-08-05 second/third re-review + 2026-08-06 publication-review (P0/P1 clear)'
updated_at: 2026-08-06T04:50:00Z
---



<!-- BEGIN:decisions -->
ACCEPT plan for harvest with the P1-1 conservative torn-state refusal folded into task B1 acceptance criteria (proceed only on clean pre-mode classification; any anomaly refuses). P0 clear; P1 resolved in-cycle; RE-REVIEW 2026-08-06 raised and resolved P1-3 (unreachable topology precondition -> target-aware read-only inspection over existing reads); SECOND RE-REVIEW 2026-08-05 raised and resolved P1-4 (overstated "provably-safe"/"topology-gated" self-repair guarantee -> retained-explicit TOCTOU, DETECTION-not-prevention, post-claim two-active fail-closed to operator remediation with NO forbidden active->queued rollback; constrained forward repair judged ACCEPTABLE, not re-scoped to report-only); no outstanding P0/P1. Hardening REQUIRED and completed (H1-H8). Hardening REQUIRED and completed (H1-H8; H3 recast to malformed-status hard-exclusion in the third re-review). Backward/auto/silent repair permanently excluded. THIRD RE-REVIEW 2026-08-05 corrected two P2 factual defects against backlogit 1.8.0 source: the re-claim is STRICTLY SINGLE-SHOT (NOT idempotent; active->active is rejected with ErrShipmentConflict — the double-claim guard), and the nonexistent `blocked` shipment status / blocked->queued transition is removed from supported repair paths (a legacy `blocked` record is malformed legacy data => halt for manual remediation). P0/P1 remain clear. PUBLICATION-REVIEW 2026-08-06 corrected one P2 contract defect: the P1-1 torn-state refusal was operationalized as "every manifest item `matched`", which is UNREACHABLE because pre-mode `matched` keys on a SINGLE `expected_status` while a valid queued-with-active-work target has MIXED task statuses — replaced with a PER-TASK ALLOWED-STATE classification (reachable mixed-status target; refuse on any per-item anomaly) that preserves the conservative refusal without weakening ambiguity refusal. PUBLICATION-REVIEW (P1) 2026-08-06 then corrected a further factual defect (P1-2): the per-task allowed-state predicate misrepresented a COMPLETED manifest task as a live `status: done` record ("queue-or-archive for `done`"; status in {queued|active|done}) — backlogit persists a completed task as `status: archived` + `archived_status: done` in `archive/` ONLY (verified read-only against real archived records e.g. `.backlogit/archive/012-DL.md`), never as a live `status: done` — replaced with a precise PER-TASK ROLE predicate requiring a UNIQUE, NON-CONFLICTING record per role (live-queued/live-active are queue records with matching status and no archive copy; archived-completed = `status: archived` + `archived_status: done` + valid `archived_from` provenance in archive with no conflicting queue record) that FAILS CLOSED on duplicate/conflicting/missing/malformed-provenance. P0/P1 remain clear.
<!-- END:decisions -->

<!-- BEGIN:findings -->
P0-1 (checked): could auto/silent repair ever fire? NO — repair is reachable only via the explicit `repair-record-status` mode + `--confirm`; pre/post/safe-close remain no-mutation (H1 isolation). CLEAR. P0-2 (checked): could repair create two active shipments? NO — topology precondition (zero OTHER active) + exactly-one-active==target post-condition (H2). CLEAR. P1-1 (RAISED): precondition (iii) 'tasks consistent with a silently-dropped claim, not torn/partial' is under-specified — the skill could mis-repair a genuinely torn mid-flight state. RESOLUTION: make the precondition CONSERVATIVE/refuse-by-default — proceed ONLY when the existing pre-mode per-item classification is clean (every manifest item `matched`; NO `missing`/`orphan`/`status-mismatch`/`pre-archived` anomaly) AND the record is `queued`; ANY pre-mode anomaly => REFUSE + report-and-halt. P1-2 (checked): audit/telemetry must fire on REFUSE and DEGRADED, not only success (H5). CLEAR. P2 (minor): `--confirm` operator-only semantics — acceptable for v1.

RE-REVIEW 2026-08-06 (independent BLOCKED-review repair, route claude-opus-4.8/anthropic/high): P1-3 (RAISED): precondition (i) as originally written ('invoke `autoharness gate pipeline-topology` and require zero other active shipments') is UNREACHABLE for the exact state being repaired — `topology.py::_evaluate_core` runs `_detect_before_consistency` before `_active_invariant_check`, so the gate early-returns `SHIPMENT_STATE_INCONSISTENT` (blocked) on the target's own queued-with-active-work condition before the zero-active signal is ever computed; the repair mode could never satisfy the precondition. RESOLUTION (accepted): replace precondition (i) with a TARGET-AWARE READ-ONLY REPAIR-INSPECTION composed of EXISTING backlogit read commands (`shipment list` + per-shipment/per-task status reads) that requires ZERO active shipments AND that the target is the SOLE queued-with-active-work inconsistency (every OTHER queued/blocked shipment has no active/done task), and REFUSES on any other active shipment / any non-target inconsistency / any ambiguity (fail closed on all non-target ambiguity). Forward-only queued->active via existing atomic ClaimShipment (061-F); no locks/CAS; operator-invoked `--confirm`; no silent/broad/backward repair. Folded into 112.001-T AC precondition (i), 112.002-T test AC (refuse when target not sole inconsistency), 112-F IMPL-PLAN + hardening H7, and 013-DL Addendum A. WIDTH-ISOLATION: inspection is SKILL prose over existing reads (single template family) — no new gate/CLI code, no separate task required; 118-S membership unchanged {112.001,112.002,112.003,112.004}. P1-3 RESOLVED. No new P0/P1 outstanding after re-review.

SECOND RE-REVIEW 2026-08-05 (independent BLOCKED-review repair, route claude-opus-4.8/anthropic/high): P1-4 (RAISED): the plan/provenance described the forward re-claim as "provably-safe" and "topology-gated", OVERSTATING the guarantee. The target-aware inspection and the ClaimShipment are SEPARATE, UNLOCKED operations; a concurrent claim can race between them; the post-condition may only DETECT a second active AFTER target activation; and the target's active->queued rollback is FORBIDDEN (backlogit 1.8.0 isValidShipmentTransition). So the zero-active inspection + ClaimShipment do NOT prevent two active shipments, do NOT serialize claims, and are NOT provably safe. RESOLUTION (accepted): RETAIN the TOCTOU limitation explicitly (identical to the already-accepted 109-F claim TOCTOU: best-effort DETECTION, not prevention); NEVER claim prevention/serialization/atomic exclusion/"provably safe"; the GLOBAL post-condition DETECTS post-claim two-active and FAILS CLOSED (audit+telemetry, HALT, operator remediation) with NO forbidden active->queued rollback; fail-closed manual remediation = operator resolves via backlogit's sanctioned lifecycle transitions only; a concurrent-claim/post-condition-failure test is REQUIRED. ACCEPTABILITY: the constrained operator-invoked FORWARD repair REMAINS ACCEPTABLE with this residual risk (operator-invoked/single-shot; same risk class as 109-F; detect + fail-closed bounds blast radius; no forbidden rollback); report-only was considered and REJECTED (no safety gain, loses a legitimate operator capability). Folded into 112.001-T (AC 4/5 TOCTOU + post-condition detection + no-rollback remediation), 112.002-T (concurrent-claim/post-condition-failure test), 112.003-T (docs residual TOCTOU + no-rollback), 112.004-T (audit two-active outcome), 112-F IMPL-PLAN/DoD/goals + hardening H8, and 013-DL Addendum B. WIDTH-ISOLATION: prose + audit/telemetry in the shipment-reconcile SKILL family; 118-S membership unchanged {112.001,112.002,112.003,112.004}. P1-4 RESOLVED. No new P0/P1 outstanding after the second re-review.

THIRD RE-REVIEW 2026-08-05 (independent BLOCKED-review repair, route claude-opus-4.8/anthropic/high — two P2 factual corrections verified against backlogit 1.8.0 source): P2-1 (RAISED — ClaimShipment miscalled "idempotent"): 013-DL described the forward re-claim as "atomic and idempotent." Verified WRONG: `isValidShipmentTransition` permits only `queued->active` and `active->{shipped,abandoned}`, so a re-claim on an already-`active` shipment (`active->active`) returns `ErrShipmentConflict` — REJECTED, not a no-op. RESOLUTION (accepted): re-cast as atomic and STRICTLY SINGLE-SHOT (NOT idempotent); the rejection IS the double-claim guard and preserves the TOCTOU fail-closed behavior (a repeat/racing second claim fails closed, never silently re-activates). Folded into 013-DL Addendum C + chosen-direction (iv), 112-F IMPL-PLAN precondition (iv), 112.002-T (test note), 112.003-T (docs). P2-2 (RAISED — nonexistent `blocked` shipment lifecycle): 112-F/013-DL/tasks referenced a `blocked` shipment record and a `blocked->queued` gate-resolution transition. Verified: backlogit 1.8.0 `ShipmentStatus` is ONLY `queued|active|shipped|abandoned` and `isValidShipmentTransition` has no blocked state and no blocked->queued edge — the transition is fabricated. RESOLUTION (accepted): REMOVE `blocked` from all supported repair paths; the re-claimable status is `queued` only; a non-lifecycle/legacy `blocked` status is classified as MALFORMED LEGACY DATA and HALTED for operator manual remediation (no fabricated transition). Hardening H3 recast BLOCKED-HARD-EXCLUSION -> MALFORMED-STATUS-HARD-EXCLUSION. Folded into 013-DL Addendum C + chosen-direction (i)(b)/(ii), 112-F IMPL-PLAN (ii)/H3/DoD, 112.001-T (AC 2(i)/(ii)), 112.002-T (test), 112.003-T (docs). WIDTH-ISOLATION: shipment-reconcile SKILL family; 118-S membership unchanged {112.001,112.002,112.003,112.004}; deps/edges unchanged. Both P2 RESOLVED; no new P0/P1 outstanding after the third re-review.

PUBLICATION-REVIEW 2026-08-06 (independent publication-review repair, route claude-opus-4.8/anthropic/high): P2-3 (RAISED — unreachable torn-state gate): the P1-1 resolution folded into 112.001-T AC 2(iii) required "the existing pre-mode per-item classification is CLEAN (every manifest item `matched`)". Verified against `templates/skills/shipment-reconcile/SKILL.md.tmpl` (read-only, NOT mutated): pre-mode classifies an item `matched` ONLY when its declared status equals the SINGLE `expected_status` parameter (classification table ~line 40; pre-mode step 3 ~line 159). A valid queued-with-active-work TARGET has MIXED manifest-task statuses by definition (some `active`, some `done`, possibly some `queued`), so NO single `expected_status` can make every task `matched` — the all-`matched` gate is UNREACHABLE and would REFUSE the exact reparable state, contradicting chosen-direction precondition (iii). RESOLUTION (accepted): replace the reuse-pre-mode-`matched` gate with a PER-TASK ALLOWED-STATE classification defining the reachable narrow silently-dropped-claim target state — each manifest task file PRESENT in the location expected for its status (queue for `queued`/`active`; queue-or-archive for `done`), status within the allowed lifecycle set {`queued`|`active`|`done`}, NO orphan, NO missing/malformed/out-of-set/torn anomaly; proceed only when EVERY task satisfies the predicate, refuse on ANY per-item anomaly. Ambiguity refusal is UNWEAKENED (the torn signal is a per-item ANOMALY, not the mixedness of statuses); all other invariants preserved (operator-invoked `--confirm`, single-shot, forward-only via ClaimShipment, TOCTOU detection-not-prevention, no active->queued rollback, malformed-legacy-status halt, fail-closed non-target ambiguity, no-auto-mutation pre/post/safe-close default whose single-`expected_status` classification is UNCHANGED). Folded into 112.001-T (AC 2(iii)), 112.002-T (mixed-status proceed regression guard + per-item anomaly refusal), 112.003-T (docs AC 1), 112.004-T (audit records per-task statuses), 112-F IMPL-PLAN precondition (iii)/DoD (3), and 013-DL Addendum D. WIDTH-ISOLATION: contract-only prose correction within the shipment-reconcile SKILL family; NO template mutation (tasks remain queued/unimplemented); 118-S membership unchanged {112.001,112.002,112.003,112.004}; deps/edges unchanged. P2-3 RESOLVED; no new P0/P1 outstanding after the publication-review.

PUBLICATION-REVIEW (P1) 2026-08-06 (independent publication-review repair, route claude-opus-4.8/anthropic/high): P1-2 (RAISED — archived-completed misrepresented as live `done`): the PER-TASK ALLOWED-STATE predicate from P2-3 / 013-DL Addendum D described a completed manifest task as PRESENT "queue-or-archive for `done`" with status within {`queued`|`active`|`done`} — i.e. as a live `status: done` record. Verified read-only against real backlogit archived records (`.backlogit/archive/012-DL.md` and the archived review artifacts): an archived/completed artifact carries `status: archived` PLUS `archived_status: done` PLUS provenance `archived_from` and lives in `archive/` ONLY — there is NO live `status: done` queue record. A predicate admitting a live `done` record is factually wrong and cannot detect duplicate/conflicting records. RESOLUTION (accepted): define the predicate PRECISELY per ROLE requiring a UNIQUE, NON-CONFLICTING representation — a `queued`/`active` task is a UNIQUE LIVE QUEUE record (queue file, matching status, NO archive copy); a COMPLETED task is a UNIQUE ARCHIVE record ONLY (`status: archived` + `archived_status: done` + valid `archived_from` provenance, NEVER a live `status: done`, NO conflicting queue record). FAIL CLOSED on ANY duplicate (id in both queue and archive), conflicting (queue status vs role mismatch, archive-alongside-live-queue, or a live `status: done` in queue), missing (no queue or archive file), malformed/absent archive provenance (`archived_status`/`archived_from`), orphan, out-of-role, or torn-partial anomaly; proceed only when EVERY manifest task satisfies its role predicate uniquely and non-conflictingly. Ambiguity refusal UNWEAKENED (per-item anomaly, not mixedness of roles). All other invariants preserved (operator-invoked `--confirm`, single-shot, forward-only via ClaimShipment, TOCTOU detection-not-prevention, no active->queued rollback, malformed-legacy-status halt, fail-closed non-target ambiguity, unchanged single-`expected_status` pre/post/safe-close default). Folded into 112.001-T (AC 2(iii)), 112.002-T (mixed-ROLE proceed regression guard + duplicate/conflicting/missing/malformed-provenance refusal), 112.003-T (docs AC 1/4), 112.004-T (audit records per-task role + archive provenance), 112-F IMPL-PLAN precondition (iii)/DoD (3), and 013-DL Addendum E. WIDTH-ISOLATION: contract-only prose within the shipment-reconcile SKILL family; NO template mutation (tasks remain queued/unimplemented); 118-S membership unchanged {112.001,112.002,112.003,112.004}; deps/edges unchanged. P1-2 RESOLVED; no new P0/P1 outstanding after the publication-review.

PUBLICATION-REVIEW (P1) 2026-08-06 — SECOND ALIGNMENT (independent publication-review repair, route claude-opus-4.8/anthropic/high, DARK_MODE): P1-2b (RAISED — single archive representation too narrow): the P1-2 resolution (013-DL Addendum E) accepted ONLY the `status: archived` + `archived_status: done` + `archived_from` archive representation and rejected a live-or-archive `status: done` outright. Verified read-only against real backlogit archived records that backlogit persists a completed artifact in EITHER of TWO valid archive-directory representations, both present in this workspace: (a) TERMINAL RELOCATION — an archive record with `status: done` and NO `archived_status`/`archived_from` (e.g. `.backlogit/archive/002.001-T.md`, `.backlogit/archive/104.001-T.md`, and THIS very review archive `112.001-R-…md`, which carries `status: done`); and (b) EXPLICIT ARCHIVAL — `status: archived` + `archived_status: done` + valid `archived_from` (e.g. `.backlogit/archive/012-DL.md`). Addendum E's single-representation predicate would FAIL CLOSED on representation (a) — the exact shape in which many completed tasks and this review itself are archived. RESOLUTION (accepted): a COMPLETED task MUST be a UNIQUE ARCHIVE record (archive-only, no conflicting live queue record) in EITHER representation — (a) `status: done` (provenance NOT required) OR (b) `status: archived` + `archived_status: done` + valid `archived_from`. FAIL CLOSED on ANY duplicate (id in both queue and archive), conflicting (archive-alongside-live-queue, or a live `status: done` in the QUEUE), missing, malformed/absent provenance on a `status: archived` record, ANY OTHER archived status/provenance shape, orphan, out-of-role, or torn-partial anomaly; proceed only when EVERY manifest task satisfies its role predicate uniquely and non-conflictingly. Ambiguity refusal UNWEAKENED. Folded into 112.001-T (AC 2(iii)), 112.002-T (mixed-ROLE proceed regression guard + refusal set), 112.003-T (docs AC 1/4), 112.004-T (audit records both archive representations), 112-F IMPL-PLAN precondition (iii)/DoD (3), and 013-DL Addendum F. WIDTH-ISOLATION: contract-only prose within the shipment-reconcile SKILL family; NO template mutation (tasks remain queued/unimplemented); 118-S membership unchanged {112.001,112.002,112.003,112.004}; deps/edges unchanged. P1-2b RESOLVED; VERDICT REMAINS PASS; no new P0/P1 outstanding.
<!-- END:findings -->

<!-- BEGIN:summary -->
Independent plan-review of 112-F (constrained operator-invoked shipment-record-status repair; 013-DL Option C). VERDICT: PASS after one review-fix cycle plus a 2026-08-06 re-review plus a 2026-08-05 second independent re-review — P0/P1 CLEAR. Reviewed against the no-auto-mutation safety invariant, the at-most-one-active invariant, and the operator's do-not-invent-locks constraint. Re-review corrected the unreachable topology precondition (P1-3); the second re-review corrected the overstated "provably-safe"/"topology-gated" guarantee (P1-4) to a retained-explicit TOCTOU with DETECTION-not-prevention semantics, post-claim two-active fail-closed to operator remediation, and no forbidden active->queued rollback. The third re-review corrected two P2 factual defects: ClaimShipment is STRICTLY SINGLE-SHOT (NOT idempotent — active->active rejected with ErrShipmentConflict, the double-claim guard), and the nonexistent `blocked` shipment status / blocked->queued transition is removed from supported repair paths (legacy `blocked` => malformed legacy data => halt for manual remediation). PUBLICATION-REVIEW 2026-08-06 corrected one further P2 contract defect (P2-3): the P1-1 torn-state refusal, operationalized as an all-`matched` pre-mode gate, was UNREACHABLE for a mixed-status target (pre-mode `matched` keys on a single `expected_status`) — replaced with a PER-TASK ALLOWED-STATE classification over a reachable mixed-status target, preserving conservative refusal and all other invariants. PUBLICATION-REVIEW (P1) 2026-08-06 then corrected the completed-task representation (P1-2): a completed task is archived (`status: archived` + `archived_status: done` + `archived_from`), never a live `status: done` in the queue; a SECOND alignment (P1-2b) broadened this to accept EITHER of backlogit's two valid archive representations — (a) a `status: done` archive record (no provenance required) OR (b) `status: archived` + `archived_status: done` + valid `archived_from` — both unique in archive with no conflicting live queue record, failing closed on duplicate/conflict/missing/malformed/any-other-archived-status (013-DL Addenda E + F). VERDICT REMAINS PASS; P0/P1 CLEAR.
<!-- END:summary -->