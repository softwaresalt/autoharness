---
artifact_type: task
commit: cd48473
created_at: 2026-08-05T03:24:05.0939471Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-two-axis-v1
    size_source: agent
dependencies:
    - 109.016-T
id: 109.020-T
labels:
    - topology
    - ship-agent
    - reconcile
    - safe-close
    - p-015
    - p-001
parent_id: 109-F
priority: medium
status: done
title: 'A7b: point Ship agent safe-close summary to corrected canonical shipment-reconcile skill (non-cascading terminal marker)'
updated_at: 2026-08-05T05:50:27.7303341Z
---



<!-- BEGIN:acceptance-criteria -->
* The Ship agent safe-close SUMMARY in `templates/agents/_ship.agent.md.tmpl` and installed `.github/agents/_ship.agent.md` references the corrected canonical `shipment-reconcile` skill algorithm and contains NO inline instruction to call the cascade `backlogit_ship_shipment` for a partial-feature shipment.
* The Ship agent summary states the non-cascading shipment-record closure at pointer level: move the shipment record to live `status: shipped` (`backlogit move <shipment_id> --status shipped`), verify live status, archive only that shipment record (`backlogit archive <shipment_id>`), verify `archived_status: shipped`; the authoritative step-by-step lives in the shipment-reconcile skill (109.016-T), not duplicated in the agent.
* Any residual cascade description — e.g. "treats the shipment as a proxy for the covering feature" or "archives the parent and unshipped siblings" — is removed or reframed as the FORBIDDEN behavior the skill replaces. Describe the actual `backlogit_ship_shipment` mechanics at HEAD `fd8d2c9` accurately: it REQUEUES + DETACHES unshipped descendant tasks (sets them `queued` and clears `parent_id`) rather than archiving them, and PRESERVES a non-member covering feature (snapshot/restore) rather than archiving it — but it remains P-015-forbidden for a partial-feature shipment because it requeues/detaches the downstream siblings and archives the release-scope members outside the safe-close ordering.
* Scoped verify_workspace / template-structure assertions confirm the agent summary points to the corrected skill and that no `backlogit_ship_shipment` cascade call remains in the Ship safe-close path; assertions parse the pointer, not merely count strings.
* Change is confined to `templates/agents/_ship.agent.md.tmpl` and installed `.github/agents/_ship.agent.md` plus the scoped assertion files; no backlogit source is mutated and the canonical skill algorithm itself is owned by 109.016-T.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
[Shipment A / integration companion to A7] Update the Ship agent template + installed agent safe-close SUMMARY so it points to the corrected canonical `shipment-reconcile` skill (109.016-T) and no longer instructs or implies the P-015-forbidden cascade `backlogit_ship_shipment` for partial-feature shipments. The agent carries only a summary pointer; the authoritative non-cascading move-to-`shipped` -> verify -> archive -> verify sequence and the sequence-aware protected set live in the skill. Ships in 114-S alongside 109.016-T so that, after the mandatory pre-self-close reload (109.019-T), 114-S itself closes under the corrected contract.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on 109.016-T (the canonical skill correction). Edit the Ship Step 5/6 closure summary in `_ship.agent.md.tmpl` to reference `shipment-reconcile` safe-close mode and the non-cascading shipment-record terminal-marker sequence; remove/replace any inline cascade `backlogit_ship_shipment` proxy-for-feature language (currently near `_ship.agent.md:484-517`). Keep the agent a thin pointer; do not duplicate the algorithm. Coordinate template-structure assertions with 109.015-T. No backlogit source mutation.
<!-- END:implementation-notes -->