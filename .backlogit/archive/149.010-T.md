---
archived_from: .backlogit/queue/149.010-T.md
archived_status: done
artifact_type: task
commit: f93afa0eee8d228ff4a7ac54cf3b2b3b4ec5eeb9
created_at: 2026-08-28T04:03:55.717397Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 149.005-T
id: 149.010-T
labels:
    - S1
    - tests
parent_id: 149-F
priority: medium
references:
    - docs/plans/2026-08-27-pre-review-detector-sdk-plan.md
    - tests/
status: archived
title: 'U10 — Tests: assembler and cycle detection'
updated_at: 2026-08-30T01:18:50.1351698Z
---

Unit tests for the DAG assembler: cycle -> invalid/exit 2 with zero nodes evaluated, and blocked_upstream propagation. Domain: tests. Files: 1.

<!-- BEGIN:acceptance-criteria -->
* **INV-4**: the cycle test asserts the evaluation **COUNT is exactly zero**, not merely that the exit code is 2. A gate that exits 2 while having already evaluated nodes has auto-broken the cycle.
* `blocked_upstream` propagation asserted for **both** `failed` and `insufficient_evidence` upstreams, and asserted NOT `passed` and NOT `skipped`.
* Outcomes are read from the canonical `status` field only (D-9).
* Deterministic node iteration — the reported cycle is stable across runs.
* No network access.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan unit **U10**. Create `tests/test_detectors_assembler.py`.

**2 scenarios**:
1. **Cycle injection** -> `invalid`, exit 2, and **zero nodes evaluated**.
2. **Upstream propagation** — an upstream `failed` AND an upstream `insufficient_evidence` each yield downstream `blocked_upstream` (asserted NOT `passed`, NOT `skipped`).

**Gate command (Q5)**: `PYTHONPATH=src python -m unittest discover -s tests`
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
**Depends on U5 only.** The report-emitter scenarios that previously lived here moved to U10b (149.014-T, shape + freshness semantics) and U10c (149.015-T, epoch determinism + concurrency), so this unit no longer depends on U7.

**Split provenance (PR #414 cycle 1)**: the D-11 epoch-key correction added four report scenarios (refresh at unchanged HEAD, deterministic fingerprinting, stale sibling rejection, concurrent same-key writes). Keeping them here would have made an 8-scenario, 2-file unit — over the plan's max-4-scenario granularity budget and over the 2-hour rule. U10 keeps the assembler concern only.

Learnings-Researcher P2: `docs/compound/2026-08-21-ship-executable-set-must-wire-into-actual-loop-variable.md` — the 'defined but not wired' class. A registry whose nodes are never actually evaluated would present as passing. The zero-evaluation COUNT assertion (scenario 1) and U11's real-outcome end-to-end run are the two guards against it.

RK5: this suite is what makes the D-3 cycle-detection duplication safe — it tests the behavior independently of `topology.py`.
<!-- END:implementation-notes -->