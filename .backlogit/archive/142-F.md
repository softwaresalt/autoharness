---
artifact_type: feature
created_at: 2026-08-21T20:21:27.02786Z
custom_fields:
    harness_status: pending
id: 142-F
labels:
    - bug
    - cli
    - install-correctness
    - templates
    - p-021
    - verify-workspace
priority: high
references:
    - docs/plans/2026-08-21-verify-workspace-variable-derivation-plan.md
    - docs/plans/2026-08-21-verify-workspace-variable-derivation-hardening.md
    - docs/reviews/2026-08-21-verify-workspace-variable-derivation-review.md
    - .backlogit/queue/023-DL.md
    - docs/decisions/2026-08-20-template-dogfood-render-parity-spike.md
status: done
title: verify-workspace template-variable derivation must conform to the install-harness resolution contract
updated_at: 2026-08-22T06:13:06.278262Z
---

Make `autoharness.verify_workspace._derive_template_variables` conform to the `.github/skills/install-harness/SKILL.md` resolution tables so that `autoharness verify-workspace --workspace .` stages a tree with ZERO unresolved `{{...}}` placeholders.

**Source stash:** 8FA8FC22 (DEFERRED SCOPE EXPANSION, P-021)
**Deliberation:** 023-DL
**Plan:** `docs/plans/2026-08-21-verify-workspace-variable-derivation-plan.md`
**Hardening:** `docs/plans/2026-08-21-verify-workspace-variable-derivation-hardening.md` — HARDENED (B1–B4; H9/B8 added in review-fix cycle 3)
**Review:** `docs/reviews/2026-08-21-verify-workspace-variable-derivation-review.md` (PASS re-gated at cycle 3, 0 unresolved P0/P1; B5–B7 applied cycle 1, B8 applied cycle 3)

**Baseline (Stage-measured, from `.autoharness/staging/verify-workspace-report.json`):** 83 unresolved occurrences / 62 distinct variables / 10 staged files. `.autoharness/config.yaml` 45; `_orchestrator.agent.md` 15; `escalation-protocol.instructions.md` 6; `copilot-instructions.md` 4; `constitution.instructions.md` 4; `graphtor-docs.instructions.md` 2; `AGENTS.md` 2; `start.ps1` 2; `start.sh` 2; `github-pr-automation.instructions.md` 1. `{{DEFAULT_BRANCH}}` alone is 12. This is materially larger than the stash entry's own 4-pair / 21-occurrence measurement.

**Decision (023-DL):** option (d) HYBRID — close the derivation gap against the SKILL.md table as single source of truth, with an explicit per-variable split into RESOLVED-FROM-SOURCE vs DERIVE-TO-EMPTY-STRING. Option (c) "renderer is not authoritative" was REJECTED on evidence: this renderer produces the `.autoharness/staging/` refresh tree, the same module raises unresolved placeholders as hard errors (verify_workspace.py:3494, :3689), and `tests/test_scope_containment_policy_contract.py` imports it to prove byte-identity of four clean template/dogfood pairs.

**Binding constraints:** C1 SKILL.md is the single source of truth. C2 config-first, detection-as-fallback. C3 preserve PROSE-ONLY vs RAW-STORAGE split for escalation/role routes. C4 round-trip through `templates/harness-config.yaml.tmpl`. C5 no change to `_render_template`. C6 manifest checksums refreshed atomically.

**Feature acceptance criteria**
- AC-F1. `autoharness verify-workspace --workspace .` reports 0 unresolved placeholders and NO new blockers/warnings versus the pre-change baseline (0 blockers, 0 warnings per the 145-S closure record).
- AC-F2. Zero-unresolved is NOT reportable unless the staged `.autoharness/config.yaml` parses as YAML and the staged `start.sh`/`start.ps1` array literals are structurally valid (hardening H4 elevation). Zero-unresolved alone is not evidence of a correct render.
- AC-F3. `tests/test_scope_containment_policy_contract.py` fully green including `test_clean_pairs_are_byte_identical_via_render_template`.
- AC-F4. A NEGATIVE test proves raw escalation slots stay empty and are NOT equal to a nested override's values (amendment B2).
- AC-F5. Polymorphic `config.model_routing` shape handled: scalar-shorthand and mapping forms both covered by tests (amendment B6).
- AC-F6. Per-variable provenance table recorded, restricted to config/profile field, SKILL.md documented default, or empty string. "Observed in the current dogfood copy" is a FORBIDDEN source (amendment B4).
- AC-F7. `.autoharness/harness-manifest.yaml` checksum scan clean.
- AC-F8 (amendment B8, review-fix cycle 3). Variable selection is ARTIFACT/ROLE-AWARE: a config declaring DIFFERENT `model_routing.stage.escalation` and `model_routing.ship.escalation` renders `_stage.agent.md` and `_ship.agent.md` with their OWN role's collapsed `{{ESCALATION_*}}` triple, and a test asserts the two triples are NOT EQUAL. `_render_template` remains byte-identical (C5) and the RAW `LEGACY_`/`STAGE_`/`SHIP_ESCALATION_*` families remain global and byte-identical across artifacts (C3). Owned by 142.007-T.

**Out of scope (P-021 C1):** the F3/F4(2) semantic prose drift (~1,200 lines of bidirectional normative drift) explicitly refused by the originating spike; any change to `_render_template`; any backfill of `.autoharness/workspace-profile.yaml`; writing the staged config back over the live workspace config (amendment B7).