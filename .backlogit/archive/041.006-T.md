---
archived_from: .backlogit/archive/041.006-T.md
artifact_type: task
commit: d37547430c6eefe5cff2576a70b3abb590024423
created_at: 2026-05-17T23:03:04.5579265-07:00
id: 041.006-T
labels:
    - release
    - tag
    - ci-cd
    - pypi
parent_id: 041-F
priority: medium
status: archived
title: Push v1.4.4 annotated tag and confirm release workflow success
updated_at: 2026-05-17T23:35:15.1729654-07:00
---

Push the annotated `v1.4.4` tag to trigger the release workflow. Gate: the PR from 041.005-T must be fully merged to `main` before this task begins.

## Pre-condition (P-001 gate)

**Do NOT push this tag until**:
- `041.005-T` is complete (PR merged, `main` updated)
- `git log --oneline -1` on `main` shows the merge commit from 041.005-T at HEAD

## Steps

### 1. Sync local main
```
git checkout main
git pull origin main
```

### 2. Confirm version at HEAD
```
python -c "import tomllib; f=open('pyproject.toml','rb'); print(tomllib.load(f)['project']['version'])"
# → 1.4.4
```

### 3. Push annotated tag
```
git tag -a v1.4.4 -m "Release v1.4.4"
git push origin v1.4.4
```

### 4. Monitor release workflow
Navigate to `https://github.com/softwaresalt/autoharness/actions` and confirm the release workflow (triggered by the `v1.4.4` tag) completes with all green steps:
- Validate tag format ✓
- Extract changelog section ✓
- Build wheel + sdist ✓
- Twine check ✓
- Pre-publish check ✓
- PyPI publish (OIDC) ✓
- Smoke test (`uv tool run autoharness version` returns `1.4.4`) ✓
- GitHub Release create/update ✓

### 5. Confirm GitHub Release published
```
gh release view v1.4.4
```
Expect: release exists, notes present, tag `v1.4.4`.

## No Trusted Publisher reconfiguration needed
The Trusted Publisher was configured during 033-S for `release.yml`. No changes to the workflow filename or environment mean it continues to work as-is.

## Width constraint
Touch only git tag operations. No code, doc, or backlog changes in this task.