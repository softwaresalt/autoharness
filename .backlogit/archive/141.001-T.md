---
archived_from: .backlogit/queue/141.001-T.md
archived_status: done
artifact_type: task
commit: ca9059bf9c651b61c9d0a458568ffc798ff4cf91
created_at: 2026-08-21T20:22:54.9093945Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 141.001-T
labels:
    - bug
    - tests
    - diagnosis
    - bisect
    - windows
parent_id: 141-F
priority: medium
references:
    - docs/plans/2026-08-21-full-suite-test-isolation-plan.md
    - .backlogit/queue/024-DL.md
    - docs/compound/097-S-canonical-unittest-gate.md
status: archived
title: Bounded empirical bisection to a minimal reproducing pair (diagnosis only; terminal verdict PAIR-ISOLATED or INCONCLUSIVE)
updated_at: 2026-08-22T03:55:26.6481072Z
---

DIAGNOSIS ONLY — this task produces EVIDENCE, not a fix, and changes NO source or test file. It is first because the causal predecessor is not determinable within Stage's role boundary (Stage may not run test suites).

**Plan:** `docs/plans/2026-08-21-full-suite-test-isolation-plan.md` Task 1 (+ amendments A2R, A5)
**Successor:** remediation lives in 143.002-T (shipment 151-S), NOT in this shipment.

## Terminal outcome contract (amendment A2R — NON-NEGOTIABLE)

This task has EXACTLY TWO terminal outcomes and BOTH close it `done`:

* **`VERDICT: PAIR-ISOLATED`** — a minimal reproducing pair was isolated (AC1–AC3 met).
* **`VERDICT: INCONCLUSIVE`** — the time box was exhausted; the NARROWED CANDIDATE SET is the deliverable (AC4 met).

Both are legitimate, independently valuable diagnostic deliverables. This task must **NEVER** be returned `blocked` and must never be left in any status other than `done`. Rationale: `.github/agents/_ship.agent.md:325-340` derives the executable task set from the manifest and treats **any** member status other than `queued`/`active`/`done`/pre-archived as a **FAIL-CLOSED HALT, never a skip**, while the manifest itself is "never mutated to make execution proceed". A `blocked` member would therefore deadlock the whole of 149-S instead of allowing it to close. The recorded `VERDICT` token — not a task status — is the machine-readable signal that 143.002-T's Step 0 precondition gate consumes.

## Protocol (FIXED — do not improvise)
1. Baseline on the CANONICAL gate: `$env:PYTHONPATH='src'; python -m unittest discover -s tests` (per `docs/compound/097-S-canonical-unittest-gate.md`). Capture the five failing IDs AND the FULL VERBATIM stderr for each — especially the `git init` exit-128 message text, which has never been recorded and is the single highest-value unrecorded datum in this finding.
2. Confirm the "exclusion" result. `python -m unittest` has NO deselect / `--ignore` facility, so this step MUST be run as a POSITIVE dotted-name enumeration of the COMPLEMENT SET (amendment A5), produced by a deterministic generator rather than hand-listed:

   ```powershell
   $env:PYTHONPATH = 'src'
   $mods = Get-ChildItem tests -Filter 'test_*.py' -Name |
       Where-Object { $_ -notlike 'test_scope_containment_*' } |
       ForEach-Object { 'tests.' + $_.Substring(0, $_.Length - 3) } |
       Sort-Object
   python -m unittest $mods
   ```

   At plan time this selects 82 of the 85 `tests/test_*.py` modules; the three `test_scope_containment_*` modules are exactly the complement. `Sort-Object` makes the ordering deterministic and reproducible across rounds. Record the generated module COUNT and the exact list alongside the result. Expect green. **If NOT green, the entry premise is falsified — record `VERDICT: INCONCLUSIVE` with the falsification evidence and STOP.** (Corrected in review-fix cycle 1: the original wording asked for an exclusion run that the canonical runner cannot express.)
3. Binary-search the polluting set. Each round runs only {candidate subset} + {the five victims}. Halve by module, then by TestCase class, then by test method. Bound: ~8 rounds.
4. Record the minimal reproducing pair (polluter test ID → victim test ID) and the observed MECHANISM, quoting the captured git stderr verbatim. Record `VERDICT: PAIR-ISOLATED`.
5. **TIME-BOX EXHAUSTION (amendment A2R).** If the minimal pair is not isolated within the time box, STOP the search — do NOT continue past the bound and do NOT proceed to a speculative fix (there is no fix work in this shipment to slip into). Record the NARROWED CANDIDATE SET, the rounds performed, and `VERDICT: INCONCLUSIVE`, then close this task `done`. The stop is enforced structurally rather than by task status: remediation lives in a separate successor shipment (151-S) whose own Step 0 gate reads this VERDICT, and 143.002-T forbids any source edit that does not cite a recorded minimal reproducing pair. No fourth attempt.

## Subset selection mechanism (amendment A5 — mandatory)
`unittest discover` has NO deselect facility. Every round names its subset POSITIVELY by dotted name:
`$env:PYTHONPATH='src'; python -m unittest tests.test_a tests.test_b tests.test_repo_root_artifacts.RepoRootTrackedJsonAllowlistTest.test_root_tracked_json_matches_allowlist ...`
pytest may be used as a CROSS-REFERENCE only and NEVER as the measurement gate — switching runners mid-bisect changes the thing being measured.

## Search-space narrowing already done by Stage (do not re-tread)
REFUTED: cwd rebinding — no `os.chdir`/`monkeypatch.chdir`/`chdir(` anywhere in `tests/`.
REFUTED: env leakage — no bare `os.environ[...]` assignment anywhere in `tests/`; every `patch.dict(os.environ, ...)` is context-managed; no unstopped `mock.patch(...).start()`; `GIT_DIR`/`GIT_CONFIG*`/`HOME`/`TMPDIR` never touched.
REFUTED: working-tree writes by the suspected polluters — the three `test_scope_containment_*` modules write nothing into the live tree; their tempfile use is system-temp and context-managed.
LEADING SUSPECT: 58 `tempfile.TemporaryDirectory(dir=Path.cwd())` sites creating temp trees INSIDE the live working tree.

## Acceptance criteria
- AC1. (PAIR-ISOLATED only) A minimal reproducing pair is recorded as an exact, RE-RUNNABLE command that fails, plus the same command minus the polluter that passes.
- AC2. The `git init` exit-128 stderr text is recorded VERBATIM. Binding in BOTH verdicts — it is captured in protocol step 1, before any bisection.
- AC3. (PAIR-ISOLATED only) The mechanism is stated as a CAUSAL claim with evidence — not a correlation ("excluding module X makes it pass" is not sufficient).
- AC4. (INCONCLUSIVE only) The NARROWED CANDIDATE SET is recorded, together with the rounds performed and the subsets enumerated, in a form 143.002-T can consume. The task is closed `done` — **not** returned blocked.
- AC5. Exactly one `VERDICT:` token (`PAIR-ISOLATED` or `INCONCLUSIVE`) is recorded on this task record. An absent, ambiguous, or multiple verdict is not acceptable — 143.002-T's Step 0 gate reads this token and cannot proceed without it.

## Scope guard (024-DL R5)
If the bisect implicates production code under `src/autoharness/` (a module-level cache or global), that is a DIFFERENT contract surface — STOP and re-enter P-021 capture. Stage's static scan found no such cache in `autoharness/backlog_root.py` (120 lines, no caching) or `autoharness/gates/topology.py` (module-level constants are immutable frozensets/compiled regexes), but the scan was not exhaustive.