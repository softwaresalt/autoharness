---
archived_from: .backlogit/queue/165.008-T.md
archived_status: done
artifact_type: task
commit: 9cc98c41de1cad9175e29b8190dbe4b81c85a1d5
created_at: 2026-09-12T22:51:26.4326628Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 165.005-T
    - 165.006-T
id: 165.008-T
labels:
    - templates
    - documentation
    - agent-instructions
    - dogfood
    - installed-mirror
    - parity
parent_id: 165-F
priority: medium
status: archived
title: 'Agent sequencing contract: template sources AND installed dogfood mirrors in one atomic commit'
updated_at: 2026-09-16T07:19:45.1790509Z
---

Update the agent sequencing contract in BOTH the TEMPLATE SOURCES and the INSTALLED DOGFOOD MIRRORS as ONE ATOMIC UNIT — one task, one commit.

ATOMICITY IS MANDATORY (review-fix cycle 2, finding 8; reaffirmed cycle 3, finding 10). Sources and mirrors MUST be updated in the SAME commit; a commit that touches one without the other fails this task. A dependency-only ordering does NOT prevent drift, it merely schedules it: any window in which template sources state the four-state contract while the installed `.github/agents/` mirrors still state retired numeric-adjacency sequencing is a live wrong-contract window for every agent session that starts inside it, and the installed mirrors are what the dogfooded agents in THIS repository actually read. That window is not acceptable at any duration.

HISTORICAL NOTE (no longer an instruction): an earlier cycle-1 decomposition split this work into a sources task and a separate mirrors task joined by a dependency edge. That split is REVERSED and the separate mirrors task is ARCHIVED into this one. There is no separate mirror task, no mirror follow-up, and no second commit — do not re-derive one from this note.

EDIT SURFACES — TEMPLATE SOURCES (verified by grep):
- `templates/agents/_orchestrator.agent.md.tmpl` — L312, L314, L339, L341
- `templates/agents/_ship.agent.md.tmpl` — L172-175, L200-201, L224-225, L235

EDIT SURFACES — INSTALLED DOGFOOD MIRRORS (same commit):
- `.github/agents/_orchestrator.agent.md`
- `.github/agents/_ship.agent.md`

REMOVED FROM SCOPE (corrected mapping): `.github/instructions/workflows.instructions.md` contains NO `pre_claim`, NO `PREDECESSOR_`, and NO `dag-readiness` token — verified by grep. The first-round plan listed it in error. DO NOT edit it, and do NOT manufacture a change there. Likewise `templates/policies/workflow-policies.md.tmpl` contains no `pre_claim`/`PREDECESSOR_` token and is NOT an edit surface. No other installed instruction file is in scope.

CONTENT REQUIREMENTS (identical in sources and mirrors):
1. Predecessors derive from EXPLICIT `blocks` DAG edges only; numeric adjacency is retired from the claim path.
2. The four derivation states and their provenance values (`explicit`, `declared_root`, `genesis`, `unsequenced`) are documented as agent-observable output, along with the UNSEQUENCED_SHIPMENT token and its two remedies.
3. GENESIS IS THE SOLE-RECORD RULE (PR review-fix cycle 2, thread PRRT_kwDORzpWpM6h3frF; REPLACES the cycle-2 "no shipped-terminal and no abandoned history" wording): state that `genesis` applies ONLY to a workspace whose candidate shipment is the ONLY shipment record in existence — live and archived counted together, REGARDLESS OF STATUS. ANY other shipment record disqualifies genesis, including a record whose status the gate cannot classify (which disqualifies fail-closed). An agent must NOT expect a second, third, or later-numbered edge-less shipment to pass as `genesis` — in a workspace holding more than one shipment record, every intended root must carry `dag-root` explicitly or it blocks as `unsequenced`. The agent text MUST NOT restate the retired G2/G3/G4 status-enumerating form, which admitted every unenumerated value and was a verified fail-open. THE AGENT TEXT MUST ALSO NOT CALL `blocked` A SHIPMENT STATUS (PR review-fix cycle 3, thread PRRT_kwDORzpWpM6h3ttS): the current shipment status vocabulary is EXACTLY {`queued`, `active`, `shipped`, `abandoned`}, and `blocked` on a shipment is malformed legacy data that makes a LIVE record fail closed at read time rather than deriving any state. Writing `blocked` into agent contract text as a valid status would teach every installed agent a status the runtime rejects.
4. `pre_claim` is the SOLE claim authority; `dag-readiness` is advisory and NON-AUTHORIZING and must never be treated as permission.
5. SHIP MUST NOT SELF-DECLARE A ROOT. Applying the `dag-root` label is an operator/Stage act on backlog data.
   NARROWED CLAIM (review-fix cycle 3, finding 6): state this accurately and no more strongly than it is true. `dag-root` is a VERSION-CONTROLLED, REVIEW-GATED DECLARATION recorded in backlog data inside the repository trust boundary. Its force comes from being visible in a diff, attributable to a commit, gated by review, and auditable after the fact — NOT from a mechanical permission check. Nothing in the gate prevents a sufficiently privileged writer from adding the label. The contract text MUST NOT claim that self-declaration is technically impossible, mechanically enforced, permission-checked, or that there is "no escape hatch"; it is an agent-contract rule enforced by review and audit, and `dag-root` is NOT a security boundary. Overstating this would teach every reader a guarantee the implementation does not provide.
6. The `audit_sequencing` phase is named as the migration path for workspaces with previously-implicit sequencing. State that the audit run is a READ-ONLY REPORT (165.003-T) which performs NO BACKLOG MUTATION and NO MIGRATION-STATE OR LEDGER WRITE — and, per PR review-fix cycle 1 (thread PRRT_kwDORzpWpM6h3Tgi), do NOT state it as "writes nothing" or "persists nothing", because the ordinary `pipeline-topology` telemetry emission is unconditional, observational, and fail-open on every phase — that the durable record of a migration is the VERSION-CONTROLLED OPERATOR COMMIT that adds the edges or root labels, and that a code-only rollback does NOT undo those already-committed edges or root labels.
7. No unresolved template placeholder tokens introduced; markdownlint heading hierarchy clean (P-008).

RECORDED PARITY CHECK: for EACH of the two file pairs, confirm in the task record that the installed copy and its template source state the SAME contract, and record the confirmation. Drift found must be FIXED, not noted. Because both halves land in one commit, the parity check is performed against the final working tree before commit.

MANDATORY MEMBER OF 173-S. This task may not be deferred, split back out, or partially delivered.

SIZE: M (~2h). COMPLEXITY: medium (one contract text authored once, then mirrored within the same working context).
Plan §T8. Depends on 165.005-T and 165.006-T. Blocks 165.012-T (PR review-fix cycle 1: 165.012-T edits the SAME four agent-contract files — the two template sources and the two installed mirrors — so it is serialized behind this task to prevent conflicting edits to the same contract text within one shipment).