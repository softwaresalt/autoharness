---
artifact_type: task
created_at: 2026-08-06T01:09:20.4038743Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 1.0.0
    size_source: agent
dependencies:
    - 111.004-T
id: 111.005-T
parent_id: 111-F
priority: medium
status: done
title: 'D: install/tune wiring + first-party manifest registration so the backlogit-pack overlay instruction is actually installed (standard artifacts[].checksum)'
updated_at: 2026-08-07T06:11:04.9433431Z
---



<!-- BEGIN:acceptance-criteria -->
1. install-harness SKILL wiring: when the backlogit capability pack is active, install the overlay instruction template `templates/instructions/backlogit.instructions.md.tmpl` to `.github/instructions/backlogit.instructions.md`, resolving all {{VARIABLE}} customization points at install time. RATIONALE (re-review 2026-08-05, 111.001-R P1-3): this overlay is currently NOT installed in the dogfood/self-install workspace — `.autoharness/harness-manifest.yaml` records NO such artifact and `.github/instructions/` contains no `backlogit.instructions.md` — so the host of the checkpoint-recovery section (111.004-T) would otherwise never land. 2. MANIFEST REGISTRATION: record the installed overlay as a STANDARD FIRST-PARTY manifest artifact under `artifacts:` with a SINGLE `checksum` field, mirroring the existing `agent-engram`/`agent-intercom`/`graphtor-docs` overlay artifact entries — NOT the community-template `installed_checksum`/`source_checksum` pair (that dual-checksum surface is not part of the first-party overlay contract and is not supported here). Add the backlogit pack's `capability_pack_overlays[].verification_checks` entry for the installed overlay (`backlogit.instructions.md installed at .github/instructions/`) consistent with its existing `overlay_targets: instructions`. 3. tune-harness SKILL wiring: on re-tune the backlogit overlay is (re)installed/refreshed and its manifest `checksum` recorded, consistent with the other capability-pack overlays. 4. Idempotent: re-install/re-tune does NOT duplicate the artifact entry. 5. Ensure the overlay is installed in ANY target workspace whenever the backlogit pack is enabled (not only this dogfood workspace). 6. NO SCHEMA CHANGE / NO NEW VERIFIER PRIMITIVE: reuse the EXISTING first-party `artifacts[].checksum` manifest surface only; verification (section-presence + artifact-checksum coherence) is owned by 111.003-T.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Add install-harness + tune-harness SKILL wiring so the backlogit capability-pack overlay instruction (the host of the checkpoint-recovery / prune-on-restore section authored in 111.004-T) is ACTUALLY installed to `.github/instructions/backlogit.instructions.md` and recorded as a standard first-party manifest artifact whenever the backlogit pack is active. Corrects the never-installed-host gap surfaced in re-review 111.001-R P1-3, using the standard first-party single-`checksum` contract rather than the community-template installed_checksum/source_checksum pair.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on 111.004-T (overlay section authored first). Source: re-review 2026-08-05 (second independent BLOCKED-review repair), 111.001-R P1-3. WIDTH ISOLATION: install/tune skill family (install-harness SKILL + tune-harness SKILL + harness-manifest artifact recording) — distinct from the instruction-template authoring in 111.004-T and the verify+docs in 111.003-T, so it is a separate bounded task rather than being folded in. Prior art: the agent-engram / agent-intercom / graphtor-docs overlay artifact entries in `.autoharness/harness-manifest.yaml` each use a single `checksum` field; replicate that pattern for the backlogit overlay.
<!-- END:implementation-notes -->