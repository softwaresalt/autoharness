---
archived_from: .backlogit/queue/090.004-T.md
archived_status: done
artifact_type: task
created_at: 2026-07-27T03:01:45.6441481Z
id: 090.004-T
parent_id: 090-F
priority: medium
status: archived
title: Surface WorkSizingSnapshot unsized-count data-quality gap
updated_at: 2026-07-27T21:47:03.8887231Z
---

## Description

<!-- BEGIN:description -->
Fix WorkSizingSnapshot unsized-count clamping to report data-quality unavailability instead of silently zeroing (source: stash 2D22ED3D).

Files (2): src/autoharness/telemetry/sizing.py, tests/test_telemetry_backlogit_sizing.py

Current state: _composition (sizing.py:104-159) computes unsized = max(len(unique_ids) - sized_count, 0) at line 147, silently clamping to 0 whenever the histogram overcounts relative to unique_ids (e.g. malformed backlogit data producing more sized entries than known unique IDs). This masks a data-quality problem that the function already has a precedent for surfacing: malformed values elsewhere in the same function return the "unavailable" tuple (lines 139-145) instead of a coerced number.

TDD (red->green): add test_composition_flags_inconsistent_sized_count_as_unavailable to test_telemetry_backlogit_sizing.py, constructing a histogram where sized_count exceeds len(unique_ids), asserting _composition degrades the ENTIRE composition to the existing unavailable tuple (None, {}, None, preserved ruleset_version, preserved skipped) -- there is no per-field/per-bucket "unavailable" marker, and WorkSizingSnapshot forbids an "unavailable" histogram bucket (epoch.py:526-531,568-573) (fails first, since current code clamps to 0). Then change line 147 to detect sized_count > len(unique_ids) and return that same 5-tuple, exactly like the existing malformed-value pattern (sizing.py:139-145), instead of clamping via max(...,0).
<!-- END:description -->

## Acceptance Criteria

<!-- BEGIN:acceptance-criteria -->
- Overfull sized histograms (sized_count > len(unique_ids)) degrade the whole composition to the existing unavailable tuple (None, {}, None, preserved ruleset_version, preserved skipped), matching sizing.py:139-145, instead of silently clamping unsized to 0. There is no per-field unavailable and no "unavailable" histogram bucket (epoch.py:526-531,568-573).
- Normal (non-overfull) histograms are unaffected and still compute unsized counts numerically.
<!-- END:acceptance-criteria -->

## Implementation Notes

<!-- BEGIN:implementation-notes -->
Verification: python -m pytest tests/test_telemetry_backlogit_sizing.py -v
<!-- END:implementation-notes -->
