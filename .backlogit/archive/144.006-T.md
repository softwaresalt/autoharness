---
archived_from: .backlogit/queue/144.006-T.md
archived_status: done
artifact_type: task
commit: f0cad43c04ad98809685db0fb247db1e9a287bb6
created_at: 2026-08-22T08:18:17.9721524Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 144.005-T
id: 144.006-T
labels:
    - assertion-integrity
    - diagnostics
    - exit-status-masking
    - mechanism-a
parent_id: 144-F
priority: medium
references:
    - docs/plans/2026-08-22-git-config-env-containment-plan.md
    - docs/plans/2026-08-22-git-config-env-containment-hardening.md
    - docs/compound/2026-08-08-shell-pipeline-exit-status-masking-in-version-probes.md
    - src/autoharness/gates/topology.py
    - tests/test_telemetry_gitignore_template.py
status: archived
title: 'Assertion integrity: stop laundering git infrastructure failures into false domain diagnoses (_run_git + check-ignore)'
updated_at: 2026-08-22T23:10:56.2464408Z
---

**LAYER 5 — assertion integrity. NO assertion is weakened; only failure-path DIAGNOSIS changes.**

Plan: `docs/plans/2026-08-22-git-config-env-containment-plan.md` Task 6. Amendments A3, A6 BINDING.

This is the ONLY task in `144-F` that touches `src/autoharness/`, and only the `_run_git` diagnostic seam. Gate verdict semantics, tokens, and exit codes are OUT OF SCOPE and MUST NOT change.

## Part 1 — `src/autoharness/gates/topology.py:200` `_run_git`

Currently `check=False` and `return ""` on nonzero exit: a git INFRASTRUCTURE failure is laundered into "no branch" -> `detached_head` -> `BRANCH_MISMATCH` -> `exit_code == 1`. Victims #1 and #2 assert `exit_code == 0` and therefore fail with a FALSE DOMAIN DIAGNOSIS. Same anti-pattern as `docs/compound/2026-08-08-shell-pipeline-exit-status-masking-in-version-probes.md` (a failing probe's exit status discarded while partial stdout is still consumed).

**A3 (BINDING) — strictly additive and schema-safe:**

**A3R (BINDING, cycle 3) — expected absence is NOT invocation failure.**

The original A3 bound the diagnostic to fire on ANY nonzero exit. Verified against the ACTUAL call sites in `src/autoharness/gates/topology.py`:

```
L571  _run_git(["git","--no-pager","branch","--show-current"], ...)
L574  _run_git(["git","--no-pager","symbolic-ref","--quiet","--short",
               "refs/remotes/origin/HEAD"], ...)
L583  _run_git(["git","--no-pager","worktree","list","--porcelain"], ...)
```

`git symbolic-ref --quiet` exits **1** when the ref does not exist, and `--quiet` means exactly "do not print an error, just exit nonzero". That is the DESIGNED way to ask whether `refs/remotes/origin/HEAD` exists. In any clone where `origin/HEAD` is unset, exit 1 is the NORMAL, EXPECTED answer and `default_branch()` correctly falls through to its next rung.

Under A3 as written that ordinary path would populate `git_invocation_error` on EVERY run — making A3's own binding claim ("absent whenever every git invocation succeeded") FALSE in routine operation and turning the diagnostic from signal into noise at the exact site it was added to clarify. And because `--quiet` suppresses stderr, the key would be populated with the EMPTY STRING: a present-but-meaningless diagnostic.

`_run_git` therefore gains a caller-declared expected-absence set:

```python
def _run_git(argv: list[str], cwd: Path,
             expected_absence_codes: frozenset[int] = frozenset()) -> str:
```

- Add ONE new key, `details['git_invocation_error']`, carrying the captured stderr.
- It is populated **ONLY** when the exit code is nonzero **AND NOT** in `expected_absence_codes`.
- It is **ABSENT** (key not present at all) whenever every git invocation either succeeded or returned a declared expected-absence code — so the success-path `details` payload is BYTE-IDENTICAL to today.
- It is **NEVER** populated with an empty string. If an unexpected nonzero exit produced no stderr, record the exit code instead, so a present key is always meaningful.
- Exhaustive call-site declarations:
  - `branch --show-current` -> `frozenset()` (exit 0 even on detached HEAD, which reports empty stdout; any nonzero is a real failure)
  - `symbolic-ref --quiet --short refs/remotes/origin/HEAD` -> `frozenset({1})`
  - `worktree list --porcelain` -> `frozenset()`
- Additional required tests: expected-absence exit 1 at the `symbolic-ref` site produces **NO** `git_invocation_error` key and the identical verdict as today; an unexpected exit (e.g. 128) at the same site **DOES** populate it; a no-stderr unexpected exit records the exit code rather than `""`.
- It never replaces, renames, or reorders an existing key.
- `_run_git`'s return contract is UNCHANGED: still `""` on nonzero exit, so the gate's token and exit code are IDENTICAL on every path.
- Add a **verdict-equality test**: for a simulated nonzero git exit, `result.exit_code`, `check.token`, and EVERY pre-existing `details` key equal the pre-change behavior, AND the new key carries the captured stderr.
- **Re-run the consumer grep at THIS task's head** and record the result: `scripts/ci-topology-check.sh` and `tests/test_ci_topology_check_entrypoint.py` for whole-`details` equality or strict-key consumption. If ANY consumer compares exactly, route the diagnostic to stderr logging only and DROP the `details` key.

Pre-verified at plan-review time (review finding N1, informational only, NOT a substitute for the grep above): `scripts/ci-topology-check.sh` has zero `details` references; `tests/test_ci_topology_check_entrypoint.py` has zero; every `details` assertion in `tests/test_gates_topology.py` is an individual key lookup (`['current_branch']`, `['default_branch']`, `['detached_head']`, `['resolved_via_ci_env_fallback']`, `['default_branch_resolved_via_ci_env_fallback']`), never whole-dict equality; there is no JSON schema for topology output under `schemas/`.

## Part 2 — `tests/test_telemetry_gitignore_template.py:33`

`self.assertEqual(result.returncode, 0, f"{rel} is not gitignored")` reports a gitignore defect when git actually died at config-parse time. `git check-ignore` returns `0` = ignored, `1` = not ignored, `>=128` = error.

- Keep the `returncode == 0` assertion **EXACTLY AS STRONG** — do not relax, branch, or make it conditional.
- Branch only the MESSAGE on `returncode >= 2`, reporting the captured stderr as a git INVOCATION failure rather than a gitignore defect. Requires adding `capture_output`-derived stderr to the message (already captured at that seam).
- Add a test proving the new message appears for a simulated `returncode=128` and the ORIGINAL message for `returncode=1`.

**A6/AIG-2 (BINDING):** this message argument is the SINGLE authorized assertion-argument divergence in the whole feature. It must be cited line-by-line in the task record, and the assertion's non-message arguments (`result.returncode`, `0`) must still compare EQUAL under AIG-1.

## Acceptance

- No assertion removed, relaxed, or made conditional anywhere.
- Verdict-equality test green.
- Consumer grep re-run and recorded.
- Canonical gate (R3 PowerShell invocation) still green; `pytest` MUST NOT be substituted.

---

## Execution Record

### Part 1 — `_run_git` diagnostic seam (A3R)

`src/autoharness/gates/topology.py`:

- `_run_git(argv, cwd, expected_absence_codes=frozenset(), error_sink=None, error_key="")`.
  Return contract UNCHANGED: still `""` on any nonzero exit. When
  `error_sink`/`error_key` are supplied AND the exit is nonzero AND NOT in
  `expected_absence_codes`, `error_sink[error_key]` is set to
  `completed.stderr.strip()`, or, if that is empty, to
  `f"git exited with status {completed.returncode}"` (never an empty
  string). A caller that never supplies `error_sink`/`error_key` (every
  call site anywhere else in the codebase, and every pre-144.006-T test)
  observes byte-identical behavior — proven by
  `RunGitDiagnosticTests.test_no_sink_supplied_is_a_silent_noop`.
- `FilesystemTopologyReaders.__init__` gained `self._git_invocation_errors: dict[str, str] = {}`,
  cleared at the top of each of the three wrapper methods before the
  `_run_git` call (so a later successful call always clears a stale error
  from an earlier failed call at the same site).
- Exhaustive call-site wiring exactly as specified:
  - `current_branch()` -> `branch --show-current`, `expected_absence_codes=frozenset()`, key `"current_branch"`.
  - `default_branch()` -> `symbolic-ref --quiet --short refs/remotes/origin/HEAD`, `expected_absence_codes=frozenset({1})`, key `"default_branch"`.
  - `worktree_porcelain()` -> `worktree list --porcelain`, `expected_absence_codes=frozenset()`, key `"worktree_porcelain"`.
- New public accessor `FilesystemTopologyReaders.git_invocation_error(name)` returns the
  captured error or `None`.
- New module-level helper `_collect_git_invocation_error(readers, *names)` looks the
  accessor up via `getattr(readers, "git_invocation_error", None)` **defensively** — a
  reader that does not define the method (every `_FakeReaders`-derived test fixture that
  predates this task, `_NullReaders`, and any third-party `TopologyReaders`
  implementation) is completely unaffected; it simply never gains the new key.
  Proven by `BranchOwnershipGitInvocationErrorTests.test_reader_without_git_invocation_error_method_is_unaffected`
  (exercised directly against `_NullReaders`).
- `_branch_ownership_check` and `_worktree_uniqueness_check` each compute
  `git_invocation_error` once (via the helper above) and add it to their
  `details` dict **only when truthy**, at every one of their existing return
  points (5 in `_branch_ownership_check`, 3 in `_worktree_uniqueness_check`) —
  never replacing, renaming, or reordering an existing key; the new key is
  always appended last.
- **Consumer grep re-run at this task's head** (required, not just the
  plan-review-time pre-verification): `Select-String -Path
  scripts\ci-topology-check.sh -Pattern "details"` and `Select-String -Path
  tests\test_ci_topology_check_entrypoint.py -Pattern "details"` both
  returned **zero matches**. No whole-`details`-dict equality or
  strict-key consumption exists anywhere in either file. The additive key
  is therefore safe as specified; no stderr-only/dropped-details fallback
  is needed.
- Required tests, all added to `tests/test_gates_topology.py`:
  - `RunGitDiagnosticTests` (5 tests, direct `_run_git` tests via a mocked
    `subprocess.run` — "simulated nonzero git exit" per the plan's own
    language, since a real cross-platform-reliable git infrastructure
    failure is not reproducibly constructible):
    expected-absence exit 1 -> no error-sink entry, empty return;
    unexpected exit 128 with stderr -> error-sink entry equals the captured
    stderr; unexpected exit 128 with NO stderr -> error-sink entry names the
    exit code, never `""`; a successful exit never populates the sink; a
    caller passing no sink/key at all sees the identical pre-change `""`
    return with no error.
  - `BranchOwnershipGitInvocationErrorTests` (4 tests): a no-error reader's
    `details` are byte-identical to a reader that explicitly reports no
    error (empty `git_errors={}`); a simulated invocation error surfaces
    additively on the `BRANCH_MISMATCH` path (verdict-equality: identical
    `exit_code`/`token`/`status`/every pre-existing `details` key as the
    baseline reader, PLUS the new key carrying the captured stderr) and
    independently on the `BRANCH_OK` path; a reader lacking the accessor
    entirely (`_NullReaders`) raises no `AttributeError`.
  - `WorktreeGitInvocationErrorTests` (1 test): same additive/verdict-equality
    proof for the `worktree_porcelain` call site via `WORKTREE_TOPOLOGY_OK`.
- `_FakeReaders` (test fixture) gained an optional `git_errors` constructor
  parameter (default `{}`, so every existing call site is unaffected) and a
  `git_invocation_error(name)` method, purely additive.
- AST assertion-inventory check (AIG-1, source-order-sorted this time —
  `ast.walk`'s traversal order is NOT source order for a whole module, so the
  mechanical check was corrected to sort by `(lineno, col_offset)` before
  comparing) over `tests/test_gates_topology.py`: **0 mismatches** among the
  182 pre-existing assert calls; 32 new assert calls appended by the 10 new
  tests. Zero pre-existing assertions edited.
- Isolated module run: 104/104 pass (94 pre-existing + 10 new).

### Part 2 — `test_telemetry_gitignore_template.py` check-ignore message (A6/AIG-2)

- New module-level helper `_check_ignore_failure_message(rel, returncode, stderr)`:
  branches ONLY the message text on `returncode >= 2` (a genuine git
  invocation failure per `git check-ignore`'s documented exit-status
  contract: 0=ignored, 1=not-ignored, >=2=fatal error) vs. the ORIGINAL
  `"{rel} is not gitignored"` message for `returncode == 1` (and, by the
  same non-branching default, for the passing `returncode == 0` case, where
  the message is never even inspected by `assertEqual`).
- `test_git_check_ignore_matches_metrics_artifacts`'s single assertion is now:
  `self.assertEqual(result.returncode, 0, _check_ignore_failure_message(rel, result.returncode, result.stderr))`
  — the non-message arguments (`result.returncode`, `0`) are byte-identical
  to the pre-change call; ONLY the message argument (previously the inline
  f-string `f"{rel} is not gitignored"`) now delegates to the helper. This is
  the SINGLE authorized assertion-argument divergence (AIG-2), cited
  line-by-line here per H6/A6.
- Required test added (`CheckIgnoreDiagnosisMessageTests`, 2 tests):
  `returncode=128` with a simulated stderr produces the new invocation-failure
  message (contains "invocation failed", "128", and the verbatim stderr;
  does NOT contain "is not gitignored"); `returncode=1` reproduces the
  EXACT original message string.
- AST assertion-inventory check (AIG-1/AIG-2), source-order-sorted, over
  `tests/test_telemetry_gitignore_template.py`: exactly **1** mismatch among
  the 13 pre-existing assert calls — the cited line above, non-message
  arguments (`result.returncode`, `0`) unchanged, only the message argument
  differs — and 5 new assert calls appended by the 2 new tests. This is
  EXACTLY the single authorized divergence AIG-2 permits; no other
  pre-existing assertion in this module was touched.
- Isolated module run: 8/8 pass (6 pre-existing + 2 new).

### Canonical suite

Full canonical suite (`python -m unittest discover -s tests`, `PYTHONPATH=src;tests`):
**1823 tests, failures=0, errors=0, skipped=20** (1811 at Task 5's close + 12
new: 10 in `test_gates_topology.py` + 2 in `test_telemetry_gitignore_template.py`).

### Acceptance: MET

- No assertion removed, relaxed, or made conditional anywhere — confirmed
  mechanically via AIG-1 (source-order-corrected) for both touched modules;
  the ONE authorized divergence (AIG-2) is cited above line-by-line with its
  non-message arguments proven unchanged.
- Verdict-equality tests green (`BranchOwnershipGitInvocationErrorTests`,
  `WorktreeGitInvocationErrorTests`).
- Consumer grep re-run at this task's head and recorded (zero matches in
  both files; additive key confirmed safe).
- Canonical gate green: 1823 tests, failures=0, errors=0, skipped=20 (`python
  -m unittest discover`, not `pytest`).
- Gate token/exit-code/verdict semantics in `src/autoharness/gates/topology.py`
  are completely unchanged — only the diagnostic seam and one test-file
  message were touched, per this task's own explicit scope boundary (the
  ONLY task in `144-F` touching `src/autoharness/`).
