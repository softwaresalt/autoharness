---
artifact_type: task
created_at: 2026-05-17T15:03:37.0781799-07:00
id: 036.001-T
labels:
    - agent-engram
    - workspace-discovery
    - skill-authoring
parent_id: 036-F
priority: medium
status: done
title: 'WS-A: Fix workspace-discovery — add root .mcp.json to Engram/Intercom detection scan paths'
updated_at: 2026-05-17T15:17:56.1659382-07:00
---

## What

Update `workspace-discovery/SKILL.md` Steps 1.5c (agent-intercom) and 1.5d (agent-engram) to scan `.mcp.json` at the workspace root in addition to `.vscode/mcp.json` and `.vscode/settings.json`.

Also add `.cursor/mcp.json` to align with the environment-agnostic principle (AGENTS.md Rule 3).

Also update the `instruction_markers` pattern list to recognise `<!-- engram:start -->` and `<!-- engram:end -->` markers in `AGENTS.md` and `copilot-instructions.md`.

## File

`.github/skills/workspace-discovery/SKILL.md`

## Acceptance Criteria

- Step 1.5c detection signal table includes `.mcp.json` and `.cursor/mcp.json`
- Step 1.5d detection signal table includes `.mcp.json` and `.cursor/mcp.json`
- instruction_markers matching patterns include `engram:start` / `engram:end` delimiter markers
- Existing detection logic for `.vscode/mcp.json` is preserved (additive change only)
- Re-running workspace-discovery against this dogfood workspace would now produce `agent_engram.detected: true`

## Constraint

Single file change. ~30–45 min. Width-isolated to workspace-discovery SKILL.md only.