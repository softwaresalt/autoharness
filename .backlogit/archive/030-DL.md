---
artifact_type: deliberation
created_at: 2026-08-25T21:05:02.4001347Z
custom_fields:
    linked_stash_id: 08D71FD5
    linked_stash_kind: feature
    linked_stash_priority: medium
    linked_stash_text: |-
        OPERATOR INTAKE (2026-08-25, verbatim): "I also suggest that you either design into the orchestrator or craft anew a proper brainstorming/research agent or skill for the purpose of interactive ideation and feature development."

        CLASSIFICATION: CAPTURE-ONLY INTAKE by the Orchestrator. NOT triaged, NOT prioritized beyond a provisional medium, NOT planned, NOT harvested. Stage owns triage/deliberation/planning for this entry.

        MOTIVATING OBSERVATION (this session): the operator directed that "the interaction and pushbacks are critically important" during a brainstorming exercise. The Orchestrator then improvised a dialectical loop -- pose independent adversarial positions, require the Stage subagent to defend or adopt each, iterate. That loop produced the session's sharpest result, but it has NO template, NO protocol, and NO primitive backing it. It worked by improvisation, which is precisely the nondeterminism the workspace is otherwise trying to remove.

        GAP ANALYSIS (evidence-grounded against existing primitives, verified by reading each template):
        - brainstorm/SKILL.md.tmpl -- requirements INTAKE funnel. Elicits stable R# IDs, success criteria, scope boundaries, handoff to impl-plan. Pressure-tests framing at Step 1.3 but has NO structured adversarial loop and NO independent challenger. Assumes the operator broadly knows WHAT they want and shapes it.
        - deliberate/SKILL.md.tmpl -- option SELECTION and rationale capture. Identifies 2-4 options, compares trade-offs, iterates with the operator at Phase 3.3. But options are generated ONCE by a SINGLE reasoner; there is no divergent expansion beyond 2-4 and no independent challenge of the option set itself.
        - spike/SKILL.md.tmpl -- time-boxed EMPIRICAL investigation of a narrow question.
        - iterative-experiment/SKILL.md.tmpl -- autonomous optimization loop requiring a NUMERIC metric command, baseline, and modify/measure/keep/revert. Not applicable to conceptual ideation.
        - adversarial-review.agent.md.tmpl -- multi-model parallel review with consensus/majority/plurality/unique confidence tiers and capped re-review cycles. Closest existing adversarial machinery, but it reviews ARTIFACTS/DIFFS against a RULESET to find DEFECTS. It answers "what is wrong with this code" -- not "what should we build, and which framing of the question is wrong."
        - research/learnings-researcher.agent.md.tmpl -- Tier 1 read-only keyword retrieval over docs/compound. Retrieval only; no synthesis, no ideation, no challenge.

        NAMED MISSING CAPABILITIES: (1) DIVERGENT generation of an expansive option space, versus deliberate's single-reasoner 2-4 options; (2) INDEPENDENT adversarial challenge of IDEAS rather than of code; (3) MULTI-ROUND dialectic with position -> challenge -> defend/revise -> re-challenge and explicit convergence tracking; (4) FRAMING CRITIQUE -- challenging whether the question itself is correctly posed, which was the single highest-value move in this session; (5) a guard against OVER-CONSERVATISM as a failure mode. Note that adversarial-review is tuned to find defects, which structurally biases toward "no" -- stash 34AAF1C7 has now been triaged to a deferral five consecutive times, which is direct evidence that the existing primitives have no counterweight to unproductive conservatism.

        KEY DESIGN INSIGHT (transferable precedent already ratified in this workspace): .github/instructions/escalation-protocol.instructions.md defines ESCALATION_DEGRADED to include a "same-route no-op" -- escalating to an identical model tuple is not a real escalation. The same logic applies directly to ideation: an agent challenging its OWN ideas with ITSELF is not a real challenge. This is a principled, already-accepted argument that genuine adversarial ideation requires MODEL DIVERSITY, which in turn implies a subagent-dispatching AGENT rather than a leaf-executor SKILL -- mirroring why adversarial-review is an agent.

        OPEN DESIGN QUESTIONS FOR STAGE (framing only, NOT a decomposition and NOT an authorization to harvest): (a) agent versus skill, given that harness-architecture Primitive 4 declares skills leaf executors while the documented depth model permits skills at depth 2 to spawn personas at depth 3, and adversarial-review already sets subagent_depth 2; (b) whether to extend brainstorm/deliberate in place versus craft a new primitive, weighed against duplicating consensus machinery that adversarial-review already owns; (c) where the primitive sits in the Stage pipeline relative to brainstorm -> deliberate -> impl-plan, and whether it precedes or wraps them; (d) whether the Orchestrator needs a corresponding trigger phrase and routing rule, since the operator explicitly raised designing it into the Orchestrator; (e) how convergence/termination of the dialectic is bounded, which must respect the circuit-breaker instruction and should NOT naively assume more rounds are better; (f) whether the operator stays in the loop each round (interactive) or the loop runs autonomously and reports (batch), and how that interacts with dark factory mode under P-017 where the operator is AFK.

        SURFACES REQUIRING UPDATE IF BUILT: templates/ definition, the installed dogfood copy under .github/, the install-harness SKILL.md variable resolution table, harness-architecture primitive mapping, the Orchestrator/Stage agent templates if a trigger or routing rule is added, and docs.

        STATUS: intake only. No triage, no routing, no planning, no harvest, no shipment association performed. Provenance: Orchestrator session 2026-08-25, alongside deliberation 028-DL and stash 34AAF1C7.
    queue_position: 12
id: 030-DL
links:
    - target_id: 029-DL
      link_type: informs
    - target_id: 028-DL
      link_type: informs
priority: medium
status: done
title: 'Adversarial ideation primitive: agent ruling + durability trigger gate'
updated_at: 2026-08-31T22:31:19.4357264Z
---

# Adversarial ideation primitive: agent ruling + durability trigger gate

## Problem Frame

<!-- BEGIN:problem-frame -->
Operator asked to design into the Orchestrator or craft anew a brainstorming/research agent or skill for interactive ideation. Motivating evidence: an ad-hoc adversarial dialectic (3 rounds, 8/5/3 substantive challenges) produced two reversals of a top recommendation via new measurements, and broke a 4-session deferral loop on stash 34AAF1C7. The capability has no template, no protocol, and no primitive backing it.
<!-- END:problem-frame -->

## Options

<!-- BEGIN:options -->
- A: Embed ideation logic in the Orchestrator (REJECTED: makes the router a doer; Elective Agents table at _orchestrator.agent.md L110-121 is the precedent for trigger-phrase-plus-routing-rule only).
- B: Extend brainstorm/deliberate skills in place (REJECTED: skills cannot declare model_family/model_provider frontmatter per P-013.5, so they can request but never guarantee challenger route diversity -- review/SKILL.md L136 must hedge 'cross-model preferred but not blocking').
- C: Reuse adversarial-review wholesale (REJECTED as-is: its consensus tiering weights unique findings LOW=1 vs consensus HIGH=3, which is agreement-seeking and inverts the goal for divergent ideation; it is also structurally single-round).
- D (CHOSEN): New adversarial-ideation AGENT composing adversarial-review's dispatch/routing machinery with new round-loop, novelty-ledger, framing-critique and advocacy-floor machinery.
<!-- END:options -->

## Chosen Direction

<!-- BEGIN:chosen-direction -->
CONDITIONAL BUILD as a new agent. Agent (not skill) on the ROUTE-DECLARATION premise, not the dispatch premise -- the intake's 'skills cannot dispatch' claim is FALSE and contradicted by review/SKILL.md L33-35 and plan-review/SKILL.md L11-13, both of which spawn cross-model persona subagents. Not Orchestrator-embedded (upheld). Not an elective agent either: elective agents are 'operator-initiated only, never invoked autonomously' (L120), which contradicts the required autonomous gate trigger.

GATE CRITERION (EXPANDED round-2 addendum, 2 components -> 4). MUST NOT be harvested or shipped unless the same unit of work delivers ALL FOUR:
(A) COUNTER -- machine-computable repeat-deferral count over the UNION of the two reverse indices (backlogit custom_fields.linked_stash_id AND docs/decisions source_stash frontmatter), threshold >=2.
(B) BYPASS PENALTY (the recursive requirement) -- deliberation creation MUST go through `backlogit deliberate <stash-id>`, and verify-harness MUST FAIL on any newly created -DL item lacking linked_stash_id. Enforce-on-new-only: day-one blast radius zero, 4 legacy misses grandfathered.
(C) DEFER-EMITS-AN-ARTIFACT -- a Stage defer decision on a stash entry MUST emit a countable artifact (a -DL item at minimum). Without this the counter measures DELIBERATIONS rather than DEFERRALS and structurally cannot see the 34AAF1C7 pattern.
(D) PENALIZER -- verify-harness FAILs on >=2 counted deferrals with no linked ideation artifact.

If ANY of (A)-(D) is descoped, the recommendation INVERTS to DO NOT BUILD.
<!-- END:chosen-direction -->

## Open Questions

<!-- BEGIN:open-questions -->
1. Whether verify-harness's check-extension surface can host the D3 gate (inspected at section level, not line-by-line) -- resolvable during implementation, NOT a precondition for deciding. 2. The installed dogfood copy under .github/agents/ is MANDATORY for this agent unlike adversarial-review (which has no installed copy), because the D3 gate would otherwise fail permanently on an artifact that cannot be produced. 3. SPUN OFF, not resolved here: review/SKILL.md and plan-review/SKILL.md spawn subagents while harness-architecture L163 and role-enforcement L81 forbid skills from spawning -- either the rule or the skills are wrong; needs its own stash entry. 4. The P-006-analogue producer ('Requires adversarial ideation: yes|no', absent->yes) is INERT in this workspace because impl-plan/plan-review/harvest/deliberate are not installed (stash 8AC574F1).
<!-- END:open-questions -->

## Notes

<!-- BEGIN:notes -->
DECIDED 2026-08-31 (Stage dark-factory run; route claude-opus-5/anthropic/high). Decision D5 in docs/decisions/2026-08-31-dark-factory-staging-triage-and-shipment-portfolio.md. RESOLUTION: the selected shape - agent ruling plus durability trigger gate - authorizes the primitive CONDITIONALLY, gated on a durability trigger, i.e. evidence that the improvised adversarial dialectic RECURS and is being RE-IMPROVISED. This run produced no such evidence: its adversarial machinery was the EXISTING review-persona layer, not a re-improvisation. Building a new ideation primitive now would be premature framework complexity, which the operator's own ordering policy warns against. The deliberation is therefore decided and moves to done; source stash 08D71FD5 stays ACTIVE as the durability counter and is NOT archived.
<!-- END:notes -->
