---
artifact_type: deliberation
created_at: 2026-08-28T06:29:09.9070537Z
id: 031-DL
labels:
    - pre-review-evidence
    - fault-line-dag
    - detector-sdk
    - shipment-portfolio
    - stage-deliberation
    - read-only
links:
    - target_id: 148-F
      link_type: informs
    - target_id: 149-F
      link_type: informs
    - target_id: 029-DL
      link_type: related_to
    - target_id: 028-DL
      link_type: related_to
priority: medium
references:
    - docs/decisions/2026-08-27-pre-review-evidence-dag-shipment-portfolio-deliberation.md
    - docs/decisions/2026-08-27-recurring-review-issues-tooling-opportunities-spike.md
    - docs/plans/2026-08-27-policy-registry-and-review-persona-layer-plan.md
    - docs/plans/2026-08-27-pre-review-detector-sdk-plan.md
status: done
title: Harness-wide deterministic pre-review evidence DAG and staged shipment portfolio
updated_at: 2026-08-28T06:30:35.6333798Z
---

Backing work item for deliberation artifact `031-DL`, whose document is `docs/decisions/2026-08-27-pre-review-evidence-dag-shipment-portfolio-deliberation.md` (frontmatter `deliberation_id: "031-DL"`). Created retroactively to close a dangling-ID defect: the decision artifact claimed `031-DL` but no backing backlogit item existed, so every downstream citation of `031-DL` (plan provenance, 148-F, 149-F, task bodies) resolved to nothing.

Normalizes nine imported harness-level stash entries against the 15 dual-source recurring review families, selects a detector-SDK-plus-composable-derived-DAG architecture, defines an epoch-indexed evidence DAG contract, and stages an eleven-shipment dependency-ordered portfolio with report-only-first rollout.

<!-- BEGIN:chosen-direction -->
Detector SDK plus composable derived DAG, with an epoch-indexed evidence-node contract and a report-only-first rollout (no check promoted to blocking in the first shipments).

Staged portfolio S0..S11, dependency-ordered. Two units are staged so far:

* **S0 — Prerequisite closure** (`148-F` / `156-S`): policy registry and review-persona layer install/restore, owning stash `336F3AB7`. Plan: `docs/plans/2026-08-27-policy-registry-and-review-persona-layer-plan.md`.
* **S1 — Detector SDK, evidence-node contract, gate pre-review reader** (`149-F` / `157-S`). Plan: `docs/plans/2026-08-27-pre-review-detector-sdk-plan.md`. Blocked by `156-S`.

Governing laws carried into both plans: **Law 1** (report-only first; nothing promoted to blocking by accretion) and **Law 2** (no artifact without a named reader). Section R3 records hidden prerequisites that must not be duplicated.
<!-- END:chosen-direction -->

<!-- BEGIN:notes -->
**Lineage.** Prior spike: `docs/decisions/2026-08-27-recurring-review-issues-tooling-opportunities-spike.md`. Related deliberations: `028-DL`, `029-DL`.

**Why this item was created retroactively.** The deliberation document was authored with `deliberation_id: "031-DL"` in frontmatter, but the corresponding backlogit artifact was never created, leaving the ID dangling. A current-HEAD local review of `1bafd85e` flagged this as a P2 finding. The remedy chosen was to create the backing item rather than strip the ID, because the ID is cited across the plan, both features, and multiple task bodies; stripping it would have required edits to every citation and would have lost the artifact identity. The `DL` type counter stood at `030-DL`, so this item allocated exactly `031-DL` and the pre-existing citations resolve unchanged.

**Status.** Marked `done`: the deliberation itself is complete and `decision_status: decided`. Downstream execution is owned by `156-S` and `157-S`, not by this item.
<!-- END:notes -->

<!-- BEGIN:open-questions -->
Operator decisions Q1-Q7 are recorded in the decision artifact. Those consumed by the staged units:

* **Q1 APPROVED** — report persistence allowed, with named consumers.
* **Q5 APPROVED** — authoritative test command is `PYTHONPATH=src python -m unittest discover -s tests`.
* **Q7 APPROVED** — clear S0; do not waive it. S0 must be staged before S1.

Still open: S2-S11 are NOT staged. Stash `D911A3B2` and `89E833E1` remain ACTIVE and own later S2-S10 scope; they must not be archived. S1 unit U11 (`149.011-T`) carries an RK1 falsification gate that is a genuine early exit.
<!-- END:open-questions -->

<!-- BEGIN:options -->
Architectural options considered, plus the eleven-shipment portfolio sequencing alternatives, are recorded in full in the linked decision artifact. This item is the backlogit identity for that artifact and does not restate its option analysis.
<!-- END:options -->

<!-- BEGIN:problem-frame -->
Nine harness-level stash entries and 15 dual-source recurring review families describe overlapping symptoms of one underlying condition: pre-review evidence is produced ad hoc, is not addressable, and cannot be composed or replayed deterministically. The deliberation frames whether a detector SDK plus a derived evidence DAG is the right primitive, and in what dependency order the work can be staged without creating hidden prerequisites.

The **nine** imported/elevated entries are `D911A3B2` (epic) plus eight
features: `39AA674D`, `926FEA6D`, `A02280C8`, `3F80F8A3`, `C327A8DE`,
`7A3F570B`, `89E833E1`, `8CB5A9B9`.

A tenth stash ID, `34AAF1C7`, also appears in the decision artifact's
`source_stash_ids` but is **not** one of the imported nine: it is a
pre-existing **living tracker that is RETAINED, not imported or elevated**, and
only its branch (a) (PR-review convergence) is consumed here, via S9. Branch (b)
(reasoning-state identity) stays in the stash, still blocked on A8.

Nine imported/elevated + one retained tracker = the ten IDs listed in
frontmatter. The prose count "Nine" is correct and matches the "`D911A3B2` epic
+ eight features" arithmetic; what was missing was the tracker's distinct
provenance.
<!-- END:problem-frame -->