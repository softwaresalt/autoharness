---
archived_from: .backlogit/queue/041.004-T.md
artifact_type: task
commit: 6b43b560348c9c6bdabaad1fa5039b5167b35e0d
created_at: 2026-05-17T19:21:16.6867924-07:00
id: 041.004-T
labels:
    - ship-agent
    - template
parent_id: 041-F
priority: medium
status: archived
title: Apply same Step 4/5 hardening to templates/agents/ship.agent.md.tmpl (template parity)
updated_at: 2026-05-17T20:27:43.473436-07:00
---

## Context

`templates/agents/ship.agent.md.tmpl` is the source template for the Ship agent. The installed `.github/agents/ship.agent.md` is separately hardened in 041.003-T. This task applies identical hardening to the template so all future workspace installs receive the strengthened gate from day one — no manual patching required after install.

The template is 51KB+ (large file). Ship should use `view_range` to read only the Step 4 and Step 5 sections before editing.

## Work

Apply the exact same changes as 041.003-T to `templates/agents/ship.agent.md.tmpl`:

### Step 4 hardening (locate the PR Lifecycle step — approx lines 300–350)

Replace the existing `### Step 4:` / PR Lifecycle block with the hardened version from 041.003-T:
- Add bullet 3: P-014 Copilot Review Merge Gate block (NON-NEGOTIABLE), naming `github-pr-automation.instructions.md` and §1.9, with explicit log output for passing gate
- Add bullet 4: Operator approval as a distinct named gate (never treat silence or green CI as approval)
- Add bullet 5: Last-mile re-check requirement if new commits arrive between gate check and approval

### Step 5 hardening (locate Post-Merge Closure section)

After the "Merge Confirmation Gate" block and before or after "Release Closure Completion Gate", add:

```markdown
#### Post-Merge Closure PR Copilot Gate (P-014, NON-NEGOTIABLE)

When a post-merge closure branch and PR are created:

1. Request Copilot Review immediately after PR creation (per §1.1 of
   `.github/instructions/github-pr-automation.instructions.md`).
2. Poll for review completion per §1.2 back-off cadence.
3. Address any comments per §1.3–§1.7.
4. Run §1.9 readiness gate before presenting the post-merge closure PR for merge.
5. Obtain explicit operator approval — the prior main PR approval does not transfer.
6. P-014 applies in full. Record a P-014 violation via P-005 telemetry if this gate is skipped.
```

## Acceptance Criteria

- [ ] Template Step 4 contains P-014 gate block with §1.9 reference
- [ ] Template Step 4 operator approval gate is a distinct named step
- [ ] Template Step 5 contains "Post-Merge Closure PR Copilot Gate" subsection
- [ ] Template parity: changes in template match 041.003-T exactly in intent and structure
- [ ] File passes markdownlint (P-008) — no heading hierarchy violations introduced
- [ ] YAML frontmatter valid (critical: this is a .tmpl file with frontmatter)

<!-- BEGIN:Dependencies -->
Depends on 041.003-T. Apply same changes to template as were applied to the installed artifact.
<!-- END:Dependencies -->