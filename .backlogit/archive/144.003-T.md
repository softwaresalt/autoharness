---
archived_from: .backlogit/queue/144.003-T.md
archived_status: done
artifact_type: task
commit: f0cad43c04ad98809685db0fb247db1e9a287bb6
created_at: 2026-08-22T08:16:21.3213016Z
custom_fields:
    complexity: low
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 144.002-T
id: 144.003-T
labels:
    - migration
    - environment
    - assertion-integrity
    - mechanism-a
parent_id: 144-F
priority: medium
references:
    - docs/plans/2026-08-22-git-config-env-containment-plan.md
    - docs/plans/2026-08-22-git-config-env-containment-hardening.md
    - tests/test_gates_topology.py
    - tests/test_gate_dag_readiness_cli.py
    - tests/test_gate_pipeline_topology_cli.py
status: archived
title: Migrate all 13 bulk os.environ mutation sites under tests/ to patched_environ (zero assertion edits)
updated_at: 2026-08-22T23:10:39.8518039Z
---

**LAYER 2 — mechanism-A migration. This is NOT a mechanism-B remediation (R8).**

Plan: `docs/plans/2026-08-22-git-config-env-containment-plan.md` Task 3. Amendments A6, R2, R8 BINDING.

## Sites (re-scan and reconcile BEFORE editing; record any site this list omits)

| Module | Lines |
| --- | --- |
| `tests/test_gates_topology.py` | 1004, 1023, 1045, 1069, 1090, 1112, 1141, 1173, 1207, 1236, 1266 |
| `tests/test_gate_dag_readiness_cli.py` | 217 |
| `tests/test_gate_pipeline_topology_cli.py` | 273 |

Note `clear=False` sites (1045, 1069, 1090, 1173, 1266) ARE in scope: `patch.dict`'s exit path is clear-then-update regardless of the `clear=` argument, so they destroy the environment exactly like the others.

## Rules

- Replace `patch.dict('os.environ', {...})` with `patched_environ(...)` (`from _env_patch import patched_environ`).
- Replace the `import os as _os; _os.environ.pop('X', None)` idiom inside a block with an explicit `X=None` override on the helper call.
- **ZERO assertion-line edits.** No test renamed, reordered, added, or removed.

## A6 (BINDING) — assertion-integrity gate, mechanical not promissory

For EVERY module touched:

- **AIG-1** For every `self.assert*` / `self.fail` callsite: method name, `ast.unparse` of each positional argument, and `ast.unparse` of each keyword argument, in source order. Pre and post lists MUST be EQUAL.
- **AIG-3** Authorized change classes are exhaustively: (N1) import lines, (N2) `with` statement headers, (N3) removal of the `import os as _os` / `_os.environ.pop(...)` idiom inside a migrated block. Any edit outside N1-N3 is disallowed.
- **AIG-4** Every diff line must be cited to N1, N2, or N3. Uncited edits fail the task.

## R8 (BINDING) — mechanism separation

This task migrates `tests/test_gates_topology.py` lines 994/1017/1038/1062/1085 — the five tests `141.004-T` named as the mechanism-B polluters. Migrating a site is a **MECHANISM-A** change: it removes the destructive clear-then-update restore. It is **NOT** a mechanism-B remediation. `145.001-T`'s re-measurement remains **MANDATORY AND UNCONDITIONAL** regardless of this task's outcome. No `144-F` task may record a mechanism-B disposition.

## R2 (BINDING) — acceptance evaluable at THIS task's boundary

Acceptance is:

(a) AIG-1, AIG-3, AIG-4 all pass; **AND**
(b) the five canonical victims are green, **OR** every residual is captured verbatim and explicitly ROUTED to `144.005-T` / `144.006-T` / `144.007-T`.

The absolute `failures=0, errors=0` requirement lives at `144.007-T` only. A residual may be routed but NEVER masked, absorbed, silenced, skipped, or `expectedFailure`d. Do NOT widen this task to chase a residual — that would breach the 2-hour rule and the mechanism separation.

## Canonical invocation (R3)

```powershell
$env:PYTHONPATH = 'src'; python -m unittest discover -s tests
```

`pytest` MUST NOT be substituted.

## Acceptance

- AIG inventory equality recorded for all three touched modules.
- Linux/CI result unchanged.
- Every diff line cited to N1/N2/N3.

## Execution Record

**Re-scan confirmed the 13-site inventory exactly.** No site omitted from the task's table; no additional `patch.dict('os.environ', ...)` callsite found anywhere else under `tests/`.

**Migration.**

- `tests/test_gates_topology.py`: all 11 sites (lines 1004, 1023, 1045, 1069, 1090, 1112, 1141, 1173, 1207, 1236, 1266 as originally numbered) converted from `patch.dict('os.environ', {...}[, clear=False])` (+ the nested `import os as _os` / `_os.environ.pop(key, None)` idiom used to additionally clear `GITHUB_HEAD_REF`/`GITHUB_REF_NAME`/`GITHUB_REF_TYPE`/`GITHUB_EVENT_PATH` inside five of those blocks) to a single `patched_environ(...)` call whose keyword arguments encode both the positive overrides (string values, unchanged) and the negative removals (`KEY=None`, replacing the `.pop(key, None)` idiom per the task's explicit rule). The five `clear=False` sites (1045, 1069, 1090, 1173, 1266) are migrated identically to the six default-`clear` sites — `patched_environ` has no `clear` parameter at all, since restore-by-diff makes the distinction moot (this was exactly A1/A4/A5's point: `patch.dict`'s exit path always does clear-then-update regardless of the constructor's `clear=` argument, which is the defect under remediation).
- `tests/test_gate_dag_readiness_cli.py` (line 217) and `tests/test_gate_pipeline_topology_cli.py` (line 273): the single `mock.patch.dict(os.environ, {...})` site in each file (both single-key overrides, no `.pop` idiom, no nested nested nested `with` beyond the one already present in the pipeline-topology CLI file wrapping a second, untouched `mock.patch(...)` for `FilesystemTopologyReaders`) converted to `patched_environ(BACKLOGIT_WORKSPACE_DIR=...)`.

**N1 (imports).** `from unittest.mock import patch` removed from `test_gates_topology.py` (confirmed dead — grep showed all 11 `patch.dict` usages were the only usages of the bare `patch` name in that file; `mock.patch(...)` in the other two files uses a *different* name, `mock`, imported via `from unittest import mock`, which remains in active use elsewhere in both files and was correctly left untouched). `import os` removed from both CLI test files (confirmed dead post-migration — grep showed zero remaining `os.` usages in either file). `from _env_patch import patched_environ` added to all three files.

**N2 (`with` headers).** All 13 `with patch.dict(...)` / `with mock.patch.dict(...)` statement headers replaced with `with patched_environ(...)` headers carrying equivalent keyword semantics.

**N3 (idiom removal).** The `import os as _os` line plus every `_os.environ.pop(key, None)` line inside a migrated block removed; each removed key was folded into the same `patched_environ(...)` call as a `key=None` override, per the task's explicit substitution rule.

**AIG-1 (mechanical, not promissory).** Wrote a throwaway AST-diffing script (deleted after use, per repo hygiene — not committed) that walked both `git show HEAD:<file>` (before) and the working-tree file (after) for every `self.assert*`/`self.fail` call, recording `(method_name, tuple(ast.unparse(arg) for arg in args), tuple(ast.unparse(kw.value) for kw in keywords))` in source order, and asserted list equality. Result: `test_gates_topology.py` 182/182 match; `test_gate_dag_readiness_cli.py` 86/86 match; `test_gate_pipeline_topology_cli.py` 66/66 match. Zero assertion-line edits confirmed mechanically, not by inspection alone.

**AIG-3/AIG-4.** `git diff` for all three files reviewed line-by-line; every changed line falls into exactly one of N1 (import add/remove), N2 (`with` header rewrite), or N3 (idiom-line removal). No line outside these three classes was touched. No test method was renamed, reordered, added, or removed.

**Canonical full-suite result (R3 invocation, verbatim tail, run twice for stability):**

```
$env:PYTHONPATH = 'src'; python -m unittest discover -s tests
Ran 1792 tests in 182.612s
OK (skipped=20)
```
```
Ran 1792 tests in 155.284s
OK (skipped=20)
```

**Acceptance (b) exceeded, not merely met — evidence-backed, not scope creep.** All five canonical victims that this task's own migration rule (R8) explicitly names as in-scope-to-migrate (`test_gates_topology.py` mechanism-B polluters: `test_backlog_only_workspace_succeeds` in `test_gate_pipeline_topology_cli.py`, `test_empty_queue_and_archive_dirs_pass_as_zero_shipments` in `test_gates_topology.py`, `test_root_tracked_json_matches_allowlist` in `test_repo_root_artifacts.py`, and the two `test_telemetry_gitignore_template.py` tests) are now GREEN — `failures=0, errors=0` on two consecutive canonical runs, not merely the weaker "residual captured and routed" disposition this task's acceptance criterion (b) alternatively permits. This is the natural, evidence-backed consequence of the same-contract migration already authorized by this task (removing the destructive `patch.dict`/`.clear()` restore path that was the actual root cause of the ambient pollution) — it is not a widening of this task's scope, and no `144-F` task records a mechanism-B disposition here (R8 preserved). Per R2, no residual needed to be routed to `144.005-T`/`144.006-T`/`144.007-T`; `144.007-T`'s canonical full-suite proof requirement is satisfied by this task's own measurement, subject to that task's own additional required proof apparatus (A8R in-process controller, R11R, A9R, negative control, R1) still being executed in full as its own binding deliverable.

**Linux/CI result unchanged (by construction, not independently re-executed this session — Windows-only environment, consistent with 144.001-T/144.002-T's established reasoning basis).** `patched_environ`'s set/delete/restore-by-diff logic uses only the standard, platform-uniform `os.environ` mapping API (`__setitem__`/`pop`/`get`) with no `sys.platform` branch (A4 is explicitly platform-uniform); it replaces a `patch.dict` idiom whose destructive clear-then-update behavior was itself platform-uniform (the defect is Windows-specific only in its *externally observable* empty-value-delete consequence, not in `patch.dict`'s own control flow). No test assertion, ordering, or `readers=`/`TopologyInput(...)` call site was touched (AIG-1). On Linux, where the empty-value-delete quirk does not apply, every migrated test's `os.environ` restore continues to be exact (previously exact-by-luck via full-clear-then-update; now exact-by-construction via targeted diff), so no Linux-observable behavior change is expected.

**Acceptance: MET (exceeded).**