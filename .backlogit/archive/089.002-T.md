---
artifact_type: task
created_at: 2026-07-26T08:05:12.5046452Z
id: 089.002-T
labels:
    - 088-F
    - chore
    - tdd
parent_id: 089-F
priority: medium
references:
    - docs/plans/2026-07-26-088-f-review-followup-hardening-plan.md
status: done
title: benchmark-early-decline-carries-capture-failed-provenance
updated_at: 2026-07-26T08:42:07.5124028Z
---

Fix `_run_compression_case`'s early-decline return path
(`experiments/088-compression-experiment/brainspace/benchmark.py`, ~L203-215,
the `"modifiedResult" not in result` branch) so it carries `case.capture_failed`
and `case.provenance` into the returned `CaseResult`, matching the reporting
shape the full (non-declined) path already uses at L266 (`"capture_succeeded":
not case.capture_failed`) and L288-296 (provenance/capture-failed notes
annotations).

Scope: exactly 2 files — `brainspace/benchmark.py` (fix) and
`tests/test_benchmark_runner.py` (tests). Reuse the existing `criteria`
dict / `notes` string containers — do not add new `CaseResult` dataclass
fields (would widen the change surface to report renderers/schema beyond this
fix's scope).

<!-- BEGIN:acceptance-criteria -->
- TDD red phase: add `test_early_decline_case_carries_capture_failed_into_result`
  to `tests/test_benchmark_runner.py` — a `BenchmarkCase` with
  `capture_failed=True` that the hook declines (no `modifiedResult`) must
  produce a `CaseResult` whose `criteria` reflects the capture failure (e.g.
  `capture_succeeded is False`) rather than omitting it. Confirm this test
  fails against current code (the early-decline branch currently returns only
  `{"compressed_at_all": False}`).
- Add `test_early_decline_case_carries_non_live_provenance_into_result` — a
  declined `BenchmarkCase` with a non-`"live"` `provenance` (e.g.
  `"replayed"`) must have that provenance reflected in the returned result
  (criteria or notes), not silently reported as an unannotated decline.
- TDD green phase: extend the early-decline `CaseResult` construction to
  include the capture_failed/provenance evidence; both new tests pass; no
  existing test in `test_benchmark_runner.py` regresses (in particular
  `test_decline_control_case_reports_no_durable_row`,
  `test_report_decline_correct_count_tracked_separately_from_safe_win_count`).
- `pytest experiments/088-compression-experiment/tests/test_benchmark_runner.py -v`
  is green.
- The evidence oracle / decide-then-stash / byte-equivalent retrieval
  invariants are unaffected (this fix only changes what the early-decline
  `CaseResult` reports, not hook/store behavior).
<!-- END:acceptance-criteria -->