---
archived_from: .backlogit/archive/041.005-T.md
artifact_type: task
commit: 6b43b560348c9c6bdabaad1fa5039b5167b35e0d
created_at: 2026-05-17T19:21:38.9416817-07:00
id: 041.005-T
labels:
    - pr-lifecycle
    - skill-template
    - copilot-review
parent_id: 041-F
priority: medium
status: archived
title: Harden pr-lifecycle/SKILL.md.tmpl — Step 5b P-014 reference + Step 5c last-mile §1.9 re-check
updated_at: 2026-05-17T20:27:43.7827771-07:00
---

## Context

`templates/skills/pr-lifecycle/SKILL.md.tmpl` already has strong §1.9 coverage in Step 5a (Pre-Merge Review Readiness Verification). However it is missing one critical sub-step: a **last-mile §1.9 re-check** that runs *after* the operator signals approval and *before* the merge action executes. 

The incident pattern is:
1. §1.9 gate checks — clean
2. Agent presents PR as ready
3. Time passes (operator is reviewing, or the agent moves on assuming approval)
4. New commits push, or Copilot posts a comment
5. Merge executes without re-verifying

The fix is a final §1.9 re-check at the moment of merge execution, not just at the moment of readiness presentation.

Also: Step 5b's "Never auto-merge" instruction should be augmented with an explicit P-014 cross-reference so agents reading the skill know the policy name to cite in violation telemetry.

## Work

### Step 5b hardening

In `templates/skills/pr-lifecycle/SKILL.md.tmpl`, locate Step 5b (Present merge readiness) and add after the existing "Wait for explicit user approval before any merge action" bullet:

```markdown
5. **Operator approval gate (P-014)**: After the §1.9 gate passes, wait for an
   explicit operator approval signal. Green CI is not approval. A passing §1.9
   gate is not approval. Record P-014 (via P-005 telemetry) if merge is executed
   without an explicit approval signal.
```

### Add new Step 5c: Last-mile §1.9 re-check before merge execution

After Step 5b, add a new step:

```markdown
#### Step 5c: Last-Mile §1.9 Re-check Before Merge Execution

After receiving operator approval and before executing the merge:

1. Check whether any new commits have been pushed to the branch since the §1.9
   gate ran. If yes, re-run §1.9 in full — the prior gate result is stale.
2. Check whether any new Copilot review threads have been opened since the §1.9
   gate ran (query `reviewThreads` filtered to `isResolved == false` and
   `author.login == "copilot-pull-request-reviewer"`). If any new unresolved
   threads exist, halt and report — do not proceed to merge.
3. If the branch HEAD and review state are unchanged from the §1.9 gate run,
   log `P-014 LAST-MILE CHECK PASSED: branch unchanged, no new unresolved threads`.
4. Execute the merge only after this check passes.

This last-mile check closes the race window between approval receipt and merge
execution. It is a lightweight incremental query (not a full §1.9 re-run) when the
branch has not changed.
```

## Acceptance Criteria

- [ ] Step 5b contains P-014 reference and "Record P-014 via P-005 telemetry" instruction
- [ ] Step 5c exists as a distinct named step in the skill
- [ ] Step 5c includes the branch-change detection check
- [ ] Step 5c includes the new-unresolved-thread check
- [ ] Step 5c includes the log output format `P-014 LAST-MILE CHECK PASSED`
- [ ] Step 5c correctly positions the last-mile check as between approval receipt and merge execution
- [ ] File passes markdownlint (P-008)
- [ ] YAML frontmatter valid

<!-- BEGIN:Dependencies -->
No hard dependencies. Can execute in parallel with 041.001-T after 041.002-T is complete (needs §1.9 to be defined before referencing it).
<!-- END:Dependencies -->