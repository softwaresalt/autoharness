---
archived_from: .backlogit/queue/149-F.md
archived_status: done
artifact_type: feature
commit: f93afa0eee8d228ff4a7ac54cf3b2b3b4ec5eeb9
created_at: 2026-08-28T03:57:43.691578Z
custom_fields:
    harness_status: pending
id: 149-F
labels:
    - S1
    - detector-sdk
    - pre-review
    - gate
    - D911A3B2
    - 89E833E1
priority: critical
references:
    - docs/plans/2026-08-27-pre-review-detector-sdk-plan.md
    - docs/decisions/2026-08-27-pre-review-evidence-dag-shipment-portfolio-deliberation.md
status: archived
title: S1 — Detector SDK, evidence-node contract, and gate pre-review reader
updated_at: 2026-08-30T01:17:44.4847904Z
---

Detector/evidence-node SDK: node contract (D1-D10), registry, applicability engine, derived DAG assembler with cycle detection, report emitter, `autoharness gate pre-review [--json] [--base <ref>]`, and exactly one reference detector (ART-01 section-marker conformance). Report-only, exit 0 except invalid registry. Depends on S0.

<!-- BEGIN:description -->
**Authoritative scope**: `031-DL` `docs/decisions/2026-08-27-pre-review-evidence-dag-shipment-portfolio-deliberation.md` section 'Shipment Portfolio' -> **S1 — Detector SDK, evidence-node contract, and `gate pre-review` reader**, plus section 'DAG Contract' D1-D10.

**Reviewed plan**: `docs/plans/2026-08-27-pre-review-detector-sdk-plan.md` (impl-plan -> plan-harden -> plan-review, `decision: PASS`, `dispatch_mode: single-agent-declared-degradation`).

## SPLIT LINEAGE — both umbrella stash entries REMAIN ACTIVE

**`D911A3B2`** (critical, epic) — *program frame*, disposition **RETAIN (scope-narrowed)**. It is the program epic over **S1-S10**. **NOT consumed, NOT archived by this feature** — it continues to own S2-S10. Two inherited narrowings bind here:
1. 'evidence persistence' is bounded to an **epoch-keyed report** (`<head_sha>-<fingerprint>`, D-11) under Law 1 — never a persisted graph.
2. 'extension points for repository-specific adapters' is **deferred** until a second consumer repository exists (Law 2).

**`89E833E1`** (critical, feature) — disposition **SPLIT (3 ways)**. **NOT consumed, NOT archived** — splits (b) and (c) remain its property:

| Split | Content | Target | Owned here? |
|---|---|---|---|
| **(a)** | Detector/evidence-node **SDK + node contract** | **S1** | **YES — this feature** |
| (b) | DAG family assembly + incremental evaluation + shipment attachment | S8 | No |
| (c) | 'Require verified evidence at every applicable terminal node' + waiver/override authority | S10 | No — blocking-policy promotion, must NOT travel with (a) |
| *pruned* | 'query/visualization' | — | **PRUNED** until a reader exists (Law 2) |

## Operator authority consumed

* **Q1 APPROVED** — a derived pre-review evidence report MAY persist under `.autoharness/gates/` because named PR-readiness and closure consumers require it. It remains **derived evidence, not the source of truth**; **report-only first**; **no blocking authority**.
* **Q5 APPROVED** — authoritative test command `PYTHONPATH=src python -m unittest discover -s tests`; acceptance criteria do NOT substitute pytest.
* **Q7 APPROVED** — S0 cleared, not waived; S1 is sequenced after S0 via a shipment-level `blocks` edge.

## Reference detector

**`ART-01`** = backlogit section-marker conformance. Each `.backlogit/templates/<type>.md` declares a `sections:` list in frontmatter; the body declares `<!-- BEGIN:<name> -->` / `<!-- END:<name> -->` pairs. `ART-01` asserts every artifact under `.backlogit/queue/` carries a well-formed pair per declared section and that every `required: true` section is non-empty. Chosen because it is closed-surface, declared-shape, deterministic, reads pre-existing data, and needs **no new author-minted identifier** (A8/RK2).
<!-- END:description -->

<!-- BEGIN:dod -->
* `gate pre-review` runs on a real diff and emits a schema-valid report.
* **RK1 falsification gate**: `ART-01` re-detects at least one historical defect from PR #234 / #185 / #183 / #189 / #202 / #123 / #213 (local fixtures, no network). If it re-detects NONE, **halt the shipment and escalate** for the Option A reconsideration — do not proceed with a weaker claim.
* Registry cycle injection -> `invalid`, exit 2, and **zero nodes evaluated** (count asserted, not just exit code).
* **Exit-code invariance**: across every fixture the only observed exit codes are 0 and 2. Any 1 or 3 is a hard stop.
* Exit 0 even when `ART-01` reports `failed` (report-only).
* **INV-1** no persisted graph; **INV-2** report never read back as authoritative; **INV-6** exactly one detector; **INV-7** zero backlogit mutation.
* **INV-8 (D-11)** epoch key is a deterministic function of HEAD plus the freshness fingerprint; a rerun at unchanged HEAD after a tool-version change writes **new fresh evidence** under a new key without overwrite, and stale siblings are never consumed as fresh.
* **INV-9 (D-12)** no user-controlled ref text ever reaches a `git` argument unresolved; only validated `^[0-9a-f]{40}$` SHAs are passed to discovery, and an option-like `--base` is rejected with exit 2 and **no file written**.
* Ref-resolution containment: refs resolve only inside `autoharness.detectors`; anything else is `invalid`, never imported.
* **Contract-freeze and canonical outcome (D-9)**: the status vocabulary is exactly the 8 named values, carried on **ONE** canonical `status` field; a contradictory outcome pair is unrepresentable (no independent `verdict` field; any alias is derived and unserialized).
* **Schema mirror parity (D-10)**: the `detectors` block is present in BOTH the pointer and the versioned schema, they remain equal except `$id`, and detector validation cases run against the **versioned** (runtime-resolved) document.
* Back-compat: config with no `detectors` block validates, yields zero nodes, exit 0.
* Gate: `PYTHONPATH=src python -m unittest discover -s tests` green.
<!-- END:dod -->

<!-- BEGIN:goals -->
1. Land the node/evidence/outcome contract (D1-D8) reusing `CheckResult` semantics, with **one canonical `status` field** (D-9).
2. Extend the validation-gates schema — **both the pointer and the versioned file** (D-10) — with a schema-validated `detectors` registry block.
3. Build the applicability engine over `discover_modified_files` with fail-closed FC1/FC2.
4. Assemble the DAG at read time with three-colour cycle detection; cycle = registry defect -> exit 2, evaluate nothing.
5. Ship `ART-01` end-to-end — the RK1 falsification test for whether the SDK is justified at all.
6. Emit the **epoch-keyed** report (`<head_sha>-<fingerprint>`, D-11) under the Q1 exception, with the D6 freshness rule and refresh-at-unchanged-HEAD support.
7. Wire `autoharness gate pre-review [--json] [--base <ref>]` following the `dag-readiness` shape, with **option-safe user ref resolution** (D-12).
<!-- END:goals -->