---
artifact_type: task
commit: c3929ea
created_at: 2026-05-17T12:49:26.1859701-07:00
id: 035.005-T
labels:
    - docs
    - changelog
    - release
parent_id: 035-F
priority: medium
status: done
title: Update CHANGELOG for PyPI release pipeline and confirm version strategy
updated_at: 2026-05-17T12:57:19.4650777-07:00
---

The CHANGELOG currently has a 1.4.1 entry (template and startup-script changes from PR #80). The PyPI distribution pipeline is a separate, substantive change that needs a changelog entry so the release workflow's changelog-extraction step finds valid notes.

Decision point for Ship to present to operator (and choose the default unless operator has already indicated preference):
- **Recommended default**: Amend the existing `## 1.4.1` section to add a new `### Added` bullet for the PyPI distribution pipeline. This treats 1.4.1 as the first PyPI-published release. pyproject.toml version stays at `1.4.1`.
- **Alternative**: Bump to `1.4.2` in pyproject.toml and `src/autoharness/__init__.py` (and plugin.json + marketplace manifest if applicable), add a new `## 1.4.2` CHANGELOG section. Use only if operator wants a clean version boundary.

Regardless of which option is chosen:

Add to the relevant CHANGELOG section:
```
- Added a PyPI-backed release pipeline: on every `v*` tag push, the release
  workflow builds a wheel and sdist, validates with twine, publishes to PyPI
  via OIDC Trusted Publisher, smoke-tests the published package, and creates
  or updates the GitHub Release. `uv tool install autoharness` and
  `uv tool upgrade autoharness` now resolve from PyPI rather than requiring a
  Git URL clone. The Git URL remains valid as an explicit snapshot or developer
  install path.
```

Verify:
- `python -c "import tomllib; open('pyproject.toml','rb') as f: v=tomllib.load(f)['project']['version']"` matches the CHANGELOG section header
- Release workflow changelog-extraction step (awk against CHANGELOG.md) would find a non-empty section for the version

Commit message: `chore: update CHANGELOG for 1.4.1 PyPI release pipeline`