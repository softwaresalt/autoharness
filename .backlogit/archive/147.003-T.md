---
artifact_type: task
commit: f2797817c547376fc237fcecc2ee71518d9ee58c
created_at: 2026-08-24T07:18:48.5994694Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 1.0.0
    size_source: agent
dependencies:
    - 147.001-T
    - 147.002-T
id: 147.003-T
labels:
    - tests
    - regression
    - cascade-close
    - safety-gate
parent_id: 147-F
priority: medium
status: done
title: Regression coverage for the two-set cascade-close gate (8 scenarios + structural assertions)
updated_at: 2026-08-24T08:55:00.1157561Z
---

New file: `tests/test_cascade_close_archived_ids_postcondition.py` -- a repo-side contract test over the template text, matching the existing `tests/test_shipment_reconcile_*.py` pattern.

SCENARIOS (all mandatory)
1. All-new members -> gate passes.
2. Omitted truly pre-archived tasks -> gate passes (this is the case the old gate wrongly failed).
3. An allowed pre-archived member still INCLUDED in `archived_ids` -> gate passes (inclusion and omission are both acceptable).
4. Missing required IDs (`required_ids - archived_ids` non-empty) -> HALT.
5. Unexpected IDs (`archived_ids - allowed_ids` non-empty) -> HALT.
6. Incorrect persisted final state -> HALT (preserved check still asserted).
7. An archive-directory record declaring `status: done` is NOT treated as truly archived -> it is therefore in `required_ids`, and omitting it HALTs. MANDATORY: this pins the exact confusion that invalidated the 2026-08-18 spike (027-DL Option D rejection).
8. (AMENDMENT A4, BINDING) A case in which a POST-close status read would PASS but the Step 0(b) snapshot correctly HALTs -- pinning the snapshot-timing failure mode by test, not only by prose.

STRUCTURAL ASSERTIONS
- (AMENDMENT A2, BINDING) Assert the PRESENCE of BOTH distinct halt strings in the skill template text -- `cascade archived unexpected artifact` AND `cascade did not archive required artifact` -- so deleting either condition breaks a test rather than silently widening the gate.
- Assert the shipment record is an UNCONDITIONAL member of `required_ids` and the set is not derived only by iterating Step 0(b)'s manifest-member entries.
- Assert qualifying feature members' declared statuses are captured in the pre-close status map after Step 0(c) identifies them and before the cascade invocation.
- Assert the policy template no longer contains the full-set-equality claim ("nothing more, nothing less" / "must never be relaxed" as applied to `archived_ids`).
- Assert the 1.19.0 changelog row is preserved byte-identical.

ACCEPTANCE
- Targeted `tests.test_cascade_close_archived_ids_postcondition` coverage is green.
- Existing `tests/test_shipment_reconcile_*.py` and `tests/test_shipment_closure_classification.py` still green.
- `PYTHONPATH=src python -m unittest discover -s tests` is green as the canonical full test gate.
- Every normative clause of the corrected contract maps to at least one scenario (no orphan clause).

WIDTH ISOLATION: tests only. No template or doc changes in this task.