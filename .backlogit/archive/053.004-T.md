---
artifact_type: task
created_at: 2026-07-02T23:03:33.199759-07:00
id: 053.004-T
labels:
    - model-routing
    - model-tier
    - p013
    - cleanup
parent_id: 053-F
priority: medium
references:
    - templates/agents/*.agent.md.tmpl
    - templates/agents/review/*.agent.md.tmpl
    - templates/agents/research/*.agent.md.tmpl
    - .github/agents/_orchestrator.agent.md
    - .github/agents/.ship.agent.md
    - .github/agents/.stage.agent.md
    - src/autoharness/verify_workspace.py
    - templates/policies/workflow-policies.md.tmpl
    - templates/skills/doc-review/SKILL.md.tmpl
    - tests/test_verify_workspace.py
status: done
title: Remove model_tier frontmatter field from agent definitions (keep config model_routing)
updated_at: 2026-07-12T10:27:42.5632037-07:00
---

<!-- BEGIN:description -->
Remove the redundant per-agent `model_tier` frontmatter integer from all agent definitions.

OPERATOR DECISION (2026-07-11): config-driven `model_routing` in .autoharness/config.yaml is KEPT (explicitly wanted). Only the `model_tier` frontmatter field is removed. The earlier "remove model_routing" framing was a misnomer for the operator's actual intent: retire the redundant per-agent tier integer while preserving config-driven agent/model routing.

Routing stays config-driven: each agent template hardcodes which tier's `{{TIER_n_*}}` placeholders it resolves, and the installer fills those from config `model_routing.tierN` into the agent's `model_family` / `model_provider` / `reasoning_effort` frontmatter (all KEPT). `max_subagent_tier` and `subagent_depth` are also KEPT — only `model_tier` is redundant (a literal integer duplicating the tier the template already selects). Its only consumer is verify_workspace's frontmatter tier check.

Scope:
1. Remove `model_tier:` line from agent templates (templates/agents/**/*.agent.md.tmpl) + installed instances (.github/agents/{_orchestrator,.ship,.stage}.agent.md).
2. verify_workspace `_add_frontmatter_tier_check`: validate only `max_subagent_tier` (drop model_tier); update docstring.
3. verify_workspace `p013_policy_in_workflow_policies` must_contain: drop `model_tier`.
4. Reframe P-013.1 + P-013.4 in workflow-policies.md.tmpl: tier is config-resolved, not a frontmatter integer; keep max_subagent_tier requirement + tier-routing intent.
5. doc-review SKILL check (model_tier presence) — remove/change.
6. Update docs/product-specs/orchestrator-model-routing-spec.md + docs/compound/p013-orchestrator-model-routing.md to reflect removal.

Leave historical plan/memory/deliberation docs intact (dated point-in-time records). Skip gitignored .autoharness/staging.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
TDD: update tests/test_verify_workspace.py to encode the new contract (verify no longer requires model_tier; still requires max_subagent_tier); add a scan test asserting no agent frontmatter (templates + installed instances) contains a `model_tier` field. Confirm RED before implementing. `model_tier` is a literal int in templates (not a {{VARIABLE}} placeholder), so removal is deterministic. Config schemas + templates/harness-config.yaml.tmpl are UNCHANGED (model_routing config kept). Construct-2 schema-version skew note is obsolete (config not being removed).
<!-- END:implementation-notes -->