---
archived_from: .backlogit/queue/128-S.md
archived_status: shipped
artifact_type: shipment
commit: 915923c25453739b6da955fe247bd4b38a11e830
created_at: 2026-08-10T08:13:30.0105463Z
custom_fields:
    items:
        - 119-F
        - 119.001-T
        - 119.002-T
        - 119.003-T
        - 119.004-T
        - 119.005-T
        - 119.006-T
dependencies:
    - 127-S
id: 128-S
links:
    - target_id: 125-S
      link_type: supersedes
priority: critical
status: archived
title: 'S2 Copilot supervisor: supervision core library, unwired (P0)'
updated_at: 2026-08-13T05:27:44.5698098Z
---

Shipment 2 of the strict serial chain `127-S` -> `128-S` -> `129-S` for Plan 1. Supersedes the retired `125-S`. BLOCKED until `127-S` ships.

MANIFEST CONTRACT (H10.5 — fully-covered root covering feature). Lists the covering feature `119-F` FIRST, then all six of its children (`119.001-T`..`119.006-T`) in dependency order. Same two invariants as `127-S`: (i) FULL COVERAGE — `119-F` has exactly these six children, so `returnUnreleasedFeatureItems(119-F)` returns the empty set at close; (ii) ROOT PLACEMENT — `119-F` has no parent feature, so `featureScopeRoots` cannot escape into `120-F` or `117-F`.

SYMMETRIC GUARANTEE FROM THE PREDECESSOR: nothing in this shipment is reachable from the `127-S` close either. No `127-S` member has `119-F` as an ancestor, so `featureScopeRoots` never discovers `119-F` and `returnUnreleasedFeatureItems` is never invoked for it. When Ship claims `128-S`, all six tasks are guaranteed to still be `queued` with `parent_id: 119-F` intact — no repair, no re-adoption, no verification-then-fix step. The cross-shipment `blocks` edges (`119.001-T` -> `118.003-T`, `119.004-T` -> `118.004-T`) are dependency edges, not parent edges, and the close path never traverses them.

NO SHIP-SIDE MITIGATION IS REQUIRED OR PERMITTED. CLOSE-PATH CONTRACT: this manifest intentionally lists its covering feature, which is the FULLY-COVERED ROOT exception to the task-only rule in docs/compound/097-S-shipment-task-only-safe-close.md - see the "Reconciliation - the FULLY-COVERED ROOT exception" section there. The task-only rule targets PARTIAL-feature shipments; task-only membership does NOT make this topology safe (ARM A orphaned 14/14 tasks). Close with a single `backlogit shipment ship`. Expected `ShipShipment` result: `returned_ids: []`; `archived_ids` = the six tasks + `119-F` + `128-S`.

SCOPE: the supervision core library (P0) — `process.py` / `process_pty.py` behind one `ChildProcess` Protocol, `session.py` state machine, `events.py` typed bus, `journal.py` redacted append-only journal, `recovery.py` cancellation / bounded restart budget / resume. UNWIRED (H10.2): nothing here is reachable from `start.ps1`, `start.sh`, or the CLI, so this shipment is still zero observable behavior change.

RESOLUTION - ALL GATES ON THIS SHIPMENT ARE CLOSED (2026-08-11, operator rulings accepted). F18 + F22 + F23 (RULING 1 - explicit DRAINING state and cancellation invariant): 119.003-T now declares DRAINING the SOLE TERMINAL GATEWAY - every path from any post-LOCKING phase to any terminal state routes through it, there is no CANCELLING -> EXITED edge and no direct failure edge to FAILED, DRAINING always releases the lock idempotently, and cancellation is legal from EVERY post-LOCKING phase. A mandatory GRAPH-PROPERTY test replaces hand-enumerated path lists, because the defect being prevented is an edge nobody thought to enumerate. 119.006-T's 'cancel during launch' case is consequently SATISFIABLE, and every cancellation and failure test now asserts the lock was released exactly once. F19 (RULING 2 - contracts in the shared supervisor core): the event catalog moved UP into 118.003-T contracts.py; 119.003-T emits SessionPhaseChanged against that catalog without importing the bus, and 119.004-T's dependency on 119.003-T was REMOVED and replaced with a dependency on 118.003-T, eliminating the cycle. F24 (RULING 6 - core owns ignore-rule behavior): 119.005-T no longer references a nonexistent gitignore template; journal.py now idempotently ensures the ignore file itself at journal-root creation, with a test asserting a fresh session directory is ACTUALLY git-ignored, so the H6 containment claim is enforced rather than asserted. F28 (RULING 10 - behavioral enforcement): 119.004-T's lexical denylist is demoted to a fast pre-filter and replaced by a sys.addaudithook socket interception, with a MANDATORY POSITIVE CONTROL proving the hook catches socket.create_server, socketserver.TCPServer, asyncio.start_server and http.server.HTTPServer - restoring the strength of the control that discharges cycle-1 P0 F2. F29 (RULING 11 - preserve inherited TTY/PTY): InheritStdioChildProcess is now the DEFAULT backend and terminal attachment is the contract being migrated; PipeChildProcess is retained for tests and non-interactive runs only; 119.002-T now degrades to INHERITED STDIO rather than to pipes when the PTY extra is unavailable, so a missing extra costs output capture but never terminal attachment; and 119.005-T journals an explicit ChildOutputUnavailable(reason=inherited-stdio) marker instead of silently emitting an empty stream. NO P0 OR P1 REMAINS OPEN AGAINST 128-S.

GATE STATE REVERTED 2026-08-11: the Cycle-16 GATE-CLEAR declaration is WITHDRAWN. The confirmatory current-HEAD Copilot review of 90a011c5 returned three surviving P1s (F30, F31, F32/F33) - see docs/reviews/2026-08-09-copilot-cli-supervisor-control-plane-review.md. F31's behavioural-enforcement sibling and the DRAINING invariant remain applied here, but the gate map they belonged to is withdrawn pending F30/F31/F32-F33. Stage HALTED per the operator's one-bounded-pass standing instruction and attempted no remediation. NOT gate-clear.

GATE STATE 2026-08-11 (Cycle 18): the F30/F31/F32-F33 gate is CLEARED by three final operator rulings, validated structurally (verifier V13 quantified close-path predicate with negative controls; V14 approval-reachability from the runtime chain tail). No F30/F31/F32-F33 obligation lands in this shipment; it is cleared as part of the program-wide gate. All three findings are RESOLVED; 0 unresolved P0 / 0 unresolved P1. GATE-CLEAR IS NOT AN INSTRUCTION TO CLAIM - claiming remains Ship's decision under Ship's own Role Boundary.
