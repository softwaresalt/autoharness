---
archived_from: .backlogit/queue/036.006-T.md
artifact_type: task
commit: 8cd4fcd70bf62db95d58a34613856e630c4ad8c6
created_at: 2026-05-17T15:03:38.0305043-07:00
id: 036.006-T
labels:
    - agent-engram
    - install-harness
    - overlay-contract
    - skill-authoring
parent_id: 036-F
priority: medium
status: archived
title: 'WS-F: Harden agent-engram overlay contract in install-harness SKILL.md'
updated_at: 2026-05-17T17:12:41.2134722-07:00
---

## What

The install-harness SKILL.md currently has agent-engram referenced in variable tables and a partial overlay target map example (line ~580), but the formal overlay contract in Step 1.3b is not fully defined — it is shown as a commented stub/example in the manifest output section (lines ~1049-1058).

Promote the agent-engram overlay entry from example/stub to a fully specified, first-class overlay contract alongside agent-intercom and backlogit.

### Required contract elements (per harness-architecture.instructions.md)

1. **Eligibility signals** (when to recommend agent-engram):
   - `.mcp.json` (root), `.vscode/mcp.json`, or `.cursor/mcp.json` references `engram` server
   - `.engram/config.toml`, `.engram/registry.yaml`, or `.engram/code-graph/` present
   - `AGENTS.md` or `copilot-instructions.md` contains `<!-- engram:start -->` marker or Engram tool names

2. **Recommendation logic**: recommend when `mcp_configured: true` OR `detected: true` in agent_engram profile section

3. **Overlay targets**:
   - `foundation-docs` (AGENTS.md, copilot-instructions.md)
   - `agent-engram.instructions.md` (primary instruction file)
   - `pipeline-agents` (stage.agent.md, ship.agent.md — session-start binding, pre-work search)
   - `analysis-heavy-workflows` (research/planning/build skills — impact_analysis, unified_search guidance)

4. **Behavior deltas**:
   - Session start: daemon readiness check (`get_workspace_status`)
   - Before planning: `unified_search` / `impact_analysis` runs before impl-plan
   - Before build: `impact_analysis` on primary scope before code changes
   - Fallback: ENGRAM_DEGRADED mode (file-based tools) when daemon unavailable

5. **Verification checks**:
   - `agent-engram.instructions.md` installed at `.github/instructions/`
   - stage.agent.md and ship.agent.md contain Engram session-start checks
   - copilot-instructions.md engram block cross-references installed instructions
   - No unresolved `{{VARIABLE}}` in agent-engram.instructions.md

6. **Tuning drift checks**:
   - `agent-engram.instructions.md` checksum vs. template checksum
   - stage/ship agent Engram sections present (text search for `get_workspace_status`)
   - copilot-instructions.md contains `engram:start` marker

## Acceptance Criteria

- install-harness SKILL.md Step 1.3b contains a non-stub agent-engram overlay entry with all six contract elements
- The manifest output example section (line ~1049) is updated to reflect a real (non-commented) agent-engram entry
- No template variable `{{AGENT_ENGRAM_ENABLED}}` goes undefined when the pack is active

## Constraint

Single file, targeted section additions. ~45–60 min. Width-isolated to install-harness SKILL.md only.