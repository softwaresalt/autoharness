---
archived_from: .backlogit/queue/038.004-T.md
artifact_type: task
commit: 8cd4fcd70bf62db95d58a34613856e630c4ad8c6
created_at: 2026-05-17T15:10:37.3397754-07:00
id: 038.004-T
labels:
    - graphtor-docs
    - install-harness
    - overlay-contract
    - skill-authoring
parent_id: 038-F
priority: medium
status: archived
title: 'GD-D: Harden graphtor-docs overlay contract in install-harness SKILL.md'
updated_at: 2026-05-17T17:12:43.9098045-07:00
---

## What

Add a fully specified, first-class graphtor-docs overlay contract to `install-harness SKILL.md` Step 1.3b, alongside the agent-engram (WS-F) and agent-intercom (IC-D) contracts. All three must be present for 034-S to be complete.

### Required contract elements (per harness-architecture.instructions.md)

1. **Eligibility signals**:
   - `.mcp.json`, `.vscode/mcp.json`, `.cursor/mcp.json` references `graphtor` or `graphtor-docs` server
   - `.graphtor/` directory or `sources.yaml` / `config.toml` present
   - `AGENTS.md` or agent files reference `graphtor-docs` tool names (search_local_docs, search_semantic, research_topic)

2. **Recommendation logic**: Recommend when `mcp_configured: true` OR `detected: true` in `graphtor_docs` profile section

3. **Overlay targets**:
   - `graphtor-docs.instructions.md` (primary instruction file, two template vars to resolve)
   - `pipeline-agents` (stage.agent.md, ship.agent.md — session-start server check, pre-planning/build doc search)
   - `research-skills` (any research or learnings-retrieval skill — prefer graphtor-docs for doc lookup)
   - `workspace-profile` (requires `graphtor_docs` section with `sources_path` and `binary_path`)

4. **Behavior deltas**:
   - Session start: `get_status` check (once per session, not per tool call)
   - Before planning: `research_topic` / `search_local_docs` before impl-plan
   - Before build: `search_local_docs` / `search_semantic` for doc questions
   - Routing split (when Engram also active): Engram for code/impact, graphtor-docs for documentation
   - Fallback: `GRAPHTOR_UNAVAILABLE` — file-based fallback (grep, direct file read)

5. **Verification checks**:
   - `graphtor-docs.instructions.md` installed at `.github/instructions/`
   - Zero `{{VARIABLE}}` placeholders in installed file (verify both `GRAPHTOR_SOURCES_PATH` and `GRAPHTOR_BINARY_PATH` resolved)
   - stage.agent.md and ship.agent.md contain `GRAPHTOR_UNAVAILABLE` references
   - `workspace-profile.yaml` has `graphtor_docs` section

6. **Tuning drift checks**:
   - `graphtor-docs.instructions.md` checksum vs. expected (note: resolved vars mean checksum is workspace-specific, not template-identical — tuner must compare rendered content, not raw template)
   - stage/ship agent graphtor-docs sections present (text search for `GRAPHTOR_UNAVAILABLE`)
   - workspace-profile.yaml contains `graphtor_docs` section

### Variable resolution documentation

Document how `{{GRAPHTOR_SOURCES_PATH}}` and `{{GRAPHTOR_BINARY_PATH}}` are resolved during installation: discovery-first (check known paths), then conventional defaults. These are the only two variable-bearing templates in the current instruction set — document the pattern for future template authors.

## Acceptance Criteria

- install-harness SKILL.md Step 1.3b contains a non-stub graphtor-docs overlay entry with all six contract elements
- Variable resolution method for both graphtor-docs template vars is documented
- Tuner drift check note explains why checksum comparison differs from template-identical packs
- No template variable goes undefined when the pack is active

## Constraint

Single file, targeted section additions. ~45–60 min. Width-isolated to install-harness SKILL.md only.

## Dependency

GD-C should be complete first (agents must be woven before documenting the weaving pattern for future installs).