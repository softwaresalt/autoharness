---
archived_from: .backlogit/queue/145.002-T.md
archived_status: done
artifact_type: task
commit: fed1319bac9e1ac3c2f2eeb448390fbfc192f155
created_at: 2026-08-22T08:19:49.1311806Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 145.001-T
id: 145.002-T
labels:
    - conditional
    - remediation
    - test-order
    - mechanism-b
parent_id: 145-F
priority: medium
references:
    - docs/plans/2026-08-22-git-config-env-containment-plan.md
    - docs/reviews/2026-08-22-git-config-env-containment-review.md
    - tests/test_gates_topology.py
status: archived
title: Remediate a surviving mechanism-B order dependence, or close as an evidenced no-op
updated_at: 2026-08-23T00:34:38.5402903Z
---

**CONDITIONAL. Content is determined by `145.001-T`'s recorded terminal disposition.**

Plan: `docs/plans/2026-08-22-git-config-env-containment-plan.md` Task 9. Amendments A10, R3, R7 BINDING.

## Branch on `145.001-T`'s disposition

### If `SUBSUMED`

Close as a **recorded no-op**, citing `145.001-T`'s evidence: the standalone run, all five polluter -> victim pairings, AND the A10 negative control. **No source edit.** The closure record must state explicitly that mechanism B was resolved as a downstream consequence of `144.003-T`'s migration and was verified independently, not assumed.

### If `SURVIVES` or `INCONCLUSIVE-VACUOUS`

Isolate the residual shared state from `145.001-T`'s minimal reproducing pair and remove it AT ITS SOURCE.

Same constraints as `144.003-T`:
- ZERO assertion edits (AIG-1 assertion-inventory equality required).
- No `skipTest`, no `expectedFailure`, no test deletion, no test reordering, no test renaming.
- No masking, absorbing, or silencing of the failure.
- No mutation of global/system git config.

Proof required: re-run the standalone file AND the full canonical suite; both green on Windows and Linux/CI.

## R7 (BINDING) — 2-hour bound, return blocked rather than expand

This task carries `complexity: high` because its content is UNKNOWN until `145.001-T` reports. Its mandatory de-risking predecessor is `145.001-T` (measurement before remediation), which satisfies the two-axis granularity gate.

If, once `145.001-T` reports `SURVIVES`, the remediation is assessed to EXCEED the 2-hour rule, this task MUST **return blocked** with the measured scope for Stage re-decomposition. It MUST NOT expand in place, and it MUST NOT be closed by skipping, reordering, or deleting a test.

## R3 (BINDING)

`pytest` MUST NOT be substituted for any measurement or proof.

## Acceptance

Either a recorded no-op with cited A10 evidence, or a source-level fix with AIG-1 equality plus green standalone and full canonical runs on both platforms — or a return-blocked with measured scope under R7.

## Disposition (Recorded) — Closed as a recorded no-op

`145.001-T` recorded a terminal disposition of **`SUBSUMED`** (see
`.backlogit/archive/145.001-T.md`), meeting all three A10 conditions:

1. standalone `tests/test_gates_topology.py` green (104/104, `OK`);
2. all five named polluter -> `FilesystemTopologyReadersTests.test_empty_queue_and_archive_dirs_pass_as_zero_shipments`
   pairings run individually and green;
3. a reverted-checkout negative control (throwaway, never committed) that
   reproduced the original failure for all five pairings when `144.003-T`'s
   migration was locally undone for exactly those five methods, proving the
   measurement was not vacuous.

Per this task's own branch instructions ("If `SUBSUMED`: close as a
recorded no-op... No source edit"), this task closes with **no source
change**. Mechanism B (the `BranchOwnershipTests` intra-file order
dependence originally measured standalone by `141.004-T`) was resolved as
a **downstream, independently-verified consequence** of `144.003-T`'s
migration of the five polluters from the destructive
`patch.dict('os.environ', {...}[, clear=False])` + manual
`_os.environ.pop(...)` idiom to `patched_environ`'s restore-by-diff
semantics (152-S/144-F, mechanism A) — this conclusion is **verified**,
not assumed, by the reverted-checkout negative control reproducing the
original failure and the restored code eliminating it again.

No remediation was required, authorized, or performed. `145-F`'s
acceptance criteria (terminal disposition + no-op closure with cited
evidence) are satisfied by `145.001-T`'s evidence alone.