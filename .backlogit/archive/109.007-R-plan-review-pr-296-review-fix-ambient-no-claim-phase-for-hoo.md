---
archived_from: .backlogit/queue/109.007-R-plan-review-pr-296-review-fix-ambient-no-claim-phase-for-hoo.md
archived_status: done
artifact_type: review
created_at: 2026-08-05T04:31:23.1863285Z
id: 109.007-R
labels:
    - plan-review
    - pr-296
    - topology
    - ambient-phase
    - a2
    - p-006
parent_id: 109-F
status: archived
title: 'Plan-review: PR #296 review-fix — ambient no-claim phase for hook/CI A2 — PASS'
updated_at: 2026-08-05T04:31:23.1863285Z
---



<!-- BEGIN:decisions -->
APPROVE for Orchestrator independent current-HEAD review, then reply + resolve thread PRRT_kwDORzpWpM6WifnT via gh API and commit/push (Stage does not commit/push, claim shipments, or perform PR/GitHub actions). No hierarchy change: task-only manifests remain 114-S=10, 115-S=7, 116-S=3; dependency chain 114-S -> 115-S -> 116-S intact. Handoff token remains 114-S. All edited tasks retain both size and complexity unchanged. Validation contract satisfied: no-target zero-active hook/CI PASSES; one active resolved target PASSES; one active unresolved/mismatched or 2+ active BLOCKS; missing target does NOT skip target-independent topology checks. This review supersedes only the ambient/default-phase statements addressed above; it does not authorize implementation, build, PR, or shipment claim (P-001/P-009/P-015/P-016 preserved).
<!-- END:decisions -->

<!-- BEGIN:findings -->
Single hosted thread, concrete fix:

PRRT_kwDORzpWpM6WifnT (comment 3717880550, 109.002-T:25) RESOLVED — introduced a distinct explicit `ambient` phase (fourth `--phase` value) with AT-MOST-ONE-active-and-target-when-present semantics and made it the deterministic hook/CI resolution, REPLACING the former `lifecycle` default. Ambient: zero active -> pass (no-claim non-blocking); exactly-one active AND equals resolved target -> pass; exactly-one active NOT the resolved target OR one active with no resolvable target -> blocked; two-or-more active -> blocked; no resolvable target still runs target-INDEPENDENT checks (detect-before scan + at-most-one count) fail-closed while target-DEPENDENT readiness/ownership are skipped. `pre_claim` NOT reused (it would reject a valid single active ambient target); `lifecycle` NOT reused (it rejects zero-active ordinary work). Ambient distinct from lifecycle at the zero-active row.

Shipment-scoped agent phases UNCHANGED: pre_claim=ZERO-active; post_claim/lifecycle=exactly-one-same-target. Agent shipment-scoped mode requires one of pre_claim|post_claim|lifecycle and rejects the non-scoped `ambient` with exit 2.

Aligned artifacts (task-only; no membership/dependency/status/size/complexity change): 109.002-T (A1 parser/domain: enum -> <pre_claim|post_claim|lifecycle|ambient>, hook/CI default resolves ambient, agent mode rejects ambient exit 2); 109.005-T (A2 invariant: ambient bullet + matrix row + description + impl-notes); 109.006-T (A6 tests: ambient matrix row, ambient-vs-lifecycle zero-active assertion, `--phase ambient` rejected in agent scoped mode, no-target ambient pass/block cases); 109.007-T + 109.008-T (B1/B2 hooks: resolve --phase ambient, zero-active non-blocking); 109.011-T (C1 CI: resolve --phase ambient, zero-active ordinary CI non-blocking); 109.010-T (B4 docs: document ambient phase + deterministic resolution + zero-active distinction); 109-F (description invariant (1) + DoD flag/test bullets include ambient).

RECONCILIATION — grep across all 109-F queue artifacts confirms no remaining `default lifecycle` hook/CI resolution and no remaining statement that ambient no-claim work is gated by exactly-one-active. Archived historical review records (109.001-R..109.006-R) left immutable; agent-scoped Ship/Orchestrator ordering (109.017-T/109.018-T) untouched.

RESIDUAL-FIX ADDENDUM (independent review BLOCKED P0=0/P1=2/P2=1/P3=2) — a follow-up Stage residual-fix pass corrected wording defects this reconciliation had missed, without any membership/dependency/status/size/complexity change (manifests remain task-only 114-S=10 / 115-S=7 / 116-S=3): (P1) 109.011-T C1 CI contract no longer claims branch-to-claimed-shipment ownership is "always enforced" — it now states the target-INDEPENDENT invariants (at-most-one-active count + detect-before scan) always run fail-closed while the target-DEPENDENT ownership/readiness checks run ONLY when a target resolves, so a zero-active/no-target CI run PASSES and 2+ active BLOCKS; (P1) 109.002-T A1 scopes the explicit `--phase`-only source to shipment-scoped agent mode (hook/ci deterministically resolves an OMITTED phase to `ambient`), removing the contradiction with the deterministic hook/ci resolution; (P2) replaced the malformed schema-v0/empty/zero-timestamp checkpoint-20260805-043205.json with a supported schema-v1 resolved checkpoint (checkpoint-20260805-044138.json: agent=stage, session_id, phase=review-fix, feature_id=109-F); (P3) 109.005-T A2 description enum now includes `ambient` (pre_claim|post_claim|lifecycle|ambient); (P3) Stage memory now references review `109.007-R` (was the stale `109.006-R` self-reference). Post-fix consistency grep confirms every `--phase` enum includes `ambient`, no unscoped "supplied ONLY by --phase" / "always enforced ownership" statement remains, and the only surviving `lifecycle`-default mentions are the explanatory "REPLACES the former lifecycle default" notes. Stage boundary preserved: no source/templates/config, git, PR, build, branch/worktree, claim, commit, or push; `.backlogit/config.yaml`, `resolve_thread.graphql`, `threads_query.graphql` untouched.
<!-- END:findings -->

<!-- BEGIN:summary -->
Bounded Stage review-fix cycle for PR #296 at HEAD f2c819daa8c5e11085d324763d07c5821c514792. Scope: unresolved Copilot thread PRRT_kwDORzpWpM6WifnT / comment 3717880550 on .backlogit/queue/109.002-T.md:25 — ambient no-claim hook/CI runs defaulting to `--phase lifecycle` (exactly-one-active-and-target) would BLOCK ordinary zero-active commits/CI even though the no-target A2 path still runs, contradicting the hook tasks' promise that zero claims is non-blocking. Operator removed the review-cycle limit for this session. Planning/backlog-artifact-only; no source/template/config mutation, no commit/push, no shipment claim, no branch/worktree, no PR/GitHub action. Verdict: PASS.

PLAN-HARDEN (P-006): moderate blast radius — a single new deterministic phase value in the A2 contract propagated across the CLI/domain parser, the A2 invariant, tests, both hook families, CI, docs, and the feature DoD. Hardened by (a) confining the change to a new fourth `--phase` value rather than mutating the existing shipment-scoped phase semantics, (b) preserving agent shipment-scoped phases unchanged and rejecting the non-scoped `ambient` in agent mode with exit 2, and (c) an exhaustive phase x count matrix asserting ambient is DISTINCT from lifecycle only at the zero-active row. No backlogit source semantics depended on (phase is a gate-domain-input concept only).
<!-- END:summary -->