---
artifact_type: task
created_at: 2026-08-17T23:34:15.8202406Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 129.003-T
labels:
    - runbook
    - docs
    - rollback
    - BED0DDED
parent_id: 129-F
priority: medium
references:
    - docs/plans/2026-08-17-backlogit-self-migration-plan.md
    - docs/plans/2026-08-17-backlogit-self-migration-hardening.md
status: rejected
title: Author migration runbook and rollback/recovery procedure
updated_at: 2026-08-18T04:58:24.1353278Z
---

Create docs/runbooks/2026-08-17-backlogit-storage-root-migration.md containing: the ordered command sequence; the P1-P7 preconditions table; the exclusive-open (FileShare.None) lock probe with bounded retry; the post-migration single-root assertion; the parity-verification method (Markdown/JSONL inventory, NOT db bytes -- see H13); the E9/H6 residue prohibition on checking out any pre-migration ref after Commit B, including the mandated "git fetch origin && git checkout -B main origin/main" main-sync form; and the BACKLOGIT_WORKSPACE_DIR escape hatch (empirically verified this session: with both roots populated, backlogit errors "ambiguous workspace root"; with BACKLOGIT_WORKSPACE_DIR=.backlog or .backlogit it resolves cleanly -- and src/autoharness/backlog_root.py returns on the validated override at line 126, BEFORE the ambiguity check at line 136). CORRECTED 2026-08-17: the runbook MUST document the CONTAINED IN-REPO backup (hardening H4) as the PRIMARY recovery mechanism -- the canonical backup root .copilot\session-state\7ced3fcb-faba-47fb-81f9-09e0670a393f\files\backlog-premigration-<UTC-timestamp>\, the G1-G6 containment gates and their recorded results, the contents-not-directory copy rule that keeps G5 satisfied, the bounded file-count/inventory verification method, the git clean -x/-X prohibition for the whole backup window, and the operator-gated disposal rule (H15). It MUST NOT reference an out-of-repository or $env:TEMP backup: writing outside the working directory is a Constitution Principle IV / CLI-containment violation. backlogit migrate --rollback remains SECONDARY, UNVERIFIED and operator-gated. The runbook's "if both roots exist" recovery section MUST reproduce the H16 non-destructive contract verbatim in effect: (1) set a VERIFIED BACKLOGIT_WORKSPACE_DIR override (exactly .backlog or .backlogit, existing, a real directory, not a symlink/reparse point) to restore tooling without touching the filesystem; (2) PRESERVE both roots and the backup -- delete nothing, restore nothing, move nothing; (3) record evidence (per-root inventories, which root holds the authoritative config.yaml, git status --porcelain, resolver output, backup path and verification result, timestamps); (4) HALT and request EXPLICIT OPERATOR APPROVAL for any deletion or restoration. It MUST state the absolute prohibitions: never auto-delete a root, never run a broad git checkout -- . or any whole-worktree reset, never guess which root is authoritative. It MUST record WHY the former automatic rollback was withdrawn: because .backlogit is git-tracked (E1, 1613 files), git checkout -- . after the rename would restore .backlogit from HEAD while .backlog still exists on disk, MANUFACTURING the exact dual-root state H10 exists to prevent -- the prescribed recovery would have caused the failure it was meant to cure. It MUST state that a future approved rollback targets an ENUMERATED PATHSPEC LIST for tracked files plus a named source/destination copy-back for the backlog root, and that pathspec-less and wildcard forms remain forbidden even after approval. THIS DOCUMENT MUST BE COMMITTED BEFORE 129.006-T EXECUTES -- a rollback procedure authored after the irreversible step is not a rollback procedure. ACCEPTANCE: runbook committed; contains an explicit "if both roots exist" preserve-and-halt recovery section; no out-of-repository backup reference anywhere; markdownlint-clean with valid frontmatter. Plan T003; hardening H4, H9, H10, H15, H16.