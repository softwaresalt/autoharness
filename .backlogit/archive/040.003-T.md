---
archived_from: .backlogit/queue/040.003-T.md
artifact_type: task
commit: 77fd377e7eb2390c05c0a7916e92ea9c5cb3fefa
created_at: 2026-05-17T19:13:19.2746266-07:00
id: 040.003-T
labels:
    - chore
    - backlog-hygiene
    - pr89-thread-3
parent_id: 040-F
priority: medium
status: archived
title: 'Remove stale queue copy: delete .backlogit/queue/035-S.md now that archive copy is canonical'
updated_at: 2026-05-17T21:56:30.8766011-07:00
---

## Context

PR #89 Copilot review thread (unresolved) — file: `.backlogit/queue/035-S.md`

Both `.backlogit/archive/035-S.md` and `.backlogit/queue/035-S.md` currently exist with identical content (same frontmatter, same Release Record body). The archive copy is the canonical record for a shipped-and-archived shipment. The queue copy is a stale artifact that was never removed after archiving.

Keeping the queue copy creates ambiguity: tooling that scans `.backlogit/queue/` would incorrectly surface 035-S as an active queue item.

## Fix

Delete the file `.backlogit/queue/035-S.md`.

> **Note**: This is a destructive file operation. Per P-005 / Primitive 5 guardrails, Ship must confirm this is the intended action before executing. The operator has authorised this deletion as part of this backlog task. Ship should not prompt for additional approval beyond the normal PR review gate — the task description constitutes the authorisation record.

The queue directory may become empty after this deletion. If `.backlogit/queue/` becomes empty and the tooling requires the directory to exist, Ship should add a `.gitkeep` file (empty, no content) to preserve the directory. If other queue items exist, no `.gitkeep` is needed.

## Verification After Deletion

Run: `Get-ChildItem .backlogit\queue\` (or equivalent) and confirm `035-S.md` is absent. Confirm `.backlogit\archive\035-S.md` still exists and is correct (Task 040.002-T must be completed before this task so the archive copy is already fixed when the queue copy is removed).

## Dependency

This task depends on **040.002-T** (fix the archive copy first). Complete 040.002-T before deleting the queue copy so the archive is the fully-correct record at the moment of deletion.

## PR #89 Thread Reply (post-fix)

After pushing the fix commit (which includes the deletion), reply to the original PR #89 Copilot thread on the queue file:

> "Resolved in [PR link] — `.backlogit/queue/035-S.md` deleted; `.backlogit/archive/035-S.md` is now the sole canonical record for shipment 035-S."

Then resolve the thread via GraphQL `resolveReviewThread`.

## Acceptance Criteria

- [ ] `.backlogit/queue/035-S.md` does not exist in the repo after the fix PR merges
- [ ] `.backlogit/archive/035-S.md` still present and correct
- [ ] `.backlogit/queue/` directory still present (`.gitkeep` added if directory would otherwise be empty)
- [ ] PR #89 thread replied to and resolved