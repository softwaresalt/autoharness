---
archived_from: .backlogit/queue/138-F.md
archived_status: done
artifact_type: feature
commit: 77ee301a2cb91cda5c244d0d52363a8d95277dc7
created_at: 2026-08-21T03:11:08.5275077Z
custom_fields:
    harness_status: pending
    source_deliberation_id: 021-DL
    source_stash_id: 7852CE0D
id: 138-F
labels:
    - bug
    - tests
    - docs
    - p-021
    - baseline-red
    - prerequisite
    - gate-atomic
priority: high
status: archived
title: 'Gate-atomic baseline repair: malformed plan frontmatter (Gate 1) + P-021 contract-test load of the archived 019-DL artifact (pytest)'
updated_at: 2026-08-21T09:43:24.2746949Z
---

GATE-ATOMIC BASELINE REPAIR. Covering feature for the single first-executable task in the three-shipment chain. It repairs BOTH baseline blockers that keep Ship's mandatory gates red, atomically, so that an executable first task exists at all.

SCOPE EXPANSION (2026-08-20, Stage correction): this feature originally owned only the P-021 contract-test path defect. It now also owns the malformed-frontmatter one-line repair formerly held by 136.001-T in 144-S. Rationale below.

BLOCKER 1 - Ship Gate 1 (YAML frontmatter validity)
docs/plans/2026-08-02-structural-navigation-benchmark-suite-plan.md line 12 carries an unquoted YAML scalar containing ': ' (blast_radius: elevated (result-integrity + multi-family: eval code / tests / docs / fixtures)). mdfront.Decode fails; because `backlogit docs lint` aborts on the first decode error, the entire repo-wide lint emits no report.

BLOCKER 2 - configured pytest suite
tests/test_scope_containment_boundary_contract.py:127 and tests/test_scope_containment_semantics_contract.py:137 hard-load the 019-DL deliberation artifact from `.backlogit/queue/019-DL.md`, a path that no longer exists because the artifact was archived (merge f72109e2, PR #374) to `.backlogit/archive/019-DL.md`. The read happens in `setUpClass`, so the FileNotFoundError collapses both modules' entire test classes.

WHY THEY ARE ONE FEATURE / ONE TASK - the circular mandatory-gate dependency
Ship evaluates ALL mandatory gates before completing EVERY task. Before this correction:
* 146-S (this feature) repaired only BLOCKER 2, so its own first task would still complete with Gate 1 red.
* 144-S owned BLOCKER 1 (136.001-T) but was blocked by 146-S, and running 144-S first would still hit the BLOCKER 2 pytest collapse.
That is a genuine cycle in the GATE dependency graph even though the recorded `blocks` edges were acyclic: there was NO executable first task anywhere in the chain. Repairing both blockers in one atomic task is the only structure that yields a first task able to leave every mandatory gate green. Splitting them again re-creates the deadlock.

BOTH PREVIOUSLY-AUTHORIZED SCOPES ARE PRESERVED IN FULL. Neither the known scalar fix (from 395EBE60 / 136.001-T / docs/plans/2026-08-20-docline-lint-restoration-plan.md Task 1) nor the test-path fix (from 7852CE0D / 021-DL) is dropped, weakened, or deferred. 136.001-T is SUPERSEDED BY 138.001-T, stamped and archived (not deleted), and removed from 144-S membership.

Chosen direction for BLOCKER 2 (021-DL Option B): host a lifecycle-stable resolver in `tests/test_scope_containment_policy_contract.py` (the module both siblings already import from, preserving the one-way 011->013->012 allocation edge), delegating root resolution to the already-shipped `autoharness.backlog_root.resolve_backlog_root` with `env={}` isolation, probing `queue/<id>.md` then `archive/<id>.md`. Add a behavioural regression assertion (resolves, resolves uniquely, retains C4/C5 AMENDMENT markers) and a structural guard (no lifecycle-volatile queue-anchored path outside the resolver).

FILE BUDGET: exactly four files - three `tests/` modules plus ONE line of the 2026-08-02 benchmark plan.

Explicitly NOT in scope: the repo-wide docs/ frontmatter sweep (remains 136.002-T in 144-S); the frontmatter regression guard (remains 136.003-T in 144-S); any generic backlog-path abstraction in `src/`; any change to `resolve_backlog_root`; any change to a B1-B18 assertion's subject or strength; any other file under `docs/`; anything under `src/`, `schemas/`, `templates/`, or `.github/`.

P-021 C1: the BLOCKER 2 defect fails same-contract-surface against both pre-existing shipments (144-S/136-F owns the `backlogit docs lint` + plan-frontmatter surface; 145-S/137-F owns the agent-template paired-edit + stash-archive naming surface), so it was captured as stash 7852CE0D, deliberated as 021-DL, and harvested into its own feature and prerequisite shipment rather than absorbed. The subsequent absorption of BLOCKER 1 into this feature is the REVERSE direction and is authorized: it moves an ALREADY-STAGED, already-reviewed, already-authorized unit of work into the earliest gate-atomic position. It creates no new scope, so it is not a C1 expansion and requires no deferred entry.

DELIBERATION LIFECYCLE: 021-DL is complete, fully harvested here, and was ARCHIVED by Stage on 2026-08-20 to `.backlogit/archive/021-DL.md` (provenance, capture text, amendments and timestamps preserved unmodified). `custom_fields.source_deliberation_id` is RETAINED on this feature by the established idempotent convention: it points at an ALREADY-ARCHIVED source, and the Ship cleanup step ("if it exists and is not already archived, archive it; if already archived or not found, skip and log") is therefore a NO-OP. Retaining the field preserves the provenance link; it does not schedule work. This matters because the installed dogfood `.github/agents/_ship.agent.md` does not currently carry that cleanup step at all (it exists only in `templates/agents/_ship.agent.md.tmpl`), and this shipment executes BEFORE the 145-S template/dogfood migration, so no later migration could have performed the archival.

Artifacts:
* Stash: 7852CE0D (BLOCKER 2), 395EBE60 (BLOCKER 1, via superseded 136.001-T)
* Deliberations: 021-DL (`.backlogit/archive/021-DL.md`, archived 2026-08-20); `docs/decisions/2026-08-20-docline-lint-hard-abort-malformed-frontmatter-deliberation.md`
* Plans: `docs/plans/2026-08-20-p021-contract-test-deliberation-path-resolution-plan.md` (primary; requires_plan_hardening: no); `docs/plans/2026-08-20-docline-lint-restoration-plan.md` (Task 1 relocated here by Amendment R1)
* Review: `docs/reviews/2026-08-20-p021-contract-test-deliberation-path-resolution-review.md` (PASS, 0 unresolved P0/P1, plus scope-expansion addendum)
* Superseded: 136.001-T -> 138.001-T

Ordering: this feature's shipment is an execution-blocking prerequisite and now contains the only gate-green-capable first task. Deterministic order 146-S -> 144-S -> 145-S. 144-S now begins with the docs/ sweep (136.002-T).