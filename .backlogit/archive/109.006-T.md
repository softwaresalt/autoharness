---
artifact_type: task
commit: d372e48
created_at: 2026-08-04T23:38:35.4486795Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-two-axis-v1
    size_source: agent
dependencies:
    - 109.003-T
    - 109.009-T
    - 109.004-T
    - 109.001-T
    - 109.005-T
id: 109.006-T
parent_id: 109-F
priority: medium
status: done
title: 'A6: deterministic unit tests for the topology gate core'
updated_at: 2026-08-05T05:52:25.6957272Z
---



<!-- BEGIN:acceptance-criteria -->
* Each check has pass + fail-closed cases.
* A2 PHASE-AWARE matrix asserted, driven by the EXPLICIT `--phase` flag (never inferred): PRE-CLAIM (`--phase pre_claim`) {0 active -> pass, 1 -> blocked, 2+ -> blocked}; POST-CLAIM (`--phase post_claim` AND `--phase lifecycle`, asserted equivalent) {0 -> blocked, exactly-1-and-target -> pass, exactly-1-not-target -> blocked, 2+ -> blocked}; AMBIENT (deterministically resolved for hook/ci, `--phase ambient`) {0 -> pass, exactly-1-and-target -> pass, exactly-1-not-target -> blocked, exactly-1-no-resolvable-target -> blocked, 2+ -> blocked}, with an explicit assertion that AMBIENT is DISTINCT from lifecycle at the zero-active row (ambient passes, lifecycle blocks).
* Post-claim GLOBAL re-verify RACE/ROLLBACK cases: (i) target becomes sole-active -> proceed; (ii) target still queued -> ONE retry that FIRST re-runs full topology + A2 pre-claim ZERO-active precondition + detect-before scan before reclaim; (iii) another active shipment appears post-claim (exactly-one-not-target or two-or-more) or SHIPMENT_STATE_INCONSISTENT -> CLAIM_VERIFY_FAILED, no reclaim.
* Readiness matrix is explicit: live `shipped` + complete closure passes; archived provenance `archived_status: shipped|done` + complete closure passes; `abandoned`, live queued/active/blocked, archived active/queued/blocked/abandoned, generic archived state, missing/unknown provenance, or missing/incomplete closure including P-020 all block.
* Stage spike/research worktree exception path is tested.
* Missing `--shipment` in agent shipment-scoped mode -> exit 2 (invalid) is asserted.
* Missing/empty/invalid `--phase` in agent shipment-scoped mode -> exit 2 (invalid) is asserted, INCLUDING an explicit assertion that the non-scoped `--phase ambient` is rejected as invalid (exit 2) in agent shipment-scoped mode; the phase x count matrix is driven strictly by the explicit `--phase` flag (pre_claim vs post_claim/lifecycle vs ambient), never inferred from mode or claim state.
* Readiness/ownership evaluated against the SUPPLIED target shipment (parametrized: correct target passes; wrong/unready target blocks).
* Non-shipment hook/ci target-resolution: implicit resolution from the currently-claimed shipment is exercised; the ambient-only path (no resolvable target) still enforces A2/A3b fail-closed and skips readiness. The deterministic ambient phase resolves to `ambient` (NOT `lifecycle`), so a zero-active no-target hook/CI run PASSES (non-blocking) while a mismatched single active, a single active with no resolvable target, or 2+ active BLOCKS.
* No live backlogit or git mutation in tests.
* Tests run under the repo's canonical unittest gate.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
[Shipment A / core] Add deterministic, offline unit tests for the pipeline-topology gate core: all four checks (active-shipment, branch ownership, worktree uniqueness+exception, readiness), the A2 phase-aware pre-claim ZERO-active vs post-claim/lifecycle exactly-one-same-target vs ambient at-most-one-active-and-target-when-present matrix driven by the explicit `--phase` flag, the `--shipment` target contract AND the `--phase` contract (both required/fail-closed with exit 2 in agent scoped mode; deterministic ambient resolution in hook/ci mode), the detect-before and post-claim GLOBAL re-verify paths (including the race/rollback + pre-retry full-revalidation cases), exit-code contract, and the --force audit path. Use injected fake backlogit-state readers and fake git worktree/branch inputs.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Mirror gate.py run_fn injection for determinism (docs/compound/097-S-canonical-unittest-gate.md).
<!-- END:implementation-notes -->