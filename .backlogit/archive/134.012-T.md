---
archived_from: .backlogit/queue/134.012-T.md
archived_status: done
artifact_type: task
commit: e2af4dfe1b403b85cab7f237a4f7f9b621370d70
created_at: 2026-08-19T15:37:06.207302Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 1.0.0
    size_source: agent
dependencies:
    - 134.011-T
    - 134.013-T
id: 134.012-T
labels:
    - tests
    - contract
    - verification
    - regression
parent_id: 134-F
priority: medium
references:
    - tests/test_scope_containment_semantics_contract.py
    - tests/test_scope_containment_policy_contract.py
    - tests/test_circuit_breaker_policy_contract.py
status: archived
title: 'Contract test: P-021 behaviour-map allocation guards + C2/C3 capture-and-discharge semantics (threadless discharge, per-field IDs, single-write capture)'
updated_at: 2026-08-20T11:22:21.7136396Z
---

Add a semantic regression suite proving the P-021 C2/C3 behavioural invariants hold on every carrier, so each defect found in the prior fix cycles fails a distinct test.

<!-- BEGIN:acceptance-criteria -->
- [ ] New file tests/test_scope_containment_semantics_contract.py exists and runs under the canonical unittest gate, reusing `from autoharness.verify_workspace import _derive_template_variables, _render_template` and the LF-normalization approach from tests/test_circuit_breaker_policy_contract.py. This file is authored LAST of the three, because its RANGE-COMPLETENESS guard imports the `OWNED_BEHAVIOURS` constant from BOTH sibling modules (tests/test_scope_containment_policy_contract.py and tests/test_scope_containment_boundary_contract.py) and therefore requires both to exist. Neither sibling may import from this file: the allocation check flows one way only, which is what keeps the three-way guard acyclic.
- [ ] CARRIER-SET RESOLUTION rule: every criterion below names the BEHAVIOUR ID (B1-B18) it asserts, and resolves its carrier subset from the `CLAUSE -> BEHAVIOR -> CARRIER-SUBSET MAPPING` block in 134.011-T, which is the single source of truth for both presence and subsets. This task MUST NOT restate the matrix or hardcode a carrier list: a hardcoded list is what let the symmetric-guard coverage sit at two surfaces while the clause row named four. Where a subset is deliberately NARROWER than its clause row, the narrowing is declared in 134.011-T with its justification, and this suite inherits it rather than re-deriving it. Where a subset is DERIVED (B16 = `B7 INTERSECT B8`, B17 = `B6 UNION {007-fix-ci}`), the suite computes the derivation from the mapping rather than materializing the result, so a derived subset cannot drift from its parents.
- [ ] SUBSET-FIDELITY guard, spanning the COMPLETE authoritative range B1-B18: a test asserts that each behaviour subset this suite exercises matches the subset declared for that behaviour ID in 134.011-T, that every behaviour subset is a SUBSET of its clause row, and that every member of every clause row is covered by at least one behaviour (including declared cross-clause discharges such as 008's C5 carriage being asserted under B14). This is what keeps the three test files from drifting apart the way the three matrix copies did, and it must fail if a behaviour is asserted against a carrier the mapping excludes -- for example demanding threadless discharge (B8) from github-pr-automation, or thread-reply ordering (B7) from circuit-breaker, neither of which those surfaces can faithfully carry. Two declared exemptions from the clause-row subset check, and ONLY these two: B6 and B17, both of which descend from the single-write invariant -- a Ship ROLE property derived from C5 rather than a registry clause -- so their members are not drawn from a clause row and 008 legitimately appears in them while being absent from the C2 row. The test MUST assert the exemption list is exactly {B6, B17}, so a future behaviour cannot quietly opt out of the subset check.
- [ ] RANGE-COMPLETENESS guard: the test declares the authoritative behaviour range as B1-B18 and asserts that every ID in that range is DISCHARGED by EXACTLY ONE of the THREE contract-test files, with this suite declaring an explicit `OWNED_BEHAVIOURS` constant and importing the sibling constants rather than restating them -- 134.011-T's policy-contract module covering B2 (the literal C1 test text), B13 (Stage intake precedence) and B15 (C7 violation action), and 134.013-T's boundary module covering B1, B10, B11, B12, B14 and B18. This suite owns the remaining CAPTURE-AND-DISCHARGE group: B3, B4, B5, B6, B7, B8, B9 and the two derived behaviours B16 and B17. The guard imports each sibling's constant instead of hardcoding a copy, so a behaviour moved between files cannot be double-counted or dropped by a stale literal; hardcoding the sibling sets here would recreate, at the allocation layer, exactly the duplicated-source-of-truth failure the matrix consolidation repaired. DISCHARGE IS A COVERAGE CLAIM, NOT A NAME-MATCH: an ID counts as discharged ONLY when its owning test asserts the behaviour's FULL mapped semantics across its COMPLETE declared carrier subset. A test that names a behaviour while asserting a proper subset of its carriers, or only part of its mapped clause text, MUST NOT be counted -- that is a partially-covered behaviour masquerading as a covered one, which is worse than an uncovered behaviour because the allocation guard reports it green. The guard therefore cross-checks each discharged ID against the subset declared in 134.011-T and fails on any shortfall; the three known shortfalls it must reject if reintroduced are B2 asserted on 005 alone or with fewer than all five not-sufficient phrases ('same file', 'same function', 'same PR', 'same subsystem', 'related') or without the three worked cases, B13 asserted on 008 alone, and B15 discharged by section-presence alone without the telemetry-AND-halt violation action. The union of the THREE constants MUST equal B1-B18 exactly, and they MUST be PAIRWISE disjoint; the guard fails if a behaviour is claimed by more than one file or by none. Scoping the check to a single file would be wrong in the opposite direction -- it would force this suite to duplicate assertions that correctly live in the policy-contract and boundary suites, which is the duplication that caused the original matrix drift. It also asserts that every SEMANTIC assertion in this suite carries a behaviour ID. Two assertion scopes are legitimate and both must be ID-bound: SUBSET-SCOPED, which asserts positive marker text across exactly the behaviour's declared carrier subset; and UNIVERSAL, which is a negative guard that names the behaviour whose root form it protects but deliberately runs across ALL carriers or a full clause row, because the error it forbids is wrong wherever it appears (B5 FUSED-REF, B3 NO-PR/NO-THREAD-ASSUMPTION). A universal guard MUST be labelled `UNIVERSAL scope` so it is never mistaken for a subset violation by the SUBSET-FIDELITY check. Structural criteria that assert no policy content -- file existence, the canonical gate, docstring and marker-stability conventions -- are outside this requirement and carry no ID. This is the guard that would have caught the present defect: the dual-path and entry-reuse tests were correct in substance but unnumbered, so they sat outside the mapping and were governed by neither the subset check nor the completeness check. An unnumbered assertion is invisible to the audit, which is a strictly worse failure mode than a wrong one, because nothing reports it.
- [ ] DERIVED-SUBSET guard: for each derived behaviour, the test recomputes the derivation from its parents and asserts the result matches what the suite exercises -- B16 equals `B7 INTERSECT B8` and B17 equals `B6 UNION {007-fix-ci}`. It MUST fail if a parent subset changes without the derived subset following, which is the drift mode a materialized copy would reintroduce.
- [ ] C3-SYMMETRIC-GUARD test (behaviour B9, hardening H12): asserts the C3 symmetric guard is present on EVERY surface in the B9 subset resolved from 134.011-T -- workflow-policies.md.tmpl (134.001-T, authoritative), `_ship.agent.md.tmpl` (134.004-T), circuit-breaker.instructions.md.tmpl (134.005-T, guard-only carrier) and fix-ci/SKILL.md.tmpl (134.007-T) -- stating that a same-contract-surface completion of the authorized change IS in scope and must be fixed, and that deferral without a captured entry and a residual-risk record is itself a C7 violation. The Ship and fix-ci surfaces are required members: 134.004-T and 134.007-T both author the guard, and a two-surface assertion would leave the two loops that actually run fix cycles unguarded. This guards scope CONTRACTION, the mirror failure of the expansion P-021 exists to prevent.
- [ ] CONDITIONAL-C3 test (behaviour B16, SUBSET-SCOPED component asserting the AUTHORITATIVE-OWNER carrier 001[A] ONLY, regresses fix cycle 2): this criterion carries a behaviour ID because every semantic assertion in this suite must, but it is NOT the load-bearing B16 criterion -- the complete B16 carrier subset is asserted by the FIX-CI-DUAL-PATH criterion below, which is where the structural per-behaviour audit resolves B16. A behaviour discharged by this criterion ALONE would be a partially-covered behaviour masquerading as a covered one, which the RANGE-COMPLETENESS guard must reject: asserts the authoritative C3 text in workflow-policies.md.tmpl makes the thread-reply obligation CONDITIONAL on a thread existing, and a NEGATIVE guard asserts C3 is NOT phrased as an unconditional thread-reply requirement on any carrier. The guard must fail if a carrier states a reply is required for every finding without a thread-availability qualifier.
- [ ] THREADLESS-DISCHARGE test (behaviour B8, regresses fix cycle 2): asserts every surface in the B8 subset (workflow-policies.md.tmpl authoritative, `_ship.agent.md.tmpl`, fix-ci/SKILL.md.tmpl) states an explicit threadless disposition, and that the absence of a reply is stated to be NOT a C3 shortfall. For fix-ci the assertion is scoped to its CI-FINDING path specifically, not to the surface as a whole, since the same surface also handles thread-present review comments under B7. github-pr-automation and pr-lifecycle are deliberately EXCLUDED: every finding on those surfaces is a PR review comment and therefore always has a thread, so demanding threadless text there would force a contract their subject matter cannot support.
- [ ] THREE-RECORD-CITATION test (behaviour B8, regresses fix cycle 3): asserts EVERY threadless discharge names all three residual-risk records -- task-level, run-level AND closure -- on the authoritative surface and on every threadless carrier. A discharge naming only two of the three fails. This is the exact defect fix cycle 3 repaired in fix-ci.
- [ ] PER-FIELD-ID test (behaviour B5, regresses fix cycle 1): asserts every surface in the B5 subset (authoritative registry, `_ship.agent.md.tmpl`, fix-ci/SKILL.md.tmpl) states source-ref availability is judged INDEPENDENTLY PER FIELD, that `N/A` is a per-field availability marker and never a path-level default, and a NEGATIVE guard asserts no carrier instructs that the PR number and review-thread ID are marked `N/A` together as a blanket pairing on the threadless path. The guard must fail if a carrier reintroduces the paired-N/A wording. The POSITIVE assertion is scoped to B5 because a surface with no threadless case has no occasion to state per-field availability; the NEGATIVE guard runs across ALL carriers, since a fused pairing is wrong wherever it appears.
- [ ] FUSED-REF guard (behaviour B5, regresses the root form of the fix cycle 1 defect), stated as TWO assertions with DIFFERENT scopes, because conflating them is itself the applicability defect found in the owner-contract reconciliation. (a) UNIVERSAL NEGATIVE, across every C2 carrier plus the plan clause table and the authoritative registry: NO surface expresses the source refs as a single fused `PR/thread` identifier. A fused token is wrong wherever it appears, including on surfaces where both identifiers happen always to exist, because it encodes the assumption that the two are jointly present or jointly absent. (b) POSITIVE, scoped to the B5 SUBSET ONLY (001[A], 004, 007-fix-ci): the PR number and the review-thread ID each carry an INDEPENDENT applicability qualifier and an explicit per-field `N/A` case. Assertion (b) MUST NOT run universally: B5 deliberately excludes 006 and 007-pr-lifecycle because on a PR-review-comment surface both identifiers always exist at capture, so demanding a per-field `N/A` qualifier there would require text those surfaces have no occasion to carry -- the exact false-requirement failure mode the carrier-role model exists to prevent.
- [ ] ASYMMETRIC-QUALIFIER guard (behaviour B5, regresses the terminal P1 of the PR #372 review-fix loop). This is a THIRD defective shape, distinct from the two the guards above already catch, and it survived three fix cycles precisely because it is neither of them. The fused form uses ONE token (`PR/thread`); the paired form marks BOTH refs `N/A` together as a path-level default; the ASYMMETRIC form uses TWO separate tokens with NO paired-`N/A` instruction -- `PR number, review-thread ID (when applicable)` -- so it PASSES the fused-ref guard and PASSES the paired-`N/A` guard while still contradicting C2, because the single trailing qualifier attaches to the review-thread ID alone and thereby states the PR number UNCONDITIONALLY. (a) NEGATIVE, universal across every C2 carrier plus the plan clause table, the hardening H7 payload enumeration and the authoritative registry: NO live surface enumerates the source refs with an applicability qualifier attached to only ONE of the PR number and the review-thread ID. The guard must FAIL against the historical wording `source refs -- PR number, review-thread ID (when applicable), task ID, feature ID, shipment ID`, which is the exact form that survived in hardening H7 item (4) and `134.007-T` criterion (4) through HEAD `d52ab147`. (b) The guard MUST distinguish a qualifier that is ABSENT FROM BOTH refs -- correct on 006 and 007-pr-lifecycle, which B5 excludes because both identifiers always exist on a PR-review-comment surface -- from a qualifier present on ONE ref only, which is wrong EVERYWHERE. Symmetry, not presence, is what this guard tests: both qualified or neither, never one. (c) A test that merely greps for the literal string `(when applicable)` is VACUOUS and does not satisfy this criterion, since the same defect can be written `review-thread ID if one exists`, `where a thread exists`, or as a footnote; the assertion must resolve which REF each applicability qualifier governs.
- [ ] LIVE-CARRIER AGREEMENT test (behaviour B5): asserts that every LIVE source-ref enumeration in the B5 subset agrees with authoritative C2 in `134.001-T` on all three of its stated properties -- separate refs, independent per-field availability, explicit per-field `N/A` -- rather than merely avoiding the three known defective forms. Avoiding the defects is necessary and not sufficient: a surface that omits the availability rule entirely trips none of the negative guards and still leaves the rule unauthored, which is exactly how `134.007-T` came to be a mapped B5 carrier that never stated the behaviour. The test resolves the expected form FROM `134.001-T` at assertion time; it MUST NOT hardcode a copy of the wording, or the authority and the test can drift apart in the same direction and agree with each other while both diverge from C2. Clearly dated historical defect records -- the correction paragraphs in `134.001-T`, `134.011-T`, `134.012-T`, the memory sections and the circuit-breaker records -- are EXCLUDED from both this test and the negative guards above, since they quote the defective forms deliberately as evidence and a guard that failed on them would forbid recording the history it exists to prevent repeating.
- [ ] SINGLE-WRITE test (behaviour B6, regresses the original capture-completeness defect), covering BOTH mapped carriers -- 004 and 008 -- because B6's subset names two and a test asserting only the Ship half leaves the consumer half undischarged. (a) SHIP HALF (004): asserts `_ship.agent.md.tmpl` states the C2 capture is Ship's ONLY write to the deferred entry, and a NEGATIVE guard asserts no carrier instructs Ship to edit, amend, back-fill, re-classify, re-prioritize, or duplicate a captured entry afterwards. The guard must fail against the historical defective wording that originated this feature's first P1: a thread-present flow instructing Ship to capture first and "only afterward record the PR and review-thread IDs in the captured stash entry", which is a second write and contradicts the capture-only carve-out. (b) STAGE-CONSUMER HALF (008): asserts `_stage.agent.md.tmpl` states that Stage CONSUMES the Ship-owned residual-risk records to reconcile late identifiers under its OWN pre-existing stash authority, and that doing so requires NO Ship write and NO change to the carve-out. The two halves must be asserted TOGETHER: the invariant is only coherent if the field Ship can never back-fill has a designated party who can, so a suite proving Ship writes once without proving anyone closes the resulting gap would pass on a policy that permanently strands every `N/A`. Neither half may be written so as to weaken the other -- (b) MUST NOT be satisfiable by any text granting Ship a second write.
- [ ] CAPTURE-FIRST test (behaviour B3): asserts every surface in the B3 subset (authoritative registry, `_ship.agent.md.tmpl`, circuit-breaker.instructions.md.tmpl, github-pr-automation.instructions.md.tmpl, pr-lifecycle/SKILL.md.tmpl, fix-ci/SKILL.md.tmpl) makes capture a precondition for closing the finding. The thread-ordering half of this behaviour -- that replying to or resolving an out-of-scope thread before the capture exists is prohibited -- is asserted separately under B7, because circuit-breaker carries capture-before-close WITHOUT any thread step and would fail an ordering assertion it is not contracted to satisfy.
- [ ] THREAD-PRESENT-ORDERING test (behaviour B7): asserts every surface in the B7 subset (authoritative registry, `_ship.agent.md.tmpl`, github-pr-automation.instructions.md.tmpl, pr-lifecycle/SKILL.md.tmpl, fix-ci/SKILL.md.tmpl) states the full ordered continuation -- capture, then a substantive reply CITING the generated deferred entry ID, then thread resolution permitted only after that reply, then the PR/closure residual-risk record naming the SAME ID -- and that a reply omitting the deferred entry ID does not satisfy C3. fix-ci IS a required member: it maintains a review-thread inventory, replies per template, resolves Copilot threads via GraphQL and enforces a NON-NEGOTIABLE reply gate, so its out-of-scope review comments need the same ordering as any other thread-present surface. Only circuit-breaker is excluded, because it performs no thread operation at all.
- [ ] FIX-CI-DUAL-PATH test (behaviour B16, asserted across the FULL B16 subset derived as `B7 INTERSECT B8`; this is the canonical, load-bearing B16 criterion to which the structural per-behaviour audit must resolve, regresses the readiness fix cycle 3 defect): asserts every carrier in the B16 subset carries BOTH C3 dispositions -- threadless discharge and thread-present reply ordering -- and asserts each carrier's OWN selector rather than forcing one selector across the subset. For 001[A] and 004 the test asserts selection on ACTUAL THREAD AVAILABILITY (`WHERE A REVIEW THREAD EXISTS` / `no review thread exists for the finding at the moment it is classified`), and a NEGATIVE guard asserts neither surface conditions the disposition on FINDING KIND, because a pre-PR local-review finding is review-kind yet threadless and a kind-based selector would demand a reply on a nonexistent thread. For 007-fix-ci the test asserts the PATH split (CI-check-failure path threadless, review-comment path thread-present) and that the split is presented as an implementation of thread availability on that surface, not as a competing rule. The selector guard must fail against the historical wording "selects between them by FINDING KIND" applied to 001 or 004, and against any text conditioning the registry's or Ship's disposition on the kind of finding rather than on whether a thread exists. A further NEGATIVE guard asserts no surface describes fix-ci as having no review threads or as threadless by nature; it must fail against the historical wording "a review thread never surfaces inside a fix-ci run". Because B16 is DERIVED as `B7 INTERSECT B8`, this test resolves its subset by computing that intersection from the mapping rather than reading a literal list, so a carrier that gains or loses a disposition is picked up automatically. This is the test that prevents a carrier's path set from being inferred from the loop's name instead of the finding kinds it handles, AND prevents one carrier's selector from being generalized onto carriers whose authoritative selector differs.
- [ ] FIX-CI-ENTRY-REUSE test (behaviour B17): asserts that for every carrier in the B17 subset, when an out-of-scope finding already captured -- during the CURRENT run OR any PRIOR run -- later surfaces on a review thread, the surface reuses the EXISTING deferred entry -- citing its ID in the reply -- and MUST NOT create a second entry, consistent with the SINGLE-WRITE CAPTURE INVARIANT and 134.004-T's LATE-SURFACING THREAD rule. The PRIOR-RUN case is asserted explicitly and is not treated as a corner case: the commonest real path is a CI finding captured in an earlier run with `review-thread ID: N/A` that only gains a thread when a later run's review reaches that code, so a rule scoped to "this run" would permit exactly the duplicate the behaviour exists to prevent. Reuse is keyed on the expansion's stable deferred-entry ID, NEVER on run boundaries, and a test satisfiable only within a single run must fail. On 008 the same assertion takes its Stage-side form: reconciliation attaches late identifiers to the existing entry rather than creating a duplicate. A dual-path surface is exactly where duplicate capture is most likely, because the same finding can be seen twice through two different intake paths in one run. REUSE IS CONDITIONAL ON POSITIVE IDENTITY CONFIRMATION, and the four-case disposition truth table 134.004-T declares is asserted WITH CARRIER-ROLE SCOPING rather than uniformly across the B17 subset, because the subset mixes an authoring carrier with a reference-only one. (i) ON THE AUTHORING CARRIER (004): the test asserts the FULL truth table verbatim in its owner's terms -- zero candidates -> capture; exactly one POSITIVELY CONFIRMED candidate -> reuse; exactly one UNCONFIRMED candidate -> capture with the `DISCOVERY-STATUS: AMBIGUOUS` token and every candidate ID; more than one candidate -> the same ambiguity capture -- together with the assertion that both discovery-failure tokens are written INSIDE field (2) of the existing six-field payload and add NO seventh field, so the behaviour cannot be satisfied by a carrier that widens the payload. (ii) ON THE REFERENCE-ONLY CARRIER (007-fix-ci): the test asserts REFERENCE INTEGRITY, NOT RESTATEMENT -- that the surface cites 134.004-T's DEFERRED-ENTRY DISCOVERY and DISCOVERY FAIL-SAFE BY NAME and resolves the procedure from that owner at the point of use. It MUST NOT require 007 to reproduce the truth table, the lookup sources, the join keys or the token vocabulary. Demanding restatement there would contradict hardening H5 and would re-create the precise defect the owner-reconciliation cycle removed from this very surface, where a summary of 004's disposition rules was BOTH a duplicated source of truth AND, once 004 was corrected, a stale one still carrying the superseded suppress-capture semantics. A test that passes only when 007 restates the procedure is therefore not a stricter test but a WRONG one, and this criterion is not satisfied by any assertion of that shape. (iii) ON THE STAGE CARRIER (008): the same behaviour takes its reconciliation form -- late identifiers attach to the existing entry rather than creating a duplicate. A NEGATIVE guard asserts that no carrier suppresses capture on a discovery failure, and must FAIL against the historical wording `creates NO entry, because the finding is provably already captured` -- an unconfirmed candidate is not proof of capture, and suppressing capture on that basis converted a recoverable duplicate into an unrecoverable silent loss. That negative guard is UNIVERSAL across the subset, since suppressing capture is wrong on a referencing surface as much as on an authoring one.
- [ ] SIX-FIELD-PAYLOAD test (behaviour B4, hardening H7): asserts every surface in the B4 subset -- `_ship.agent.md.tmpl`, github-pr-automation.instructions.md.tmpl, pr-lifecycle/SKILL.md.tmpl and fix-ci/SKILL.md.tmpl, plus the authoritative registry -- enumerates all six capture-payload fields explicitly rather than referring to 'per C2'. circuit-breaker.instructions.md.tmpl is EXCLUDED although it is a C2 carrier: H7 names 004/006/007 as the payload carriers and 134.005-T contracts only for the capture-before-close requirement, so asserting the payload there would demand text its task does not author.
- [ ] NO-PR/NO-THREAD-ASSUMPTION guard (behaviour B3, UNIVERSAL scope): asserts no C2 carrier makes the existence of a PR or a review thread a PRECONDITION for capture. Capture must be unconditional on every surface, since Ship's local review runs before PR creation and CI findings never have a thread. This negative guard runs across the FULL C2 row (not a subset), because the assumption is wrong wherever it appears. It must fail against the historical defective wording in the pre-correction C2 source-ref enumeration -- "source refs (PR number, review-thread ID when applicable, ...)" -- which qualified ONLY the thread ID and therefore stated the PR number unconditionally, encoding the assumption that a PR always exists at capture. This is the root-cause guard for the whole class of defect the prior cycles kept rediscovering.
- [ ] Every test method carries a docstring naming the specific prior-cycle defect or dangling obligation it regresses, so a future failure is diagnosable without re-deriving the history.
- [ ] Marker strings are stable distinctive substrings, and each is resolved from the task that AUTHORS the text on that carrier, never from a surface that restates it. The owner set is DERIVED from this file's `OWNED_BEHAVIOURS` by taking the union of the authoring tasks in each owned behaviour's carrier subset -- it is NOT a pre-split list carried over from 134.012-T, and a criterion added or moved changes it. For {B3, B4, B5, B6, B7, B8, B9, B16, B17} that union is SIX tasks: 134.001-T (authoritative C2/C3 -- B3, B4, B5, B7, B8, B9, B16), 134.004-T (Ship defer-capture, single-write, late-surfacing, discovery -- every behaviour in this file: B3, B4, B5, B6, B7, B8, B9, B16, B17), 134.005-T (circuit-breaker operational restatement -- B3 and the B9 `[G]` guard ONLY; the circuit-breaker is excluded from B4 by H7 and from B7 because it performs no thread operation), 134.006-T (github-pr-automation ordered sequence -- B3, B4, B7), 134.007-T (pr-lifecycle and fix-ci -- B3, B4, B5, B7, B8, B9, B16, B17) and 134.008-T (Stage-side single-write consumer and reuse carrier -- B6, B17). 134.002-T is NOT an owner of this file: its ONLY mapped behaviour anywhere is B11 (the Role Boundary forbidden-verb list), whose subset is `001[A], 002[N]` and which is owned by 134.013-T. Its former listing here as a provisional-priority carrier was never map-supported in the first place -- B12's subset is `001[A], 004, 006, 007-pr-lifecycle, 007-fix-ci` and has never included 002 -- so this entry was not made stale by the cycle-1 split, it was wrong before it. 134.003-T (B18) and 134.009-T (B10) are likewise not owners here, though those two ARE simple split consequences. Examples: `SINGLE-WRITE CAPTURE INVARIANT` (004), `per-field availability marker` (001/004). Prefer short distinctive tokens over fragile long sentences.
- [ ] Full canonical gate green: $env:PYTHONPATH='src'; python -m unittest discover -s tests. Root `python -m pytest` is NOT the canonical gate here (docs/compound/097-S-canonical-unittest-gate.md).
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Sibling of 134.011-T. Where 134.011-T proves each clause is PRESENT on each carrier, this task proves each clause SAYS THE RIGHT THING on each carrier.

The distinction is load-bearing and is exactly why this task exists. All three defects found in the prior fix cycles were on surfaces where the clause was present and byte-identical to its dogfood, so both a byte-identity test and a clause-presence test would have passed while the contract was internally contradictory:

1. Fix cycle 1 -- the threadless path marked a KNOWN PR number `N/A`, permanently discarding a real identifier under the single-write invariant.
2. Fix cycle 2 -- authoritative C3 demanded a review-thread reply even on surfaces where no thread can exist, making the clause unsatisfiable for CI and pre-PR findings.
3. Fix cycle 3 -- the fix-ci threadless discharge named only two of the three required residual-risk records, weakening the only traceability mechanism available without a thread.

Each is a SEMANTIC contradiction between carriers, not a missing clause. This suite is what converts that class of defect from a review finding into a test failure.

The NO-PR/NO-THREAD-ASSUMPTION guard is deliberately broader than any single prior defect: all three cycles trace to one root cause -- the policy was first authored assuming every finding arrives on a PR review thread -- so the suite guards the root assumption directly rather than only its three known symptoms.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on BOTH sibling test tasks for their `OWNED_BEHAVIOURS` constants, which this file's RANGE-COMPLETENESS guard IMPORTS rather than restates: 134.013-T (boundary group) and 134.011-T (policy-contract group). The 134.011-T edge is therefore load-bearing twice over -- it carries the authoritative map AND the producer-side constant this file consumes -- and 134.011-T in turn depends on every content task (134.002-T through 134.010-T). Authoring order is 011 -> 013 -> 012, which is exactly the order the constants must exist in for the consumer guard to resolve. A single edge to 011 is used instead of re-enumerating all nine content edges: 011 already transitively orders this task after all carrier content, and it establishes the render/normalize helper pattern this file reuses. Adding the nine edges again would add no ordering and would duplicate a dependency surface that must be kept in sync.

Separate test FILE from both siblings (tests/test_scope_containment_semantics_contract.py vs tests/test_scope_containment_policy_contract.py vs tests/test_scope_containment_boundary_contract.py), so there is no file contention between the three tasks and a structural, semantic and boundary failure each stay distinguishable.

TEST-DIRECTION NOTE: several criteria require NEGATIVE guards (asserting an absent phrasing), which are easy to write in a way that passes vacuously. Each negative guard MUST be validated by temporarily reintroducing the old defective wording and confirming the test fails; a guard that cannot be shown to fail against the historical defect does not satisfy its criterion.

Commit with an explicit enumerated path list only (hardening H13).

SIZING RE-ESTIMATE (Staging PR #372 Copilot review-fix cycle 1, 2026-08-19, thread PRRT_kwDORzpWpM6amwKd): lowered L -> M by TAKING the split declared below rather than deferring it to implementation. Six criteria covering the CLAUSE-BOUNDARY behaviours (B1, B10, B11, B12, B14, B18) moved to the new sibling 134.013-T. What remains is the behaviour-map allocation/structural guards plus the CAPTURE-AND-DISCHARGE group (B3-B9, B16, B17) -- roughly thirteen assertions over the shared carrier-resolution helper.

SPLIT RATIONALE, superseding the pre-declared split note below: the review's point was that a KNOWN-NECESSARY split deferred to implementation lands at the worst possible moment -- mid-execution, with the 2-hour bound already breached and the least context available to choose the cut. The cut taken follows the pre-declared line exactly (capture-and-discharge stays; clause-boundary moves), extended only by LINEAGE for the three behaviours registered after that line was written: B16 and B17 are DERIVED from B7/B8 and B6 and MUST stay with their parents, or the DERIVED-SUBSET guard would have to recompute an intersection whose operands live in another module; B18 was split out of B11 in the owner-contract reconciliation and moves with its sibling. The two groups share only the carrier-resolution PATTERN -- a convention, not a table -- so neither file duplicates the other's map, and the map itself stays in 134.011-T where it has always been.

The estimate PREVIOUSLY sat near the 2-hour bound rather than safely inside it, and the split line was DECLARED IN ADVANCE rather than improvised mid-execution: split at the behaviour boundary into capture-and-discharge semantics (B3-B9 plus the three-record, fused-ref and no-PR/no-thread guards) and clause-boundary semantics (B1, B10, B11, B12, B14 plus SUBSET-FIDELITY), a clean cut because the two groups share only the carrier-resolution helper. That split has now BEEN TAKEN (see SPLIT RATIONALE above); this note is retained because the declared line is what made the cut mechanical when the review required it. The one adjustment made at execution time: SUBSET-FIDELITY stayed with the allocation guards in this file rather than moving with the clause-boundary group, since it governs all eighteen behaviours across all three files and is not a clause-boundary assertion.
<!-- END:implementation-notes -->
