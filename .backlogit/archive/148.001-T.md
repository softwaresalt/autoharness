---
archived_from: .backlogit/queue/148.001-T.md
archived_status: done
artifact_type: task
commit: ec894bf3a7c4c1ad91b97191f5f8a94177419c9f
created_at: 2026-08-28T03:46:46.9497974Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 148.001-T
labels:
    - S0
    - install
    - policy-registry
    - config
parent_id: 148-F
priority: medium
references:
    - docs/plans/2026-08-27-policy-registry-and-review-persona-layer-plan.md
    - templates/policies/workflow-policies.md.tmpl
    - .autoharness/harness-manifest.yaml
status: archived
title: U1 — Render and install the policy registry, and register it in the manifest
updated_at: 2026-08-29T18:19:03.8791543Z
---

Render templates/policies/workflow-policies.md.tmpl to .github/policies/workflow-policies.md (LF-only) and append its harness-manifest entry. Render-only: no policy text authoring. Domain: installed artifact + config. Files: 2.

<!-- BEGIN:acceptance-criteria -->
* `.github/policies/workflow-policies.md` exists and is LF-only.
* **INV-1**: rendered body is byte-identical to the template modulo the 9 bound placeholders. Any diff beyond placeholder substitution FAILS this unit.
* **Placeholder scan (CORRECTED 2026-08-28, review-fix cycle 3 — D9-B)**: zero unresolved `{{...}}` in the output, **subject to the named, commented `EXEMPT_POLICY_PROSE_META_TOKEN` rule**. The template carries a **literal `{{...}}` ellipsis meta-token at L359**, inside P-013.5 fail-closed-verification prose ("`model_family` / `model_provider` is empty or an unresolved `{{...}}` placeholder"). That token is intended literal registry content and survives the render, so a **bare** "zero unresolved `{{...}}`" assertion is **impossible as written** and would fail permanently. The exemption is a **closed allow-list of exactly ONE token** — the bare ellipsis form, which carries no identifier and can never be a bindable variable. Every other `{{...}}` match is a genuine unbound variable and MUST fail. This rule is **distinct** from the persona-side `EXEMPT_OUTPUT_SCHEMA_EXEMPLARS` rule (D8-D) and from U8 scenario 2's route exemption; do not conflate or widen any of the three.
* 27 `P-0NN` headings present; P-001 through P-021 all resolvable.
* Manifest entry present; `checksum` recomputed from the installed file matches (SHA-256 over raw LF bytes).
* `{{TEST_COMMAND}}` rendered to the Q5 value verbatim.
* **VALUE-LEVEL BINDING ASSERTION (ADDED 2026-08-28, review-fix cycle 3 — D9-A)**: the rendered **compile / red-phase clauses** must contain the correct compile-check value:
  * template **L48** (harness-architect postcondition — "the harness compiles (`{{BUILD_CHECK_COMMAND}}`)") renders to contain the literal `python -m py_compile src/autoharness/cli.py`;
  * template **L88** (red-phase precondition — "`{{BUILD_CHECK_COMMAND}}` exits 0 AND `{{TEST_COMMAND}}` exits non-zero with expected failure markers") renders to contain the same literal;
  * the rendered registry contains **no** occurrence of `pip install -e .`.
  This assertion is **REQUIRED and is not implied by the placeholder scan**: a *wrong* binding resolves exactly as cleanly as a right one, so "no unresolved placeholders" structurally cannot detect it. A wrong value here FAILS this unit.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan unit **U1**. Create `.github/policies/workflow-policies.md` by rendering `templates/policies/workflow-policies.md.tmpl` (83,467 bytes, 27 `P-0NN` headings), then append a manifest entry.

**Bind all 9 template placeholders**:
* `{{TEST_COMMAND}}` = `PYTHONPATH=src python -m unittest discover -s tests` — **Q5 APPROVED**; do NOT take the profile's stale `pytest`
* `{{BUILD_CHECK_COMMAND}}` = `python -m py_compile src/autoharness/cli.py` — **CORRECTED 2026-08-28, review-fix cycle 3 (plan decision D9)**
* `{{DEFAULT_BRANCH}}` = `main`
* `{{BACKLOG_TOOL_NAME}}` = `backlogit`
* `{{BACKLOG_DIRECTORY}}` = `.backlogit`
* `{{STATUS_DONE}}` = `done`
* `{{FEATURE_SHIPMENTS}}` = `true`
* `{{OP_SHIP_SHIPMENT_MCP}}` = `backlogit_ship_shipment`
* `{{DATE}}` = install date

**D9 — `{{BUILD_CHECK_COMMAND}}` is the COMPILE CHECK, not the build. CORRECTED 2026-08-28 (review-fix cycle 3).** The prior revision of this task bound it to `pip install -e .`, sourced from `workspace-profile.yaml` `build.command`. That was a **wrong-source, wrong-semantics** binding on the most authoritative surface this shipment installs.

**Authoritative source is `.autoharness/harness-manifest.yaml` `variables:`**, which declares TWO SEPARATE variables that MUST NOT be conflated:

| Manifest variable | Authoritative value | Meaning | Bound by U1? |
|---|---|---|---|
| `BUILD_COMMAND` | `pip install -e .` | Install/build the distribution | **NO** — the registry template does not reference it |
| `BUILD_CHECK_COMMAND` | `python -m py_compile src/autoharness/cli.py` | Cheap **compile check**: does the harness still compile? | **YES** — this is the one U1 binds |

**Why the semantics are not interchangeable.** The template references `{{BUILD_CHECK_COMMAND}}` in exactly two clauses, both requiring a *compile* check:
* **L48** (harness-architect postcondition): "the harness compiles (`{{BUILD_CHECK_COMMAND}}`)". `pip install -e .` is an install, not a compile assertion.
* **L88** (red-phase precondition): "`{{BUILD_CHECK_COMMAND}}` exits 0 AND `{{TEST_COMMAND}}` exits non-zero with expected failure markers". Binding an install command here makes the red-phase gate assert the wrong property and couples every red-phase transition to a mutating environment operation.

**Source-of-truth rule (D9-C)**: for every U1 binding, the manifest `variables:` block is authoritative over `workspace-profile.yaml`. This is the same rule D2 already applies to `{{TEST_COMMAND}}`; neither D2 nor D9 licenses reading a binding from the profile.

Manifest entry fields: `path`, `primitive`, `template: "policies/workflow-policies.md.tmpl"`, `checksum` (SHA-256 over raw LF bytes), `note` citing `336F3AB7`.

**Execution posture**: migration-first — the artifact must exist before U2 changes what the engine asserts about it.

**Pre-flight (hardening step 1)**: capture `verify-workspace` output BEFORE this unit so RK-B status-changes are attributable rather than guessed.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Forbidden: editing policy text (D1 — the template is SSOT; authoring here forks it and recreates the drift `336F3AB7` reports). Forbidden: changing `workspace-profile.yaml` (D7 — it feeds `profile_hash`).

RK-C: compute the checksum over raw LF bytes; a CRLF write on Windows silently invalidates it against CI.

U1 and U7 both edit `.autoharness/harness-manifest.yaml`; serialization is by ordering, not locking (execution is strictly serial under P-001).

**RK-A is a REALIZED risk on this unit (2026-08-28, review-fix cycle 3).** "Rendering an 83 KB registry with a wrong variable binding silently publishes a wrong authoritative policy" actually happened here: `{{BUILD_CHECK_COMMAND}}` was pinned to `pip install -e .`. The lesson Ship must carry: **a placeholder scan cannot catch a wrong binding.** Resolution-shaped assertions ("no `{{...}}` remain") are blind to value correctness by construction. That is why this unit now carries the D9-A value-level assertion in addition to the scan, and why D9 names the manifest — not the profile — as the source.

**Do not "fix" the two variables by making them agree.** `BUILD_COMMAND` (`pip install -e .`) and `BUILD_CHECK_COMMAND` (`python -m py_compile src/autoharness/cli.py`) are both correct, existing, authoritative manifest values with different meanings. This unit binds only the latter and edits neither.
<!-- END:implementation-notes -->