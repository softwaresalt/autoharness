---
archived_from: .backlogit/queue/150.005-T.md
archived_status: done
artifact_type: task
commit: 8b79de94e6705f4e950257073b263369a7e258a7
created_at: 2026-08-30T05:18:06.0241954Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: autoharness-2h-rule-v1
    size_source: agent
dependencies:
    - 150.004-T
id: 150.005-T
labels:
    - release
    - versioning
    - packaging
parent_id: 150-F
priority: medium
references:
    - docs/plans/2026-08-29-v1_5_0-release-preparation-plan.md
status: archived
title: Synchronized version bump to 1.5.0 across all six version surfaces
updated_at: 2026-08-30T20:42:47.9912871Z
---

Bump the version from 1.4.11 to 1.5.0 across ALL SIX surfaces in five files. Verified present at main 484da671:

1. pyproject.toml L7            version = "1.4.11"
2. src/autoharness/__init__.py L7   fallback "1.4.11"  (for editable/pre-install contexts)
3. plugin.json L4               "version"
4. .github/plugin/marketplace.json L9   "version"   (metadata.version)
5. .github/plugin/marketplace.json L15  "version"   (plugins[0].version) <-- SECOND OCCURRENCE IN THE SAME FILE, EASY TO MISS
6. uv.lock L16                  version = "1.4.11"

ACTUAL ENFORCEMENT BOUNDARY (corrected after PR #422 review — an earlier version of this task overstated the undetected-drift risk):
- tests/test_verify_workspace.py:147-164 (test_distribution_and_plugin_versions_stay_in_sync) ALREADY compares pyproject.toml against the __init__.py fallback, plugin.json, marketplace metadata.version, AND marketplace plugins[0].version.
- So FIVE of the six loci ARE guarded by the unittest suite, which is a real CI gate. If any of those five drift, it surfaces as a TEST FAILURE — not as silent drift. Ship must NOT misread a failure in those five as "invisible to CI".
- ONLY uv.lock is absent from that guard. It is the one surface that can drift undetected and therefore needs explicit manual confirmation.
- release.yml separately validates ONLY pyproject.toml against the pushed tag (L26-L45).

Keep the six-surface bump regardless — the point is to update all six, with correct expectations about which are machine-checked.

PA-4 CONSTRAINT (uv.lock): regenerate with `uv lock` — do NOT hand-edit. Then INSPECT THE DIFF: it must touch ONLY the autoharness version field. If any other package's version, hash, or resolution marker changes, REVERT AND ESCALATE. An opportunistic dependency bump is not in this release's scope and would silently widen the blast radius of a "version bump". This matters more for uv.lock than the others precisely because it is the unguarded surface.

INV-4: confirm `git diff` touches NO versioned schema mirror under schemas/**/<version>.schema.json. Published mirrors must never be mutated in place (see docs/compound/2026-08-08-schema-mirror-mutated-in-place-without-version-bump.md).

ACCEPTANCE:
- All six loci read 1.5.0; grep the repo for any remaining "1.4.11" and justify every hit (CHANGELOG history entries are expected and correct).
- test_distribution_and_plugin_versions_stay_in_sync PASSES (this is the machine check for five of the six).
- uv.lock diff limited to the autoharness version, confirmed by manual inspection.
- No schema mirror modified.