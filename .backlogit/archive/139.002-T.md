---
archived_from: .backlogit/queue/139.002-T.md
archived_status: done
artifact_type: task
commit: f57d0f0c50f2ea005a688e91ebf42f4decda51cd
created_at: 2026-08-21T06:40:42.0928332Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 139.001-T
id: 139.002-T
labels:
    - bug
    - tests
    - contract
    - regression-guard
parent_id: 139-F
priority: high
references:
    - docs/plans/2026-08-20-ship-pre-archived-manifest-member-execution-plan.md
    - docs/reviews/2026-08-20-ship-pre-archived-manifest-member-execution-review.md
status: archived
title: Add discriminating regression coverage for the Ship pre-archived execution-exclusion contract (template + installed mirror)
updated_at: 2026-08-21T11:42:55.2786205Z
---

Add a discriminating regression module pinning the pre-archived execution-exclusion contract in BOTH Ship agent files.

FILE (exactly one, new): `tests/test_ship_pre_archived_manifest_members.py`

PATTERN: follow the established template+mirror contract-test pattern of `tests/test_ship_safe_close_pointer.py` - module-level `_TEMPLATE = ROOT / 'templates' / 'agents' / '_ship.agent.md.tmpl'`, `_MIRROR = ROOT / '.github' / 'agents' / '_ship.agent.md'`, a `_files()` helper yielding `('template', text)` and `('mirror', text)`, `unittest.TestCase`, whitespace-normalized (`' '.join(text.split())`) comparisons for multi-line prose anchors, and `self.subTest(file=label)` per file. Standard library only; no new dependency.

ASSERTIONS:
- A1 (contract present, BOTH files): the text declares the executable-task-set derivation, the explicit `pre-archived` exclusion, the exhaustive status rule (keep queued/active; skip-and-report archived; any other/missing/unreadable status halts fail-closed), the `-T` artifact-type filter preceding status reads with the covering feature resolved via `parent_id`, and that the manifest is the CLOSURE MEMBERSHIP record rather than the executable set.
- A2 (NEGATIVE CONTROL, mirror): the pre-fix unconditional formulation - a loop header iterating "each task in the shipment/feature" with no derivation preamble - is no longer present in the installed mirror. This is the assertion that makes the guard discriminating rather than vacuous: it is written to FAIL against the pre-Task-139.001-T text at HEAD e88a8d62. Record that fact in the module docstring.
- A3 (tolerated, not fatal, BOTH files): a `pre-archived` member is stated to be expected and tolerated and does not halt the run.
- A4 (empty-set halt, BOTH files): an empty executable set over a non-empty manifest halts and reports, does not advance to build/PR, and does not trigger a closure path.
- A6 (ordering + distinct reporting, BOTH files): the Step 0.5 item 1a early-warning is stated to be unchanged and to run strictly BEFORE the derivation, and `already_done` is reported distinctly from `pre_archived_skipped`.
- A7 (template variable hygiene): `templates/agents/_ship.agent.md.tmpl` contains no `{{STATUS_ARCHIVED}}` token (and no other newly introduced unresolved variable).

EXPLICITLY OUT OF SCOPE (plan amendment A3.1, review finding P1-1): do NOT add any new `classify_shipment_close_path` test. The closure-side invariant is ALREADY pinned and green in `tests/test_shipment_closure_classification.py` by `test_mixed_pre_archived_and_queued_manifest_members_still_selects_cascade` (queued feature + mixed queued/pre-archived children -> CASCADE; the exact 144-S shape), `test_feature_queued_children_pre_archived_still_selects_cascade`, `test_feature_pre_archived_children_queued_still_selects_cascade`, `test_all_manifest_members_pre_archived_still_selects_cascade`, and `test_pre_archived_out_of_manifest_child_falls_back_to_safe_close` - shipped by feature 132-F / shipment 141-S. Instead, CITE those tests BY NAME in this module's docstring as the authoritative closure-side invariant, and state that the execution-side exclusion must never be "fixed" later by stripping pre-archived members out of a manifest.

ACCEPTANCE CRITERIA:
- Module imports and runs under the configured suite with no new dependency.
- All of A1, A2, A3, A4, A6, A7 present and passing after 139.001-T.
- A2 is demonstrably discriminating; the module docstring records that it fails against the pre-fix text at HEAD e88a8d62.
- The docstring cites the five existing closure tests by name.
- No new `classify_shipment_close_path` test is added.
- No existing test is weakened, deleted, re-scoped, or reordered.
- No non-test file is modified by this task.
- Configured suite green at task completion.

ORDERING: depends on 139.001-T. Ship evaluates the configured suite at every task boundary, so a test-first (red) ordering would break the gate on 139.001-T. The contract lands first; the discriminating guard lands second, with A2 plus the docstring note proving the guard discriminates rather than passing vacuously.