---
artifact_type: task
commit: a412c21
created_at: 2026-07-31T01:42:08.9561372Z
dependencies:
    - 102.001-T
id: 102.002-T
labels:
    - ship-agent
    - claim-integrity
    - agent-template
parent_id: 102-F
priority: medium
references:
    - docs/plans/2026-07-30-ship-claim-integrity-preflight-plan.md
status: done
title: Add queued-with-active-work early-warning to Ship intake
updated_at: 2026-07-31T03:50:22.4958958Z
---

Unit B of docs/plans/2026-07-30-ship-claim-integrity-preflight-plan.md. Add an intake consistency scan that runs IMMEDIATELY AFTER the shipment record is loaded and BEFORE the existing status validation AND BEFORE the Step 0.5.4 claim, in BOTH template and dogfood. WHY pre-claim/pre-validation: (a) the claim flips queued->active and would mask a pre-existing inconsistency if the scan ran after it (e.g. near Step 0.5.6); (b) Step 0.5.1 validation accepts only queued/active and rejects 'blocked' earlier, so the blocked-with-active-work diagnostic is only reachable if the scan precedes validation (templates/agents/_ship.agent.md.tmpl:160-164). Scan: if the loaded record is 'queued' OR 'blocked' while any manifest task is already active/done, halt SHIPMENT_STATE_INCONSISTENT (detect-and-report only, no auto-repair). Remediation: 'queued' -> resolve and re-claim; 'blocked' -> resolve the blocking gate and transition blocked->queued BEFORE any claim, NEVER re-claim-to-active directly on a blocked record (docs/compound/2026-05-07-backlogit-shipment-status-constraints.md:32-44). Enumerate manifest task IDs via 'backlogit shipment get {id}' and read each via 'backlogit get {task_id}' (CLI fallback path); task-only manifest (097-S), covering feature derived via parent_id, not scanned. Files (3 = 2 authored + 1 mechanical checksum refresh): templates/agents/_ship.agent.md.tmpl, .github/agents/_ship.agent.md, AND .autoharness/harness-manifest.yaml. This task edits the mirror AFTER 102.001-T, so it lands the FINAL mirror bytes and writes the authoritative end-state sha256 checksum for the .github/agents/_ship.agent.md entry; gate = test_manifest_tracks_dogfood_ship_agent_checksum (tests/test_telemetry_ship_lifecycle.py:46-53). Verify: frontmatter valid, no unresolved {{VAR}}, markdownlint, scan positioned before validation/claim, manifest checksum refreshed. Depends on 102.001-T (same file pair; sequence to avoid conflict; B lands last).