---
artifact_type: task
commit: cae73534
created_at: 2026-08-21T03:11:44.3059434Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 138.001-T
labels:
    - bug
    - tests
    - docs
    - p-021
    - baseline-red
    - atomic
    - gate-atomic
parent_id: 138-F
priority: high
status: done
title: 'Gate-atomic baseline repair: quote the malformed benchmark-plan frontmatter scalar AND resolve 019-DL through a lifecycle-stable resolver'
updated_at: 2026-08-21T09:09:22.0356339Z
---

GATE-ATOMIC BASELINE REPAIR. This is the FIRST EXECUTABLE TASK of the whole three-shipment chain. It repairs BOTH baseline blockers that currently keep Ship's mandatory gates red, in ONE task, so that a first task exists which can actually leave every mandatory gate green.

WHY BOTH SCOPES ARE IN ONE TASK (do not split): Ship gates EVERY task. Two independent blockers are red at baseline:
* BLOCKER 1 (Gate 1, YAML frontmatter validity): docs/plans/2026-08-02-structural-navigation-benchmark-suite-plan.md line 12 carries an unquoted YAML scalar containing ': ', so mdfront.Decode fails and the workspace-wide docline lint aborts with no report.
* BLOCKER 2 (pytest): two P-021 contract modules hard-load the archived 019-DL artifact from a stale `.backlogit/queue/` path inside `setUpClass`, collapsing both test classes.
Repairing only one leaves the other red, so any task completing at that point fails its own gate. Previously BLOCKER 1 was owned by 136.001-T inside 144-S while 144-S was blocked by 146-S, and 146-S owned only BLOCKER 2 - a circular mandatory-gate dependency with NO executable first task. Merging both repairs here dissolves that cycle. Splitting them again re-creates it.

======================================================================
SCOPE A - quote the malformed blast_radius scalar (carried in verbatim from superseded 136.001-T; nothing dropped)
======================================================================

FILE: docs/plans/2026-08-02-structural-navigation-benchmark-suite-plan.md, line 12.

Current:
  blast_radius: elevated (result-integrity + multi-family: eval code / tests / docs / fixtures)

Change to (content byte-identical inside the quotes):
  blast_radius: "elevated (result-integrity + multi-family: eval code / tests / docs / fixtures)"

The value is an unquoted YAML scalar containing ': ' inside "multi-family: eval code", so YAML parses it as a nested mapping in a scalar position and mdfront.Decode fails with 'mapping values are not allowed in this context'. Because `backlogit docs lint` aborts on the FIRST decode error, this one file suppresses docline conformance reporting across all of docs/.

SCOPE A ACCEPTANCE
A1. The file's frontmatter parses as YAML.
A2. `git diff` shows EXACTLY ONE changed line in this file. The 2026-08-02 plan belongs to feature 085-F and is otherwise untouched - no reformatting, no other frontmatter edits.
A3. `backlogit docs lint --path docs/plans/2026-08-02-structural-navigation-benchmark-suite-plan.md` no longer reports a decode error.
A4. Ship's Gate 1 (YAML frontmatter validity) is green at this task's completion gate.

======================================================================
SCOPE B - resolve 019-DL through a lifecycle-stable resolver (original 138.001-T scope, unchanged)
======================================================================

Repair both P-021 contract-test loads of the archived 019-DL deliberation artifact via a shared lifecycle-stable resolver, plus two regression guards.

FILES (exactly three, all under `tests/`):

1. `tests/test_scope_containment_policy_contract.py` (134.011-T, base module)
   Add a module-level helper resolving a backlog artifact by ID:
   * Resolve the storage root via `autoharness.backlog_root.resolve_backlog_root(_REPO_ROOT, env={})`. Importable because `pyproject.toml` sets `pythonpath = ["src"]`.
   * The explicit `env={}` is MANDATORY (plan amendment A2): `resolve_backlog_root` consults `BACKLOGIT_WORKSPACE_DIR` first and raises `BacklogUnavailableError` when the override names a missing directory. Without isolation, any developer or CI runner with that variable set makes these contract tests fail for a reason unrelated to the code under test - the same nondeterministic-red class this task removes. Mirror the isolation pattern already used in `tests/test_backlog_root.py`.
   * Probe `queue/<artifact_id>.md`, then `archive/<artifact_id>.md`; return the first that exists.
   * On a miss, raise an error naming the artifact ID and EVERY probed candidate path.
   Host chosen because both siblings already import from this module, so the resolver rides the existing one-way allocation edge (011 -> 013 -> 012) and introduces no import cycle. Do NOT modify `OWNED_BEHAVIOURS` or the matrix.

2. `tests/test_scope_containment_boundary_contract.py` (134.013-T)
   * Line 127: replace the hardcoded `repo_root / ".backlogit" / "queue" / "019-DL.md"` construction with a resolver call; import the resolver alongside the constants this module already shares.
   * Lines 56, 69, 76, 214, 291, 329: update stale prose citations of `.backlogit/queue/019-DL.md` to cite 019-DL by artifact ID and note the location is resolved, not hardcoded. Docstring/comment lines only.

3. `tests/test_scope_containment_semantics_contract.py` (134.012-T)
   * Line 137: replace the hardcoded `_REPO_ROOT / ".backlogit" / "queue" / "019-DL.md"` construction with the same resolver call, imported from the base module alongside the constants it already imports.

REGRESSION GUARD 1 - behavioural (primary):
Assert that the resolver locates 019-DL; that it resolves to EXACTLY ONE location (simultaneous presence in both `queue/` and `archive/` signals a half-completed archival and must fail loudly - do not weaken this by reflex); and that the loaded text still contains the `C4 AMENDMENT` and `C5 AMENDMENT` markers, proving content equivalence rather than silent coverage reduction. The failure message must name backlog lifecycle drift explicitly.

REGRESSION GUARD 2 - structural (secondary):
Assert no P-021 contract module constructs a lifecycle-volatile `queue/`-anchored artifact path outside the resolver. Scope the scan to CODE lines of the three named modules so prose citations do not false-positive. The guard MUST exempt the resolver's own definition explicitly and BY NAME (plan amendment A3) so the exemption is visible and cannot silently widen. This guard is what prevents recurrence: it fails at authoring time rather than at the next archival. If it proves brittle it may be NARROWED, but MUST NOT be dropped silently without recording why.

SCOPE B ACCEPTANCE
B1. Both `setUpClass` collapses are eliminated and this change introduces no NEW failure. (Amendment A1: Stage ran no tests and cannot bound other pre-existing causes of red. If further pre-existing failures surface on OTHER surfaces, Ship MUST NOT absorb them - classify under P-021 C1, capture a `DEFERRED SCOPE EXPANSION` entry, and escalate as a shipment-level blocker. That is not a licence to widen this task.)
B2. Neither sibling module contains a hardcoded `queue/019-DL.md` path.
B3. Both modules load the artifact through the shared resolver.
B4. Guard 1 fails, with a lifecycle-drift message, if 019-DL is moved or duplicated across `queue/` and `archive/`.
B5. Guard 2 fails if a new lifecycle-volatile path literal is introduced into any of the three modules.
B6. No B1-B18 assertion is weakened, removed, skipped, or has its subject changed. The three-way allocation guards (RANGE-COMPLETENESS, pairwise-disjointness, expected-allocation) remain green and untouched.

======================================================================
COMBINED FILE BUDGET (supersedes the former "no file outside tests/" criterion)
======================================================================
EXACTLY FOUR files may be modified:
* the three `tests/` modules named in Scope B, and
* docs/plans/2026-08-02-structural-navigation-benchmark-suite-plan.md (ONE line only, per A2).
Nothing else. In particular: no `src/`, no `schemas/`, no `templates/`, no `.github/`, no other file under `docs/`. The docs/ sweep is NOT part of this task - it remains 136.002-T in 144-S.

WIDTH ISOLATION NOTE: this task deliberately crosses two families (tests + one docs line) because the gate-atomicity constraint above requires it. This is a bounded, explicitly-enumerated four-file exception recorded at staging time, not a licence to widen. The docs side is a single quoting change with a byte-identical value.

COMBINED ACCEPTANCE
C1. All of A1-A4 and B1-B6 hold.
C2. At this task's completion gate, ALL of Ship's mandatory gates are green: Gate 1 (frontmatter validity), Gates 2-4, and the configured pytest suite - subject only to the B1/A-amendment escape hatch for pre-existing failures on OTHER surfaces, which are captured under P-021 C1 and escalated, never absorbed.

EVIDENCE (verified read-only by Stage; Ship must confirm the green transition at execution):
* `git cat-file -e origin/main:.backlogit/queue/019-DL.md` -> exit 128 (absent)
* `git cat-file -e origin/main:.backlogit/archive/019-DL.md` -> exit 0 (present)
* Archived copy retains `C5 AMENDMENT` (line 111) and `C4 AMENDMENT` (line 116)
* docs/plans/2026-08-02-structural-navigation-benchmark-suite-plan.md line 12 re-read 2026-08-20: still unquoted, still contains `multi-family: eval code`

COORDINATION: `137.003-T` in 145-S also edits `tests/test_scope_containment_boundary_contract.py` (correcting false `stash remove`/`archive` CLI-alias comments). This shipment merges FIRST, so `137.003-T` rebases onto the repaired file. Line ranges are disjoint - trivial textual rebase, no semantic conflict, no re-plan of 145-S.

TRACEABILITY
* Scope A absorbed from 136.001-T (superseded-by this task, archived 2026-08-20, not deleted). 136.001-T removed from 144-S membership; the former 136.002-T -> 136.001-T and 136.003-T -> 136.001-T edges were retargeted/removed accordingly.
* Stash: 7852CE0D (Scope B), 395EBE60 (Scope A)
* Deliberations: 021-DL (Scope B; `.backlogit/archive/021-DL.md`, archived 2026-08-20), `docs/decisions/2026-08-20-docline-lint-hard-abort-malformed-frontmatter-deliberation.md` (Scope A)
* Plans: `docs/plans/2026-08-20-p021-contract-test-deliberation-path-resolution-plan.md` (primary, Task 1 + Amendment A4); `docs/plans/2026-08-20-docline-lint-restoration-plan.md` (Task 1, relocated here by Amendment R1)
* Review: `docs/reviews/2026-08-20-p021-contract-test-deliberation-path-resolution-review.md` (PASS; scope-expansion addendum 2026-08-20)

SIZING: size S, complexity low. Scope A adds one trivial single-line quoting edit (formerly XS/trivial) to an S/low task; the combined effort remains comfortably inside the 2-hour rule on the effort axis and adds no uncertainty on the complexity axis, so neither axis forces a split.