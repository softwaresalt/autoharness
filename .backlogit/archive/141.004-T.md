---
artifact_type: task
created_at: 2026-08-21T20:23:51.7166868Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 141.003-T
id: 141.004-T
labels:
    - bug
    - tests
    - isolation
    - topology
parent_id: 141-F
priority: medium
references:
    - docs/plans/2026-08-21-full-suite-test-isolation-plan.md
status: done
title: De-couple test_gates_topology.py from ambient cwd (34 sites) and empty the guard allowlist
updated_at: 2026-08-22T03:28:52.5182825Z
---

Remove ambient-cwd coupling from `tests/test_gates_topology.py` (34 of the 58 sites — the largest single module) and EMPTY the isolation guard's allowlist.

**Plan:** `docs/plans/2026-08-21-full-suite-test-isolation-plan.md` Task 2 (+ amendments A4, H3 refinement)

## Steps
1. **Per-call-site containment review (H3 refinement) — mandatory before editing.** This module mixes concerns: some tests exercise filesystem topology READERS against a synthetic workspace (containment likely irrelevant), others exercise workspace/backlog-root resolution (containment likely required). Determine and record per site.
2. Replace `dir=Path.cwd()` with `dir=Path(__file__).resolve().parents[1]` where containment is required; plain `tempfile.TemporaryDirectory()` only where it provably is not. Never relocate a containment test to system temp; default to anchoring when uncertain.
3. **EMPTY the isolation guard's allowlist and assert it is empty**, so the allowlist cannot survive as a permanent escape hatch (amendment A4).

## Acceptance criteria
- AC-a. Zero `dir=Path.cwd()` sites remain anywhere under `tests/` (all 58 addressed across 141.002-T, 141.003-T and this task).
- AC-b. `tests/test_test_suite_isolation_contract.py` is GREEN with an EMPTY allowlist, and asserts the allowlist is empty.
- AC-c. All 34 sites have a recorded containment determination with justification.
- AC-d. `test_gates_topology.py` passes IN ISOLATION before and after with an IDENTICAL pass count. No assertion is weakened.
- AC-e. Victim #2 (`FilesystemTopologyReadersTests::test_empty_queue_and_archive_dirs_pass_as_zero_shipments`) lives in this module, so the the **assertion-integrity gate (AIG)** defined in `docs/plans/2026-08-21-full-suite-test-isolation-plan.md` applies: **AIG-1** its assertion callsite set is exactly equal before and after (AST-extracted, not eyeballed); **AIG-2** its temp workspace stays inside the repository — anchor, never relocate to system temp; **AIG-3/AIG-4** every changed non-assertion line in this file is cited as N1 (anchor edits, permitted inside the test method body), N2, N3, N4 or N6. This module carries 34 of the 58 anchor sites, so in-method N1 edits to a victim's own file are EXPECTED here and are explicitly authorized.

## Dependency
Depends on 141.003-T (shared allowlist file; serialised to avoid conflict).

## Note
34 sites in one module is at the upper edge of the 2-hour bound. If the containment review shows the sites are heterogeneous enough to exceed it, split by TestCase class and record the split — do NOT batch-edit without per-site review.