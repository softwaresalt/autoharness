---
artifact_type: review
created_at: 2026-08-05T23:49:08.6096986Z
id: 111.001-R
parent_id: 111-F
status: done
title: 'Plan-review: 111-F crash-resumption — PASS after 1 fix cycle + re-reviews + 2026-08-06 Copilot PR #304 alignment (engram fail-closed + installed-mirror decomposition; P0/P1 clear)'
updated_at: 2026-08-06T08:32:00Z
---



<!-- BEGIN:decisions -->
ACCEPT plan for harvest with P1-1 (live-session-vs-crash disambiguation; default operator handoff) folded into task C1 acceptance and P1-2 (prune allowlist) folded into task C2 acceptance. P0 clear; both P1 resolved in-cycle; RE-REVIEW 2026-08-06 raised and resolved P1-3 (never-installed instruction template -> host protocol in the backlogit-pack overlay with install/checksum/verify wiring); SECOND RE-REVIEW 2026-08-05 raised and resolved P1-4 (wrong community-template installed_checksum/source_checksum dual-checksum + false "already-installed" host -> standard first-party single artifacts[].checksum + section-presence, plus a dedicated install/tune task 111.005-T that actually installs and records the overlay) and P1-5 (unreachable dead-session predicate: checkpoint V1 has no heartbeat/lease, age cannot prove a session dead -> recovery is EXPLICITLY OPERATOR-CONFIRMED before any restore/prune; no auto dead-session recovery, no live-session hijack); no outstanding P0/P1. Hardening REQUIRED and completed (H1-H7, with H1 and H6 revised and H7 added). Candidates (a)/(c) remain deferred; no external binary mutation. THIRD RE-REVIEW 2026-08-05 raised and resolved P1-6 (role-ownership recovery contract: explicit unique checkpoint selection + CheckpointV1 `agent`-ownership validation + Orchestrator owner-exclusive restore routing, fail closed on missing/invalid/ambiguous ownership or non-unique selection; hardening H7); no outstanding P0/P1. FOURTH RE-REVIEW 2026-08-05 (publication-review) raised and resolved P1-7 (zero-candidate semantics: zero active recovery candidates = no recovery needed => normal orchestration continues, NOT a failure and NOT an operator handoff; the explicit-selection + `agent`-ownership-validation + owner-exclusive-routing + fail-closed gate engages ONLY when one or more valid candidates exist); no outstanding P0/P1. FIFTH ALIGNMENT 2026-08-06 raised and resolved P1-8 (owner-exclusive routing wording gap: DoD #3, 111-F hardening H7 never-clause, 111.003-T verify AC#1(a)/docs AC#3, and 111.001-T implementation-notes named owner-exclusive routing / the never-direct clause over "restore/resume" (or "restore") only, understating the P1-6 contract which routes ALL THREE operations — restore, resume, AND prune — exclusively to the owning agent; corrected so every normative surface reads "routes restore/resume/prune EXCLUSIVELY to the owning agent ... never restores/resumes/prunes Stage/Ship-owned state directly"); no scope/decomposition/edge change (12 tasks / 14 edges unchanged); no outstanding P0/P1. SEVENTH ALIGNMENT 2026-08-06 (publication-review) raised and resolved P1-10 (owner-agent recovery conflict — the EXISTING Stage/Ship owner agent templates still fresh-start on an invalid checkpoint and resolve active checkpoints before a confirmed successful resume; delegating to the Orchestrator does not fix the owner protocols): added a NEW width-isolated task C5/111.006-T (size S / complexity medium) updating BOTH owner protocols to the fail-closed lifecycle (no fresh-start on invalid; resolve only after confirmed successful owner resume; zero-candidate normal startup; explicit operator selection; owner `agent`-ownership validation; owner-exclusive restore/resume/prune after operator confirmation), wired deps (111.006->111.001, 111.003->111.006), hardening H8, 002-SP addendum, 119-S membership {111.001,111.004,111.005,111.006,111.002,111.003}; DECOMPOSITION CHANGED to 13 tasks (110×3, 111×6, 112×4) / 16 task-blocks edges (110:2, 111:10, 112:4); VERDICT REMAINS PASS, no outstanding P0/P1.

COPILOT PR #304 ALIGNMENT 2026-08-06: P1-11 (engram-unavailable contradiction) resolved to a single FAIL-CLOSED OPERATOR HANDOFF (file-based prune degradation eliminated) across 111.002-T/111.004-T/111-F H5/verify+docs; P1-12 (findings 4/5, installed dogfood mirrors omitted) resolved by adding width-isolated task C6/111.007-T (refresh `.github/agents/_orchestrator/_stage/_ship.agent.md` mirrors + harness-manifest checksums + verification; deps 111.007->111.001, 111.007->111.006; 111.003 verify AC#9 + dep on 111.007). DECOMPOSITION CHANGED to 14 tasks (110×3, 111×7, 112×4) / 19 task-blocks edges (110:2, 111:13, 112:4); 119-S membership {111.001,111.004,111.005,111.006,111.007,111.002,111.003}; VERDICT REMAINS PASS, no outstanding P0/P1.
<!-- END:decisions -->

<!-- BEGIN:findings -->
P0-1 (checked): could resume auto-proceed on ambiguous/torn state? NO — H1 fail-safe => operator handoff. CLEAR. P0-2 (checked): could resume introduce parallelism/new worktree? NO — H2 single-active cursor only. CLEAR. P1-1 (RAISED): 'detect an interrupted session' can FALSE-POSITIVE on a checkpoint belonging to a CONCURRENT LIVE session (not a crash), risking a hijack/double-drive. RESOLUTION: require a POSITIVE interrupted-signal before any auto-resume — auto-resume ONLY when the session can be confirmed dead (e.g., checkpoint age beyond a liveness threshold AND no live heartbeat/lock); otherwise DEFAULT to operator handoff. Never auto-resume a checkpoint that could belong to a live session. P1-2 (RAISED): prune-on-restore could drop safety-critical state. RESOLUTION: explicit prune allowlist — NEVER prune the active-shipment cursor, unresolved-checkpoint pointer, or gate verdicts; prune only superseded action-observation history via bounded summarize. P2 (minor): interaction with cleanup_checkpoints retention — non-blocking.

RE-REVIEW 2026-08-06 (independent BLOCKED-review repair, route claude-opus-4.8/anthropic/high): P1-3 (RAISED): task C2 created a NEW STANDALONE checkpoint-recovery/prune-on-restore instruction template with NO install path — the capability-pack-registry maps each pack to exactly one `overlay_instruction`, and no pack references the new file, so it would NEVER be installed into a consuming workspace (dead prose). RESOLUTION (accepted): HOST the protocol as a new section INSIDE the already-installed backlogit capability-pack overlay instruction `templates/instructions/backlogit.instructions.md.tmpl` (the backlogit pack already installs it and threads the checkpoint workflow; and the backlogit pack is a hard precondition of crash-resumption). Wire it via the existing backlogit Formal Overlay Contract with harness-manifest installed_checksum/source_checksum coverage and a verify-harness presence+checksum assertion; resolve all {{VARIABLE}} at install time; keep technology-agnostic with an engram-degraded fallback. Folded into 111.004-T (host + install/manifest/checksum/variable ACs), 111.003-T (verify + checksum coverage AC), 111-F IMPL-PLAN + DoD + hardening H6. WIDTH-ISOLATION: stays within the instruction-template family (an already-installed overlay); install/verify wiring reuses the existing overlay contract + manifest + verify-harness — no new pack/schema, no separate task required; 119-S membership unchanged {111.001,111.002,111.003,111.004}. P1-3 RESOLVED. No new P0/P1 outstanding after re-review.

SECOND RE-REVIEW 2026-08-05 (independent BLOCKED-review repair, route claude-opus-4.8/anthropic/high): P1-4 (RAISED — overlay checksum/install contract): the P1-3 resolution used the community-template `installed_checksum`/`source_checksum` dual-checksum surface and assumed the backlogit overlay was ALREADY installed. Verified against the repo: `.autoharness/harness-manifest.yaml` uses a SINGLE `artifacts[].checksum` field (no installed_checksum/source_checksum anywhere), and there is NO `.github/instructions/backlogit.instructions.md` artifact or on-disk file — the overlay is NOT installed in this dogfood workspace, and source-vs-installed drift comparison is not a supported first-party surface. RESOLUTION (accepted): replan on the STANDARD first-party contract — a SINGLE `artifacts[].checksum` (as used by the agent-engram/agent-intercom/graphtor-docs overlays) + a TARGETED SECTION-PRESENCE assertion; add a dedicated install/tune task 111.005-T that actually installs `templates/instructions/backlogit.instructions.md.tmpl` -> `.github/instructions/backlogit.instructions.md` when the backlogit pack is active and records it as a first-party manifest artifact; do NOT assert installed-vs-source drift comparison. Folded into 111.004-T (host-authoring only; removed dual-checksum), 111.005-T (NEW install/tune registration, single checksum), 111.003-T (verify = section-presence + single artifact-checksum coherence), 111-F IMPL-PLAN/DoD + hardening H6. DECOMPOSITION CHANGED: added 111.005-T (install/tune skill family, width-isolated from instruction-template authoring); 119-S membership now {111.001,111.002,111.003,111.004,111.005}; deps 111.005->111.004 and 111.003->111.005; no cycles.
P1-5 (RAISED — dead-session predicate): the P1-1 resolution's auto-resume-when-confirmed-dead predicate ("checkpoint age beyond a liveness threshold AND no live heartbeat/lock") is UNREACHABLE — checkpoint schema V1 exposes NO heartbeat/session-lock/lease (only created_at/updated_at), the informing spike 002-SP explicitly excluded new checkpoint-schema/runtime plumbing, and age alone cannot distinguish a live from a dead session, so "no live heartbeat/lock" can never be confirmed and automatic dead-session recovery is unsafe/unreachable. RESOLUTION (accepted): NO automatic dead-session recovery and NO live-session hijack; recovery is EXPLICITLY OPERATOR-CONFIRMED before any restore/prune. Candidate (d) remains a useful bounded orchestration feature because operator confirmation is sufficient (no automatic-recovery substrate is deferred/needed). Folded into 111.001-T (AC 1/3 operator-confirmation, no dead-session auto-recovery), 111.003-T (verify the operator-confirmation gate presence + docs), 111-F IMPL-PLAN/DoD + hardening H1, and a correction addendum on spike 002-SP. P1-4 + P1-5 RESOLVED. No new P0/P1 outstanding after the second re-review.

THIRD RE-REVIEW 2026-08-05 (independent BLOCKED-review repair, route claude-opus-4.8/anthropic/high): P1-6 (RAISED — role-ownership recovery contract): the recovery protocol drove restore/resume over "the recorded single-active cursor" without establishing WHICH checkpoint is selected when several are unresolved, nor WHO is authorized to perform the restore. Verified against live state: MULTIPLE checkpoints can be ACTIVE concurrently across agents (this workspace currently shows a `stage`-owned active checkpoint AND two `ship`-owned active checkpoints), so a "detect the active checkpoint" step is AMBIGUOUS and could silently restore the wrong session or let the Orchestrator drive another agent's owned restore, violating P-001 role separation. The backlogit CheckpointV1 schema carries an authoritative `agent` field (`required,oneof=ship stage`) that was not being honored for routing. RESOLUTION (accepted): the recovery contract now (1) REQUIRES the operator to EXPLICITLY SELECT a SINGLE checkpoint by filename (Orchestrator NEVER auto-picks); (2) VALIDATES the selected checkpoint's CheckpointV1 `agent` ownership (`stage`/`ship`); (3) the Orchestrator ROUTES restore/resume/prune EXCLUSIVELY to that owning agent (stage-owned => Stage subagent only; ship-owned => Ship subagent only) and NEVER executes Stage/Ship-owned restore/resume/prune work itself directly; (4) FAILS CLOSED (operator handoff; no restore/resume/prune/resolve) on missing/invalid/ambiguous `agent` ownership OR a checkpoint that cannot be uniquely selected. Uses ONLY the existing CheckpointV1 `agent` field (no new schema/plumbing; in-boundary for the prose layer). Folded into 111.001-T (AC 1/2), 111.003-T (verify the selection+ownership+routing gate + docs), 111-F IMPL-PLAN/DoD + hardening H7, and a correction addendum on spike 002-SP. WIDTH-ISOLATION: orchestrator agent-template prose (single template family); no new task; 119-S membership unchanged {111.001,111.002,111.003,111.004,111.005}; deps/edges unchanged. P1-6 RESOLVED. No new P0/P1 outstanding after the third re-review.

FOURTH RE-REVIEW 2026-08-05 (independent PUBLICATION-review repair, route claude-opus-4.8/anthropic/high): P1-7 (RAISED — zero-candidate = no-recovery-needed, not failure): the role-ownership recovery contract (P1-6) and the spike's third addendum described FAIL CLOSED on "a checkpoint that cannot be UNIQUELY selected", which over-broadly swept the ZERO-CANDIDATE case into fail-closed/operator-handoff. That is incorrect: at session start the expected steady state is that NO active recovery candidate exists (an ordinary clean start with nothing to recover); treating that as failure would force a spurious operator handoff on every normal session and misclassify the happy path as an anomaly. RESOLUTION (accepted): the recovery contract is explicitly TWO-BRANCH — (i) ZERO active recovery candidates => NO recovery needed, the Orchestrator CONTINUES NORMAL ORCHESTRATION (NOT a failure, NOT an operator handoff); (ii) ONE OR MORE valid candidates exist => explicit operator selection by filename (never auto-pick) + CheckpointV1 `agent`-ownership validation (`stage`/`ship`) + Orchestrator owner-exclusive restore/resume/prune routing engages, with FAIL CLOSED applying ONLY AMONG THOSE EXISTING CANDIDATES on a non-unique/ambiguous selection or missing/invalid/ambiguous `agent` ownership. Zero candidates NEVER triggers fail-closed. Uses only existing CheckpointV1 fields (no new schema/plumbing; in-boundary for the prose layer). Folded into 111.001-T (AC 1/2), 111.003-T (verify + docs cover the zero-candidate no-op), 111-F IMPL-PLAN/DoD + hardening H7 clarification, and a fourth correction addendum on spike 002-SP. WIDTH-ISOLATION: orchestrator agent-template prose (single template family); no new task; 119-S membership unchanged {111.001,111.002,111.003,111.004,111.005}; deps/edges unchanged. P1-7 RESOLVED. No new P0/P1 outstanding after the fourth re-review.

FIFTH ALIGNMENT 2026-08-06 (owner-exclusive routing wording conformance): P1-8 (RAISED — normative surfaces understated the owner-exclusive routing to "restore/resume"/"restore" only): the P1-6 role-ownership contract routes restore/resume/prune EXCLUSIVELY to the owning agent and the Orchestrator NEVER performs ANY of the three on Stage/Ship-owned state directly, but 111-F DoD #3, 111-F hardening H7 (never-clause), 111.003-T verify AC#1(a)/docs AC#3, and 111.001-T implementation-notes each named only a subset ("restore/resume" or "restore"), so those artifacts OVERSTATED resolution before the all-three-operation criterion existed in their text. RESOLUTION (accepted): aligned every normative surface to explicitly cover ALL THREE operations — "routes restore/resume/prune EXCLUSIVELY to the owning agent ... never restores/resumes/prunes Stage/Ship-owned state directly". No new mechanism; uses only the existing CheckpointV1 `agent` field; no scope/decomposition/dependency change (12 tasks / 14 blocks edges unchanged). Folded into 111-F (DoD #3, hardening H7), 111.003-T (verify AC#1(a) + docs AC#3), 111.001-T (implementation-notes); durable memory `2026-08-05-stage-publication-review-repair-p017-final` annotated; single active Stage checkpoint checkpoint-20260806-023057.json unchanged. P1-8 RESOLVED. No outstanding P0/P1 after the fifth alignment.

SIXTH ALIGNMENT 2026-08-06 (durable-surface never-direct conformance + verifier strengthening): P1-9 (RAISED — durable memory/stash/archive/publication surfaces still restore-only after P1-8): the FIFTH alignment widened only the LIVE backlog artifacts (111-F DoD#3/H7, 111.003-T, 111.001-T) to all three operations, but four durable authoritative surfaces still stated the Orchestrator never-direct clause over RESTORE only — (1) stash 34D50F2D candidate-(d) living-tracker note, (2) authoritative final memory `2026-08-05-stage-publication-review-repair-p017-final`, (3) docs/memory publication record `2026-08-05-stage-publication-review-repair-p017.md`, and (4) the archive/002-SP correction addendum. RESOLUTION (accepted): every active authoritative surface now states the never-direct clause over ALL THREE operations — the Orchestrator NEVER directly RESTORES, RESUMES, or PRUNES Stage/Ship-owned state; append-only history preserved (stash appended alignment note + inline fix; memory `-final` corrected with appended alignment note + new annotation memory; docs record inline fix + appended correction subsection; 002-SP FIFTH addendum appended, prior addenda untouched). No active authoritative surface remains restore-only. Additionally, P2 STRENGTHENING (no verdict change): 111.003-T verifier ACs extended with AC#6 (H3 — resolve_checkpoint asserted ONLY AFTER a confirmed successful owner resume by the owning agent, named host = orchestrator agent-template crash-resumption section) and AC#7 (H5/C3 — degraded-mode fallback assertion over the two NAMED host sections/artifacts: orchestrator crash-resumption section for backlogit-unreachable => operator handoff, and backlogit overlay `.github/instructions/backlogit.instructions.md` checkpoint-recovery/prune-on-restore section for engram-unreachable => bounded file-based prune degradation); 111.002-T (C3) hosting detail named to those same two existing hosts. WIDTH-ISOLATION preserved (instruction/agent-template families only; no new task/file); no scope/decomposition/dependency change (12 tasks / 14 blocks edges unchanged); deliberation 013-DL linked informs->112-F and archived (deliberation only; 112-F/112.001..004-T and 118-S remain queued/intact; 117-S sole eligible). VERDICT UNCHANGED: PASS, P0/P1 CLEAR. P1-9 RESOLVED. No outstanding P0/P1 after the sixth alignment.

SEVENTH ALIGNMENT 2026-08-06 (independent PUBLICATION-review repair — owner-agent recovery conflict, route claude-opus-4.8/anthropic/high): P1-10 (RAISED — owner agent protocols still fresh-start on invalid checkpoint and resolve active checkpoints before confirmed resume): the recovery contract had been threaded through the ORCHESTRATOR routing (111.001-T) and its owner-exclusive routing, but the EXISTING owner agent templates themselves (Stage `templates/agents/_stage.agent.md.tmpl` + Ship `templates/agents/_ship.agent.md.tmpl`) still (a) FRESH-START on an invalid checkpoint (silently discard + start a fresh session) and (b) RESOLVE active checkpoints BEFORE a confirmed successful resume — both directly conflict with the fail-closed lifecycle. Merely delegating to the Orchestrator does NOT change the owner protocols, so the conflict would ship. RESOLUTION (accepted): add a NEW bounded, width-isolated task C5/111.006-T (owner agent-template family — Stage + Ship agent templates; size S / complexity medium) that updates BOTH owner protocols to the fail-closed lifecycle — ZERO-CANDIDATE NORMAL STARTUP; EXPLICIT OPERATOR FILENAME SELECTION when own candidates exist (no auto-pick); CheckpointV1 `agent`-ownership validation against the agent's OWN role (Stage restores/resumes/prunes/RESOLVES only `stage`-owned; Ship only `ship`-owned — CROSS-ROLE RESOLUTION prohibited alongside cross-role restore/resume/prune); OWNER-EXCLUSIVE restore/resume/prune/RESOLVE after EXPLICIT OPERATOR CONFIRMATION; OWNER-SCOPED RESOLUTION of ONLY the single explicitly-selected, ownership-matched checkpoint (never a bulk sweep) and ONLY AFTER a CONFIRMED SUCCESSFUL owner resume; and FAIL CLOSED with NO FRESH-START FALLBACK on any invalid/ambiguous/torn/malformed read — changing the owner protocols themselves, not mere delegation. WIRED: dependency 111.006-T->111.001-T (contract) and 111.003-T->111.006-T (verify AC#8 asserts the owner-template recovery sections); hardening H8 added to 111-F; 002-SP SIXTH correction addendum; owner templates install via the existing first-party agent-template install path (no new install primitive/schema); 119-S topological membership now {111.001,111.004,111.005,111.006,111.002,111.003}. DECOMPOSITION CHANGED: 111-F now has SIX tasks {001,002,003,004,005,006}; workspace total 13 tasks (110×3, 111×6, 112×4); 16 task-blocks edges (110:2, 111:10, 112:4), no cycles. VERDICT REMAINS PASS. P1-10 RESOLVED. No outstanding P0/P1 after the seventh alignment.

COPILOT PR #304 ALIGNMENT 2026-08-06 (independent review repair, route claude-opus-4.8/anthropic/high, DARK_MODE — findings 2 + 4/5). TWO alignments folded, both P1-class, both RESOLVED:
- P1-11 (finding 2, on 111.002-T thread PRRT_kwDORzpWpM6W6Nc9 / comment PRRC_kwDORzpWpM7eJQQS — Engram-unavailable behavior contradiction): 111.002-T (C2 prune) and 111.004-T described a "bounded file-based prune degradation" fallback when the engram substrate is unavailable, contradicting 111-F H5 / the verifier / docs which require fail-closed. RESOLUTION (accepted): choose ONE behavior — FAIL-CLOSED OPERATOR HANDOFF when the required prune substrate (engram) is unavailable. The file-based prune degradation path is ELIMINATED (a bounded file-based prune could not be proven safe AND explicitly non-resuming, so the safe choice is fail-closed). Aligned across 111.002-T (AC), 111.004-T (AC#4), 111-F H5 + degraded-fallback bullet, and the verify/docs surfaces (111.003-T). Contradiction removed.
- P1-12 (findings 4 + 5, on 111.006-T thread PRRT_kwDORzpWpM6W6NdM... and 111.001-T thread PRRT_kwDORzpWpM6W6Nd4 / comments ...JQRK and ...JQRl — installed dogfood MIRRORS omitted from scope): findings 4/5 flag that the planned owner/orchestrator template edits (111.001-T orchestrator recovery-routing, 111.006-T owner-template lifecycle) must ALSO refresh the INSTALLED DOGFOOD MIRRORS `.github/agents/_orchestrator.agent.md`, `.github/agents/_stage.agent.md`, `.github/agents/_ship.agent.md` (the runtime agents that actually receive the behavior) PLUS the `.autoharness/harness-manifest.yaml` per-artifact checksum + verification updates, otherwise the active dogfood Orchestrator/Stage/Ship never receive the new behavior. RESOLUTION (accepted): rather than widen 111.001-T/111.006-T (which would break width isolation across three mirror files + manifest), added ONE bounded, width-isolated task C6/111.007-T (size S / complexity medium) that refreshes all THREE installed mirrors + their harness-manifest checksums (LF-normalized committed git-blob) + verification, depending on both source-edit tasks (deps 111.007->111.001, 111.007->111.006), with 111.003-T verify gaining AC#9 (installed-mirror section-presence + three-mirror manifest-checksum coherence) and depending on 111.007-T. 111.001-T and 111.006-T implementation-notes gained mirror cross-refs to C6. DECOMPOSITION CHANGED: 111-F now has SEVEN tasks {001,002,003,004,005,006,007}; workspace total 14 tasks (110×3, 111×7, 112×4); 19 task-blocks edges (110:2, 111:13, 112:4), no cycles; 119-S topological membership now {111.001,111.004,111.005,111.006,111.007,111.002,111.003}; hardening H5 wording (single fail-closed) + DoD items updated on 111-F. Serial chain 117-S->118-S->119-S intact; task-only manifests. VERDICT REMAINS PASS; both P1-11 and P1-12 RESOLVED; no outstanding P0/P1 after the Copilot PR #304 alignment.
<!-- END:findings -->

<!-- BEGIN:summary -->
Independent plan-review of 111-F (crash-resumption + context-pruning-on-restore over checkpoint/engram substrate; 002-SP). VERDICT: PASS after one review-fix cycle plus a 2026-08-06 re-review plus a 2026-08-05 second independent re-review plus a 2026-08-05 THIRD independent re-review — P0/P1 CLEAR. Reviewed against P-001/P-016, fail-safe restore, role separation, and the external-binary boundary. Re-review corrected a never-installed instruction template (P1-3); the second re-review corrected the overlay checksum/install contract (P1-4: community dual-checksum + false already-installed host -> standard first-party single artifacts[].checksum + section-presence + a dedicated install/tune task 111.005-T) and the unreachable dead-session predicate (P1-5: checkpoint V1 has no liveness source -> recovery is explicitly operator-confirmed, no auto dead-session recovery, no live-session hijack); the third re-review added the role-ownership recovery contract (P1-6: explicit unique checkpoint selection + CheckpointV1 `agent`-ownership validation + Orchestrator owner-exclusive restore routing, fail closed on missing/invalid/ambiguous ownership or non-unique selection; hardening H7); the fourth (publication) re-review corrected the zero-candidate semantics (P1-7: zero active recovery candidates = no recovery needed => normal orchestration continues, NOT a failure/handoff; the selection/ownership/routing/fail-closed gate engages only when one or more candidates exist). A FIFTH alignment (P1-8, 2026-08-06) then closed a wording gap so the owner-exclusive recovery routing is stated over ALL THREE operations (restore/resume/prune) on every normative surface (111-F DoD #3 + hardening H7, 111.003-T verify + docs, 111.001-T notes). A SIXTH alignment (P1-9, 2026-08-06) strengthened the verifier (111.003-T AC#6/#7 + 111.002-T hosting). A SEVENTH alignment (P1-10, 2026-08-06 publication-review) resolved the owner-agent recovery conflict by adding NEW width-isolated task 111.006-T (C5) that updates BOTH owner agent templates (Stage + Ship) to the fail-closed lifecycle — zero-candidate normal startup, explicit operator filename selection, CheckpointV1 `agent`-ownership validation against own role, owner-exclusive restore/resume/prune/RESOLVE after operator confirmation (CROSS-ROLE RESOLUTION prohibited), OWNER-SCOPED resolution of ONLY the explicitly-selected checkpoint (never bulk) after confirmed successful owner resume, and fail-closed with NO fresh-start fallback — with hardening H8 added to 111-F and verify coverage in 111.003-T AC#8. RECONCILED CURRENT STATE (publication-review 2026-08-06): 111-F now has SIX tasks {001,002,003,004,005,006}; workspace total 13 tasks (110×3, 111×6, 112×4) / 16 task-blocks edges (110:2, 111:10, 112:4), no cycles; 119-S topological membership {111.001,111.004,111.005,111.006,111.002,111.003}; hardening H1-H8 (H8 = owner-agent fail-closed lifecycle). The single active valid Stage checkpoint is now checkpoint-20260806-072043.json (ACTIVE-CHECKPOINT ROLL 2026-08-06; created via the supported `backlogit checkpoint create` lifecycle, carrying the complete final state as first-class content; it ROLLS FORWARD FROM and supersedes/resolves the now-RESOLVED checkpoint-20260806-062506.json — which recorded stale F3 wording mislabeling an 'active' predecessor as a terminal/non-blocking predecessor state and was RESOLVED via `backlogit checkpoint resolve` — itself having superseded the now-RESOLVED checkpoint-20260806-053524.json — which had been improperly amended in place after creation and was RESOLVED via `backlogit checkpoint resolve` — itself having superseded the RESOLVED chain checkpoint-20260806-034020.json -> checkpoint-20260806-023057.json). COPILOT PR #304 ALIGNMENT 2026-08-06 then folded two further P1 alignments: P1-11 chose a single fail-closed operator-handoff for engram-unavailable prune (eliminating the contradictory file-based prune degradation across 111.002-T/111.004-T/111-F H5/verify+docs), and P1-12 added a width-isolated task C6/111.007-T refreshing the three installed dogfood agent MIRRORS (`.github/agents/_orchestrator/_stage/_ship.agent.md`) + harness-manifest checksums + verification (so the active dogfood agents actually receive the behavior), changing the decomposition to 14 tasks (110×3, 111×7, 112×4) / 19 task-blocks edges (110:2, 111:13, 112:4) and 119-S membership {111.001,111.004,111.005,111.006,111.007,111.002,111.003}. The active Stage checkpoint has been rolled to its current successor via the supported create+resolve lifecycle (sole active now `checkpoint-20260806-083118.json`; `checkpoint-20260806-072043.json` RESOLVED/superseded). The 112-F provenance was corrected on the same PR #304 branch: the cleanup-triggering `custom_fields.source_stash_id` is omitted in favor of the non-cleanup `source_stash_tracker_id` so 936C68F3 stays ACTIVE (Ship must not retire it). VERDICT REMAINS PASS; P0/P1 CLEAR. [CHECKPOINT-PROVENANCE ROLL 2026-08-06 (append-only, prior text preserved): the "sole active now `checkpoint-20260806-083118.json`" pointer above is SUPERSEDED — `checkpoint-20260806-083118.json` is now RESOLVED/superseded via the supported `backlogit checkpoint create`+`resolve` lifecycle and the SOLE active valid Stage checkpoint is `checkpoint-20260806-150505.json`, which corrects the F3 112-F provenance from the cleanup-triggering `custom_fields.source_stash_id` to the NON-cleanup `custom_fields.source_stash_tracker_id: 936C68F3` (936C68F3 stays ACTIVE; Ship MUST NOT archive it). Structural state (14 tasks / 19 task-blocks edges / manifests / review verdicts) UNCHANGED. VERDICT REMAINS PASS; P0/P1 CLEAR.]
<!-- END:summary -->