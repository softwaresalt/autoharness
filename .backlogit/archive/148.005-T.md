---
archived_from: .backlogit/queue/148.005-T.md
archived_status: done
artifact_type: task
commit: ec894bf3a7c4c1ad91b97191f5f8a94177419c9f
created_at: 2026-08-28T03:51:41.8955354Z
custom_fields:
    complexity: trivial
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 148.003-T
id: 148.005-T
labels:
    - S0
    - install
    - review-personas
    - security
parent_id: 148-F
priority: medium
references:
    - docs/plans/2026-08-27-policy-registry-and-review-persona-layer-plan.md
    - templates/agents/review/
status: archived
title: U5 — Install the 4 security and parity personas
updated_at: 2026-08-29T18:21:11.4505572Z
---

Render python-reviewer (from the EXISTING technology-reviewer template, per the U3 mapping), security-reviewer, security-lens-reviewer, and agent-native-parity-reviewer into .github/agents/subagents/. Domain: installed artifacts. Files: 4.

<!-- BEGIN:acceptance-criteria -->
* All 4 files exist under `.github/agents/subagents/` with the exact cited filenames.
* `python-reviewer.agent.md` is rendered from `templates/agents/review/technology-reviewer.agent.md.tmpl` — **not** from a Python-specific template, and no such template is created.
* The rendered `python-reviewer.agent.md` contains no residual `technology-reviewer` identity string where the installed persona name is expected (frontmatter `name` and self-references resolve to the language-specific identity).
* All written LF-only.
* **Placeholder scan — zero unresolved `{{...}}`, SUBJECT TO the named `EXEMPT_OUTPUT_SCHEMA_EXEMPLARS` rule (D8-D; cross-reference CORRECTED/ADDED 2026-08-28, review-fix cycle 3).** In particular `{{PRIMARY_LANGUAGE_LOWER}}` and `{{PRIMARY_LANGUAGE}}` MUST be bound, not emitted literally, and the four `{{LANGUAGE_*_CHECKS}}` variables MUST be bound to the verbatim D8-B values (below). But a **bare** "zero unresolved `{{...}}`" assertion is **impossible as written** for this unit and would fail permanently: `technology-reviewer.agent.md.tmpl` **L42/L43**, `security-reviewer.agent.md.tmpl` **L48/L49**, and `agent-native-parity-reviewer.agent.md.tmpl` **L34/L35** each carry output-schema exemplars inside a fenced JSON block. The assertion therefore reads:
  * **exempt iff BOTH** (a) the token is a member of the **closed 3-token allow-list** — `{{file_path}}`, `{{line_number}}`, `{{principle_number}}` — **and** (b) it is inside a fenced output-schema code block. Neither condition alone suffices. (Only the first two of the three occur in this unit's templates; the allow-list is stated in full because it is one shared rule, not a per-unit list.)
  * These are **exemplar tokens intentionally retained** as literal persona content describing the JSON a reviewer emits at review time. They have **no install-time value** and **MUST NOT be bound to fabricated content**.
  * **Every other `{{...}}` match is a genuine unbound variable and MUST fail** — this exemption gives Ship **no** licence to leave `{{PRIMARY_LANGUAGE}}`, `{{PRIMARY_LANGUAGE_LOWER}}`, or any `{{LANGUAGE_*_CHECKS}}` token unresolved.
  * Authoritative definition is plan **D8-D**, pinned by U3 (`148.003-T`). This unit consumes it; it does not redefine or widen it. Distinct from U8 scenario 2's route exemption and from U1's `EXEMPT_POLICY_PROSE_META_TOKEN` registry rule (D9-B).
* No installed copy hand-patched relative to its template beyond variable binding.

**ADDED 2026-08-28 (review-fix cycle 2, P2-2) — verbatim binding contract:**

* The `python-reviewer` render MUST bind all four synthesized persona variables to the **verbatim D8-B values pinned in U3** (`148.003-T`):
  * `{{LANGUAGE_SAFETY_CHECKS}}`
  * `{{LANGUAGE_IDIOM_CHECKS}}`
  * `{{LANGUAGE_ERROR_HANDLING_CHECKS}}`
  * `{{LANGUAGE_PERFORMANCE_CHECKS}}`
* Ship MUST NOT **synthesize, paraphrase, extend, reorder, or abridge** these values, and MUST NOT emit them unresolved. Any deviation from the D8-B text is a **HARD STOP**, not a judgement call. This is the guard against unreviewed persona content reaching a normative review surface.
* `templates/agents/review/technology-reviewer.agent.md.tmpl` MUST remain **unmodified** — binding happens at render time into the installed artifact only (D8-C). Editing the template to inline Python content FAILS this unit.
* Verified by U8 scenario 5.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Plan unit **U5**. Render 4 security and parity personas into `.github/agents/subagents/`:

| Installed persona | Source template |
|---|---|
| `python-reviewer.agent.md` | **`templates/agents/review/technology-reviewer.agent.md.tmpl`** (existing; mapping pinned in U3) |
| `security-reviewer.agent.md` | `templates/agents/review/security-reviewer.agent.md.tmpl` |
| `security-lens-reviewer.agent.md` | `templates/agents/review/security-lens-reviewer.agent.md.tmpl` |
| `agent-native-parity-reviewer.agent.md` | `templates/agents/review/agent-native-parity-reviewer.agent.md.tmpl` |

**CORRECTED 2026-08-28.** The prior revision sourced `python-reviewer` from a new template to be authored in U3. No such template is authored. The source is the **existing** `technology-reviewer` template, rendered through the mapping declared in installed `install-harness/SKILL.md` L1203 with `PRIMARY_LANGUAGE_LOWER = python`.

**Note the deliberate name change across the render**: template basename `technology-reviewer` -> installed filename `python-reviewer`. This is correct and required; it is not drift.

**Execution posture**: mechanical render, LF-only, no hand-patching. "Mechanical" is now literally true for this unit: as of the 2026-08-28 review-fix cycle 2 correction, **every** placeholder in the source template has a pinned binding (U3 / D8), so no value is left to Ship's discretion.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
**Depends on U3** (`148.003-T`) — U3 pins the `technology-reviewer` -> `python-reviewer` mapping and its placeholder bindings. U3 no longer authors a template, so this dependency is now a *mapping and binding* prerequisite rather than a *file* prerequisite; the edge is retained because the render must not be improvised.

**Strengthened 2026-08-28 (P2-2)**: the U3 edge is now load-bearing for a second reason. Four of this template's placeholders (`{{LANGUAGE_SAFETY_CHECKS}}`, `{{LANGUAGE_IDIOM_CHECKS}}`, `{{LANGUAGE_ERROR_HANDLING_CHECKS}}`, `{{LANGUAGE_PERFORMANCE_CHECKS}}`) are classed by installed `install-harness/SKILL.md` L329-L332 as *"Synthesized from language ... model"*. Without U3's D8-B pins, this unit could not be executed mechanically at all — Ship would have to invent normative review-persona content. With the pins, the render is a substitution.

RK-E: installing the security personas makes `plan-review` security dispatch newly *available*, changing future review behavior. This is the intended effect and is what unblocks S2/S10. It changes no exit code and promotes nothing to blocking.

RK-I: because the installed filename differs from the template basename, U7's manifest entry for `python-reviewer.agent.md` must record `template: "agents/review/technology-reviewer.agent.md.tmpl"` explicitly, or a provenance check will misread the pair as drift.

RK-J (new): the D8-B values for the four `LANGUAGE_*_CHECKS` are Stage-reviewed prose rather than resolver-derived, and are authoritative for this shipment only. Recorded in the plan; not a blocker for this unit.

**2-hour rule**: unchanged at 4 mechanical renders. The D8 pins **reduce** the work in this unit (no synthesis required), so **size remains S / complexity trivial**. No split required.

Manifest registration is U7, not here.
<!-- END:implementation-notes -->