---
artifact_type: task
created_at: 2026-08-17T23:34:34.6389711Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 129.001-T
    - 129.002-T
    - 129.003-T
id: 129.004-T
labels:
    - commit-b
    - preflight
    - backup
    - gate
    - containment
    - BED0DDED
parent_id: 129-F
priority: medium
references:
    - docs/plans/2026-08-17-backlogit-self-migration-plan.md
    - docs/plans/2026-08-17-backlogit-self-migration-hardening.md
status: rejected
title: Pre-flight idle gate, inventory snapshot, and contained in-repo backup
updated_at: 2026-08-18T05:03:31.7628077Z
---

START OF COMMIT B. Verify the H1 idle gate as a HARD precondition (any failure HALTS the shipment; it does not downgrade to a warning): P1 exactly one worktree via git worktree list; P2 working tree clean apart from intended changes; P3 operator confirmation that no other agent session is active and no active shipment other than this one; P4 zero active checkpoints via backlogit checkpoint list; P6 Test-Path .backlog is False. CORRECTED 2026-08-17 (supersedes the prior out-of-tree $env:TEMP backup, which was a Constitution Principle IV / CLI-containment violation and a P1 defect): the backup MUST be written INSIDE the working directory and OUTSIDE both root-level storage candidates .backlog and .backlogit. Canonical backup root: .copilot\session-state\7ced3fcb-faba-47fb-81f9-09e0670a393f\files\backlog-premigration-<UTC-timestamp>\ (basename MUST NOT be .backlog/.backlogit and MUST NOT begin with a dot). BEFORE COPYING ANY BYTE, run and record containment gates G1-G6 (see hardening H4; all six passed at staging time): G1 canonical containment -- GetFullPath(BACKUP_ROOT) starts with the repo root plus a separator; G2 no FILE_ATTRIBUTE_REPARSE_POINT on any path segment from repo root down; G3 ignored -- git check-ignore -v returns a matching rule (.gitignore:4:*.copilot); G4 unstageable -- git status --porcelain does not surface it AND git add without -f exits non-zero with nothing staged; G5 non-candidate naming -- basename not in {.backlog,.backlogit} AND a post-copy recursive scan finds ZERO directories named .backlog/.backlogit at any depth inside the backup; G6 resolver-undiscoverable -- resolve_backlog_root(BACKUP_ROOT) and resolve_backlog_root(parent) both raise BacklogUnavailableError, resolve_backlog_root(repo) still returns the live root, and backlogit --cwd BACKUP_ROOT errors and creates nothing. If ANY gate fails, HALT and select another existing ignored in-repo path (documented fallback .autoharness/staging/, ignored via .gitignore:6) and re-run G1-G6 in full -- NEVER write outside the working directory to work around a failed gate. G5 IS LOAD-BEARING (negative control proved that creating files\.backlogit\ made resolve_backlog_root(files) return that directory): copy the CONTENTS of .backlogit\* into BACKUP_ROOT, NEVER the .backlogit directory itself -- Copy-Item .backlogit -Destination X -Recurse creates X\.backlogit and violates G5. Capture: (a) full recursive relative-path + size inventory of .backlogit (expect 1656 files, of which 1613 are git-tracked; archive/ = 820; queue/ = 12); (b) counts by artifact_type and status from backlogit query; (c) checkpoint count (expect 32, zero quarantined) and stash entry count; (d) the byte-for-byte contents copy. BACKUP VERIFICATION IS A BOUNDED FILE-COUNT / INVENTORY CHECK: backup file count equals source inventory count (1656); the set of relative paths under BACKUP_ROOT equals the set under .backlogit with an EMPTY symmetric difference; per-path size equality for every Markdown/JSONL/YAML artifact (backlogit.db/-wal/-shm byte differences are EXPECTED and EXCLUDED per H13); archive/=820 and queue/=12 subtotals match; write backup-manifest.json recording count, relative-path inventory, sizes and the six gate results. Any mismatch means the backup is INVALID -- HALT; 129.005-T and 129.006-T MUST NOT run. PRESERVATION: because the backup is deliberately gitignored it would be destroyed by git clean -x/-X -- git clean with -x or -X is PROHIBITED, and plain git clean -fd is prohibited, for the entire window from this task until operator-approved disposal; Copilot session state MUST NOT be cleared or pruned (H15); Ship MUST re-assert backup existence and the file-count/inventory check immediately before 129.006-T, at the same point the H2 lock probe is re-run, and a missing or mismatched backup at that moment HALTS the shipment. Backup disposal remains operator-gated (H15), never automatic, never before merge and 129.009-T verification. ACCEPTANCE: G1-G6 all recorded PASS; backup exists at the contained in-repo path; bounded inventory verification passes; backup-manifest.json written; absolute backup path recorded in the task record, runbook and handoff. An unverified backup is not a rollback capability. Plan T004; hardening H1, H4, H15.