---
archived_from: .backlogit/queue/146.001-T.md
archived_status: done
artifact_type: task
created_at: 2026-08-23T23:49:57.003779Z
custom_fields:
    complexity: low
    size: XS
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
id: 146.001-T
labels:
    - docs
    - compound
    - corpus
    - p-021
parent_id: 146-F
priority: low
references:
    - docs/plans/2026-08-23-docs-compound-source-value-semantics-plan.md
    - docs/compound/2026-08-09-next-eligible-detail-scoping-and-vacuous-tiebreak-tests.md
status: archived
title: Correct the outlier `source` value and relocate its provenance verbatim into `citations`
updated_at: 2026-08-24T00:57:01.9454397Z
---

Plan: `docs/plans/2026-08-23-docs-compound-source-value-semantics-plan.md` (Task 1, as amended by A2 and A4).

Surface: **corpus data** (`docs/compound/`, one file). Width-isolated from the test surface (146.002-T) and the template family (146.003-T) per P-003 / 026-DL R6.

## Steps

1. **RE-MEASURE FIRST (026-DL R5).** Re-run a full-frontmatter parse over `docs/compound/**/*.md` at execution HEAD and record the counts. Discovery MUST be RECURSIVE - `Path("docs/compound").rglob("*.md")` - the same corpus domain the existing guard in `tests/test_docs_compound_frontmatter_contract.py` already uses and the same domain 146.002-T asserts over. The authoring template writes new learnings to a `{category}` subdirectory, so a non-recursive `docs/compound/*.md` scan could miss a legal nested outlier and hand 146.002-T an incomplete exemption allowlist. The corpus grew 73 -> 75 in two days, so the 74/75 baseline MUST NOT be trusted blind.
   - Exactly one non-conforming file, and it is the expected one -> proceed. Record that 146.002-T's exemption allowlist must therefore be EMPTY.
   - **More than one** -> fix ONLY the known outlier below; capture the extras as a NEW P-021 deferred stash entry for Stage; record the exact surviving list and the capture ID and hand it to 146.002-T as its exemption allowlist. Do NOT silently widen scope. (A2)
   - **Zero** -> the finding no longer reproduces; record that outcome with evidence and close `done`. 146.002-T and 146.003-T still proceed; the shipment records this half as a verified no-op rather than implying a correction was made. (A2)
2. In `docs/compound/2026-08-09-next-eligible-detail-scoping-and-vacuous-tiebreak-tests.md` ONLY, set:
   `source: "docs/compound/2026-08-09-next-eligible-detail-scoping-and-vacuous-tiebreak-tests.md"`
3. Add a `citations` key carrying the displaced value VERBATIM:
   ```yaml
   citations:
     - "123-S / 115-F (PR #323 Copilot review)"
   ```
   Place it consistently with the template's Phase 3 key order.
4. Change nothing else. `title`, `date`, `tags`, `doc_type` survive byte-identical.

## Why adding `citations` is safe (A4)

docline's normalizer folds non-contract keys under a `docline` namespace ("move, never drop"), so adding a key could in principle invite churn. It does not here: 74 of 75 corpus files already carry non-contract keys (`title`, `date`, `tags`, and often `problem_type`, `category`, `root_cause`, `component`, `severity`, `citations`), all of which survived 140.001-T's migration with proven body invariance and verbatim key preservation. `citations` is no more exposed than `tags` already is, and it is the key the authoring contract designates for provenance.

## Preserved prior contract

140.001-T AC3 is NOT amended and NOT re-run. This is a NEW authorization on the same surface. See 026-DL.

## Acceptance criteria

- **AC1.1** `git diff` for the changed file is confined entirely to the YAML frontmatter block; **zero body bytes change** (025-DL R4). Show the diff.
- **AC1.2** The provenance string `123-S / 115-F (PR #323 Copilot review)` is still present character-for-character, now under `citations`. Evidence: grep before and after.
- **AC1.3** `title`, `date`, `tags`, `doc_type` unchanged (025-DL R5). No key other than `citations` is added. (A4)
- **AC1.4** Exactly ONE file modified. Evidence: `git diff --name-only`.
- **AC1.5** `backlogit docs lint --path docs/compound` passes.
- **AC1.6** The step-1 re-measurement is recorded in the task evidence with its counts and the recursive `docs/compound/**/*.md` discovery it used, including the zero-found and >1-found branches.