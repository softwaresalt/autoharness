---
artifact_type: task
created_at: 2026-08-03T22:59:25.5452206Z
custom_fields:
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 108.002-T
id: 108.004-T
labels:
    - telemetry
    - runtime
    - non-conflation
parent_id: 108-F
priority: medium
status: done
title: ToolTelemetryEvent runtime model + serialization for the non-conflated complexity field
updated_at: 2026-08-04T06:01:26.55398Z
---

Extend the ToolTelemetryEvent runtime model and serialization to accept and round-trip the additive optional complexity field added to the schema by 108.002-T. REQUIRED because the schema is NOT an inert forward contract: a live strict runtime model exists at src/autoharness/telemetry/tool_event.py with additionalProperties:false (_SCHEMA_PROPERTY_NAMES), plus a composer (tool_event_compose.py) and jsonl serialization (tool_event_jsonl.py), and tests cross-check to_dict()/from_mapping() against the ratified schema (ToolTelemetryEventSchemaConformanceTests, round-trip tests). Add task_complexity_label/complexity_source to _SCHEMA_PROPERTY_NAMES, the dataclass fields, and to to_dict/from_mapping (nullable-safe, provenance-aware), then thread the field through the composer and the jsonl path. Extend the runtime tests (test_telemetry_tool_event.py, test_telemetry_tool_event_compose.py, test_telemetry_tool_event_jsonl.py) so schema-conformance and round-trip cross-checks cover the new field. No emitter wiring (084-F owns emission). Depends on the schema change (108.002-T). Family: python-runtime. Acceptance: to_dict()/from_mapping() round-trip preserves the field and its provenance; a record omitting the field stays valid (backward-compatible); the schema-conformance test passes against the updated schema; no emission call-site changes.