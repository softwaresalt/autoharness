---
artifact_type: task
commit: 60bf0c2e
created_at: 2026-09-15T22:16:40.1355997Z
custom_fields:
    complexity: high
    size: M
    size_ruleset_version: ah-stage-sizing-v1
    size_source: agent
dependencies:
    - 166.003-T
    - 166.001-T
    - 166.004-T
id: 166.005-T
labels:
    - skill
    - shipment-reconcile
    - close-mechanism
    - template-parity
parent_id: 166-F
priority: medium
status: done
title: 'Safe-close Steps 2/3/5/6/8: baseline-invariance gate + approval-gated rollback + fail-closed record-transition halt (+ template mirror)'
updated_at: 2026-09-16T22:55:45.4073393Z
---

REFRAMED IN PLACE 2026-09-15; CORRECTED 2026-09-16 (plan revision 2); CORRECTED AGAIN 2026-09-16 (plan REVISION 4 — group B evidence-honesty propagation). Supersedes the Terminal-Close Sub-Procedure. NO sub-procedure and NO third verdict are added. Authoritative plan: docs/plans/2026-09-15-flat-manifest-shipment-closure-plan.md (unit U5, REVISION 4). Authoritative decision: docs/decisions/2026-09-15-flat-manifest-shipment-closure-deliberation.md (revision 2; D6 governs rollback).

DEPENDENCIES (machine DAG, authoritative): depends on 166.001-T + 166.003-T + 166.004-T. (CORRECTED 2026-09-16: the previous prose "Depends on 166.003-T + 166.004-T" OMITTED 166.001-T and disagreed with the machine DAG. The machine DAG is authoritative and is unchanged; only the prose was wrong.)

SURFACES (identical edits to BOTH, byte-parity required):
- .github/skills/shipment-reconcile/SKILL.md
- templates/skills/shipment-reconcile/SKILL.md.tmpl

SCOPE: Safe-Close Mode Steps 2, 3, 5, 6 and 8.

CHANGES
1. STEP 2 — the protected set becomes an OBSERVATION SET. Out-of-manifest artifacts sharing the covering feature's hierarchy are enumerated in order to be WATCHED for change, NOT in order to be REQUIRED PRESENT. DELETE the sequence-aware-exclusion provenance requirement (predecessor shipment must carry archived_status: shipped|done) entirely — INV-3 makes it moot, because an out-of-manifest artifact may be in ANY state and still must not gate closure.
2. STEP 3 — REPLACE the baseline-integrity PRESENCE gate ("confirm every protected-set member currently exists in queue/; if any is already in archive/ or missing, halt with HALT — cascade detected") with the INV-7 BASELINE-INVARIANCE gate: record each observed artifact's baseline LOCATION and CONTENT HASH alongside the existing `git status --short -- ".backlogit/"` capture. An observed artifact that is already archived, descoped, or missing AT BASELINE is RECORDED AS BASELINE STATE and is explicitly NOT A HALT — it carries no signal about THIS run. This is the change that closes the same class for 168-S (stash 3CA122AC) and that RETROSPECTIVELY explains the 173-S deadlock (173-S itself was closed by operator action on 2026-09-16 and is now a regression fixture, not a live blocker).
3. STEP 5 — the verify-after-each invariant compares against the BASELINE FINGERPRINT (did anything outside closure scope CHANGE relative to baseline?) rather than against "still present in queue/". A change to any observed artifact — relocation, deletion, content edit, or a cleared parent_id — is a cascade and halts. Retain the git-status cross-check. NOTE: the cleared-parent_id case is the in-workspace detector for the separate upstream defect tracked by durable ACTIVE stash entry 63363CF5.
4. STEP 6 — REWRITE THE ROLLBACK ACTION TO THE D6 APPROVAL-GATED SEQUENCE. (REVISION-2 CORRECTION: revision 1 instructed "PRESERVE UNCHANGED ... Step 6's git-revert-on-cascade action". That instruction is WITHDRAWN and MUST NOT be carried forward. An automatic, immediate, or notification-only `git restore`/`git revert` is a DESTRUCTIVE ACTION taken without operator approval, executed at the exact moment the agent's model of the workspace has been PROVEN WRONG by a detected cascade — the worst possible moment to trust an unvalidated path list.) Step 6 MUST become exactly this six-step sequence, in this order, with no step skippable and no step reorderable:
   (1) CAPTURE EVIDENCE FIRST — record the detected cascade, the exact diverging artifact IDs, their baseline vs observed location and content hash, the full `git status --short -- ".backlogit/"` output, and the command that caused the divergence.
   (2) HALT IMMEDIATELY — perform NO further mutation of any kind, including no rollback.
   (3) EMIT P-005 — raise the policy-violation telemetry event with the captured evidence attached.
   (4) REQUEST EXPLICIT OPERATOR APPROVAL — present the proposed rollback, the exact paths it would touch, and what would be lost. Notification is NOT approval; silence is NOT approval; an absent operator means the halt STANDS.
   (5) REVALIDATE AFTER APPROVAL — re-read the workspace and re-confirm that the exact paths and their state still match what the operator approved. If ANYTHING has changed since approval, HALT AGAIN and re-request.
   (6) EXECUTE ONLY THE APPROVED ROLLBACK — restricted to the revalidated paths, nothing wider.
   The skill text MUST NOT contain any automatic, immediate, or notification-only destructive-revert language on this or any other step.
5. STEP 8 — add the fail-closed halt for the residual R1 gap (plan-review P1-3). When `backlogit move <shipment_id> --status shipped` is refused (backlogit 1.10.1 returns exit 9: "shipment must be shipped via ShipShipment, not a direct status update") AND the manifest is NOT cascade-eligible under Step 0(c), HALT with a NEW code RECONCILE_FAIL_NO_SAFE_RECORD_TRANSITION. The text MUST state: (a) there is no known safe path to archived_status: shipped for this shape in 1.10.1 — this is INV-11, an EXTERNAL RUNTIME PREREQUISITE tracked by durable ACTIVE stash entry 7F9CB5E9 (successor to the too-broadly-archived 2B42392E), and it BLOCKS operational multi-shipment/split delivery even though INV-4/INV-5 make it contract-complete; (b) the cascade op MUST NOT be substituted, because the engine is BELIEVED (INDICATIVE, UNPROVEN — REVISION 4: this was previously and wrongly worded "the measured engine RETURNS ... and SILENTLY CLEARS") to return live out-of-manifest siblings and to clear their parent_id, orphaning them — tracked separately as 63363CF5. This belief is stated as RATIONALE for refusing the substitution; it is NOT a measured fact and, being unproven, it can only ever justify the FAIL-CLOSED refusal, never a CASCADE; (c) archiving an active shipment record stamps archived_status: active and fails the existing provenance gate; (d) this is an unresolved UPSTREAM TOOLING GAP requiring operator escalation. NO silent workaround, NO retry, NO fallback, NO hand-edit of the record.
6. ADD the negative scenarios to the Deterministic Safe-Close Scenario Matrix: (i) out-of-manifest descendant status: done -> SAFE_CLOSE; (ii) out-of-manifest descendant live queued -> SAFE_CLOSE; (iii) out-of-manifest descendant in archive/ with no status -> SAFE_CLOSE; (iv) out-of-manifest live GRANDCHILD -> SAFE_CLOSE; (v) observed artifact archived AT BASELINE -> recorded, NOT a halt; (vi) observed artifact changed DURING the run -> halt, then the D6 approval-gated sequence (NEVER an automatic revert); (vii) partial/intermediate shipment with refused record transition -> RECONCILE_FAIL_NO_SAFE_RECORD_TRANSITION with no cascade substitution; (viii) out-of-manifest descendant declaring a NON-CANONICAL archived variant (`Archived`, `ARCHIVED`, `" archived "`) -> SAFE_CLOSE; (ix) TORN out-of-manifest descendant present in both queue/ and archive/ -> SAFE_CLOSE.
7. PRESERVE UNCHANGED: Step 4's per-item archive loop and its manifest-items-only pre-archived classification; Steps 7/9/10; and the explicit non-goal that clearing parent_id is REJECTED and must never be introduced as an alternative or migration step.

EVIDENCE RULE — REPLACED IN REVISION 4 (D7a / D13 / plan R24 / H17). *** THE PRIOR RULE ("Ground every claim in the in-workspace fixtures from 166.002-T") IS WITHDRAWN AS UNSOUND *** — those are CLASSIFIER fixtures calling a pure Python function; they never invoke the backlogit Go engine and CANNOT ground an ENGINE claim. CLASSIFIER LAW (provable by 166.002-T's fixtures) and ENGINE LAW (not provable by any fixture) are DISJOINT. The ONLY authoritatively held engine proposition is sourced to the PATH-SCOPED git comparison between 358b63b4 and e4ca20e5 (empty diff + IDENTICAL blob OIDs for the two excluded siblings), NOT to the unfiltered `git show --stat e4ca20e5` diffstat (34 paths; COMBINED PUBLICATION COMMIT, not close-only). All other engine behaviour — including returned_ids contents and parent_id clearing — is INDICATIVE AND UNPROVEN: cite as rationale, never as measured fact, and never as authorization for a CASCADE. The external %TEMP% spike evidence remains NON-AUTHORITATIVE (P-005 containment/destructive-approval violation).

ACCEPTANCE: 166.002-T Part B doc-contract tests pass against BOTH mirrors, including the approval-gated-rollback assertion and the negative assertion that no automatic/notification-only revert language remains; both mirrors byte-identical on every changed block; no unresolved {{...}} placeholders introduced.

SIZE: M. COMPLEXITY: high.