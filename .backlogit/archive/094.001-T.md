---
artifact_type: task
commit: 612f47d29c88644b0c9c2cd9fecb06922c439610
created_at: 2026-07-29T07:18:39.3369748Z
id: 094.001-T
labels:
    - agent-engram
    - documentation
parent_id: 094-F
priority: medium
references:
    - .claude/instructions.md
    - .mcp.json
status: done
title: Replace stale engram HTTP-endpoint note in .claude/instructions.md with stdio-transport note
updated_at: 2026-07-29T07:37:41.8694284Z
---

<!-- BEGIN:description -->
Fix the stale engram transport note in `.claude/instructions.md`. Line 4 currently reads: "Engram is running as an MCP server at `http://127.0.0.1:7437/mcp`." This is wrong — `.mcp.json` registers engram as a stdio MCP server (`type: stdio`, `command: engram`, `args: ["shim"]`). Replace the HTTP-endpoint sentence with a transport-neutral note reflecting the stdio registration and remove any HTTP/port access directive.

Suggested replacement (implementer may refine wording, must preserve intent):
"Engram is registered as a stdio MCP server in `.mcp.json` (command `engram shim`); access it through the registered Engram MCP tools. There is no HTTP endpoint or port. Full protocol: `.github/instructions/agent-engram.instructions.md`."

Single file only: `.claude/instructions.md`. Do NOT touch schemas, CLI, templates, or `agent-engram.instructions.md(.tmpl)` (already transport-neutral). Est. ~20 min. Plan: docs/plans/2026-07-29-engram-claude-instructions-transport-fix-plan.md.
<!-- END:description -->

<!-- BEGIN:acceptance-criteria -->
1. `.claude/instructions.md` no longer contains `http://127.0.0.1:7437/mcp` or any HTTP/port access directive for engram.
2. Replacement text reflects the stdio transport per `.mcp.json` and is transport-neutral (no invented port).
3. Cross-reference to `.github/instructions/agent-engram.instructions.md` is present.
4. No other files modified; `grep 7437` across the owned tree returns no live agent-facing config hit (stash/archive historical references excluded).
<!-- END:acceptance-criteria -->

<!-- BEGIN:notes -->
Documentation-only change: no application/test code. The TDD red phase (P-002) reduces to a grep/absence assertion (AC #1/#4) rather than a unit test; harness-architect may treat the verification as a content assertion.

RESIDUAL RISK (out of scope, flag for follow-up stash): the same block still lists `create_task` / `update_task` / `query_changes` in its engram tool table — drift already corrected for `copilot-instructions.md` by archived task 036.005-T but never mirrored to `.claude/instructions.md`. Excluded here to keep this fix width-isolated to the transport defect named in stash 8FD768E9.
<!-- END:notes -->