---
artifact_type: task
commit: a22c2c77
created_at: 2026-08-10T07:08:36.0647872Z
custom_fields:
    complexity: medium
    origin_feature: 117-F
    size: M
    size_ruleset_version: 2h-width-isolated-v1
    size_source: agent
id: 118.001-T
labels:
    - shipment-1
    - P0
    - characterization
    - test-only
    - powershell
    - H1
parent_id: 118-F
priority: critical
references:
    - start.ps1
    - docs/plans/2026-08-09-copilot-cli-supervisor-control-plane-plan.md
status: done
title: T1 Characterization suite for start.ps1 observable launch contract
updated_at: 2026-08-12T23:56:34.7492602Z
---

SIZE: M. COMPLEXITY: medium. (Two independent axes; also emitted as structured backlog fields.) Shipment 1 (P0). TEST-ONLY, zero production change. Pin the CURRENT observable contract of start.ps1 BEFORE any logic moves to Python (hardening H1: characterize-before-migrate is a hard ordering constraint). Assert the observable contract, not script structure, so the future shim can satisfy the identical suite: (a) .env.local parsing - KEY=VALUE lines only, single matching surrounding quote pair stripped, NO-CLOBBER precedence (an already-set process variable wins); (b) COPILOT_HOME defaults to <root>/.copilot and ENGRAM_DATA_DIR to <root>/.engram, each honoring a pre-set value; (c) TOKEN RESOLUTION IS TWO DIFFERENT CONTRACTS AND MUST BE PINNED AS TWO (F17 re-inventory, verified against the working tree): GITHUB_TOKEN is resolved ONLY when unset, behind a guarded Get-Command gh plus try/catch, and IS non-fatal when gh is absent or fails; GITHUB_PERSONAL_ACCESS_TOKEN is assigned UNCONDITIONALLY AND UNGUARDED at start.ps1:65, so when gh is absent the statement ERRORS and when gh fails the variable is set EMPTY. THIS PAT-WITHOUT-gh FAILURE IS PINNED AS BASELINE EVIDENCE ONLY - IT IS NOT A PERMANENT PRODUCT MANDATE (P1-B ruling, 2026-08-12). Its sole purpose is to record, with a test, what the legacy script does TODAY so the migration delta is provable rather than asserted. The TARGET behavior is defined by 120.001-T: PAT setup is NON-FATAL when gh is absent, emitting an explicit warning/event and exposing no secret. This assertion is therefore an APPROVED-DELTA scenario and is expected to be REPLACED - not preserved - at 120.007-T; do not read it as a requirement that the error survive the migration. Pin the behavior that EXISTS, not the behavior that ought to exist - changing the PAT path is an EXPLICIT BEHAVIOR CHANGE, is out of scope for Shipment 1's zero-behavior-change contract, and is deferred to Shipment 3 where it is intentional and approved; (d) Copilot exe resolution order COPILOT_EXE_PATH -> COPILOT_EXE -> PATH, and hard failure with the current actionable message when unresolvable; (e) --remote appended ONLY when COPILOT_USE_REMOTE is 'true' or '1' AND the operator did not already pass --remote; (f) operator argv forwarded VERBATIM to the child; (g) child exit code propagated unchanged; (h) SIDECAR SIDE EFFECTS ARE OBSERVABLE AND MUST BE PINNED (F17): backlogit sync runs when backlogit resolves, and Engram pre-warm runs DIRECT sync first with a bind + daemon-sync fallback, each non-fatal with a warning - these migrate to 120.002-T and cannot be characterized as absent; (i) TERMINAL ATTACHMENT (F29 ruling): the child is launched with INHERITED stdio handles and therefore observes an interactive terminal - assert TTY attachment explicitly via the child's own isatty() result, because it is the single property the migration is most likely to break silently. Mock gh / backlogit / engram / copilot; no real Copilot invocation. ACCEPTANCE: suite passes against today's start.ps1 unmodified, and is later re-run by 120.007-T BYTE-IDENTICAL FOR EVERY UNCHANGED SCENARIO. The ONE approved exception ON THIS WINDOWS SUITE is the PAT-without-gh scenario above, whose assertion is replaced at 120.007-T by the approved-delta assertion for the new non-fatal warning behavior. (There are THREE named approved deltas in total across the migration, stated as a platform/scenario matrix - ruling A, 2026-08-12, superseding the earlier two-delta framing. THIS one is WINDOWS_PAT_NO_GH and it lands HERE, on the WINDOWS baseline pinned by this task - it is NOT a POSIX delta, and any wording that classifies it as POSIX is a defect. The other two land on the POSIX suite 118.002-T: POSIX_ENGRAM_DATA_DIR and POSIX_PAT_BOOTSTRAP, where 120.001-T supplies the ENGRAM_DATA_DIR default and PAT resolution that start.sh lacks. 120.007-T is the authority on both.) Any OTHER need to edit an assertion during migration is escalated as an operator product decision, not an implementation detail.