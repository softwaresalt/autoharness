---
archived_from: .backlogit/queue/168.005-T.md
archived_status: queued
artifact_type: task
created_at: 2026-09-18 04:50:22.274321+00:00
custom_fields:
  complexity: medium
  size: M
  size_ruleset_version: ah-stage-sizing-v1
  size_source: agent
dependencies:
- 168.001-T
id: 168.005-T
labels:
- test
- p-004
- retired
- superseded
- never-executed
parent_id: 168-F
priority: high
retired_at: '2026-09-20'
retired_never_executed: true
status: archived
title: 'RETIRED (superseded design, never executed; re-harvest only after 185-S and 187-S ship) - P-004 T5a (RED): import-safe thirteen-token contract tests observed failing before the gate exists'
updated_at: 2026-09-19 00:19:48.568115+00:00
---

NEVER CLAIMED, NEVER EXECUTED, NEVER SHIPPED. This record is archived directly from the queued state. No task in it was ever claimed, no commit was ever produced under it, and NOTHING HERE MAY BE READ AS IMPLYING THIS UNIT SHIPPED. Its archival is a retirement, not a completion.

SUPERSEDED DESIGN, NEVER EXECUTED. This record encodes the SUPERSEDED P-004 architecture. The governing plan docs/plans/2026-09-18-p004-observation-gate-plan.md reduced its own scope at revision 1 - bootstrap, schema, storage lifecycle, identity and MCP parity moved to the foundations that own them - but the carriers were never reconciled, so they continued to instruct an executor to build the old design. Independent plan-review attempt 01 recorded this as finding O1 (P0): a shipment manifest is an EXECUTABLE INSTRUCTION SET, not commentary, and at claim time Ship reads the manifest, not the plan's Reduction section.

ORIGINAL DEFECT LINKAGE PRESERVED. The underlying defect this unit was created to address - that policy P-004 declares a red-phase precondition with no executable producer - REMAINS OPEN and is carried forward by the revision-7 plan. Retiring the carrier retires the SUPERSEDED DESIGN, not the requirement.

RE-HARVEST, NOT RESTORATION. The P-004 red-phase gate is still wanted. It is now governed by docs/plans/2026-09-18-p004-observation-gate-plan.md at REVISION 7, which is deliberately WITHHELD FROM HARVEST and has NO live tasks and NO shipment. Stage will create a FRESH feature, task set and shipment only after BOTH foundation shipments 185-S and 187-S have actually shipped AND the plan holds an independent PASS. THIS RECORD IS NEVER RESTORED: the fresh harvest is authored against the then-current repository state. Retaining no queued shipment here is deliberate - an empty or perpetually blocked queued shipment is itself a defect.

NO RESTORATION PATH. This record MUST NOT be restored, re-harvested or revived by any restoration clause, anywhere. Its deliverable already exists on this branch, its stated root cause is known to be false, and its graph position was circular. Any future work on harness-architect conformance must be harvested fresh against the then-current repository state, never by reviving this unit.

TDD RED PHASE. Author the composed state-machine contract tests for all THIRTEEN P-004 gate tokens BEFORE P004Result, validate_declared_harness_set() and run_p004_gate() exist, and OBSERVE THEM RED. One concrete legitimate passing state and one concrete failing state per token: P004_UNEXPECTED_GREEN, P004_UNEXPECTED_RED, P004_MISSING_OBSERVATION, P004_UNDECLARED_OBSERVATION, P004_MARKER_ABSENT, P004_EMPTY_RED_SET, P004_SET_OVERLAP, P004_DUPLICATE_DECLARATION, P004_MARKER_MISPLACED, P004_INCONCLUSIVE_OUTCOME, P004_DECLARED_ID_MISMATCH, P004_HARNESS_SET_MISSING, P004_HARNESS_SET_STALE.
IMPORT SAFETY (BINDING, AND THE REASON THIS TASK EXISTS AS WRITTEN): a RED test module that raises at IMPORT time does NOT produce a red observation. The loader records a _FailedTest placeholder and the gate classifies it P004_MISSING_OBSERVATION, which is a MISSING OBSERVATION and NOT valid red - so a RED task whose tests cannot be collected FAILS ITS OWN GATE. This module is therefore authored so that (1) every module under test that does not yet exist is imported INSIDE the test body through a small _load_boundary() helper, never at module top level; (2) each declared RED test INDIVIDUALLY executes and fails or errors on its own, producing ITS OWN detail_text containing ITS OWN declared marker; (3) the module imports cleanly under unittest.defaultTestLoader with ZERO loader.errors, asserted directly; (4) each test carries the exact marker string recorded for it in the shipment declaration.
REQUIRED CASES: (1) the crossed-marker two-red-test case, where each test fails with the OTHER test's declared marker, emitting P004_MARKER_ABSENT naming BOTH ids; (2) a @unittest.skip-ped declared id emitting P004_MISSING_OBSERVATION; (3) a module- or class-addressed declared name yielding several leaf tests, emitting P004_MISSING_OBSERVATION for the declared id and P004_UNDECLARED_OBSERVATION for each surplus id; (4) a _FailedTest whose id() does not equal the requested name, still ATTRIBUTED to the requested name, proving result correlation is by test-object identity and not by string comparison; (5) SEPARATELY from case 4, a successfully loaded TestCase whose id() does not equal the requested test_id emitting P004_DECLARED_ID_MISMATCH - declaration conformance is enforced INDEPENDENTLY of identity-based result attribution, and neither check may stand in for the other; (6) a two-name load in which only the SECOND name fails, attributing the load error to that name alone, proving per-name loader.errors slice attribution; (7) SUBTEST CASES - a declared expected_red test whose only failing path is a self.subTest block must be observed as a FAILURE; a test with three subtests of which the second and third fail must produce a detail_text concatenating both failing subtests' formatted text in call order with a marker declared against the third matched; a test whose subtests all pass must remain success; (8) TOTAL OUTCOME MATRIX - every declared class crossed with every observed outcome, INCLUDING expectedFailure and unexpectedSuccess. A declared expected_red test decorated @unittest.expectedFailure that fails is reported by TestResult as an EXPECTED FAILURE, NOT a failure, and a declared expected_green_characterization test that unexpectedly succeeds is reported as an UNEXPECTED SUCCESS: both are DETERMINISTICALLY FAIL-CLOSED as P004_INCONCLUSIVE_OUTCOME, never silently folded into pass or into red. No declared-class x observed-outcome cell is left undefined; (9) SCHEMA-ERROR-MAPPING CASES - validate_declared_harness_set() converts the jsonschema required error for a missing marker into P004_MARKER_MISPLACED naming the offending EntrySubject, the additionalProperties error for a surplus marker into the same token naming its entry, and the minItems error into P004_EMPTY_RED_SET naming the CollectionSubject, with every assertion made on the returned token value and NEVER on exception message text; plus a synthetic ValidationError whose (absolute_path, validator) pair matches no mapping row raising P004_DECLARATION_SCHEMA_ERROR and HALTING; (10) SUBJECT-MODEL ASSERTIONS - one per subject kind, proving a token names exactly one of TestSubject, CollectionSubject, EntrySubject or DocumentSubject. The impossible invariant that every token names a test_id is replaced by this model: P004_EMPTY_RED_SET is collection-level and has no test_id to name, and P004_MARKER_MISPLACED can fire on an entry that carries no test_id at all.
Tests import the real boundary and MUST NOT re-implement the comparison. Scope: tests/. Blocked by 168.001-T (declaration shape) ONLY. This task does NOT depend on 168.007-T or 168.012-T: red precedes implementation, and reversing that would make this a test-after task wearing a red-phase label. Stash 76EBDE6D. Plan docs/plans/2026-09-17-p004-red-phase-precondition-scoping-plan.md (plan_id p004-red-phase-precondition-scoping, plan_role active, revision 7). Verdict manifest docs/reviews/2026-09-17-p004-red-phase-precondition-scoping-plan-review.md (latest_attempt 07, verdict REMEDIATED-PENDING-REVIEW at plan revision 7, awaiting independent attempt 08).
