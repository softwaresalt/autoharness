---
artifact_type: review
created_at: 2026-08-05T00:37:00Z
id: 109.003-R
labels:
    - plan-review
    - plan-harden
    - p-006
    - topology
    - review-fix-cycle-3
parent_id: 109-F
status: done
title: 'Cycle-3 plan-harden + plan-review: 109-F P1 sequencing-timing + shipment-scoped gate — PASS'
updated_at: 2026-08-05T00:37:00Z
---



<!-- BEGIN:decisions -->
APPROVED (repairs verified). Cycle-3 review-fix of 109-F — the THIRD and FINAL bounded
cycle — resolves both high-severity (P1) findings on reviewed HEAD b2efcf3 (P0=0, P1=2).
No new gates bypassed; the two-axis 2h granularity gate holds (109.019-T moved is S/low,
single agent-template family; all amended tasks remain <=2h and width-isolated within
their shipment). Serial sequence remains A(114-S)->B(115-S)->C(116-S) via supported blocks
edges; all three shipments stay `status: queued`; 114-S is the single eligible cursor.
Task-only manifests preserved; every one of the 19 feature tasks appears exactly once.
Route claude-opus-4.8/anthropic/high, DARK_MODE_ACTIVE. No source/template/config mutated
by Stage; only backlog + planning + memory artifacts touched. Both P1s are RESOLVED, not
deferred.
<!-- END:decisions -->

<!-- BEGIN:findings -->
P1-1 (sequencing repair ships too late — 109.019-T was in 115-S but the installed
Orchestrator attempts the invalid `blocked -> queued` cursor-advance immediately AFTER
114-S closes, before 115-S could run the correction) — RESOLVED. 109.019-T (B8 sequencing
correction) MOVED from 115-S to 114-S so the corrected queued-from-start + blocks-suppression
contract is INSTALLED by shipment A, before the 114 -> 115 transition. Membership updated at
the manifest level (removed from 115-S items, appended to 114-S items); unique membership
preserved. Added a MANDATORY post-predecessor-closure CONTEXT RELOAD acceptance criterion to
109.019-T and to the feature DoD: after 114-S merges and P-020 post-merge closure completes,
the Orchestrator MUST reload the current `main` agent instructions (re-read the freshly merged
_orchestrator/_ship templates) BEFORE cursor-advance / 115-S selection, so the corrected
sequencing prose is the version in effect — never a stale in-context copy. 114-S now contains
BOTH prerequisites for a correct 114->115 transition: 109.016-T (safe-close sequence-awareness,
already present) and 109.019-T (sequencing prose correction + reload mandate). Revised counts:
114-S = 9 tasks, 115-S = 7 tasks, 116-S = 3 tasks (19 total, each exactly once). 114-S has no
blocking predecessor (eligible first); 115-S blocks-on 114-S; 116-S blocks-on 115-S.

P1-2 (pre-claim gate not bound to a selected shipment — the CLI contract in 109.002-T lacked
a required shipment identifier while 109.004-T/109.017-T/109.018-T needed shipment-scoped
validation) — RESOLVED. Amended the plan to add a `--shipment <SHIPMENT_ID>` target flag to
`autoharness gate pipeline-topology`:
  - CLI PARSER (109.002-T): `--shipment` plumbed and carried on the gate domain-input
    {mode, target_shipment_id, json, force}. In agent shipment-scoped operation (pre-claim /
    route-eligibility / cursor-advance) `--shipment` is REQUIRED; missing/empty -> exit 2
    (invalid), fail-closed, never fail-open.
  - DOMAIN READER (109.004-T): readiness/prior-closure evaluated against the EXPLICIT target
    from the domain-input; agent-mode missing target -> exit 2; defines the deterministic
    ambient target-resolution rule (currently-claimed active shipment, then current-branch
    slug) reused by hook/ci.
  - SHIP INTEGRATION (109.017-T): every Ship gate invocation passes `--shipment` = the
    shipment being claimed/operated on at each of the five lifecycle points. Explicit task
    dependency on 109.002-T added (CLI contract).
  - ORCHESTRATOR INTEGRATION (109.018-T): passes `--shipment` = the candidate shipment at
    route-to-Ship and the successor shipment at cursor-advance. Explicit task dependency on
    109.002-T added.
  - NON-SHIPMENT HOOK/CI CONTEXTS (109.007-T pre-push, 109.008-T pre-commit, 109.011-T CI):
    deterministic implicit target-resolution (claimed-shipment / branch slug); ambient-only
    invariants (A2 at-most-one-active, A3b worktree uniqueness, branch ownership) run
    fail-closed when no target resolves; existence-guarded so ordinary no-claim commits/pushes
    are not blocked. This is a deliberately non-shipment-scoped mode that does NOT weaken
    fail-closed AGENT mode (agent mode still requires the explicit `--shipment`).
  - TESTS (109.006-T): added cases for missing `--shipment` -> exit 2 (agent scoped),
    readiness/ownership scoped to a supplied target (parametrized), and hook/ci ambient
    resolution incl. the no-target ambient-only path.
  - DOCS (109.010-T): documents the required-in-agent vs implicit-resolution-in-hook/ci
    target contract and exit-code semantics.
  - FEATURE DoD: added the `--shipment` contract bullet and the explicit-target-passing bullet.

PLAN-HARDEN (P-006) re-assessment: the amendments fall within the already-hardened blast
radius (gate CLI/domain-reader + agent-template family, already in scope). New/retained safety
constraints hardened into acceptance criteria: (H10) shipment-scoped agent-mode invocation is
fail-closed on a missing/unresolvable target (exit 2) — never fail-open; (H11) hook/ci ambient
resolution is deterministic and existence-guarded and does not weaken agent-mode fail-closed;
(H12) the sequencing correction installs in 114-S AND mandates a post-closure Orchestrator
context reload before 115-S selection, closing the timing gap. Fail-closed exit-code contract
(0/1/2) and the read-only external-guard boundary (no backlogit mutation) are preserved. No
P0/P1 findings remain.
<!-- END:findings -->

<!-- BEGIN:summary -->
Cycle-3 (final bounded) plan-harden + plan-review for 109-F resolving 2 P1 findings on reviewed
HEAD b2efcf3: (1) sequencing repair timing — 109.019-T moved into 114-S so the correction is
installed before the 114->115 transition, plus a mandatory post-114-closure Orchestrator context
reload before cursor-advance/115-S selection; (2) pre-claim gate not shipment-bound — added a
`--shipment <ID>` target flag, REQUIRED and fail-closed in agent shipment-scoped modes (missing
-> exit 2), passed explicitly by Ship (claim/build/PR/closure) and Orchestrator (route/cursor),
with a deterministic non-shipment hook/ci implicit-resolution contract that preserves fail-closed
agent mode. Route claude-opus-4.8/anthropic/high, DARK_MODE_ACTIVE. VERDICT: PASS. Revised
task-only manifests: 114-S=9, 115-S=7, 116-S=3 (19 tasks, each exactly once); all `queued`;
serial A->B->C via blocks edges; 114-S the only eligible cursor. Tasks amended: 109.002-T,
109.004-T, 109.006-T, 109.007-T, 109.008-T, 109.010-T, 109.011-T, 109.017-T (+dep 109.002-T),
109.018-T (+dep 109.002-T), 109.019-T (moved 115-S->114-S); feature 109-F DoD updated. Evidence:
backlogit shipment.go isValidShipmentTransition (L336-345, queued->active/active->shipped|abandoned
only); revised 114-S/115-S/116-S manifests; installed _ship.agent.md / _orchestrator.agent.md
integration points. Handoff token = shipment 114-S.
<!-- END:summary -->
