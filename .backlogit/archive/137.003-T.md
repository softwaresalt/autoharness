---
archived_from: .backlogit/queue/137.003-T.md
archived_status: done
artifact_type: task
commit: a1bce32f5f0173d82d8ac1301c66876e6b177356
created_at: 2026-08-21T00:46:11.9083567Z
custom_fields:
    complexity: medium
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 137.002-T
id: 137.003-T
labels:
    - templates
    - dogfood
    - agent-contract
    - deprecation
    - cli
    - verifier
    - tests
    - manifest
    - atomic
parent_id: 137-F
priority: medium
references:
    - docs/plans/2026-08-20-ship-stash-archive-operation-migration-plan.md
    - docs/plans/2026-08-20-ship-stash-archive-operation-migration-hardening.md
status: archived
title: 'Atomic stash_archive migration: Ship template + dogfood mirror, verifier marker, contract tests, manifest checksum'
updated_at: 2026-08-21T15:33:19.4309149Z
---

ATOMIC TASK. Migrate the Ship agent contract off the deprecated `backlogit_stash_remove` to `backlogit_stash_archive` across EVERY artifact the migration invalidates, in ONE task and ONE commit. This is also a PAIRED EDIT on a divergent template/dogfood pair — perform it under the maintenance contract landed in 137.002-T.

*** WHY THIS TASK IS ATOMIC (supersedes the former 137.003/137.005/137.006 split) ***
The former split was NOT executable under Ship's per-task quality gate, which runs the full configured suite before completing EACH task. The three tasks formed a mutually-breaking chain in which every intermediate state was red:
- editing `.github/agents/_ship.agent.md` (old 137.003-T) breaks `verify_workspace.py`'s `ship_source_artifact_cleanup` marker, breaks `tests/test_scope_containment_policy_contract.py::test_post_merge_step7_source_artifact_cleanup_is_unweakened`, and invalidates the `.github/agents/_ship.agent.md` manifest checksum asserted by `test_manifest_checksum_matches_actual_dogfood_bytes_for_all_eight_pairs`;
- editing `verify_workspace.py` (old 137.005-T) breaks the `tests/test_verify_workspace.py` marker-list fixtures;
- the fixes for all of the above lived in old 137.006-T, which itself could not go green before its predecessors landed.
Per the harvest rule "every invalidated assertion/checksum is assigned to the SAME task that invalidates it", the whole chain collapses to this one task. Old 137.005-T and 137.006-T are SUPERSEDED BY THIS TASK and archived; their full content is carried below.

*** WIDTH-ISOLATION CARVE-OUT (explicit, deliberate) ***
Old 137.005-T carried a "this is the only CLI/source-family task; do not bundle template, policy, or test edits into it" rule. That rule is OVERRIDDEN HERE and only here. Width isolation is a granularity heuristic; leaving a task unable to pass its own quality gate is a correctness defect. The mutual-breakage constraint (H1) wins. This carve-out authorizes bundling exactly the artifacts listed below and NOTHING else — it is not a licence to widen scope further.

=== FILES AND SITES (all verified by Stage) ===

(1) TEMPLATE + DOGFOOD MIRROR (paired edit)
- `templates/agents/_ship.agent.md.tmpl` — line 38 (Role Boundary table), line 819 (post-merge Step 7)
- `.github/agents/_ship.agent.md` — line 47 (Role Boundary table)

(2) VERIFIER
- `src/autoharness/verify_workspace.py` — line 296 (`ship_source_artifact_cleanup` must_contain marker)

(3) CONTRACT TESTS
- `tests/test_verify_workspace.py` — lines 1860, 3135 (marker-list fixture strings)
- `tests/test_scope_containment_policy_contract.py` — line 595 (`assertIn("backlogit_stash_remove", ship_template_text)`)
- `tests/test_scope_containment_boundary_contract.py` — lines 79, 87, 332, 552 (comment/docstring text only, no assertions)

(4) MANIFEST
- `.autoharness/harness-manifest.yaml` — checksum for `.github/agents/_ship.agent.md`

*** TOOL SURFACE GROUND TRUTH (verified read-only against installed backlogit v1.10.0) ***
BOTH surfaces expose the archive operation as primary. There is NO inversion and NO missing surface.
- MCP: `backlogit_stash_archive` ("Archive an active stash entry") is EXPOSED and is the PRIMARY operation. `backlogit_stash_remove` is also exposed but self-described "[Deprecated: use backlogit_stash_archive]".
- CLI: `backlogit stash archive <id>` is the canonical subcommand; `stash remove` is RETAINED as a DEPRECATED ALIAS resolving to the same archive handler. It is NOT absent.
- `.mcp.json` runs the same `backlogit mcp` executable with tools ["*"], so `backlogit_stash_archive` is reachable in this workspace.
A direct rename to the archive operation is therefore correct, executable, and complete on both surfaces.

=== REQUIRED WORDING (P-012 MCP-first / CLI-fallback) ===
1. MCP primary: call `backlogit_stash_archive`.
2. CLI fallback: `backlogit stash archive <id>` — exposed, non-destructive.
3. Do not describe the operation in removal semantics.
4. `backlogit_stash_remove` MUST NOT be specified as an execution fallback or any other prescriptive path. It may appear ONLY as non-prescriptive legacy/deprecation context.
5. Do NOT write that the CLI lacks a `stash remove` subcommand. That claim is FALSE on v1.10.0. The correct statement is that `stash archive` is canonical and `stash remove` survives as a deprecated alias of it. Comments at `tests/test_scope_containment_boundary_contract.py` lines 87 and 552 currently assert the false version and MUST be corrected here.

PRESERVE the surrounding clause wording exactly, including the parenthetical distinguishing this manifest-derived closure operation from discretionary removal — that distinction is load-bearing for P-021 C5 and must survive verbatim. Specifically, these assertions must still pass unchanged:
- `workflow_policy_text` contains "manifest-derived retirement of the source stash entry that fed the shipped scope"
- `ship_template_text` AND `ship_dogfood_text` contain "a manifest-derived closure operation, distinct from discretionary removal"
- `ship_template_text` AND `ship_dogfood_text` contain "discretionary removal or archival of stash entries"
- `ship_template_text` AND `ship_dogfood_text` contain "create a capture-only stash entry (P-021 C5)"

At the template's Step 7 site, update the follow-on sentence so the already-retired case reads in archive terms: "If the stash entry is already archived, skip and log it."

=== VERIFIER MARKER (from superseded 137.005-T) ===
Update `ship_source_artifact_cleanup`'s `must_contain` marker from `backlogit_stash_remove` to the canonical archive wording actually landed in (1). Leave the other markers (`source_stash_id`, `source_deliberation_id`, `backlogit_archive_item`) unchanged. Then update the two `tests/test_verify_workspace.py` fixture strings to match the new marker list exactly — those fixtures are invalidated BY this same edit and are fixed HERE.

=== TEST + MANIFEST (from superseded 137.006-T) ===
Assertions must express the archive contract as it actually lands: MCP primary `backlogit_stash_archive`, CLI fallback `backlogit stash archive`. `backlogit_stash_remove` MUST NOT be asserted as a required prescriptive execution path; it may only be asserted where a test is deliberately proving deprecated/legacy behaviour is absent from prescriptive surfaces.

CARE REQUIRED: some assertions encode the P-021 C5 CLAUSE SEMANTICS, not merely the operation string. Update the operation name WITHOUT weakening any assertion about the removal/archival distinction. If an assertion currently proves that C5 prohibits both discretionary verbs, it must still prove that afterwards.

Refresh the `.github/agents/_ship.agent.md` manifest checksum BECAUSE THE BYTES LEGITIMATELY CHANGED HERE — not to silence unexplained drift. Compute it from the LF-normalized committed git blob via `git cat-file -p :<path>` (staged) / `HEAD:<path>` (post-commit), never a raw Windows working-tree read. If any OTHER checksum is also stale, that is a SEPARATE finding to capture as a deferred entry, not to absorb into this task. See `docs/compound/2026-08-15-checksum-drift-fix-correctly-surfaces-preexisting-self-hosted-customization.md`.

=== HARDENING CONSTRAINTS ===
- H1 (P1) — DISCHARGED BY CONSTRUCTION. The mutually-breaking set is now one task, so no intermediate red state can be observed by any gate. Do NOT re-split this task.
- H4 (P2): the mirror carries ONLY the Role Boundary site while the template has 2. This asymmetry is EXPECTED and MUST NOT be "fixed" by back-porting the missing Step 7 block into the mirror. That back-port is the 6D62077C prose-drift problem and is explicitly out of scope.
- H6 (P3) — ABSORBED. The checksum refresh is in THIS task, so the "forgot to refresh, got a confusing parity failure" mode is no longer reachable.
- H3 (P1) — HISTORICAL RECORDS ARE IMMUTABLE. Do NOT use an unscoped global find-and-replace. `backlogit_stash_remove` also appears in four historical artifacts that must NOT be edited:
  - `docs/closure/143-S-134-F-post-merge-closure.md`
  - `docs/memory/2026-08-18-stage-b48a482a-p021-scope-containment.md`
  - `docs/memory/2026-08-20-ship-143-s-full-lifecycle-closure.md`
  - `docs/plans/2026-08-18-bounded-fix-cycle-scope-containment-hardening.md`

=== ACCEPTANCE (single gate, evaluated once, all green) ===
- Both Ship files name `backlogit_stash_archive` as MCP primary and `backlogit stash archive` as CLI fallback, and no longer present removal as the primary semantics.
- No prescriptive execution path in any edited file names `backlogit_stash_remove`.
- No file states that the CLI lacks a `stash remove` subcommand.
- Role Boundary clause semantics unchanged apart from the operation; all four preserved-assertion strings above still pass.
- `verify-workspace` passes against this repository's own install.
- FULL configured test suite green — `verify_workspace.py`, `test_verify_workspace.py`, and all three `test_scope_containment_*_contract.py` modules — with NO red intermediate state at any point in this task.
- Manifest checksum matches the actual committed bytes of the edited mirror.
- `git diff --name-only` contains NO path under `docs/closure/` or `docs/memory/`.
- Everything lands in ONE commit.

SUPERSEDES: 137.005-T (verifier marker), 137.006-T (contract tests + manifest checksum) — both archived 2026-08-20; see their archived records for original wording.
DEPENDS ON: 137.002-T (maintenance contract must land first).
ORDER-INDEPENDENT OF: 137.004-T (policy clause + registry template; touches no artifact this task touches and is independently gate-green) and 137.001-T (shares the file `tests/test_scope_containment_policy_contract.py` but no assertion overlap — sequence either way, resolve textual conflict if both edit near line 595).

Plan: docs/plans/2026-08-20-ship-stash-archive-operation-migration-plan.md (Task A+C+D, merged)
Hardening: docs/plans/2026-08-20-ship-stash-archive-operation-migration-hardening.md (H1, H3, H4, H6)