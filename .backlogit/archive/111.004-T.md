---
artifact_type: task
created_at: 2026-08-05T23:51:14.9380676Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 1.0.0
    size_source: agent
dependencies:
    - 111.001-T
id: 111.004-T
parent_id: 111-F
priority: medium
status: done
title: 'C2: author checkpoint-recovery + prune-on-restore section in the backlogit-pack overlay instruction template (prune allowlist; install/manifest wiring in 111.005-T)'
updated_at: 2026-08-07T06:10:49.6820664Z
---



<!-- BEGIN:acceptance-criteria -->
1. Prune-on-restore is a bounded READ-select-summarize over restored checkpoint/engram state; drops only SUPERSEDED action-observation history. 2. PRUNE ALLOWLIST (review P1-2, H4): NEVER prune the active-shipment cursor, the unresolved-checkpoint pointer, or gate verdicts. 3. Ties to the existing context-efficiency instruction / P-020 substrate (no new engine). 4. DEGRADED FALLBACK when engram unreachable: FAIL CLOSED to OPERATOR HANDOFF — NO prune, NO resume (SINGLE, UNAMBIGUOUS behavior aligned with 111-F hardening H5, DoD#6, and 111.002-T AC#2; the earlier "bounded file-based prune degradation" path is REMOVED because it was not proven safe-and-explicitly-non-resuming and contradicted H5). The operator-confirmation-before-restore gate (111.001-T) still governs whether restore runs at all; a substrate-unreachable condition never reaches restore/prune/resolve. 5. HOST (re-review 2026-08-05, 111.001-R P1-3 CORRECTED): author the protocol as a NEW checkpoint-recovery/prune-on-restore section INSIDE the backlogit capability-pack overlay instruction template `templates/instructions/backlogit.instructions.md.tmpl` (woven via the backlogit Formal Overlay Contract). The overlay is the correct home because the backlogit pack is a hard precondition of crash-resumption (it drives the backlogit checkpoint API) — NOT a standalone instruction template that no capability pack installs. IMPORTANT: this overlay is NOT currently installed in the dogfood/self-install workspace (see 111.005-T); the install/tune wiring + first-party manifest registration that GUARANTEES it lands is a SEPARATE width-isolated task (111.005-T). THIS task authors ONLY the overlay section content. 6. VARIABLE/AGNOSTICISM: any {{VARIABLE}} customization points are authored so they resolve at install time (no unresolved placeholders in installed output); protocol stays TECHNOLOGY-AGNOSTIC (no provider/binary hardcoding) and its engram-unreachable path is the fail-closed operator handoff of AC#4. 7. VERIFICATION contract note: coverage is targeted SECTION-PRESENCE + the STANDARD first-party `artifacts[].checksum` coherence (owned by 111.003-T) — do NOT introduce community-template `installed_checksum`/`source_checksum` or installed-vs-source drift comparison (not part of the first-party overlay contract).
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Author the checkpoint-recovery / prune-on-restore protocol (bounded context-pruning on restore over checkpoint/engram state) as a new section inside the backlogit capability-pack overlay instruction template (`templates/instructions/backlogit.instructions.md.tmpl`). Do NOT create a standalone instruction template with no install path. The install/tune wiring + first-party manifest registration that guarantees this overlay is actually installed is the SEPARATE task 111.005-T; this task authors ONLY the section content.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
Depends on C1. Source: 002-SP finding 3 + hardening H4 + review 111.001-R P1-2 and P1-3 (re-review 2026-08-05). Single template family: instruction template (the backlogit-pack overlay instruction). Rationale: the backlogit pack overlay is the bounded, technology-agnostic home for a checkpoint-workflow section because the pack drives the backlogit checkpoint API and is a hard precondition of crash-resumption. Re-review 2026-08-05 CORRECTED the prior "already-installed" assumption and the installed_checksum/source_checksum dual-checksum wiring: the overlay is NOT currently installed, so install/tune + manifest registration was split into 111.005-T using the STANDARD first-party `artifacts[].checksum` contract, and verification (111.003-T) is section-presence + single-checksum coherence (no source-drift comparison). PUBLICATION-REVIEW 2026-08-06 (engram-unavailable contradiction): AC#4 changed from "bounded file-based prune degradation" to FAIL-CLOSED OPERATOR HANDOFF, aligning with 111-F H5 and 111.002-T (single behavior, no file-based prune).
<!-- END:implementation-notes -->