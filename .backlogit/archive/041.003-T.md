---
archived_from: .backlogit/archive/041.003-T.md
artifact_type: task
commit: 6b43b560348c9c6bdabaad1fa5039b5167b35e0d
created_at: 2026-05-17T19:21:00.4179936-07:00
id: 041.003-T
labels:
    - ship-agent
    - installed-artifact
    - pr-lifecycle
parent_id: 041-F
priority: medium
status: archived
title: Harden installed .github/agents/ship.agent.md — Step 4 P-014 gate + Step 5 post-merge Copilot poll
updated_at: 2026-05-17T20:27:42.9051702-07:00
---

## Context

The installed `.github/agents/ship.agent.md` Step 4 currently reads:

```
### Step 4: PR Lifecycle
1. Push the branch and invoke the `pr-lifecycle` skill.
2. Handle CI feedback via the `fix-ci` skill if needed.
3. Wait for operator approval before merge.
```

This three-line step:
- Does not reference `github-pr-automation.instructions.md` at the agent level
- Does not name the §1.9 gate that must run before merge readiness is declared
- Does not reference P-014 (once that policy exists after 041.001-T)
- Treats "wait for operator approval" as a single undifferentiated step — an agent that believes CI is green and the review loop is done could interpret this as passive waiting rather than an active gate

**Dependency**: This task should execute after 041.001-T (P-014 exists) and 041.002-T (instructions file installed).

## Work

### Step 4 hardening

Replace Step 4 in `.github/agents/ship.agent.md` with:

```markdown
### Step 4: PR Lifecycle

1. Push the branch and invoke the `pr-lifecycle` skill.
2. Handle CI feedback via the `fix-ci` skill if needed.
3. **P-014 Copilot Review Merge Gate (NON-NEGOTIABLE)**: Before presenting the PR as
   merge-ready, read `.github/instructions/github-pr-automation.instructions.md` and
   execute the §1.9 Pre-Merge Review Readiness Verification:
   - Run the §1.9 readiness query (with full thread pagination).
   - Evaluate Check 1 (no pending Copilot review), Check 2 (review covers current HEAD),
     and Check 3 (zero unresolved Copilot threads) in order.
   - If any check fails: halt. Record a P-014 violation via P-005 telemetry. Do not proceed.
   - If all three checks pass: log `P-014 GATE PASSED: §1.9 verified at HEAD={headRefOid}`.
4. **Operator approval gate**: After the §1.9 gate passes, present the PR readiness summary
   to the operator and wait for an explicit approval signal. Never treat silence, green CI,
   or a passing §1.9 gate as approval. Never auto-merge.
5. Execute the merge only after receiving explicit operator approval AND having a §1.9 gate
   pass on record. If new commits are pushed between the §1.9 check and the approval signal,
   re-run §1.9 before executing the merge.
```

### Step 5 post-merge closure hardening

In Step 5 (Post-Merge Closure), after the existing "Merge Confirmation Gate" block, add a new subsection:

```markdown
#### Post-Merge Closure PR Copilot Gate (P-014, NON-NEGOTIABLE)

When a post-merge closure branch and PR are created:

1. Request Copilot Review immediately after PR creation (per §1.1 of
   `.github/instructions/github-pr-automation.instructions.md`).
2. Poll for review completion per §1.2 back-off cadence.
3. Address any comments per §1.3–§1.7.
4. Run §1.9 readiness gate before presenting the post-merge closure PR for merge.
5. Obtain explicit operator approval — the prior main PR approval does not transfer.
6. P-014 applies in full. Record a P-014 violation via P-005 telemetry if this gate is skipped.
```

## Acceptance Criteria

- [ ] Step 4 bullet 3 explicitly names `github-pr-automation.instructions.md` and §1.9
- [ ] Step 4 bullet 4 makes operator approval a distinct named gate after §1.9 passes
- [ ] Step 4 bullet 5 adds the last-mile re-check condition (new commits between gate check and approval)
- [ ] Step 5 contains the "Post-Merge Closure PR Copilot Gate" subsection with P-014 reference
- [ ] P-014 referenced by name in both Step 4 and Step 5
- [ ] File passes markdownlint (P-008)
- [ ] YAML frontmatter valid

<!-- BEGIN:Dependencies -->
Depends on 041.001-T (P-014 policy exists) and 041.002-T (instructions file installed). Execute in order: 041.001-T → 041.002-T → this task.
<!-- END:Dependencies -->