---
archived_from: .backlogit/queue/118.002-T.md
archived_status: done
artifact_type: task
commit: 8ccd3a2ded777393703136da6747e846619f4294
created_at: 2026-08-10T07:08:40.0739461Z
custom_fields:
    complexity: low
    origin_feature: 117-F
    size: S
    size_ruleset_version: 2h-width-isolated-v1
    size_source: agent
id: 118.002-T
labels:
    - shipment-1
    - P0
    - characterization
    - test-only
    - bash
    - H1
    - H3
parent_id: 118-F
priority: critical
references:
    - start.sh
    - docs/plans/2026-08-09-copilot-cli-supervisor-control-plane-plan.md
status: archived
title: T2 Characterization suite for start.sh observable launch contract (POSIX parity)
updated_at: 2026-08-13T02:21:12.619907Z
---

SIZE: S. COMPLEXITY: low. (Two independent axes; also emitted as structured backlog fields.) Shipment 1 (P0). TEST-ONLY, zero production change. POSIX counterpart of 118.001-T, but DELIBERATELY NOT A MIRROR OF IT. F17 RULING - COMPLETE RE-INVENTORY (verified against the 80-line working-tree start.sh): the script implements only FIVE dimensions - (1) .env.local no-clobber parsing with CR stripping, trailing-whitespace trimming and single matching quote-pair stripping; (2) COPILOT_HOME defaulting to ./.copilot while honoring a pre-set value; (3) an UNGUARDED export GITHUB_TOKEN="$(gh auth token)" - and because `export` is a BUILTIN COMMAND, `set -e` does NOT abort when gh fails, so the observable result is an EMPTY GITHUB_TOKEN and that exact behavior is what must be pinned; (4) Copilot exe resolution COPILOT_EXE_PATH -> COPILOT_EXE -> PATH with the current actionable message and exit 1; (5) exec of the resolved exe - verbatim argv passthrough, exit status propagated by PROCESS REPLACEMENT, and TERMINAL ATTACHMENT PRESERVED because exec replaces the shell (F29: assert TTY attachment explicitly). THE ABSENCES ARE PART OF THE BASELINE AND MUST BE ASSERTED AS ABSENT: no ENGRAM_DATA_DIR default (that line is commented out), no GITHUB_PERSONAL_ACCESS_TOKEN, no COPILOT_USE_REMOTE / --remote logic, no backlogit sync, no Engram pre-warm. The plan's original premise that start.ps1 and start.sh are 'two divergent implementations of the same seven-dimension policy' was FALSE and is withdrawn. Any cross-platform normalization that gives start.sh a dimension it does not have today is an EXPLICIT BEHAVIOR CHANGE, is forbidden in Shipment 1, and is deferred to Shipment 3 (120.003-T for --remote composition, 120.002-T for sidecars) under separate operator approval. Width-isolated from 118.001-T because it is a different language surface with different failure modes (set -euo pipefail interactions, pipeline exit-status semantics). Explicitly assert that no trailing `|| true` or equivalent masks the child's exit status (compound learning 2026-08-08-shell-pipeline-exit-status-masking-in-version-probes). Mock all external tools. ACCEPTANCE: suite passes against today's start.sh unmodified, and is later re-run by 120.007-T BYTE-IDENTICAL FOR EVERY UNCHANGED SCENARIO. THIS TASK IS PRESERVED UNCHANGED AS THE BASELINE CHARACTERIZATION OF CURRENT POSIX BEHAVIOR (P1-2 ruling, 2026-08-12) - it records what start.sh does TODAY and is deliberately NOT rewritten to anticipate the migration. TWO of the absences it pins are KNOWN, NAMED, APPROVED MIGRATION DELTAS and are therefore BASELINE EVIDENCE, NOT PERMANENT PRODUCT MANDATES: "no ENGRAM_DATA_DIR default" is the baseline for DELTA POSIX_ENGRAM_DATA_DIR, and "no GITHUB_PERSONAL_ACCESS_TOKEN" is the baseline for DELTA POSIX_PAT_BOOTSTRAP (ruling A, 2026-08-12; these are the two POSIX entries of the three-delta platform/scenario matrix owned by 120.007-T - the third, WINDOWS_PAT_NO_GH, lands on the WINDOWS baseline 118.001-T and is NOT pinned here). 120.001-T is the single source of truth for bootstrap policy and deliberately supplies BOTH on every platform, so POSIX gains them at migration; 120.007-T REPLACES those two absence assertions with positive assertions of the new behavior. Pinning them here is still correct and still required - the delta is only provable against a recorded baseline - but do NOT read either as a requirement that POSIX remain without them. Every OTHER assertion in this suite, including the TTY assertions, remains byte-identical after migration, and any further assertion change is escalated as an operator product decision.