---
archived_from: .backlogit/archive/041.002-T.md
artifact_type: task
commit: 6b43b560348c9c6bdabaad1fa5039b5167b35e0d
created_at: 2026-05-17T19:20:35.326317-07:00
id: 041.002-T
labels:
    - instructions
    - copilot-review
    - critical-path
parent_id: 041-F
priority: medium
status: archived
title: Install github-pr-automation.instructions.md and add post-merge closure scope (§1.9 + §1.10)
updated_at: 2026-05-17T20:27:42.6716431-07:00
---

## Context — Critical Path Item

**Root cause of all three incidents**: `.github/instructions/github-pr-automation.instructions.md` does not exist in this workspace. The `pr-lifecycle` skill references it for §1.9 Pre-Merge Review Readiness Verification, but the file is absent. The gate protocol is unreachable at runtime regardless of what the skill template says.

The template source is at `templates/instructions/github-pr-automation.instructions.md.tmpl`. It is complete and contains the full §1.9 gate protocol with all three checks and correct bot identity handling (§1.9.2).

## Work

### Part A — Install the file

Resolve the template variables and install the file at:
`.github/instructions/github-pr-automation.instructions.md`

Template variables to resolve:
- `{{REPO_OWNER}}` → `softwaresalt`
- `{{REPO_NAME}}` → `autoharness`
- `{{FORMAT_CHECK_COMMAND}}` → (no format check in this repo; use: `# no format check command configured`)
- `{{LINT_COMMAND}}` → `markdownlint "**/*.md"`
- `{{TEST_COMMAND}}` → `uv run python -m pytest`

The installed file should retain the `Generated by autoharness | Template: github-pr-automation.instructions.md.tmpl` footer line.

### Part B — Harden the installed file with post-merge closure scope

After installation, add the following scope extension to §1.9's opening paragraph (after the existing "This gate is a NON-NEGOTIABLE pre-merge verification" sentence):

```markdown
This gate applies to **all pull requests** created or merged by the Ship agent:
feature PRs, chore PRs, and post-merge closure PRs. There is no exception for
"small" or "hygiene" PRs. Every merge requires a fresh Copilot review covering
the current HEAD and zero unresolved Copilot threads.
```

Also add a new §1.10 section at the end of Part 1:

```markdown
### 1.10 Post-Merge Closure PR Copilot Surveillance

When the Ship agent creates a dedicated post-merge closure branch and PR:

1. Request Copilot Review per §1.1 immediately after PR creation.
2. Poll per §1.2 back-off cadence.
3. Apply the full §1.3–§1.7 fix cycle for any comments raised.
4. Run §1.9 readiness gate before presenting the post-merge closure PR for merge.
5. Obtain explicit operator approval before merging the post-merge closure PR.

Post-merge closure PRs are not exempt from the P-014 gate. The operator must
approve each merge individually — approval for the main PR does not carry over
to the post-merge closure PR.
```

### Part C — Template parity

Apply the same §1.9 scope extension and §1.10 addition to `templates/instructions/github-pr-automation.instructions.md.tmpl` so future installs include these from day one.

## Acceptance Criteria

- [ ] `.github/instructions/github-pr-automation.instructions.md` exists and is not empty
- [ ] All `{{VARIABLE}}` placeholders resolved — no unresolved variables in the installed file
- [ ] §1.9 opening paragraph contains the "all pull requests" scope extension
- [ ] §1.10 section present in both installed file and template
- [ ] Template parity: `templates/instructions/github-pr-automation.instructions.md.tmpl` also updated
- [ ] Installed file passes markdownlint (P-008)
- [ ] YAML frontmatter is valid in both files