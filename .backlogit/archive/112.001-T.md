---
artifact_type: task
created_at: 2026-08-05T23:50:52.9223042Z
custom_fields:
    complexity: low
    size: S
    size_ruleset_version: 1.0.0
    size_source: agent
id: 112.001-T
parent_id: 112-F
priority: medium
status: done
title: 'B1: shipment-record-status inconsistency DETECTION + REPORT-ONLY diagnostic + operator-remediation guidance in shipment-reconcile SKILL (no mutation)'
updated_at: 2026-08-07T02:52:46.5552868Z
---



<!-- BEGIN:acceptance-criteria -->
1. Add a READ-ONLY DETECTION of the queued-with-active-work inconsistency to templates/skills/shipment-reconcile/SKILL.md.tmpl composed from EXISTING read-only backlogit reads (`shipment list` + per-shipment/per-task status reads) plus a PER-TASK ALLOWED-STATE ROLE classification used ONLY to DESCRIBE the inconsistency (NEVER to gate a mutation): each manifest task is classified as a UNIQUE, NON-CONFLICTING record for its role — a `queued`/`active` task is a UNIQUE LIVE QUEUE record (`status: queued`/`active`, no archive copy); a COMPLETED task is a UNIQUE ARCHIVE record in EITHER of backlogit's two valid representations ((a) `status: done` in `archive/`, provenance not required, OR (b) `status: archived` + `archived_status: done` + valid `archived_from`), with no conflicting live queue record. 2. EMIT a REPORT-ONLY diagnostic: the offending shipment id, its record status, the per-task role classification, and any per-item anomaly (duplicate — id in both queue and archive; conflicting — queue status vs role mismatch, archive-alongside-live-queue, or a live `status: done` in queue; missing; malformed/absent provenance on a `status: archived` record; any-other-archived-status; orphan; out-of-role; torn-partial). On ANY ambiguity, REPORT it and HALT — never mutate. 3. NO MUTATION, NO REPAIR MODE, NO ClaimShipment: the skill performs NO queued->active re-claim and NO status write. The report-and-halt / no-auto-mutation DEFAULT is PRESERVED on pre/post/safe-close AND this new detection path (detection is strictly read-only). 4. OPERATOR-REMEDIATION GUIDANCE inline with the diagnostic: point the operator to the SUPPORTED manual remediation (backlogit sanctioned lifecycle transitions ONLY) and explicitly state autoharness performs NO auto-repair; a record-only forward re-claim is UNSUPPORTED because backlogit 1.8.0 ClaimShipment is MANIFEST-WIDE (activates every `queued` member + cascades parent status, all-or-nothing rollback) and STRICTLY SINGLE-SHOT (active->active rejected with ErrShipmentConflict), there is NO active->queued transition, and there is NO `blocked` shipment status. Never fabricate a blocked->queued or active->queued transition. 5. DEGRADED (backlogit unreachable) => REPORT the degraded condition + HALT (no blind action). 6. Operator-invoked and READ-ONLY; no `--confirm` mutation gate is needed because nothing is mutated. WIDTH ISOLATION: single template family (shipment-reconcile SKILL prose over existing reads); no new gate/CLI code.
<!-- END:acceptance-criteria -->

<!-- BEGIN:description -->
Add a READ-ONLY DETECTION of the queued-with-active-work inconsistency to templates/skills/shipment-reconcile/SKILL.md.tmpl that EMITS a REPORT-ONLY diagnostic plus operator-remediation guidance, and performs NO mutation (no ClaimShipment, no repair mode). Re-scoped from the withdrawn constrained forward-repair mode per Copilot PR #304 finding 1 (record-only forward re-claim is unsupported by backlogit 1.8.0). This is the core, no-mutation detection prose.
<!-- END:description -->

<!-- BEGIN:implementation-notes -->
RE-SCOPED per Copilot PR #304 finding 1 (2026-08-06): the prior B1 authored a mutation-bearing `repair-record-status` mode using ClaimShipment as a forward re-claim. Verified read-only against backlogit 1.8.0 (`internal/core/shipment_lifecycle.go`, `internal/core/shipment.go`; C:\Source\GitHub\backlogit NOT mutated) that ClaimShipment is manifest-wide activation (not record-only) + all-or-nothing + strictly single-shot with no active->queued and no `blocked` status, so a clean record-only repair of the mixed-role target is UNREACHABLE via supported ops. Scope withdrawn to READ-ONLY detection + report + operator-remediation guidance; TOCTOU/post-condition/two-active machinery dropped with the mutation. Source of truth: 013-DL Option C (re-scoped by Addendum G) + 112.001-R re-scope addendum. The per-task allowed-state role classification (013-DL Addenda D/E/F) is retained but used ONLY to DESCRIBE the inconsistency in the report. Single template family (shipment-reconcile SKILL); read-only/additive; complexity now LOW (no mutation authority).
<!-- END:implementation-notes -->