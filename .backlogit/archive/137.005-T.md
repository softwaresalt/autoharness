---
archived_from: .backlogit/queue/137.005-T.md
archived_status: queued
artifact_type: task
created_at: 2026-08-21T00:46:47.4267157Z
custom_fields:
    complexity: low
    size: XS
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 137.005-T
labels:
    - cli
    - verifier
    - deprecation
parent_id: 137-F
priority: medium
references:
    - docs/plans/2026-08-20-ship-stash-archive-operation-migration-plan.md
    - docs/plans/2026-08-20-ship-stash-archive-operation-migration-hardening.md
status: archived
title: '[SUPERSEDED by 137.003-T] Update verify_workspace ship_source_artifact_cleanup marker to stash_archive'
updated_at: 2026-08-21T02:14:18.4507155Z
---

*** SUPERSEDED 2026-08-20 — NOT EXECUTABLE — DO NOT SCHEDULE ***
SUPERSEDED BY: 137.003-T ("Atomic stash_archive migration: Ship template + dogfood mirror, verifier marker, contract tests, manifest checksum").
REASON: this task and 137.003-T were MUTUALLY BREAKING (H1). Split across two tasks, every intermediate state left `verify_workspace` and/or `tests/test_verify_workspace.py` RED, which Ship's per-task quality gate evaluates before completing EACH task — so neither task could ever be completed. The whole mutually-breaking chain (old 137.003-T + 137.005-T + 137.006-T) was collapsed into a single atomic task, 137.003-T, so that every assertion invalidated by an edit is repaired by the SAME task that invalidates it.
DISPOSITION: scope fully carried into 137.003-T under the heading "VERIFIER MARKER (from superseded 137.005-T)"; nothing dropped. RETAINED in shipment 145-S membership as a NON-EXECUTABLE pre-archived member (CORRECTED 2026-08-20 — see CLOSURE-VALIDITY CORRECTION below). Dependency edge 137.005-T -> 137.003-T removed. Archived, not deleted, so the original wording remains auditable.
TRACEABILITY: 137.005-T -> 137.003-T (superseded-by). Source stash entry 8D570CF8; feature 137-F; shipment 145-S.

--- ORIGINAL TASK TEXT BELOW, UNMODIFIED ---

Update the verifier's marker assertion so it no longer requires the deprecated operation name.

FILE: src/autoharness/verify_workspace.py — line 296

The ship_source_artifact_cleanup check currently requires .github/agents/_ship.agent.md to contain "backlogit_stash_remove" in its must_contain marker list. Update that required marker to match whatever canonical wording 137.003-T actually lands.

*** MARKER CHOICE ***
backlogit v1.10.0 DOES expose the backlogit_stash_archive MCP tool (verified read-only against the installed manifest), so 137.003-T documents backlogit_stash_archive as the MCP primary with `backlogit stash archive` as the CLI fallback. The marker must assert the ARCHIVE-semantics contract that actually lands, and must NOT assert backlogit_stash_remove. Coordinate the exact marker string with 137.003-T.

Leave the other markers (source_stash_id, source_deliberation_id, backlogit_archive_item) unchanged.

H1 (P1) ORDERING HAZARD — MANDATORY
This task and 137.003-T are MUTUALLY BREAKING if split:
- 137.003-T alone: the mirror no longer contains the old marker, so the verifier fails.
- This task alone: the verifier demands a marker the mirror does not yet have, so it fails.
Either ordering produces a red intermediate state. They MUST land in the same commit, or in adjacent commits within a single PR that is never evaluated between them. Do NOT split them across PRs.

WIDTH ISOLATION
This is the only CLI/source-family task in the shipment. Do not bundle template, policy, or test edits into it.

ACCEPTANCE
- Marker list matches the wording actually landed by 137.003-T and names the archive operation.
- No marker requires backlogit_stash_remove.
- `verify-workspace` passes against this repository's own install.

Plan: docs/plans/2026-08-20-ship-stash-archive-operation-migration-plan.md (Task C + ADDENDUM)
Hardening: docs/plans/2026-08-20-ship-stash-archive-operation-migration-hardening.md (H1)

*** CLOSURE-VALIDITY CORRECTION 2026-08-20 (supersedes the DISPOSITION line's "Removed from shipment 145-S membership.") ***
MEMBERSHIP: this record IS a member of shipment 145-S `custom_fields.items`, restored via the official `backlogit_add_to_shipment` operation.
WHY: excluding it left 145-S unclosable on BOTH supported paths. (1) CASCADE — `autoharness.gates.shipment_closure.classify_shipment_close_path` enumerates a root feature's children across BOTH `.backlogit/queue/` and `.backlogit/archive/`, so 137-F's six children were not fully covered by a five-item manifest and the classifier refused cascade ("feature member '137-F' has children outside the manifest"). (2) SAFE-CLOSE — P-015 then puts every omitted child in the PROTECTED set, whose baseline integrity gate requires each protected member to be present in `queue/`; both of these are already archived, which safe-close classifies as a pre-existing cascade and halts. Restoring full-child membership makes 137-F a verified fully-covered root, so the classifier now returns CASCADE, and P-015 exception item 7 explicitly tolerates a pre-archived manifest member (the cascade op is idempotent and still returns it in `archived_ids`, so shipment-reconcile's exact-match post-condition holds unchanged).
STILL NOT EXECUTABLE — MANIFEST MEMBERSHIP IS NOT A SCHEDULE: this record stays in `.backlogit/archive/` with `status: archived`, keeps its superseded-by pointer to 137.003-T, and declares no dependencies. Ship's pre-mode reconcile classifies it `pre-archived` (valid, PROCEED); Ship's Step 0.5 item 1a consistency scan halts only on `active`/`done`, never on `archived`; and Ship's Step 2 execution loop has no queued or active record to claim. Do NOT re-open, re-queue, un-archive, or schedule it.
ATOMIC OWNERSHIP UNCHANGED: 137.003-T remains the sole executable owner of this scope, and the dependency-edge removals recorded above still stand.