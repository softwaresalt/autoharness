---
artifact_type: task
created_at: 2026-08-17T23:34:52.15498Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 129.004-T
id: 129.005-T
labels:
    - commit-b
    - dry-run
    - locks
    - BED0DDED
parent_id: 129-F
priority: medium
references:
    - docs/plans/2026-08-17-backlogit-self-migration-plan.md
    - docs/plans/2026-08-17-backlogit-self-migration-hardening.md
status: rejected
title: Stop backlogit processes, prove lock release, execute migration dry-run
updated_at: 2026-08-18T05:05:17.485143Z
---

COMMIT B. Stop every backlogit.exe process by explicit PID (Stop-Process -Id; at staging time PIDs 1740, 6548, 11364 from D:\Tools\backlogit.exe). VERIFIED CONSTRAINT: all three SQLite files (.backlogit/backlogit.db, -wal, -shm) are currently held under exclusive Windows locks by those processes; a directory rename CANNOT succeed against an open handle. Get-Process alone is INSUFFICIENT proof -- Windows releases handles asynchronously after process exit -- so re-prove release with an exclusive-open probe ([System.IO.File]::Open with FileShare.None) against all three files, with bounded retry (5 attempts, 2s apart), and HALT if it still fails. Then run: backlogit migrate --workspace-dir --dry-run --format json. Review the report and confirm it describes a .backlogit -> .backlog rename and does NOT report unexpected deletions or source-import behavior. If the dry-run reports anything else, HALT and return to Stage (H9: no agent may fabricate migrate behavior not observed in its own dry-run). NOTE the dry-run itself opens the workspace in a fresh process, so the lock probe MUST be re-run after it and immediately before 129.006-T. ACCEPTANCE: zero backlogit.exe processes; exclusive-open probe SUCCEEDS on all three files; dry-run report captured verbatim; no mutation performed. Plan T005; hardening H2, H9.