---
artifact_type: task
commit: 92acc44
created_at: 2026-08-06T08:17:09.170641Z
custom_fields:
    complexity: medium
    size: S
    size_ruleset_version: 1.0.0
    size_source: agent
dependencies:
    - 111.001-T
    - 111.006-T
id: 111.007-T
parent_id: 111-F
priority: medium
status: done
title: 'C6: refresh installed dogfood agent MIRRORS (.github/agents/_orchestrator/_stage/_ship) + harness-manifest checksums for the three updated agent templates'
updated_at: 2026-08-07T06:18:37.7488716Z
---

Refresh the INSTALLED dogfood agent MIRRORS so the active self-install harness actually receives the new crash-resumption behavior authored in the templates, and keep harness-manifest checksum coherence. Corrects the gap surfaced by Copilot PR #304 findings 4 (Stage/Ship mirrors) and 5 (Orchestrator mirror): the crash-resumption work updated only the SOURCE templates (`templates/agents/_orchestrator.agent.md.tmpl` in C1/111.001-T; `templates/agents/_stage.agent.md.tmpl` + `templates/agents/_ship.agent.md.tmpl` in C5/111.006-T) but the installed dogfood mirrors `.github/agents/_orchestrator.agent.md`, `.github/agents/_stage.agent.md`, and `.github/agents/_ship.agent.md` — and their `.autoharness/harness-manifest.yaml` `artifacts[].checksum` entries — were never included, so the active dogfood Orchestrator/Stage/Ship agents would NOT receive the new behavior and the manifest would drift. WIDTH ISOLATION: this is the dogfood agent-MIRROR install/manifest family (regenerate the three installed mirrors from their updated templates + refresh the three manifest checksums), DISTINCT from the agent-template AUTHORING widths (C1/111.001-T orchestrator template, C5/111.006-T owner templates) — the same split pattern used by 111.005-T for the backlogit overlay install/manifest vs 111.004-T authoring. Single coherent width, mechanical, well under the 2-hour rule.

<!-- BEGIN:acceptance-criteria -->
1. Regenerate/refresh ALL THREE installed dogfood agent mirrors from their updated source templates via the EXISTING first-party agent-template install path (no new install primitive/schema): `.github/agents/_orchestrator.agent.md` from `templates/agents/_orchestrator.agent.md.tmpl` (C1/111.001-T); `.github/agents/_stage.agent.md` from `templates/agents/_stage.agent.md.tmpl` and `.github/agents/_ship.agent.md` from `templates/agents/_ship.agent.md.tmpl` (C5/111.006-T). Each installed mirror MUST contain the new crash-resumption / owner-agent fail-closed recovery prose (all {{VARIABLE}} customization points resolved at install time — no unresolved placeholders). 2. Ensure the ACTIVE dogfood Orchestrator, Stage, and Ship agents receive the new behavior (this is the whole point of finding 4/5): the installed `.github/agents/_*.agent.md` files are the runtime agents, not the templates. 3. Refresh the `.autoharness/harness-manifest.yaml` `artifacts[].checksum` for each of the three installed mirrors so manifest coherence holds; each checksum MUST be computed from the LF-normalized (`.gitattributes` eol=lf) COMMITTED git blob via `git cat-file -p HEAD:<path>`, NEVER a raw Windows working-tree read (per the documented CRLF/LF checksum gotcha in docs/compound and the existing manifest notes for these three files). 4. Update the manifest `note` for each of the three mirror entries to record this refresh (crash-resumption / owner-agent fail-closed recovery lifecycle, 111-F / 119-S). 5. Idempotent: re-install/re-tune does NOT duplicate the artifact entries; the three entries already exist and are updated in place. 6. NO new manifest primitive, NO schema change: reuse the EXISTING single `artifacts[].checksum` surface only. 7. Verification (manifest-checksum coherence for the three mirrors + installed-mirror section-presence) is owned by 111.003-T AC#9; this task performs the install/mirror + checksum refresh only. 8. WIDTH ISOLATION preserved: dogfood agent-mirror install/manifest family only — this task authors NO template prose (that is C1/111.001-T and C5/111.006-T) and touches NO backlogit-overlay install (that is 111.005-T).
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Depends on C1/111.001-T (orchestrator template authored) AND C5/111.006-T (Stage + Ship owner templates authored) — the mirrors can only be regenerated after their source templates are final. Source: Copilot PR #304 review findings 4 (installed `.github/agents/_stage.agent.md` + `_ship.agent.md` mirrors + harness-manifest checksum + verification) and 5 (installed `.github/agents/_orchestrator.agent.md` mirror + harness-manifest checksum/verification; ensure the active dogfood Orchestrator receives behavior). The reviewer permitted either folding this scope into 111.001-T/111.006-T or splitting into a bounded install/mirror task; split chosen to preserve the plan's strict WIDTH ISOLATION (authoring vs install/manifest), mirroring the 111.004-T (author) / 111.005-T (install+manifest) split. Prior art for the LF-normalized git-blob checksum procedure: the existing `_orchestrator/_stage/_ship.agent.md` manifest notes (115-S/116-S) and the docs/compound CRLF/LF checksum gotcha. Verification owned by 111.003-T AC#9. Single mechanical width, well under 2h.
<!-- END:implementation-notes -->