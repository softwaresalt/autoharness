# Shipment Reconciliation Report — 174-S (mode: safe-close, CASCADE path)

- **Timestamp**: 2026-09-17T06:25:22Z
- **Shipment**: 174-S
- **Merge commit**: `d8615e9d5eb93a1d1616a735ca1433236868bee1` (PR #454, merge commit strategy, two parents — P-009 satisfied)
- **Invocation context**: Ship Step 6 closure, post-merge, under a `.backlogit/queue/174-S.md`
  single-writer lock (token `f815ad3e...`, acquired and released via
  `templates/skills/file-lock/scripts/{acquire,release}_lock.ps1 -WorkspaceRoot C:\Source\GitHub\autoharness`,
  since the installed `.github/skills/file-lock/` mirror lacks the scripts directory in this
  workspace).

## Step 0 — Load, Snapshot, Classify

**Step 0(a) — manifest loaded** via `backlogit_get_shipment(174-S)`:
`items: [166.002-T, 166.001-T, 166.003-T, 166.004-T, 166.005-T, 166.006-T, 166-F]`.

**Step 0(b) — pre-close `parent_id`/declared-`status` snapshot** (read from each task's own
frontmatter, all found in `.backlogit/archive/` — pre-archived by prior task-completion in the
merged feature branch, not by this closure step):

| Task ID | Location (pre-close) | `parent_id` | Declared `status` |
|---|---|---|---|
| 166.001-T | archive/ | 166-F | done |
| 166.002-T | archive/ | 166-F | done |
| 166.003-T | archive/ | 166-F | done |
| 166.004-T | archive/ | 166-F | done |
| 166.005-T | archive/ | 166-F | done |
| 166.006-T | archive/ | 166-F | done |

No task item was found in **both** or **neither** location — no
`RECONCILE_FAIL_SNAPSHOT_AMBIGUOUS`/`RECONCILE_FAIL_SNAPSHOT_MISSING`. All 6 declared
`status: done`, **not** `status: archived` — none is truly engine-inert by declared status,
despite residing in `archive/` by location alone (location ≠ declared status, per the skill's
own governing rule).

**Step 0(c) — classification** via `classify_shipment_close_path(items, ".backlogit")`
(`src/autoharness/gates/shipment_closure.py`):

```
ClosePathDecision(
    close_path=CASCADE,
    reason='every feature member is a root whose out-of-manifest descendants are engine-inert; cascade close is permitted',
    qualifying_feature_ids=('166-F',),
    out_of_manifest_descendant_ids=(),
)
```

`166-F`'s only `parent_id` descendants are the 6 manifest task items themselves (all
`166.00X-T` records declare `parent_id: 166-F`; no other backlog record anywhere declares
`parent_id: 166-F`) — the out-of-manifest descendant set is the **empty set**, which
vacuously satisfies "every out-of-manifest descendant is engine-inert." `CASCADE` selected.

**Linked-deliberation snapshot extension**: `166-F`'s frontmatter has no
`custom_fields.source_deliberation_id`, and neither its description nor its `references`
list (which names `docs/decisions/2026-09-15-flat-manifest-shipment-closure-deliberation.md`
by **path**, not by embedded ID) contains a literal match for the engine's
`\b(?:DL\d+|[0-9]+(?:\.[0-9]+)*-DL)\b` matcher. Validated linked-deliberation set: **empty**.

## Pre-Invocation Classifier Revalidation (round-12/13 hardening)

Immediately before invocation, re-ran `classify_shipment_close_path` fresh (never reusing
Step 0(c)'s enumeration) — result **identical**: same `CASCADE` verdict, same
`qualifying_feature_ids=('166-F',)`, same `out_of_manifest_descendant_ids=()`. Independently
re-collected 166-F's linked-deliberation snapshot using the same three engine-defined
sources — result **identical**: empty set. No drift detected on either axis; proceeded to
baseline-fingerprint capture.

## Baseline-Fingerprint Capture (INV-7, before invocation)

Observation set (out-of-manifest descendants) is **empty** — no descendant exists to
fingerprint. This is the trivial/vacuous case of the baseline-invariance snapshot.

## Step 1 — Cascade Invocation

```
backlogit shipment ship 174-S \
  --sha d8615e9d5eb93a1d1616a735ca1433236868bee1 \
  --message "Merge pull request #454: 174-S flat-manifest shipment closure (166-F)" \
  --author "Derek Williams <42183845+softwaresalt@users.noreply.github.com>"
```

Result:

```json
{
  "shipment_id": "174-S",
  "shipment_status": "shipped",
  "archived_ids": ["166.001-T","166.002-T","166.003-T","166.004-T","166.005-T","166.006-T","166-F","174-S"],
  "returned_ids": [],
  "commit_sha": "d8615e9d5eb93a1d1616a735ca1433236868bee1"
}
```

## Step 2 — `returned_ids` Verification

`returned_ids` is `[]` (empty). **PASS.**

## Step 3 — Two-Set `allowed_ids` / `required_ids` Gate

**`allowed_ids`** = manifest task items (166.001-T..166.006-T) + qualifying feature member
(166-F) + validated linked deliberations (none) + shipment record (174-S) =
`{166.001-T, 166.002-T, 166.003-T, 166.004-T, 166.005-T, 166.006-T, 166-F, 174-S}` (8 members).

**`required_ids`** = shipment record (174-S, unconditional) + qualifying feature member
(166-F, unconditional) + every other `allowed_ids` member not truly `status: archived`
pre-close = all 6 tasks (each declared `status: done` pre-close, not `archived`) =
`{174-S, 166-F, 166.001-T, 166.002-T, 166.003-T, 166.004-T, 166.005-T, 166.006-T}` (8 members,
identical set to `allowed_ids` here since no member was pre-archived).

- **Unexpected-artifact check**: `archived_ids − allowed_ids` = `{}` (empty). **PASS.**
- **Missing-required-artifact check**: `required_ids − archived_ids` = `{}` (empty). **PASS.**

## Step 4 — `parent_id` Preservation

Re-read all 6 archived tasks' frontmatter post-invocation: every one still declares
`parent_id: 166-F`, unchanged from the Step 0(b) snapshot. **PASS.**

## Step 5 — Out-of-Manifest Descendant Baseline Invariance (INV-7/INV-10)

Observation set was empty at baseline-fingerprint capture (no out-of-manifest descendants
exist under `166-F`). Vacuously **PASS** — nothing to have drifted.

## Step 6 — Cascade-Close Report Summary

| Check | Result |
|---|---|
| Classifier verdict | `CASCADE` |
| Qualifying feature IDs | `166-F` |
| Validated linked deliberation IDs | (none) |
| Pre-invocation revalidation (classifier + linked-deliberation) | identical, no drift |
| `returned_ids` | `[]` |
| `archived_ids − allowed_ids` | `{}` |
| `required_ids − archived_ids` | `{}` |
| `parent_id` preservation | all 6 tasks unchanged |
| Out-of-manifest baseline invariance | vacuously satisfied (empty observation set) |

## Step 7 — Gate Decision

All checks PASS. **`recommendation: CLOSED`.** Proceeding to post-mode reconciliation.

## Post-Mode Reconciliation

Scanned `.backlogit/archive/` for all 7 manifest items (166.001-T..166.006-T, 166-F):
all present. The shipment record `174-S` is also present in `.backlogit/archive/` with
`archived_status: shipped`, `status: archived`. Zero `missing`. Unrelated pre-existing
queued shipment `166-S` (SHIP-8, unrelated `references`) confirmed untouched
(`status: queued`, unchanged).

**Post-mode result: PROCEED.** Shipment 174-S closure is complete.
