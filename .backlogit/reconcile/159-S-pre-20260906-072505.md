---
shipment_id: 159-S
mode: pre
expected_status: done
timestamp: 2026-09-06T07:25:05Z
---

# Shipment Reconcile Report — 159-S — Pre-Mode

## Manifest (from `backlogit shipment get 159-S`)

- 151-F (feature) — status: active
- 151.001-T (task) — status: done
- 151.002-T (task) — status: done
- 151.003-T (task) — status: done
- 151.004-T (task) — status: done
- 151.005-T (task) — status: done
- 151.006-T (task) — status: done
- 151.007-T (task) — status: done — STOWAWAY (445C1DFB / 032-DL), already-implemented checkpoint hygiene repair, resides in `.backlogit/archive/` pre-run

## Per-item classification

| Item | artifact_type | Location | Declared status | Classification |
|---|---|---|---|---|
| 151-F | feature | queue | active | reference/root-verified (see scope note below) |
| 151.001-T | task | queue | done | matched |
| 151.002-T | task | queue | done | matched |
| 151.003-T | task | queue | done | matched |
| 151.004-T | task | queue | done | matched |
| 151.005-T | task | queue | done | matched |
| 151.006-T | task | queue | done | matched |
| 151.007-T | task | archive | done | pre-archived (already archived before this run; declared status `done`, NOT `archived` — location is descriptive only, never truly-archived for the pre-close snapshot) |

**Scope note on 151-F (task-artifact-filter parity):** the per-item
`expected_status` comparison is applied to task-artifact manifest members
only, mirroring the mandatory task-artifact filter this same skill already
applies to the Shipment-Record-Status Classification and to Ship's own
Step 0.5 intake early-warning. A covering feature that is cascade-eligible
(confirmed below) is legitimately `active`, never `done`, immediately before
the cascade close: `backlogit_ship_shipment`'s own engine
(`setArtifactStatus(featureID, StatusDone, ...)`) is what transitions it, not
prior task completion. Comparing 151-F's pre-close status against a single
uniform task-oriented `expected_status` value would misclassify a
structurally-expected state as `status-mismatch`; 151-F's correctness is
instead verified directly via the P-015 root/coverage classifier (below),
which is the authoritative check for feature-member eligibility.

## Orphan scan

Scanned `.backlogit/queue/*.md` for any frontmatter declaring
`shipment_id: 159-S` not present in the manifest. This backlogit version's
item schema does not carry a `shipment_id` frontmatter field on task/feature
records (confirmed on 151-F and 151.001-T), so the scan is structurally
empty by convention, not a false negative. **Result: no orphans.**

## Shipment-record-status classification

Record status: `active` → **record-consistent** (always consistent for
`active`/`done` per the classification table's out-of-scope rule for those
two statuses; not evaluated further).

## P-015 close-path classification (reverified immediately before closure)

`classify_shipment_close_path(['151-F','151.001-T',...,'151.007-T'], '.backlogit')`
→ `CASCADE`, qualifying_feature_ids=('151-F',), reason: "every feature
member is a verified fully-covered root; cascade close is permitted".
151-F confirmed root (no `parent_id`), exactly 7 live descendants
(151.001-T .. 151.007-T, enumerated across both `queue/` and `archive/`),
all 7 present in the manifest. No linked deliberation declared on 151-F's
`custom_fields` (no `source_deliberation_id`).

## Gate decision

All items `matched` or `pre-archived`; no orphans; shipment-record-status
`record-consistent`.

**recommendation: PROCEED**

Proceeding to Safe-Close Mode Step 0 → Cascade Close Sub-Procedure (P-015
verified fully-covered-root exception).
