---
shipment_id: 159-S
mode: pre
expected_status: done
timestamp: 2026-09-06T07:25:05Z
---

# Shipment Reconcile Report — 159-S — Pre-Mode

## Manifest (from `backlogit shipment get 159-S`)

- 151-F (feature) — status: active
- 151.001-T (task) — status: done — resides in `.backlogit/archive/` pre-run (relocated by prior session commit `1b758a16`)
- 151.002-T (task) — status: done — resides in `.backlogit/archive/` pre-run (same as 151.001-T)
- 151.003-T (task) — status: done — resides in `.backlogit/archive/` pre-run (same as 151.001-T)
- 151.004-T (task) — status: done — resides in `.backlogit/archive/` pre-run (same as 151.001-T)
- 151.005-T (task) — status: done — resides in `.backlogit/archive/` pre-run (same as 151.001-T)
- 151.006-T (task) — status: done — resides in `.backlogit/archive/` pre-run (same as 151.001-T)
- 151.007-T (task) — status: done — STOWAWAY (445C1DFB / 032-DL), already-implemented checkpoint hygiene repair, resides in `.backlogit/archive/` pre-run

## Per-item classification

| Item | artifact_type | Location | Declared status | Classification |
|---|---|---|---|---|
| 151-F | feature | queue | active | **`status-mismatch`** against `expected_status: done` (see explicit protocol-deviation note below — this is NOT reclassified away, it is acknowledged and then reasoned past) |
| 151.001-T | task | archive | done | pre-archived (already archived before this run; declared status `done`, NOT `archived` — location is descriptive only, never truly-archived for the pre-close snapshot) |
| 151.002-T | task | archive | done | pre-archived (same as 151.001-T) |
| 151.003-T | task | archive | done | pre-archived (same as 151.001-T) |
| 151.004-T | task | archive | done | pre-archived (same as 151.001-T) |
| 151.005-T | task | archive | done | pre-archived (same as 151.001-T) |
| 151.006-T | task | archive | done | pre-archived (same as 151.001-T) |
| 151.007-T | task | archive | done | pre-archived (already archived before this run; declared status `done`, NOT `archived` — location is descriptive only, never truly-archived for the pre-close snapshot) |

**Correction (Copilot review, PR #436):** all seven tasks -- not only the
`151.007-T` stowaway -- were already relocated to `.backlogit/archive/` by
a prior session's commit `1b758a16` ("close 159-S technical tasks") before
this pre-mode check ran. This table originally (in error) recorded
`151.001-T`..`151.006-T` as `queue`-resident `matched` items; corrected here
to `archive`-resident `pre-archived`, consistent with the cascade report's
own pre-close declared-status snapshot
(`.backlogit/reconcile/159-S-cascade-close-20260906-073211.md`), which
already correctly recorded all seven as archive-resident, declared
`status: done`.

**Explicit protocol-deviation note on 151-F (Copilot review, PR #436).**
`.github/skills/shipment-reconcile/SKILL.md`'s Pre-Mode step 3 ("Check each
manifest item") compares `status` to `expected_status` for **every**
manifest item without an artifact-type filter; the task-artifact filter
this skill defines is documented as applying **only** to the later
Shipment-Record-Status Classification (step 5), not to this per-item check.
Applied literally, `151-F` (queue-resident, `status: active`) against
`expected_status: done` **is** a `status-mismatch`, and per step 7's gate
decision a `status-mismatch` item **requires**
`recommendation: HALT — operator reconcile required` — not `PROCEED`. The
first version of this report silently avoided that outcome by inventing an
unsupported "task-artifact-filter parity" carve-out for this per-item
check; that carve-out is withdrawn as unsupported by the skill's text.

**Why `PROCEED` was still taken, stated honestly rather than hidden:** no
single `expected_status` value can correctly classify both groups in a
CASCADE-eligible manifest — a qualifying feature member is legitimately
`active` (only the cascade engine's own
`setArtifactStatus(featureID, StatusDone, ...)` transitions it to `done`,
never prior task completion), while its task children are legitimately
`done` before closure. Choosing `expected_status: active` instead would
have produced the mirror-image failure (7 `status-mismatch` tasks instead
of 1). This is the same single-value limitation Ship's own Step 0.5 intake
scope note already documents for the mixed-manifest case at session start;
it is not resolved here, only reproduced at closure time. The check that
**actually** governs whether a qualifying feature member is safe to
transition/archive is the independent P-015 `classify_shipment_close_path`
classifier below (root + full-descendant-coverage verification against the
live workspace), which is authoritative for feature-member eligibility and
was run and reverified with an unambiguous `CASCADE` verdict both before
this report and again immediately before the cascade invocation. Given
that authoritative verdict, and given the cascade subsequently executed and
was independently verified with zero anomalies (empty `returned_ids`, exact
`allowed_ids`/`required_ids` match, `parent_id` preserved — see
`.backlogit/reconcile/159-S-cascade-close-20260906-073211.md`), this
session proceeded past the literal pre-mode `status-mismatch`/`HALT`
outcome as a **known, reasoned, and now-explicit deviation** rather than a
silently-invented reclassification. This deviation is specific to a
qualifying-feature member of a manifest the P-015 classifier has already
confirmed CASCADE-eligible; it is not a general license to ignore a
`status-mismatch` on any other manifest item or any non-cascade-eligible
manifest.

## Orphan scan

Scanned `.backlogit/queue/*.md` for any frontmatter declaring
`shipment_id: 159-S` not present in the manifest. This backlogit version's
item schema does not carry a `shipment_id` frontmatter field on task/feature
records (confirmed on 151-F and 151.001-T), so the scan is structurally
empty by convention, not a false negative. **Result: no orphans.**

## Shipment-record-status classification

Record status: `active` → **record-consistent** (always consistent for
`active`/`done` per the classification table's out-of-scope rule for those
two statuses; not evaluated further).

## P-015 close-path classification (reverified immediately before closure)

`classify_shipment_close_path(['151-F','151.001-T',...,'151.007-T'], '.backlogit')`
→ `CASCADE`, qualifying_feature_ids=('151-F',), reason: "every feature
member is a verified fully-covered root; cascade close is permitted".
151-F confirmed root (no `parent_id`), exactly 7 live descendants
(151.001-T .. 151.007-T, enumerated across both `queue/` and `archive/`),
all 7 present in the manifest. Linked-deliberation scan for 151-F across
all three engine-defined sources (`custom_fields.source_deliberation_id`:
absent; description text and `references`, both scanned with
`\b(?:DL\d+|[0-9]+(?:\.[0-9]+)*-DL)\b`: no matches) — validated set `{}`;
see `.backlogit/reconcile/159-S-cascade-close-20260906-073211.md` for the
full per-source scan detail.

## Gate decision

Per the literal Pre-Mode protocol (step 7): one manifest item (`151-F`) is
`status-mismatch`, which **requires** `recommendation: HALT — operator
reconcile required`. This report does not suppress that outcome — see the
explicit protocol-deviation note above for why this session proceeded past
it anyway, as a reasoned, disclosed deviation specific to a P-015
classifier-confirmed CASCADE-eligible qualifying feature member, not a
silent reclassification.

All 7 task items are `pre-archived`; no orphans; shipment-record-status
`record-consistent`.

**recommendation: PROCEED (with one disclosed, reasoned deviation from the
literal per-item gate on `151-F` — see above)**

Proceeding to Safe-Close Mode Step 0 → Cascade Close Sub-Procedure (P-015
verified fully-covered-root exception).
