---
shipment_id: 161-S
mode: pre
expected_status: active
timestamp: 2026-09-11T16:25:21-07:00
---

# Shipment Reconcile Report — 161-S — Pre-Mode

## Manifest (from `backlogit shipment get 161-S`)

- 153-F (feature) — status: active — resides in `.backlogit/queue/`
- 153.001-T (task) — status: done — resides in `.backlogit/archive/` pre-run
- 153.002-T (task) — status: done — resides in `.backlogit/archive/` pre-run
- 153.003-T (task) — status: done — resides in `.backlogit/archive/` pre-run
- 153.004-T (task) — status: done — resides in `.backlogit/archive/` pre-run
- 153.005-T (task) — status: done — resides in `.backlogit/archive/` pre-run

## Per-item classification

| Item | artifact_type | Location | Declared status | Classification |
|---|---|---|---|---|
| 153-F | feature | queue | active | matches `expected_status: active` (the shipment's own record was still `active` at this scan; 153-F was its qualifying feature, itself pre-cascade `active` — this is the mirror case of 159-S, resolved here by scanning at `expected_status: active` rather than `done`, since the shipment record itself had not yet transitioned) |
| 153.001-T | task | archive | done | pre-archived (already relocated to archive before this run by the normal Step 2 task-completion loop during 161-S execution; declared status `done`, NOT `archived` — location is descriptive only, never truly-archived for the pre-close snapshot) |
| 153.002-T | task | archive | done | pre-archived (same as 153.001-T) |
| 153.003-T | task | archive | done | pre-archived (same as 153.001-T) |
| 153.004-T | task | archive | done | pre-archived (same as 153.001-T) |
| 153.005-T | task | archive | done | pre-archived (same as 153.001-T) |

No `status-mismatch` items on this scan (unlike the 159-S precedent, this
check ran with `expected_status: active` matching the shipment record's own
pre-close status, which also matches the qualifying feature's pre-close
status — both were still `active`, consistent with the cascade engine being
the only actor that later transitions the feature to `done`).

## Orphan scan

Scanned `.backlogit/queue/*.md` for any frontmatter declaring
`shipment_id: 161-S` not present in the manifest. This backlogit version's
item schema does not carry a `shipment_id` frontmatter field on task/feature
records, so the scan is structurally empty by convention, not a false
negative. Additionally confirmed via directory listing and grep that no
other `153.*` artifacts exist beyond the 5 known manifest tasks. **Result: no
orphans.**

## Shipment-record-status classification

Record status: `active` → **record-consistent** (always consistent for
`active`/`done` per the classification table's out-of-scope rule for those
two statuses; not evaluated further).

## P-015 close-path classification (run live, immediately before closure)

`classify_shipment_close_path(['153-F','153.001-T','153.002-T','153.003-T','153.004-T','153.005-T'], '.backlogit')`
→ `CASCADE`, `qualifying_feature_ids=('153-F',)`, reason: "every feature
member is a verified fully-covered root; cascade close is permitted".
153-F confirmed root (no `parent_id`), exactly 5 live descendants
(153.001-T .. 153.005-T, enumerated across both `queue/` and `archive/`),
all 5 present in the manifest. A sibling `153-S.md` archive record was
confirmed to be an unrelated different shipment artifact, not a descendant
of 153-F.

## Linked-deliberation scan for 153-F (run live, before the cascade invocation)

Scanned the live `.backlogit/queue/153-F.md` record (read in full
immediately before the Step 1 cascade invocation below — genuinely live,
not reconstructed post-hoc) across all three engine-defined sources:

* **Source 1 — `custom_fields.source_deliberation_id`**: field absent from
  153-F's `custom_fields` (only `harness_status: pending` is declared). No
  candidate ID.
* **Source 2 — description text**, matched with the engine's own
  `\b(?:DL\d+|[0-9]+(?:\.[0-9]+)*-DL)\b` pattern: scanned the full
  description body ("Six findings in the four concurrency-pack scripts
  copied verbatim into scripts/. ... Fix MUST be template-first plus
  re-copy plus manifest checksum refresh. Source stash 74C62374.").
  `74C62374` is a stash ID (also present as a 153-F label), not a
  deliberation ID, and does not match the pattern. **No matches.**
* **Source 3 — `references`**, same matcher applied to each of the two
  `references` entries (`docs/plans/2026-08-31-ship3-file-lock-script-security-hardening-plan.md`,
  `docs/decisions/2026-08-31-dark-factory-staging-triage-and-shipment-portfolio.md`).
  **No matches** in either entry.

**Validated linked-deliberation ID set for `153-F`: `{}` (empty).** All
three engine-defined sources were scanned live and independently produced
no candidate.

## Gate decision

All 5 task items are `pre-archived`; the qualifying feature matches the
scan's `expected_status: active`; no orphans; shipment-record-status
`record-consistent`; P-015 classifier confirms `CASCADE`; linked-deliberation
scan (live) confirms empty set.

**recommendation: PROCEED**

Proceeding to Safe-Close Mode Step 0 → Cascade Close Sub-Procedure (P-015
verified fully-covered-root exception).
