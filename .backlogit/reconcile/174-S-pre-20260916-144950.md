# Shipment Reconciliation Report — 174-S (mode: pre)

- **Timestamp**: 2026-09-16T21:49:50Z
- **Shipment**: 174-S
- **Expected status**: active (shipment claimed this session at 2026-09-16 21:50:04Z; cascade
  moved shipment record + all manifest items to `active`)
- **Invocation context**: Ship Step 0.5 intake (post-claim); not Ship Step 6 closure — no
  `file-lock` acquisition required per Required Protocol step 1.

## Manifest Item Classification

| Item ID | artifact_type | Location | Declared status | Classification |
|---|---|---|---|---|
| 166.002-T | task | queue/ | active | matched |
| 166.001-T | task | queue/ | active | matched |
| 166.003-T | task | queue/ | active | matched |
| 166.004-T | task | queue/ | active | matched |
| 166.005-T | task | queue/ | active | matched |
| 166.006-T | task | queue/ | active | matched |
| 166-F | feature | queue/ | active | matched |

All 7 manifest items located in `.backlogit/queue/`, declared `status: active`,
matching `expected_status: active`. Zero `missing`, zero `status-mismatch`,
zero `pre-archived`.

## Orphan Scan

Scanned `.backlogit/queue/*.md` for frontmatter declaring `shipment_id: 174-S`.
Zero matches. (Note: this backlogit workspace's task/feature records do not carry
a back-reference `shipment_id` field — membership is tracked solely via the
shipment record's own `custom_fields.items`. Zero orphan-scan hits is the
expected/normal result for this data model, not a defect.)

## Shipment-Record-Status Classification

Task-artifact filter applied: 6 of 7 manifest entries are `artifact_type: task`
(166-F excluded from aggregation as the covering feature).

Record's own status: `active` → **record-consistent** (by definition; active is
the normal in-progress lifecycle state, not evaluated further per the Required
Protocol's scope note).

## Gate Decision

**recommendation: PROCEED**

All items `matched`, zero orphans, record-status `record-consistent`.
