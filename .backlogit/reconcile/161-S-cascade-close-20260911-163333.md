---
shipment_id: 161-S
mode: safe-close (cascade)
merge_commit_sha: 6da9aed580f9ed232a6871281f47567c9060ffa8
timestamp: 2026-09-11T16:33:33-07:00
---

# Shipment Reconcile Report — 161-S — Cascade Close Sub-Procedure (P-015 verified fully-covered-root exception)

## Step 0 — Classifier reverification (immediately before closure)

`classify_shipment_close_path` re-run against the live manifest
(`153-F`, `153.001-T`..`153.005-T`) over `.backlogit`:

```
ClosePathDecision(close_path=CASCADE, reason='every feature member is a
verified fully-covered root; cascade close is permitted',
qualifying_feature_ids=('153-F',))
```

153-F verified root (no `parent_id`); exactly 5 live descendants enumerated
across `queue/` + `archive/` (153.001-T..153.005-T), all 5 present in the
manifest. A sibling `153-S.md` archive record was confirmed to be an
unrelated, different shipment artifact — not a descendant of 153-F.

## Linked-deliberation scan (153-F, all three engine-defined sources — run live)

See the Pre-Mode report (`161-S-pre-20260911-162521.md`) for the full
per-source detail. This scan was executed live against
`.backlogit/queue/153-F.md` immediately before the Step 1 cascade
invocation below (verified by the file's queue-residence in `git status`
at scan time) — not reconstructed post-hoc.

**Validated linked-deliberation ID set for `153-F`: `{}` (empty).**

## Pre-archived manifest members (expected and tolerated)

At the time this sub-procedure began, all 5 task members already resided in
`.backlogit/archive/` (location only) while declaring `status: done` (NOT
`status: archived`) — this session's own Step 2 task-completion loop
archived them individually as each task was completed during 161-S
execution, ahead of this shipment-record closure. Per the sub-procedure's
own tolerance rule, this pre-archived-by-location state is expected and does
not disqualify CASCADE, since Step 0(c)'s classifier already scans both
`queue/` and `archive/`. The covering feature `153-F` remained in `queue/`
with declared `status: active`.

## Pre-close declared-status + parent_id snapshot (captured before the cascade invocation)

| ID | Location (pre-close) | Declared status (pre-close) | parent_id |
|---|---|---|---|
| 153-F | queue | active | (none — root) |
| 153.001-T | archive | done | 153-F |
| 153.002-T | archive | done | 153-F |
| 153.003-T | archive | done | 153-F |
| 153.004-T | archive | done | 153-F |
| 153.005-T | archive | done | 153-F |
| 161-S (shipment record) | queue | active | n/a |

None of the above were truly `status: archived` pre-close, so every one is a
`required_ids` member (in addition to the shipment record and qualifying
feature being unconditionally required regardless of pre-close status).

## Step 1 — Cascade invocation

```
backlogit shipment ship 161-S --sha 6da9aed580f9ed232a6871281f47567c9060ffa8 \
  --message "Merge pull request #444 from softwaresalt/feat/161-s-ship-3-file-lock-script-security-hardening-template-first" \
  --author "Derek Williams <42183845+softwaresalt@users.noreply.github.com>"
```

Result:

```json
{
  "shipment_id": "161-S",
  "shipment_status": "shipped",
  "archived_ids": [
    "153.001-T","153.002-T","153.003-T","153.004-T","153.005-T",
    "153-F","161-S"
  ],
  "returned_ids": [],
  "commit_sha": "6da9aed580f9ed232a6871281f47567c9060ffa8"
}
```

## Step 2 — `returned_ids` check

`returned_ids` is empty (`[]`). PASS — no classifier/engine mismatch.

## Step 3 — Two-set gate (`allowed_ids` / `required_ids`)

* `allowed_ids` = {153.001-T, 153.002-T, 153.003-T, 153.004-T, 153.005-T,
  153-F, 161-S} (7 ids — manifest tasks + qualifying feature 153-F +
  shipment record 161-S; validated linked-deliberation set for 153-F is
  `{}` per the live three-source scan above, so no linked deliberation ID
  is added)
* `required_ids` = {161-S, 153-F} (unconditional) ∪ {153.001-T..153.005-T}
  (not truly archived pre-close) = same 7 ids
* `archived_ids - allowed_ids` = `{}` — PASS (no unexpected artifact archived)
* `required_ids - archived_ids` = `{}` — PASS (no required artifact left
  unarchived)

## Step 4 — `parent_id` preservation check

Re-read from `.backlogit/archive/` post-close:

| ID | status (post-close) | archived_status | parent_id (post-close) | matches pre-close snapshot |
|---|---|---|---|---|
| 153.001-T | archived | done | 153-F | yes |
| 153.002-T | archived | done | 153-F | yes |
| 153.003-T | archived | done | 153-F | yes |
| 153.004-T | archived | done | 153-F | yes |
| 153.005-T | archived | done | 153-F | yes |

PASS — no `parent_id` cleared or altered on any archived task.

## Shipment record and qualifying feature — archived provenance

| ID | status | archived_status |
|---|---|---|
| 161-S | archived | shipped |
| 153-F | archived | done |

`161-S` carries `archived_status: shipped` (required terminal provenance for
the shipment record). `153-F` carries `archived_status: done` (expected
engine behavior for a qualifying feature member — `setArtifactStatus`
unconditionally forces the feature to `done` before `archiveItems` archives
it; this is not `shipped` and is not expected to be).

## Post-Mode

1. Archive file for shipment itself: `.backlogit/archive/161-S.md` present. PASS.
2. Per-item archive check: all 7 members (`153-F`, `153.001-T`..`153.005-T`,
   `161-S`) have corresponding archive files present. PASS.
3. Deleted-file guard: `git status -- ".backlogit/archive/"` shows only
   modifications/additions (`M`, `??`, staged rename `161-S.md`
   queue→archive) consistent with this closure's own archival — no
   unexpected deletions. PASS.
4. File-lock released: `.backlogit/queue/.161-S.md.lock` released via
   `scripts/release_lock.ps1` with the session's captured token
   (`57e48d1f...`). The lock-target file itself had already relocated to
   `archive/` by the time of release (expected — release operates on the
   lock sentinel path, not the target's current location); confirmed
   post-release that no lock sentinel remains at
   `.backlogit/queue/.161-S.md.lock`.

## Gate decision

`returned_ids` empty; `archived_ids - allowed_ids` empty; `required_ids -
archived_ids` empty; every `parent_id` preserved; post-mode archive presence
and deleted-file guard both PASS; linked-deliberation scan executed live
(no reconstruction/disclosure deviation required, unlike the 159-S
precedent).

**recommendation: CLOSED**
