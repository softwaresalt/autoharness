---
shipment_id: 159-S
mode: safe-close (cascade)
merge_commit_sha: cb474a0a7d1fdfe2bbfe0dd3e2a6110aefb533ab
timestamp: 2026-09-06T07:32:11Z
---

# Shipment Reconcile Report — 159-S — Cascade Close Sub-Procedure (P-015 verified fully-covered-root exception)

## Step 0 — Classifier reverification (immediately before closure)

`classify_shipment_close_path` re-run against the live manifest
(`151-F`, `151.001-T`..`151.007-T`) over `.backlogit`:

```
ClosePathDecision(close_path=CASCADE, reason='every feature member is a
verified fully-covered root; cascade close is permitted',
qualifying_feature_ids=('151-F',))
```

151-F verified root (no `parent_id`); exactly 7 live descendants enumerated
across `queue/` + `archive/` (151.001-T..151.007-T), all 7 present in the
manifest.

## Linked-deliberation scan (151-F, all three engine-defined sources — corrected)

Reconstructed from preserved pre-close evidence without mutating any
archived identity: commit `1b758a16` (`.backlogit/queue/151-F.md`, the
last pre-archive revision of the qualifying feature, `updated_at:
2026-09-06T02:09:44Z`) is byte-identical, on `custom_fields`, description
body, and `references`, to the current
`.backlogit/archive/151-F.md`, so the scan below is evaluated directly
against the archived record with no loss of pre-close fidelity.

* **Source 1 — `custom_fields.source_deliberation_id`**: field absent
  from `151-F`'s `custom_fields` (only `harness_status: pending` is
  declared). No candidate ID.
* **Source 2 — description text**, matched with the engine's own
  `\b(?:DL\d+|[0-9]+(?:\.[0-9]+)*-DL)\b` pattern (`internal/core.deliberationIDPattern`,
  never a broader "any embedded ID" reading): scanned the full description
  body ("Two shipped verify-workspace assertions ... Source stash
  053E2BD2 (primary) and B698F01B (documentation-accuracy half only).").
  `053E2BD2` and `B698F01B` are stash IDs (also present as `151-F` labels),
  not deliberation IDs, and neither matches the pattern. **No matches.**
* **Source 3 — `references`**, same matcher applied to each of the three
  `references` entries (`docs/plans/2026-08-31-ship1-v1_5_0-guardrail-contract-restoration-plan.md`,
  `docs/decisions/2026-08-31-dark-factory-staging-triage-and-shipment-portfolio.md`,
  `docs/decisions/2026-08-31-v1_5_0-guardrail-template-contract-mismatch-spike.md`).
  **No matches** in any of the three entries.

**Validated linked-deliberation ID set for `151-F`: `{}` (empty).** All
three engine-defined sources were scanned and independently produced no
candidate; the empty set is itself the validated result, not an
unchecked absence. This confirms (rather than merely assumes) that
`allowed_ids`/`required_ids` in Step 3 below correctly omit any linked
deliberation.

## Pre-archived manifest members (expected and tolerated)

At the time this sub-procedure began, all 7 task members already resided in
`.backlogit/archive/` (location only) while declaring `status: done` (NOT
`status: archived`) — a prior session's commit `1b758a16` ("close 159-S
technical tasks") had completed and relocated them ahead of this session's
shipment-record closure, which was itself blocked only by the stale queue
lock. Per the sub-procedure's own tolerance rule, this pre-archived-by-location
state is expected and does not disqualify CASCADE, since Step 0(c)'s
classifier already scans both `queue/` and `archive/`. The covering feature
`151-F` remained in `queue/` with declared `status: active`.

## Pre-close declared-status + parent_id snapshot (captured before the cascade invocation)

| ID | Location (pre-close) | Declared status (pre-close) | parent_id |
|---|---|---|---|
| 151-F | queue | active | (none — root) |
| 151.001-T | archive | done | 151-F |
| 151.002-T | archive | done | 151-F |
| 151.003-T | archive | done | 151-F |
| 151.004-T | archive | done | 151-F |
| 151.005-T | archive | done | 151-F |
| 151.006-T | archive | done | 151-F |
| 151.007-T | archive | done | 151-F |
| 159-S (shipment record) | queue | active | n/a |

None of the above were truly `status: archived` pre-close, so every one is a
`required_ids` member (in addition to the shipment record and qualifying
feature being unconditionally required regardless of pre-close status).

## Step 1 — Cascade invocation

```
backlogit shipment ship 159-S --sha cb474a0a7d1fdfe2bbfe0dd3e2a6110aefb533ab \
  --message "Merge pull request #435" \
  --author "Derek Williams <42183845+softwaresalt@users.noreply.github.com>"
```

Result:

```json
{
  "shipment_id": "159-S",
  "shipment_status": "shipped",
  "archived_ids": [
    "151.001-T","151.002-T","151.003-T","151.004-T",
    "151.005-T","151.006-T","151.007-T","151-F","159-S"
  ],
  "returned_ids": [],
  "commit_sha": "cb474a0a7d1fdfe2bbfe0dd3e2a6110aefb533ab"
}
```

## Step 2 — `returned_ids` check

`returned_ids` is empty (`[]`). PASS — no classifier/engine mismatch.

## Step 3 — Two-set gate (`allowed_ids` / `required_ids`)

* `allowed_ids` = {151.001-T, 151.002-T, 151.003-T, 151.004-T, 151.005-T,
  151.006-T, 151.007-T, 151-F, 159-S} (9 ids — manifest tasks + qualifying
  feature 151-F + shipment record 159-S; validated linked-deliberation set
  for 151-F is `{}` per the three-source scan above, so no linked
  deliberation ID is added)
* `required_ids` = {159-S, 151-F} (unconditional) ∪ {151.001-T..151.007-T}
  (not truly archived pre-close) = same 9 ids
* `archived_ids - allowed_ids` = `{}` — PASS (no unexpected artifact archived)
* `required_ids - archived_ids` = `{}` — PASS (no required artifact left
  unarchived)

## Step 4 — `parent_id` preservation check

Re-read from `.backlogit/archive/` post-close:

| ID | status (post-close) | archived_status | parent_id (post-close) | matches pre-close snapshot |
|---|---|---|---|---|
| 151.001-T | archived | done | 151-F | yes |
| 151.002-T | archived | done | 151-F | yes |
| 151.003-T | archived | done | 151-F | yes |
| 151.004-T | archived | done | 151-F | yes |
| 151.005-T | archived | done | 151-F | yes |
| 151.006-T | archived | done | 151-F | yes |
| 151.007-T | archived | done | 151-F | yes |

PASS — no `parent_id` cleared or altered on any archived task.

## Shipment record and qualifying feature — archived provenance

| ID | status | archived_status |
|---|---|---|
| 159-S | archived | shipped |
| 151-F | archived | done |

`159-S` carries `archived_status: shipped` (required terminal provenance for
the shipment record). `151-F` carries `archived_status: done` (expected
engine behavior for a qualifying feature member — `setArtifactStatus`
unconditionally forces the feature to `done` before `archiveItems` archives
it; this is not `shipped` and is not expected to be).

## Post-Mode

1. Archive file for shipment itself: `.backlogit/archive/159-S.md` present. PASS.
2. Per-item archive check: all 9 members (`151-F`, `151.001-T`..`151.007-T`,
   `159-S`) have corresponding archive files present. PASS.
3. Deleted-file guard: `git status --short -- ".backlogit/archive/"` shows
   only modifications/additions (`M`, `AM`, `??`) consistent with this
   closure's own archival — no unexpected deletions. PASS.

## Process Deviation Disclosure (P-005, added 2026-09-07)

The "Linked-deliberation scan" section above documents the validated
three-source scan for `151-F`, but its **chronology** requires disclosure:
this scan was **reconstructed in a later, resumed session** (round 4,
commit `42428afe`) from the preserved pre-close snapshot (commit
`1b758a16`, byte-identical to the archived `151-F` record on
`custom_fields`, description, and `references`) -- it was **not executed
live** as a `shipment-reconcile` Step 0(c) pre-mutation gate immediately
before the Step 1 cascade invocation below, as the skill's contract
requires.

The reconstruction proves the scan's **inputs and outcome** were unchanged
(a live scan would have read the identical data and produced the identical
empty result), which is a data-supply guarantee -- but it cannot recreate
the **pre-mutation halt opportunity** that a live Step 0(c) gate exists to
preserve; that is a category-different guarantee and was not satisfied.

**Disposition (operator, 2026-09-08): `accepted-with-remediation` P-005
process deviation**, captured as P-021 stash entry `856B6770`
(`856B6770` was itself only captured on `2026-09-08T04:53:59Z`; a
2026-09-07 disposition date for it is chronologically impossible and is
corrected here to the ground-truth `2026-09-08`), distinct
from and additional to the separately-disclosed Pre-Mode
`status-mismatch`/`HALT` deviation (entry `15A02E21`). The **mechanical
archival outcome below stands as verified and final** and is unaffected by
this disclosure. Remediation (a durable pre-mutation evidence record, a
fail-closed live-execution check, and a replay test) is tracked as a
**separate follow-up shipment**, not folded into shipment `169-S`. This
disposition is not precedent for treating post-hoc reconstruction as
equivalent to live pre-mutation execution in any future invocation of this
sub-procedure.

## Gate decision

`returned_ids` empty; `archived_ids - allowed_ids` empty; `required_ids -
archived_ids` empty; every `parent_id` preserved; post-mode archive presence
and deleted-file guard both PASS.

**recommendation: CLOSED (mechanical verification only)** -- this
recommendation certifies the two-set gate and `parent_id`-preservation
check above; it does not, by itself, certify that every upstream gate
(including Step 0(c)'s live pre-mutation linked-deliberation collection)
executed as designed. See "Process Deviation Disclosure" above for the
disclosed, operator-dispositioned (`accepted-with-remediation` P-005)
Step 0(c) live-execution deviation, entry `856B6770`.
