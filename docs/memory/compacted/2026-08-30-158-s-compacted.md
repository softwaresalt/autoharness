---
title: "Compacted memory — 158-S v1.5.0 release execution"
date: 2026-08-30
doc_type: memory
agent: "Ship"
source: "150-F / 158-S"
---

# Compacted Memory — 158-S (v1.5.0 Release)

Compacted from `docs/memory/2026-08-30/ship-158-s-v1_5_0-release-execution-memory.md`
(verbose original archived at `docs/archive/memory/2026-08-30/`).

**Outcome**: `autoharness` v1.5.0 published to PyPI and GitHub Release live.
Shipment `158-S` safe-closed (cascade path, `150-F` done, `158-S` shipped).

**Merges**: PR #423 (main impl, `8b79de94`) → PR #425 (post-merge hotfix,
`GITHUB_HEAD_REF` push-context test fix, P-021 stash `E738A7D1`, `8922b62e`)
→ PR #426 (PyPI publish fix, `core-metadata-version=2.4`, `ca3232a8`).

**Key learnings** (full detail in `docs/compound/`):
1. GitHub Actions push events set `GITHUB_HEAD_REF` to empty string, not
   absent — untested combination since prior post-merge `test` jobs were
   path-filtered to skipped.
2. Unpinned `hatchling` build dependency drifted to Metadata-Version 2.5
   (PEP 794), incompatible with the pinned publish action's older twine;
   local `uvx twine check` couldn't reproduce it (resolves latest twine).

**Rollback executed**: first tag attempt failed at publish; PyPI probe
confirmed 404 (safe, pre-upload) before any remediation; tag deleted, root
cause fixed, re-tagged successfully. No version burned.

**Environment note**: WSL used as build/publish-verification transport
throughout (native Windows sandbox networking cannot reach
`files.pythonhosted.org`; GitHub Actions runner unaffected).

**Follow-ups for Stage**: P-021 stash `E738A7D1` requires deliberation;
stashes `8E10B13B`, `6A2D62DD`, `2E67938C` remain deferred, untouched.
