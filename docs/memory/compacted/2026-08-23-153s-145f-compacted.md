# 153-S / 145-F Compacted Session Summary

**Shipment**: 153-S
**Feature**: 145-F — mechanism B: `BranchOwnershipTests` intra-file order
pollution, re-measured at mechanism-A's fixed head (bug E8158860/9DD9E323)
**Feature PR**: #401, merged `fed1319bac9e1ac3c2f2eeb448390fbfc192f155`
(2 parents: `bbaf327f` + `d33dc898`, verified)
**Compound learning**: `docs/compound/2026-08-23-153-s-145-f-measurement-task-review-scrutiny.md`

## What shipped

A pure measurement/diagnosis shipment: no production source change.
`145.001-T` re-measured the intra-file test-order dependence `141.004-T`
originally found, at mechanism A's fixed head (152-S), per
`docs/plans/2026-08-22-git-config-env-containment-plan.md` Task 8 and
binding Amendment A10. Standalone `test_gates_topology.py`: 104/104 green.
All 5 named polluter->victim pairings individually green against current
code. The mandatory A10 reverted-checkout negative control (surgically
reverting the 5 `BranchOwnershipTests` methods to their pre-`34d194a4`
form, then restoring to byte-identical HEAD) reproduced the original
failure in all 5 cases, proving non-vacuity. Disposition: **`SUBSUMED`**.
`145.002-T` closed as a recorded no-op citing that evidence. Canonical
Windows full suite re-run: 1830 tests, OK, skipped=20 — no regression
against the 152-S baseline.

## Key lessons (full detail in the compound-learning doc)

1. **A "no source change" evidence-recording task is still fully in
   review scope.** A fresh Copilot review on a backlog-metadata-only PR
   still raised 2 legitimate findings: missing verbatim command
   transcripts (summarized one-liners aren't verifiable), and a causal
   misattribution (blamed deliberate per-test `GITHUB_*` override values
   instead of the actual established mechanism-A leaked state,
   `GIT_CONFIG_VALUE_2`). Both passed P-021 C1 (same-contract-surface
   completions of the task's own already-authorized deliverable) and were
   fixed directly in one review-fix cycle.
2. **Cross-check causal narratives against already-established sibling
   records** before writing a new one — the correct mechanism-A causal
   chain was already fully documented in `144-F.md` and predicted in
   `145-F.md`; the first-pass causal statement should have matched them
   and didn't.
3. **The surgical revert -> measure -> restore -> verify-zero-diff
   technique for a throwaway negative control is deterministic and
   reusable** — re-run a second time in this same session (to capture
   verbatim transcripts) with byte-identical results both times.
4. **`backlogit shipment ship` (P-015 cascade close)** again took roughly
   a minute for a small manifest (1 feature + 2 tasks); zero
   `returned_ids`, exact `archived_ids` match — no unexpected out-of-
   manifest sweep this time.

## Session-end state

153-S fully shipped via the P-015 verified fully-covered-root cascade
path. Post-merge closure branch
`post-merge/145-f-mechanism-b-branchownershiptests-order-pollution`
carries the backlog cascade-close mutation plus documentation commits,
pending its own closure PR, review, CI, P-018, and operator approval. No
successor shipment was claimed; none exists to check at this time per the
operator's explicit scope for this invocation.
