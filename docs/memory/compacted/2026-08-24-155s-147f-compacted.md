---
title: "Compacted memory - 155-S / 147-F (P-015 archived_ids two-set gate)"
date: 2026-08-24
source: "docs/memory/compacted/2026-08-24-155s-147f-compacted.md"
doc_type: "reference"
agent: ship
---

# 155-S / 147-F - Compacted Session Memory

Consolidates:
`docs/archive/memory/2026-08-24-ship-155-s-147-f-shipped-closure.md` (Ship,
merge + closure).

## Decisions

* `027-DL`: correct the false "full-set equality" claim over `archived_ids`
  in the P-015 policy and `shipment-reconcile` skill; `archived_ids` is a
  TRANSITION LOG (artifacts actually transitioned during the invocation),
  not a manifest echo. Replace with a two-set `allowed_ids`/`required_ids`
  gate: `required_ids` unconditionally includes the shipment record and
  every qualifying feature member; a pre-archived manifest task item or a
  qualifying feature's validated linked deliberation MAY be included or
  omitted from `archived_ids` without failing the gate.
* No Backlogit engine change, no fresh behavioral spike, no relaxation of
  unexpected/out-of-scope-ID detection (retains full strength as
  `archived_ids - allowed_ids`).
* Resolves deferred scope expansion `5CFA8198` (captured at 154-S/146-F
  closure), whose own recorded archive condition -- "archive this entry
  once 155-S has shipped and the four corrections are merged" -- is now
  satisfied; archived via `backlogit stash archive 5CFA8198`.

## Files modified (147-F)

* `templates/policies/workflow-policies.md.tmpl` - P-015 fully-covered-root
  exception item 7 corrected (in-place supersession note); additive
  changelog rows `1.21.0`-`1.24.0`.
* `templates/skills/shipment-reconcile/SKILL.md.tmpl` - Step 0(b)/(c)
  declared-status snapshot capture; Cascade Close Sub-Procedure step 3
  rewritten as two independently-evaluated halt conditions; linked-
  deliberation snapshot extension (engine-defined matcher only); full-depth
  descendant-coverage fix for the fully-covered-root classifier
  (`_build_children_index`/`_enumerate_descendants` replacing single-level
  `_enumerate_children`).
* `src/autoharness/gates/shipment_closure.py` - full-depth descendant
  coverage.
* `tests/test_cascade_close_archived_ids_postcondition.py` (new, 57 tests) -
  contract regression coverage.
* Evidence trail corrected: 2026-08-18 spike marked SUPERSEDED; compound
  notes corrected with retraction sections; 154-S/146-F closure record got
  an append-only forward cross-reference (historical narrative/verdict
  unchanged).

## Merge and closure outcome

* PR #407 merged `--merge` (no admin) at `2026-08-24T22:06:39Z`, merge SHA
  `a7aa820e3c7dbb96e95bb8376e3022a229b55cb1`, two parents verified (P-009).
* P-018 `SATISFIED` at final HEAD `4552a113`; all required CI checks green.
* Backlog cascade-close (P-015 verified fully-covered-root exception):
  `classify_shipment_close_path` -> `CASCADE`; `backlogit shipment ship
  155-S` executed; live workspace confirmed all 6 artifacts (`147-F`,
  `147.001-T`..`147.004-T`, `155-S`) archived with correct provenance and
  `parent_id` preserved. Pre-close, `147.001-T`..`147.004-T` were declared
  `status: done` (directory-relocated to `.backlogit/archive/` but **not**
  truly archived), so the corrected two-set gate made their inclusion in
  `archived_ids` mandatory (`required_ids`), not the optional
  pre-archived-omission case -- only linked deliberation `027-DL` was truly
  pre-archived and correctly exercised that optional tolerance (by
  omission). No discrepancy between the raw response and the
  required/allowed sets on this shipment's own first live cascade close.
* Runtime verification: `cli` surface probe PASS (`uv run autoharness
  --help`, exit 0) - the only applicable surface.
* Operational closure: `READY` - no open conditions (resolves 154-S's
  `5CFA8198` condition).

## Pre-existing dirty state adjudication (housekeeping, unrelated to 147-F)

* `023-DL` (verify-workspace variable derivation, already shipped as
  142-F/150-S) had a pending, uncommitted archival from 2026-08-23 -
  committed as a distinct housekeeping commit on the post-merge branch.
* `.mcp.json` and `.backlogit/runtime/` are pre-existing local workspace
  state, explicitly out of scope per PR #407's own record - left untouched
  and uncommitted throughout.

## Follow-ups

None outstanding for 155-S/147-F. External backlogit stash `B57F9E24`
remains active and unrelated to this scope (untouched).
