---
compacted_from: docs/archive/memory/2026-08-12-ship-127-S-118-F-session.md
release_unit: 127-S / 118-F
date: 2026-08-12
---

# Compacted Memory — 127-S / 118-F (S1 Copilot Supervisor Safety Contracts + Characterization Baseline)

**Source (archived verbose original)**:
`docs/archive/memory/2026-08-12-ship-127-S-118-F-session.md`

## Decisions

1. All 7 tasks implemented test-first; characterization suites for
   `start.ps1`/`start.sh` pin *current* behavior including documented
   absences as approved baseline evidence, not permanent mandates.
2. Local review findings (2) fixed directly; hosted Copilot review run
   across 4 remediation rounds (21 threads across 6 raw review
   submissions) with 17 fixed, 2 deferred as P2 (circuit
   breaker exhausted at 3 cycles), 2 informational-only.
3. Self-caught and fixed a latent bug (`BacklogUnavailableError` missing
   positional arg) introduced by my own round-1 remediation, before it was
   ever flagged externally.
4. **Corrected an initial close-path decision**: a provisional
   "safe-close" conclusion (based on a partial reading of P-015's
   exception clause) was overturned after re-reading the full current
   agent instructions and running the actual
   `classify_shipment_close_path` classifier, which returned `CASCADE` —
   matching the shipment's own manifest description and the operator's
   brief.
5. Closed `127-S` via the cascade path (`backlogit shipment ship`) — the
   first real-world (non-simulated) exercise of the new P-015 verified
   fully-covered-root exception, following mandatory dynamic engine
   attestation and a 66/66 closure-simulation re-run.

## Files Modified

- `src/autoharness/supervise/{__init__,result,errors,contracts,redact,locking}.py`
  — new safety-contract modules (typed result envelope, error taxonomy +
  exit-code table, gated-action catalog, secret redaction, OS-locked
  guard-file session locking).
- `src/autoharness/gates/shipment_closure.py` — new P-015 close-path
  classifier.
- `tests/test_supervise_*.py`, `tests/test_shipment_closure_classification.py`,
  `tests/test_start_ps1_characterization.py`,
  `tests/test_start_sh_characterization.py` — new test suites.
- `templates/policies/workflow-policies.md.tmpl`,
  `.github/agents/_ship.agent.md` (generated),
  `docs/compound/097-S-shipment-task-only-safe-close.md` — coherent P-015
  amendment (verified fully-covered-root exception).
- `.backlogit/` state — full task/feature/shipment lifecycle + cascade
  close.

## Key Learnings (cross-references)

- `docs/compound/2026-08-12-hosted-review-catches-fail-closed-gaps-local-review-and-ci-miss.md`
  — hosted review found real race conditions and fail-closed gaps across 3
  new safety-critical modules that local review + green CI both missed;
  also records the self-caught-latent-bug lesson.
- `docs/compound/2026-08-12-windows-stale-process-exe-old-detection.md` —
  Windows renames a running process's image to `*.exe.old` when the
  on-disk binary is replaced under an open handle; reliable stale-MCP-
  daemon detection technique.
- `docs/compound/2026-08-12-close-path-decisions-must-use-the-classifier-not-summarized-prose.md`
  — a close-path (or similarly conditional policy) decision must always be
  made by re-reading the full current source of truth and running the
  actual classifier, never from a summarized/paraphrased prior reading.

## Outcomes

- PR #326 merged via merge commit `8ccd3a2ded777393703136da6747e846619f4294`
  (two parents, P-009 preserved structurally and by repo config).
- Dynamic engine attestation: CLI `v1.9.0`/`39528a4`/build
  `2026-08-12T03:49:03Z`; stale MCP process (PID 45252) stopped
  PID-specifically.
- Closure simulation re-run against attested engine: 66/66 passed.
- Cascade close: `backlogit shipment ship 127-S` ->
  `returned_ids: []`, `archived_ids` = all 7 tasks + `118-F` + `127-S` —
  matched predicted outcome exactly; no `parent_id` cleared; no
  out-of-scope mutation.
- `127-S`: `active -> shipped -> archived` (`archived_status: shipped`).
- `118-F`: `active -> done -> archived` (`archived_status: done`).
- 21 Copilot review comments across 4 remediation rounds (6 raw review
  submissions); 17 fixed/replied/GraphQL-
  resolved, 2 deferred as P2, 2 informational-only (one subsequently
  remediated on the post-merge closure PR).
- Dark-factory bounded scope `127-S` complete; `128-S`/`129-S` explicitly
  NOT claimed or expanded into.

## Provenance Chain

`docs/closure/127-S-118-F-post-merge-closure.md` (full detail) ->
`docs/archive/memory/2026-08-12-ship-127-S-118-F-session.md` (archived
verbose original) -> this compacted summary.

## Deferred

- P2: symlink-following in `shipment_closure.py`'s backlog lookup path.
- P2 (pre-existing): symlink-containment tradeoff in `locking.py:106`.
- Informational: `shipment-reconcile` skill template not yet wired to call
  `classify_shipment_close_path` automatically — Ship invokes it manually
  until a future shipment closes this gap.
