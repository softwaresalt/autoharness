---
compacted_from: docs/archive/memory/2026-08-18-ship-staging-publication-and-139-s-execution-closure.md
release_unit: 139-S / 130-F
date: 2026-08-18
---

# Compacted Memory — 139-S / 130-F (Enforce Backlogit Checkpoint Payload Contract)

**Source (archived verbose original)**:
`docs/archive/memory/2026-08-18-ship-staging-publication-and-139-s-execution-closure.md`

## Decisions

1. Executed Step A first: pushed `chore/stage-138-S` without renaming,
   opened a docs/backlog-only staging PR (#356) carrying all five
   Stage-owned commits, triaged 11 Copilot comments, merged with a merge
   commit (`a31cb1e3`) before claiming any shipment.
2. Diagnosed and TDD-fixed a topology-gate false positive
   (`PREDECESSOR_NOT_SHIPPED`) in `_prior_shipment_id`; a first-pass fix
   (skip only the direct violator) proved incomplete under hosted review
   and was redesigned to disable the entire numeric-adjacency fallback for
   any target with any explicit reverse-dependency edge, anywhere in the
   shipment set.
3. Implemented all 7 tasks of 130-F via TDD: canonical CheckpointV1
   contract (`schema_version: 1`, official CLI/MCP route, auto-populated
   timestamps, domain data under `context`), Stage/Ship template + mirror
   convergence, `cli_command` fallback registry entry, checksum refresh,
   and anti-regression contract tests.
4. Carried PR #357 through two rounds of hosted Copilot review (9
   `reviewThreads`): fixed 4 genuine findings (checkpoint/index-sync
   ordering self-contradiction in the Stage mirror, write-site wording x2,
   anti-regression regex loophole), declined 5 with documented rationale
   (2x shell-safe `cli_command` transport, 3x CLI-fallback-gating
   pre-existing scope). Two additional findings surfaced only as
   never-threaded "Suppressed comments" on `topology.py`: one (the
   multi-hop gap) was fixed; the other (a forward-dependent false
   negative in that same fix) was discovered only during post-merge
   closure and left unfixed on `main` — see the Deferred section below.
5. Merged PR #357 with a verified 2-parent merge commit (`9bb3a24b...`)
   after P-018/P-009/P-014 gates all passed.
6. Closed `139-S` via the P-015 verified fully-covered-root cascade
   exception (`classify_shipment_close_path` → CASCADE, `130-F` fully
   covered by all 7 manifest children); verified `returned_ids: []`,
   `archived_ids` exactly the 9 expected artifacts, and `parent_id`
   preserved on all 7 tasks against a pre-close snapshot.

## Files Modified

- `templates/agents/_ship.agent.md.tmpl`, `templates/agents/_stage.agent.md.tmpl`
  — checkpoint write-site wording clarified.
- `tests/test_checkpoint_payload_contract.py` — grew to 30 tests
  (clause-bound negation detector, regex-bound schema_version assertion).
- `src/autoharness/gates/topology.py` — `_prior_shipment_id` redesigned to
  disable the numeric-adjacency fallback entirely once any explicit reverse
  edge exists for the target.
- `tests/test_gates_topology.py` — 93 tests total, added multi-hop
  reverse-dependency coverage.
- `.github/agents/_stage.agent.md` — Step 6 reordered (checkpoint creation
  before index sync); manifest checksum refreshed.
- `.backlogit/` — cascade close of `139-S`: archived `130.001-T`..
  `130.007-T`, `130-F`, `139-S`; `returned_ids: []`.

## Key Learnings (cross-references)

- `docs/compound/2026-08-18-topology-gate-multi-hop-reverse-dependency-fallback.md`
  — disabling an inference fallback must be keyed on "does any explicit
  relationship exist for this target at all," not "is this specific
  candidate the violator."
- `docs/compound/2026-08-18-stage-agent-checkpoint-index-sync-ordering-self-contradiction.md`
  — inserting a step into an already-numbered procedure requires
  re-scanning the whole list for stale finality/ordinal claims, not just
  the diff region.
- Reused (not re-derived): the P-015 verified-fully-covered-root classifier
  discipline from `docs/compound/097-S-shipment-task-only-safe-close.md`
  and prior 129-S/137-S closures.

## Outcomes

- PR #356 (staging) merged `a31cb1e3`; PR #357 (feature) merged
  `9bb3a24b946694924e2d7306daa9a5b863784d2a` — both verified 2-parent merge
  commits, both P-009-compliant.
- Targeted tests: 30 (checkpoint contract) + 93 (topology), all pass. Full
  suite: 1549 passed, 1 pre-existing unrelated flake (isolated re-run
  confirmed not a regression).
- P-018 gate: `SATISFIED` — all 9 `reviewThreads` fixed or
  declined-with-rationale, replied-to, and GraphQL-resolved (0 unresolved
  threads).
- Cascade close: `backlogit shipment ship 139-S` → `returned_ids: []`,
  `archived_ids` = 7 tasks + `130-F` + `139-S` exactly, matching the
  manifest exactly; `parent_id` preserved on all 7 tasks.
- `backlogit sync` post-close: 855 artifacts indexed, no residue.
- `138-S` (declared predecessor dependency `138-S -> 139-S`) remains
  `queued`, untouched, now dependency-eligible for a future session.
- **Notable operational timing observation**: `backlogit shipment ship`
  took ~7 minutes wall-clock with zero intermediate stdout for 9 archived
  artifacts — distinguishable from a hang only by polling `git status
  --short` for incremental file mutations mid-run.

## Provenance Chain

`docs/closure/139-S-130-F-post-merge-closure.md` (full detail) ->
`docs/archive/memory/2026-08-18-ship-staging-publication-and-139-s-execution-closure.md`
(archived verbose original) -> this compacted summary.

## Deferred

- **A genuine, verified-but-unapplied fix for a residual defect on
  `main`**: `_prior_shipment_id`'s multi-hop redesign (merged this
  shipment) also wrongly suppresses the implicit-predecessor fallback for
  any numerically-HIGHER shipment's normal forward dependency on the
  target — not just genuine lower-numbered reverse edges. Discovered
  during this session's post-merge closure review (never posted as a
  GitHub review thread, only as a suppressed comment). A verified fix +
  regression test is fully written up in
  `docs/compound/2026-08-18-topology-gate-forward-dependent-suppression-residual-defect.md`
  for Stage to pick up as a new hotfix task; not committed here because it
  is out of `139-S`'s bounded-stop scope and the post-merge closure
  branch's scope. Both originally-declined Copilot findings (shell-safe
  transport, CLI-fallback gating) remain declined-with-rationale, not
  deferred.
