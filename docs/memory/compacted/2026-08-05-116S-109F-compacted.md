---
compacted_from: 2026-08-05-ship-116-S-109-F-session.md
shipment: 116-S
feature: 109-F
date: 2026-08-05
feature_closed: true
---

# Compacted: Ship 116-S / 109-F Session (final shipment — feature closed)

Executed Ship end-to-end for `116-S` (gate C / remote CI validation
backstop) — the **final** link in the serial `114-S → 115-S → 116-S` split
of covering feature `109-F` — under the already-active P-017 dark-factory
contract (resolved route `claude-sonnet-5`/`anthropic`/`high`; the
operator-explicit removal of the 3-cycle review-fix cap was needed and
used in full this time; MCP/Engram/Graphtor/Intercom all declared
degraded — CLI-only via `backlogit`/`git`/`gh`).

Implemented all 3 manifest tasks (`109.011-T` C1 CI entrypoint,
`109.014-T` C2 CI workflow job with required-vs-advisory toggle,
`109.012-T` C3 rollout docs + CI-path tests). Live CI surfaced 2 real
defects local review couldn't reach (detached-HEAD + `GITHUB_REF_TYPE`
branch-resolution gaps, fixed early in the session).

PR #302 then went through **9 rounds of hosted Copilot review, 13
distinct threads, zero noise findings** — each fix narrowed scope without
challenging the underlying resolution strategy until the final round:
round 1 (7 threads: install-command resolver gap, stale checksum,
doc-accuracy); round 2 (`{{FEATURE_SHIPMENTS}}`-gating); round 3
(`GITHUB_EVENT_PATH` default-branch fallback); round 4 (dangling
`needs`/`results` reference); round 4b (fork-PR-named-`main` false match +
corrected "NON-BYPASSABLE" overclaim, new threat-model doc section); a
live-CI-only regression from a test's own environment-leakage gap (fixed,
not a Copilot finding); round 5 (hard-coded `main` trigger filter → new
`{{CI_DEFAULT_BRANCH}}` variable); round 6/7 (round 5's own fallback-to-
`main` on unresolvable default branch was **fail-open, not fail-closed** —
replaced with halt-and-ask-operator guidance). Every thread individually
replied to with its fixing SHA and resolved via GraphQL; final P-018 gate:
`SATISFIED`, 9 rounds, 0 unresolved. Full pattern write-up:
`docs/compound/2026-08-05-116-S-copilot-escalation-and-ci-default-branch-fail-open.md`
(also captures a `mode='ci'` test env-var-scrubbing rule, the PyYAML
Norway-problem gotcha, and the PowerShell commit-message quoting
workaround). Full test suite grew to 1261 passed / 11 skipped / 403
subtests by final HEAD `bc14ba6`.

Merged via `gh pr merge 302 --merge` → merge commit `64b6e934...` (two
parents, confirmed ancestor of `origin/main`). P-009/P-016/P-018/§1.9 all
verified PASS at final HEAD, re-verified unconditionally immediately
before merge (no HEAD drift).

Post-merge: re-read the freshly merged Ship agent instructions and
shipment-reconcile skill before self-closing `116-S` (confirmed neither
changed in this merge). Created closure branch
`post-merge/109-f-topology-gate-c-remote-ci-validation-backstop` from
synced `main`.

Safe-closed `116-S` as a **single-artifact, non-cascading** shipment: all
3 manifest tasks were already pre-archived; protected set was `109-F`
alone (both prior sibling shipments already archived); shipment record
moved live→shipped→verified→archived→verified `archived_status: shipped`;
`backlogit shipment ship` never called.

**Because this was the feature's final planned shipment**, Ship performed
a separate, evidence-based `109-F` terminal-state determination — NOT
inferred from the shipment closure alone: verified all 3 sub-shipments
archived-shipped, zero queue-resident descendants (0 of 23 tasks + 7
reviews remain queued), and zero other shipments referencing the feature.
Closed `109-F` (`status: done` → archived `archived_status: done`,
matching the `105-F`/`107-F`/`108-F` convention). No unrelated backlog
artifact was touched by either closure step.

Full closure evidence: `docs/archive/closure/116-S-109-F-post-merge-closure.md`
(`closure_status: READY`, `feature_terminal_status: done`,
`feature_archived_status: done`). Verbose session record archived to
`docs/archive/memory/2026-08-05-ship-116-S-109-F-session.md`.

**Outstanding**: none for `109-F` — the serial `114-S → 115-S → 116-S`
chain is fully complete, no successor shipment exists or is planned. No
new follow-up defects identified. The documented, accepted, out-of-scope
threat-model limitation (`pull_request`-triggered CI executing its own
workflow definition from the PR's proposed head) remains mitigated via
CODEOWNERS-required review, not a defect. The post-merge closure PR
requires its own separate explicit operator approval (P-014) — the
dark-mode approval for PR #302 does not automatically re-cover a second
PR's approval without scope re-confirmation.
