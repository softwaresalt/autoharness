---
compacted_from: 2026-08-05-ship-114-S-109-F-session.md
shipment: 114-S
feature: 109-F
date: 2026-08-05
---

# Compacted: Ship 114-S / 109-F Session

Executed Ship end-to-end for `114-S` (gate A / deterministic core of the
staged `pipeline-topology` rollout, covering feature `109-F`) under an
explicit P-017 dark-factory activation (ordered scope `114-S → 115-S →
116-S`, only `114-S` this turn; review-fix cycle cap removed for this
session; intercom degraded/local-only).

PR #297 carried through **12 rounds of hosted Copilot review (27 threads,
all resolved)** — the longest single-PR review loop recorded in this repo.
Every finding across rounds 4-11 was an instance of the same "silent
fail-open in backlog-frontmatter parsing" defect class (missing/malformed
shipment or task fields, glob-injection via unvalidated artifact ids,
duplicate-record merging, container-shape vs. per-member-shape gaps,
target-vs-predecessor ambiguity asymmetry). Round 12 had **no new threads**
(not "clean" — the final review body still carried two suppressed,
unaddressed findings unrelated to this pattern: a no-op bounded-retry gap
in `topology.py:680` and a telemetry-outcome mapping gap in
`cli.py:735-739`; both are documented as required follow-ups, not fixed by
PR #297). Full pattern breakdown and the three-layer (presence →
container-shape → member-shape) validation checklist derived from it:
`docs/compound/114-S-109-F-copilot-review-fix-patterns.md`. Full test
suite grew to 1187 passed / 7 skipped by final HEAD `1feee9a`.

`REVIEW_TIMEOUT` (empty unresolved-thread list) occurred twice
(round 9, round 12); both resolved cleanly on immediate retry — confirmed
as "retry", never an implicit pass/fail.

Merged via `gh pr merge 297 --merge` → merge commit `cef4040...` (two
parents, confirmed ancestor of `origin/main`). P-009/P-016/P-018/§1.9 all
verified PASS at final HEAD, re-verified unconditionally immediately
before merge (no HEAD drift).

Post-merge: per the merged 109.019-T contract, re-read the freshly merged
Ship agent instructions and shipment-reconcile skill BEFORE self-closing
`114-S` (this shipment's own deliverable requires it). Created closure
branch `post-merge/109-f-topology-gate-a-core` from synced `main` before
any mutation (an initial near-miss almost committed the safe-close
directly on `main` — caught and corrected before commit). Safe-closed
`114-S` as a **single-artifact, non-cascading, partial-feature** shipment:
all 10 manifest tasks were already pre-archived; protected set (covering
feature `109-F` + 10 sibling tasks under `115-S`/`116-S`) verified intact
before and after every mutation; `backlogit shipment ship` never called.

Full closure evidence: `docs/closure/114-S-109-F-post-merge-closure.md`
(`closure_status: READY_WITH_CONDITIONS` — the three residual defects below
are why this is conditional rather than an unconditional `READY`; the gate
subcommand is not wired into any hook/automated caller today (no automated
blast radius), though it remains manually invocable via the CLI, so this is
not a "zero live blast radius" claim in the absolute sense — must be fixed
before `115-S` installs the activating hook). Verbose session record
archived to `docs/archive/memory/2026-08-05-ship-114-S-109-F-session.md`.

**Outstanding**: `115-S`/`116-S` remain queued, untouched, per explicit
operator instruction (Ship does not start them this turn). The post-merge
closure PR requires its own separate explicit operator approval (P-014) —
the dark-mode approval for PR #297 does not carry over. **Three follow-ups**
identified via this closure PR's own Copilot review require Stage triage,
all pre-existing in merged `main` and independent of this closure's
docs-only diff:
1. no-op bounded-retry gap (`topology.py:680`);
2. telemetry outcome-mapping gap for `exit_code == 2` (`cli.py:735-739`);
3. `closure_complete()` (`topology.py:505-518`) validates only
   `compaction_status`, never `closure_status`/releasability — so the
   "condition" above is a process commitment, not currently tool-enforced;
   flagged three times now (twice in PR #297, once here).
A separate, non-blocking audit-log discrepancy was also noted (not
fabricated): `.backlogit/logs/114-S.jsonl` omits the intermediate
`shipment_status_changed: shipped` event that comparable prior closures
record before archival; the archived record's `archived_status: shipped`
field is independently confirmed correct.
