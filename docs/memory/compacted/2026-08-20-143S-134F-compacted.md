---
date: 2026-08-20
agent: ship
shipment: 143-S
feature: 134-F
disposition: shipped
type: compacted-summary
source: docs/memory/2026-08-20-ship-143-s-full-lifecycle-closure.md
---

# 143-S / 134-F — Compacted Summary

Full Ship lifecycle closure for shipment 143-S (feature 134-F): authored
workflow policy P-021 (bounded fix-cycle scope containment and deferred
expansion capture) across agent/instruction/skill templates plus three new
contract test suites. PR #373 merged (merge `e2af4dfe`, 2 parents, verified).
An auxiliary P-021 C4 forward-authorized CI-unblocker unit (`135-F` /
`135.001-T`, from deferred stash `D71F6283` via deliberation `020-DL`) rode
the same branch/PR, fixed a PowerShell checklist-rendering truncation defect,
and was closed on its own path outside 143-S's manifest/protected set.

**Key learnings**:
* Cascade shipment close (`backlogit shipment ship`) archived an out-of-manifest
  linked deliberation (`019-DL`) via `custom_fields.source_deliberation_id`
  — a reference field, not a `parent_id` hierarchy edge the classifier
  walks. The Cascade Close Sub-Procedure's own exact-match post-condition
  caught this as designed; reverted the one unintended artifact, retained the
  legitimate closure, disclosed the engine-behavior gap as a Stage-owned
  follow-up. See `docs/compound/2026-08-20-cascade-close-archives-out-of-manifest-linked-deliberation.md`.
* Auxiliary work correctly stayed off the shipment manifest and its own
  covering feature/task closed independently post-merge, exactly per its
  authored scope instructions — no P-015/P-001 boundary crossed.
* Post-merge Step 7 "Source artifact cleanup" (retiring `custom_fields.source_stash_id`
  / archiving `custom_fields.source_deliberation_id` for shipped top-level
  items) was added to the Ship contract by this very shipment's own merge —
  caught only via the mandatory pre-self-close context reload plus a Copilot
  review finding on the closure PR. `134-F`'s stash `B48A482A` was already
  removed; its deliberation `019-DL` was archived properly through this
  explicit step.
* Canonical suite green at 1677 passed / 20 skipped, matching the
  pre-verified expectation exactly; all 4 required CI checks green; P-018
  `SATISFIED`; P-014 `READY_WITH_FOLLOWUPS` (0 P0/P1).
* Protected operator git stash (`operator-work-before-ship-143-S`) verified
  untouched throughout — safe for the Orchestrator to restore now.

Full detail in `docs/memory/2026-08-20-ship-143-s-full-lifecycle-closure.md`
and `docs/closure/143-S-134-F-post-merge-closure.md`.
