---
date: 2026-08-18
agent: ship
shipment: 142-S
feature: 133-F
disposition: shipped
type: compacted-summary
source: docs/archive/memory/2026-08-18-ship-142-s-full-lifecycle-closure.md
---

# 142-S / 133-F — Compacted Summary

Full P-017 dark-mode Ship lifecycle for shipment 142-S (feature 133-F, stash
1EFDA8EE), final shipment in the 141-S -> 142-S global sequence: staging PR
#367 (merge `ebe5c2d4`) then implementation PR #368 (merge `093e0996`, 2
parents each, verified). Removed three stale tracked root scratch artifacts
(`out.json`, `res.json`, `results.json` -- accidental external-workspace
verify-workspace outputs, no functional dependents) and added a
deterministic regression test enforcing a tracked root-JSON allowlist
(`{.mcp.json, plugin.json}`) via `git ls-files` semantics rather than a
`.gitignore` rule.

**Key learnings**:
* Independently re-verifying Stage-provided evidence (hashes/sizes/line
  counts) before trusting it paid off — all confirmed accurate this time
  (unlike the original stash claim that all 3 files were identical, which
  Stage had already corrected).
* Copilot found a real risk in Stage's own acceptance-command wording (an
  unscoped `git diff --cached --stat` would have swept in protected
  operator-staged submodule entries and caused a false fail-closed halt) —
  fixed by scoping to explicit literal pathspecs.
* TDD executed in full including the "control fires" proof: temporarily
  restoring a deleted file and confirming the new test fails by name before
  reverting — stronger evidence than RED/GREEN alone.
* `classify_shipment_close_path` correctly identified 133-F as a verified
  fully-covered root -> CASCADE close path, confirmed via all post-condition
  checks (archived_ids exact match, returned_ids empty, parent_id
  preserved).
* One local-build-environment test flake (`test_checklist_report_prints_non_interactively`)
  encountered and correctly diagnosed as unrelated/pre-existing (file
  untouched vs `main`; CI's own `test` job passed cleanly) rather than
  either ignored or falsely blamed on this shipment.
* Minor non-blocking process note: verify `git rev-parse HEAD` SHA length
  (40 hex chars) before every `backlogit update --commit` call — one
  transient mistracked SHA occurred and was caught/corrected this session.
* Protected operator-staged submodule entries (`.gitmodules`, 3 gitlinks)
  verified byte-identical across every checkout/commit/merge/cascade
  operation this session — zero drift.

Full detail archived at
`docs/archive/memory/2026-08-18-ship-142-s-full-lifecycle-closure.md`
and in `docs/closure/142-S-133-F-post-merge-closure.md`.
