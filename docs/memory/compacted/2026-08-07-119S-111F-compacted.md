---
compacted_from: docs/archive/memory/2026-08-07-ship-119-S-111-F-session.md
release_unit: 119-S / 111-F
date: 2026-08-07
---

# Compacted: 119-S / 111-F — Crash-Resumption + Prune-on-Restore Protocol (34D50F2D candidate d)

**Decisions**: Implemented an operator-confirmed crash-resumption +
prune-on-restore protocol as autoharness product-template orchestration
prose over the EXISTING backlogit-checkpoint + agent-engram substrate
(no new checkpoint-schema field, no new runtime engine). Orchestrator
gets exclusive owner-routing (never performs Stage/Ship-owned restore/
resume/prune directly); Stage/Ship each get their own owner-scoped
fail-closed crash-resumption lifecycle (zero-candidate no-op, explicit
operator filename selection, ownership validation against own role,
`resolve_checkpoint` only after a confirmed successful owner resume, no
fresh-start fallback on invalid/ambiguous reads); the backlogit overlay
instruction gets a bounded read-select-summarize prune-on-restore section,
newly installed to close a pre-existing install gap.

**Files modified**: `templates/agents/_orchestrator.agent.md.tmpl`,
`_stage.agent.md.tmpl`, `_ship.agent.md.tmpl` + installed mirrors;
`templates/instructions/backlogit.instructions.md.tmpl` + newly-installed
`.github/instructions/backlogit.instructions.md`; `install-harness`/
`tune-harness` SKILL.md (Formal Overlay Contract); `verify_workspace.py`
(5 new `PACK_ASSERTIONS`); `.autoharness/harness-manifest.yaml` (6
checksums refreshed across 3 review rounds); `tests/test_crash_resumption_protocol.py`
(25 tests); `docs/design-docs/crash-resumption-prune-on-restore-protocol.md`.

**Key learnings** (durable form in compound docs, not repeated here):
1. `docs/compound/2026-08-07-copilot-review-fix-introduces-new-filter-bug.md`
   — across 3 full Copilot review rounds (16 total comments), each
   round's fix for a genuine "unsafe filter hides bad state" finding
   itself introduced a NEW, more subtle unsafe filter, caught by the next
   re-armed review. Generalizable rule: prefer "unfiltered enumeration +
   anomaly-first fail-closed check + only-then partition" over composing
   successively narrower filters; never conflate a fail-closed handoff
   (no action, state preserved) with an explicit disposition (resolve, or
   a named archival/abandonment decision) — a recurring shape whenever a
   fallback/degraded/handoff path risks being treated as satisfying a
   downstream completion gate.
2. CRLF/LF checksum gotcha (consistent with prior shipments): manifest
   checksums must be computed from committed git blob bytes, never a raw
   Windows working-tree read.

**Outcomes**: PR #310 merged (`8262bd2`, 2 parents, P-009 preserved). 16
Copilot findings across 3 rounds all fixed/replied/resolved (review-fix
cap explicitly removed for this shipment by operator directive) — P-018
gate final result `SATISFIED`, 0 unresolved threads. §1.9 gate PASS.
Safe-closed `119-S` (shipment `archived_status: shipped`; 7 tasks
pre-archived via the merge commit's own task loop) and covering feature
`111-F` (`archived_status: done`, fully covered — no sibling tasks
outside the manifest). Provenance chain intact: `34D50F2D` (stash
tracker, living/ACTIVE for deferred candidates (a)/(c); candidate (d)
marked `CONSUMED`) → `111-F` → `119-S`. Named stash
`preserve-unrelated-before-117-S-pipeline` and unrelated tracker
`936C68F3` both confirmed untouched. This was the **final** shipment in
the `117-S → 118-S → 119-S` serial chain — no successor remains queued.

**Failed approaches**: none of note — every Copilot finding across all 3
rounds was resolved on its first remediation attempt; no revert or
rollback was needed at any point in this shipment's lifecycle.
