---
title: "Compacted memory - 154-S / 146-F (docs/compound source value semantics)"
date: 2026-08-23
source: "docs/memory/compacted/2026-08-23-154s-146f-compacted.md"
doc_type: "reference"
agent: ship
---

# 154-S / 146-F - Compacted Session Memory

Consolidates:
`docs/archive/memory/2026-08-23-stage-external-backlogit-verification-and-compound-source-semantics.md`
(Stage, pre-claim) and
`docs/archive/memory/2026-08-23-ship-154-s-146-f-shipped-closure.md` (Ship,
merge + closure).

## Decisions

* `026-DL`: `docs/compound/` source value semantics correction is a SAME
  CONTRACT SURFACE, SEPARATE SUCCESSOR FEATURE from `140-F`/`025-DL`
  (already archived/shipped, cannot absorb new work). `140.001-T` AC3
  (verbatim frontmatter preservation) preserved, not amended. Displaced
  provenance string relocated verbatim into `citations` rather than
  destroyed.
* ONE shipment (`154-S`), not a serial chain - all three tasks share one
  contract surface, one review, one estimate.
* Stash `FAE1E7B7` deliberated, planned, reviewed PASS, harvested into
  `146-F`/`154-S`; CONSUMED and ARCHIVED.
* Stashes `3C7AAC71`/`90F2A9F8` (backlogit upstream, FIXED UPSTREAM PR #374)
  and `024-DL`/`025-DL` (RESOLVED-AND-DELIVERED) closed out in the same
  Stage session; `B57F9E24` remains ACTIVE (external backlogit V1-probe
  misclassification, `internal/events/memory.go:CreateCheckpoint`) and was
  never touched or implemented in this scope.

## Files modified (146-F)

* `docs/compound/2026-08-09-next-eligible-detail-scoping-and-vacuous-tiebreak-tests.md`
  - `source` corrected to self-referential path; displaced provenance
    string preserved verbatim under a new `citations` key.
* `tests/test_docs_compound_frontmatter_contract.py` - ratcheted from
  non-emptiness-only assertion to value-shape conformance.
* `templates/skills/compound/SKILL.md.tmpl` - Quality Criteria bullet
  aligned to the same value-shape standard.

## Merge and closure outcome

* PR #404 merged `--merge` (no admin) at `2026-08-24T01:56:19Z`, merge SHA
  `98e2d7264c8089250a0cf442aef362c98287ef77`, two parents verified (P-009).
* P-018 `SATISFIED` at final HEAD; all required CI checks green.
* Backlog cascade-close (P-015 verified fully-covered-root exception):
  `classify_shipment_close_path` -> `CASCADE`; `backlogit shipment ship
  154-S` executed; live workspace confirmed all 5 artifacts
  (`146-F`, `146.001-T`, `146.002-T`, `146.003-T`, `154-S`) archived with
  correct provenance and `parent_id` preserved.
* Runtime verification: `cli` surface probe PASS (`uv run autoharness
  --help`, exit 0) - the only applicable surface; no production source
  changed.
* Operational closure: `READY`, no residual risk, no open follow-ups.

## Failed approaches / anomalies worth remembering

* A prior in-PR commit (`42d8a7b2`) moved `146-F`/`146.003-T` to
  `status: done` via `backlogit move`, which this workspace's
  `registry.yaml` routes straight to `.backlogit/archive/` by directory
  rule - but that alone does not stamp `archived_from`/`archived_status`/
  `commit`. Required a corrective `backlogit archive <id>` pass on all
  four manifest items before shipment-level cascade close.
* `backlogit shipment ship`'s `archived_ids` response on `1.10.1` omitted
  the three pre-archived TASK items (only the feature + shipment were
  listed), contradicting the 2026-08-18 spike's byte-identical-shape
  invariant observed on `1.9.0`. Live-workspace state (queue/archive
  presence, `parent_id`, `archived_status`/`commit`) was the authoritative
  check and confirmed correctness despite the under-reported response.
  See `docs/compound/2026-08-23-cascade-close-archived-ids-omits-pre-archived-tasks-on-1101.md`.

## Follow-ups

None outstanding for 154-S itself. `B57F9E24` remains active and unrelated
to this scope.
