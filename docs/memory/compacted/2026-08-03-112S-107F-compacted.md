---
compacted_from: docs/archive/memory/2026-08-03-ship-112-S-107-F-session.md
shipment: 112-S
feature: 107-F
pr: 292
date: 2026-08-03
---

# Compacted: Ship 112-S / 107-F

Resumed Ship with operator approval scoped to PR #292 only. Re-verified all
defense-in-depth gates from scratch at HEAD `a8f96576e8939759948dc388de5fd2a0a6e3096a`
(branch/worktree topology, PR CI/mergeability, Local Review Readiness block,
P-018 copilot-review gate, 8/8 resolved threads, P-009 merge-strategy repo
settings) — all passed, no drift from the prior session's recorded state.

Merged PR #292 (`gh pr merge 292 --merge`) → merge commit
`5311a3127e247e11a9ab25b1bfc0bd4095393a77`, two parents, confirmed ancestor
of `origin/main`.

Post-merge: synced `main`, created closure branch
`post-merge/107-f-size-complexity-staging`, ran the required CLI runtime
probe (PASS), then safe-closed the backlog single-artifact-only (never
`backlogit shipment ship`): `107-F`'s only children are this shipment's 5
tasks, so the feature was closed alongside `112-S`. Explicitly archived all
5 tasks + shipment + feature; `113-S`/`108-F` verified untouched
(`queued`) after every mutation.

**Key learning (recurring, 3rd occurrence)**: tasks physically relocated to
`.backlogit/archive/` by an earlier `move --status done` are NOT
equivalent to an explicit `archive` transition — `status` stays `done`
until `backlogit archive <id>` is run explicitly. Caught proactively this
time via the documented pre-flight status-field check
(`docs/compound/2026-08-02-backlogit-done-move-vs-explicit-archive.md`,
now updated with this 3rd occurrence) before any "pre-archived, skip"
decision. Open follow-up (still unresolved after 3 occurrences): harden
Step 5 with a scripted/hard pre-flight check rather than a doc reminder.

Closure artifact: `docs/archive/closure/112-S-107-F-post-merge-closure.md`
(closure verdict READY; releasability READY). Closure PR still requires
its own separate, explicit operator approval (P-014) before merge; `113-S`
remains blocked until then.
