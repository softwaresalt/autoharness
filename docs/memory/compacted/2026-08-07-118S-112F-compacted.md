---
compacted_from: docs/archive/memory/2026-08-07-ship-118-S-112-F-session.md
release_unit: 118-S / 112-F
date: 2026-08-07
---

# Compacted: 118-S / 112-F — Mixed-Role Detection (Report-Only)

**Decisions**: Added `mode: detect-mixed-role` to `shipment-reconcile`
SKILL.md.tmpl — strictly READ-ONLY DETECTION + REPORT-ONLY diagnostic for
the queued-with-active-work / mixed-role shipment-status inconsistency,
using backlogit's two valid completed-task archive representations
((a) `status: done` in archive/, no provenance, or (b) `status: archived`
+ `archived_status: done` + `archived_from`). No mutation, no
`ClaimShipment` re-claim, no status write added. True record-only forward
auto-repair remains DEFERRED as UNSUPPORTED by backlogit 1.8.0 (manifest-
wide, single-shot ClaimShipment; no active→queued edge; no `blocked`
shipment status).

**Files modified**: `templates/skills/shipment-reconcile/SKILL.md.tmpl`
(classification tables, protocol, audit+telemetry, operator-remediation
guidance); `tests/test_shipment_reconcile_mixed_role_detection.py` (33
tests); `docs/compound/2026-08-06-shipment-mixed-role-detection-report-only.md`
+ `tests/test_shipment_mixed_role_detection_compound_doc.py` (10 tests).

**Key learnings** (durable form in compound docs, not repeated here):
1. `docs/compound/2026-08-02-backlogit-done-move-vs-explicit-archive.md` —
   6th occurrence: a protected-set member (covering feature) found already
   in `archive/` with `status: done` during safe-close's baseline gate is
   not automatically a cascade if the artifact's event log shows no
   cascade-op event and all its children are verified covered by the
   manifest; explicitly archive it as a deliberate final step once
   children are confirmed archived.
2. `docs/compound/2026-08-06-shipment-mixed-role-detection-report-only.md`
   — the detection capability's design rationale and archive-
   representation classification.

**Outcomes**: PR #308 merged (`f4f517c6`, 2 parents, P-009 preserved). 3
Copilot findings fixed/replied/resolved in one pass. §1.9/P-018 gates PASS.
Safe-closed 118-S (shipment `archived_status: shipped`; 4 tasks + feature
`112-F` explicitly archived, `archived_status: done`). Provenance chain
intact: `936C68F3` (stash tracker, living/ACTIVE, re-verified before and
after) → `013-DL` (deliberation, archived, cleanup-safe) → `112-F` →
`118-S`. `119-S` explicitly NOT claimed; its `pre_claim` topology gate
verified to pass.

**Failed approaches**: none of note — the local-review P2 fix and the 3
Copilot findings were each resolved on the first remediation attempt.
