---
compacted_from: docs/archive/memory/2026-08-08-ship-120-S-082-F-session.md
release_unit: 120-S / 082-F
date: 2026-08-08
---

# 120-S / 082-F Compacted Memory

## Decisions

- Documented Engram and graphtor-docs telemetry surfaces against the ratified
  `ToolTelemetryEvent` v1.0 contract via three new docs; source-verified all
  field-provenance claims directly against `engram`/`graphtor` read-only
  workspaces rather than inferring from public knowledge.
- Carried forward the operator-owned `.gitmodules`/`references/atv-phoenix`
  gitlink as a standalone labeled commit, unaltered, no backlog item.
- Isolated 9 unrelated dirty-worktree files (121-S model-route rename,
  checkpoints, rename-deliberation doc) via a labeled `git stash push -u`
  before branch creation — 5th precedent occurrence of this Ship pattern
  (117-S, 114-S, 098-S, 059-S, 120-S).
- Feature 082-F fully covered by 120-S's 3-task manifest (no siblings) — safe-
  closed both the shipment (`shipped`) and the feature (`done`) via the
  non-cascading single-artifact sequence.

## Files modified/created

- `docs/telemetry/engram-evidence-map.md`, `docs/telemetry/graphtor-docs-evidence-map.md`,
  `docs/telemetry/cross-pack-adapter-gap-report.md` (new).
- `docs/telemetry/backlogit-evidence-map.md`, `docs/telemetry/backlogit-sensitivity-guardrails.md`
  (back-link additions only).
- `.gitmodules` / `references/atv-phoenix` (operator-owned gitlink).
- `docs/closure/120-S-082-F-post-merge-closure.md`,
  `docs/compound/2026-08-08-state-vs-call-outcome-conflation-in-telemetry-mapping.md`.

## Key learnings / cross-references

- `docs/compound/2026-08-08-state-vs-call-outcome-conflation-in-telemetry-mapping.md`:
  a subsystem's own reported state (or a completion-time field, or a field
  absent from the wire) must never be conflated with the calling operation's
  own outcome/timing/provenance — the pattern behind all 5 Copilot findings on
  PR #314.
- CI flakiness triage: confirm a failing check's plausibility against the
  actual diff (`git diff --name-only`) before assuming a code fix is needed; a
  pre-existing concurrency test flake in unrelated test infrastructure merited
  a simple retry, not remediation.

## Outcomes

- PR #314 merged via merge commit `ca066a053c891fa2152c85c2f2936f6507e81fa3`
  (two parents, P-009 preserved).
- 120-S: `status: shipped` -> `archived_status: shipped`.
- 082-F: `status: done` -> `archived_status: done`.
- `closure_status: READY`, `feature_terminal_status: done`.
- Dark-mode cursor advances to **121-S** (feature 113-F); the isolated stash
  from this session must be restored to `main` as part of 121-S's own
  preflight (it carries 121-S-relevant content).

## Provenance chain

120-S (this shipment) <- 082-F (feature, fully covered) <- 3 tasks
(082.001-T, 082.002-T, 082.003-T, all done/archived) <- Stage-authored plan
`docs/plans/2026-08-07-082F-cross-pack-measurability-plan.md` (not
consolidated this pass — no appended review section, does not meet the
compaction candidate criterion) <- review `082.001-R` (PASS).
