---
date: 2026-08-18
agent: ship
shipment: 141-S
feature: 132-F
disposition: shipped
type: compacted-summary
source: docs/archive/memory/2026-08-18-ship-141-s-p015-cascade-pre-archived-full-lifecycle.md
---

# 141-S / 132-F — Compacted Summary

Full P-017 dark-mode Ship lifecycle for shipment 141-S (feature 132-F,
stash EDE3CC2D): staging PR #364 (merge `bd1b2e96`) then implementation PR
#365 (merge `01c1735b`, 2 parents each, verified). Fixed a documentation/
contract gap: the Cascade Close Sub-Procedure and P-015 policy text had no
explicit tolerance for pre-archived manifest members, even though the
classifier (`classify_shipment_close_path`) already scanned queue+archive
transparently and the underlying cascade op is idempotent over them (proved
by Stage's spike). No gate/engine code changed — TDD (9 regression tests)
confirmed the classifier already behaves correctly; only the skill
template + policy text + tests were updated.

**Key learnings**:
* Role-boundary correction pattern: Copilot findings on Stage-owned staging
  artifacts were valid but not directly editable by Ship (P-010) — replied
  explaining the boundary, fixed the real defect at the already-authorized
  implementation-scope file, resolved the thread.
* `backlogit shipment claim` cascades to activate the full manifest
  atomically — expected tool behavior.
* This shipment's own manifest is itself a live CASCADE instance — its
  closure directly production-validates the contract it documents (unlike
  140-S, which deviated from CASCADE to manual safe-close; this closes that
  tracked residual).
* Protected operator-staged submodule entries (`.gitmodules`, 3 gitlinks)
  verified byte-identical across 7+ branch transitions and 11 commits this
  session — zero drift.
* CHANGELOG.md: judged not warranted (doc/policy/test-only change,
  consistent with the repo's precedent of selective, behavior-affecting-only
  entries — e.g. 140-S's actual gate-code fix also received none).

Full detail archived at
`docs/archive/memory/2026-08-18-ship-141-s-p015-cascade-pre-archived-full-lifecycle.md`
and in `docs/closure/141-S-132-F-post-merge-closure.md`.
