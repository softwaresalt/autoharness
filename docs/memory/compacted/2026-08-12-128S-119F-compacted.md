---
compacted_from: docs/archive/memory/2026-08-12-ship-128-S-119-F-session.md
release_unit: 128-S / 119-F
date: 2026-08-12
---

# Compacted Memory — 128-S / 119-F (S2 Copilot Supervisor Supervision Core Library, Unwired)

**Source (archived verbose original)**:
`docs/archive/memory/2026-08-12-ship-128-S-119-F-session.md`

## Decisions

1. All 6 tasks implemented test-first in dependency order (protocol/
   adapters -> session state machine -> typed events -> journal ->
   recovery/restart); the S1-frozen gated-action catalog and session
   state contract (distinct `CANCELLED` terminal state, no direct
   pre-terminal-to-`FAILED` edge) preserved unchanged.
2. UNWIRED invariant enforced as a hard gate throughout: reverified via
   grep (zero hits) after implementation, after every review-fix commit,
   pre-merge, and post-merge.
3. Two hosted Copilot findings (`session.py:75`, `recovery.py:136`)
   dismissed as false positives only after fetching the frozen task spec
   text (`backlogit get 119.003-T`/`119.006-T`) and checking literal
   acceptance-criteria wording — both suggested changes would have
   violated the frozen spec, not fixed a bug.

   **CORRECTED (post-merge closure PR #329's own hosted review)**: this
   was wrong — the spec check was run against a paraphrase of each
   comment, not its exact text, and both findings were genuine. Fixed and
   merged via PR #330 (`f47c1b65bdc28aad5594856db7a592f0092f929f`). See
   the rewritten compound doc; lesson generalized to "quote the exact
   comment text, never a paraphrase, when triaging."
4. Review-fix circuit breaker (3 cycles) enforced strictly: a 5th Copilot
   review round on the final fix commit was not opened as a 4th cycle.
   2 findings with posted threads were replied-to/resolved as documented
   follow-ups; 4 suppressed-only findings (review-body text, no thread)
   were documented in the PR body only, not separately mined or fixed,
   per explicit operator instruction.
5. Close path selected via the machine-checkable classifier
   (`classify_shipment_close_path`), confirming `CASCADE` for `119-F`'s
   fully-covered-root topology — reapplying the `127-S`/`118-F` precedent
   without re-derivation.

## Files Modified

- `src/autoharness/supervise/{protocol,process_adapter,process_pty,session,events,journal,recovery}.py`
  — new supervision core library modules, plus corresponding
  `tests/test_supervise_*.py` suites (initial TDD implementation).
- Review-fix cycle 3 (commit `06c280bd`): `recovery.py` (fail-closed
  `confirm_restart()` exception handling; old-child termination before
  `LAUNCHING`), `process_pty.py` (`ChildProcessError`/`TimeoutError`
  disambiguation in `PtyChildProcess.close()`; `WinPtyChildProcess.close()`
  terminate-then-close with guaranteed handle clearing), `journal.py`
  (`_ensure_initialized()` now handles a crash during the first header
  write) — plus 8 new regression tests across the 3 corresponding test
  files.
- `.backlogit/` state — full task/feature/shipment lifecycle + cascade
  close for the 6 tasks, `119-F`, `128-S`.

## Key Learnings (cross-references)

- `docs/compound/2026-08-12-verify-hosted-review-findings-against-frozen-task-spec.md`
  — **corrected**: originally framed as "verify against frozen spec
  before dismissing," this session's own initial application of that
  lesson still produced 2 wrong dismissals because it checked a
  paraphrase, not the exact comment text. Corrected lesson: quote the
  literal review-comment text before triaging as a false positive; a
  closure/compound doc is itself a review-checkable artifact.
- Cross-reference (reused, not re-derived):
  `docs/compound/114-S-109-F-copilot-review-fix-patterns.md` (suppressed-
  vs-active comment distinction) and
  `docs/compound/2026-08-12-close-path-decisions-must-use-the-classifier-not-summarized-prose.md`
  (close-path classifier discipline, from `127-S`).

## Outcomes

- PR #328 merged via merge commit `915923c25453739b6da955fe247bd4b38a11e830`
  (two parents, P-009 preserved structurally and by repo config).
- Full test suite 1681/17/0 on merged `main`; `uv run autoharness --help`
  smoke test PASS pre- and post-merge; UNWIRED invariant held.
- Dynamic engine attestation: CLI `v1.9.0`/`39528a4`/build
  `2026-08-12T03:49:03Z`; no stray process found (prior session's cleanup
  held).
- Closure simulation re-run against attested engine: 66/66 passed.
- Cascade close: `backlogit shipment ship 128-S` -> `returned_ids: []`,
  `archived_ids` = all 6 tasks + `119-F` + `128-S` — matched predicted
  outcome exactly; every task's `parent_id: 119-F` preserved; `129-S`
  confirmed untouched (`queued`).
- `backlogit doctor` post-close: 62 pre-existing, unrelated warnings
  (`007`-`046` range); zero findings referencing this shipment's
  artifacts.
- 21 Copilot review comments (posted threads) across 5 review rounds; 19
  fixed/replied/GraphQL-resolved, 2 documented as follow-ups (circuit
  breaker exhausted); 4 additional suppressed-only findings documented in
  the PR body only.
- Dark-factory bounded scope `128-S` complete; `129-S` explicitly NOT
  claimed or expanded into.
- **Correction PR #330** (discovered via closure PR #329's own hosted
  review): 2 findings mis-triaged as false positives above were genuine;
  fixed in PR #330 (merge commit `f47c1b65bdc28aad5594856db7a592f0092f929f`,
  two-parent merge-commit strategy, full suite re-verified 1706/17/0)
  before this closure's final form.

## Provenance Chain

`docs/closure/128-S-119-F-post-merge-closure.md` (full detail) ->
`docs/archive/memory/2026-08-12-ship-128-S-119-F-session.md` (archived
verbose original) -> this compacted summary.

## Deferred

- 2 Copilot findings (posted threads) on commit `06c280bd`, deferred per
  exhausted 3-cycle review-fix circuit breaker.
- 4 suppressed-only Copilot findings (review-body text, no thread) from
  the same 5th review round, documented but not independently triaged.
- `129-S` is now unblocked (predecessor `128-S` shipped) but was not
  claimed this session — routed to the Orchestrator to advance the
  cursor after reloading current `main`.
