---
compacted_from: 2026-08-06-ship-117-S-110-F-session.md
shipment: 117-S
feature: 110-F
date: 2026-08-06
feature_closed: true
---

# Compacted: Ship 117-S / 110-F Session (feature fully covered, closed)

Executed Ship end-to-end for `117-S` — Phase 1 of the deferred DAG-visibility
follow-up from spike `001-SP` (stash `33CC445C`) — under an operator-approved
bounded P-017 dark-factory contract (resolved route
`claude-sonnet-5`/`anthropic`/`high`; merge + admin fallback pre-approved,
scope strictly `117-S`, first of serial chain `117-S → 118-S → 119-S`;
`118-S` **not** claimed; review-fix-cycle cap explicitly removed by operator
but not needed — 2 review rounds sufficed). No MCP tools available
(backlogit/engram/intercom/graphtor all degraded — CLI-only via
`backlogit`/`git`/`gh`).

Implemented all 3 manifest tasks: `110.001-T` (new `compute_dag_readiness()`
analyzer in `gates/topology.py` reusing the existing pipeline-topology
shipment-blocks reader; cycle detection newly owned by the analyzer, not the
reused reader), `110.003-T` (`autoharness gate dag-readiness [--json]` CLI
subcommand, existence-guarded + DEGRADED-safe), `110.002-T` (reference doc +
cross-refs). 33 new unit tests, full suite 1294 passed/11 skipped/403
subtests.

Local review (code-review task agent, 2 rounds): round 1 found one real P1
(ready-set/predecessor-finished logic failed open on ambiguous
live+archive shipment provenance — the sibling topology gate already
guards this, the new analyzer didn't); fixed with 2 new regression tests.
Round 2: verdict READY, zero P0/P1, one non-blocking P2 (tie-break
determinism) resolved with a clarifying comment only.

PR #305 hit a genuine **GitHub Actions infrastructure degradation**
(`Failed to resolve action download info` / `Service Unavailable` fetching
pinned actions) across both CI jobs and the auto-triggered Copilot review
workflow — not a code defect. Multiple `gh run rerun` cycles (escalating
from `--failed` to a full rerun once the run settled) were needed before a
clean CI pass (`detect code changes`, `test`, `pipeline-topology (ambient)`,
`ci gate` all green). The Copilot review workflow itself is not rerunnable
(`gh run rerun` refuses "cannot be retried") and posted `state: COMMENTED`
with an explicit "Copilot encountered an error... re-request a review" body
and **zero inline threads** — per the documented completion signal
(`state != PENDING` counts as complete) plus zero unresolved threads, P-018
gate (`autoharness gate copilot-review`) returned `SATISFIED` both times it
was checked (no repository-approved re-request wrapper configured; the
`gh pr edit --add-reviewer` fallback is explicitly disclaimed as
unreliable for Copilot's special identity, so this was recorded as-is per
protocol rather than forced). Zero inline comments meant the operator's
"fix/reply/resolve each Copilot comment" requirement was vacuously
satisfied.

P-009 confirmed via repo settings (`allow_merge_commit: true`,
`allow_squash_merge/allow_rebase_merge: false`) before merge. Merged via
`gh pr merge 305 --merge` → merge commit `24b488f6...` (two parents
confirmed, ancestor of `origin/main` confirmed).

Post-merge: created closure branch
`post-merge/110-f-dag-readiness-critical-path-reporting` from synced
`main`. Safe-closed `117-S` as a single-artifact, non-cascading shipment
(`backlogit shipment ship` never called): protected set was `110-F` alone
(no sibling tasks outside the manifest — genuinely fully covered).

**Pre-flight archive-status trap caught proactively** (5th recorded
occurrence, see
`docs/compound/2026-08-02-backlogit-done-move-vs-explicit-archive.md`):
all 3 manifest tasks were physically under `.backlogit/archive/` from the
task-loop's own `move --status done` commits, but still carried
`status: done`, not `status: archived` — re-reading the compound doc during
closure caught this before accepting the "pre-archived" skip
classification; all 3 explicitly archived and reverified. Shipment record
moved live `shipped` → verified → archived → verified `archived_status:
shipped`.

Because `110-F`'s only 3 children are exactly this shipment's manifest,
Ship performed a separate, deliberate feature closure: `status: done` →
verified → archived → verified `archived_status: done`. Named stash
`preserve-unrelated-before-117-S-pipeline` confirmed present and untouched
throughout (never popped/dropped).

Full closure evidence: `docs/memory/compacted` (this file) plus the
archived verbose session record. **Next cursor: `118-S`** (not claimed this
session, per scope constraint).

**Outstanding**: none new. The cross-shipment archive-status-trap hardening
follow-up (scripted pre-flight `status:` check in Step 5 Closure Tasks /
`shipment-reconcile` safe-close, rather than relying on compound-doc
recall) remains open, now reinforced by a 5th occurrence.
