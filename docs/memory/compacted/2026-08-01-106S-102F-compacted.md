---
type: memory-compacted
shipment: 106-S
feature: 102-F
tasks: [102.001-T, 102.002-T]
feature_pr: 270
merge_commit: 72ceba1e1bf4d7619f153ecee4f1dd47063f3ae3
reviewed_head: f01151588205edc6329afdc045012cf201b39338
compacted_at: 2026-08-01
tags: [ship-agent, claim-integrity, backlogit, copilot-review, p-018, p-015, p-020, dogfood-self-mod]
---

# Compacted Memory — 106-S / 102-F Ship claim-integrity verification

Dense per-release-unit consolidation (P-020 Tier-1 post-merge floor). No verbose
Stage memory original existed for this unit; this is the authoritative compacted
record. The 102-F plan was consolidated into a decided-plan in the same pass
(`docs/plans/2026-07-30-ship-claim-integrity-preflight-decided-plan.md`; verbose
original archived to `docs/archive/plans/`).

## What shipped

- **Unit A (`102.001-T`)** post-claim shipment-status verify → `CLAIM_VERIFY_FAILED`
  (CLI-preferred re-read; retry-once only on `queued`; `blocked` halts immediately,
  no retry/claim).
- **Unit B (`102.002-T`)** intake early-warning → `SHIPMENT_STATE_INCONSISTENT`
  (runs before status validation AND before claim; task-artifact-filtered;
  shipment-existence guarded in the generic intake).
- Applied to template `templates/agents/_ship.agent.md.tmpl` + dogfood mirror
  `.github/agents/_ship.agent.md`; mirror checksum regenerated in
  `.autoharness/harness-manifest.yaml`
  (`bf56d28b8f5951c9a3d656d578aa883f883556e723deae84245b5c53ee888833`). Unit B
  landed the final mirror bytes + end-state checksum. TDD harness
  `tests/test_ship_claim_integrity_guards.py` (10 `unittest` test methods).

## Merge

- PR **#270** merged to `main` as merge commit **`72ceba1`** (two parents
  `c74d6c3` base + `f011515` feature HEAD → P-009 merge-commit satisfied).
- Reviewed HEAD at merge `f011515`. Operator-approved.

## Decisions & rationale (condensed)

- Placement of Unit B **before** validation + claim is mandatory: the existing
  status validation accepts only `queued`/`active` (rejects `blocked` earlier) and
  a `queued → active` claim would mask the inconsistency.
- Unit A prefers the **CLI** re-read because MCP (`Transport closed`) is the exact
  flaky surface the guard catches — the verify must not be defeated by the same
  transient.
- `blocked` is claim-prevention state: remediation is `blocked → queued` (after the
  gate clears) before any claim; never re-claim-to-`active` on `blocked`.

## Copilot review (3 rounds, P-018 engaged; 11 threads all resolved)

- **R1** (9 comments on `e16d7ae`): key learning — `{{STATUS_BLOCKED}}` **is** a
  real registered placeholder (SKILL.md:227, registry `status_values.blocked`,
  `verify_workspace.py:178`); the earlier assumption it didn't exist was WRONG,
  Copilot was correct. Fixes: template `{{STATUS_BLOCKED}}` placeholderization,
  task-artifact filtering of the Unit B scan (template + mirror), 6 test
  hardenings. Commit `8616454`.
- **R2** (1 comment on `8616454`): mirror scans were unconditional but the generic
  Work Intake allows a "no shipment" path → would deref an unset `shipment_id`.
  Fix: shipment-exists guards on mirror steps 1a + 5, added a test. Commit
  `f011515`.
- **R3** (C11 on `f011515`): asked for a pre-claim shipment-status validation gate
  in the mirror — identified as **exactly the plan's deferred P3-3** (out of scope
  to keep single-family blast radius). Hit the 3-cycle review-fix circuit breaker →
  HALTED and escalated. **Operator chose Option A: defer C11.** Replied citing the
  plan P3-3 + residual-risk analysis + deferred stash `2970FA4E`; resolved the
  thread → P-018 flipped to `SATISFIED`.

## Gotchas / failed approaches

- **CI flake** `test_two_writers_interleaved_seal_preserve_every_distinct_segment`
  (`test_telemetry_jsonl_sink.py`): timing-sensitive interleaved-writer test,
  pre-existing, never touched by this shipment, passes locally. Re-ran the failed
  CI job → passed.
- **Race window** on the P-018 gate: a gate check run *before* a fresh Copilot
  review registers falsely shows 0 comments. Always verify
  `latestCopilotReviewCommit == HEAD` AND unresolved == 0 with back-off after each push.
- **Shell-safety**: PowerShell corrupts backticks/`$` in double-quoted
  `gh api -f body=...`; write bodies to BOM-less UTF-8 files and pass
  `--field body=@file`.
- **Dogfood self-modification**: this shipment edited the Ship agent's own template
  + dogfood mirror — surgical, internally-consistent edits, kept in sync, mirror
  checksum regenerated (LF-pinned via `.gitattributes`).

## Closure

- Safe-close (097-S / P-015): manifest tasks `102.001-T` + `102.002-T` were
  done+archived during build; shipment `106-S` archived single-artifact (never
  `shipment ship`); covering feature **`102-F` preserved** in queue.
- Runtime verification: CLI smoke `uv run autoharness --help` PASS; full local
  build `unittest discover` → **770 OK (skipped 7)**.
- Compound learning: `docs/compound/106-S-claim-integrity-guards.md`.
- Operational closure: `docs/closure/106-S-102-F-post-merge-closure.md` (READY).

## Follow-ups

- **C11 / P3-3** deferred → stash `2970FA4E` (`shipment-reconcile` pre-mode
  shipment-record-status classification).
- **R3** backlogit-internal `blocked → active` illegal-flip → routed upstream.
