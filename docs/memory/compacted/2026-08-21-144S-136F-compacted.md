---
title: "Compacted memory - 144-S / 136-F"
date: 2026-08-21
agent: ship
shipment: 144-S
feature: 136-F
tasks: ["136.002-T", "136.003-T"]
pr: 382
merge_commit: c4e4851cb2e4e1ebee72f675b4bd96264f3a87ad
source: docs/archive/memory/2026-08-21-ship-144-s-execution-and-closure-session.md
---

# Compacted memory: shipment 144-S

**What shipped**: restored workspace-wide `backlogit docs lint` traversal
(the known malformed file was already fixed by `138.001-T`/`146-S`); added
`tests/test_docs_frontmatter_decodes.py`, a regression guard classifying
every `docs/**/*.md` frontmatter block as none/ok/unterminated and failing
on unterminated or malformed-YAML blocks with file:line.

**Key decisions**:
* `136.002-T` sweep found zero additional confirmed hazards and zero
  out-of-scope captures (legitimate zero/zero outcome), cross-checked via
  direct PyYAML parsing (476 files) and `backlogit docs lint` output (536
  findings, none decode-type).
* `136.003-T` guard TDD-verified via temporary local revert of the
  `138.001-T` fix (not committed): fails and names the correct file:line,
  then passes again once restored.
* Copilot review (PR #382) caught 2 findings, both fixed: stale
  `.backlogit/queue/...` docstring paths (should read `.../archive/...`
  once tasks are done), and an unterminated-frontmatter detection gap
  (regex could not distinguish "no frontmatter" from "opened, never
  closed" -- replaced with a three-way `_frontmatter_status` classifier).
* Self-review (own pass) fixed a BOM-handling gap (`utf-8` -> `utf-8-sig`).

**Files modified**: `tests/test_docs_frontmatter_decodes.py` (new);
`.backlogit/` metadata (claim/done/archive transitions).

**Failed approaches**: none -- the regex-based extractor worked for the
cases it was designed for but was insufficiently general (see Copilot
finding above); replaced rather than patched.

**Outcome**: PR #382 merged (`c4e4851c`, two parents confirmed, merge-commit
strategy per P-009). P-015 closure: `classify_shipment_close_path` returned
**CASCADE** (`136-F` fully covered by all three manifest children including
pre-archived `136.001-T`); `backlogit shipment ship 144-S` archived
`136.002-T`, `136.003-T`, `136-F`, `144-S` exactly, `returned_ids: []`,
`136.001-T.parent_id` preserved as `136-F`. Source stash `395EBE60` was
already retired by Stage before this session (confirmed absent).

**Follow-ups (not new, already tracked)**: stash entries `E8158860`
(full-suite test-isolation failures, reproduced identically here),
`F73BA065` and `90F2A9F8` (docline lint required-field/hard-abort
findings), `8FA8FC22` (unrelated template-variable gap). All remain
Stage's to deliberate (P-021 C6).

**Compound learning promoted**: `docs/compound/2026-08-21-backlogit-1-10-shipment-claim-cascades-to-children.md`
(backlogit 1.10.0 cascades shipment claim to the covering feature and
queued tasks -- previously noted only in the 147-S session memory).

Full verbose session record:
`docs/archive/memory/2026-08-21-ship-144-s-execution-and-closure-session.md`.
