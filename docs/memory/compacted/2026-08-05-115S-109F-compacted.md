---
compacted_from: 2026-08-05-ship-115-S-109-F-session.md
shipment: 115-S
feature: 109-F
date: 2026-08-05
---

# Compacted: Ship 115-S / 109-F Session

Executed Ship end-to-end for `115-S` (gate B / hooks + install adapters of
the staged `pipeline-topology` rollout, covering feature `109-F`) under the
already-active P-017 dark-factory contract (ordered scope `114-S → 115-S →
116-S`, strictly `115-S` this turn; resolved route
`claude-sonnet-5`/`anthropic`/`high`; review-fix cycle cap removed for this
session but not actually needed; MCP/Engram/Graphtor/Intercom all
declared degraded — CLI-only via `backlogit`/`git`/`gh`).

Implemented all 10 manifest tasks
(`109.007/008/010/013/015/017/018/021/022/023-T`), enforcing the required
109.021/022/023-T ordering ahead of any hook/agent activation task. These
three tasks closed `114-S`'s carried-forward `READY_WITH_CONDITIONS`
conditions: a read-only `CLAIM_NOT_OBSERVED` retry-required contract
(replacing an illusory post-claim self-retry), a `cli.py` telemetry
outcome-mapping fix (non-zero/non-blocked/non-forced results now map to
`failed`), and a `closure_complete()` fix requiring actual
`closure_status`/releasability validation (not `compaction_status` alone).

Local code-review found 2 findings (P1 checksum drift from a PowerShell
CRLF-remangling gotcha when computing manifest checksums; P2 dangling
cross-reference in the Ship agent mirror's `CLAIM_NOT_OBSERVED`
queued-branch reclaim sequence) — both fixed in one round.

PR #300 carried through **2 rounds of hosted Copilot review**: round 1
raised 10 real threads, all converging on two root causes — (a) 8 threads:
the topology gate's `_branch_ownership_check` never recognized
`post-merge/{feature_slug}` closure branches (feature-scoped, not
shipment-scoped — fixed via a new `BRANCH_POST_MERGE_CLOSURE_ELIGIBLE`
pass-through token); (b) 2 threads: `install-harness`/`tune-harness`
SKILL.md falsely declared the pre-push hook check "universal" when it is
gated by `local_gating.pre_push_enabled`. All 10 fixed in one round,
replied-to individually with the fixing commit and resolved via GraphQL;
round 2 (fresh re-review) returned **zero new threads**. Full pattern
breakdown: `docs/compound/115-S-109-F-checksum-and-branch-ownership-patterns.md`.
Full test suite grew to 1223 passed / 7 skipped / 359 subtests by final
HEAD `e9bf88d`.

Merged via `gh pr merge 300 --merge` → merge commit `04cdea1...` (two
parents, confirmed ancestor of `origin/main`). P-009/P-016/P-018/§1.9 all
verified PASS at final HEAD, re-verified unconditionally immediately
before merge (no HEAD drift).

Post-merge: re-read the freshly merged Ship agent instructions and
shipment-reconcile skill before self-closing `115-S`. Created closure
branch `post-merge/109-f-topology-gate-b-hooks-install-adapters` from
synced `main`. Live-verified the session's own Root Cause B fix
(`BRANCH_POST_MERGE_CLOSURE_ELIGIBLE`) actually resolves the
`TOPOLOGY_GATE: lifecycle` check on the real post-merge closure branch.

**Safe-close pre-flight caught a real bookkeeping gap**: task `109.013-T`
was still `status: active` (never advanced to `done`) despite its
implementation being fully merged earlier this session — fixed via
`backlogit move 109.013-T --status done` before the shipment-record close.
Lesson: always verify every manifest task's live status directly before
safe-close; never infer completeness from commit history or a prior
session's summary.

Safe-closed `115-S` as a **single-artifact, non-cascading,
partial-feature** shipment: 9 of 10 manifest tasks were already
pre-archived, 1 (`109.013-T`) fixed live; protected set (covering feature
`109-F` + 3 sibling tasks under `116-S`) verified intact before and after
every mutation; `backlogit shipment ship` never called.

Full closure evidence: `docs/archive/closure/115-S-109-F-post-merge-closure.md`
(`closure_status: READY` — no residual defects carried forward this round;
`114-S`'s three conditions are now closed by this shipment's own
109.021/022/023-T, confirmed via commits present in `main`'s history).
Verbose session record archived to
`docs/archive/memory/2026-08-05-ship-115-S-109-F-session.md`.

**Outstanding**: `116-S` (gate C) remains queued; its `pre_claim` topology
gate was confirmed `blocked: PREDECESSOR_CLOSURE_INCOMPLETE` until this
closure document existed, and confirmed `passed`/eligible immediately
after writing it. Per explicit operator instruction, Ship does not claim
or start `116-S` this turn. The post-merge closure PR requires its own
separate explicit operator approval (P-014) — the dark-mode approval for
PR #300 does not automatically re-cover a second PR's approval without
scope re-confirmation. No new follow-up defects identified this round.
