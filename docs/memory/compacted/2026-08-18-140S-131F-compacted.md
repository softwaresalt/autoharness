---
compacted_from: docs/archive/memory/2026-08-18-ship-140-s-topology-hotfix-full-lifecycle.md
release_unit: 140-S / 131-F
date: 2026-08-18
---

# Compacted Memory — 140-S / 131-F (Topology Gate Directional-Predicate Reliability Hotfix)

**Source (archived verbose original)**:
`docs/archive/memory/2026-08-18-ship-140-s-topology-hotfix-full-lifecycle.md`

## Decisions

1. Executed Step A first: pushed the pre-existing `chore/stage-topology-hotfix-cancel-138`
   branch unchanged, opened docs/backlog-only staging PR #359 (operator scope
   correction, 129-F/task rejections, 138-S cancellation handoff, 131-F/140-S
   hotfix planning artifacts), merged with a verified 2-parent merge commit
   (`de896ebf`) before claiming any shipment.
2. Claimed 140-S, TDD-fixed `_prior_shipment_id`'s direction-blind reverse-edge
   suppression (restricted to numerically-lower declaring shipments only) per
   the verified diff already authored in
   `docs/compound/2026-08-18-topology-gate-forward-dependent-suppression-residual-defect.md`,
   plus one regression test.
3. Merged feature PR #360 with a verified 2-parent merge commit (`57b5af38`)
   after a clean (zero-thread) Copilot review and P-018/P-009/P-014 gates.
4. Marked the `139-S` closure doc's `topology-forward-dependent-suppression-fix`
   condition `satisfied: true` (append-only Addendum, historical narrative
   preserved) so `closure_complete("139-S")` no longer blocks `138-S`.
5. Closed `140-S` via **manual safe-close**, not the P-015 cascade path, even
   though the classifier returned CASCADE-eligible: both manifest items
   (`131-F`, `131.001-T`) were already individually archived by ordinary
   Step-2 task-completion routing before the closure step ran, an unresolved
   precondition the cascade sub-procedure has no tolerance for. Both
   classified `pre-archived`; shipment record itself moved to `shipped` ->
   verified -> archived -> verified `archived_status: shipped`.

## Files Modified

- `src/autoharness/gates/topology.py` — `_prior_shipment_id` restricted to a
  directional (numerically-lower-only) predicate; third correction to this
  same function (skip-violator -> any-direction -> directional).
- `tests/test_gates_topology.py` — added
  `test_higher_numbered_forward_dependent_does_not_suppress_targets_own_predecessor_check`
  (94 tests total, 113 subtests).
- `docs/closure/139-S-130-F-post-merge-closure.md` — condition satisfied,
  `closure_status: READY`, append-only Addendum.
- `docs/closure/140-S-131-F-post-merge-closure.md` — new closure record
  (interim `PENDING_CLOSURE_PR` pending closure-branch PR merge).
- `.backlogit/` — `131-F`/`131.001-T` archived (done); `140-S` shipped and
  archived via manual safe-close.

## Key Learnings (cross-references)

- `docs/compound/2026-08-18-ship-role-boundary-copilot-findings-in-forbidden-artifacts.md`
  — when Copilot flags a valid defect inside a P-010-forbidden artifact
  (backlog acceptance-criteria field, plan/review doc), reply + classify +
  GraphQL-resolve; never edit the forbidden artifact, even trivially.
- `docs/compound/2026-08-18-lifecycle-gate-must-precede-safe-close-mutation.md`
  — the `lifecycle` topology gate phase requires the target shipment to
  still be `active`; running safe-close's `shipped`+archive mutation first
  makes the gate unconditionally fail. Caught and corrected in this session
  (nothing yet committed) by restoring pre-close queue-file state from the
  feature-branch merge commit before redoing the mutation in order.
- Reused (not re-derived): the P-015 verified-fully-covered-root classifier
  and the 139-S closure's pre-archived/protected-set safe-close discipline.

## Outcomes

- PR #359 (staging) merged `de896ebf2ddd14d0b104118a902e4bec1c554a63`;
  PR #360 (feature) merged `57b5af38008905ea47ce01887c1680205b75350e` — both
  verified 2-parent merge commits, both P-009-compliant.
- Targeted tests: 94/94 (113/113 subtests). Full suite: 1550 passed, 1
  pre-existing unrelated flake, 20 skipped — no new failures.
- P-018 gate: `SATISFIED` on both PRs (3 replied+resolved threads on #359,
  zero threads on #360).
- Manual safe-close: `140-S` `archived_status: shipped` verified; protected
  set (empty) intact throughout.
- `138-S` remains `queued`, untouched; both its blocking predecessors
  (`139-S`, `140-S`) are now shipped — gate-eligible for a **future**
  session's mechanical abandonment only, not abandoned this session.

## Provenance Chain

`docs/closure/140-S-131-F-post-merge-closure.md` (full detail) ->
`docs/archive/memory/2026-08-18-ship-140-s-topology-hotfix-full-lifecycle.md`
(archived verbose original) -> this compacted summary.

## Deferred

None — no findings were declined-with-rationale on the feature PR (zero
Copilot threads); the three staging-PR findings were replied-to and
resolved, not deferred. `138-S` mechanical abandonment is explicitly
deferred to a future session per operator instruction, not a compaction
deferral.
