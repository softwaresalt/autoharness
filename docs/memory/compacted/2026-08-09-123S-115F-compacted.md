---
compacted_from: docs/archive/memory/2026-08-09-ship-123-S-115-F-session.md
release_unit: 123-S / 115-F
date: 2026-08-09
---

# Compacted Memory — 123-S / 115-F (Deterministic Next-Eligible Resumption Advisory)

**Source (archived verbose original)**:
`docs/archive/memory/2026-08-09-ship-123-S-115-F-session.md`

## Decisions

1. Local Review Gate performed directly (not delegated) — single
   continuous diff, modest size.
2. All 3 Copilot findings fixed immediately (not deferred) — none were
   false positives; genuine contract/test-quality defects, fixed within
   the bounded review-fix cycle budget (1 of 3 used).
3. `115-F` closed as a separate step after `123-S` safe-close, following
   the established `121-S`/`119-S`/`122-S` precedent for fully-covered
   features.
4. Verified `.github/agents/_ship.agent.md` and
   `templates/skills/shipment-reconcile/SKILL.md.tmpl` were untouched by
   this PR's diff during the mandatory pre-self-close context reload —
   no contract drift risk.

## Files Modified

- `src/autoharness/gates/topology.py` — `NextEligibleResult` +
  `compute_next_eligible`; `next_eligible_detail` scoping fix (Copilot).
- `src/autoharness/cli.py` — `_gate_dag_readiness_command` wiring +
  `_format_next_eligible_line`.
- `tests/test_gates_dag_readiness.py`,
  `tests/test_gate_dag_readiness_cli.py` — 29 new tests total; tie-break
  fixture fix + assertion corrections (Copilot).
- `docs/dag-readiness-gate.md`, `docs/gates-reference.md` — full
  documentation; branch-numbering fix (Copilot).
- `.backlogit/` state — lifecycle commits.

## Key Learnings (cross-reference)

- `docs/compound/2026-08-09-next-eligible-detail-scoping-and-vacuous-tiebreak-tests.md`
  — per-branch field population in a multi-outcome contract must be
  checked against the written spec text branch-by-branch, not inferred by
  analogy from neighboring branches; a tie-break regression test must
  construct its fixture so the primary and fallback sort keys disagree,
  or the test passes vacuously even with the primary sort key removed.

## Outcomes

- PR #323 merged via merge commit `5fa949ad5f05f35690f44e9577ab8d2bb25fd7ae`
  (two parents, P-009 preserved).
- `123-S`: `active -> shipped -> archived` (`archived_status: shipped`).
- `115-F`: `active -> done -> archived` (`archived_status: done`), fully
  covered, zero unshipped siblings.
- 3 Copilot review comments (round 1) fixed/replied/GraphQL-resolved;
  round 2 clean (`SATISFIED`).
- Dark-mode bounded scope `[123-S]` complete — sole shipment, fully
  shipped/closed, no scope expansion.

## Provenance Chain

`docs/closure/123-S-115-F-post-merge-closure.md` (full detail) ->
`docs/archive/memory/2026-08-09-ship-123-S-115-F-session.md` (archived
verbose original) -> this compacted summary.

## Deferred

- Out-of-scope agent-template weaving of the `next_eligible` cursor into
  Orchestrator/Stage/Ship consultation flows, and the corresponding
  installed dogfood mirror refresh, remain deferred per `115-F`'s own
  description — re-triage only after the cursor proves stable in
  practice.
