---
compacted_from: docs/archive/memory/2026-08-08-ship-122-S-114-F-session.md
release_unit: 122-S / 114-F
date: 2026-08-08
---

# Compacted Memory — 122-S / 114-F (Capability-Pack Runtime Detection + Pre-Merge-Install Checklist)

**Source (archived verbose original)**:
`docs/archive/memory/2026-08-08-ship-122-S-114-F-session.md`

## Decisions

1. Local Review Gate performed directly (not delegated) — single-domain
   diff, modest size.
2. Fixed the sh exit-status-masking bug found by Copilot (not deferred as
   follow-up) — a real correctness bug in this shipment's own new
   detection logic.
3. Strengthened tests with fixture-driven, controlled-executable detection
   test classes for both ps1 and sh, rather than a narrow patch, since the
   root cause was "the test never exercised the reproducing shape."
4. Followed 121-S/119-S precedent exactly for fully-covered-feature
   closure: safe-close never touches the covering feature; `114-F` closed
   as a separate, subsequent step only after verifying zero `114.*`
   descendants remain queued.

## Files Modified

- `scripts/deploy-harness.sh`, `templates/scripts/deploy-harness.sh.tmpl` —
  exit-status masking fix.
- `tests/test_deploy_harness_scripts.py` — new fixture helpers + 2 new
  test classes.
- `.backlogit/` state — lifecycle commits.
- `scripts/deploy-harness.ps1`, `templates/scripts/deploy-harness.ps1.tmpl`,
  `docs/installation.md` — original feature implementation.

## Key Learnings (cross-reference)

- `docs/compound/2026-08-08-shell-pipeline-exit-status-masking-in-version-probes.md`
  — piping a version/health-check probe through a text filter and then
  discarding the pipeline's exit status with a trailing `|| true` throws
  away the probed command's own failure signal, even though `pipefail`
  correctly propagated it in the first place, because the filter stage
  still forwards whatever partial input it received regardless of the
  pipeline's ultimate exit status. Fix: capture raw output via plain
  command substitution first (no trailing `|| true` on the substitution
  itself), gate on that command's own exit status, then post-process only
  the already-captured, already-successful string.

## Outcomes

- PR #318 merged via merge commit `d923820e29473cb24e0c4c7d76070b4d811d55a5`
  (two parents, P-009 preserved).
- `122-S`: `active -> shipped -> archived` (`archived_status: shipped`).
- `114-F`: `active -> done -> archived` (`archived_status: done`), fully
  covered, zero unshipped siblings.
- 3 Copilot review comments (round 1) fixed/replied/GraphQL-resolved;
  round 2 clean.
- Dark-mode sequence `120-S -> 121-S -> 122-S` complete — all three
  shipped/closed.

## Provenance Chain

`docs/archive/closure/122-S-114-F-post-merge-closure.md` (full detail) ->
`docs/memory/2026-08-08-ship-122-S-114-F-session.md` (archived verbose
original) -> this compacted summary.

## Deferred

- Full native capability-pack runtime provisioning/install execution and
  supply-chain/OS-matrix design remain deferred per
  `docs/decisions/2026-08-07-capability-pack-runtime-installer-deliberation.md`
  Option C, tracked by the still-**ACTIVE** living tracker
  `.backlogit/stash.jsonl` entry `47971057` (`PARTIALLY-CONSUMED — LIVING
  TRACKER`) — unchanged by this shipment. `114-F`'s `"47971057"` label is
  provenance only, not the tracker itself; the actionable resumption
  pointer is the stash entry.
