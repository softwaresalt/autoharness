---
compacted_from: docs/archive/memory/2026-08-08-ship-121-S-113-F-session.md
release_unit: 121-S / 113-F
date: 2026-08-08
---

# Compacted: 121-S / 113-F Ship Closure

**Shipment 121-S / Feature 113-F** (F02FD596 nested per-role escalation
hierarchy + E8B5B3C5 session-start dynamic reload) — PR #316, merge commit
`db8630b6ce7b83bebf9a0006940fcccf01bf3ee0`.

**Session**: resumed after an operator-dismissed startup-checkpoint anomaly
(confirmed root cause: dirty-worktree repairs relocated into remainder stash
`ab16544a...` during 120-S isolation, exposing old malformed committed
snapshots as an apparent false-positive anomaly — not a new interrupted
session). Fixed 1 bounded, operator-authorized additional Copilot review
round (round 5 of PR #316's lifetime): a schema-versioning-discipline
violation where new nested-escalation schema properties were added to both
the root `harness-config.schema.json` AND the published `1.0.0.schema.json`
mirror without a version bump. Fixed by mirroring the tool-telemetry-event
v1.0->v1.1 precedent (commit `6da2f55b`): restored the old mirror, published
a new `1.1.0.schema.json` mirror, bumped `current_version`/`known_versions`,
no forced migration entry (purely additive).

**Decisions**: no `CONTRACT_MIGRATIONS` entry (would break an existing
`migration_proposals == []` test); dogfood config's own `schema_version`
bumped to 1.1.0 as a judgment call (flagged in closure); checksum recomputed
from LF-normalized git blob, not raw CRLF working-tree bytes; `gh api graphql
query=@file` requires `-F` not `-f` in this environment.

**Outcomes**: PR #316 merged (two-parent merge commit, P-009 preserved);
P-018 `SATISFIED`; local review `READY` at final HEAD `c355d378`; shipment
121-S safe-closed (`shipped` -> archived); feature 113-F fully covered by
this shipment alone, archived `done`; remainder stash `ab16544a...`
confirmed untouched throughout.

**Key learning** (full detail:
`docs/compound/2026-08-08-schema-mirror-mutated-in-place-without-version-bump.md`):
a schema-behavior change applied uniformly across "the schema files" for a
contract with published versioned mirrors silently redefines an
already-published version unless the old mirror is deliberately excluded and
the version bumped instead — second occurrence of this bug class; a
"mirror preserved unchanged" regression test should ship with the first
versioned contract, not be added retroactively.

**Provenance**: deliberation
`docs/decisions/2026-08-07-model-routing-hierarchy-dynamic-reload-deliberation.md`
-> plan/hardening/review (113.001-R, PASS) -> this shipment -> closure
`docs/archive/closure/121-S-113-F-post-merge-closure.md`.
