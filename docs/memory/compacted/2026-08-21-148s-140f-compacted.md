---
title: "Compacted: Ship 148-S resume, execution, and closure (docs/compound docline conformance)"
date: 2026-08-21
shipment: 148-S
feature: 140-F
pr: 387
merge_commit: 291dafd8cd5c1ff937c6499476161ae450fb2f0a
source: docs/archive/memory/2026-08-21-ship-148-s-resume-execution-and-closure-session.md
---

# Compacted: 148-S / 140-F

Resumed a transport-interrupted Ship session (both tasks already committed,
no checkpoint/PR yet). Diagnosed via zero-candidate checkpoint enumeration,
created a fresh resume checkpoint, committed the pending 140.002-T archival
move, then executed the full remaining lifecycle.

**Validation**: targeted tests 15/15, full suite (final, post-fix) 1693
passed / 20 skipped / 5 pre-existing `E8158860` failures (not a regression),
`docs lint` 0 violations.

**Local review**: `READY_WITH_FOLLOWUPS`; one P2 (pre-existing `source:` file
carrying provenance text, correctly untouched per AC3) captured as deferred
stash `FAE1E7B7`.

**PR #387, two Copilot review cycles**: cycle 1 (5 threads) fixed a
raw-regex-vs-YAML-semantic emptiness gap, a non-recursive corpus scan, a
vacuous new-template-variable check (required diffing pre-task git history
to derive correctly), a checkpoint schema fix (`resume_hint` is top-level,
not nested under `context`), and a PR-description accuracy correction
(migration approach was additive-only, not `--apply`, per the task's own
execution record). Cycle 2 (2 threads): declined resolving the checkpoint
pre-merge (operator's explicit crash-recoverability directive took
precedence), refreshed its content instead; fixed a stale-HEAD PR-body
reference via `gh pr edit` (no commit, no re-armed review cycle).

**Merge**: P-018 `SATISFIED`, P-014 gate passed, merge-commit strategy
verified, 2-parent merge commit confirmed at `291dafd8`.

**Closure**: P-015 classifier returned `CASCADE` (140-F verified
fully-covered root). `backlogit shipment ship` swept in an out-of-manifest
deliberation (`025-DL`, plain `references` link, not `parent_id`) --
recurrence of the known 143-S/134-F/019-DL surprise; reverted only that
artifact, re-verified all other post-conditions, recorded as a new
disposition section on the existing compound learning doc.

**Key learnings**: (1) a vacuous-check fix must exclude only what the diff
actually added, not the whole example block, or it over-corrects into false
positives; (2) `resume_hint` is a Checkpoint Payload Contract top-level
field; (3) `gh pr edit` body edits don't re-arm Copilot review; (4) the
cascade out-of-manifest-reference-link surprise is a recurring engine
behavior, not a one-off -- strengthens the case for a general Stage-owned
fix over a narrow one.

**Open follow-ups**: `FAE1E7B7` (Stage deliberation), `E8158860` (unrelated,
tracked separately), cascade reference-link hardening (Stage-owned).

Full detail: `docs/archive/memory/2026-08-21-ship-148-s-resume-execution-and-closure-session.md`.
Structured closure record: `docs/closure/148-S-140-F-post-merge-closure.md`.
