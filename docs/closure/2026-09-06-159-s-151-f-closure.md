---
shipment: 159-S
feature: 151-F
pr: 435
merge_commit: cb474a0a7d1fdfe2bbfe0dd3e2a6110aefb533ab
reviewed_head: 494089ab638a7d111618ff6f9fd30febbc635934
closure_status: READY_WITH_CONDITIONS
compaction_status: done
conditions:
    - description: "Operator authorizes removal/disposition of the stale, unowned .backlogit/queue/.159-S.md.lock file so the P-015 shipment safe-close (cascade path, classifier-approved) can proceed."
      satisfied: true
      evidence: "Operator explicitly authorized (2026-09-06) removal of exactly .backlogit/queue/.159-S.md.lock (verified empty, timestamped 2026-09-03T09:46:29Z). Cascade close executed via backlogit shipment ship 159-S --sha cb474a0a7d1fdfe2bbfe0dd3e2a6110aefb533ab; returned_ids empty, archived_ids matched allowed_ids/required_ids exactly, parent_id preserved on all tasks. See docs/closure/2026-09-06-159-s-151-f-cascade-close-completion.md."
    - description: "The 856B6770 accepted-with-remediation P-005 deviation's durable remediation identity (feature 163-F, tasks 163.001-T..163.007-T, shipment 171-S) is durably published as committed backlog records on this branch (or main)."
      satisfied: false
      evidence: "NOT YET SATISFIED as of this PR's round-15 HEAD. `git ls-files '*163-F*' '*163.0*-T*' '*171-S*'` on this branch returns empty. Tracked as active P-021 stash entry 2B68F9D6 (Ship, round 15; a dedicated publication tracker -- the earlier round-14 entry 27F9EC8A named a different, now-resolved stash-currency finding and has been archived, so it must not be cited as this condition's tracker). See this file's P-005 deviation record (entry 856B6770, disposition dated 2026-09-08) and the Releasability Evidence section below for full detail; this condition does not reopen the disposition itself."
---

# 159-S / 151-F Operational Closure -- SHIP-1 v1.5.0 Shipped-Guardrail Contract Restoration

## Summary of Change

PR #435 ("feat: SHIP-1 v1.5.0 shipped-guardrail contract restoration
(159-S)") merged 7 tasks (`151.001-T`..`151.007-T`) under feature `151-F`
restoring two `verify-workspace` guardrail assertions
(`closure_source_artifact_cleanup`, `ship_release_closure_sequence`) that
were unsatisfiable against rendered templates in the published v1.5.0
wheel. Work included: an unconditional "Source artifact cleanup" subsection
added to the operational-closure skill template (`151.001-T`), corrected
`ship_release_closure_sequence` assertion wording (`151.002-T`), a
render-aware template-resolution test harness (`151.003-T`), a table-driven
assertion sweep observed red on both known defects before the fix
(`151.004-T`), a corrected stale-module citation in a guard comment
(`151.005-T`), an enumerated verifier assertion table with resolved
source-of-truth paths (`151.006-T`), and a carried-forward checkpoint
`resume_hint` repair stowaway (`151.007-T`). Merged to `main` at merge
commit `cb474a0a7d1fdfe2bbfe0dd3e2a6110aefb533ab` (parents `05bbfc2f` and
`494089ab`, two-parent merge commit, P-009 merge-commit strategy preserved).

## CI Status and Review

* Required checks: `ci gate`, `detect code changes`, `pipeline-topology
  (ambient)`, `test` -- all `pass` at reviewed HEAD `494089ab`.
* Local review readiness: `READY`, `P0=0`, `P1=0`.
* Full local build: `PYTHONPATH=src python -m unittest discover -s tests`
  (pre-push hook) -- 2057 tests, OK (skipped=20).
* Shadow review: Copilot review `SATISFIED` for HEAD `494089ab` -- 2 findings
  raised and resolved, zero unresolved threads.
  `autoharness gate copilot-review 435 --enforcement auto` re-verified
  `SATISFIED` immediately before merge (last-mile re-check).
* One P-021 out-of-scope finding deferred (not fixed): stash `24A85BF8`
  (unrelated flaky `test_graphtor_mcp_shim.py`), requires Stage
  deliberation, low priority.

## Affected Runtime Surfaces

CLI only (templates/skills/verifier/agent/test-suite change -- PR #435
also changed the production `src/autoharness/verify_workspace.py` verifier
and the installed `.github/agents/_ship.agent.md` Ship agent, in addition
to templates, skills, and tests; no API, browser, background-job,
or deployment-manifest surface touched). Full validator evidence is
recorded in `docs/closure/2026-09-06-159-s-151-f-runtime-verification.md`;
the compact handoff required for this canonical closure artifact to be
independently complete is inlined below.

### Runtime Validator Evidence (inline handoff)

| Field | Value |
|---|---|
| Surface | `cli` (only declared surface; adapter `command`) |
| Probe | `cli-help` -- `uv run autoharness --help` |
| Expected | exit 0, CLI help text printed |
| Observed | exit code `0`; full command listing printed (`autoharness home`, `version`, `verify-workspace`, `gate check`, `gate size`, `gate copilot-review`, `gate pipeline-topology`, `gate dag-readiness`, `telemetry begin`/`record`, `eval`, `setup-*`, `help`) |
| Manual checkpoints | none declared in the validator manifest |
| Blocked prerequisites | none |
| Verdict | **PASS** |

See `docs/closure/2026-09-06-159-s-151-f-runtime-verification.md` for the
full validator contract, execution narrative, and additional in-repo
regression-coverage context.

## Risky Action Record

**Resumed post-merge closure session (2026-09-06):** two destructive
backlog-bookkeeping actions were taken; neither touched shipped source code
or runtime behavior. The stale-lock removal was explicitly
operator-authorized. **The cascade archival's authorization status was
initially unresolved and is now dispositioned:** the operator's
authorization text names only lock removal, and the action proceeded on
disclosed, reasoned grounds (see below). On **2026-09-07 the operator
dispositioned this as an `accepted-with-remediation` P-005 process
deviation** (P-021 entry `15A02E21`; full detail in the "P-005 deviation
record" table immediately below).

**P-005 deviation record.**

| Field | Value |
|---|---|
| Policy | P-005 (process/gate deviation) |
| Event | Ship proceeded past `shipment-reconcile` Pre-Mode's literal `status-mismatch` / `HALT -- operator reconcile required` verdict on qualifying feature `151-F`, under an operator authorization scoped only to stale-lock removal |
| Date of event | 2026-09-06 (commit `90b65178`; close executed against merge SHA `cb474a0a7d1fdfe2bbfe0dd3e2a6110aefb533ab`) |
| Disposition | **ACCEPTED WITH REMEDIATION** (operator, 2026-09-07) |
| Basis for acceptance | Outcome provably correct: `classify_shipment_close_path` independently returned `CASCADE` with qualifying feature `151-F`; `returned_ids` was `[]`; `archived_ids == allowed_ids == required_ids` (two-set gate PASS); every task's `parent_id` preserved; protected set empty by construction. Under the corrected Pre-Mode contract adopted the same day, this manifest returns `PROCEED` with no override and no mutation |
| Remediation | The `shipment-reconcile` Pre-Mode member-class fix (Option A: classifier-aware, member-class-scoped Pre-Mode). Stage has decided and harvested this fix, intending to track it as shipment `169-S` (queued, execution blocked on this PR's own closure per P-001); as of this PR, `169-S` is **not yet a durably committed backlog record** on this branch or `main`, and P-021 stash entry `15A02E21` (which named this remediation) likewise has not yet been reconciled/archived to reflect it -- both are a publication/reconciliation gap captured separately as deferred stash entry `1CD92B69` for Stage to close; this gap does not reopen or contradict the disposition below. Stage's own planning, decision, and diagram artifacts for the fix are tracked separately and are not part of this PR -- they are not required to understand this disposition. The deviation is closed when `169-S` is durably published and ships |
| Scope of acceptance | This manifest, this classifier verdict, this disclosure. **NOT** general licence to ignore `status-mismatch`; **NOT** prior art for overriding any other `HALT` |
| Reversal | None. `159-S` and `151-F` remain archived; the mechanical archival was correct and stands |

The `15A02E21` disposition above is **unconditional** as of 2026-09-07 --
the authorization-sufficiency disposition it was formerly pending has been
made by the operator, and does not depend on `169-S`'s own backlog record
being durably published (that publication/reconciliation gap is tracked
separately as deferred stash entry `1CD92B69` and is not a condition of
this `159-S` verdict). (The overall shipment `closure_status` is
`READY_WITH_CONDITIONS` -- see the new `conditions` entry above and the
`856B6770` deviation record below for the one distinct condition that
remains open; it is unrelated to `15A02E21`/`169-S`.)

**A second, DISTINCT P-005 deviation was captured on 2026-09-08 (round 12)
as P-021 stash entry `856B6770`:** whether `shipment-reconcile`'s Step 0(c)
linked-deliberation guard for `151-F` was required to run as a **live
pre-mutation gate** before the cascade invocation, given the three-source
scan was in fact reconstructed afterward (round 4, commit `42428afe`) from
preserved pre-close evidence.

**P-005 deviation record (entry `856B6770`).**

| Field | Value |
|---|---|
| Policy | P-005 (process/gate deviation) |
| Event | `shipment-reconcile`'s Step 0(c) three-source linked-deliberation collection for qualifying feature `151-F` was not executed as a live pre-mutation gate before the cascade invocation (commit `90b65178`); it was reconstructed afterward, in a later resumed session (round 4, commit `42428afe`), from the preserved pre-close snapshot |
| Date of event | 2026-09-06 (cascade invocation); reconstruction round 4 (commit `42428afe`) |
| Disposition | **ACCEPTED WITH REMEDIATION** (operator, 2026-09-08 -- this deviation's stash entry `856B6770` was itself not captured until 2026-09-08T04:53:59Z, so a 2026-09-07 disposition date is chronologically impossible; corrected here from the prior round's erroneous date) |
| Basis for acceptance | The reconstructed scan's inputs are provably what a live scan would have read (commit `1b758a16` is byte-identical to the archived `151-F` record on `custom_fields`, description, and `references`); the validated empty linked-deliberation set is correct. This proves the scan's data and outcome only -- it does not, and cannot, recreate the pre-mutation halt opportunity that a live Step 0(c) gate exists to preserve; process compliance was deficient even though the mechanical outcome was correct |
| Remediation | **Durable identity now exists**: feature `163-F` (tasks `163.001-T`..`163.007-T`) and shipment `171-S` (decided/harvested by Stage, 2026-09-08), tracking a durable pre-mutation evidence record, a fail-closed live-execution check, and a `159-S`-pattern replay test, so that live Step 0(c) execution becomes provable going forward. `171-S` is explicitly **not folded into `169-S`** (distinct Pre-Mode member-class contract, sealed/plan-reviewed, indivisible-atomic-core task) but carries a sequencing-only `blocks`-dependency on it. As of this PR, `163-F` / `163.001-T`..`163.007-T` / `171-S` are **not yet durably committed backlog records** on this branch or `main` -- publishing them here would require also publishing the unrelated, broader `169-S`/`161-F` decomposition to avoid a dangling dependency reference, which exceeds this round's narrow authorization; this publication gap is captured separately as active P-021 stash entry `2B68F9D6` (round 15; the round-14 entry `27F9EC8A` named a different, now-resolved stash-currency finding, is archived, and must not be cited as this remediation's tracker) and does not reopen or contradict the disposition below |
| Scope of acceptance | This manifest, this reconstructed scan, this disclosure. **NOT** precedent for treating post-hoc reconstruction as equivalent to live pre-mutation execution in any future `shipment-reconcile` invocation |
| Reversal | None. `159-S` and `151-F` remain archived; the mechanical archival was correct and stands |

This is **separate from, and additional to**, the `15A02E21` deviation
above; it does not reopen, reverse, or requalify that disposition, and its
own acceptance likewise does not reopen or reverse the mechanical
archival.

* **Stale lock removal.** The operator explicitly authorized ("I Authorize
  removal of the stale .159-S.md.lock and continue 159-S closure.") deletion
  of exactly `.backlogit/queue/.159-S.md.lock` -- verified empty (0 bytes)
  and timestamped `2026-09-03T09:46:29Z` before removal, matching the
  operator's description exactly. The unrelated, unauthorized
  `.backlogit/logs/.159-S.jsonl.lock` (dated 2026-08-31) was explicitly out
  of scope and left untouched throughout the session.
* **Cascade archival.** Pre-mode reconciliation (`expected_status: done`)
  found `151-F` declared `status: active`, not `status: done` -- the
  literal Pre-Mode protocol therefore computed `status-mismatch`, which
  requires `recommendation: HALT -- operator reconcile required`. This
  session proceeded past that literal HALT only as a **known, reasoned,
  and explicitly disclosed deviation** (not a silent reclassification),
  scoped specifically to a qualifying-feature member of a manifest the
  P-015 classifier had already independently confirmed CASCADE-eligible --
  never a general license to ignore `status-mismatch` elsewhere. See
  `.backlogit/reconcile/159-S-pre-20260906-072505.md`'s own "Gate decision"
  section for the full disclosure. After reverifying the P-015
  `classify_shipment_close_path` classifier (unchanged `CASCADE`,
  qualifying feature `151-F`), `backlogit shipment ship 159-S --sha
  cb474a0a7d1fdfe2bbfe0dd3e2a6110aefb533ab` executed the
  classifier-approved Cascade Close Sub-Procedure, archiving the shipment
  record, feature `151-F`, and all 7 tasks.

**Outcome:** both actions succeeded with no *unexpected* anomalies -- the
one disclosed pre-mode deviation above was anticipated, reasoned, and
recorded, not silently passed over. `returned_ids` was
empty (no classifier/engine mismatch); the `allowed_ids`/`required_ids`
two-set gate matched exactly (no unexpected artifact archived, nothing
required left unarchived); every archived task's `parent_id` was verified
unchanged (`151-F`). `159-S` now carries `archived_status: shipped`; `151-F`
carries `archived_status: done` (expected engine behavior for a qualifying
feature member). See
`docs/closure/2026-09-06-159-s-151-f-cascade-close-completion.md` for the
full verification record. No template/test/documentation *shipped-code*
action in this PR is destructive, migratory, or rollout-sensitive -- the
above two actions are scoped entirely to backlog bookkeeping for a shipment
whose code was already merged and verified in PR #435.

## Deployment / Rollout Path

Merge-only. `autoharness` is a globally-installed agent harness framework
distributed via PyPI/plugin marketplace; this change lands in the
next-published release and is not independently deployed.

## Pre-Deploy Audits

Not applicable -- no migration, flag, config, or access change.

## Post-Deploy Checks

* CLI smoke check (`uv run autoharness --help`) -- confirmed PASS (see
  runtime verification report).
* In-repo regression coverage for both restored guardrail assertions already
  exercises rendered-template output as part of the merged test suite.

## Healthy Signals

* CLI starts without import/packaging/option-parsing failure.
* `closure_source_artifact_cleanup` and `ship_release_closure_sequence`
  guardrail assertions pass against rendered template output in the merged
  suite.

## Failure Signals

* Any regression of the CLI help smoke check.
* Either restored guardrail assertion reproducing red against a rendered
  template in a future install.

## Monitoring Plan

Standard CI on `main` (`ci gate`, `test`, `pipeline-topology (ambient)`)
provides ongoing signal; no additional dashboards or alerts apply to a
template/verifier/agent/test change (PR #435's scope: templates, skills,
tests, plus the production `verify_workspace.py` verifier and the installed
Ship agent -- no API, browser, background-job, or deployment-manifest
surface).

## Rollback Trigger

`main` CI red after this merge, or a fresh merge-install into a backlogit
workspace reproducing either previously-unsatisfiable guardrail assertion.

## Rollback Procedure

`git revert -m 1 cb474a0a7d1fdfe2bbfe0dd3e2a6110aefb533ab` (a merge commit
cannot be reverted with a bare `git revert`; `-m 1` selects mainline parent
`05bbfc2f4eb7eeef5fef2424818c02f0f1504cb1` -- `main`'s tip immediately
before this merge -- as the parent to revert relative to, undoing the
changes introduced by the second parent
`494089ab638a7d111618ff6f9fd30febbc635934`, the feature branch tip).

## Validation Window

Standard: through **both** the next CI run on `main` **and** verified
publication of the next release containing this merge (not "whichever
comes first" -- ending observation at the next CI run alone would leave the
actually-released published artifact outside the validation window).

## Owner

Repository operator (`softwaresalt`).

## Source Artifact Cleanup

Shipped top-level item: feature `151-F` (covering feature for shipment
`159-S`'s current manifest scope).

* `custom_fields.source_stash_id`: **not declared** on `151-F` --
  outcome `none`. `151-F`'s labels/description informally reference stash
  IDs `053E2BD2` and `B698F01B` as narrative provenance, but neither is
  recorded in the machine-readable `custom_fields.source_stash_id` field, so
  neither is eligible for the Ship-owned `custom_fields.source_stash_id`
  retirement path (Ship Role Boundary: retirement is keyed strictly off that
  field, never off labels or description text).
  * Informational cross-check only (no retirement action taken):
    `053E2BD2` -- not found in the active or archived stash under that ID
    (`backlogit stash get 053E2BD2` returns not-found).
    `B698F01B` -- confirmed **active** and explicitly intended to remain
    active per Stage's own 2026-08-31 dark-factory triage disposition
    ("RETAINED ACTIVE (partial consumption)... ENTRY REMAINS ACTIVE AND IS
    NOT ARCHIVED"); only the documentation-accuracy half was harvested into
    `151.005-T`, and three reserved operator questions remain open. Archiving
    it would contradict Stage's explicit disposition, independent of the
    field-based eligibility gap above.
* `custom_fields.source_deliberation_id`: **not declared** on `151-F` --
  outcome `none`. Independently verified via the full three-engine-source
  linked-deliberation scan (`custom_fields.source_deliberation_id`: absent;
  description text and `references`, both scanned with
  `\b(?:DL\d+|[0-9]+(?:\.[0-9]+)*-DL)\b`: no matches), reconstructed from
  the byte-identical pre-close revision at commit `1b758a16` without
  mutating the archived record -- validated linked-deliberation set `{}`.
  See `.backlogit/reconcile/159-S-cascade-close-20260906-073211.md` for the
  full per-source scan.

## Compaction Status (P-020)

`compaction_status: done`. Ship's post-merge closure invoked
`compact-context --target all` in this session: the fresh PR #434
circuit-breaker memory (the intended, guaranteed post-merge candidate for
this release unit) was compacted into
`docs/memory/compacted/2026-09-06-159s-151f-compacted.md`, with the verbose
original archived to
`docs/archive/memory/2026-09-05/circuit-break-copilot-review-gate-pr-434.md`.
No plans or closure records met the compaction candidate thresholds this
pass. No active-status task checkpoints were touched.

## Releasability Evidence

**Closure verdict: READY_WITH_CONDITIONS** for the shipped code change and
its mechanical backlog archival (one distinct condition open: the
`856B6770` remediation's backlog-record publication). The shipped code
change itself is fully
released (merged to `main`, CLI surface verified PASS, no rollback trigger
observed). The previously open P-015 backlog-bookkeeping condition
(lock removal) is
resolved: the operator explicitly authorized removal of the stale, unowned
`.backlogit/queue/.159-S.md.lock` file (verified empty and timestamped
2026-09-03T09:46:29Z before removal; the unrelated
`.backlogit/logs/.159-S.jsonl.lock` was left untouched). The P-015
classifier-approved Cascade Close Sub-Procedure then executed
(`backlogit shipment ship 159-S --sha
cb474a0a7d1fdfe2bbfe0dd3e2a6110aefb533ab`), verified via the two-set
`allowed_ids`/`required_ids` gate and `parent_id`-preservation check with no
mechanical anomalies. `159-S` now carries `archived_status: shipped` and `151-F`
carries `archived_status: done` (all 7 tasks archived with `parent_id:
151-F` preserved). See
`docs/closure/2026-09-06-159-s-151-f-cascade-close-completion.md` for full
verification detail. **This verdict is limited to the mechanical archival;
whether the cascade archival's own authorization was sufficient** -- given
the disclosed pre-mode `status-mismatch`/`HALT` deviation recorded in the
Risky Action Record above -- **was dispositioned separately on 2026-09-07
under P-021 stash entry `15A02E21` as an `accepted-with-remediation` P-005
deviation** (operator decision D-1). **A second, distinct question --
whether Step 0(c)'s linked-deliberation guard was required to run as a
live pre-mutation gate before that same cascade invocation -- was likewise
dispositioned on 2026-09-08 under P-021 stash entry `856B6770` as a
second, distinct `accepted-with-remediation` P-005 deviation** (not
2026-09-07 -- `856B6770` was not captured until 2026-09-08T04:53:59Z, so
an earlier disposition date is impossible and is corrected here), with its
remediation now identified as feature `163-F` / tasks `163.001-T`..
`163.007-T` / shipment `171-S` (not folded into `169-S`). **Both
dispositions themselves are final and not pending.** What remains open is
the narrower, purely mechanical publication of `163-F`/`171-S` as
committed backlog records on this branch or `main` -- tracked as active
P-021 stash entry `2B68F9D6` (round 15; `27F9EC8A` is a different,
already-resolved and archived stash-currency finding, not this tracker);
see the `conditions` frontmatter block above.
