---
shipment: 162-S
feature: 154-F
pr: 446
merge_commit: 1b2312273c1570c7557d367429c24e7e8905c0de
last_code_affecting_head: a8ae3bafb74703ea820818869f51bcd1ab5748e3
closure_status: READY
compaction_status: done
conditions:
    - description: "Explicit operator merge approval for PR #446. Dark-mode activation record for shipment 162-S declares merge_approval_pre_authorized=false and admin_fallback_pre_authorized=false, so neither dark-mode auto-merge nor admin fallback may substitute for an explicit operator approval signal."
      satisfied: true
      evidence: "Operator explicitly authorized dark-mode implicit merge approval for the bounded 162-S scope ('Merge approval is implicit with dark factory mode'), setting merge_approval_pre_authorized=true while admin_fallback_pre_authorized remained false and all other gates remained required. At the point of merge, HEAD `361f07a0b2d0527df4a63e40efe90f6817196796` carried: local review readiness READY_WITH_FOLLOWUPS with P0=0/P1=0; P-018 `autoharness gate copilot-review 446 --repo softwaresalt/autoharness --enforcement auto` returned SATISFIED (zero unresolved Copilot threads, 9 review rounds all resolved); all 4 required CI checks green (`test`, `ci gate`, `pipeline-topology (ambient)`, `detect code changes`); P-009 confirmed (`allow_merge_commit: true`, squash/rebase both disabled); pipeline-topology lifecycle gate passed. PR #446 was merged via `gh pr merge 446 --merge`, producing two-parent merge commit `1b2312273c1570c7557d367429c24e7e8905c0de` (parents `416046d2` and reviewed HEAD `361f07a0`), confirmed present in `origin/main` via `git merge-base --is-ancestor`. No admin fallback was used or authorized. Post-merge: shipment 162-S safe-closed via the verified fully-covered-root cascade exception (archived_ids exactly matched the computed allowed_ids/required_ids set, zero returned_ids); `compact-context --target all` invoked per P-020, compacting this session's memory directly (docs/memory/compacted/2026-09-12-ship-162-s-full-pipeline-and-merge-compacted.md); compaction_status finalized to done."
---

# 162-S / 154-F Operational Closure (finalized, post-merge) -- SHIP-4 Review-Persona, Policy, and Agent-Architecture Contract Integrity

## Evidence Currency Model

Per the same reasoning documented in
`docs/closure/160-S-152-F-post-merge-closure.md`: this artifact distinguishes
**code-affecting commits** (`last_code_affecting_head`, refreshed only when
a commit changes template/policy/instruction/skill/test content) from the
PR's **live, ever-advancing HEAD**. A committed file cannot truthfully
assert it covers the commit that introduces it, and repeatedly rewriting a
"current HEAD" claim after each new commit only produces a claim that is
stale the instant it is written. Live, merge-time-authoritative state
(current `headRefOid`, local review readiness, the P-018
`autoharness gate copilot-review` verdict, and CI check status) is tracked
externally in the PR body's `## Local Review Readiness` block and MUST be
re-verified directly against GitHub immediately before merge -- it is never
asserted as a permanently-fixed fact in this file.

## Summary of Change

PR #446 ("SHIP-4: review-persona, policy, and agent-architecture contract
integrity (162-S)") closes 4 tasks (`154.001-T`..`154.004-T`) under feature
`154-F`. Five defects are fixed: (1) the security-reviewer persona's
purpose-based finding-suppression rule, replaced with a report-first,
classify-scope-afterwards discipline naming P-021 as the disposition channel
(`154.001-T`, BA035180); (2) P-007's ungated `git restore` archive-integrity
remediation, replaced with Decisions G1-G9 requiring a fresh, live,
non-synthesizable operator approval, plus completion of the
constitution-reviewer's principle checklist (`154.002-T`, C0EA1175 /
701073F9); (3) a leaf-executor/skill-spawning architecture contradiction
between `harness-architecture.instructions.md` /
`role-enforcement.instructions.md` and the `review`/`plan-review` skills'
declared one-hop persona spawning, resolved with a Bounded One-Hop
Review-Family Exception and a static document-layer verifier
(`154.003-T`, 7628C291); (4) the technology-reviewer persona's dangling
reference to an absent `python.instructions.md`, closed via Decision F's
graceful-reference fallback (`154.004-T`, F0ADCC03).

During Ship's own local adversarial review (before any hosted review), a
self-found P1 identified that the same P-007 defect this shipment fixes was
independently restated, unfixed, in two more carriers not in 154.002-T's
original file list: `templates/agents/_ship.agent.md.tmpl` (the Ship agent's
own Closure Tasks steps) and `.github/skills/shipment-reconcile/SKILL.md`
(+ template). Both were brought into the same G1-gated contract in the same
review-fix cycle. Several further rounds of hosted Copilot review found and
were remediated for: (a) a prose error attributing a retained template
placeholder to the wrong artifact; (b) a document-layer detector accepting
an unanchored depth token; (c) a non-hermetic test that could shell out to
git/gh; (d) a P-007 recovery procedure that restored/staged an entire
directory instead of only the approved paths; (e) the same detector
accepting a negated depth declaration; (f) a test that verified only the
first occurrence of an instruction, not every occurrence. All were fixed in
the same cycle they were found. **No source code under `src/` was touched.**

Four out-of-scope findings surfaced during this work were captured as P-021
deferred stash entries rather than fixed in-cycle: `4C0126A5` (P-015's own
cascade-revert steps also lack an approval gate -- a different policy
contract), `CEB6555A` (the new verifier's vocabulary gap for
`verify-harness`'s "dispatch" wording), `206ED296` (further hardening
opportunity for one test's negative-coverage assertion), and `CE6749C9`
(the operator-authorized but out-of-scope `scripts/check_eraser_diagrams.py`
carried on this branch, never committed).

## Invariants to Preserve

* Every template/installed-mirror pair this shipment touches must remain in
  agreement after variable resolution (asserted by dedicated contract tests
  for each pair).
* No `{{VARIABLE}}` placeholder may remain unresolved in any installed
  mirror.
* No skill's runtime behavior may change as a side effect of the
  architecture-rule amendment (154.003-T's binding scope forbids this).
* `.github/instructions/python.instructions.md` must remain absent in this
  repository (F2 closes the dangling reference without creating it).
* The P-007 archive-integrity remediation, in every carrier that restates
  it, must require a live G1 approval before any `git restore`/`git add`,
  scoped only to the exact detected/approved paths -- never an unconditional
  or directory-wide operation.

## Validator Evidence

See `docs/closure/2026-09-11-162-s-154-f-runtime-verification.md` for the
full structured probe/build/CI evidence anchored to `last_code_affecting_head`.
Summary: CLI surface unaffected and confirmed working (`uv run autoharness
--help`, exit 0); full test suite green at `last_code_affecting_head`
(2190 tests, `OK (skipped=51)`); `verify-workspace` reports 0 blockers/0
warnings across every edit in this shipment; CI green through
`last_code_affecting_head`; all three new contract test files demonstrated
RED before GREEN per binding H7. Live current-HEAD CI/gate state is tracked
externally per the Evidence Currency Model above.

## Releasability Evidence

* `runtime_validation.releasability.required`: `false` for this workspace
  profile (per `.autoharness/workspace-profile.yaml`).
* No monitoring, rollback, or observation-window requirements apply: this is
  a documentation/policy/instruction/skill/test content change with no
  runtime code path affected.
* Verdict: **READY** (content-change releasability; no runtime rollout
  risk).

## Source Artifact Cleanup

No `custom_fields.source_stash_id` or `custom_fields.source_deliberation_id`
is declared on feature `154-F` or shipment `162-S` (checked via
`backlogit get`); this section records `none` for both, consistent with the
absence of such fields on these artifacts.

## Follow-Up Items

* `4C0126A5`, `CEB6555A`, `206ED296`, `72676271` -- P-021 deferred stash
  entries, require Stage deliberation (see PR body for full detail).
  `72676271` is high-priority: the installed dogfood Ship agent
  (`.github/agents/_ship.agent.md`) never invokes `shipment-reconcile`'s
  `mode: post`, so this repository's own Ship workflow never runs the P-007
  deleted-file guard regardless of this shipment's fix.
* `CE6749C9` -- `scripts/check_eraser_diagrams.py`, an operator-authorized
  but out-of-scope carried file, classified out of 162-S's contract surface
  and never committed by this PR; requires Stage deliberation on whether/how
  to formally capture it as a future work item.
* `9B5FD7D5` (pre-existing, referenced by 154.004-T) -- recomposing this
  dogfood workspace under Decision F1 remains deferred.

## Compaction Status (P-020)

`pending` -- finalized to `done`/`degraded` at Ship post-merge closure after
`compact-context` runs.

## Merge Readiness Gate Status (finalized, historical)

The merge has already happened (see the `conditions` entry in the frontmatter
above for the full evidence trail). This section is retained as a historical
record of the gate state actually observed at merge time and is **not** a
live re-checkable claim:

* Local review readiness: `READY_WITH_FOLLOWUPS`, P0=0/P1=0, at HEAD `361f07a0`.
* P-018: `SATISFIED`, zero unresolved Copilot threads, at HEAD `361f07a0` (9 review rounds, all resolved).
* CI: all 4 required checks green (`test`, `ci gate`, `pipeline-topology (ambient)`, `detect code changes`) at HEAD `361f07a0`.
* P-009 merge-commit-only strategy: confirmed (`allow_merge_commit: true`, squash/rebase both disabled).
* Explicit operator approval: granted (dark-mode implicit merge approval, bounded to 162-S). Merge commit `1b2312273c1570c7557d367429c24e7e8905c0de`.
