---
shipment: 160-S
feature: 152-F
pr: 439
merge_commit: null
reviewed_head: 373a24ea777bd19aa11c54fe655c99b34c8bb8e2
closure_status: READY_WITH_CONDITIONS
compaction_status: pending
conditions:
    - description: "Explicit operator merge approval for PR #439. Dark-mode activation record for shipment 160-S declares merge_approval_pre_authorized=false and admin_fallback_pre_authorized=false, so neither dark-mode auto-merge nor admin fallback may substitute for an explicit operator approval signal."
      satisfied: false
      evidence: "Not yet requested/granted as of this closure artifact's creation (session halts here per the operator brief). PR #439 is READY_WITH_FOLLOWUPS pending only this approval: all required CI checks pass, P-018 Copilot-review gate is SATISFIED at reviewed HEAD, P-009 merge-commit strategy is available (not squash/rebase-only, see Pre-Deploy Audits), and zero unresolved P0/P1 findings remain."
---

# 160-S / 152-F Operational Closure (pre-merge) -- SHIP-2 Release and CI Pipeline Fail-Closed Gates

## Summary of Change

PR #439 ("SHIP-2: Release and CI pipeline fail-closed gates (160-S)") closes
3 tasks (`152.001-T`, `152.002-T`, `152.003-T`) under feature `152-F`. The
PyPI pre-publish probe used by `.github/workflows/release.yml` previously
treated any HTTP 2xx response as proof the target version was already
published, with no host, body, or version validation -- an unattended re-tag
could silently smoke-test and release around pre-existing artifacts. This
shipment extracts the inline heredoc probe into an importable, hermetically
testable module (`build_support/pypi_probe.py`, `152.002-T`), hardens it to
validate the resolved response host, decode/validate the JSON body, and
require an exact version match, changing the CLI's already-published outcome
from a silent exit 0 to a fail-closed exit 2 with an explicit remedy message
(`152.001-T`), and adds an end-to-end push-context test plus an AST-based
static guard to `tests/test_gates_topology.py` for the unrelated
`GITHUB_HEAD_REF` ambient-empty-string convention (`152.003-T`). This
artifact records **pre-merge** readiness: PR #439 is open against `main`
(not yet merged); merge is gated on explicit operator approval per the
governing dark-mode activation record (`merge_approval_pre_authorized=false`).

## Invariants to Preserve

* The probe reports `PRESENT` (and the CLI exits 2) only when the response's
  resolved host is `pypi.org` (after redirects, including on the 404 path),
  the body decodes as JSON, and the decoded body names the exact requested
  version. Any other well-formed-but-wrong-identity response is a transport
  or integrity error, never silent success.
* `skip-existing: true` remains on the publish step as declared defense in
  depth; it is not the primary fail-closed mechanism.
* No change to the pinned publish action SHA, the release workflow's
  trigger, permissions, or secret handling (binding H3).
* Every `patched_environ(GITHUB_HEAD_REF=...)` call site in
  `tests/test_gates_topology.py` remains preceded by
  `_clear_ambient_github_head_ref()` (enforced by the AST guard added in
  `152.003-T`, now also covering `except`/`case` blocks after review-fix
  `2686f786`).

## Runtime Validator Evidence (inline handoff)

| Field | Value |
|---|---|
| Surface | `cli` (only declared surface; adapter `command`) |
| Probe | `cli-help` -- `uv run autoharness --help` |
| Expected | exit 0, CLI help text printed |
| Observed | exit code `0`; CLI help text printed |
| Manual checkpoints | none declared in the validator manifest |
| Blocked prerequisites | none |
| Verdict | **PASS** |

Full validator contract, execution narrative, and rationale for why the
`autoharness` CLI package is unaffected by construction (this shipment
touches only `build_support/` -- imported solely by the release workflow's
probe step, never by the installed package -- and `tests/`) are recorded in
`docs/closure/2026-09-08-160-s-152-f-runtime-verification.md`.

## CI Status and Review

* Required checks at reviewed HEAD `373a24ea`: `ci gate`, `detect code
  changes`, `pipeline-topology (ambient)`, `test` -- all `pass`.
* Local review readiness: `READY_WITH_FOLLOWUPS`, `P0=0`, `P1=0`.
* Full local build: `uv build` (sdist + wheel; `build_support/**` verified
  present in sdist, absent from wheel) and `PYTHONPATH=src python -m
  unittest discover -s tests` -- 2087 tests, 0 failures, 20 skipped
  (re-run at reviewed HEAD `373a24ea`; also enforced by the repo's pre-push
  hook, which additionally runs `markdownlint '**/*.md'`).
* Shadow review: Copilot review completed a fifth round at HEAD `373a24ea`
  (this closure artifact and its companion runtime-verification artifact
  were themselves the subject: both had gone stale relative to `373a24ea`,
  citing the round-3 reviewed HEAD `0af4644a` and a pre-round-4 test count).
  Both findings were in-scope (this session's own readiness-record accuracy,
  not a code-behavior change) and fixed directly by this update -- no new
  P-021 deferred entry was required. `autoharness gate copilot-review 439
  --enforcement auto` returns `SATISFIED` once both threads are resolved
  following this update.
* Four P-021 out-of-scope findings deferred (not fixed) this session:
  `364681C4` (release.yml concurrency race), `D25080A3` (AST guard
  nested-call detection gap), `65402F31` (pre-existing `__main__` guard
  misplacement in `tests/test_gates_topology.py`), plus reuse (not a new
  entry) of `24A85BF8` (unrelated flaky `test_graphtor_mcp_shim.py` test,
  originally captured under shipment 159-S). All four require Stage
  deliberation; none block this shipment's release-blocker condition (the
  CLI help smoke check).

## Pre-Deploy Audits

* No migrations, feature flags, config, or access changes.
* Merge strategy: repository is configured with "Allow merge commits"
  available (P-009 requires a merge commit, not squash/rebase, at actual
  merge time -- this will be verified again immediately before merge).
* No secrets, permissions, or trigger changes to any workflow (binding H3).

## Deployment / Rollout Path

Merge-only. This is a repository/tooling change (release workflow +
non-runtime build helper + tests), not an application deploy. The next real
"rollout" of this change is the *next* tag-triggered release run after
merge, which will exercise the new `python -m build_support.pypi_probe`
step for the first time in production. There is no canary or phased rollout
mechanism for a GitHub Actions workflow step; the first live exercise of
this code path is the next release tag push.

## Post-Deploy Checks

* First release run after merge: confirm the "Check PyPI pre-publish
  state" step invokes `python -m build_support.pypi_probe` (not the old
  heredoc), exits 0 for a genuinely new version, and that the run proceeds
  to publish and smoke-test as before.
* If a release is ever re-tagged for an already-published version, confirm
  the step now exits 2 with the R1/R2/R3 remedy message on stderr and the
  job fails, instead of silently succeeding.

## Risky Action Record

None. No `ProposedAction`/`ActionRisk` entries were required -- all actions
taken this session (branch creation, task claim, commits, push, PR
creation, review-thread replies/resolutions, stash captures) are within
Ship's ordinary role boundary and did not require elevated approval. The
one genuinely irreversible-adjacent surface touched (`.github/workflows/release.yml`,
the release/publish path) was edited under `152.001-T`'s own declared
`careful` + `freeze-scope` safety mode (bounded to the pre-publish probe
step only, per binding H3), verified via the full test suite and a manual
`uv build`/sdist-wheel content diff rather than a live release run.

## Healthy Signals

* Next real release run: probe step exits 0 for the new version, publish
  and smoke-test steps proceed and succeed as before.
* No change in release cadence or manual intervention required for a
  normal (non-duplicate) release.

## Failure Signals

* A release run for a genuinely new version incorrectly exits 2 (probe
  false-positive) -- would indicate a regression in host/body/version
  validation logic.
* A re-tagged already-published version incorrectly exits 0 and proceeds to
  publish/smoke-test/release (probe false-negative) -- would indicate the
  fail-closed contract regressed; this is exactly the defect class this
  shipment closes, so any recurrence is a high-priority regression.

## Monitoring Plan

Passive: observe the next 1-2 real release workflow runs (tag pushes) after
merge for the probe step's behavior (see Post-Deploy Checks). No dashboards,
alerts, or additional logging were added or required -- the workflow's own
GitHub Actions run log is sufficient evidence, matching this repo's existing
release-observability posture (no `release-observability` capability pack
installed).

## Rollback Trigger

Any observed probe false-positive/false-negative in a real release run (see
Failure Signals), or any full-suite regression discovered after merge that
was not caught by CI.

## Rollback Procedure

Revert the merge commit on `main` (two-parent merge commit per P-009,
straightforward `git revert -m 1`), which restores the prior inline heredoc
probe behavior. No data migration, external state, or irreversible side
effect is introduced by this change, so a plain revert is sufficient
mitigation.

## Validation Window

Through the next real tag-triggered release run after merge (release
cadence for this repository is irregular/manual; there is no fixed SLA
window). No time-boxed monitoring window beyond that first live exercise is
required given the change is fully covered by hermetic, deterministic tests.

## Owner

The Ship agent/operator pairing that approves and merges this PR is the
owner of first-release-run observation, consistent with this shipment's
change being tooling/process rather than a user-facing runtime surface.

## Source Artifact Cleanup

Feature `152-F`'s covering backlog record does not populate the structured
`custom_fields.source_stash_id` or `custom_fields.source_deliberation_id`
fields (verified via `backlogit get 152-F`: `custom_fields:
map[harness_status:pending]`) -- `none` for both fields under this
convention; no Ship-side retirement action against those fields is
applicable. Provenance is instead recorded via labels (`8E10B13B`,
`E738A7D1`) and description prose ("Source stash 8E10B13B, E738A7D1") on
`152-F`, both already archived by Stage's dark-factory harvest on
2026-08-31 (visible in `.backlogit/archive/stash.jsonl`, each entry's own
"STAGE DARK-FACTORY CONSUMPTION" append-only footer) prior to this Ship
session -- no further retirement action was required or taken here.

## Compaction Status (P-020)

`pending`. This PR has not yet merged; `compact-context` is invoked as part
of post-merge closure (Step 5 of the Ship pipeline), not before. This field
will be finalized to `done` or `degraded` once merge occurs and post-merge
closure runs.

## Releasability Evidence

Per `.autoharness/workspace-profile.yaml`, `runtime_validation.releasability.required`
is `false` for this workspace -- no additional required evidence beyond the
runtime validator evidence above is declared. Given that, and the single
outstanding condition (explicit operator merge approval, not yet granted),
this closure artifact's overall status is:

**READY_WITH_CONDITIONS** -- all gates that can be satisfied autonomously
(CI, P-018 Copilot-review, P-014 local readiness, runtime validator PASS)
are satisfied at reviewed HEAD `0af4644a`; the sole remaining condition is
explicit operator approval to merge, which this session does not have
pre-authorization to substitute for or bypass.
