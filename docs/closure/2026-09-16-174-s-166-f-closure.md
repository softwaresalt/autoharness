---
shipment: 174-S
feature: 166-F
pr: 454
merge_commit: null
last_code_affecting_head: f469d28b182d8b81cb0b53769404ab8635451079
closure_status: READY_WITH_CONDITIONS
compaction_status: pending
conditions:
    - description: "Explicit operator merge approval for PR #454. No blanket merge or admin-fallback authority has been given for this shipment; the operator must review the PR readiness summary and give an explicit merge signal before any merge is attempted."
      satisfied: false
      evidence: "Not yet requested/received as of this artifact's writing (pre-merge snapshot)."
---

# 174-S / 166-F Operational Closure (pre-merge) -- Flat-Manifest Shipment Closure

## Evidence Currency Model

Per the same reasoning documented in prior closure artifacts (e.g.
`docs/closure/2026-09-14-173-s-165-f-closure.md`): this artifact
distinguishes **code-affecting commits** (`last_code_affecting_head`) from
the PR's **live, ever-advancing HEAD**. Live, merge-time-authoritative state
(current `headRefOid`, local review readiness, the P-018
`autoharness gate copilot-review` verdict, and CI check status) is tracked
externally in the PR body's `## Local Review Readiness` block and MUST be
re-verified directly against GitHub immediately before merge -- it is never
asserted as a permanently-fixed fact in this file.

## Summary of Change

PR #454 ships feature `166-F` (6 tasks, `166.001-T`..`166.006-T`) for
shipment `174-S`: rewrites P-015 from the withdrawn hierarchical-descendant-
coverage closure premise to a flat-manifest, engine-inertness shipment-closure
model, per the operator architectural correction recorded in
`docs/decisions/2026-09-15-flat-manifest-shipment-closure-deliberation.md`.
`src/autoharness/gates/shipment_closure.py`'s `classify_shipment_close_path`
classifier now judges a qualifying root feature's coverage against its
declared manifest membership plus an exact-parsed-scalar inertness predicate
over each out-of-manifest descendant, `.github/policies/workflow-policies.md`
and `.github/skills/shipment-reconcile/SKILL.md` (plus their `.tmpl`
mirrors) were rewritten to match, and a retrospective report documents the
mirror-parity/harness-gate dry run.

The Step 3 local review gate (5 always-on personas + Security Reviewer +
Template Integrity Reviewer) found 15 findings that failed the P-021 C1
same-contract-surface test and were captured as deferred stash entries; the
P0/P1 findings it did surface (a symlink-traversal fail-closed gap and an
`OSError` fail-closed gap) were fixed in-cycle.

The Step 4 P-018 Copilot-review gate then ran 6 distinct rounds (each
re-arming on every push, per its documented re-review behavior), surfacing
24 total Copilot threads, all genuine, narrowly-scoped, in-cycle-fixable
findings except one (a TOCTOU/symlink-race hardening request that would
require a new descriptor-relative traversal capability -- correctly deferred
per P-021 C1 as stash `92FC85DD`, since it is a new capability rather than a
same-contract-surface completion, and reusing `bootstrap_grant.py`'s private
helpers directly would additionally require a new cross-module public API
surface already separately deferred as out of scope by stash `815830AB`):

* Round 1 (10 threads): a fail-closed fix for a missing/non-string declared
  `id` in `_scan_backlog`; the `required_ids(S)` formula reconciled across
  policy, skill, and docstring; baseline-fingerprint capture/verification
  added to the Cascade Close Sub-Procedure; generic-template-purity fixes;
  the Ship-agent Step 5 closure-summary rewrite; a stale retrospective
  headline correction.
* Round 2 (3 threads): directory-symlink/junction containment guard for
  `queue`/`archive` (not only leaf files); a stale module-docstring
  correction; a PR readiness-block staleness fix.
* Round 3 (1 thread): the decision document's own `required_ids(S)` table
  (predating the shipped 155-S rule) reconciled via a REVISION 5 banner.
* Round 4 (2 threads): the TOCTOU/symlink-race finding deferred per above;
  one readiness-staleness comment already moot by the time it was checked.
* Round 5 (2 threads): a genuine `.tmpl`-mirror gap -- the round-1
  baseline-fingerprint capture/verification content had not actually been
  mirrored into `SKILL.md.tmpl` despite the PR description's claim -- fixed,
  with a new doc-contract test added to prevent recurrence; one further
  readiness-staleness comment already moot.
* Round 6 (3 threads): `_scan_backlog`'s backlog-wide id read was validated
  only for non-emptiness, unlike manifest-item resolution's
  `_ARTIFACT_ID_PATTERN` shape check, so a malformed/path-traversal-shaped id
  could still authorize `CASCADE` -- fixed with a new regression test; the
  Cascade Close Sub-Procedure's pre-archived-member preamble (both
  `SKILL.md` and its `.tmpl` mirror) still claimed a `CASCADE`-qualifying
  manifest "has no protected set by construction", contradicted by the
  round-1 baseline-fingerprint safeguard -- reworded to state the safeguard
  explicitly, with the doc-contract test extended.

This 6-round cycle exceeded the nominal 3-cycle review-fix circuit breaker
in the Ship agent's Stop Conditions table; each round surfaced a new,
genuine, narrowly-scoped finding that converged to fix rather than an
unresolved ping-pong on the same finding, so review-fix work continued
through round 6 (flagged in the PR body for operator visibility) rather than
halting mid-cycle with a P-018-blocked, non-mergeable PR. Round 7 returned
`SATISFIED` with zero new threads.

**Operator-authorized extension (rounds 7-12).** The operator explicitly
directed continued autonomous completion beyond the nominal 3-cycle limit
for any further genuine same-contract-surface finding (P-021 C1). Rounds
7-11 surfaced additional genuine findings, all fixed in-cycle except one
requiring a deliberation-artifact edit outside Ship's Role Boundary
(deferred, stash `7667738E`):

* Round 7 (1 thread): the P-015 Required Check/Violation Action paragraphs
  used a single universal `closure_scope(S)` baseline-invariance scope
  regardless of close path, forcing a fail-closed HALT even on a legitimate
  `CASCADE` archival of a `validated_linked_deliberations(S)` member --
  made path-specific (`closure_scope(S)` for safe-close, `allowed_ids(S)`
  for `CASCADE`).
* Round 8 (1 thread): INV-6's own statement of the engine-inertness
  containment gate was broader than the round-7 path-specific rule --
  scoped precisely to the classifier's `parent_id` descendant set, exposed
  as the new `ClosePathDecision.out_of_manifest_descendant_ids` field
  (classifier-first TDD), and explicitly excluded from
  `validated_linked_deliberations(S)`.
* Round 9 (4 threads): stale "Step 4"/"Step 5" cross-reference corrections
  in the Ship agent doc and `shipment-reconcile` skill (doc-only, no code
  change).
* Round 10 (5 threads): a stale Step 0(b)/Step 0(c) cross-reference for the
  linked-deliberation admission condition; `SKILL.md`'s own
  INV-1..INV-11 vocabulary summary diverged from the policy's actual
  numbering -- both corrected. One finding (the deliberation document's own
  stale INV-6 restatement) requires editing
  `docs/decisions/2026-09-15-flat-manifest-shipment-closure-deliberation.md`,
  which is outside Ship's Role Boundary -- deferred as stash `7667738E`
  rather than fixed directly.
* Round 11 (6 threads): the Step 5 SAFE_CLOSE closure-tasks bullet
  conflated per-item archival with the shipment-record-closing sequence
  (rewritten to separate them); `topology._frontmatter()` failed to catch
  `UnicodeDecodeError` on malformed-UTF-8 backlog files, crashing the
  classifier instead of failing closed (fixed via TDD, classifier suite now
  53 tests); the 1.25.0 Amendment Log row's claim that the 1.19.0-1.24.0
  rows were never edited was false -- 1.24.0's wording had been normalized
  in round 6 to remove a withdrawn term this file's own tests ban, and the
  1.25.0 row now states this accurately; and this closure evidence and the
  paired runtime-verification evidence were both stale and refreshed.
* Round 12 (3 threads): the Cascade Close Sub-Procedure's
  baseline-fingerprint capture did not itself revalidate that Step 0(c)'s
  `CASCADE` classification still held immediately before invocation (a
  TOCTOU gap) -- a pre-invocation classifier revalidation step was added,
  requiring an identical verdict/qualifying-feature-set/descendant-set to
  Step 0(c)'s, halting without invoking either close path on any drift;
  this closure artifact's own `last_code_affecting_head` anchor and stale
  evidence were refreshed again to the current HEAD.
* Round 13 (5 threads, this round): the round-12 pre-invocation
  revalidation re-ran only `classify_shipment_close_path`, which
  intentionally never inspects `validated_linked_deliberations(S)` -- so a
  linked deliberation gained or changed by a qualifying feature after Step
  0(c) could leave every classifier-compared value identical while still
  letting the engine reach a not-yet-authorized artifact; the
  revalidation step was extended to also independently re-collect and
  compare each qualifying feature's linked-deliberation snapshot (IDs,
  locations, declared statuses) against Step 0(c)'s. A second finding: the
  Cascade sub-procedure's own "protected set... has no pre-archived
  exemption" sentence was stale and self-contradicting -- Safe-Close Mode
  step 3 already tolerates an already-archived observation-set member as
  baseline state (not a halt), so the sentence was rewritten to state the
  actually-governing rule (observation-set members remain
  baseline-invariance-protected for the closure's duration regardless of
  whether their baseline happens to be `archive` or `queue`) instead of the
  withdrawn absolute claim. A third finding: the paired
  runtime-verification evidence still stated a stale "2342 tests" figure
  from a round-11 arithmetic slip, inconsistent with this file's own
  (correct) "2344 tests" figure -- corrected to 2344 in both files.

One pre-existing test (`test_two_writers_interleaved_seal_preserve_every_distinct_segment`
in `tests/test_telemetry_jsonl_sink.py`, zero diff in that file across this
PR's range) failed once during a push's pre-push hook test run, then passed
clean both on 4 isolated re-runs and on the immediate retry -- a known-flaky,
timing-sensitive two-writer concurrency test, not a regression introduced by
this shipment. A second, unrelated flaky test
(`tests/test_graphtor_mcp_shim.py::GraphtorMcpShimHandshakeTests::test_child_stdin_write_error_fails_requests_without_crashing`)
was similarly observed to fail once under full-suite load/timing and pass
in isolation during the round 7-11 extension; neither is a regression
introduced by this shipment.

## Invariants to Preserve

* `classify_shipment_close_path` must continue to fail closed to
  `SAFE_CLOSE` on any missing, non-string, or malformed/unsafe-shaped
  declared `id`; any torn/duplicate/ambiguous backlog record; any
  symlinked/reparse-point leaf file or directory component
  (`backlog_dir`/`queue`/`archive`); and any non-exact `status: archived`
  match (no `.lower()`, `.strip()`, `casefold`, alias, or `str()` coercion).
* The Cascade Close Sub-Procedure's `returned_ids`/`allowed_ids`/
  `required_ids` two-set gate, `parent_id`-preservation check, and INV-7/
  INV-10 baseline-fingerprint capture-and-verify pair must all continue to
  halt fail-closed with a P-005 violation on any postcondition mismatch.
* `.github/skills/shipment-reconcile/SKILL.md` and
  `templates/skills/shipment-reconcile/SKILL.md.tmpl` must remain mirror-
  consistent for the Cascade Close Sub-Procedure's baseline-fingerprint
  content (enforced by
  `test_cascade_procedure_defines_baseline_fingerprint_invariance_check`).
* No `{{VARIABLE}}` placeholder may remain unresolved in any installed
  mirror touched by this shipment.

## Validator Evidence

See
`docs/closure/2026-09-16-174-s-flat-manifest-shipment-closure-runtime-verification.md`
for the full structured probe/build/CI evidence anchored to
`last_code_affecting_head`. Summary: **PASS**. The only surface materially
touched is a Python module reachable through the `shipment-reconcile` skill
and Ship's own Step 5 closure invocation, not a new CLI subcommand -- the
CLI-help smoke probe (the workspace profile's only required surface)
confirmed the CLI starts without import/packaging/option-parsing failure.
Full local test suite green (2344 tests, `OK (skipped=54)`) re-verified
after every P-018 review-fix round through round 13; CI green on
`ubuntu-latest` at the final reviewed HEAD. Live current-HEAD CI/gate state
is tracked externally per the Evidence Currency Model above and was
re-verified directly against GitHub immediately before this artifact was
written.

## Releasability Evidence (pre-merge snapshot)

* `runtime_validation.releasability.required`: `false` for this workspace
  profile (per `.autoharness/workspace-profile.yaml`).
* No monitoring, rollback, or observation-window requirements apply beyond
  the standard post-merge CLI re-verification and the shipment's own
  post-merge `shipment-reconcile` closure exercise (which will directly
  exercise the rewritten classifier live, against this shipment's own
  manifest).
* Verdict (**pre-merge snapshot only**): **READY** on the merits -- all
  technical gates satisfied (local review, full local build, CI, P-018
  Copilot-review `SATISFIED`, P-009 merge-commit-only strategy confirmed).
  Overall closure status is **READY_WITH_CONDITIONS** solely because
  explicit operator merge approval has not yet been requested/received (see
  `conditions` in the frontmatter above).

## Source Artifact Cleanup

Checked via `backlogit get` on feature `166-F` and shipment `174-S`: neither
declares a `custom_fields.source_stash_id` or
`custom_fields.source_deliberation_id` (166-F's `custom_fields` carries only
`harness_status: pending`; 174-S's carries only the manifest `items` list).
This section records `none` for both. Note: this shipment does **not**
absorb stash entry `14F4D6F3` (a high-priority bug report captured from the
explicit-branch-contract report, carried forward per explicit operator
instruction as deferred intake) -- it remains untouched, unclaimed, and
unresolved by this PR, exactly as directed.

## Follow-Up Items

16 out-of-scope findings deferred per P-021 across Step 3 local review and
the Step 4 P-018 Copilot-review gate rounds 1-6, all tracked for Stage
deliberation: `AD0F128D`, `25B0D5F2`, `2E31C659`, `4E4C54DE`, `429E3F7F`,
`815830AB`, `A0FAE77A`, `CC2E9329`, `85BFB54C`, `9D8C4949`, `AF0CC40E`,
`46A985E5`, `F6330460`, `25E10837`, `AD01B943`, `92FC85DD`. One further
finding deferred during the round 7-12 operator-authorized extension,
`7667738E` (a genuine same-contract-surface finding whose fix requires
editing a deliberation artifact, outside Ship's Role Boundary), brings the
total to 17. `92FC85DD` (the TOCTOU/symlink-race hardening finding) and
`815830AB` (the cross-module import-coupling finding it references) are the
two entries most likely to warrant follow-on shipment scope; the rest are
documentation-convention or incidental pre-existing-defect notes.

## Compaction Status (P-020)

`pending` -- will be finalized to `done` (or `degraded`) by Ship post-merge
closure after `compact-context --target all` runs, per protocol. Do not
treat this artifact's `pending` value as an open blocker on the merge
decision itself; it blocks only Orchestrator next-shipment routing (P-001 +
P-020) until finalized.

## Merge Readiness Gate Status

Per the Evidence Currency Model above, this section deliberately does not
hardcode a specific "current" HEAD as a permanent fact beyond what was true
at the moment of writing. At the point this artifact was written, the live
state was: local review readiness `READY_WITH_FOLLOWUPS` with P0=0/P1=0 at
reviewed HEAD `00b96730`; the P-018 `autoharness gate copilot-review`
verdict `SATISFIED` with zero unresolved Copilot threads; all 4 required CI
checks (`test`, `ci gate`, `pipeline-topology (ambient)`,
`detect code changes`) green; PR mergeability `mergeable: MERGEABLE`,
`mergeStateStatus: CLEAN`. These must be re-verified fresh at the actual
merge gate, not read out of this file.

* P-009 merge-commit-only strategy: confirmed -- repository settings
  `allow_merge_commit: true`, `allow_squash_merge: false`,
  `allow_rebase_merge: false` (squash/rebase unavailable at the repository
  level). Merge will be executed via `gh pr merge 454 --merge`.
* Explicit operator approval: **not yet received** -- pending as of this
  artifact's writing. No admin fallback is authorized or anticipated to be
  needed; the normal merge path is expected to succeed on the first attempt
  given the clean mergeability state above.
