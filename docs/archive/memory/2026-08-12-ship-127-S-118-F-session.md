---
shipment: 127-S
feature: 118-F
tasks: [118.001-T, 118.002-T, 118.003-T, 118.004-T, 118.005-T, 118.006-T, 118.007-T]
pr: 326
merge_commit: 8ccd3a2ded777393703136da6747e846619f4294
session_date: 2026-08-12
---

# Ship session memory — 127-S / 118-F (S1 Copilot supervisor: safety contracts + characterization baseline)

## Scope

Shipment `127-S` implemented Shipment 1 of the 3-shipment Plan-1 chain
(`127-S -> 128-S -> 129-S`, superseding retired `124-S`/`125-S`/`126-S`).
Covering feature `118-F` (root, no parent) has exactly 7 children, all of
which are this shipment's manifest — a "fully-covered root" topology. Scope
was safety contracts + characterization baseline only, with an explicit
**zero-observable-behavior-change** boundary: no runtime wiring, pure
additions plus characterization tests pinning today's `start.ps1`/`start.sh`
contract.

## What was built (test-first, all 7 tasks)

- `118.001-T` / `118.002-T`: characterization test suites pinning the exact
  current behavior of `start.ps1` (Windows) and `start.sh` (POSIX) —
  including documented *absences* (no `ENGRAM_DATA_DIR` default, no
  `GITHUB_PERSONAL_ACCESS_TOKEN`, no `--remote` logic, no sidecars on POSIX)
  as approved baseline evidence for later migration deltas, not permanent
  mandates.
- `118.003-T`: `src/autoharness/supervise/{result,errors,contracts}.py` — a
  typed `SupervisorResult` envelope, an `AutoharnessError` taxonomy with a
  single machine-readable exit-code contract table, and the frozen,
  exhaustive gated-action catalog (`session_restart`, `force_unlock`) with a
  two-variant fallback-policy tagged union (`USE_SAFE_DEFAULT` / `REFUSE`).
- `118.004-T`: `src/autoharness/supervise/redact.py` — the single
  secret-redaction choke point.
- `118.005-T` / `118.006-T`: `src/autoharness/supervise/locking.py` — a
  stable, never-deleted OS-locked guard file (`msvcrt.locking` /
  `fcntl.flock`) with holder metadata in a separate removable record file;
  `force_unlock`/stale-cleanup acquire the *same* guard lock before
  reading/replacing/removing metadata; a live holder blocks cleanup, proven
  by real >=8-contender parallel race tests.
- `118.007-T`: `src/autoharness/gates/shipment_closure.py` — the P-015
  verified fully-covered-root close-path classifier
  (`classify_shipment_close_path`), plus coherent amendment of P-015 in
  `templates/policies/workflow-policies.md.tmpl`, the Ship agent template,
  and `docs/compound/097-S-shipment-task-only-safe-close.md`.

## Review cycle (the bulk of this session's effort)

- **Local report-only review**: 2 findings, both fixed with regression
  tests. 0 of 3 available review-fix cycles used.
- **Fix-CI**: 3 cycles — pytest->unittest conversion (CI's `test` job runs
  bare `python -m unittest discover`, no pytest installed), a *real*
  Windows `_windows_pid_exists` liveness bug fix in `locking.py`, and a
  Windows-only test-suite platform-gating fix.
- **Hosted Copilot review, 4 rounds, 20 threads total** (16 initial + 4
  follow-on): genuine race conditions in `locking.py`'s
  acquire/release/force_unlock ordering, multiple fail-closed gaps in
  `redact.py` (unsupported types, non-string keys, exception-message/
  exception-class-name leak vectors), and multiple fail-closed gaps in
  `shipment_closure.py`'s destructive classifier (frontmatter-id
  verification, malformed `parent_id` ambiguity, filename-stem trust
  fallback, symlink-following). 3 of 3 available Copilot review-fix cycles
  used; round 4's single finding (symlink-following, same risk class as an
  already-accepted tradeoff) was correctly deferred as a P2 follow-up per
  the exhausted-circuit-breaker protocol rather than pushed as a 4th fix.
  See `docs/compound/2026-08-12-hosted-review-catches-fail-closed-gaps-local-review-and-ci-miss.md`.
- Self-caught a latent bug I introduced in my own Round-1 fix (a
  `BacklogUnavailableError` call missing the required `path` positional
  arg) before it was ever flagged, by re-reading the full diff before the
  Round-2 push.
- P-018 copilot-review gate: `SATISFIED` at final HEAD `3c0c2836`, 0
  unresolved threads.

## Merge

PR #326 merged `2026-08-13T02:14:36Z` via `gh pr merge --merge` (merge
commit only, P-009 verified both by repo config and structurally — 2
parents). Merge commit `8ccd3a2ded777393703136da6747e846619f4294`.

## Close-path decision correction (important)

Initial planning (across a context-compaction boundary) concluded
"safe-close" based on a partial reading of the Ship agent's P-015 section
that captured the unconditional prohibition but missed the conditional
"verified fully-covered-root exception" clause immediately following it.
Re-reading the full current section before acting surfaced the exception;
running `classify_shipment_close_path` against the real manifest returned
`CASCADE`, matching both the shipment's own Stage-authored manifest
description and the operator's brief (both of which named
`backlogit shipment ship 127-S` as the expected close command). See
`docs/compound/2026-08-12-close-path-decisions-must-use-the-classifier-not-summarized-prose.md`.

## Close mechanics

- Dynamic engine attestation: CLI `v1.9.0` / `39528a4` / build
  `2026-08-12T03:49:03Z`. A stale MCP daemon process (PID 45252) was still
  serving the pre-upgrade `v1.8.0-dirty`/`fd8d2c9d` binary (Windows showed
  its image as `backlogit.exe.old`) — stopped via
  `Stop-Process -Id 45252 -Force` (PID-specific, never by name). See
  `docs/compound/2026-08-12-windows-stale-process-exe-old-detection.md`.
  This session never used any MCP tool function directly (no `backlogit_*`
  functions exposed in this agent's toolset) — all mutations went through
  the CLI, so CLI/MCP staleness never affected an actual mutation; the
  restart was pure operator-mandated hygiene.
- `docs/spikes/2026-08-09-plan1-shipment-topology-proof/sim-shipment-closure.ps1`
  re-run against the attested engine: **66/66 assertions passed**.
- Lifecycle topology gate re-run with the narrow pre-authorized `--force`
  override: sole token `PREDECESSOR_NOT_SHIPPED` for archived, superseded
  `126-S` — exit 0 forced, audit logged.
- `backlogit shipment ship 127-S --sha 8ccd3a2d...` executed. Result:
  `returned_ids: []`, `archived_ids` = all 7 tasks + `118-F` + `127-S` —
  exactly matching the manifest's own predicted outcome. Verified no
  `parent_id` cleared on any artifact and no artifact outside the 9-item
  manifest touched (`git status --short` showed exactly those 9 files).

## Follow-ups (non-blocking)

1. P2: symlink-following in `shipment_closure.py`'s backlog lookup path
   (round-4 Copilot finding, deferred).
2. P2 (pre-existing, re-confirmed not newly introduced): symlink-
   containment tradeoff in `locking.py:106`.
3. Informational: the `shipment-reconcile` skill template has not yet been
   wired to call `classify_shipment_close_path` automatically — Ship
   currently must invoke the classifier manually as documented in the
   agent instructions' item "e". Explicitly out of scope for this
   zero-runtime-wiring slice.
4. Informational: a Copilot test-coverage-enhancement suggestion (not a
   defect).

## Explicitly NOT done (out of scope, per operator brief)

- Did not claim, plan, or touch `128-S`/`129-S`.
- No runtime wiring of the new supervisor modules into any executable path.
