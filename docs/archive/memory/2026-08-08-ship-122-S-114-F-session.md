---
shipment: 122-S
feature: 114-F
tasks: [114.001-T, 114.002-T, 114.003-T]
session_role: ship
date: 2026-08-08
---

# Ship Session Memory — 122-S / 114-F (Capability-Pack Runtime Detection + Pre-Merge-Install Checklist)

## Summary

Final shipment in the `120-S -> 121-S -> 122-S` dark-mode sequence, resumed
from a prior pre-claim halt. Completed end-to-end: claim -> branch ->
implementation (per-pack presence+version detection for `backlogit`,
`engram`, `graphtor-docs` across `deploy-harness.ps1`/`.sh` and their
`.tmpl` counterparts, plus a pre-merge-install checklist report and docs) ->
full test suite -> local review -> PR #318 -> two rounds of Copilot review
(round 1: 3 valid findings, all fixed; round 2: clean) -> P-018 gate
`SATISFIED` -> dark-mode merge authorization -> merge commit `d923820e` ->
post-merge closure (backlog safe-close of both `122-S` and fully-covered
`114-F`, compound learning, this memory, closure doc, P-020 compaction).

## Checkpoint Anomaly Disposition (carried forward, not re-litigated)

Per the operator's explicit, expanded disposition this session: the four
files `checkpoint-20260802-192655.json`, `checkpoint-20260802-045420.json`,
`checkpoint-20260725-233536.json`, `checkpoint-20260726-005804.json` were
already fully addressed in a prior session (before this session started).
Read-only re-inspection this session (`Get-Content ... | Select-String
'"status"'`) confirmed the exact shape the operator described: the two
08-02 files have no readable `"status"` field via this simple grep
(malformed/schema-mismatched — consistent with "malformed for 08-02
records"), and the two 07-25/07-26 files (093-S) show `"status":"active"`
in their raw JSON (093-S is itself long since shipped/archived — "active"
here is the stale-committed-baseline artifact, not a live recovery
candidate). None of the four were touched, restored, resolved, or repaired
this session. Stash `ab16544a1636651d2368825d08cbd5e7c26ec755` was
re-verified present at `stash@{0}`, byte-identical, via `git rev-parse
"stash@{0}"` both before this session's work and again at closure — never
applied, popped, or dropped.

## Key Decisions

1. Performed the local Review Gate directly (not delegated) — single-domain
   diff, modest size, well within direct-review scope.
2. Fixed the sh exit-status-masking bug (see the new compound-learning doc)
   rather than accepting it as a follow-up — it was a real, Copilot-caught
   correctness bug in the exact detection logic this shipment introduces.
3. Strengthened tests with two new fixture-driven pack-detection test
   classes (ps1 + sh) rather than a narrow one-line assertion patch, since
   the root cause was "the test never exercised the reproducing shape."
4. Followed the 121-S/119-S precedent exactly for the fully-covered-feature
   closure pattern: shipment safe-close never touches the covering feature;
   `114-F` was closed as a distinct, subsequent step only after
   independently verifying zero `114.*` descendants remain in the live
   queue.

## Files Modified

- `scripts/deploy-harness.sh`, `templates/scripts/deploy-harness.sh.tmpl` —
  exit-status masking fix (commit `a1a9b9b3`).
- `tests/test_deploy_harness_scripts.py` — new fixture helpers + two new
  test classes (commit `a1a9b9b3`).
- `.backlogit/` state — task/feature/shipment lifecycle commits (`b255143d`
  pre-merge, `3dfe7ab4` post-merge safe-close).
- `scripts/deploy-harness.ps1`, `templates/scripts/deploy-harness.ps1.tmpl`,
  `docs/installation.md` — original feature implementation (commits
  `c717b67d`, `498b5c6a`, prior turn).

## Outcomes

- PR #318 merged via merge commit `d923820e29473cb24e0c4c7d76070b4d811d55a5`
  (two parents: `9a3dc6a2` main tip + `b255143d` feature HEAD — P-009
  preserved).
- `122-S`: `active -> shipped -> archived` (`archived_status: shipped`).
- `114-F`: `active -> done -> archived` (`archived_status: done`), fully
  covered by its 3 manifest tasks, zero unshipped siblings.
- `114.001-T`/`114.002-T`/`114.003-T`: pre-archived (`status: done`) from
  the pre-merge task loop.

## Deferred / Follow-Up

- Full native capability-pack runtime provisioning/install execution and
  supply-chain/OS-matrix design remain deferred per
  `docs/decisions/2026-08-07-capability-pack-runtime-installer-deliberation.md`
  Option C — tracked by the still-**ACTIVE** living tracker
  `.backlogit/stash.jsonl` entry `47971057` (`PARTIALLY-CONSUMED — LIVING
  TRACKER`), unchanged by this shipment. `114-F`'s `"47971057"` label is
  provenance only (which feature harvested the bounded increment); the
  actual resumption pointer for a future session is the stash entry
  itself, not the archived feature's label.
- Closure PR (post-merge branch) — see closure doc for status at time of
  writing.
