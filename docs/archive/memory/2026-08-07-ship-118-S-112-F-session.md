---
shipment: 118-S
feature: 112-F
session_date: 2026-08-07
agent: Ship
route: claude-sonnet-5/anthropic/high
mode: bounded-P-017-dark-contract
---

# Ship Session — 118-S / 112-F (mixed-role detection, report-only)

## Scope

Second shipment in serial chain `117-S → 118-S → 119-S` (spike `001-SP` /
stash `33CC445C`; `112-F` traces provenance to `936C68F3` part 2, `013-DL`).
Added `mode: detect-mixed-role` to `templates/skills/shipment-reconcile/
SKILL.md.tmpl`: a strictly READ-ONLY DETECTION + REPORT-ONLY diagnostic for
the queued-with-active-work / mixed-role shipment-status inconsistency.
No mutation, no `ClaimShipment` re-claim, no status write — true
record-only forward auto-repair remains explicitly DEFERRED as UNSUPPORTED
by backlogit 1.8.0.

## What happened

- Executed the full task loop (112.001-T, 112.004-T, 112.002-T, 112.003-T):
  classification tables, protocol, audit+telemetry, operator-remediation
  guidance, 33 regression tests, compound-learning doc + 10 tests.
- Local review: 1 P2 (blocked-status framing reconciliation) fixed in one
  cycle; introduced then self-corrected a forward-reference anchor
  collision in the same fix.
- PR #308: 3 genuine Copilot findings (invalid task-status-as-shipment-
  terminal-status conflation; invalid `tool_surface: "skill"` enum value;
  self-contradictory "nothing is ever written" framing) all fixed in one
  commit (`c3181cb`), replied+resolved individually via GraphQL.
- P-018/§1.9 gates SATISFIED/PASS at final HEAD `6af77b1`. Merged via
  `--merge` (P-009 merge-commit strategy) at `f4f517c6` — 2 parents
  confirmed.
- Post-merge safe-close (this session, branch
  `post-merge/118-s-mixed-role-detection-112-f`): discovered the 6th
  recorded occurrence of the "`move --status done` != explicit archive"
  gap (see compound doc), this time affecting **both** the 4 manifest
  tasks **and** the covering feature `112-F` (closed early, during the
  task loop, before the feature PR even merged — a new wrinkle vs. prior
  occurrences). Verified via the artifact's own event log (no cascade-op
  event) and via exhaustive sibling enumeration (zero `112.*` outside the
  manifest) that this was a legitimate early single-artifact close, not a
  cascade. Explicitly archived all 4 tasks, closed+archived the shipment
  record, then explicitly archived `112-F` as a deliberate final step.
  936C68F3 re-verified present/ACTIVE both before and after; 013-DL
  re-confirmed already archived (cleanup-safe no-op).

## Key learnings (see compound docs for durable form)

1. `docs/compound/2026-08-02-backlogit-done-move-vs-explicit-archive.md`
   — 6th occurrence added: a protected-set member found in `archive/`
   with `status: done` (not `archived`) during the safe-close baseline
   gate is not automatically a cascade; check the event log for absence
   of a cascade-op event and verify full-child-coverage before treating
   an early feature close as legitimate.
2. `docs/compound/2026-08-06-shipment-mixed-role-detection-report-only.md`
   — the report-only detection capability itself (from the task work).

## Provenance chain preserved

- `936C68F3` (stash tracker, part 2, living/ACTIVE) → `013-DL`
  (deliberation, archived) → `112-F` (feature, archived, `archived_status:
  done`) → `118-S` (shipment, archived, `archived_status: shipped`) → PR
  #308 (merged, `f4f517c6`).
- `119-S` explicitly NOT claimed. Its `pre_claim` topology gate verified
  to pass without claiming, per operator scope constraint.
